import React from "react";
import "./artefato.css";

/* ---------------- helpers ---------------- */
const uid = () => Math.random().toString(36).slice(2, 9);
const money = v => num(v).toLocaleString('pt-BR', {
  style: 'currency',
  currency: 'BRL'
});
/* ---------------- data e hora ----------------
   Um formato só no sistema inteiro: dia/mês/ano e hora:minuto:segundo.
   O armazenamento continua em ISO (2026-09-07T14:30:00) porque é o que
   ordena e compara certo; estas funções são só para a tela. */
const doisDig = n => String(n).padStart(2, '0');

/* hora com segundos: 14:30:05 */
const fmtHora = iso => {
  if (!iso) return '—';
  /* aceita tanto ISO completo quanto "HH:MM" solto */
  const curta = /^\d{2}:\d{2}(:\d{2})?$/.exec(String(iso));
  if (curta) return curta[1] ? String(iso) : String(iso) + ':00';
  const d = new Date(iso);
  if (isNaN(d.getTime())) return '—';
  return `${doisDig(d.getHours())}:${doisDig(d.getMinutes())}:` +
    `${doisDig(d.getSeconds())}`;
};

/* hora sem segundos, para campos de digitação e grades apertadas */
const fmtHoraCurta = iso => {
  const h = fmtHora(iso);
  return h === '—' ? h : h.slice(0, 5);
};

/* data: 07/09/2026 */
const fmtDate = iso => {
  if (!iso) return '—';
  const texto = String(iso);
  /* aceita ISO com hora junto */
  const soData = texto.length > 10 && texto.includes('T')
    ? texto.slice(0, 10) : texto;
  const [y, m, d] = soData.split('-');
  if (!y || !m || !d) return texto; /* não inventa formato para lixo */
  return `${d}/${m}/${y}`;
};

/* data e hora juntas: 07/09/2026 14:30:05 */
const fmtDataHora = iso => {
  if (!iso) return '—';
  const d = new Date(iso);
  if (isNaN(d.getTime())) return String(iso);
  return `${doisDig(d.getDate())}/${doisDig(d.getMonth() + 1)}/` +
    `${d.getFullYear()} ${doisDig(d.getHours())}:` +
    `${doisDig(d.getMinutes())}:${doisDig(d.getSeconds())}`;
};
/* ==========================================================
   BANCO — apenas os cadastros preservados na reestruturação
========================================================== */
const emptyDb = () => ({
  colaboradores: [],
  clientes: [],
  fornecedores: [],
  logs: [],
  permissoes: null,
  perfisPorCargo: {},
  niveisCustom: {},
  /* ---- Módulo de materiais ---- */
  materiais: [],
  // cadastro
  gruposMaterial: [],
  // grupo e subgrupo (hierarquia por paiId)
  unidades: [],
  // UN, KG, M…
  conversoes: [],
  // 1 rolo = 50 m
  estoques: [],
  // locais de armazenagem
  saldos: [],
  // material × estoque: físico, reservado, comprometido, trânsito
  movimentacoes: [],
  // fonte da verdade do saldo
  reservas: [],
  // material comprometido com uma origem
  centrosCusto: [],
  // preço, código e prazo de cada fornecedor
  parametrosMateriais: null,
  parametrosMaoDeObra: null,
  programacoes: [],             // a programação oficial de cada dia
  ocorrencias: [],              // o que interrompeu a produção e como foi resolvido
  custosFixos: [],              // aluguel, energia, manutenção — o custo da fábrica
  parametrosCustoIndireto: null,
  parametrosFila: null,          // espera entre setores, em horas
  /* ---- Corte ---- */
  riscos: [],
  // riscos lidos de arquivos PLT

  /* ---- Produto ---- */
  gruposProduto: [],
  tiposProduto: [],
  ordens: [],
  apontamentos: [],
  avaliacoes: [],
  produtos: [],
  subprodutos: [],
  // as partes que vieram do risco

  /* ---- Engenharia ---- */
  departamentos: [],
  etapas: [],
  equipamentos: [],
  jornada: null,
  versoesProduto: [],
  // engenharia congelada: a OP de ontem conta a verdade de ontem
  afericoes: [],
  // aferição de capacidade: prova, validação e análise
  metasOperacionais: [],
  // a meta que nasceu de uma aferição
  objetivosMelhoria: [],
  // onde se quer chegar — separado da meta
  conversas: [],
  mensagens: [],
  notas: [],
  vagas: [],
  // vagas abertas divulgadas no canal
  indicacoes: [],
  // quem o pessoal indicou para as vagas
  manifestacoes: [],
  // Conversa Aberta: sugestões, problemas, riscos, relatos
  pesquisasClima: [],
  // o termômetro rápido do dia
  configCanal: null,
  // URL base dos QR Codes
  seq: {}
});

/* ==========================================================
   MÓDULO DE MATERIAIS — modelo e motor de estoque

   Regra central: o saldo NUNCA é editado direto. Toda alteração
   nasce de uma movimentação, e o saldo materializado em `saldos`
   é apenas o acumulado — sempre reconstruível pela função
   recalcularSaldos(), que serve de auditoria da própria regra.
========================================================== */

const TIPOS_MOV = [{
  id: 'entrada_compra',
  nome: 'Entrada por compra',
  sinal: +1,
  exigeFornecedor: true,
  grupo: 'entrada'
}, {
  id: 'entrada_devolucao',
  nome: 'Devolução da produção',
  sinal: +1,
  grupo: 'entrada'
}, {
  id: 'entrada_ajuste',
  nome: 'Ajuste positivo',
  sinal: +1,
  exigeMotivo: true,
  grupo: 'ajuste'
}, {
  id: 'entrada_inventario',
  nome: 'Ajuste de inventário (+)',
  sinal: +1,
  grupo: 'ajuste'
}, {
  id: 'entrada_transf',
  nome: 'Transferência (entrada)',
  sinal: +1,
  grupo: 'transferencia'
}, {
  id: 'saida_producao',
  nome: 'Saída para produção',
  sinal: -1,
  grupo: 'saida'
}, {
  id: 'saida_setor',
  nome: 'Saída para setor',
  sinal: -1,
  grupo: 'saida'
}, {
  id: 'saida_devolucao',
  nome: 'Devolução ao fornecedor',
  sinal: -1,
  exigeFornecedor: true,
  exigeMotivo: true,
  grupo: 'saida'
}, {
  id: 'saida_perda',
  nome: 'Perda ou descarte',
  sinal: -1,
  exigeMotivo: true,
  grupo: 'saida'
}, {
  id: 'saida_ajuste',
  nome: 'Ajuste negativo',
  sinal: -1,
  exigeMotivo: true,
  grupo: 'ajuste'
}, {
  id: 'saida_inventario',
  nome: 'Ajuste de inventário (−)',
  sinal: -1,
  grupo: 'ajuste'
}, {
  id: 'saida_transf',
  nome: 'Transferência (saída)',
  sinal: -1,
  grupo: 'transferencia'
}];
function tipoMov(id) {
  return TIPOS_MOV.find(t => t.id === id) || null;
}
const MOTIVOS_DEVOLUCAO = ['Defeito', 'Quantidade incorreta', 'Cor incorreta', 'Material diferente', 'Problema de qualidade', 'Compra incorreta', 'Fora da validade', 'Outro'];

/* Unidades base do sistema, criadas na primeira carga */
const UNIDADES_PADRAO = [{
  sigla: 'UN',
  nome: 'Unidade',
  decimais: 0
}, {
  sigla: 'PC',
  nome: 'Peça',
  decimais: 0
}, {
  sigla: 'PAR',
  nome: 'Par',
  decimais: 0
}, {
  sigla: 'JG',
  nome: 'Jogo',
  decimais: 0
}, {
  sigla: 'CX',
  nome: 'Caixa',
  decimais: 0
}, {
  sigla: 'ROLO',
  nome: 'Rolo',
  decimais: 2
}, {
  sigla: 'FD',
  nome: 'Fardo',
  decimais: 2
}, {
  sigla: 'M',
  nome: 'Metro',
  decimais: 3
}, {
  sigla: 'CM',
  nome: 'Centímetro',
  decimais: 1
}, {
  sigla: 'M2',
  nome: 'Metro quadrado',
  decimais: 3
}, {
  sigla: 'KG',
  nome: 'Quilograma',
  decimais: 3
}, {
  sigla: 'G',
  nome: 'Grama',
  decimais: 1
}, {
  sigla: 'L',
  nome: 'Litro',
  decimais: 3
}, {
  sigla: 'ML',
  nome: 'Mililitro',
  decimais: 1
}];

/* Grupos e subgrupos iniciais — o usuário pode alterar tudo */
const GRUPOS_PADRAO = [['Tecidos', ['Malha', 'Tecido plano', 'Oxford', 'Brim', 'Microfibra', 'TNT', 'Forro']], ['Aviamentos', ['Linhas', 'Zíperes', 'Botões', 'Elásticos', 'Viés', 'Entretela', 'Velcro']], ['Estamparia', ['Tintas', 'Telas', 'Emulsão', 'DTF', 'Sublimação', 'Transfer']], ['Embalagens', ['Sacos', 'Caixas', 'Etiquetas', 'Tags', 'Fitas']], ['Consumo interno', ['EPI', 'Limpeza', 'Escritório', 'Manutenção']]];
const CENTROS_CUSTO_PADRAO = ['Corte', 'Costura', 'Estamparia', 'Acabamento', 'Embalagem', 'Manutenção', 'Administrativo', 'Comercial'];

/* ---------- conversão entre unidades ---------- */
/* Uma conversão diz: 1 <de> = fator <para>. Ex.: 1 ROLO = 50 M */
function converter(qtd, deSigla, paraSigla, db, materialId) {
  const q = num(qtd);
  if (!deSigla || !paraSigla || normaliza(deSigla) === normaliza(paraSigla)) return q;
  const lista = db && db.conversoes || [];
  // conversão específica do material tem prioridade sobre a geral
  const achar = (a, b) => lista.find(c => normaliza(c.de) === normaliza(a) && normaliza(c.para) === normaliza(b) && (materialId ? c.materialId === materialId || !c.materialId : !c.materialId));
  const direta = achar(deSigla, paraSigla);
  if (direta && num(direta.fator) > 0) return q * num(direta.fator);
  const inversa = achar(paraSigla, deSigla);
  if (inversa && num(inversa.fator) > 0) return q / num(inversa.fator);
  return null; // sem regra de conversão
}
function unidadeDecimais(sigla, db) {
  const u = (db && db.unidades || []).find(x => normaliza(x.sigla) === normaliza(sigla));
  return u ? num(u.decimais) : 3;
}
function fmtQtd(qtd, sigla, db) {
  const d = unidadeDecimais(sigla, db);
  return num(qtd).toLocaleString('pt-BR', {
    minimumFractionDigits: d,
    maximumFractionDigits: d
  }) + (sigla ? ' ' + sigla : '');
}

/* Custo do material: o custo médio nasce das compras, mas antes da primeira
   entrada ele é zero. Nesse caso vale o preço informado no cadastro, para que
   o produto possa ser custeado desde já. */
function custoDoMaterial(m) {
  if (!m) return 0;
  const medio = num(m.custoMedio);
  return medio > 0 ? medio : num(m.precoCompra);
}
function origemDoCusto(m) {
  if (!m) return null;
  if (num(m.custoMedio) > 0) return {
    valor: num(m.custoMedio),
    origem: 'compras',
    label: 'custo médio das compras'
  };
  if (num(m.precoCompra) > 0) return {
    valor: num(m.precoCompra),
    origem: 'cadastro',
    label: 'preço informado no cadastro'
  };
  return {
    valor: 0,
    origem: 'nenhum',
    label: 'sem custo informado'
  };
}

/* ---------- saldos ---------- */
function saldoDe(materialId, estoqueId, db) {
  const s = (db && db.saldos || []).find(x => x.materialId === materialId && x.estoqueId === estoqueId);
  return s || {
    materialId,
    estoqueId,
    fisico: 0,
    reservado: 0,
    comprometido: 0,
    transito: 0
  };
}
/* Soma de todos os locais */
function saldoTotal(materialId, db) {
  const linhas = (db && db.saldos || []).filter(x => x.materialId === materialId);
  const acc = {
    fisico: 0,
    reservado: 0,
    comprometido: 0,
    transito: 0
  };
  linhas.forEach(l => {
    acc.fisico += num(l.fisico);
    acc.reservado += num(l.reservado);
    acc.comprometido += num(l.comprometido);
    acc.transito += num(l.transito);
  });
  acc.disponivel = acc.fisico - acc.reservado - acc.comprometido;
  return acc;
}
function disponivelEm(materialId, estoqueId, db) {
  const s = saldoDe(materialId, estoqueId, db);
  return num(s.fisico) - num(s.reservado) - num(s.comprometido);
}

/* Situação do material, para cor e alerta */
function situacaoMaterial(mat, db) {
  const s = saldoTotal(mat.id, db);
  const min = num(mat.estoqueMinimo),
    max = num(mat.estoqueMaximo);
  const emCompra = s.transito > 0;
  if (s.fisico <= 0) return {
    id: 'zerado',
    label: 'Sem estoque',
    tone: 'bad',
    saldo: s
  };
  if (min > 0 && s.disponivel <= min) return {
    id: 'abaixo',
    label: 'Abaixo do mínimo',
    tone: 'bad',
    saldo: s
  };
  if (min > 0 && s.disponivel <= min * 1.2) return {
    id: 'proximo',
    label: 'Próximo do mínimo',
    tone: 'warn',
    saldo: s
  };
  if (max > 0 && s.fisico > max) return {
    id: 'excesso',
    label: 'Acima do máximo',
    tone: 'warn',
    saldo: s
  };
  if (emCompra) return {
    id: 'compra',
    label: 'Compra em andamento',
    tone: 'info',
    saldo: s
  };
  return {
    id: 'ok',
    label: 'Normal',
    tone: 'ok',
    saldo: s
  };
}

/* Dias sem movimentação */
function diasSemMovimento(materialId, db) {
  const movs = (db && db.movimentacoes || []).filter(m => m.materialId === materialId);
  if (movs.length === 0) return null;
  const ultima = movs.map(m => String(m.quando || '').slice(0, 10)).sort().pop();
  if (!ultima) return null;
  return Math.round((new Date(todayISO() + 'T00:00:00') - new Date(ultima + 'T00:00:00')) / 86400000);
}

/* ==========================================================
   aplicarMovimento — ÚNICO caminho que altera saldo.
   Devolve {erro} ou {movimento}. Nunca grava saldo por fora.
========================================================== */
function aplicarMovimento(d, dados, usuario) {
  const t = tipoMov(dados.tipo);
  if (!t) return {
    erro: 'Tipo de movimentação inválido.'
  };
  const mat = (d.materiais || []).find(m => m.id === dados.materialId);
  if (!mat) return {
    erro: 'Material não encontrado.'
  };
  const est = (d.estoques || []).find(e => e.id === dados.estoqueId);
  if (!est) return {
    erro: 'Selecione o local de estoque.'
  };
  const qtd = num(dados.quantidade);
  if (!(qtd > 0)) return {
    erro: 'A quantidade precisa ser maior que zero.'
  };
  if (t.exigeFornecedor && !dados.fornecedorId) return {
    erro: 'Informe o fornecedor desta movimentação.'
  };
  if (t.exigeMotivo && !String(dados.motivo || '').trim()) return {
    erro: 'Informe o motivo — ele fica registrado na auditoria.'
  };
  if (!usuario || !usuario.nome) return {
    erro: 'Movimentação sem usuário identificado.'
  };

  // saída não pode deixar o disponível negativo sem autorização expressa
  if (t.sinal < 0) {
    const disp = disponivelEm(dados.materialId, dados.estoqueId, d);
    if (qtd > disp && !dados.autorizadoNegativo) return {
      erro: `Disponível em ${est.nome}: ${fmtQtd(disp, mat.unidadeEstoque, d)}. ` + `A saída de ${fmtQtd(qtd, mat.unidadeEstoque, d)} deixaria o saldo negativo.`,
      podeAutorizar: true,
      disponivel: disp
    };
  }
  const custoUnit = dados.custoUnitario !== undefined && dados.custoUnitario !== '' ? num(dados.custoUnitario) : custoDoMaterial(mat);
  const mov = {
    id: uid(),
    numero: proximoNumero(d, 'movimentacao'),
    quando: agoraISO(),
    materialId: dados.materialId,
    estoqueId: dados.estoqueId,
    tipo: dados.tipo,
    sinal: t.sinal,
    quantidade: qtd,
    unidade: mat.unidadeEstoque || '',
    custoUnitario: custoUnit,
    custoTotal: custoUnit * qtd,
    loteId: dados.loteId || '',
    fornecedorId: dados.fornecedorId || '',
    centroCustoId: dados.centroCustoId || '',
    origemTipo: dados.origemTipo || '',
    // 'recebimento' | 'op' | 'inventario' | 'transferencia' | 'manual'
    origemId: dados.origemId || '',
    documento: dados.documento || '',
    motivo: dados.motivo || '',
    observacao: dados.observacao || '',
    usuario: usuario.nome,
    autorizadoNegativo: !!dados.autorizadoNegativo,
    estornoDe: dados.estornoDe || '',
    parId: dados.parId || '' // transferência: liga saída e entrada
  };
  d.movimentacoes = [...(d.movimentacoes || []), mov];
  somarSaldo(d, mov.materialId, mov.estoqueId, {
    fisico: t.sinal * qtd
  });

  // custo médio: só entradas com custo informado alteram a média
  if (t.sinal > 0 && custoUnit > 0 && t.grupo === 'entrada') atualizarCustoMedio(d, mat.id, qtd, custoUnit);

  // lote: acompanha o saldo do próprio lote
  if (mov.loteId) {
    d.lotes = (d.lotes || []).map(l => l.id === mov.loteId ? {
      ...l,
      quantidadeAtual: num(l.quantidadeAtual) + t.sinal * qtd
    } : l);
  }
  registrarLog(d, usuario, t.nome, `${mat.nome} · ${fmtQtd(qtd, mat.unidadeEstoque, d)} · ${est.nome}${mov.motivo ? ' · ' + mov.motivo : ''}`);
  return {
    movimento: mov
  };
}

/* Ajuste pontual de saldo — usado só por aplicarMovimento e pelas reservas */
function somarSaldo(d, materialId, estoqueId, delta) {
  const lista = d.saldos || [];
  const i = lista.findIndex(x => x.materialId === materialId && x.estoqueId === estoqueId);
  const atual = i >= 0 ? lista[i] : {
    id: uid(),
    materialId,
    estoqueId,
    fisico: 0,
    reservado: 0,
    comprometido: 0,
    transito: 0
  };
  const novo = {
    ...atual
  };
  Object.entries(delta).forEach(([k, v]) => {
    novo[k] = num(novo[k]) + num(v);
  });
  d.saldos = i >= 0 ? lista.map((x, j) => j === i ? novo : x) : [...lista, novo];
  return novo;
}

/* Custo médio ponderado */
function atualizarCustoMedio(d, materialId, qtdEntrada, custoEntrada) {
  d.materiais = (d.materiais || []).map(m => {
    if (m.id !== materialId) return m;
    const anterior = saldoTotal(materialId, d).fisico - num(qtdEntrada); // saldo antes da entrada
    const base = Math.max(anterior, 0);
    const medioAnterior = num(m.custoMedio);
    const novo = base + num(qtdEntrada) > 0 ? (base * medioAnterior + num(qtdEntrada) * num(custoEntrada)) / (base + num(qtdEntrada)) : num(custoEntrada);
    return {
      ...m,
      custoMedio: Number(novo.toFixed(6)),
      ultimoCusto: num(custoEntrada),
      menorCusto: m.menorCusto === undefined || num(m.menorCusto) === 0 ? num(custoEntrada) : Math.min(num(m.menorCusto), num(custoEntrada)),
      maiorCusto: Math.max(num(m.maiorCusto), num(custoEntrada)),
      ultimaCompra: todayISO()
    };
  });
}

/* Transferência: gera o par de movimentações vinculadas */
function transferir(d, dados, usuario) {
  if (dados.origemId === dados.destinoId) return {
    erro: 'Origem e destino precisam ser diferentes.'
  };
  const par = uid();
  const saida = aplicarMovimento(d, {
    materialId: dados.materialId,
    estoqueId: dados.origemId,
    tipo: 'saida_transf',
    quantidade: dados.quantidade,
    loteId: dados.loteId,
    motivo: dados.motivo,
    observacao: dados.observacao,
    origemTipo: 'transferencia',
    parId: par,
    autorizadoNegativo: dados.autorizadoNegativo
  }, usuario);
  if (saida.erro) return saida;
  const entrada = aplicarMovimento(d, {
    materialId: dados.materialId,
    estoqueId: dados.destinoId,
    tipo: 'entrada_transf',
    quantidade: dados.quantidade,
    loteId: dados.loteId,
    motivo: dados.motivo,
    observacao: dados.observacao,
    origemTipo: 'transferencia',
    parId: par,
    custoUnitario: saida.movimento.custoUnitario
  }, usuario);
  if (entrada.erro) {
    // desfaz a saída para não deixar o material no limbo
    d.movimentacoes = (d.movimentacoes || []).filter(m => m.id !== saida.movimento.id);
    somarSaldo(d, dados.materialId, dados.origemId, {
      fisico: num(dados.quantidade)
    });
    return entrada;
  }
  return {
    saida: saida.movimento,
    entrada: entrada.movimento,
    parId: par
  };
}

/* Estorno: nunca apaga, cria a contramovimentação */
function estornarMovimento(d, movId, usuario, motivo) {
  const mov = (d.movimentacoes || []).find(m => m.id === movId);
  if (!mov) return {
    erro: 'Movimentação não encontrada.'
  };
  if ((d.movimentacoes || []).some(m => m.estornoDe === movId)) return {
    erro: 'Esta movimentação já foi estornada.'
  };
  const t = tipoMov(mov.tipo);
  const inverso = t.sinal > 0 ? t.grupo === 'transferencia' ? 'saida_transf' : 'saida_ajuste' : t.grupo === 'transferencia' ? 'entrada_transf' : 'entrada_ajuste';
  return aplicarMovimento(d, {
    materialId: mov.materialId,
    estoqueId: mov.estoqueId,
    tipo: inverso,
    quantidade: mov.quantidade,
    custoUnitario: mov.custoUnitario,
    loteId: mov.loteId,
    motivo: motivo || `Estorno da movimentação ${mov.numero}`,
    origemTipo: 'estorno',
    origemId: mov.id,
    estornoDe: mov.id,
    autorizadoNegativo: true
  }, usuario);
}

/* Reconstrói todos os saldos a partir das movimentações — auditoria da regra */
function recalcularSaldos(d) {
  const antes = (d.saldos || []).map(s => ({
    ...s
  }));
  const mapa = {};
  (d.movimentacoes || []).forEach(m => {
    const k = m.materialId + '|' + m.estoqueId;
    if (!mapa[k]) mapa[k] = {
      id: uid(),
      materialId: m.materialId,
      estoqueId: m.estoqueId,
      fisico: 0,
      reservado: 0,
      comprometido: 0,
      transito: 0
    };
    mapa[k].fisico += num(m.sinal) * num(m.quantidade);
  });
  // reservas ativas voltam a compor o saldo
  (d.reservas || []).filter(r => r.status === 'Ativa').forEach(r => {
    const k = r.materialId + '|' + r.estoqueId;
    if (!mapa[k]) mapa[k] = {
      id: uid(),
      materialId: r.materialId,
      estoqueId: r.estoqueId,
      fisico: 0,
      reservado: 0,
      comprometido: 0,
      transito: 0
    };
    mapa[k].reservado += num(r.quantidade) - num(r.atendido);
  });
  d.saldos = Object.values(mapa);
  const divergencias = [];
  antes.forEach(a => {
    const novo = d.saldos.find(x => x.materialId === a.materialId && x.estoqueId === a.estoqueId);
    const fisicoNovo = novo ? num(novo.fisico) : 0;
    if (Math.abs(fisicoNovo - num(a.fisico)) > 0.0001) divergencias.push({
      materialId: a.materialId,
      estoqueId: a.estoqueId,
      antes: num(a.fisico),
      depois: fisicoNovo
    });
  });
  return divergencias;
}

/* ---------- reservas ---------- */
function reservar(d, dados, usuario) {
  const disp = disponivelEm(dados.materialId, dados.estoqueId, d);
  const qtd = num(dados.quantidade);
  if (!(qtd > 0)) return {
    erro: 'Quantidade inválida.'
  };
  if (qtd > disp) return {
    erro: `Disponível apenas ${fmtQtd(disp, '', d)} para reserva.`
  };
  const r = {
    id: uid(),
    numero: proximoNumero(d, 'reserva'),
    materialId: dados.materialId,
    estoqueId: dados.estoqueId,
    quantidade: qtd,
    atendido: 0,
    status: 'Ativa',
    origemTipo: dados.origemTipo || 'manual',
    origemId: dados.origemId || '',
    descricao: dados.descricao || '',
    quando: agoraISO(),
    usuario: usuario ? usuario.nome : ''
  };
  d.reservas = [...(d.reservas || []), r];
  somarSaldo(d, dados.materialId, dados.estoqueId, {
    reservado: qtd
  });
  registrarLog(d, usuario, 'Reservou material', `${fmtQtd(qtd, '', d)} · ${r.descricao || r.origemTipo}`);
  return {
    reserva: r
  };
}
function liberarReserva(d, reservaId, usuario) {
  const r = (d.reservas || []).find(x => x.id === reservaId);
  if (!r) return {
    erro: 'Reserva não encontrada.'
  };
  if (r.status !== 'Ativa') return {
    erro: 'Esta reserva já foi encerrada.'
  };
  const restante = num(r.quantidade) - num(r.atendido);
  d.reservas = d.reservas.map(x => x.id === reservaId ? {
    ...x,
    status: 'Liberada',
    encerradaEm: agoraISO()
  } : x);
  somarSaldo(d, r.materialId, r.estoqueId, {
    reservado: -restante
  });
  registrarLog(d, usuario, 'Liberou reserva', r.numero || '');
  return {
    ok: true
  };
}

/* Numeração sequencial por documento */
function proximoNumero(d, chave) {
  d.seq = d.seq || {};
  const atual = num(d.seq[chave]) > 0 ? num(d.seq[chave]) : 1;
  d.seq[chave] = atual + 1;
  const prefixos = {
    movimentacao: 'MOV',
    reserva: 'RES',
    requisicao: 'REQ',
    cotacao: 'COT',
    pedido: 'PC',
    recebimento: 'REC',
    inventario: 'INV',
    op: 'OP',
    lote: 'LT'
  };
  return `${prefixos[chave] || 'DOC'}-${String(atual).padStart(5, '0')}`;
}

/* Primeira carga do módulo: unidades, grupos, estoque e centros de custo */
function semearMateriais(d) {
  if ((d.unidades || []).length === 0) d.unidades = UNIDADES_PADRAO.map(u => ({
    id: uid(),
    ...u,
    ativo: true
  }));
  if ((d.conversoes || []).length === 0) d.conversoes = [{
    id: uid(),
    de: 'KG',
    para: 'G',
    fator: 1000,
    materialId: ''
  }, {
    id: uid(),
    de: 'L',
    para: 'ML',
    fator: 1000,
    materialId: ''
  }, {
    id: uid(),
    de: 'M',
    para: 'CM',
    fator: 100,
    materialId: ''
  }];
  if ((d.gruposMaterial || []).length === 0) {
    const grupos = [];
    GRUPOS_PADRAO.forEach(([nome, subs]) => {
      /* tecido é o que se corta no risco — os outros grupos não pedem risco */
      const cortavel = nome === 'Tecidos';
      const pai = {
        id: uid(),
        nome,
        paiId: '',
        ativo: true,
        cortavel
      };
      grupos.push(pai);
      subs.forEach(sn => grupos.push({
        id: uid(),
        nome: sn,
        paiId: pai.id,
        ativo: true,
        cortavel
      }));
    });
    d.gruposMaterial = grupos;
  }
  if ((d.estoques || []).length === 0) d.estoques = [{
    id: uid(),
    codigo: 'EST-01',
    nome: 'Estoque principal',
    localizacao: '',
    responsavel: '',
    tipo: 'Geral',
    ativo: true,
    padrao: true
  }];
  if ((d.centrosCusto || []).length === 0) d.centrosCusto = CENTROS_CUSTO_PADRAO.map((n, i) => ({
    id: uid(),
    codigo: `CC-${String(i + 1).padStart(2, '0')}`,
    nome: n,
    ativo: true
  }));
  if (!d.parametrosMateriais) d.parametrosMateriais = {
    alertaAumentoPreco: 10,
    // % de aumento que dispara alerta
    diasSemMovimento: 60,
    // material parado
    permitirNegativo: false,
    // exige autorização caso a caso
    metodoCusto: 'medio'
  };
  return d;
}

/* ==========================================================
   ENGENHARIA DE PRODUTO — modelo industrial

   Três conceitos que o sistema nunca confunde:
     tempo de operação  ≠  tempo de mão de obra   (grupo multiplica o segundo)
     tempo por lote     ≠  tempo por peça         (setup se dilui no lote)
     custo padrão       ≠  custo real             (ficha × produção efetiva)

   O roteiro é um GRAFO: uma operação tem N entradas e N saídas.
   Do corte saem corpo, bolso e fitas — cada um com seu caminho.
========================================================== */

/* Tipos de tempo — cada operação pode usar vários */

/* ==========================================================
   PCP — CADASTRO
   Processo é o que a fábrica sabe fazer. Etapa é esse processo
   aplicado a um produto, com tempo e sequência próprios.
========================================================== */

/* ==========================================================
   ÁRVORE DE PRODUTO — BOM multinível
   A camisa é feita de corpo, mangas e gola; o corpo é feito
   de tecido. A árvore aceita quantos níveis o produto tiver.
========================================================== */

/* ==========================================================
   MATERIAL POR ETAPA
   Saber que o produto usa 2 m de oxford é uma coisa; saber que
   esse oxford entra no corte, e a fita só na costura, é o que
   permite entregar material na bancada certa.
========================================================== */
function materiaisDaEtapa(passo, produto, db) {
  const ids = Array.isArray(passo.materiais) ? passo.materiais : [];
  if (ids.length === 0) return [];
  const c = custearProduto(produto, db);
  return ids.map(id => {
    const t = c.tecidos.find(x => x.id === id);
    if (!t) return null;
    return {
      tecidoId: t.id,
      material: t.material,
      nome: t.material ? t.material.nome : 'material removido',
      unidade: t.material ? t.material.unidadeEstoque : '',
      consumo: num(t.consumo),
      custo: num(t.custo),
      cortavel: t.cortavel
    };
  }).filter(Boolean);
}

/* ==========================================================
   LIMPEZA SELETIVA
   Apagar por bloco, respeitando quem depende de quem: produto
   sem ordem existe, ordem sem produto não. A ordem da lista é a
   ordem da dependência — o de cima precisa do de baixo.
========================================================== */
const BLOCOS_LIMPEZA = [{
  id: 'producao',
  nome: 'Produção',
  ajuda: 'ordens, apontamentos, avaliações e programações',
  /* A programação aponta para ordens e etapas: sem apagar junto,
     sobrariam dias programados para operações que não existem mais. */
  colecoes: ['ordens', 'apontamentos', 'avaliacoes', 'programacoes', 'ocorrencias'],
  precisa: []
}, {
  id: 'afericoes',
  nome: 'Aferição e metas',
  ajuda: 'aferições, metas validadas e assinaturas',
  /* Meta é sempre de uma etapa e de um produto. Apagar produtos sem
     apagar as metas deixava número validado apontando para operação
     que não existe — e o planejamento seguia usando esse número. */
  colecoes: ['afericoes', 'metasOperacionais'],
  precisa: []
}, {
  id: 'produtos',
  nome: 'Produtos',
  ajuda: 'produtos, subprodutos, modelagens e versões congeladas',
  colecoes: ['produtos', 'subprodutos', 'riscos', 'versoesProduto'],
  precisa: ['producao', 'afericoes']
}, {
  id: 'materiais',
  nome: 'Materiais e estoque',
  ajuda: 'materiais, movimentações, saldos, lotes e reservas',
  colecoes: ['materiais', 'movimentacoes', 'saldos', 'lotes', 'reservas'],
  precisa: ['produtos']
}, {
  id: 'cadastros',
  nome: 'Clientes e fornecedores',
  ajuda: 'as empresas com quem a fábrica negocia',
  colecoes: ['clientes', 'fornecedores'],
  precisa: []
}, {
  id: 'engenharia',
  nome: 'Engenharia',
  ajuda: 'departamentos, etapas e equipamentos',
  colecoes: ['departamentos', 'etapas', 'equipamentos'],
  precisa: ['produtos', 'afericoes']
}];
function contarBloco(bloco, db) {
  return bloco.colecoes.reduce((a, k) => a + (db[k] || []).length, 0);
}

/* Marcar um bloco arrasta quem depende dele: apagar materiais sem
   apagar produtos deixaria fichas apontando para o nada. */
function fecharDependencias(escolhidos) {
  const set = new Set(escolhidos);
  let mudou = true;
  while (mudou) {
    mudou = false;
    BLOCOS_LIMPEZA.forEach(b => {
      if (!set.has(b.id)) return;
      (b.precisa || []).forEach(dep => {
        if (!set.has(dep)) {
          set.add(dep);
          mudou = true;
        }
      });
    });
  }
  return [...set];
}
function limparBlocos(d, escolhidos, usuario) {
  const ids = fecharDependencias(escolhidos);
  if (ids.length === 0) return {
    erro: 'Nada foi escolhido.'
  };
  const modelo = emptyDb();
  const feito = [];
  BLOCOS_LIMPEZA.filter(b => ids.includes(b.id)).forEach(b => {
    const n = contarBloco(b, d);
    b.colecoes.forEach(k => {
      d[k] = Array.isArray(modelo[k]) ? [] : modelo[k];
    });
    if (n > 0) feito.push(`${b.nome}: ${n} registro(s)`);
  });
  /* a numeração recomeça só do que foi apagado */
  const seq = {
    ...(d.seq || {})
  };
  ids.forEach(id => {
    const b = BLOCOS_LIMPEZA.find(x => x.id === id);
    (b ? b.colecoes : []).forEach(k => {
      delete seq[k];
    });
  });
  d.seq = seq;
  registrarLog(d, usuario, 'sistema.limpar', feito.length ? feito.join(' · ') : 'nada a apagar');
  return {
    limpos: ids,
    resumo: feito
  };
}

/* ==========================================================
   QR CODE
   Biblioteca qrcode-generator (Kazuhiko Arase, licença MIT),
   embutida no arquivo. Um cartaz de fábrica não pode depender de
   internet para ser impresso, e QR feito à mão que não abre no
   celular é pior do que não ter QR nenhum.
========================================================== */
const qrcode = function () {
  const module = {
    exports: {}
  };
  const exports = module.exports;
  //---------------------------------------------------------------------
  //
  // QR Code Generator for JavaScript
  //
  // Copyright (c) 2009 Kazuhiko Arase
  //
  // URL: http://www.d-project.com/
  //
  // Licensed under the MIT license:
  //  http://www.opensource.org/licenses/mit-license.php
  //
  // The word 'QR Code' is registered trademark of
  // DENSO WAVE INCORPORATED
  //  http://www.denso-wave.com/qrcode/faqpatent-e.html
  //
  //---------------------------------------------------------------------

  var qrcode = function () {
    //---------------------------------------------------------------------
    // qrcode
    //---------------------------------------------------------------------

    /**
     * qrcode
     * @param typeNumber 1 to 40
     * @param errorCorrectionLevel 'L','M','Q','H'
     */
    var qrcode = function (typeNumber, errorCorrectionLevel) {
      var PAD0 = 0xEC;
      var PAD1 = 0x11;
      var _typeNumber = typeNumber;
      var _errorCorrectionLevel = QRErrorCorrectionLevel[errorCorrectionLevel];
      var _modules = null;
      var _moduleCount = 0;
      var _dataCache = null;
      var _dataList = [];
      var _this = {};
      var makeImpl = function (test, maskPattern) {
        _moduleCount = _typeNumber * 4 + 17;
        _modules = function (moduleCount) {
          var modules = new Array(moduleCount);
          for (var row = 0; row < moduleCount; row += 1) {
            modules[row] = new Array(moduleCount);
            for (var col = 0; col < moduleCount; col += 1) {
              modules[row][col] = null;
            }
          }
          return modules;
        }(_moduleCount);
        setupPositionProbePattern(0, 0);
        setupPositionProbePattern(_moduleCount - 7, 0);
        setupPositionProbePattern(0, _moduleCount - 7);
        setupPositionAdjustPattern();
        setupTimingPattern();
        setupTypeInfo(test, maskPattern);
        if (_typeNumber >= 7) {
          setupTypeNumber(test);
        }
        if (_dataCache == null) {
          _dataCache = createData(_typeNumber, _errorCorrectionLevel, _dataList);
        }
        mapData(_dataCache, maskPattern);
      };
      var setupPositionProbePattern = function (row, col) {
        for (var r = -1; r <= 7; r += 1) {
          if (row + r <= -1 || _moduleCount <= row + r) continue;
          for (var c = -1; c <= 7; c += 1) {
            if (col + c <= -1 || _moduleCount <= col + c) continue;
            if (0 <= r && r <= 6 && (c == 0 || c == 6) || 0 <= c && c <= 6 && (r == 0 || r == 6) || 2 <= r && r <= 4 && 2 <= c && c <= 4) {
              _modules[row + r][col + c] = true;
            } else {
              _modules[row + r][col + c] = false;
            }
          }
        }
      };
      var getBestMaskPattern = function () {
        var minLostPoint = 0;
        var pattern = 0;
        for (var i = 0; i < 8; i += 1) {
          makeImpl(true, i);
          var lostPoint = QRUtil.getLostPoint(_this);
          if (i == 0 || minLostPoint > lostPoint) {
            minLostPoint = lostPoint;
            pattern = i;
          }
        }
        return pattern;
      };
      var setupTimingPattern = function () {
        for (var r = 8; r < _moduleCount - 8; r += 1) {
          if (_modules[r][6] != null) {
            continue;
          }
          _modules[r][6] = r % 2 == 0;
        }
        for (var c = 8; c < _moduleCount - 8; c += 1) {
          if (_modules[6][c] != null) {
            continue;
          }
          _modules[6][c] = c % 2 == 0;
        }
      };
      var setupPositionAdjustPattern = function () {
        var pos = QRUtil.getPatternPosition(_typeNumber);
        for (var i = 0; i < pos.length; i += 1) {
          for (var j = 0; j < pos.length; j += 1) {
            var row = pos[i];
            var col = pos[j];
            if (_modules[row][col] != null) {
              continue;
            }
            for (var r = -2; r <= 2; r += 1) {
              for (var c = -2; c <= 2; c += 1) {
                if (r == -2 || r == 2 || c == -2 || c == 2 || r == 0 && c == 0) {
                  _modules[row + r][col + c] = true;
                } else {
                  _modules[row + r][col + c] = false;
                }
              }
            }
          }
        }
      };
      var setupTypeNumber = function (test) {
        var bits = QRUtil.getBCHTypeNumber(_typeNumber);
        for (var i = 0; i < 18; i += 1) {
          var mod = !test && (bits >> i & 1) == 1;
          _modules[Math.floor(i / 3)][i % 3 + _moduleCount - 8 - 3] = mod;
        }
        for (var i = 0; i < 18; i += 1) {
          var mod = !test && (bits >> i & 1) == 1;
          _modules[i % 3 + _moduleCount - 8 - 3][Math.floor(i / 3)] = mod;
        }
      };
      var setupTypeInfo = function (test, maskPattern) {
        var data = _errorCorrectionLevel << 3 | maskPattern;
        var bits = QRUtil.getBCHTypeInfo(data);

        // vertical
        for (var i = 0; i < 15; i += 1) {
          var mod = !test && (bits >> i & 1) == 1;
          if (i < 6) {
            _modules[i][8] = mod;
          } else if (i < 8) {
            _modules[i + 1][8] = mod;
          } else {
            _modules[_moduleCount - 15 + i][8] = mod;
          }
        }

        // horizontal
        for (var i = 0; i < 15; i += 1) {
          var mod = !test && (bits >> i & 1) == 1;
          if (i < 8) {
            _modules[8][_moduleCount - i - 1] = mod;
          } else if (i < 9) {
            _modules[8][15 - i - 1 + 1] = mod;
          } else {
            _modules[8][15 - i - 1] = mod;
          }
        }

        // fixed module
        _modules[_moduleCount - 8][8] = !test;
      };
      var mapData = function (data, maskPattern) {
        var inc = -1;
        var row = _moduleCount - 1;
        var bitIndex = 7;
        var byteIndex = 0;
        var maskFunc = QRUtil.getMaskFunction(maskPattern);
        for (var col = _moduleCount - 1; col > 0; col -= 2) {
          if (col == 6) col -= 1;
          while (true) {
            for (var c = 0; c < 2; c += 1) {
              if (_modules[row][col - c] == null) {
                var dark = false;
                if (byteIndex < data.length) {
                  dark = (data[byteIndex] >>> bitIndex & 1) == 1;
                }
                var mask = maskFunc(row, col - c);
                if (mask) {
                  dark = !dark;
                }
                _modules[row][col - c] = dark;
                bitIndex -= 1;
                if (bitIndex == -1) {
                  byteIndex += 1;
                  bitIndex = 7;
                }
              }
            }
            row += inc;
            if (row < 0 || _moduleCount <= row) {
              row -= inc;
              inc = -inc;
              break;
            }
          }
        }
      };
      var createBytes = function (buffer, rsBlocks) {
        var offset = 0;
        var maxDcCount = 0;
        var maxEcCount = 0;
        var dcdata = new Array(rsBlocks.length);
        var ecdata = new Array(rsBlocks.length);
        for (var r = 0; r < rsBlocks.length; r += 1) {
          var dcCount = rsBlocks[r].dataCount;
          var ecCount = rsBlocks[r].totalCount - dcCount;
          maxDcCount = Math.max(maxDcCount, dcCount);
          maxEcCount = Math.max(maxEcCount, ecCount);
          dcdata[r] = new Array(dcCount);
          for (var i = 0; i < dcdata[r].length; i += 1) {
            dcdata[r][i] = 0xff & buffer.getBuffer()[i + offset];
          }
          offset += dcCount;
          var rsPoly = QRUtil.getErrorCorrectPolynomial(ecCount);
          var rawPoly = qrPolynomial(dcdata[r], rsPoly.getLength() - 1);
          var modPoly = rawPoly.mod(rsPoly);
          ecdata[r] = new Array(rsPoly.getLength() - 1);
          for (var i = 0; i < ecdata[r].length; i += 1) {
            var modIndex = i + modPoly.getLength() - ecdata[r].length;
            ecdata[r][i] = modIndex >= 0 ? modPoly.getAt(modIndex) : 0;
          }
        }
        var totalCodeCount = 0;
        for (var i = 0; i < rsBlocks.length; i += 1) {
          totalCodeCount += rsBlocks[i].totalCount;
        }
        var data = new Array(totalCodeCount);
        var index = 0;
        for (var i = 0; i < maxDcCount; i += 1) {
          for (var r = 0; r < rsBlocks.length; r += 1) {
            if (i < dcdata[r].length) {
              data[index] = dcdata[r][i];
              index += 1;
            }
          }
        }
        for (var i = 0; i < maxEcCount; i += 1) {
          for (var r = 0; r < rsBlocks.length; r += 1) {
            if (i < ecdata[r].length) {
              data[index] = ecdata[r][i];
              index += 1;
            }
          }
        }
        return data;
      };
      var createData = function (typeNumber, errorCorrectionLevel, dataList) {
        var rsBlocks = QRRSBlock.getRSBlocks(typeNumber, errorCorrectionLevel);
        var buffer = qrBitBuffer();
        for (var i = 0; i < dataList.length; i += 1) {
          var data = dataList[i];
          buffer.put(data.getMode(), 4);
          buffer.put(data.getLength(), QRUtil.getLengthInBits(data.getMode(), typeNumber));
          data.write(buffer);
        }

        // calc num max data.
        var totalDataCount = 0;
        for (var i = 0; i < rsBlocks.length; i += 1) {
          totalDataCount += rsBlocks[i].dataCount;
        }
        if (buffer.getLengthInBits() > totalDataCount * 8) {
          throw 'code length overflow. (' + buffer.getLengthInBits() + '>' + totalDataCount * 8 + ')';
        }

        // end code
        if (buffer.getLengthInBits() + 4 <= totalDataCount * 8) {
          buffer.put(0, 4);
        }

        // padding
        while (buffer.getLengthInBits() % 8 != 0) {
          buffer.putBit(false);
        }

        // padding
        while (true) {
          if (buffer.getLengthInBits() >= totalDataCount * 8) {
            break;
          }
          buffer.put(PAD0, 8);
          if (buffer.getLengthInBits() >= totalDataCount * 8) {
            break;
          }
          buffer.put(PAD1, 8);
        }
        return createBytes(buffer, rsBlocks);
      };
      _this.addData = function (data, mode) {
        mode = mode || 'Byte';
        var newData = null;
        switch (mode) {
          case 'Numeric':
            newData = qrNumber(data);
            break;
          case 'Alphanumeric':
            newData = qrAlphaNum(data);
            break;
          case 'Byte':
            newData = qr8BitByte(data);
            break;
          case 'Kanji':
            newData = qrKanji(data);
            break;
          default:
            throw 'mode:' + mode;
        }
        _dataList.push(newData);
        _dataCache = null;
      };
      _this.isDark = function (row, col) {
        if (row < 0 || _moduleCount <= row || col < 0 || _moduleCount <= col) {
          throw row + ',' + col;
        }
        return _modules[row][col];
      };
      _this.getModuleCount = function () {
        return _moduleCount;
      };
      _this.make = function () {
        if (_typeNumber < 1) {
          var typeNumber = 1;
          for (; typeNumber < 40; typeNumber++) {
            var rsBlocks = QRRSBlock.getRSBlocks(typeNumber, _errorCorrectionLevel);
            var buffer = qrBitBuffer();
            for (var i = 0; i < _dataList.length; i++) {
              var data = _dataList[i];
              buffer.put(data.getMode(), 4);
              buffer.put(data.getLength(), QRUtil.getLengthInBits(data.getMode(), typeNumber));
              data.write(buffer);
            }
            var totalDataCount = 0;
            for (var i = 0; i < rsBlocks.length; i++) {
              totalDataCount += rsBlocks[i].dataCount;
            }
            if (buffer.getLengthInBits() <= totalDataCount * 8) {
              break;
            }
          }
          _typeNumber = typeNumber;
        }
        makeImpl(false, getBestMaskPattern());
      };
      _this.createTableTag = function (cellSize, margin) {
        cellSize = cellSize || 2;
        margin = typeof margin == 'undefined' ? cellSize * 4 : margin;
        var qrHtml = '';
        qrHtml += '<table style="';
        qrHtml += ' border-width: 0px; border-style: none;';
        qrHtml += ' border-collapse: collapse;';
        qrHtml += ' padding: 0px; margin: ' + margin + 'px;';
        qrHtml += '">';
        qrHtml += '<tbody>';
        for (var r = 0; r < _this.getModuleCount(); r += 1) {
          qrHtml += '<tr>';
          for (var c = 0; c < _this.getModuleCount(); c += 1) {
            qrHtml += '<td style="';
            qrHtml += ' border-width: 0px; border-style: none;';
            qrHtml += ' border-collapse: collapse;';
            qrHtml += ' padding: 0px; margin: 0px;';
            qrHtml += ' width: ' + cellSize + 'px;';
            qrHtml += ' height: ' + cellSize + 'px;';
            qrHtml += ' background-color: ';
            qrHtml += _this.isDark(r, c) ? '#000000' : '#ffffff';
            qrHtml += ';';
            qrHtml += '"/>';
          }
          qrHtml += '</tr>';
        }
        qrHtml += '</tbody>';
        qrHtml += '</table>';
        return qrHtml;
      };
      _this.createSvgTag = function (cellSize, margin, alt, title) {
        var opts = {};
        if (typeof arguments[0] == 'object') {
          // Called by options.
          opts = arguments[0];
          // overwrite cellSize and margin.
          cellSize = opts.cellSize;
          margin = opts.margin;
          alt = opts.alt;
          title = opts.title;
        }
        cellSize = cellSize || 2;
        margin = typeof margin == 'undefined' ? cellSize * 4 : margin;

        // Compose alt property surrogate
        alt = typeof alt === 'string' ? {
          text: alt
        } : alt || {};
        alt.text = alt.text || null;
        alt.id = alt.text ? alt.id || 'qrcode-description' : null;

        // Compose title property surrogate
        title = typeof title === 'string' ? {
          text: title
        } : title || {};
        title.text = title.text || null;
        title.id = title.text ? title.id || 'qrcode-title' : null;
        var size = _this.getModuleCount() * cellSize + margin * 2;
        var c,
          mc,
          r,
          mr,
          qrSvg = '',
          rect;
        rect = 'l' + cellSize + ',0 0,' + cellSize + ' -' + cellSize + ',0 0,-' + cellSize + 'z ';
        qrSvg += '<svg version="1.1" xmlns="http://www.w3.org/2000/svg"';
        qrSvg += !opts.scalable ? ' width="' + size + 'px" height="' + size + 'px"' : '';
        qrSvg += ' viewBox="0 0 ' + size + ' ' + size + '" ';
        qrSvg += ' preserveAspectRatio="xMinYMin meet"';
        qrSvg += title.text || alt.text ? ' role="img" aria-labelledby="' + escapeXml([title.id, alt.id].join(' ').trim()) + '"' : '';
        qrSvg += '>';
        qrSvg += title.text ? '<title id="' + escapeXml(title.id) + '">' + escapeXml(title.text) + '</title>' : '';
        qrSvg += alt.text ? '<description id="' + escapeXml(alt.id) + '">' + escapeXml(alt.text) + '</description>' : '';
        qrSvg += '<rect width="100%" height="100%" fill="white" cx="0" cy="0"/>';
        qrSvg += '<path d="';
        for (r = 0; r < _this.getModuleCount(); r += 1) {
          mr = r * cellSize + margin;
          for (c = 0; c < _this.getModuleCount(); c += 1) {
            if (_this.isDark(r, c)) {
              mc = c * cellSize + margin;
              qrSvg += 'M' + mc + ',' + mr + rect;
            }
          }
        }
        qrSvg += '" stroke="transparent" fill="black"/>';
        qrSvg += '</svg>';
        return qrSvg;
      };
      _this.createDataURL = function (cellSize, margin) {
        cellSize = cellSize || 2;
        margin = typeof margin == 'undefined' ? cellSize * 4 : margin;
        var size = _this.getModuleCount() * cellSize + margin * 2;
        var min = margin;
        var max = size - margin;
        return createDataURL(size, size, function (x, y) {
          if (min <= x && x < max && min <= y && y < max) {
            var c = Math.floor((x - min) / cellSize);
            var r = Math.floor((y - min) / cellSize);
            return _this.isDark(r, c) ? 0 : 1;
          } else {
            return 1;
          }
        });
      };
      _this.createImgTag = function (cellSize, margin, alt) {
        cellSize = cellSize || 2;
        margin = typeof margin == 'undefined' ? cellSize * 4 : margin;
        var size = _this.getModuleCount() * cellSize + margin * 2;
        var img = '';
        img += '<img';
        img += '\u0020src="';
        img += _this.createDataURL(cellSize, margin);
        img += '"';
        img += '\u0020width="';
        img += size;
        img += '"';
        img += '\u0020height="';
        img += size;
        img += '"';
        if (alt) {
          img += '\u0020alt="';
          img += escapeXml(alt);
          img += '"';
        }
        img += '/>';
        return img;
      };
      var escapeXml = function (s) {
        var escaped = '';
        for (var i = 0; i < s.length; i += 1) {
          var c = s.charAt(i);
          switch (c) {
            case '<':
              escaped += '&lt;';
              break;
            case '>':
              escaped += '&gt;';
              break;
            case '&':
              escaped += '&amp;';
              break;
            case '"':
              escaped += '&quot;';
              break;
            default:
              escaped += c;
              break;
          }
        }
        return escaped;
      };
      var _createHalfASCII = function (margin) {
        var cellSize = 1;
        margin = typeof margin == 'undefined' ? cellSize * 2 : margin;
        var size = _this.getModuleCount() * cellSize + margin * 2;
        var min = margin;
        var max = size - margin;
        var y, x, r1, r2, p;
        var blocks = {
          '██': '█',
          '█ ': '▀',
          ' █': '▄',
          '  ': ' '
        };
        var blocksLastLineNoMargin = {
          '██': '▀',
          '█ ': '▀',
          ' █': ' ',
          '  ': ' '
        };
        var ascii = '';
        for (y = 0; y < size; y += 2) {
          r1 = Math.floor((y - min) / cellSize);
          r2 = Math.floor((y + 1 - min) / cellSize);
          for (x = 0; x < size; x += 1) {
            p = '█';
            if (min <= x && x < max && min <= y && y < max && _this.isDark(r1, Math.floor((x - min) / cellSize))) {
              p = ' ';
            }
            if (min <= x && x < max && min <= y + 1 && y + 1 < max && _this.isDark(r2, Math.floor((x - min) / cellSize))) {
              p += ' ';
            } else {
              p += '█';
            }

            // Output 2 characters per pixel, to create full square. 1 character per pixels gives only half width of square.
            ascii += margin < 1 && y + 1 >= max ? blocksLastLineNoMargin[p] : blocks[p];
          }
          ascii += '\n';
        }
        if (size % 2 && margin > 0) {
          return ascii.substring(0, ascii.length - size - 1) + Array(size + 1).join('▀');
        }
        return ascii.substring(0, ascii.length - 1);
      };
      _this.createASCII = function (cellSize, margin) {
        cellSize = cellSize || 1;
        if (cellSize < 2) {
          return _createHalfASCII(margin);
        }
        cellSize -= 1;
        margin = typeof margin == 'undefined' ? cellSize * 2 : margin;
        var size = _this.getModuleCount() * cellSize + margin * 2;
        var min = margin;
        var max = size - margin;
        var y, x, r, p;
        var white = Array(cellSize + 1).join('██');
        var black = Array(cellSize + 1).join('  ');
        var ascii = '';
        var line = '';
        for (y = 0; y < size; y += 1) {
          r = Math.floor((y - min) / cellSize);
          line = '';
          for (x = 0; x < size; x += 1) {
            p = 1;
            if (min <= x && x < max && min <= y && y < max && _this.isDark(r, Math.floor((x - min) / cellSize))) {
              p = 0;
            }

            // Output 2 characters per pixel, to create full square. 1 character per pixels gives only half width of square.
            line += p ? white : black;
          }
          for (r = 0; r < cellSize; r += 1) {
            ascii += line + '\n';
          }
        }
        return ascii.substring(0, ascii.length - 1);
      };
      _this.renderTo2dContext = function (context, cellSize) {
        cellSize = cellSize || 2;
        var length = _this.getModuleCount();
        for (var row = 0; row < length; row++) {
          for (var col = 0; col < length; col++) {
            context.fillStyle = _this.isDark(row, col) ? 'black' : 'white';
            context.fillRect(col * cellSize, row * cellSize, cellSize, cellSize);
          }
        }
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // qrcode.stringToBytes
    //---------------------------------------------------------------------

    qrcode.stringToBytesFuncs = {
      'default': function (s) {
        var bytes = [];
        for (var i = 0; i < s.length; i += 1) {
          var c = s.charCodeAt(i);
          bytes.push(c & 0xff);
        }
        return bytes;
      }
    };
    qrcode.stringToBytes = qrcode.stringToBytesFuncs['default'];

    //---------------------------------------------------------------------
    // qrcode.createStringToBytes
    //---------------------------------------------------------------------

    /**
     * @param unicodeData base64 string of byte array.
     * [16bit Unicode],[16bit Bytes], ...
     * @param numChars
     */
    qrcode.createStringToBytes = function (unicodeData, numChars) {
      // create conversion map.

      var unicodeMap = function () {
        var bin = base64DecodeInputStream(unicodeData);
        var read = function () {
          var b = bin.read();
          if (b == -1) throw 'eof';
          return b;
        };
        var count = 0;
        var unicodeMap = {};
        while (true) {
          var b0 = bin.read();
          if (b0 == -1) break;
          var b1 = read();
          var b2 = read();
          var b3 = read();
          var k = String.fromCharCode(b0 << 8 | b1);
          var v = b2 << 8 | b3;
          unicodeMap[k] = v;
          count += 1;
        }
        if (count != numChars) {
          throw count + ' != ' + numChars;
        }
        return unicodeMap;
      }();
      var unknownChar = '?'.charCodeAt(0);
      return function (s) {
        var bytes = [];
        for (var i = 0; i < s.length; i += 1) {
          var c = s.charCodeAt(i);
          if (c < 128) {
            bytes.push(c);
          } else {
            var b = unicodeMap[s.charAt(i)];
            if (typeof b == 'number') {
              if ((b & 0xff) == b) {
                // 1byte
                bytes.push(b);
              } else {
                // 2bytes
                bytes.push(b >>> 8);
                bytes.push(b & 0xff);
              }
            } else {
              bytes.push(unknownChar);
            }
          }
        }
        return bytes;
      };
    };

    //---------------------------------------------------------------------
    // QRMode
    //---------------------------------------------------------------------

    var QRMode = {
      MODE_NUMBER: 1 << 0,
      MODE_ALPHA_NUM: 1 << 1,
      MODE_8BIT_BYTE: 1 << 2,
      MODE_KANJI: 1 << 3
    };

    //---------------------------------------------------------------------
    // QRErrorCorrectionLevel
    //---------------------------------------------------------------------

    var QRErrorCorrectionLevel = {
      L: 1,
      M: 0,
      Q: 3,
      H: 2
    };

    //---------------------------------------------------------------------
    // QRMaskPattern
    //---------------------------------------------------------------------

    var QRMaskPattern = {
      PATTERN000: 0,
      PATTERN001: 1,
      PATTERN010: 2,
      PATTERN011: 3,
      PATTERN100: 4,
      PATTERN101: 5,
      PATTERN110: 6,
      PATTERN111: 7
    };

    //---------------------------------------------------------------------
    // QRUtil
    //---------------------------------------------------------------------

    var QRUtil = function () {
      var PATTERN_POSITION_TABLE = [[], [6, 18], [6, 22], [6, 26], [6, 30], [6, 34], [6, 22, 38], [6, 24, 42], [6, 26, 46], [6, 28, 50], [6, 30, 54], [6, 32, 58], [6, 34, 62], [6, 26, 46, 66], [6, 26, 48, 70], [6, 26, 50, 74], [6, 30, 54, 78], [6, 30, 56, 82], [6, 30, 58, 86], [6, 34, 62, 90], [6, 28, 50, 72, 94], [6, 26, 50, 74, 98], [6, 30, 54, 78, 102], [6, 28, 54, 80, 106], [6, 32, 58, 84, 110], [6, 30, 58, 86, 114], [6, 34, 62, 90, 118], [6, 26, 50, 74, 98, 122], [6, 30, 54, 78, 102, 126], [6, 26, 52, 78, 104, 130], [6, 30, 56, 82, 108, 134], [6, 34, 60, 86, 112, 138], [6, 30, 58, 86, 114, 142], [6, 34, 62, 90, 118, 146], [6, 30, 54, 78, 102, 126, 150], [6, 24, 50, 76, 102, 128, 154], [6, 28, 54, 80, 106, 132, 158], [6, 32, 58, 84, 110, 136, 162], [6, 26, 54, 82, 110, 138, 166], [6, 30, 58, 86, 114, 142, 170]];
      var G15 = 1 << 10 | 1 << 8 | 1 << 5 | 1 << 4 | 1 << 2 | 1 << 1 | 1 << 0;
      var G18 = 1 << 12 | 1 << 11 | 1 << 10 | 1 << 9 | 1 << 8 | 1 << 5 | 1 << 2 | 1 << 0;
      var G15_MASK = 1 << 14 | 1 << 12 | 1 << 10 | 1 << 4 | 1 << 1;
      var _this = {};
      var getBCHDigit = function (data) {
        var digit = 0;
        while (data != 0) {
          digit += 1;
          data >>>= 1;
        }
        return digit;
      };
      _this.getBCHTypeInfo = function (data) {
        var d = data << 10;
        while (getBCHDigit(d) - getBCHDigit(G15) >= 0) {
          d ^= G15 << getBCHDigit(d) - getBCHDigit(G15);
        }
        return (data << 10 | d) ^ G15_MASK;
      };
      _this.getBCHTypeNumber = function (data) {
        var d = data << 12;
        while (getBCHDigit(d) - getBCHDigit(G18) >= 0) {
          d ^= G18 << getBCHDigit(d) - getBCHDigit(G18);
        }
        return data << 12 | d;
      };
      _this.getPatternPosition = function (typeNumber) {
        return PATTERN_POSITION_TABLE[typeNumber - 1];
      };
      _this.getMaskFunction = function (maskPattern) {
        switch (maskPattern) {
          case QRMaskPattern.PATTERN000:
            return function (i, j) {
              return (i + j) % 2 == 0;
            };
          case QRMaskPattern.PATTERN001:
            return function (i, j) {
              return i % 2 == 0;
            };
          case QRMaskPattern.PATTERN010:
            return function (i, j) {
              return j % 3 == 0;
            };
          case QRMaskPattern.PATTERN011:
            return function (i, j) {
              return (i + j) % 3 == 0;
            };
          case QRMaskPattern.PATTERN100:
            return function (i, j) {
              return (Math.floor(i / 2) + Math.floor(j / 3)) % 2 == 0;
            };
          case QRMaskPattern.PATTERN101:
            return function (i, j) {
              return i * j % 2 + i * j % 3 == 0;
            };
          case QRMaskPattern.PATTERN110:
            return function (i, j) {
              return (i * j % 2 + i * j % 3) % 2 == 0;
            };
          case QRMaskPattern.PATTERN111:
            return function (i, j) {
              return (i * j % 3 + (i + j) % 2) % 2 == 0;
            };
          default:
            throw 'bad maskPattern:' + maskPattern;
        }
      };
      _this.getErrorCorrectPolynomial = function (errorCorrectLength) {
        var a = qrPolynomial([1], 0);
        for (var i = 0; i < errorCorrectLength; i += 1) {
          a = a.multiply(qrPolynomial([1, QRMath.gexp(i)], 0));
        }
        return a;
      };
      _this.getLengthInBits = function (mode, type) {
        if (1 <= type && type < 10) {
          // 1 - 9

          switch (mode) {
            case QRMode.MODE_NUMBER:
              return 10;
            case QRMode.MODE_ALPHA_NUM:
              return 9;
            case QRMode.MODE_8BIT_BYTE:
              return 8;
            case QRMode.MODE_KANJI:
              return 8;
            default:
              throw 'mode:' + mode;
          }
        } else if (type < 27) {
          // 10 - 26

          switch (mode) {
            case QRMode.MODE_NUMBER:
              return 12;
            case QRMode.MODE_ALPHA_NUM:
              return 11;
            case QRMode.MODE_8BIT_BYTE:
              return 16;
            case QRMode.MODE_KANJI:
              return 10;
            default:
              throw 'mode:' + mode;
          }
        } else if (type < 41) {
          // 27 - 40

          switch (mode) {
            case QRMode.MODE_NUMBER:
              return 14;
            case QRMode.MODE_ALPHA_NUM:
              return 13;
            case QRMode.MODE_8BIT_BYTE:
              return 16;
            case QRMode.MODE_KANJI:
              return 12;
            default:
              throw 'mode:' + mode;
          }
        } else {
          throw 'type:' + type;
        }
      };
      _this.getLostPoint = function (qrcode) {
        var moduleCount = qrcode.getModuleCount();
        var lostPoint = 0;

        // LEVEL1

        for (var row = 0; row < moduleCount; row += 1) {
          for (var col = 0; col < moduleCount; col += 1) {
            var sameCount = 0;
            var dark = qrcode.isDark(row, col);
            for (var r = -1; r <= 1; r += 1) {
              if (row + r < 0 || moduleCount <= row + r) {
                continue;
              }
              for (var c = -1; c <= 1; c += 1) {
                if (col + c < 0 || moduleCount <= col + c) {
                  continue;
                }
                if (r == 0 && c == 0) {
                  continue;
                }
                if (dark == qrcode.isDark(row + r, col + c)) {
                  sameCount += 1;
                }
              }
            }
            if (sameCount > 5) {
              lostPoint += 3 + sameCount - 5;
            }
          }
        }
        ;

        // LEVEL2

        for (var row = 0; row < moduleCount - 1; row += 1) {
          for (var col = 0; col < moduleCount - 1; col += 1) {
            var count = 0;
            if (qrcode.isDark(row, col)) count += 1;
            if (qrcode.isDark(row + 1, col)) count += 1;
            if (qrcode.isDark(row, col + 1)) count += 1;
            if (qrcode.isDark(row + 1, col + 1)) count += 1;
            if (count == 0 || count == 4) {
              lostPoint += 3;
            }
          }
        }

        // LEVEL3

        for (var row = 0; row < moduleCount; row += 1) {
          for (var col = 0; col < moduleCount - 6; col += 1) {
            if (qrcode.isDark(row, col) && !qrcode.isDark(row, col + 1) && qrcode.isDark(row, col + 2) && qrcode.isDark(row, col + 3) && qrcode.isDark(row, col + 4) && !qrcode.isDark(row, col + 5) && qrcode.isDark(row, col + 6)) {
              lostPoint += 40;
            }
          }
        }
        for (var col = 0; col < moduleCount; col += 1) {
          for (var row = 0; row < moduleCount - 6; row += 1) {
            if (qrcode.isDark(row, col) && !qrcode.isDark(row + 1, col) && qrcode.isDark(row + 2, col) && qrcode.isDark(row + 3, col) && qrcode.isDark(row + 4, col) && !qrcode.isDark(row + 5, col) && qrcode.isDark(row + 6, col)) {
              lostPoint += 40;
            }
          }
        }

        // LEVEL4

        var darkCount = 0;
        for (var col = 0; col < moduleCount; col += 1) {
          for (var row = 0; row < moduleCount; row += 1) {
            if (qrcode.isDark(row, col)) {
              darkCount += 1;
            }
          }
        }
        var ratio = Math.abs(100 * darkCount / moduleCount / moduleCount - 50) / 5;
        lostPoint += ratio * 10;
        return lostPoint;
      };
      return _this;
    }();

    //---------------------------------------------------------------------
    // QRMath
    //---------------------------------------------------------------------

    var QRMath = function () {
      var EXP_TABLE = new Array(256);
      var LOG_TABLE = new Array(256);

      // initialize tables
      for (var i = 0; i < 8; i += 1) {
        EXP_TABLE[i] = 1 << i;
      }
      for (var i = 8; i < 256; i += 1) {
        EXP_TABLE[i] = EXP_TABLE[i - 4] ^ EXP_TABLE[i - 5] ^ EXP_TABLE[i - 6] ^ EXP_TABLE[i - 8];
      }
      for (var i = 0; i < 255; i += 1) {
        LOG_TABLE[EXP_TABLE[i]] = i;
      }
      var _this = {};
      _this.glog = function (n) {
        if (n < 1) {
          throw 'glog(' + n + ')';
        }
        return LOG_TABLE[n];
      };
      _this.gexp = function (n) {
        while (n < 0) {
          n += 255;
        }
        while (n >= 256) {
          n -= 255;
        }
        return EXP_TABLE[n];
      };
      return _this;
    }();

    //---------------------------------------------------------------------
    // qrPolynomial
    //---------------------------------------------------------------------

    function qrPolynomial(num, shift) {
      if (typeof num.length == 'undefined') {
        throw num.length + '/' + shift;
      }
      var _num = function () {
        var offset = 0;
        while (offset < num.length && num[offset] == 0) {
          offset += 1;
        }
        var _num = new Array(num.length - offset + shift);
        for (var i = 0; i < num.length - offset; i += 1) {
          _num[i] = num[i + offset];
        }
        return _num;
      }();
      var _this = {};
      _this.getAt = function (index) {
        return _num[index];
      };
      _this.getLength = function () {
        return _num.length;
      };
      _this.multiply = function (e) {
        var num = new Array(_this.getLength() + e.getLength() - 1);
        for (var i = 0; i < _this.getLength(); i += 1) {
          for (var j = 0; j < e.getLength(); j += 1) {
            num[i + j] ^= QRMath.gexp(QRMath.glog(_this.getAt(i)) + QRMath.glog(e.getAt(j)));
          }
        }
        return qrPolynomial(num, 0);
      };
      _this.mod = function (e) {
        if (_this.getLength() - e.getLength() < 0) {
          return _this;
        }
        var ratio = QRMath.glog(_this.getAt(0)) - QRMath.glog(e.getAt(0));
        var num = new Array(_this.getLength());
        for (var i = 0; i < _this.getLength(); i += 1) {
          num[i] = _this.getAt(i);
        }
        for (var i = 0; i < e.getLength(); i += 1) {
          num[i] ^= QRMath.gexp(QRMath.glog(e.getAt(i)) + ratio);
        }

        // recursive call
        return qrPolynomial(num, 0).mod(e);
      };
      return _this;
    }
    ;

    //---------------------------------------------------------------------
    // QRRSBlock
    //---------------------------------------------------------------------

    var QRRSBlock = function () {
      var RS_BLOCK_TABLE = [
      // L
      // M
      // Q
      // H

      // 1
      [1, 26, 19], [1, 26, 16], [1, 26, 13], [1, 26, 9],
      // 2
      [1, 44, 34], [1, 44, 28], [1, 44, 22], [1, 44, 16],
      // 3
      [1, 70, 55], [1, 70, 44], [2, 35, 17], [2, 35, 13],
      // 4
      [1, 100, 80], [2, 50, 32], [2, 50, 24], [4, 25, 9],
      // 5
      [1, 134, 108], [2, 67, 43], [2, 33, 15, 2, 34, 16], [2, 33, 11, 2, 34, 12],
      // 6
      [2, 86, 68], [4, 43, 27], [4, 43, 19], [4, 43, 15],
      // 7
      [2, 98, 78], [4, 49, 31], [2, 32, 14, 4, 33, 15], [4, 39, 13, 1, 40, 14],
      // 8
      [2, 121, 97], [2, 60, 38, 2, 61, 39], [4, 40, 18, 2, 41, 19], [4, 40, 14, 2, 41, 15],
      // 9
      [2, 146, 116], [3, 58, 36, 2, 59, 37], [4, 36, 16, 4, 37, 17], [4, 36, 12, 4, 37, 13],
      // 10
      [2, 86, 68, 2, 87, 69], [4, 69, 43, 1, 70, 44], [6, 43, 19, 2, 44, 20], [6, 43, 15, 2, 44, 16],
      // 11
      [4, 101, 81], [1, 80, 50, 4, 81, 51], [4, 50, 22, 4, 51, 23], [3, 36, 12, 8, 37, 13],
      // 12
      [2, 116, 92, 2, 117, 93], [6, 58, 36, 2, 59, 37], [4, 46, 20, 6, 47, 21], [7, 42, 14, 4, 43, 15],
      // 13
      [4, 133, 107], [8, 59, 37, 1, 60, 38], [8, 44, 20, 4, 45, 21], [12, 33, 11, 4, 34, 12],
      // 14
      [3, 145, 115, 1, 146, 116], [4, 64, 40, 5, 65, 41], [11, 36, 16, 5, 37, 17], [11, 36, 12, 5, 37, 13],
      // 15
      [5, 109, 87, 1, 110, 88], [5, 65, 41, 5, 66, 42], [5, 54, 24, 7, 55, 25], [11, 36, 12, 7, 37, 13],
      // 16
      [5, 122, 98, 1, 123, 99], [7, 73, 45, 3, 74, 46], [15, 43, 19, 2, 44, 20], [3, 45, 15, 13, 46, 16],
      // 17
      [1, 135, 107, 5, 136, 108], [10, 74, 46, 1, 75, 47], [1, 50, 22, 15, 51, 23], [2, 42, 14, 17, 43, 15],
      // 18
      [5, 150, 120, 1, 151, 121], [9, 69, 43, 4, 70, 44], [17, 50, 22, 1, 51, 23], [2, 42, 14, 19, 43, 15],
      // 19
      [3, 141, 113, 4, 142, 114], [3, 70, 44, 11, 71, 45], [17, 47, 21, 4, 48, 22], [9, 39, 13, 16, 40, 14],
      // 20
      [3, 135, 107, 5, 136, 108], [3, 67, 41, 13, 68, 42], [15, 54, 24, 5, 55, 25], [15, 43, 15, 10, 44, 16],
      // 21
      [4, 144, 116, 4, 145, 117], [17, 68, 42], [17, 50, 22, 6, 51, 23], [19, 46, 16, 6, 47, 17],
      // 22
      [2, 139, 111, 7, 140, 112], [17, 74, 46], [7, 54, 24, 16, 55, 25], [34, 37, 13],
      // 23
      [4, 151, 121, 5, 152, 122], [4, 75, 47, 14, 76, 48], [11, 54, 24, 14, 55, 25], [16, 45, 15, 14, 46, 16],
      // 24
      [6, 147, 117, 4, 148, 118], [6, 73, 45, 14, 74, 46], [11, 54, 24, 16, 55, 25], [30, 46, 16, 2, 47, 17],
      // 25
      [8, 132, 106, 4, 133, 107], [8, 75, 47, 13, 76, 48], [7, 54, 24, 22, 55, 25], [22, 45, 15, 13, 46, 16],
      // 26
      [10, 142, 114, 2, 143, 115], [19, 74, 46, 4, 75, 47], [28, 50, 22, 6, 51, 23], [33, 46, 16, 4, 47, 17],
      // 27
      [8, 152, 122, 4, 153, 123], [22, 73, 45, 3, 74, 46], [8, 53, 23, 26, 54, 24], [12, 45, 15, 28, 46, 16],
      // 28
      [3, 147, 117, 10, 148, 118], [3, 73, 45, 23, 74, 46], [4, 54, 24, 31, 55, 25], [11, 45, 15, 31, 46, 16],
      // 29
      [7, 146, 116, 7, 147, 117], [21, 73, 45, 7, 74, 46], [1, 53, 23, 37, 54, 24], [19, 45, 15, 26, 46, 16],
      // 30
      [5, 145, 115, 10, 146, 116], [19, 75, 47, 10, 76, 48], [15, 54, 24, 25, 55, 25], [23, 45, 15, 25, 46, 16],
      // 31
      [13, 145, 115, 3, 146, 116], [2, 74, 46, 29, 75, 47], [42, 54, 24, 1, 55, 25], [23, 45, 15, 28, 46, 16],
      // 32
      [17, 145, 115], [10, 74, 46, 23, 75, 47], [10, 54, 24, 35, 55, 25], [19, 45, 15, 35, 46, 16],
      // 33
      [17, 145, 115, 1, 146, 116], [14, 74, 46, 21, 75, 47], [29, 54, 24, 19, 55, 25], [11, 45, 15, 46, 46, 16],
      // 34
      [13, 145, 115, 6, 146, 116], [14, 74, 46, 23, 75, 47], [44, 54, 24, 7, 55, 25], [59, 46, 16, 1, 47, 17],
      // 35
      [12, 151, 121, 7, 152, 122], [12, 75, 47, 26, 76, 48], [39, 54, 24, 14, 55, 25], [22, 45, 15, 41, 46, 16],
      // 36
      [6, 151, 121, 14, 152, 122], [6, 75, 47, 34, 76, 48], [46, 54, 24, 10, 55, 25], [2, 45, 15, 64, 46, 16],
      // 37
      [17, 152, 122, 4, 153, 123], [29, 74, 46, 14, 75, 47], [49, 54, 24, 10, 55, 25], [24, 45, 15, 46, 46, 16],
      // 38
      [4, 152, 122, 18, 153, 123], [13, 74, 46, 32, 75, 47], [48, 54, 24, 14, 55, 25], [42, 45, 15, 32, 46, 16],
      // 39
      [20, 147, 117, 4, 148, 118], [40, 75, 47, 7, 76, 48], [43, 54, 24, 22, 55, 25], [10, 45, 15, 67, 46, 16],
      // 40
      [19, 148, 118, 6, 149, 119], [18, 75, 47, 31, 76, 48], [34, 54, 24, 34, 55, 25], [20, 45, 15, 61, 46, 16]];
      var qrRSBlock = function (totalCount, dataCount) {
        var _this = {};
        _this.totalCount = totalCount;
        _this.dataCount = dataCount;
        return _this;
      };
      var _this = {};
      var getRsBlockTable = function (typeNumber, errorCorrectionLevel) {
        switch (errorCorrectionLevel) {
          case QRErrorCorrectionLevel.L:
            return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 0];
          case QRErrorCorrectionLevel.M:
            return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 1];
          case QRErrorCorrectionLevel.Q:
            return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 2];
          case QRErrorCorrectionLevel.H:
            return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 3];
          default:
            return undefined;
        }
      };
      _this.getRSBlocks = function (typeNumber, errorCorrectionLevel) {
        var rsBlock = getRsBlockTable(typeNumber, errorCorrectionLevel);
        if (typeof rsBlock == 'undefined') {
          throw 'bad rs block @ typeNumber:' + typeNumber + '/errorCorrectionLevel:' + errorCorrectionLevel;
        }
        var length = rsBlock.length / 3;
        var list = [];
        for (var i = 0; i < length; i += 1) {
          var count = rsBlock[i * 3 + 0];
          var totalCount = rsBlock[i * 3 + 1];
          var dataCount = rsBlock[i * 3 + 2];
          for (var j = 0; j < count; j += 1) {
            list.push(qrRSBlock(totalCount, dataCount));
          }
        }
        return list;
      };
      return _this;
    }();

    //---------------------------------------------------------------------
    // qrBitBuffer
    //---------------------------------------------------------------------

    var qrBitBuffer = function () {
      var _buffer = [];
      var _length = 0;
      var _this = {};
      _this.getBuffer = function () {
        return _buffer;
      };
      _this.getAt = function (index) {
        var bufIndex = Math.floor(index / 8);
        return (_buffer[bufIndex] >>> 7 - index % 8 & 1) == 1;
      };
      _this.put = function (num, length) {
        for (var i = 0; i < length; i += 1) {
          _this.putBit((num >>> length - i - 1 & 1) == 1);
        }
      };
      _this.getLengthInBits = function () {
        return _length;
      };
      _this.putBit = function (bit) {
        var bufIndex = Math.floor(_length / 8);
        if (_buffer.length <= bufIndex) {
          _buffer.push(0);
        }
        if (bit) {
          _buffer[bufIndex] |= 0x80 >>> _length % 8;
        }
        _length += 1;
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // qrNumber
    //---------------------------------------------------------------------

    var qrNumber = function (data) {
      var _mode = QRMode.MODE_NUMBER;
      var _data = data;
      var _this = {};
      _this.getMode = function () {
        return _mode;
      };
      _this.getLength = function (buffer) {
        return _data.length;
      };
      _this.write = function (buffer) {
        var data = _data;
        var i = 0;
        while (i + 2 < data.length) {
          buffer.put(strToNum(data.substring(i, i + 3)), 10);
          i += 3;
        }
        if (i < data.length) {
          if (data.length - i == 1) {
            buffer.put(strToNum(data.substring(i, i + 1)), 4);
          } else if (data.length - i == 2) {
            buffer.put(strToNum(data.substring(i, i + 2)), 7);
          }
        }
      };
      var strToNum = function (s) {
        var num = 0;
        for (var i = 0; i < s.length; i += 1) {
          num = num * 10 + chatToNum(s.charAt(i));
        }
        return num;
      };
      var chatToNum = function (c) {
        if ('0' <= c && c <= '9') {
          return c.charCodeAt(0) - '0'.charCodeAt(0);
        }
        throw 'illegal char :' + c;
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // qrAlphaNum
    //---------------------------------------------------------------------

    var qrAlphaNum = function (data) {
      var _mode = QRMode.MODE_ALPHA_NUM;
      var _data = data;
      var _this = {};
      _this.getMode = function () {
        return _mode;
      };
      _this.getLength = function (buffer) {
        return _data.length;
      };
      _this.write = function (buffer) {
        var s = _data;
        var i = 0;
        while (i + 1 < s.length) {
          buffer.put(getCode(s.charAt(i)) * 45 + getCode(s.charAt(i + 1)), 11);
          i += 2;
        }
        if (i < s.length) {
          buffer.put(getCode(s.charAt(i)), 6);
        }
      };
      var getCode = function (c) {
        if ('0' <= c && c <= '9') {
          return c.charCodeAt(0) - '0'.charCodeAt(0);
        } else if ('A' <= c && c <= 'Z') {
          return c.charCodeAt(0) - 'A'.charCodeAt(0) + 10;
        } else {
          switch (c) {
            case ' ':
              return 36;
            case '$':
              return 37;
            case '%':
              return 38;
            case '*':
              return 39;
            case '+':
              return 40;
            case '-':
              return 41;
            case '.':
              return 42;
            case '/':
              return 43;
            case ':':
              return 44;
            default:
              throw 'illegal char :' + c;
          }
        }
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // qr8BitByte
    //---------------------------------------------------------------------

    var qr8BitByte = function (data) {
      var _mode = QRMode.MODE_8BIT_BYTE;
      var _data = data;
      var _bytes = qrcode.stringToBytes(data);
      var _this = {};
      _this.getMode = function () {
        return _mode;
      };
      _this.getLength = function (buffer) {
        return _bytes.length;
      };
      _this.write = function (buffer) {
        for (var i = 0; i < _bytes.length; i += 1) {
          buffer.put(_bytes[i], 8);
        }
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // qrKanji
    //---------------------------------------------------------------------

    var qrKanji = function (data) {
      var _mode = QRMode.MODE_KANJI;
      var _data = data;
      var stringToBytes = qrcode.stringToBytesFuncs['SJIS'];
      if (!stringToBytes) {
        throw 'sjis not supported.';
      }
      !function (c, code) {
        // self test for sjis support.
        var test = stringToBytes(c);
        if (test.length != 2 || (test[0] << 8 | test[1]) != code) {
          throw 'sjis not supported.';
        }
      }('\u53cb', 0x9746);
      var _bytes = stringToBytes(data);
      var _this = {};
      _this.getMode = function () {
        return _mode;
      };
      _this.getLength = function (buffer) {
        return ~~(_bytes.length / 2);
      };
      _this.write = function (buffer) {
        var data = _bytes;
        var i = 0;
        while (i + 1 < data.length) {
          var c = (0xff & data[i]) << 8 | 0xff & data[i + 1];
          if (0x8140 <= c && c <= 0x9FFC) {
            c -= 0x8140;
          } else if (0xE040 <= c && c <= 0xEBBF) {
            c -= 0xC140;
          } else {
            throw 'illegal char at ' + (i + 1) + '/' + c;
          }
          c = (c >>> 8 & 0xff) * 0xC0 + (c & 0xff);
          buffer.put(c, 13);
          i += 2;
        }
        if (i < data.length) {
          throw 'illegal char at ' + (i + 1);
        }
      };
      return _this;
    };

    //=====================================================================
    // GIF Support etc.
    //

    //---------------------------------------------------------------------
    // byteArrayOutputStream
    //---------------------------------------------------------------------

    var byteArrayOutputStream = function () {
      var _bytes = [];
      var _this = {};
      _this.writeByte = function (b) {
        _bytes.push(b & 0xff);
      };
      _this.writeShort = function (i) {
        _this.writeByte(i);
        _this.writeByte(i >>> 8);
      };
      _this.writeBytes = function (b, off, len) {
        off = off || 0;
        len = len || b.length;
        for (var i = 0; i < len; i += 1) {
          _this.writeByte(b[i + off]);
        }
      };
      _this.writeString = function (s) {
        for (var i = 0; i < s.length; i += 1) {
          _this.writeByte(s.charCodeAt(i));
        }
      };
      _this.toByteArray = function () {
        return _bytes;
      };
      _this.toString = function () {
        var s = '';
        s += '[';
        for (var i = 0; i < _bytes.length; i += 1) {
          if (i > 0) {
            s += ',';
          }
          s += _bytes[i];
        }
        s += ']';
        return s;
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // base64EncodeOutputStream
    //---------------------------------------------------------------------

    var base64EncodeOutputStream = function () {
      var _buffer = 0;
      var _buflen = 0;
      var _length = 0;
      var _base64 = '';
      var _this = {};
      var writeEncoded = function (b) {
        _base64 += String.fromCharCode(encode(b & 0x3f));
      };
      var encode = function (n) {
        if (n < 0) {
          // error.
        } else if (n < 26) {
          return 0x41 + n;
        } else if (n < 52) {
          return 0x61 + (n - 26);
        } else if (n < 62) {
          return 0x30 + (n - 52);
        } else if (n == 62) {
          return 0x2b;
        } else if (n == 63) {
          return 0x2f;
        }
        throw 'n:' + n;
      };
      _this.writeByte = function (n) {
        _buffer = _buffer << 8 | n & 0xff;
        _buflen += 8;
        _length += 1;
        while (_buflen >= 6) {
          writeEncoded(_buffer >>> _buflen - 6);
          _buflen -= 6;
        }
      };
      _this.flush = function () {
        if (_buflen > 0) {
          writeEncoded(_buffer << 6 - _buflen);
          _buffer = 0;
          _buflen = 0;
        }
        if (_length % 3 != 0) {
          // padding
          var padlen = 3 - _length % 3;
          for (var i = 0; i < padlen; i += 1) {
            _base64 += '=';
          }
        }
      };
      _this.toString = function () {
        return _base64;
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // base64DecodeInputStream
    //---------------------------------------------------------------------

    var base64DecodeInputStream = function (str) {
      var _str = str;
      var _pos = 0;
      var _buffer = 0;
      var _buflen = 0;
      var _this = {};
      _this.read = function () {
        while (_buflen < 8) {
          if (_pos >= _str.length) {
            if (_buflen == 0) {
              return -1;
            }
            throw 'unexpected end of file./' + _buflen;
          }
          var c = _str.charAt(_pos);
          _pos += 1;
          if (c == '=') {
            _buflen = 0;
            return -1;
          } else if (c.match(/^\s$/)) {
            // ignore if whitespace.
            continue;
          }
          _buffer = _buffer << 6 | decode(c.charCodeAt(0));
          _buflen += 6;
        }
        var n = _buffer >>> _buflen - 8 & 0xff;
        _buflen -= 8;
        return n;
      };
      var decode = function (c) {
        if (0x41 <= c && c <= 0x5a) {
          return c - 0x41;
        } else if (0x61 <= c && c <= 0x7a) {
          return c - 0x61 + 26;
        } else if (0x30 <= c && c <= 0x39) {
          return c - 0x30 + 52;
        } else if (c == 0x2b) {
          return 62;
        } else if (c == 0x2f) {
          return 63;
        } else {
          throw 'c:' + c;
        }
      };
      return _this;
    };

    //---------------------------------------------------------------------
    // gifImage (B/W)
    //---------------------------------------------------------------------

    var gifImage = function (width, height) {
      var _width = width;
      var _height = height;
      var _data = new Array(width * height);
      var _this = {};
      _this.setPixel = function (x, y, pixel) {
        _data[y * _width + x] = pixel;
      };
      _this.write = function (out) {
        //---------------------------------
        // GIF Signature

        out.writeString('GIF87a');

        //---------------------------------
        // Screen Descriptor

        out.writeShort(_width);
        out.writeShort(_height);
        out.writeByte(0x80); // 2bit
        out.writeByte(0);
        out.writeByte(0);

        //---------------------------------
        // Global Color Map

        // black
        out.writeByte(0x00);
        out.writeByte(0x00);
        out.writeByte(0x00);

        // white
        out.writeByte(0xff);
        out.writeByte(0xff);
        out.writeByte(0xff);

        //---------------------------------
        // Image Descriptor

        out.writeString(',');
        out.writeShort(0);
        out.writeShort(0);
        out.writeShort(_width);
        out.writeShort(_height);
        out.writeByte(0);

        //---------------------------------
        // Local Color Map

        //---------------------------------
        // Raster Data

        var lzwMinCodeSize = 2;
        var raster = getLZWRaster(lzwMinCodeSize);
        out.writeByte(lzwMinCodeSize);
        var offset = 0;
        while (raster.length - offset > 255) {
          out.writeByte(255);
          out.writeBytes(raster, offset, 255);
          offset += 255;
        }
        out.writeByte(raster.length - offset);
        out.writeBytes(raster, offset, raster.length - offset);
        out.writeByte(0x00);

        //---------------------------------
        // GIF Terminator
        out.writeString(';');
      };
      var bitOutputStream = function (out) {
        var _out = out;
        var _bitLength = 0;
        var _bitBuffer = 0;
        var _this = {};
        _this.write = function (data, length) {
          if (data >>> length != 0) {
            throw 'length over';
          }
          while (_bitLength + length >= 8) {
            _out.writeByte(0xff & (data << _bitLength | _bitBuffer));
            length -= 8 - _bitLength;
            data >>>= 8 - _bitLength;
            _bitBuffer = 0;
            _bitLength = 0;
          }
          _bitBuffer = data << _bitLength | _bitBuffer;
          _bitLength = _bitLength + length;
        };
        _this.flush = function () {
          if (_bitLength > 0) {
            _out.writeByte(_bitBuffer);
          }
        };
        return _this;
      };
      var getLZWRaster = function (lzwMinCodeSize) {
        var clearCode = 1 << lzwMinCodeSize;
        var endCode = (1 << lzwMinCodeSize) + 1;
        var bitLength = lzwMinCodeSize + 1;

        // Setup LZWTable
        var table = lzwTable();
        for (var i = 0; i < clearCode; i += 1) {
          table.add(String.fromCharCode(i));
        }
        table.add(String.fromCharCode(clearCode));
        table.add(String.fromCharCode(endCode));
        var byteOut = byteArrayOutputStream();
        var bitOut = bitOutputStream(byteOut);

        // clear code
        bitOut.write(clearCode, bitLength);
        var dataIndex = 0;
        var s = String.fromCharCode(_data[dataIndex]);
        dataIndex += 1;
        while (dataIndex < _data.length) {
          var c = String.fromCharCode(_data[dataIndex]);
          dataIndex += 1;
          if (table.contains(s + c)) {
            s = s + c;
          } else {
            bitOut.write(table.indexOf(s), bitLength);
            if (table.size() < 0xfff) {
              if (table.size() == 1 << bitLength) {
                bitLength += 1;
              }
              table.add(s + c);
            }
            s = c;
          }
        }
        bitOut.write(table.indexOf(s), bitLength);

        // end code
        bitOut.write(endCode, bitLength);
        bitOut.flush();
        return byteOut.toByteArray();
      };
      var lzwTable = function () {
        var _map = {};
        var _size = 0;
        var _this = {};
        _this.add = function (key) {
          if (_this.contains(key)) {
            throw 'dup key:' + key;
          }
          _map[key] = _size;
          _size += 1;
        };
        _this.size = function () {
          return _size;
        };
        _this.indexOf = function (key) {
          return _map[key];
        };
        _this.contains = function (key) {
          return typeof _map[key] != 'undefined';
        };
        return _this;
      };
      return _this;
    };
    var createDataURL = function (width, height, getPixel) {
      var gif = gifImage(width, height);
      for (var y = 0; y < height; y += 1) {
        for (var x = 0; x < width; x += 1) {
          gif.setPixel(x, y, getPixel(x, y));
        }
      }
      var b = byteArrayOutputStream();
      gif.write(b);
      var base64 = base64EncodeOutputStream();
      var bytes = b.toByteArray();
      for (var i = 0; i < bytes.length; i += 1) {
        base64.writeByte(bytes[i]);
      }
      base64.flush();
      return 'data:image/gif;base64,' + base64;
    };

    //---------------------------------------------------------------------
    // returns qrcode function.

    return qrcode;
  }();

  // multibyte support
  !function () {
    qrcode.stringToBytesFuncs['UTF-8'] = function (s) {
      // http://stackoverflow.com/questions/18729405/how-to-convert-utf8-string-to-byte-array
      function toUTF8Array(str) {
        var utf8 = [];
        for (var i = 0; i < str.length; i++) {
          var charcode = str.charCodeAt(i);
          if (charcode < 0x80) utf8.push(charcode);else if (charcode < 0x800) {
            utf8.push(0xc0 | charcode >> 6, 0x80 | charcode & 0x3f);
          } else if (charcode < 0xd800 || charcode >= 0xe000) {
            utf8.push(0xe0 | charcode >> 12, 0x80 | charcode >> 6 & 0x3f, 0x80 | charcode & 0x3f);
          }
          // surrogate pair
          else {
            i++;
            // UTF-16 encodes 0x10000-0x10FFFF by
            // subtracting 0x10000 and splitting the
            // 20 bits of 0x0-0xFFFFF into two halves
            charcode = 0x10000 + ((charcode & 0x3ff) << 10 | str.charCodeAt(i) & 0x3ff);
            utf8.push(0xf0 | charcode >> 18, 0x80 | charcode >> 12 & 0x3f, 0x80 | charcode >> 6 & 0x3f, 0x80 | charcode & 0x3f);
          }
        }
        return utf8;
      }
      return toUTF8Array(s);
    };
  }();
  (function (factory) {
    if (typeof define === 'function' && define.amd) {
      define([], factory);
    } else if (typeof exports === 'object') {
      module.exports = factory();
    }
  })(function () {
    return qrcode;
  });
  const lib = module.exports || (typeof qrcode !== 'undefined' ? qrcode : null);
  /* o padrão da biblioteca é SJIS; a fábrica escreve em português,
     então o modo byte passa a gravar UTF-8 */
  if (lib && lib.stringToBytes) {
    lib.stringToBytesFuncs['UTF-8'] = function (s) {
      const bytes = [];
      for (const ch of String(s)) {
        const c = ch.codePointAt(0);
        if (c < 0x80) bytes.push(c);else if (c < 0x800) bytes.push(0xc0 | c >> 6, 0x80 | c & 63);else if (c < 0x10000) bytes.push(0xe0 | c >> 12, 0x80 | c >> 6 & 63, 0x80 | c & 63);else bytes.push(0xf0 | c >> 18, 0x80 | c >> 12 & 63, 0x80 | c >> 6 & 63, 0x80 | c & 63);
      }
      return bytes;
    };
    lib.stringToBytes = lib.stringToBytesFuncs['UTF-8'];
  }
  return lib;
}();

/* Desenha o código como SVG: imprime nítido em qualquer tamanho. */
function QRCode({
  texto,
  tamanho
}) {
  const lado = num(tamanho) || 140;
  const codigo = (() => {
    if (!texto) return null;
    try {
      const q = qrcode(0, 'M'); /* 0 = escolhe a versão pelo conteúdo */
      q.addData(String(texto));
      q.make();
      return q;
    } catch (e) {
      return null;
    }
  })();
  if (!codigo) {
    return /*#__PURE__*/React.createElement("div", {
      className: "qr-vazio",
      style: {
        width: lado,
        height: lado
      }
    }, /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, texto ? 'endereço longo demais' : 'sem endereço'));
  }
  const N = codigo.getModuleCount(),
    margem = 4,
    total = N + margem * 2;
  const quadrados = [];
  for (let y = 0; y < N; y++) for (let x = 0; x < N; x++) {
    if (codigo.isDark(y, x)) quadrados.push(/*#__PURE__*/React.createElement("rect", {
      key: `${y}-${x}`,
      x: x + margem,
      y: y + margem,
      width: "1",
      height: "1",
      fill: "#1a1a1a"
    }));
  }
  return /*#__PURE__*/React.createElement("svg", {
    width: lado,
    height: lado,
    viewBox: `0 0 ${total} ${total}`,
    style: {
      display: 'block',
      background: '#fff'
    },
    shapeRendering: "crispEdges"
  }, /*#__PURE__*/React.createElement("rect", {
    width: total,
    height: total,
    fill: "#fff"
  }), quadrados);
}

/* ==========================================================
   AFERIÇÃO E METAS
   O princípio: a meta nasce da capacidade comprovada, não da
   vontade de quem manda. Prova de 10 peças estima; validação de
   3 horas confirma; só então alguém propõe uma meta — e cada
   alteração fica registrada com motivo e responsável.
========================================================== */
const FASES_AFERICAO = [{
  id: 'preparacao',
  nome: 'Preparação',
  ajuda: 'ajustar a máquina e separar o material'
}, {
  id: 'prova',
  nome: 'Prova de 10',
  ajuda: 'produzir 10 peças para estimar'
}, {
  id: 'validacao',
  nome: 'Validação',
  ajuda: '3 horas em condições normais'
}, {
  id: 'analise',
  nome: 'Análise',
  ajuda: 'ler os números e propor a meta'
}, {
  id: 'concluida',
  nome: 'Concluída',
  ajuda: 'aferição encerrada'
}];
function faseAfericao(id) {
  return FASES_AFERICAO.find(f => f.id === id) || FASES_AFERICAO[0];
}

/* Quem causou a perda muda tudo: culpar o operador por falta de
   tecido é o erro que este módulo existe para evitar. */
const MOTIVOS_PARADA = [{
  id: 'falta_tecido',
  nome: 'Falta de tecido',
  origem: 'processo'
}, {
  id: 'falta_aviamento',
  nome: 'Falta de aviamento',
  origem: 'processo'
}, {
  id: 'falta_peca',
  nome: 'Falta de peça',
  origem: 'processo'
}, {
  id: 'abastecimento',
  nome: 'Falta de abastecimento',
  origem: 'processo'
}, {
  id: 'maquina_quebrada',
  nome: 'Máquina quebrada',
  origem: 'processo'
}, {
  id: 'regulagem',
  nome: 'Regulagem',
  origem: 'processo'
}, {
  id: 'manutencao',
  nome: 'Manutenção',
  origem: 'processo'
}, {
  id: 'qualidade',
  nome: 'Problema de qualidade',
  origem: 'processo'
}, {
  id: 'troca_material',
  nome: 'Troca de material',
  origem: 'processo'
}, {
  id: 'treinamento',
  nome: 'Treinamento',
  origem: 'processo'
}, {
  id: 'alteracao_produto',
  nome: 'Alteração de produto',
  origem: 'externa'
}, {
  id: 'falta_informacao',
  nome: 'Falta de informação',
  origem: 'externa'
}, {
  id: 'aguardando_aprov',
  nome: 'Aguardando aprovação',
  origem: 'externa'
}, {
  id: 'necessidade',
  nome: 'Necessidade pessoal',
  origem: 'operador'
}, {
  id: 'pausa',
  nome: 'Pausa',
  origem: 'operador'
}, {
  id: 'dificuldade',
  nome: 'Dificuldade na execução',
  origem: 'operador'
}, {
  id: 'outro',
  nome: 'Outro',
  origem: 'processo'
}];
function motivoParada(id) {
  return MOTIVOS_PARADA.find(m => m.id === id) || MOTIVOS_PARADA[MOTIVOS_PARADA.length - 1];
}
const ORIGENS_PERDA = [{
  id: 'processo',
  nome: 'Perda do processo',
  ajuda: 'material, máquina, abastecimento, qualidade, PCP'
}, {
  id: 'operador',
  nome: 'Perda do operador',
  ajuda: 'ritmo, execução, necessidade de treinamento'
}, {
  id: 'externa',
  nome: 'Perda externa',
  ajuda: 'alteração de pedido, espera de aprovação, mudança de modelo'
}];

/* Condições normais: sem elas, o número medido não representa o
   processo e a meta sai torta. */
const CONDICOES_NORMAIS = [{
  id: 'maquina',
  nome: 'Máquina adequada',
  essencial: true
}, {
  id: 'material',
  nome: 'Material disponível',
  essencial: true
}, {
  id: 'aviamentos',
  nome: 'Aviamentos disponíveis',
  essencial: true
}, {
  id: 'metodo',
  nome: 'Método definido',
  essencial: true
}, {
  id: 'treinado',
  nome: 'Operador treinado na operação',
  essencial: true
}, {
  id: 'produto',
  nome: 'Produto corretamente especificado',
  essencial: true
}, {
  id: 'qualidade',
  nome: 'Padrão de qualidade definido',
  essencial: false
}, {
  id: 'abastec',
  nome: 'Abastecimento disponível',
  essencial: false
}, {
  id: 'normais',
  nome: 'Condições normais de trabalho',
  essencial: true
}];
const ACOES_MELHORIA = ['Treinamento', 'Alteração de método', 'Regulagem', 'Troca de máquina', 'Melhoria de layout', 'Abastecimento', 'Redução de movimentação', 'Alteração de sequência', 'Melhoria de qualidade', 'Manutenção', 'Outro'];
const STATUS_META = [{
  id: 'proposta',
  nome: 'Proposta'
}, {
  id: 'validada',
  nome: 'Validada'
}, {
  id: 'suspensa',
  nome: 'Suspensa'
}];
const PECAS_PROVA = 10;
/* 3 horas é o padrão, não a regra: um lote de 40 peças acaba em 30 min,
   e esperar 3 horas para medir seria inventar produção que não existe.
   Quem conhece a operação escolhe o período. */
const MINUTOS_VALIDACAO = 180;
const PERIODOS_VALIDACAO = [{
  minutos: 30,
  nome: '30 minutos',
  ajuda: 'lote pequeno ou operação muito rápida'
}, {
  minutos: 60,
  nome: '1 hora',
  ajuda: 'o mínimo para um número estável'
}, {
  minutos: 120,
  nome: '2 horas',
  ajuda: 'operação de ciclo médio'
}, {
  minutos: 180,
  nome: '3 horas',
  ajuda: 'padrão — pega variação de ritmo e cansaço'
}, {
  minutos: 240,
  nome: '4 horas',
  ajuda: 'meio turno'
}];
/* Abaixo disto o número mede o momento, não a operação. */
const MINIMO_CONFIAVEL = 60;
function qualidadeDoPeriodo(minutos) {
  const m = num(minutos);
  if (!(m > 0)) return {
    ok: false,
    aviso: 'Informe a duração da validação.'
  };
  if (m < 30) return {
    ok: true,
    fraco: true,
    aviso: 'Período muito curto: com menos de 30 minutos o resultado reflete o ' + 'momento, não a operação. Use o número com reserva.'
  };
  if (m < MINIMO_CONFIAVEL) return {
    ok: true,
    fraco: true,
    aviso: 'Menos de 1 hora mede bem o ritmo imediato, mas não pega a variação ' + 'do turno. Serve para lote pequeno; para meta de rotina, prefira mais tempo.'
  };
  return {
    ok: true,
    fraco: false,
    aviso: ''
  };
}

/* O `agoraISO` do sistema para no minuto — serve para log, não para
   cronômetro: uma prova de 10 peças pode durar 90 segundos, e arredondar
   isso joga fora um terço da medição. Aqui o carimbo tem segundos. */
function instanteISO() {
  return new Date().toISOString();
}

/* O `minutosEntre` compara horários de jornada (HH:MM). Aqui os instantes
   são reais e podem atravessar a meia-noite. */
function minutosDecorridos(inicioISO, fimISO) {
  const a = new Date(inicioISO),
    b = new Date(fimISO || instanteISO());
  if (isNaN(a.getTime()) || isNaN(b.getTime())) return 0;
  return Math.max(0, Number(((b - a) / 60000).toFixed(2)));
}
/* Este setor mede por aferição ou pela jornada? */
function exigeAfericao(db, departamentoId){
  const d = (db.departamentos||[]).find(x=>x.id === departamentoId);
  return !!(d && d.exigeAfericao);
}

function etapaExigeAfericao(db, etapaId){
  const e = (db.etapas||[]).find(x=>x.id === etapaId);
  if(!e) return false;
  return exigeAfericao(db, e.departamentoId);
}

/* A meta de quem não afere: o que a jornada entrega. Sai dos
   apontamentos — peças boas sobre o tempo trabalhado — sem
   cronômetro, sem prova, sem assinatura. */
function metaPelaJornada(db, etapaId, opcoes){
  const op = opcoes || {};
  const aps = (db.apontamentos||[]).filter(a=>
    a.etapaId === etapaId && num(a.minutosGastos) > 0 &&
    (!op.colaboradorId || a.colaboradorId === op.colaboradorId));

  if(aps.length === 0)
    return {semDados:true, porHora:null, origem:'jornada',
      motivo:'nenhum apontamento nesta operação'};

  const boas = aps.reduce((s,a)=>s + num(a.pecasBoas), 0);
  const minutos = aps.reduce((s,a)=>s + num(a.minutosGastos), 0);
  const dias = new Set(aps.map(a=>String(a.data||'').slice(0,10))).size;

  return {
    semDados:false, origem:'jornada',
    porHora: minutos > 0 ? Number((boas / (minutos/60)).toFixed(1)) : 0,
    boas, minutos, dias,
    apontamentos: aps.length,
    /* a jornada inteira: quanto sai num dia cheio */
    porDia: minutos > 0
      ? Math.round((boas / minutos) * jornadaDe(db).produtivo) : 0,
    /* poucos dias não fazem média */
    confiavel: dias >= 3,
  };
}

function novaAfericao(d, dados, usuario) {
  if (!dados.colaboradorId) return {
    erro: 'Escolha quem vai ser aferido.'
  };
  if (!dados.etapaId) return {
    erro: 'Escolha a operação.'
  };
  const reg = {
    id: uid(),
    codigo: proximoCodigo(d.afericoes || [], 'AFR'),
    ordemId: dados.ordemId || '',
    produtoId: dados.produtoId || '',
    etapaId: dados.etapaId,
    departamentoId: dados.departamentoId || '',
    colaboradorId: dados.colaboradorId,
    equipamentoId: dados.equipamentoId || '',
    supervisorId: dados.supervisorId || '',
    data: dados.data || todayISO(),
    turno: dados.turno || '',
    fase: 'preparacao',
    preparacao: {
      inicio: '',
      fim: '',
      minutos: 0,
      regulagem: '',
      linha: '',
      agulha: '',
      acessorios: '',
      material: '',
      observacao: ''
    },
    prova: {
      inicio: '',
      fim: '',
      minutos: 0,
      quantidade: 0,
      boas: 0,
      defeito: 0,
      retrabalho: 0,
      observacao: ''
    },
    validacao: {
      inicio: '',
      fim: '',
      minutos: 0,
      horas: [],
      paradas: [],
      /* o que foi combinado antes de começar: tempo ou lote */
      minutosPlanejados: num(dados.minutosValidacao) || MINUTOS_VALIDACAO,
      modo: dados.modoValidacao || 'tempo',
      /* tempo · lote */
      loteAlvo: num(dados.loteAlvo) || 0,
      observacao: ''
    },
    condicoes: {},
    metaProposta: null,
    criadaEm: agoraISO(),
    criadaPor: usuario ? usuario.nome : ''
  };
  d.afericoes = [...(d.afericoes || []), reg];
  registrarLog(d, usuario, 'afericao.nova', `${reg.codigo}`);
  return {
    afericao: reg
  };
}
function acharAfericao(d, id) {
  return (d.afericoes || []).find(a => a.id === id) || null;
}

/* ---- fase 1: preparação ---- */
function iniciarPreparacao(d, id, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  a.preparacao = {
    ...a.preparacao,
    inicio: instanteISO(),
    fim: '',
    minutos: 0
  };
  a.fase = 'preparacao';
  return {
    afericao: a
  };
}
function finalizarPreparacao(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  if (!a.preparacao.inicio) return {
    erro: 'A preparação ainda não foi iniciada.'
  };
  const fim = instanteISO();
  a.preparacao = {
    ...a.preparacao,
    ...(dados || {}),
    fim,
    minutos: minutosDecorridos(a.preparacao.inicio, fim)
  };
  a.fase = 'prova';
  registrarLog(d, usuario, 'afericao.preparacao', `${a.codigo} · ${duracao(a.preparacao.minutos)}`);
  return {
    afericao: a
  };
}

/* ---- fase 2: prova de 10 peças ---- */
function iniciarProva(d, id) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  a.prova = {
    ...a.prova,
    inicio: instanteISO(),
    fim: '',
    minutos: 0
  };
  a.fase = 'prova';
  return {
    afericao: a
  };
}
function finalizarProva(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  if (!a.prova.inicio) return {
    erro: 'A prova ainda não foi iniciada.'
  };
  const qtd = num(dados.quantidade) || PECAS_PROVA;
  if (qtd <= 0) return {
    erro: 'Informe quantas peças foram produzidas.'
  };
  const boas = dados.boas === undefined ? qtd : num(dados.boas);
  if (boas > qtd) return {
    erro: 'As peças boas não podem passar do total produzido.'
  };
  const fim = instanteISO();
  const minutos = num(dados.minutos) > 0 ? num(dados.minutos) : minutosDecorridos(a.prova.inicio, fim);
  if (minutos <= 0) return {
    erro: 'O tempo da prova não pode ser zero.'
  };
  a.prova = {
    ...a.prova,
    fim,
    minutos,
    quantidade: qtd,
    boas,
    defeito: num(dados.defeito),
    retrabalho: num(dados.retrabalho),
    observacao: String(dados.observacao || '').trim()
  };
  a.fase = 'validacao';
  registrarLog(d, usuario, 'afericao.prova', `${a.codigo} · ${qtd} peças em ${duracao(minutos)}`);
  return {
    afericao: a
  };
}

/* Os números da prova: primeira estimativa, nunca chamada de meta. */
function resultadoProva(a) {
  const p = a && a.prova || {};
  const qtd = num(p.quantidade),
    min = num(p.minutos);
  if (!(qtd > 0 && min > 0)) return {
    vazio: true
  };
  const tempoPeca = min / qtd;
  const porHora = 60 / tempoPeca;
  return {
    vazio: false,
    quantidade: qtd,
    minutos: min,
    tempoPorPeca: Number(tempoPeca.toFixed(4)),
    capacidadeHora: Number(porHora.toFixed(1)),
    projecao3h: Math.round(porHora * 3),
    boas: num(p.boas),
    defeito: num(p.defeito),
    retrabalho: num(p.retrabalho)
  };
}

/* ---- fase 3: validação de 3 horas ---- */
function iniciarValidacao(d, id, opcoes) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  const op = opcoes || {};
  const modo = op.modo || a.validacao.modo || 'tempo';
  /* valor informado manda, mesmo que seja inválido: reaproveitar o
     combinado anterior em silêncio esconderia o erro de quem digitou */
  const informouTempo = op.minutosPlanejados !== undefined;
  const informouLote = op.loteAlvo !== undefined;
  const planejado = informouTempo ? num(op.minutosPlanejados) : num(a.validacao.minutosPlanejados) || MINUTOS_VALIDACAO;
  const lote = informouLote ? num(op.loteAlvo) : num(a.validacao.loteAlvo);
  if (modo === 'tempo' && !(planejado > 0)) return {
    erro: 'Escolha por quanto tempo a validação vai correr.'
  };
  if (modo === 'lote' && !(lote > 0)) return {
    erro: 'Informe quantas peças fecham o lote da validação.'
  };
  a.validacao = {
    ...a.validacao,
    inicio: instanteISO(),
    fim: '',
    minutos: 0,
    modo,
    minutosPlanejados: planejado,
    loteAlvo: lote
  };
  a.fase = 'validacao';
  return {
    afericao: a
  };
}
function registrarHora(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  const produzidas = num(dados.produzidas);
  if (produzidas < 0) return {
    erro: 'Quantidade inválida.'
  };
  const boas = dados.boas === undefined ? produzidas : num(dados.boas);
  if (boas > produzidas) return {
    erro: 'As peças boas não podem passar do total.'
  };
  const horas = a.validacao.horas || [];
  /* um bloco não precisa ter 60 minutos: numa validação de 30 min existe
     um bloco de 30; num lote pequeno, blocos de 15 */
  const minutosBloco = num(dados.minutos) > 0 ? num(dados.minutos) : 60;
  const reg = {
    id: dados.id || uid(),
    hora: dados.hora || horas.length + 1,
    minutos: minutosBloco,
    produzidas,
    boas,
    rejeitadas: num(dados.rejeitadas),
    retrabalho: num(dados.retrabalho),
    observacao: String(dados.observacao || '').trim(),
    quando: agoraISO()
  };
  a.validacao.horas = dados.id ? horas.map(h => h.id === reg.id ? reg : h) : [...horas, reg];
  return {
    hora: reg
  };
}
function registrarParada(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  const minutos = num(dados.minutos);
  if (minutos <= 0) return {
    erro: 'Informe quantos minutos durou a parada.'
  };
  if (!dados.motivo) return {
    erro: 'Escolha o motivo da parada.'
  };
  const mot = motivoParada(dados.motivo);
  const reg = {
    id: dados.id || uid(),
    motivo: mot.id,
    origem: dados.origem || mot.origem,
    minutos,
    hora: num(dados.hora) || null,
    observacao: String(dados.observacao || '').trim(),
    quando: agoraISO()
  };
  const paradas = a.validacao.paradas || [];
  a.validacao.paradas = dados.id ? paradas.map(p => p.id === reg.id ? reg : p) : [...paradas, reg];
  return {
    parada: reg
  };
}
function removerParada(d, id, paradaId) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  a.validacao.paradas = (a.validacao.paradas || []).filter(p => p.id !== paradaId);
  return {
    ok: true
  };
}
function finalizarValidacao(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  if ((a.validacao.horas || []).length === 0) return {
    erro: 'Registre ao menos uma hora de produção antes de fechar.'
  };
  const fim = instanteISO();
  const blocos = (a.validacao.horas || []).reduce((s, h) => s + (num(h.minutos) || 60), 0);
  a.validacao = {
    ...a.validacao,
    fim,
    minutos: num(dados && dados.minutos) > 0 ? num(dados.minutos) : a.validacao.inicio ? minutosDecorridos(a.validacao.inicio, fim) : blocos || num(a.validacao.minutosPlanejados) || MINUTOS_VALIDACAO,
    observacao: String(dados && dados.observacao || '').trim()
  };
  a.fase = 'analise';
  registrarLog(d, usuario, 'afericao.validacao', a.codigo);
  return {
    afericao: a
  };
}

/* A leitura completa: quanto produziu, quanto prestou, quanto tempo
   realmente produziu e por que deixou de produzir. */
function analiseAfericao(a) {
  const v = a && a.validacao || {};
  const horas = v.horas || [];
  const paradas = v.paradas || [];
  const prova = resultadoProva(a);
  const produzidas = horas.reduce((s, h) => s + num(h.produzidas), 0);
  const boas = horas.reduce((s, h) => s + num(h.boas), 0);
  const rejeitadas = horas.reduce((s, h) => s + num(h.rejeitadas), 0);
  const retrabalho = horas.reduce((s, h) => s + num(h.retrabalho), 0);

  /* o tempo disponível vem, nesta ordem: do cronômetro, da soma dos
     blocos apontados, do que foi combinado no início */
  const somaBlocos = horas.reduce((s, h) => s + (num(h.minutos) || 60), 0);
  const disponivel = num(v.minutos) > 0 ? num(v.minutos) : somaBlocos || num(v.minutosPlanejados) || MINUTOS_VALIDACAO;
  const paradaTotal = paradas.reduce((s, p) => s + num(p.minutos), 0);
  const produtivo = Math.max(0, disponivel - paradaTotal);
  const porOrigem = ORIGENS_PERDA.map(o => ({
    ...o,
    minutos: paradas.filter(p => p.origem === o.id).reduce((s, p) => s + num(p.minutos), 0)
  }));

  /* produção por hora medida sobre o tempo em que dava para produzir:
     descontar a parada é o que separa desempenho de circunstância */
  const porHoraBruta = disponivel > 0 ? produzidas / (disponivel / 60) : 0;
  const porHoraProdutiva = produtivo > 0 ? produzidas / (produtivo / 60) : 0;
  const boasPorHora = disponivel > 0 ? boas / (disponivel / 60) : 0;
  const confianca = qualidadeDoPeriodo(disponivel);
  return {
    vazio: horas.length === 0,
    horas,
    paradas,
    /* quanto tempo a medição durou de fato, e se dá para confiar nela */
    periodo: disponivel,
    periodoPlanejado: num(v.minutosPlanejados) || null,
    modo: v.modo || 'tempo',
    loteAlvo: num(v.loteAlvo) || 0,
    confianca,
    produzidas,
    boas,
    rejeitadas,
    retrabalho,
    qualidade: produzidas > 0 ? Number((boas / produzidas * 100).toFixed(1)) : null,
    percentualRetrabalho: produzidas > 0 ? Number((retrabalho / produzidas * 100).toFixed(1)) : null,
    disponivel,
    paradaTotal,
    produtivo,
    utilizacao: disponivel > 0 ? Number((produtivo / disponivel * 100).toFixed(1)) : null,
    porHoraBruta: Number(porHoraBruta.toFixed(1)),
    porHoraProdutiva: Number(porHoraProdutiva.toFixed(1)),
    boasPorHora: Number(boasPorHora.toFixed(1)),
    porOrigem,
    prova,
    /* a sugestão é a capacidade de peças boas por hora: o que a
       fábrica pode contar de verdade */
    metaSugerida: Number(boasPorHora.toFixed(0))
  };
}

/* ---- condições normais: sem elas a meta não vale ---- */
function conferirCondicoes(a) {
  const marcadas = a && a.condicoes || {};
  const faltando = CONDICOES_NORMAIS.filter(c => c.essencial && !marcadas[c.id]);
  return {
    ok: faltando.length === 0,
    faltando,
    marcadas: CONDICOES_NORMAIS.filter(c => marcadas[c.id]).length,
    total: CONDICOES_NORMAIS.length
  };
}

/* ---- fase 4: a meta ---- */
function proporMeta(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  const cond = conferirCondicoes(a);
  if (!cond.ok) return {
    erro: 'Meta não validada: existem condições de produção que podem ' + 'distorcer a aferição — ' + cond.faltando.map(c => c.nome.toLowerCase()).join(', ') + '.'
  };
  const valor = num(dados.valorHora);
  if (valor <= 0) return {
    erro: 'Informe a meta em peças boas por hora.'
  };
  if (!String(dados.justificativa || '').trim()) return {
    erro: 'Escreva a justificativa da meta.'
  };
  const an = analiseAfericao(a);
  const anterior = metaVigente(d, a.etapaId, a.produtoId, a.equipamentoId);
  const reg = {
    id: uid(),
    codigo: proximoCodigo(d.metasOperacionais || [], 'MET'),
    afericaoId: a.id,
    etapaId: a.etapaId,
    produtoId: a.produtoId,
    equipamentoId: a.equipamentoId,
    departamentoId: a.departamentoId,
    colaboradorId: a.colaboradorId,
    valorHora: valor,
    porTurno: num(dados.porTurno) || Math.round(valor * 8),
    capacidadeValidada: an.boasPorHora,
    /* quem ler a meta depois precisa saber em que condição ela foi medida */
    periodoValidacao: an.periodo,
    medicaoFraca: !!(an.confianca && an.confianca.fraco),
    justificativa: String(dados.justificativa).trim(),
    status: 'validada',
    validadaPor: usuario ? usuario.nome : '',
    validadaEm: agoraISO(),
    substituiu: anterior ? anterior.id : '',
    valorAnterior: anterior ? anterior.valorHora : null,
    historico: [{
      id: uid(),
      quando: agoraISO(),
      quem: usuario ? usuario.nome : '',
      o_que: 'Meta validada',
      de: anterior ? anterior.valorHora : null,
      para: valor,
      motivo: String(dados.justificativa).trim()
    }]
  };
  /* a meta anterior sai de cena, mas fica no histórico */
  if (anterior) {
    anterior.status = 'suspensa';
    anterior.historico = [...(anterior.historico || []), {
      id: uid(),
      quando: agoraISO(),
      quem: usuario ? usuario.nome : '',
      o_que: 'Substituída por nova aferição',
      motivo: `Nova meta ${reg.codigo}`
    }];
  }
  d.metasOperacionais = [...(d.metasOperacionais || []), reg];
  a.metaProposta = reg.id;
  a.fase = 'concluida';
  registrarLog(d, usuario, 'meta.validada', `${reg.codigo} · ${valor}/h${anterior ? ` (era ${anterior.valorHora}/h)` : ''}`);
  return {
    meta: reg
  };
}
function metaVigente(d, etapaId, produtoId, equipamentoId) {
  const candidatas = (d.metasOperacionais || []).filter(m => m.status === 'validada' && m.etapaId === etapaId && (!produtoId || !m.produtoId || m.produtoId === produtoId));
  if (candidatas.length === 0) return null;
  /* a mais específica primeiro: mesma máquina, mesmo produto */
  const pontos = m => (m.equipamentoId === equipamentoId ? 2 : 0) + (m.produtoId === produtoId ? 1 : 0);
  return candidatas.slice().sort((x, y) => {
    const d2 = pontos(y) - pontos(x);
    return d2 !== 0 ? d2 : String(y.validadaEm).localeCompare(String(x.validadaEm));
  })[0];
}
function alterarMeta(d, metaId, valor, motivo, usuario) {
  const m = (d.metasOperacionais || []).find(x => x.id === metaId);
  if (!m) return {
    erro: 'Meta não encontrada.'
  };
  const novo = num(valor);
  if (novo <= 0) return {
    erro: 'Valor inválido.'
  };
  if (!String(motivo || '').trim()) return {
    erro: 'Toda alteração de meta precisa de justificativa registrada.'
  };
  const de = m.valorHora;
  m.valorHora = novo;
  m.porTurno = Math.round(novo * 8);
  m.historico = [...(m.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    o_que: 'Meta alterada',
    de,
    para: novo,
    motivo: String(motivo).trim()
  }];
  registrarLog(d, usuario, 'meta.alterada', `${m.codigo} · ${de}/h → ${novo}/h`);
  return {
    meta: m
  };
}
function suspenderMeta(d, metaId, motivo, usuario) {
  const m = (d.metasOperacionais || []).find(x => x.id === metaId);
  if (!m) return {
    erro: 'Meta não encontrada.'
  };
  if (!String(motivo || '').trim()) return {
    erro: 'Informe o motivo da suspensão.'
  };
  m.status = 'suspensa';
  m.historico = [...(m.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    o_que: 'Meta suspensa',
    motivo: String(motivo).trim()
  }];
  registrarLog(d, usuario, 'meta.suspensa', `${m.codigo} · ${motivo}`);
  return {
    meta: m
  };
}

/* ---- objetivo de melhoria: separado da meta, de propósito ---- */
function salvarObjetivo(d, dados, usuario) {
  const meta = (d.metasOperacionais || []).find(m => m.id === dados.metaId);
  if (!meta) return {
    erro: 'Escolha a meta de referência.'
  };
  const alvo = num(dados.objetivoHora);
  if (alvo <= 0) return {
    erro: 'Informe o objetivo em peças por hora.'
  };
  if (alvo <= meta.valorHora) return {
    erro: 'O objetivo precisa ser maior que a meta atual — senão não é melhoria.'
  };
  const reg = {
    id: dados.id || uid(),
    codigo: dados.codigo || proximoCodigo(d.objetivosMelhoria || [], 'OBJ'),
    metaId: meta.id,
    etapaId: meta.etapaId,
    produtoId: meta.produtoId,
    metaAtual: meta.valorHora,
    objetivoHora: alvo,
    ganho: Number((alvo - meta.valorHora).toFixed(1)),
    prazo: dados.prazo || '',
    responsavelId: dados.responsavelId || '',
    acoes: dados.acoes || [],
    observacao: String(dados.observacao || '').trim(),
    status: dados.status || 'ativo',
    criadoEm: dados.criadoEm || agoraISO()
  };
  d.objetivosMelhoria = dados.id ? (d.objetivosMelhoria || []).map(o => o.id === reg.id ? reg : o) : [...(d.objetivosMelhoria || []), reg];
  registrarLog(d, usuario, 'objetivo.salvo', `${reg.codigo} · ${meta.valorHora}/h → ${alvo}/h`);
  return {
    objetivo: reg
  };
}
function salvarAcaoObjetivo(d, objetivoId, acao, usuario) {
  const o = (d.objetivosMelhoria || []).find(x => x.id === objetivoId);
  if (!o) return {
    erro: 'Objetivo não encontrado.'
  };
  if (!String(acao.oQue || '').trim()) return {
    erro: 'Descreva a ação.'
  };
  if (!acao.responsavelId) return {
    erro: 'Toda ação precisa de responsável.'
  };
  if (!acao.prazo) return {
    erro: 'Toda ação precisa de prazo.'
  };
  const reg = {
    id: acao.id || uid(),
    tipo: acao.tipo || ACOES_MELHORIA[0],
    oQue: String(acao.oQue).trim(),
    responsavelId: acao.responsavelId,
    prazo: acao.prazo,
    status: acao.status || 'nao_iniciado',
    observacao: String(acao.observacao || '').trim()
  };
  o.acoes = acao.id ? (o.acoes || []).map(x => x.id === reg.id ? reg : x) : [...(o.acoes || []), reg];
  return {
    acao: reg
  };
}

/* ---- desempenho contra a meta ---- */
function desempenhoContraMeta(db, opcoes) {
  const op = opcoes || {};
  const linhas = [];
  (db.apontamentos || []).forEach(ap => {
    if (op.de && String(ap.data || '') < op.de) return;
    if (op.ate && String(ap.data || '') > op.ate) return;
    const et = (db.etapas || []).find(e => e.nome === ap.etapa);
    if (!et) return;
    const meta = metaVigente(db, et.id, ap.produtoId, ap.equipamentoId);
    if (!meta) return;
    const minutos = num(ap.minutosGastos);
    if (minutos <= 0) return;
    const boasPorHora = num(ap.pecasBoas) / (minutos / 60);
    linhas.push({
      apontamento: ap,
      colaboradorId: ap.colaboradorId,
      colaborador: ap.colaborador,
      etapa: ap.etapa,
      data: ap.data,
      meta: meta.valorHora,
      real: Number(boasPorHora.toFixed(1)),
      eficiencia: Number((boasPorHora / meta.valorHora * 100).toFixed(1)),
      qualidade: num(ap.pecasBoas) + num(ap.pecasDefeito) > 0 ? Number((num(ap.pecasBoas) / (num(ap.pecasBoas) + num(ap.pecasDefeito)) * 100).toFixed(1)) : null
    });
  });
  return linhas;
}
function faixaEficiencia(pct) {
  if (pct === null || pct === undefined) return {
    marca: '—',
    nome: 'sem meta'
  };
  if (pct >= 100) return {
    marca: '🟢',
    nome: 'na meta ou acima'
  };
  if (pct >= 90) return {
    marca: '🟡',
    nome: 'perto da meta'
  };
  return {
    marca: '🔴',
    nome: 'abaixo da meta'
  };
}

/* ---- alertas: onde o número pode estar mentindo ---- */
function alertasAfericao(db) {
  const avisos = [];
  (db.metasOperacionais || []).filter(m => m.status === 'validada').forEach(m => {
    const a = (db.afericoes || []).find(x => x.id === m.afericaoId);
    const cap = num(m.capacidadeValidada);
    if (cap > 0 && m.valorHora > cap * 1.1) avisos.push({
      tipo: 'meta_acima',
      grave: true,
      texto: `Meta ${m.codigo} está ${Math.round((m.valorHora / cap - 1) * 100)}% acima da ` + `capacidade validada (${cap.toFixed(1)}/h).`
    });
    if (a) {
      const an = analiseAfericao(a);
      if (an.qualidade !== null && an.qualidade < 90) avisos.push({
        tipo: 'qualidade',
        grave: true,
        texto: `Aferição ${a.codigo} fechou com ${an.qualidade}% de qualidade — ` + `meta feita sobre produção que não passou na revisão.`
      });
      if (an.percentualRetrabalho !== null && an.percentualRetrabalho > 10) avisos.push({
        tipo: 'retrabalho',
        grave: false,
        texto: `Aferição ${a.codigo} teve ${an.percentualRetrabalho}% de retrabalho.`
      });
      const externa = (an.porOrigem.find(o => o.id === 'externa') || {}).minutos || 0;
      if (an.disponivel > 0 && externa / an.disponivel > 0.15) avisos.push({
        tipo: 'parada_externa',
        grave: false,
        texto: `Aferição ${a.codigo} perdeu ${duracao(externa)} por causa externa — ` + `o número mede a espera, não o operador.`
      });
    }
  });
  /* operação com apontamento mas sem aferição: o PCP planeja no escuro */
  const comApontamento = new Set((db.apontamentos || []).map(ap => ap.etapa));
  comApontamento.forEach(nome => {
    const et = (db.etapas || []).find(e => e.nome === nome);
    if (!et) return;
    if (!metaVigente(db, et.id)) avisos.push({
      tipo: 'sem_afericao',
      grave: false,
      texto: `A operação ${nome} tem apontamento mas nenhuma meta aferida.`
    });
  });
  return avisos;
}

/* ---- o que o PCP ganha com isso ---- */
function capacidadePlanejavel(db, etapaId, horasDia) {
  const meta = metaVigente(db, etapaId);
  if (!meta) return null;
  const horas = num(horasDia) > 0 ? num(horasDia) : jornadaDe(db).produtivo / 60;
  return {
    meta,
    porHora: meta.valorHora,
    porDia: Math.round(meta.valorHora * horas),
    horas: Number(horas.toFixed(2))
  };
}

function indicadoresAfericao(db, opcoes) {
  const op = opcoes || {};
  let afs = (db.afericoes || []).slice();
  if (op.de) afs = afs.filter(a => String(a.data || '') >= op.de);
  if (op.ate) afs = afs.filter(a => String(a.data || '') <= op.ate);
  const metas = db.metasOperacionais || [];
  const objetivos = db.objetivosMelhoria || [];
  const analises = afs.filter(a => a.fase === 'analise' || a.fase === 'concluida').map(analiseAfericao).filter(x => !x.vazio);
  const media = (lista, campo) => {
    const vals = lista.map(x => x[campo]).filter(v => v !== null && v !== undefined);
    return vals.length ? Number((vals.reduce((a, b) => a + b, 0) / vals.length).toFixed(1)) : null;
  };
  const somaOrigem = id => analises.reduce((s, an) => s + ((an.porOrigem.find(o => o.id === id) || {}).minutos || 0), 0);
  return {
    afericoes: afs.length,
    emAndamento: afs.filter(a => a.fase !== 'concluida').length,
    concluidas: afs.filter(a => a.fase === 'concluida').length,
    operadores: new Set(afs.map(a => a.colaboradorId)).size,
    operacoes: new Set(afs.map(a => a.etapaId)).size,
    metasValidadas: metas.filter(m => m.status === 'validada').length,
    metasSuspensas: metas.filter(m => m.status === 'suspensa').length,
    objetivosAtivos: objetivos.filter(o => o.status === 'ativo').length,
    objetivosAtingidos: objetivos.filter(o => o.status === 'atingido').length,
    produtividadeMedia: media(analises, 'boasPorHora'),
    qualidadeMedia: media(analises, 'qualidade'),
    retrabalhoMedio: media(analises, 'percentualRetrabalho'),
    utilizacaoMedia: media(analises, 'utilizacao'),
    perdaProcesso: somaOrigem('processo'),
    perdaOperador: somaOrigem('operador'),
    perdaExterna: somaOrigem('externa'),
    alertas: alertasAfericao(db)
  };
}

/* O papel que sai da aferição e volta assinado. Duas assinaturas:
   quem conduziu a medição e quem foi medido. A do colaborador não é
   concordância com a meta — é ciência de que ele viu os números que
   saíram do trabalho dele. */
function registrarAssinatura(d, id, dados, usuario) {
  const a = acharAfericao(d, id);
  if (!a) return {
    erro: 'Aferição não encontrada.'
  };
  const papel = dados.papel;
  if (papel !== 'supervisor' && papel !== 'colaborador') return {
    erro: 'Informe quem está assinando.'
  };
  const nome = String(dados.nome || '').trim();
  if (!nome) return {
    erro: 'Informe o nome de quem assinou.'
  };
  a.assinaturas = {
    ...(a.assinaturas || {}),
    [papel]: {
      nome,
      /* o que a pessoa assinou fica preso ao número daquele momento:
         se a aferição for refeita, a assinatura antiga não vale para
         o número novo */
      capacidade: analiseAfericao(a).boasPorHora,
      observacao: String(dados.observacao || '').trim(),
      registradoPor: usuario ? usuario.nome : '',
      em: agoraISO()
    }
  };
  registrarLog(d, usuario, 'afericao.assinatura', `${a.codigo} · ${papel} · ${nome}`);
  return {
    afericao: a
  };
}

/* Tudo que o relatório precisa mostrar, num lugar só. */
function relatorioAfericao(a, db) {
  const an = analiseAfericao(a);
  const prova = resultadoProva(a);
  const cond = conferirCondicoes(a);
  const col = (db.colaboradores || []).find(c => c.id === a.colaboradorId);
  const sup = (db.colaboradores || []).find(c => c.id === a.supervisorId);
  const et = (db.etapas || []).find(e => e.id === a.etapaId);
  const dp = (db.departamentos || []).find(x => x.id === a.departamentoId);
  const eq = (db.equipamentos || []).find(x => x.id === a.equipamentoId);
  const pr = (db.produtos || []).find(p => p.id === a.produtoId);
  const ordem = (db.ordens || []).find(o => o.id === a.ordemId);
  const meta = (db.metasOperacionais || []).find(m => m.id === a.metaProposta);
  const assin = a.assinaturas || {};

  /* se a aferição mudou depois de assinada, a assinatura não cobre o
     número atual — e o relatório precisa dizer isso */
  const desatualizada = p => p && num(p.capacidade) !== an.boasPorHora;
  return {
    afericao: a,
    analise: an,
    prova,
    condicoes: cond,
    colaborador: col ? col.nome : '—',
    supervisor: sup ? sup.nome : '',
    operacao: et ? et.nome : '—',
    setor: dp ? dp.nome : '—',
    maquina: eq ? eq.nome : '',
    produto: pr ? montarNomeProduto(pr, db) || pr.codigo : '',
    ordem: ordem ? ordem.codigo : '',
    meta,
    assinaturas: assin,
    supervisorDesatualizado: desatualizada(assin.supervisor),
    colaboradorDesatualizado: desatualizada(assin.colaborador),
    completo: !!(assin.supervisor && assin.colaborador)
  };
}

/* Histórico do operador e do processo: dois olhares sobre o mesmo dado. */
function historicoOperador(db, colaboradorId) {
  return (db.afericoes || []).filter(a => a.colaboradorId === colaboradorId && (a.fase === 'analise' || a.fase === 'concluida')).map(a => {
    const an = analiseAfericao(a);
    const et = (db.etapas || []).find(e => e.id === a.etapaId);
    const pr = (db.produtos || []).find(p => p.id === a.produtoId);
    const meta = (db.metasOperacionais || []).find(m => m.id === a.metaProposta);
    return {
      afericao: a,
      data: a.data,
      etapa: et ? et.nome : '—',
      produto: pr ? montarNomeProduto(pr, db) || pr.codigo : '—',
      resultado: an.boasPorHora,
      qualidade: an.qualidade,
      meta: meta ? meta.valorHora : null
    };
  }).sort((a, b) => String(b.data).localeCompare(String(a.data)));
}
function historicoOperacao(db, etapaId) {
  return (db.afericoes || []).filter(a => a.etapaId === etapaId && (a.fase === 'analise' || a.fase === 'concluida')).map(a => {
    const an = analiseAfericao(a);
    const col = (db.colaboradores || []).find(c => c.id === a.colaboradorId);
    const eq = (db.equipamentos || []).find(e => e.id === a.equipamentoId);
    return {
      afericao: a,
      data: a.data,
      colaborador: col ? col.nome : '—',
      equipamento: eq ? eq.nome : '—',
      resultado: an.boasPorHora,
      qualidade: an.qualidade
    };
  }).sort((a, b) => String(b.data).localeCompare(String(a.data)));
}

/* ==========================================================
   ENGENHARIA DO PRODUTO
   O produto deixa de ser um cadastro solto e passa a ter
   maturidade: rascunho enquanto se desenha, liberado quando a
   engenharia fecha, e uma versão congelada a cada mudança que
   afeta produção — para que a ordem de ontem continue contando
   a verdade de ontem.
========================================================== */
const STATUS_PRODUTO = [{
  id: 'rascunho',
  nome: 'Rascunho',
  produz: false,
  amostra: true,
  ajuda: 'em cadastro, ainda incompleto'
}, {
  id: 'desenvolvimento',
  nome: 'Em desenvolvimento',
  produz: false,
  amostra: true,
  ajuda: 'engenharia sendo montada'
}, {
  id: 'validacao',
  nome: 'Em validação',
  produz: false,
  amostra: true,
  ajuda: 'aguardando aprovação da amostra'
}, {
  id: 'liberado',
  nome: 'Liberado para produção',
  produz: true,
  amostra: true,
  ajuda: 'pode abrir ordem normalmente'
}, {
  id: 'bloqueado',
  nome: 'Bloqueado',
  produz: false,
  amostra: false,
  ajuda: 'produção suspensa'
}, {
  id: 'obsoleto',
  nome: 'Obsoleto',
  produz: false,
  amostra: false,
  ajuda: 'não se produz mais'
}];
function statusProduto(id) {
  return STATUS_PRODUTO.find(s => s.id === id) || STATUS_PRODUTO[0];
}
/* produto de antes desta versão não tinha status: se tem processo e
   tecido, estava em uso — tratar como liberado seria mentir, mas
   travar a fábrica por causa de um campo novo seria pior */
function statusDoProduto(produto) {
  if (!produto) return 'rascunho';
  if (produto.status) return produto.status;
  return (produto.processo || []).length > 0 ? 'liberado' : 'rascunho';
}

/* O que a engenharia precisa ter. Obrigatório é o que impede produzir;
   o resto é qualidade de cadastro, que o sistema mostra mas não trava. */
const ITENS_ENGENHARIA = [{
  id: 'identidade',
  nome: 'Identificação',
  obrigatorio: true,
  confere: (p, db) => {
    const falta = [];
    if (!String(p.codigo || '').trim()) falta.push('código');
    if (!p.grupoId) falta.push('grupo');
    return falta.length ? {
      ok: false,
      falta
    } : {
      ok: true
    };
  }
}, {
  id: 'medidas',
  nome: 'Grade e medidas',
  obrigatorio: false,
  confere: p => {
    const m = p.medida || {};
    if (m.formato === 'grade' && (m.tamanhos || []).length === 0) return {
      ok: false,
      falta: ['tamanhos da grade']
    };
    return {
      ok: true
    };
  }
}, {
  id: 'tecidos',
  nome: 'Tecidos e materiais',
  obrigatorio: true,
  confere: p => {
    if ((p.tecidos || []).length === 0) return {
      ok: false,
      falta: ['nenhum material']
    };
    return {
      ok: true
    };
  }
}, {
  id: 'modelagem',
  nome: 'Modelagem',
  obrigatorio: false,
  confere: (p, db) => {
    const cortaveis = (p.tecidos || []).filter(t => {
      const m = (db.materiais || []).find(x => x.id === t.materialId);
      return m && ehTecido(m, db);
    });
    if (cortaveis.length === 0) return {
      ok: true,
      vazio: true
    };
    const sem = cortaveis.filter(t => !t.riscoId);
    return sem.length ? {
      ok: false,
      falta: [`${sem.length} tecido(s) sem modelagem`]
    } : {
      ok: true
    };
  }
}, {
  id: 'componentes',
  nome: 'Componentes e partes',
  obrigatorio: false,
  confere: (p, db) => {
    const sp = (db.subprodutos || []).filter(x => x.produtoId === p.id);
    return sp.length ? {
      ok: true
    } : {
      ok: true,
      vazio: true
    };
  }
}, {
  id: 'roteiro',
  nome: 'Roteiro de produção',
  obrigatorio: true,
  confere: p => {
    if ((p.processo || []).length === 0) return {
      ok: false,
      falta: ['nenhuma operação']
    };
    const semEtapa = (p.processo || []).filter(e => !e.etapaId);
    return semEtapa.length ? {
      ok: false,
      falta: [`${semEtapa.length} operação(ões) sem etapa`]
    } : {
      ok: true
    };
  }
}, {
  id: 'tempos',
  nome: 'Tempos das operações',
  obrigatorio: true,
  confere: p => {
    const sem = (p.processo || []).filter(e => !(num(e.tempo) > 0));
    return sem.length ? {
      ok: false,
      falta: [`${sem.length} operação(ões) sem tempo`]
    } : {
      ok: true
    };
  }
}, {
  id: 'consumo',
  nome: 'Consumo dos materiais',
  obrigatorio: true,
  confere: (p, db) => {
    const c = custearProduto(p, db);
    const semConsumo = (c.porMaterial || []).filter(g => g.indefinido);
    return semConsumo.length ? {
      ok: false,
      falta: [`${semConsumo.length} material(is) sem consumo definido`]
    } : {
      ok: true
    };
  }
}, {
  id: 'custo',
  nome: 'Custo apurado',
  obrigatorio: false,
  confere: (p, db) => {
    const c = custearProduto(p, db);
    return c.custoTecido > 0 ? {
      ok: true
    } : {
      ok: false,
      falta: ['custo de material zerado']
    };
  }
}, {
  id: 'capacidade',
  nome: 'Capacidade aferida',
  obrigatorio: false,
  confere: (p, db) => {
    const ops = (p.processo || []).map(e => e.etapaId).filter(Boolean);
    if (ops.length === 0) return {
      ok: true,
      vazio: true
    };
    const sem = ops.filter(id => !metaVigente(db, id, p.id));
    return sem.length ? {
      ok: false,
      falta: [`${sem.length} de ${ops.length} operação(ões) sem meta aferida`]
    } : {
      ok: true
    };
  }
}];
function checklistEngenharia(produto, db) {
  if (!produto) return {
    itens: [],
    pronto: false,
    obrigatoriosOk: false
  };
  const itens = ITENS_ENGENHARIA.map(it => {
    let r;
    try {
      r = it.confere(produto, db) || {
        ok: false
      };
    } catch (e) {
      r = {
        ok: false,
        falta: ['não foi possível conferir']
      };
    }
    return {
      ...it,
      ...r,
      falta: r.falta || []
    };
  });
  const obrigatorios = itens.filter(i => i.obrigatorio);
  const pendentesObrig = obrigatorios.filter(i => !i.ok);
  return {
    itens,
    obrigatoriosOk: pendentesObrig.length === 0,
    pendentes: itens.filter(i => !i.ok),
    pendentesObrigatorios: pendentesObrig,
    completos: itens.filter(i => i.ok && !i.vazio).length,
    total: itens.length,
    /* quanto da engenharia está de pé, para a barra de progresso */
    percentual: Math.round(itens.filter(i => i.ok).length / itens.length * 100),
    pronto: pendentesObrig.length === 0
  };
}

/* Mudar o status é decisão de gente, mas liberar exige que a
   engenharia obrigatória esteja de pé. */
function mudarStatusProduto(d, produtoId, novo, motivo, usuario) {
  const p = (d.produtos || []).find(x => x.id === produtoId);
  if (!p) return {
    erro: 'Produto não encontrado.'
  };
  if (!STATUS_PRODUTO.some(s => s.id === novo)) return {
    erro: 'Situação inválida.'
  };
  if (novo === 'liberado') {
    const ck = checklistEngenharia(p, d);
    if (!ck.obrigatoriosOk) return {
      erro: 'A engenharia ainda não está completa: ' + ck.pendentesObrigatorios.map(i => i.nome.toLowerCase()).join(', ') + '.'
    };
  }
  if ((novo === 'bloqueado' || novo === 'obsoleto') && !String(motivo || '').trim()) return {
    erro: 'Informe o motivo — quem for procurar este produto depois ' + 'precisa saber por que ele saiu de linha.'
  };
  const antes = statusDoProduto(p);
  p.status = novo;
  p.historicoStatus = [...(p.historicoStatus || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    de: antes,
    para: novo,
    motivo: String(motivo || '').trim()
  }];
  registrarLog(d, usuario, 'produto.status', `${p.codigo} · ${statusProduto(antes).nome} → ${statusProduto(novo).nome}`);
  return {
    produto: p
  };
}

/* ---------------- Versão da engenharia ----------------
   Congelar é o que impede que mudar o tecido hoje reescreva o custo
   da ordem de três meses atrás. */
function retratoDaEngenharia(produto, db) {
  const c = custearProduto(produto, db);
  const r = resumoProcesso(produto, db);
  return {
    tecidos: (produto.tecidos || []).map(t => {
      const m = (db.materiais || []).find(x => x.id === t.materialId);
      const risco = (db.riscos || []).find(x => x.id === t.riscoId);
      return {
        id: t.id,
        materialId: t.materialId,
        material: m ? m.nome : 'material removido',
        apelido: t.apelido || '',
        riscoId: t.riscoId || '',
        modelagem: risco ? risco.codigo : '',
        quantidade: num(t.quantidade),
        perda: num(t.perda)
      };
    }),
    processo: (produto.processo || []).map((e, i) => {
      const et = (db.etapas || []).find(x => x.id === e.etapaId);
      const dp = (db.departamentos || []).find(x => x.id === e.departamentoId);
      return {
        ordem: i + 1,
        etapaId: e.etapaId,
        etapa: et ? et.nome : 'etapa removida',
        setor: dp ? dp.nome : '',
        modo: e.modo,
        tempo: e.tempo,
        pessoas: num(e.pessoas),
        minutosPorPeca: (r.passos[i] || {}).minutosHomem || 0
      };
    }),
    subprodutos: (db.subprodutos || []).filter(x => x.produtoId === produto.id).map(x => ({
      id: x.id,
      codigo: x.codigo,
      nome: x.nome,
      porProduto: num(x.porProduto),
      consumo: num(x.consumo)
    })),
    medida: produto.medida || {},
    ficha: produto.ficha || {},
    custoMaterial: c.custoTecido,
    consumoTotal: c.consumoTotal,
    minutosPorPeca: num(r.minutosHomem)
  };
}
function versoesDoProduto(db, produtoId) {
  return (db.versoesProduto || []).filter(v => v.produtoId === produtoId).sort((a, b) => num(b.numero) - num(a.numero));
}
function versaoVigente(db, produtoId) {
  return versoesDoProduto(db, produtoId)[0] || null;
}
function congelarVersao(d, produtoId, dados, usuario) {
  const p = (d.produtos || []).find(x => x.id === produtoId);
  if (!p) return {
    erro: 'Produto não encontrado.'
  };
  const motivo = String((dados || {}).motivo || '').trim();
  const anterior = versaoVigente(d, produtoId);
  if (anterior && !motivo) return {
    erro: 'Descreva o que mudou desta versão para a próxima — ' + 'é o que explica, meses depois, por que o custo mudou.'
  };
  const retrato = retratoDaEngenharia(p, d);
  const numero = anterior ? num(anterior.numero) + 1 : 1;
  const reg = {
    id: uid(),
    produtoId,
    numero,
    codigo: `V${String(numero).padStart(2, '0')}`,
    motivo,
    /* o que mudou de verdade, comparado com a versão anterior */
    mudancas: anterior ? compararEngenharia(anterior.retrato, retrato) : [],
    retrato,
    statusNaEpoca: statusDoProduto(p),
    criadaEm: agoraISO(),
    criadaPor: usuario ? usuario.nome : ''
  };
  d.versoesProduto = [...(d.versoesProduto || []), reg];
  registrarLog(d, usuario, 'produto.versao', `${p.codigo} · ${reg.codigo}${motivo ? ' · ' + motivo : ''}`);
  return {
    versao: reg
  };
}

/* O que mudou entre duas versões, em linguagem de gente. */
function compararEngenharia(antes, agora) {
  const mudancas = [];
  if (!antes || !agora) return mudancas;
  const nomes = l => (l || []).map(x => x.material || x.etapa || x.nome).join(' · ');
  if (nomes(antes.tecidos) !== nomes(agora.tecidos)) mudancas.push({
    campo: 'Materiais',
    de: nomes(antes.tecidos) || 'nenhum',
    para: nomes(agora.tecidos) || 'nenhum'
  });
  if (nomes(antes.processo) !== nomes(agora.processo)) mudancas.push({
    campo: 'Roteiro',
    de: `${(antes.processo || []).length} operação(ões)`,
    para: `${(agora.processo || []).length} operação(ões)`
  });
  const dif = (rot, a, b, sufixo) => {
    const x = num(a),
      y = num(b);
    if (Math.abs(x - y) > 0.0001) mudancas.push({
      campo: rot,
      de: x.toFixed(4) + (sufixo || ''),
      para: y.toFixed(4) + (sufixo || ''),
      variacao: x > 0 ? Number(((y / x - 1) * 100).toFixed(1)) : null
    });
  };
  dif('Custo de material', antes.custoMaterial, agora.custoMaterial);
  dif('Consumo total', antes.consumoTotal, agora.consumoTotal);
  dif('Tempo por peça', antes.minutosPorPeca, agora.minutosPorPeca, ' min');
  (agora.processo || []).forEach(e => {
    const velha = (antes.processo || []).find(x => x.etapaId === e.etapaId);
    if (velha && Math.abs(num(velha.minutosPorPeca) - num(e.minutosPorPeca)) > 0.0001) mudancas.push({
      campo: `Tempo · ${e.etapa}`,
      de: duracao(velha.minutosPorPeca),
      para: duracao(e.minutosPorPeca)
    });
  });
  return mudancas;
}

/* A engenharia mudou desde a última versão congelada? */
function engenhariaMudou(produto, db) {
  const v = versaoVigente(db, produto.id);
  if (!v) return {
    mudou: true,
    semVersao: true,
    mudancas: []
  };
  const mudancas = compararEngenharia(v.retrato, retratoDaEngenharia(produto, db));
  return {
    mudou: mudancas.length > 0,
    semVersao: false,
    mudancas,
    versao: v
  };
}

/* ==========================================================
   PRODUÇÃO REAL — o elo entre o que foi planejado e o que
   aconteceu de fato.
   O apontamento deixa de ser um registro solto e passa a
   apontar para a ordem e para a etapa dela. Sem esse vínculo
   não dá para saber quanto falta, onde travou, nem quando
   termina.
========================================================== */

/* Um apontamento pertence a uma etapa de uma ordem. O formato
   antigo (só data, colaborador e nome da etapa) continua válido:
   o sistema lê os dois. */
function apontamentosDaOrdem(db, ordemId) {
  return (db.apontamentos || []).filter(ap => ap.ordemId === ordemId);
}
function apontamentosDaTarefa(db, ordemId, tarefaId) {
  return (db.apontamentos || []).filter(ap => ap.ordemId === ordemId && ap.tarefaId === tarefaId);
}

/* Quanto cada etapa já entregou. É daqui que sai o estoque em
   processo: o que o corte produziu e a costura ainda não consumiu. */
function avancoDaOrdem(ordem, db) {
  const produto = (db.produtos || []).find(p => p.id === ordem.produtoId);
  const meta = ordem.quantidade;
  const etapas = (ordem.tarefas || []).map((t, i) => {
    const et = (db.etapas || []).find(e => e.id === t.etapaId);
    const dp = (db.departamentos || []).find(d => d.id === t.departamentoId);
    const aps = apontamentosDaTarefa(db, ordem.id, t.id);
    const produzidas = aps.reduce((s, ap) => s + num(ap.pecasBoas) + num(ap.pecasDefeito), 0);
    const boas = aps.reduce((s, ap) => s + num(ap.pecasBoas), 0);
    const defeito = aps.reduce((s, ap) => s + num(ap.pecasDefeito), 0);
    const retrabalho = aps.reduce((s, ap) => s + num(ap.retrabalho), 0);
    const minutos = aps.reduce((s, ap) => s + num(ap.minutosGastos), 0);
    const parada = aps.reduce((s, ap) => s + (ap.paradas || []).reduce((x, p) => x + num(p.minutos), 0), 0);

    /* a etapa só pode processar o que a anterior entregou */
    const disponivel = i === 0 ? meta : null; /* preenchido no laço abaixo */
    return {
      tarefa: t,
      ordem: i + 1,
      etapa: et ? et.nome : 'etapa removida',
      setor: dp ? dp.nome : '—',
      apontamentos: aps,
      planejado: meta,
      produzidas,
      boas,
      defeito,
      retrabalho,
      minutos,
      parada,
      disponivel,
      saldo: Math.max(0, meta - boas),
      concluida: !!t.concluida || boas >= meta,
      qualidade: produzidas > 0 ? Number((boas / produzidas * 100).toFixed(1)) : null,
      porHora: minutos > 0 ? Number((boas / (minutos / 60)).toFixed(1)) : null,
      meta: metaVigente(db, t.etapaId, ordem.produtoId, t.equipamentoId)
    };
  });

  /* o que cada etapa tem em mãos vem da etapa anterior */
  etapas.forEach((e, i) => {
    e.disponivel = i === 0 ? meta : etapas[i - 1].boas;
    e.emEspera = Math.max(0, e.disponivel - e.produzidas);
    /* etapa parada com fila na frente é onde o fluxo trava */
    e.travada = e.emEspera > 0 && e.produzidas === 0 && i > 0 && etapas[i - 1].boas > 0;
  });
  const ultima = etapas[etapas.length - 1];
  return {
    produto,
    etapas,
    planejado: meta,
    concluido: ultima ? ultima.boas : 0,
    percentual: meta > 0 && ultima ? Number((ultima.boas / meta * 100).toFixed(1)) : 0,
    /* estoque em processo: o que já entrou na linha e não saiu */
    emProcesso: etapas.length > 1 ? etapas.slice(0, -1).reduce((s, e, i) => s + Math.max(0, e.boas - (etapas[i + 1] ? etapas[i + 1].produzidas : 0)), 0) : 0,
    totalMinutos: etapas.reduce((s, e) => s + e.minutos, 0),
    totalParada: etapas.reduce((s, e) => s + e.parada, 0),
    totalBoas: etapas.reduce((s, e) => s + e.boas, 0),
    totalDefeito: etapas.reduce((s, e) => s + e.defeito, 0),
    totalRetrabalho: etapas.reduce((s, e) => s + e.retrabalho, 0)
  };
}

/* A projeção original: quanto tempo a ordem deveria levar, pelas
   capacidades que a fábrica comprovou. Sem meta aferida, cai para o
   tempo estimado do roteiro — e diz que está estimando. */
function projecaoDaOrdem(ordem, db, opcoes) {
  const op = opcoes || {};
  const jornada = jornadaDe(db);
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : jornada.produtivo;
  const qtd = num(ordem.quantidade);
  const etapas = (ordem.tarefas || []).map(t => {
    const et = (db.etapas || []).find(e => e.id === t.etapaId);
    const meta = metaVigente(db, t.etapaId, ordem.produtoId, t.equipamentoId);
    const pessoas = Math.max(1, (t.colaboradorIds || []).length || num(t.pessoas) || 1);
    /* peças por hora: da meta aferida, ou do tempo do roteiro */
    const porHora = meta ? meta.valorHora * pessoas : num(t.minutosPorPeca) > 0 ? 60 / num(t.minutosPorPeca) * pessoas : 0;
    const horas = porHora > 0 ? qtd / porHora : null;
    return {
      tarefa: t,
      etapa: et ? et.nome : 'etapa removida',
      aferida: !!meta,
      porHora: Number(porHora.toFixed(1)),
      pessoas,
      horas: horas === null ? null : Number(horas.toFixed(2)),
      minutos: horas === null ? null : Number((horas * 60).toFixed(1))
    };
  });
  const semCapacidade = etapas.filter(e => e.horas === null);
  const totalMinutos = etapas.reduce((s, e) => s + (e.minutos || 0), 0);
  const gargalo = etapas.filter(e => e.porHora > 0).sort((a, b) => a.porHora - b.porHora)[0] || null;
  return {
    etapas,
    aferidas: etapas.filter(e => e.aferida).length,
    estimadas: etapas.filter(e => !e.aferida && e.horas !== null).length,
    semCapacidade,
    totalMinutos: Number(totalMinutos.toFixed(1)),
    totalHoras: Number((totalMinutos / 60).toFixed(2)),
    dias: minutosDia > 0 ? Number((totalMinutos / minutosDia).toFixed(2)) : null,
    minutosDia,
    gargalo,
    /* a confiança da projeção depende de quantas etapas foram medidas */
    confiavel: semCapacidade.length === 0 && etapas.length > 0 && etapas.every(e => e.aferida)
  };
}

/* A projeção que se corrige sozinha: o ritmo real substitui a
   estimativa conforme os apontamentos chegam. */
function projecaoAtual(ordem, db, opcoes) {
  const original = projecaoDaOrdem(ordem, db, opcoes);
  const av = avancoDaOrdem(ordem, db);
  const minutosDia = original.minutosDia;
  const etapas = original.etapas.map(e => {
    const real = av.etapas.find(x => x.tarefa.id === e.tarefa.id);
    const feito = real ? real.boas : 0;
    const falta = Math.max(0, num(ordem.quantidade) - feito);
    /* ritmo real só vale depois de produzir algo de verdade */
    const ritmoReal = real && real.porHora !== null && feito > 0 ? real.porHora * e.pessoas : null;
    const ritmo = ritmoReal !== null ? ritmoReal : e.porHora;
    const minutosRestantes = ritmo > 0 ? falta / ritmo * 60 : null;
    return {
      ...e,
      feito,
      falta,
      ritmoReal: ritmoReal === null ? null : Number(ritmoReal.toFixed(1)),
      ritmoUsado: Number(ritmo.toFixed(1)),
      minutosRestantes: minutosRestantes === null ? null : Number(minutosRestantes.toFixed(1)),
      desvioRitmo: ritmoReal !== null && e.porHora > 0 ? Number(((ritmoReal / e.porHora - 1) * 100).toFixed(1)) : null
    };
  });
  const restante = etapas.reduce((s, e) => s + (e.minutosRestantes || 0), 0);
  const gasto = av.totalMinutos;
  const totalAtual = gasto + restante;
  return {
    original,
    avanco: av,
    etapas,
    minutosGastos: Number(gasto.toFixed(1)),
    minutosRestantes: Number(restante.toFixed(1)),
    totalAtual: Number(totalAtual.toFixed(1)),
    desvioMinutos: Number((totalAtual - original.totalMinutos).toFixed(1)),
    desvioPercentual: original.totalMinutos > 0 ? Number(((totalAtual / original.totalMinutos - 1) * 100).toFixed(1)) : null,
    diasRestantes: minutosDia > 0 ? Number((restante / minutosDia).toFixed(2)) : null,
    comecou: gasto > 0
  };
}

/* Data prevista de término considerando só os dias em que a fábrica
   trabalha — prometer sábado quando não se produz no sábado é
   prometer errado. */
function previsaoTermino(ordem, db, opcoes) {
  const p = projecaoAtual(ordem, db, opcoes);
  if (p.minutosRestantes <= 0) return {
    concluida: true,
    data: null,
    dias: 0
  };
  const minutosDia = p.original.minutosDia;
  if (!(minutosDia > 0)) return {
    concluida: false,
    data: null,
    dias: null
  };
  let faltam = p.minutosRestantes;
  const dia = new Date(todayISO());
  let contados = 0,
    guarda = 0;
  while (faltam > 0 && guarda < 400) {
    guarda++;
    dia.setDate(dia.getDate() + 1);
    const semana = dia.getDay();
    if (semana === 0 || semana === 6) continue; /* fim de semana */
    faltam -= minutosDia;
    contados++;
  }
  const data = dia.toISOString().slice(0, 10);
  const atrasada = !!ordem.entrega && data > ordem.entrega;
  return {
    concluida: false,
    data,
    dias: contados,
    atrasada,
    diasDeAtraso: atrasada ? Math.round((new Date(data) - new Date(ordem.entrega)) / 86400000) : 0
  };
}

/* Real × projetado, com a conta que interessa: de onde veio a
   diferença. Atribuir o desvio inteiro ao operador é o erro que
   este cálculo existe para impedir. */
function realVersusProjetado(ordem, db, opcoes) {
  const p = projecaoAtual(ordem, db, opcoes);
  const av = p.avanco;
  const aps = apontamentosDaOrdem(db, ordem.id);

  /* minutos perdidos por origem, somando as paradas apontadas */
  const perdas = ORIGENS_PERDA.map(o => ({
    ...o,
    minutos: aps.reduce((s, ap) => s + (ap.paradas || []).filter(x => (x.origem || motivoParada(x.motivo).origem) === o.id).reduce((y, x) => y + num(x.minutos), 0), 0)
  }));

  /* quanto deixou de sair por causa de cada perda, no ritmo planejado */
  const ritmoMedio = p.etapas.length ? p.etapas.reduce((s, e) => s + e.porHora, 0) / p.etapas.length : 0;
  const causas = perdas.filter(x => x.minutos > 0).map(x => ({
    origem: x.id,
    nome: x.nome,
    minutos: x.minutos,
    pecas: Math.round(x.minutos / 60 * ritmoMedio)
  }));
  const retrabalho = av.totalRetrabalho;
  const refugo = av.totalDefeito;
  if (retrabalho > 0) causas.push({
    origem: 'retrabalho',
    nome: 'Retrabalho',
    minutos: null,
    pecas: retrabalho
  });
  if (refugo > 0) causas.push({
    origem: 'refugo',
    nome: 'Peças rejeitadas',
    minutos: null,
    pecas: refugo
  });
  const esperado = (() => {
    /* quanto já deveria ter saído com o tempo que de fato se trabalhou */
    const produtivo = Math.max(0, av.totalMinutos - av.totalParada);
    const ultima = p.etapas[p.etapas.length - 1];
    if (!ultima || !(ultima.porHora > 0)) return null;
    return Math.round(produtivo / 60 * ultima.porHora);
  })();
  const real = av.concluido;
  const diferenca = esperado === null ? null : real - esperado;
  const explicado = causas.reduce((s, c) => s + num(c.pecas), 0);
  return {
    projecao: p,
    avanco: av,
    esperado,
    real,
    diferenca,
    causas,
    explicado,
    naoExplicado: diferenca === null ? null : Math.max(0, Math.abs(Math.min(0, diferenca)) - explicado),
    perdas,
    faixa: diferenca === null ? null : diferenca >= 0 ? 'ok' : Math.abs(diferenca) <= (esperado || 1) * 0.1 ? 'atencao' : 'atrasado'
  };
}

/* Onde o fluxo está travando agora — a etapa com fila na frente e
   menor capacidade. Aponta; não decreta. */
function gargaloDaOrdem(ordem, db) {
  const av = avancoDaOrdem(ordem, db);
  const comFila = av.etapas.filter(e => e.emEspera > 0);
  if (comFila.length === 0) return null;
  const pior = comFila.slice().sort((a, b) => b.emEspera - a.emEspera)[0];
  return {
    etapa: pior.etapa,
    setor: pior.setor,
    emEspera: pior.emEspera,
    produzidas: pior.produzidas,
    disponivel: pior.disponivel,
    travada: pior.travada,
    capacidade: pior.meta ? pior.meta.valorHora : null
  };
}

/* A torre de controle: o que está acontecendo na fábrica agora. */
function painelProducao(db, opcoes) {
  const op = opcoes || {};
  const ordens = (db.ordens || []).filter(o => o.situacao !== 'cancelada');
  const abertas = ordens.filter(o => o.situacao !== 'concluida');
  const linhas = abertas.map(o => {
    const av = avancoDaOrdem(o, db);
    const prev = previsaoTermino(o, db, op);
    const rvp = realVersusProjetado(o, db, op);
    const g = gargaloDaOrdem(o, db);
    return {
      ordem: o,
      avanco: av,
      previsao: prev,
      rvp,
      gargalo: g
    };
  });
  const hoje = todayISO();
  const doDia = (db.apontamentos || []).filter(ap => ap.data === hoje);
  return {
    ordens: linhas,
    abertas: abertas.length,
    emAndamento: linhas.filter(l => l.avanco.totalBoas > 0).length,
    atrasadas: linhas.filter(l => l.previsao.atrasada).length,
    concluidas: ordens.filter(o => o.situacao === 'concluida').length,
    producaoHoje: doDia.reduce((s, ap) => s + num(ap.pecasBoas), 0),
    defeitoHoje: doDia.reduce((s, ap) => s + num(ap.pecasDefeito), 0),
    minutosHoje: doDia.reduce((s, ap) => s + num(ap.minutosGastos), 0),
    paradaHoje: doDia.reduce((s, ap) => s + (ap.paradas || []).reduce((x, p) => x + num(p.minutos), 0), 0),
    gargalos: linhas.filter(l => l.gargalo).map(l => ({
      ordem: l.ordem,
      ...l.gargalo
    }))
  };
}

/* Registrar produção: o apontamento agora sabe de onde veio. */
function apontarProducao(d, dados, usuario) {
  const ordem = (d.ordens || []).find(o => o.id === dados.ordemId);
  if (dados.ordemId && !ordem) return {
    erro: 'Ordem não encontrada.'
  };
  const tarefa = ordem ? (ordem.tarefas || []).find(t => t.id === dados.tarefaId) : null;
  if (dados.ordemId && !tarefa) return {
    erro: 'Escolha a etapa da ordem.'
  };
  if (!dados.colaboradorId && !String(dados.colaborador || '').trim()) return {
    erro: 'Informe quem produziu.'
  };
  const boas = num(dados.pecasBoas),
    def = num(dados.pecasDefeito);
  if (boas < 0 || def < 0) return {
    erro: 'Quantidade inválida.'
  };
  const minutos = num(dados.minutosGastos);
  if(dados.inicioReal && dados.fimReal &&
     new Date(dados.fimReal) < new Date(dados.inicioReal))
    return {erro:'O fim da etapa está antes do início.'};
  if (minutos <= 0) return {
    erro: 'Informe quanto tempo levou.'
  };

  /* uma etapa não produz mais do que a anterior entregou */
  if (ordem && tarefa) {
    const av = avancoDaOrdem(ordem, d);
    const linha = av.etapas.find(e => e.tarefa.id === tarefa.id);
    if (linha && linha.ordem > 1) {
      const sobra = linha.disponivel - linha.produzidas;
      if (boas + def > sobra + 0.001) return {
        erro: `A etapa anterior entregou ${linha.disponivel} peça(s) e ` + `${linha.produzidas} já foram processadas. Não dá para apontar ` + `${boas + def} agora — no máximo ${Math.max(0, sobra)}.`
      };
    }
  }
  const col = (d.colaboradores || []).find(c => c.id === dados.colaboradorId);
  const et = tarefa ? (d.etapas || []).find(e => e.id === tarefa.etapaId) : null;
  const reg = {
    id: uid(),
    data: dados.data || todayISO(),
    /* o vínculo que faltava */
    ordemId: dados.ordemId || '',
    tarefaId: dados.tarefaId || '',
    produtoId: ordem ? ordem.produtoId : '',
    etapaId: tarefa ? tarefa.etapaId : '',
    departamentoId: tarefa ? tarefa.departamentoId : '',
    equipamentoId: dados.equipamentoId || (tarefa ? tarefa.equipamentoId : '') || '',
    colaboradorId: dados.colaboradorId || '',
    colaborador: col ? col.nome : String(dados.colaborador || '').trim(),
    etapa: et ? et.nome : String(dados.etapa || '').trim(),
    minutosPrevistos: num(dados.minutosPrevistos),
    minutosGastos: minutos,
    /* quando de fato começou e terminou. O tempo gasto pode vir daqui
       ou ser digitado direto; guardar as duas pontas permite reconstruir
       o dia e cruzar com a jornada. */
    inicioReal: dados.inicioReal || '',
    fimReal: dados.fimReal || '',
    pecasBoas: boas,
    pecasDefeito: def,
    retrabalho: num(dados.retrabalho),
    defeitos: dados.defeitos || [],
    paradas: (dados.paradas || []).map(p => ({
      id: p.id || uid(),
      motivo: p.motivo,
      origem: p.origem || motivoParada(p.motivo).origem,
      minutos: num(p.minutos),
      observacao: String(p.observacao || '').trim()
    })),
    motivoAtraso: String(dados.motivoAtraso || '').trim(),
    observacao: String(dados.observacao || '').trim(),
    criadoEm: agoraISO(),
    criadoPor: usuario ? usuario.nome : ''
  };
  d.apontamentos = [...(d.apontamentos || []), reg];

  /* a etapa se fecha sozinha quando entrega o combinado */
  if (ordem && tarefa) {
    const av = avancoDaOrdem(ordem, d);
    const linha = av.etapas.find(e => e.tarefa.id === tarefa.id);
    if (linha && linha.boas >= ordem.quantidade && !tarefa.concluida) {
      tarefa.concluida = true;
      registrarLog(d, usuario, 'producao.etapa', `${ordem.codigo} · ${linha.etapa} concluída`);
    }
    const todas = (ordem.tarefas || []).every(t => t.concluida);
    if (todas && ordem.situacao !== 'concluida') {
      ordem.situacao = 'concluida';
      ordem.concluidaEm = agoraISO();
      registrarLog(d, usuario, 'producao.ordem', `${ordem.codigo} concluída`);
    }
  }
  registrarLog(d, usuario, 'producao.apontamento', `${ordem ? ordem.codigo + ' · ' : ''}${reg.etapa} · ${boas} boa(s) em ${duracao(minutos)}`);
  return {
    apontamento: reg
  };
}

/* O fechamento da ordem: a conta final, com o que deu certo e o que
   não deu. */
function fechamentoDaOrdem(ordem, db, opcoes) {
  const rvp = realVersusProjetado(ordem, db, opcoes);
  const av = rvp.avanco;
  const p = rvp.projecao;
  const aps = apontamentosDaOrdem(db, ordem.id);
  const porPessoa = [];
  aps.forEach(ap => {
    const chave = ap.colaboradorId || ap.colaborador || 'sem nome';
    let g = porPessoa.find(x => x.chave === chave);
    if (!g) {
      g = {
        chave,
        nome: ap.colaborador || 'sem nome',
        boas: 0,
        defeito: 0,
        minutos: 0,
        etapas: new Set()
      };
      porPessoa.push(g);
    }
    g.boas += num(ap.pecasBoas);
    g.defeito += num(ap.pecasDefeito);
    g.minutos += num(ap.minutosGastos);
    if (ap.etapa) g.etapas.add(ap.etapa);
  });
  return {
    ordem,
    planejado: ordem.quantidade,
    projetado: p.original.totalMinutos,
    produzido: av.totalBoas + av.totalDefeito,
    boas: av.concluido,
    retrabalho: av.totalRetrabalho,
    rejeitada: av.totalDefeito,
    tempoProjetado: p.original.totalMinutos,
    tempoReal: av.totalMinutos,
    desvioTempo: Number((av.totalMinutos - p.original.totalMinutos).toFixed(1)),
    eficiencia: p.original.totalMinutos > 0 && av.totalMinutos > 0 ? Number((p.original.totalMinutos / av.totalMinutos * 100).toFixed(1)) : null,
    qualidade: av.totalBoas + av.totalDefeito > 0 ? Number((av.totalBoas / (av.totalBoas + av.totalDefeito) * 100).toFixed(1)) : null,
    perdas: rvp.perdas.filter(x => x.minutos > 0),
    causas: rvp.causas,
    pessoas: porPessoa.map(g => ({
      ...g,
      etapas: [...g.etapas],
      porHora: g.minutos > 0 ? Number((g.boas / (g.minutos / 60)).toFixed(1)) : null
    }))
  };
}

/* ==========================================================
   CONVERSA ABERTA
   Um canal para quem está na máquina dizer o que precisa mudar.
   Regra que guia o módulo: registro entra fácil e não sai mais —
   ninguém apaga, tudo tem situação, dono e prazo.
========================================================== */
const CATEGORIAS_CANAL = [
/* Elogio vem primeiro de propósito: canal que só recebe reclamação
   acaba lido como caixa de problema, e quem tem coisa boa a dizer não
   se dá ao trabalho de abrir. */
{
  id: 'elogio',
  nome: 'Reconhecer alguém',
  icone: '👏',
  frase: 'Quero registrar algo que foi bem feito.',
  destino: 'rh',
  sensivel: false,
  prioridade: 'baixa',
  /* reconhecimento não se "resolve": não tem prazo nem plano de ação */
  reconhecimento: true
}, {
  id: 'sugestao',
  nome: 'Dar uma sugestão',
  icone: '💡',
  frase: 'Tenho uma ideia para melhorar.',
  destino: 'setor',
  sensivel: false,
  prioridade: 'baixa'
}, {
  id: 'problema',
  nome: 'Relatar um problema',
  icone: '🛠️',
  frase: 'Existe algo dificultando meu trabalho.',
  destino: 'producao',
  sensivel: false,
  prioridade: 'media'
}, {
  id: 'risco',
  nome: 'Relatar um risco',
  icone: '⚠️',
  frase: 'Vi algo que pode causar acidente ou prejudicar a saúde.',
  destino: 'sst',
  sensivel: false,
  prioridade: 'alta'
}, {
  id: 'relacionamento',
  nome: 'Relacionamento',
  icone: '🤝',
  frase: 'Quero falar de uma situação envolvendo pessoas ou liderança.',
  destino: 'rh',
  sensivel: true,
  prioridade: 'media'
}, {
  id: 'confidencial',
  nome: 'Relato confidencial',
  icone: '🔒',
  frase: 'Quero comunicar algo delicado.',
  destino: 'rh',
  sensivel: true,
  prioridade: 'alta'
}, {
  id: 'denuncia',
  nome: 'Denúncia',
  icone: '🚨',
  frase: 'Quero relatar uma situação grave ou inadequada.',
  destino: 'direcao',
  sensivel: true,
  prioridade: 'alta'
}];
function categoriaCanal(id) {
  return CATEGORIAS_CANAL.find(c => c.id === id) || CATEGORIAS_CANAL[0];
}
const STATUS_CANAL = [{
  id: 'recebido',
  nome: 'Recebido'
}, {
  id: 'analise',
  nome: 'Em análise'
}, {
  id: 'encaminhado',
  nome: 'Encaminhado'
}, {
  id: 'tratamento',
  nome: 'Em tratamento'
}, {
  id: 'aguardando',
  nome: 'Aguardando ação'
}, {
  id: 'resolvido',
  nome: 'Resolvido'
}, {
  id: 'encerrado',
  nome: 'Encerrado'
}];
function statusCanal(id) {
  return STATUS_CANAL.find(x => x.id === id) || STATUS_CANAL[0];
}
const ABERTOS_CANAL = ['recebido', 'analise', 'encaminhado', 'tratamento', 'aguardando'];
const PRIORIDADES_CANAL = [{
  id: 'baixa',
  nome: 'Baixa',
  marca: '🟢',
  prazoDias: 30
}, {
  id: 'media',
  nome: 'Média',
  marca: '🟡',
  prazoDias: 15
}, {
  id: 'alta',
  nome: 'Alta',
  marca: '🟠',
  prazoDias: 5
}, {
  id: 'critica',
  nome: 'Crítica',
  marca: '🔴',
  prazoDias: 1
}];
function prioridadeCanal(id) {
  return PRIORIDADES_CANAL.find(x => x.id === id) || PRIORIDADES_CANAL[0];
}
const FREQUENCIAS_CANAL = ['Uma vez', 'Algumas vezes', 'Frequentemente', 'Todos os dias'];
const IMPACTOS_CANAL = ['Nenhum', 'Baixo', 'Médio', 'Alto', 'Muito alto'];

/* Temas para leitura de tendência. A ligação com NR-1/GRO é de fonte
   de informação: aponta onde olhar, não substitui avaliação técnica. */
const TEMAS_CANAL = [{
  id: 'seguranca',
  nome: 'Segurança',
  grupo: 'Segurança',
  termos: ['acidente', 'maquina', 'equipamento', 'incendio', 'choque', 'queda', 'protecao', 'epi', 'fio solto', 'tomada', 'faca', 'tesoura']
}, {
  id: 'ambiente',
  nome: 'Ambiente de trabalho',
  grupo: 'Segurança',
  termos: ['calor', 'frio', 'ruido', 'barulho', 'poeira', 'iluminacao', 'ventilacao', 'cheiro', 'fumaca', 'goteira']
}, {
  id: 'ergonomia',
  nome: 'Ergonomia',
  grupo: 'Saúde',
  termos: ['dor', 'coluna', 'postura', 'cadeira', 'bancada', 'peso', 'levantar', 'repetitivo', 'punho', 'ombro', 'joelho', 'em pe']
}, {
  id: 'saude',
  nome: 'Saúde',
  grupo: 'Saúde',
  termos: ['cansaco', 'fadiga', 'tontura', 'exposicao', 'alergia', 'respirar', 'vista', 'mal estar', 'remedio']
}, {
  id: 'ritmo',
  nome: 'Ritmo e carga',
  grupo: 'Psicossocial',
  termos: ['ritmo', 'meta', 'pressao', 'correria', 'acumulo', 'sobrecarga', 'hora extra', 'prazo curto', 'apertado', 'muito servico', 'sem parar']
}, {
  id: 'jornada',
  nome: 'Jornada',
  grupo: 'Psicossocial',
  termos: ['jornada', 'horario', 'turno', 'pausa', 'almoco', 'banheiro', 'descanso', 'folga']
}, {
  id: 'lideranca',
  nome: 'Liderança',
  grupo: 'Psicossocial',
  termos: ['lider', 'encarregad', 'supervisor', 'chefe', 'gerente', 'gestor', 'cobranca', 'tratamento', 'mandou']
}, {
  id: 'comunicacao',
  nome: 'Comunicação',
  grupo: 'Psicossocial',
  termos: ['comunicacao', 'aviso', 'informacao', 'ninguem avisa', 'nao sei', 'ordem trocada', 'ninguem falou']
}, {
  id: 'autonomia',
  nome: 'Autonomia',
  grupo: 'Psicossocial',
  termos: ['autonomia', 'decidir', 'opiniao', 'sugestao ignorada', 'nao me escutam', 'nao me ouvem']
}, {
  id: 'relacionamento',
  nome: 'Relacionamento',
  grupo: 'Psicossocial',
  termos: ['colega', 'equipe', 'fofoca', 'conflito', 'discussao', 'briga', 'grosseria', 'mal educad']
}, {
  id: 'conduta',
  nome: 'Conduta',
  grupo: 'Conduta',
  termos: ['humilh', 'constrang', 'discrimin', 'assedio', 'ameac', 'apelido', 'xingar', 'gritar', 'desrespeit', 'piada']
}, {
  id: 'producao',
  nome: 'Produção e processo',
  grupo: 'Operacional',
  termos: ['peca', 'tecido', 'linha', 'agulha', 'manutencao', 'parada', 'retrabalho', 'defeito', 'ferramenta', 'falta de material', 'maquina parada']
}];

/* Palavras que pedem olhar imediato. Nunca decidem sozinhas: elevam a
   prioridade e chamam gente — não classificam a infração. */
const TERMOS_CRITICOS = ['ameac', 'violencia', 'agressao', 'bateu', 'empurr', 'arma', 'fogo', 'incendio', 'choque eletrico', 'desmai', 'sangue', 'socorro', 'emergencia', 'iminente'];

/* O `normaliza` do sistema tira pontuação mas mantém acento — aqui
   precisamos do contrário: quem escreve no chão de fábrica escreve
   "ameaçou", "pressão", "dor de coluna", com e sem acento. */
function textoSimples(v) {
  return String(v || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase();
}
function analisarTextoCanal(texto) {
  const t = textoSimples(texto);
  const achados = TEMAS_CANAL.filter(tema => tema.termos.some(p => t.includes(textoSimples(p))));
  return {
    temas: achados.map(x => x.id),
    grupos: [...new Set(achados.map(x => x.grupo))],
    critico: TERMOS_CRITICOS.some(p => t.includes(textoSimples(p)))
  };
}

/* A prioridade sai da categoria, sobe com impacto e frequência, e vai ao
   topo se o texto sugerir urgência. Sempre revisável por gente. */
function prioridadeSugerida(dados) {
  const cat = categoriaCanal(dados.categoria);
  const ordem = PRIORIDADES_CANAL.map(p => p.id);
  let i = ordem.indexOf(cat.prioridade);
  const impacto = IMPACTOS_CANAL.indexOf(dados.impacto);
  if (impacto >= 4) i += 2;else if (impacto === 3) i += 1;
  if (FREQUENCIAS_CANAL.indexOf(dados.frequencia) >= 3) i += 1;
  if (analisarTextoCanal(dados.descricao).critico) i = ordem.length - 1;
  return ordem[Math.max(0, Math.min(i, ordem.length - 1))];
}
function protocoloCanal(d) {
  const ano = new Date().getFullYear();
  const usados = (d.manifestacoes || []).map(m => String(m.protocolo || '')).filter(p => p.startsWith(`CA-${ano}-`)).map(p => parseInt(p.slice(8), 10)).filter(n => !isNaN(n));
  return `CA-${ano}-${String((usados.length ? Math.max(...usados) : 0) + 1).padStart(5, '0')}`;
}
function registrarManifestacao(d, dados, usuario) {
  const descricao = String(dados.descricao || '').trim();
  if (descricao.length < 10) return {
    erro: 'Conte um pouco mais sobre o que aconteceu — pelo menos uma frase.'
  };
  if (!dados.categoria) return {
    erro: 'Escolha o tipo de manifestação.'
  };
  const cat = categoriaCanal(dados.categoria);
  const leitura = analisarTextoCanal(descricao);
  const prioridade = dados.prioridade || prioridadeSugerida({
    ...dados,
    descricao
  });
  const anonima = !!dados.anonima;
  const reg = {
    id: uid(),
    protocolo: protocoloCanal(d),
    categoria: cat.id,
    departamentoId: dados.departamentoId || '',
    local: String(dados.local || '').trim(),
    dataOcorrencia: dados.dataOcorrencia || todayISO(),
    descricao,
    frequencia: dados.frequencia || FREQUENCIAS_CANAL[0],
    impacto: dados.impacto || IMPACTOS_CANAL[1],
    envolvidos: anonima ? '' : String(dados.envolvidos || '').trim(),
    anexos: dados.anexos || [],
    /* identidade só é gravada quando a pessoa escolhe se identificar */
    anonima,
    colaboradorId: anonima ? '' : dados.colaboradorId || '',
    nome: anonima ? '' : String(dados.nome || '').trim(),
    matricula: anonima ? '' : String(dados.matricula || '').trim(),
    querRetorno: !!dados.querRetorno,
    /* leitura automática: sugestão de onde olhar, nunca veredito */
    temas: leitura.temas,
    grupos: leitura.grupos,
    sinalUrgencia: leitura.critico,
    prioridade,
    sensivel: cat.sensivel,
    destino: cat.destino,
    status: 'recebido',
    responsavelId: '',
    acoes: [],
    interacoes: [],
    historico: [{
      id: uid(),
      quando: agoraISO(),
      quem: anonima ? 'Anônimo' : usuario ? usuario.nome : dados.nome || 'Colaborador',
      o_que: 'Manifestação registrada',
      status: 'recebido'
    }],
    criadaEm: agoraISO(),
    criadaVia: dados.via || 'sistema'
  };
  d.manifestacoes = [...(d.manifestacoes || []), reg];
  /* o log não guarda o teor nem o autor: registra que houve manifestação */
  registrarLog(d, anonima ? null : usuario, 'canal.registro', `${reg.protocolo} · ${cat.nome} · ${prioridadeCanal(prioridade).nome}`);
  return {
    manifestacao: reg
  };
}
function moverManifestacao(d, id, novoStatus, usuario, nota) {
  const m = (d.manifestacoes || []).find(x => x.id === id);
  if (!m) return {
    erro: 'Manifestação não encontrada.'
  };
  if (!STATUS_CANAL.some(s => s.id === novoStatus)) return {
    erro: 'Situação inválida.'
  };
  if ((novoStatus === 'resolvido' || novoStatus === 'encerrado') && (m.acoes || []).some(a => a.status !== 'concluido' && a.status !== 'cancelado')) return {
    erro: 'Há ações do plano em aberto. Conclua ou cancele antes de encerrar.'
  };
  m.status = novoStatus;
  m.historico = [...(m.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : 'Sistema',
    o_que: `Situação: ${statusCanal(novoStatus).nome}`,
    nota: String(nota || '').trim(),
    status: novoStatus
  }];
  registrarLog(d, usuario, 'canal.status', `${m.protocolo} → ${statusCanal(novoStatus).nome}`);
  return {
    manifestacao: m
  };
}
function responderManifestacao(d, id, texto, usuario, paraOColaborador) {
  const m = (d.manifestacoes || []).find(x => x.id === id);
  if (!m) return {
    erro: 'Manifestação não encontrada.'
  };
  const msg = String(texto || '').trim();
  if (!msg) return {
    erro: 'Escreva a mensagem.'
  };
  m.interacoes = [...(m.interacoes || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : 'Colaborador',
    interno: !paraOColaborador,
    texto: msg
  }];
  registrarLog(d, usuario, 'canal.resposta', m.protocolo);
  return {
    manifestacao: m
  };
}
const STATUS_ACAO = [{
  id: 'nao_iniciado',
  nome: 'Não iniciado'
}, {
  id: 'andamento',
  nome: 'Em andamento'
}, {
  id: 'aguardando',
  nome: 'Aguardando'
}, {
  id: 'concluido',
  nome: 'Concluído'
}, {
  id: 'cancelado',
  nome: 'Cancelado'
}];
function salvarAcaoCanal(d, id, acao, usuario) {
  const m = (d.manifestacoes || []).find(x => x.id === id);
  if (!m) return {
    erro: 'Manifestação não encontrada.'
  };
  if (!String(acao.oQue || '').trim()) return {
    erro: 'Descreva a ação.'
  };
  if (!acao.responsavelId) return {
    erro: 'Toda ação precisa de um responsável.'
  };
  if (!acao.prazo) return {
    erro: 'Toda ação precisa de um prazo.'
  };
  const reg = {
    id: acao.id || uid(),
    oQue: String(acao.oQue).trim(),
    responsavelId: acao.responsavelId,
    prazo: acao.prazo,
    status: acao.status || 'nao_iniciado',
    evidencia: String(acao.evidencia || '').trim(),
    /* a evidência costuma ser uma foto: o texto sozinho não comprova nada */
    anexos: acao.anexos || [],
    concluidaEm: acao.status === 'concluido' ? acao.concluidaEm || todayISO() : '',
    criadaEm: acao.criadaEm || agoraISO()
  };
  m.acoes = acao.id ? (m.acoes || []).map(x => x.id === reg.id ? reg : x) : [...(m.acoes || []), reg];
  if (m.status === 'recebido' || m.status === 'analise') m.status = 'tratamento';
  m.historico = [...(m.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : 'Sistema',
    o_que: acao.id ? 'Ação atualizada' : 'Ação incluída no plano',
    status: m.status
  }];
  return {
    acao: reg
  };
}

/* Quem enxerga o quê. Sensível é sensível: líder de setor não abre
   denúncia nem relato confidencial, mesmo sendo do seu setor. */
function podeVerManifestacao(m, usuario, db) {
  if (!usuario || !m) return false;
  const perfil = usuario.perfil || '';
  if (perfil === 'Administrador') return true;
  const eu = (db.colaboradores || []).find(c => c.nome === usuario.nome);
  if (eu && m.colaboradorId === eu.id) return true; /* o próprio relato */
  if (perfil === 'Gestor') return true;
  if (m.sensivel) return false;
  return !!(eu && m.departamentoId && eu.departamentoId === m.departamentoId);
}
function manifestacoesVisiveis(db, usuario) {
  return (db.manifestacoes || []).filter(m => podeVerManifestacao(m, usuario, db));
}

/* Uma data corrompida no banco não pode derrubar a tela de quem trata
   as manifestações — devolve o que dá e segue. */
function dataValida(v) {
  const d = new Date(v);
  return isNaN(d.getTime()) ? null : d;
}
function prazoManifestacao(m) {
  const reg = m || {};
  /* Reconhecimento não tem prazo para "resolver" — cobrar prazo de um
     elogio o transformaria em pendência atrasada, que é o oposto do
     que ele é. */
  if (categoriaCanal(reg.categoria).reconhecimento) return {
    limite: '',
    dias: null,
    atrasada: false,
    semPrazo: true
  };
  const dias = prioridadeCanal(reg.prioridade).prazoDias;
  const base = dataValida(reg.criadaEm) || new Date();
  base.setDate(base.getDate() + dias);
  const limite = base.toISOString().slice(0, 10);
  const fechada = reg.status === 'resolvido' || reg.status === 'encerrado';
  return {
    limite,
    dias,
    atrasada: !fechada && todayISO() > limite,
    semPrazo: false
  };
}

/* Elogio não é problema em aberto. Separar os dois evita que o painel
   diga "12 manifestações pendentes" quando 5 são agradecimentos. */
function ehReconhecimento(m) {
  return !!categoriaCanal((m || {}).categoria).reconhecimento;
}
function indicadoresCanal(db, opcoes) {
  const op = opcoes || {};
  let lista = (db.manifestacoes || []).slice();
  if (op.de) lista = lista.filter(m => String(m.criadaEm || '').slice(0, 10) >= op.de);
  if (op.ate) lista = lista.filter(m => String(m.criadaEm || '').slice(0, 10) <= op.ate);
  const fechadas = lista.filter(m => m.status === 'resolvido' || m.status === 'encerrado');
  const diasAte = m => {
    const fim = (m.historico || []).filter(h => h && (h.status === 'resolvido' || h.status === 'encerrado')).map(h => h.quando).sort().pop();
    const dFim = dataValida(fim),
      dIni = dataValida(m.criadaEm);
    if (!dFim || !dIni) return null;
    return Math.max(0, (dFim - dIni) / 86400000);
  };
  const tempos = fechadas.map(diasAte).filter(x => x !== null);
  const noPrazo = fechadas.filter(m => !prazoManifestacao(m).atrasada).length;
  const conta = (chave, valores) => valores.map(v => ({
    id: v.id,
    nome: v.nome,
    total: lista.filter(m => m[chave] === v.id).length
  }));
  return {
    total: lista.length,
    abertas: lista.filter(m => ABERTOS_CANAL.includes(m.status) && !ehReconhecimento(m)).length,
    /* contados à parte: são coisa boa, não fila de trabalho */
    reconhecimentos: lista.filter(m => ehReconhecimento(m)).length,
    novas: lista.filter(m => m.status === 'recebido').length,
    emTratamento: lista.filter(m => m.status === 'tratamento' || m.status === 'aguardando').length,
    resolvidas: lista.filter(m => m.status === 'resolvido').length,
    encerradas: lista.filter(m => m.status === 'encerrado').length,
    anonimas: lista.filter(m => m.anonima).length,
    identificadas: lista.filter(m => !m.anonima).length,
    atrasadas: lista.filter(m => prazoManifestacao(m).atrasada).length,
    criticas: lista.filter(m => m.prioridade === 'critica' && ABERTOS_CANAL.includes(m.status)).length,
    tempoMedioResolucao: tempos.length ? Number((tempos.reduce((a, b) => a + b, 0) / tempos.length).toFixed(1)) : null,
    percentualNoPrazo: fechadas.length ? Math.round(noPrazo / fechadas.length * 100) : null,
    porStatus: conta('status', STATUS_CANAL),
    porCategoria: conta('categoria', CATEGORIAS_CANAL),
    porPrioridade: conta('prioridade', PRIORIDADES_CANAL),
    porTema: TEMAS_CANAL.map(t => ({
      ...t,
      total: lista.filter(m => (m.temas || []).includes(t.id)).length
    })).filter(t => t.total > 0).sort((a, b) => b.total - a.total),
    porDepartamento: (db.departamentos || []).map(dp => ({
      id: dp.id,
      nome: dp.nome,
      total: lista.filter(m => m.departamentoId === dp.id).length
    })).filter(x => x.total > 0).sort((a, b) => b.total - a.total),
    lista
  };
}

/* Tendência: comparar dois períodos iguais e apontar onde subiu.
   Mostra o número e sugere olhar; não conclui nada. */
function tendenciasCanal(db, dias) {
  const janela = num(dias) > 0 ? num(dias) : 30;
  const recuar = d => {
    const x = new Date(todayISO());
    x.setDate(x.getDate() - d);
    return x.toISOString().slice(0, 10);
  };
  const inicioAtual = recuar(janela),
    inicioAnterior = recuar(janela * 2);
  const entre = (de, ate) => (db.manifestacoes || []).filter(m => {
    const q = String(m.criadaEm || '').slice(0, 10);
    return q > de && q <= ate;
  });
  const agora = entre(inicioAtual, todayISO());
  const antes = entre(inicioAnterior, inicioAtual);
  const alertas = [];
  const avaliar = (rotulo, filtro, minimo) => {
    const a = agora.filter(filtro).length,
      b = antes.filter(filtro).length;
    if (a < minimo || a <= b) return;
    const variacao = b > 0 ? Math.round((a - b) / b * 100) : null;
    if (b === 0 || variacao >= 50) alertas.push({
      rotulo,
      atual: a,
      anterior: b,
      variacao
    });
  };
  (db.departamentos || []).forEach(dp => TEMAS_CANAL.forEach(t => avaliar(`${t.nome} no setor ${dp.nome}`, m => m.departamentoId === dp.id && (m.temas || []).includes(t.id), 3)));
  TEMAS_CANAL.forEach(t => avaliar(`${t.nome} na fábrica`, m => (m.temas || []).includes(t.id), 5));
  return {
    janela,
    atual: agora.length,
    anterior: antes.length,
    alertas: alertas.sort((a, b) => b.atual - a.atual).slice(0, 8)
  };
}

/* Reincidência: o mesmo assunto voltando no mesmo lugar. */
function reincidenciasCanal(db, dias) {
  const janela = num(dias) > 0 ? num(dias) : 30;
  const corte = new Date(todayISO());
  corte.setDate(corte.getDate() - janela);
  const desde = corte.toISOString().slice(0, 10);
  const recentes = (db.manifestacoes || []).filter(m => String(m.criadaEm || '').slice(0, 10) >= desde);
  const mapa = [];
  recentes.forEach(m => (m.temas || []).forEach(tid => {
    const chave = `${m.departamentoId}|${tid}`;
    let g = mapa.find(x => x.chave === chave);
    if (!g) {
      const dp = (db.departamentos || []).find(x => x.id === m.departamentoId);
      const tema = TEMAS_CANAL.find(x => x.id === tid);
      g = {
        chave,
        departamento: dp ? dp.nome : 'sem setor',
        tema: tema ? tema.nome : tid,
        total: 0,
        protocolos: []
      };
      mapa.push(g);
    }
    g.total++;
    g.protocolos.push(m.protocolo);
  }));
  return mapa.filter(g => g.total >= 3).sort((a, b) => b.total - a.total);
}

/* O termômetro do dia: uma pergunta, cinco carinhas. Sempre anônimo. */
function registrarPesquisaClima(d, dados) {
  const nota = num(dados.nota);
  if (!(nota >= 1 && nota <= 5)) return {
    erro: 'Escolha de 1 a 5.'
  };
  const reg = {
    id: uid(),
    data: todayISO(),
    quando: agoraISO(),
    nota,
    comentario: String(dados.comentario || '').trim(),
    departamentoId: dados.departamentoId || '',
    via: dados.via || 'sistema'
  };
  d.pesquisasClima = [...(d.pesquisasClima || []), reg];
  return {
    pesquisa: reg
  };
}
function climaResumo(db, dias) {
  const janela = num(dias) > 0 ? num(dias) : 30;
  const corte = new Date(todayISO());
  corte.setDate(corte.getDate() - janela);
  const desde = corte.toISOString().slice(0, 10);
  const lista = (db.pesquisasClima || []).filter(p => p.data >= desde);
  if (lista.length === 0) return {
    vazio: true,
    total: 0,
    media: null,
    porSetor: [],
    comentarios: []
  };
  const media = lista.reduce((a, p) => a + num(p.nota), 0) / lista.length;
  return {
    vazio: false,
    total: lista.length,
    media: Number(media.toFixed(2)),
    porSetor: (db.departamentos || []).map(dp => {
      const doSetor = lista.filter(p => p.departamentoId === dp.id);
      return {
        id: dp.id,
        nome: dp.nome,
        total: doSetor.length,
        media: doSetor.length ? Number((doSetor.reduce((a, p) => a + num(p.nota), 0) / doSetor.length).toFixed(2)) : null
      };
    }).filter(x => x.total > 0).sort((a, b) => a.media - b.media),
    comentarios: lista.filter(p => p.comentario).map(p => p.comentario)
  };
}

/* ---------------- Relatórios do canal ----------------
   Papel com denúncia dentro circula. Por isso o relatório completo é
   só do administrador, e o teor dos relatos sensíveis só entra se
   alguém marcar que quer — com o aviso do que isso significa. */
/* ==========================================================
   VAGAS E INDICAÇÃO
   Quem trabalha na fábrica conhece gente que serviria para a
   fábrica. Falta o caminho — e um caminho que respeite quem é
   indicado: o contato é de outra pessoa, não de quem indica.
========================================================== */
/* ==========================================================
   RECADOS — comunicação interna
   Conversa de um para um e grupos. Duas honestidades embutidas:
   sem servidor a mensagem não sai deste navegador, e conversa
   dentro do sistema da empresa não é conversa privada.
========================================================== */
const LIMITE_MENSAGEM = 2000;
/* Sem servidor, o arquivo vira base64 dentro do banco do navegador —
   e o banco inteiro é relido a cada tela. Arquivo grande deixa o
   sistema lento para todo mundo, não só para quem mandou. */
const LIMITE_ARQUIVO_CHAT = 900 * 1024;
const LIMITE_NOTA = 20000;

/* Quem participa de quê. Uma conversa individual é um grupo de dois
   sem nome — mesma estrutura, menos código para manter. */
function conversasDe(db, colaboradorId){
  if(!colaboradorId) return [];
  return (db.conversas||[])
    .filter(c=>(c.participantes||[]).includes(colaboradorId) && !c.arquivada)
    .map(c=>({...c, ...resumoConversa(db, c, colaboradorId)}))
    .sort((a,b)=>{
      const porTempo = String(b.ultimaEm||'').localeCompare(String(a.ultimaEm||''));
      return porTempo !== 0 ? porTempo : (b.ordem - a.ordem);
    });
}

function mensagensDa(db, conversaId){
  return (db.mensagens||[]).filter(m=>m.conversaId===conversaId)
    .sort((a,b)=>String(a.quando||'').localeCompare(String(b.quando||'')));
}

function resumoConversa(db, conversa, euId){
  const msgs = mensagensDa(db, conversa.id);
  const ultima = msgs[msgs.length-1] || null;
  /* duas mensagens podem cair no mesmo instante; a ordem de chegada
     desempata */
  const ordem = ultima
    ? (db.mensagens||[]).findIndex(m=>m.id===ultima.id) : -1;
  const naoLidas = msgs.filter(m=>m.autorId !== euId &&
    !(m.lidaPor||[]).includes(euId)).length;
  return {
    ultima,
    ultimaEm: ultima ? ultima.quando : conversa.criadaEm,
    ordem,
    naoLidas,
    total: msgs.length,
  };
}

/* O nome que aparece na lista. Num grupo é o nome dado; num diálogo
   é a outra pessoa — e "outra pessoa" depende de quem está olhando. */
function nomeConversa(db, conversa, euId){
  if(conversa.grupo) return conversa.nome || 'Grupo sem nome';
  const outro = (conversa.participantes||[]).find(id=>id!==euId);
  const c = (db.colaboradores||[]).find(x=>x.id===outro);
  return c ? c.nome : 'colaborador removido';
}

function participantesDa(db, conversa){
  return (conversa.participantes||[])
    .map(id=>(db.colaboradores||[]).find(c=>c.id===id))
    .filter(Boolean);
}

/* Abrir conversa com alguém. Se já existe, devolve a mesma — duas
   conversas com a mesma pessoa espalham o histórico em dois lugares. */
function abrirConversa(d, euId, outroId, usuario){
  if(!euId || !outroId) return {erro:'Escolha com quem falar.'};
  if(euId === outroId) return {erro:'Não dá para conversar consigo mesmo.'};
  const eu = (d.colaboradores||[]).find(c=>c.id===euId);
  const outro = (d.colaboradores||[]).find(c=>c.id===outroId);
  if(!eu || !outro) return {erro:'Colaborador não encontrado.'};
  if(outro.status === 'Inativo')
    return {erro:`${outro.nome} não está mais ativo no cadastro.`};

  const existente = (d.conversas||[]).find(c=>!c.grupo &&
    (c.participantes||[]).length===2 &&
    c.participantes.includes(euId) && c.participantes.includes(outroId));
  if(existente){
    if(existente.arquivada) existente.arquivada = false;
    return {conversa: existente, jaExistia:true};
  }

  const reg = {
    id: uid(), grupo:false, nome:'',
    participantes:[euId, outroId],
    criadaPor: euId, criadaEm: agoraISO(), arquivada:false,
  };
  d.conversas = [...(d.conversas||[]), reg];
  return {conversa: reg};
}

function criarGrupo(d, dados, usuario){
  const nome = String(dados.nome||'').trim();
  if(!nome) return {erro:'Dê um nome ao grupo.'};
  const membros = [...new Set((dados.participantes||[]).filter(Boolean))];
  if(membros.length < 2)
    return {erro:'Um grupo precisa de pelo menos duas pessoas.'};
  const validos = membros.filter(id=>
    (d.colaboradores||[]).some(c=>c.id===id));
  if(validos.length !== membros.length)
    return {erro:'Algum participante não está no cadastro.'};

  const reg = {
    id: uid(), grupo:true, nome,
    descricao: String(dados.descricao||'').trim(),
    participantes: validos,
    administradores: [dados.criadorId].filter(Boolean),
    criadaPor: dados.criadorId || '', criadaEm: agoraISO(), arquivada:false,
  };
  d.conversas = [...(d.conversas||[]), reg];
  registrarLog(d, usuario, 'chat.grupo', `Grupo "${nome}" criado`);
  return {conversa: reg};
}

function mudarParticipantes(d, conversaId, ids, usuario){
  const c = (d.conversas||[]).find(x=>x.id===conversaId);
  if(!c) return {erro:'Conversa não encontrada.'};
  if(!c.grupo) return {erro:'Só grupos têm participantes editáveis.'};
  const membros = [...new Set((ids||[]).filter(Boolean))];
  if(membros.length < 2)
    return {erro:'Um grupo precisa de pelo menos duas pessoas.'};

  /* quem sai não perde o que já leu, mas para de receber */
  const saindo = (c.participantes||[]).filter(x=>!membros.includes(x));
  c.participantes = membros;
  if(saindo.length)
    registrarLog(d, usuario, 'chat.grupo',
      `${saindo.length} pessoa(s) saíram de "${c.nome}"`);
  return {conversa: c};
}

function enviarMensagem(d, conversaId, autorId, texto, usuario, anexos){
  const c = (d.conversas||[]).find(x=>x.id===conversaId);
  if(!c) return {erro:'Conversa não encontrada.'};
  if(!(c.participantes||[]).includes(autorId))
    return {erro:'Você não participa desta conversa.'};
  const t = String(texto||'').trim();
  const arqs = (anexos||[]).filter(Boolean);
  /* mensagem só com arquivo é legítima: mandar a foto da peça sem
     precisar escrever nada */
  if(!t && arqs.length === 0) return {erro:'Escreva a mensagem ou anexe um arquivo.'};
  if(t.length > LIMITE_MENSAGEM)
    return {erro:`Mensagem muito longa (limite de ${LIMITE_MENSAGEM} caracteres).`};
  const grande = arqs.find(x=>num(x.tamanho) > LIMITE_ARQUIVO_CHAT);
  if(grande)
    return {erro:`"${grande.nome}" tem ${Math.round(num(grande.tamanho)/1024)} KB e `+
      `passa do limite de ${Math.round(LIMITE_ARQUIVO_CHAT/1024)} KB.`};

  const reg = {
    id: uid(), conversaId, autorId,
    texto: t,
    anexos: arqs.map(x=>({id:x.id||uid(), nome:x.nome, tipo:x.tipo||'',
      tamanho:num(x.tamanho), url:x.url})),
    /* precisão de segundos: duas mensagens no mesmo minuto precisam
       manter a ordem em que foram ditas */
    quando: instanteISO(),
    /* quem escreve já leu */
    lidaPor: [autorId],
  };
  d.mensagens = [...(d.mensagens||[]), reg];
  /* o log registra que houve conversa, nunca o teor */
  return {mensagem: reg};
}

function marcarLidas(d, conversaId, euId){
  let mudou = 0;
  (d.mensagens||[]).forEach(m=>{
    if(m.conversaId===conversaId && m.autorId!==euId &&
       !(m.lidaPor||[]).includes(euId)){
      m.lidaPor = [...(m.lidaPor||[]), euId];
      mudou++;
    }
  });
  return {marcadas: mudou};
}

function naoLidasDe(db, colaboradorId){
  return conversasDe(db, colaboradorId).reduce((s,c)=>s + c.naoLidas, 0);
}

/* Com quem dá para falar. O próprio usuário fora, inativos fora. */
function pessoasDisponiveis(db, euId){
  return (db.colaboradores||[])
    .filter(c=>c.id!==euId && c.status!=='Inativo')
    .sort((a,b)=>String(a.nome).localeCompare(String(b.nome)));
}

/* ---------------- Bloco de notas ----------------
   Anotação de trabalho: o que lembrar, o que combinar, o rascunho da
   conversa difícil. Fica só na aba de quem escreveu — mas o backup
   contém tudo, e isso a tela precisa dizer. */
function notasDe(db, colaboradorId){
  if(!colaboradorId) return [];
  const todas = (db.notas||[]);
  return todas.filter(n=>n.colaboradorId===colaboradorId)
    .sort((a,b)=>{
      /* fixadas primeiro, depois a mais mexida */
      if(!!a.fixada !== !!b.fixada) return a.fixada ? -1 : 1;
      const porTempo = String(b.alteradaEm||'')
        .localeCompare(String(a.alteradaEm||''));
      /* duas notas salvas no mesmo instante: a criada depois vem antes */
      return porTempo !== 0 ? porTempo
        : todas.indexOf(b) - todas.indexOf(a);
    });
}

function salvarNota(d, dados, usuario){
  const texto = String(dados.texto||'').trim();
  const titulo = String(dados.titulo||'').trim();
  if(!texto && !titulo) return {erro:'Escreva alguma coisa antes de salvar.'};
  if(texto.length > LIMITE_NOTA)
    return {erro:`Nota muito longa (limite de ${LIMITE_NOTA} caracteres).`};
  if(!dados.colaboradorId) return {erro:'Nota sem dono.'};

  const existente = dados.id
    ? (d.notas||[]).find(n=>n.id===dados.id) : null;
  if(dados.id && !existente) return {erro:'Nota não encontrada.'};
  /* nota é pessoal: ninguém edita a do outro */
  if(existente && existente.colaboradorId !== dados.colaboradorId)
    return {erro:'Esta nota é de outra pessoa.'};

  const reg = {
    id: existente ? existente.id : uid(),
    colaboradorId: dados.colaboradorId,
    titulo: titulo || (texto.split('\n')[0] || '').slice(0, 60),
    texto,
    fixada: !!dados.fixada,
    criadaEm: existente ? existente.criadaEm : agoraISO(),
    alteradaEm: instanteISO(),
  };
  d.notas = existente
    ? (d.notas||[]).map(n=>n.id===reg.id ? reg : n)
    : [...(d.notas||[]), reg];
  /* o log registra que houve nota, nunca o teor */
  return {nota: reg};
}

function removerNota(d, id, colaboradorId){
  const n = (d.notas||[]).find(x=>x.id===id);
  if(!n) return {erro:'Nota não encontrada.'};
  if(n.colaboradorId !== colaboradorId)
    return {erro:'Esta nota é de outra pessoa.'};
  d.notas = (d.notas||[]).filter(x=>x.id!==id);
  return {removida:true};
}

/* Sem servidor, a mensagem fica presa neste navegador. Melhor dizer
   isso na tela do que deixar alguém contar com um recado que nunca
   chegou. */
function chatFuncionaDeVerdade(){
  try{
    const p = window.location.protocol;
    return p === 'http:' || p === 'https:';
  }catch(e){ return false; }
}

const STATUS_VAGA = [{
  id: 'aberta',
  nome: 'Aberta',
  recebe: true
}, {
  id: 'pausada',
  nome: 'Pausada',
  recebe: false
}, {
  id: 'preenchida',
  nome: 'Preenchida',
  recebe: false
}, {
  id: 'encerrada',
  nome: 'Encerrada',
  recebe: false
}];
function statusVaga(id) {
  return STATUS_VAGA.find(x => x.id === id) || STATUS_VAGA[0];
}
const TIPOS_VAGA = ['Efetivo', 'Temporário', 'Aprendiz', 'Estágio', 'Banco de talentos'];
const TURNOS_VAGA = ['Manhã', 'Tarde', 'Comercial', 'A combinar'];

/* Onde a indicação está no caminho. Recusar sem dizer nada é o que
   faz a pessoa não indicar de novo — cada passo tem um retorno. */
const STATUS_INDICACAO = [{
  id: 'recebida',
  nome: 'Recebida',
  fim: false
}, {
  id: 'contato',
  nome: 'Em contato',
  fim: false
}, {
  id: 'entrevista',
  nome: 'Entrevista marcada',
  fim: false
}, {
  id: 'contratado',
  nome: 'Contratado',
  fim: true
}, {
  id: 'banco',
  nome: 'Guardado no banco',
  fim: true
}, {
  id: 'nao_seguiu',
  nome: 'Não seguiu',
  fim: true
}];
function statusIndicacao(id) {
  return STATUS_INDICACAO.find(x => x.id === id) || STATUS_INDICACAO[0];
}
function salvarVaga(d, dados, usuario) {
  const titulo = String(dados.titulo || '').trim();
  if (!titulo) return {
    erro: 'Informe o cargo da vaga.'
  };
  const existente = dados.id ? (d.vagas || []).find(v => v.id === dados.id) : null;
  if (dados.id && !existente) return {
    erro: 'Vaga não encontrada.'
  };
  const reg = {
    id: existente ? existente.id : uid(),
    codigo: existente ? existente.codigo : proximoCodigo(d.vagas, 'VAG'),
    titulo,
    departamentoId: dados.departamentoId || '',
    tipo: dados.tipo || TIPOS_VAGA[0],
    turno: dados.turno || TURNOS_VAGA[3],
    vagas: Math.max(1, num(dados.vagas) || 1),
    descricao: String(dados.descricao || '').trim(),
    requisitos: String(dados.requisitos || '').trim(),
    experiencia: String(dados.experiencia || '').trim(),
    /* salário fica de fora por escolha: divulgar faixa no chão de
       fábrica levanta comparação com quem já está lá. O RH informa
       na conversa. */
    status: dados.status || 'aberta',
    aberturaEm: existente ? existente.aberturaEm : agoraISO(),
    encerradaEm: existente ? existente.encerradaEm : '',
    criadaPor: existente ? existente.criadaPor : usuario ? usuario.nome : '',
    historico: existente ? existente.historico || [] : []
  };
  d.vagas = existente ? (d.vagas || []).map(v => v.id === reg.id ? reg : v) : [...(d.vagas || []), reg];
  registrarLog(d, usuario, existente ? 'vaga.editada' : 'vaga.criada', `${reg.codigo} · ${reg.titulo}`);
  return {
    vaga: reg
  };
}
function mudarStatusVaga(d, vagaId, novo, motivo, usuario) {
  const v = (d.vagas || []).find(x => x.id === vagaId);
  if (!v) return {
    erro: 'Vaga não encontrada.'
  };
  if (!STATUS_VAGA.some(s => s.id === novo)) return {
    erro: 'Situação inválida.'
  };

  /* fechar a vaga sem responder quem indicou é o jeito mais rápido de
     acabar com o programa de indicação */
  const pendentes = indicacoesDaVaga(d, vagaId).filter(i => !statusIndicacao(i.status).fim).length;
  const antes = v.status;
  v.status = novo;
  if (!statusVaga(novo).recebe && !v.encerradaEm) v.encerradaEm = agoraISO();
  if (statusVaga(novo).recebe) v.encerradaEm = '';
  v.historico = [...(v.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    de: antes,
    para: novo,
    motivo: String(motivo || '').trim()
  }];
  registrarLog(d, usuario, 'vaga.status', `${v.codigo} · ${statusVaga(antes).nome} → ${statusVaga(novo).nome}`);
  return {
    vaga: v,
    pendentes
  };
}
function vagasAbertas(db) {
  return (db.vagas || []).filter(v => statusVaga(v.status).recebe);
}
function indicacoesDaVaga(db, vagaId) {
  return (db.indicacoes || []).filter(i => i.vagaId === vagaId);
}

/* Registrar uma indicação. O dado mais delicado aqui não é de quem
   indica — é de quem foi indicado, que não está na tela para
   concordar. */
function registrarIndicacao(d, dados, usuario) {
  const vaga = (d.vagas || []).find(v => v.id === dados.vagaId);
  if (!vaga) return {
    erro: 'Escolha a vaga.'
  };
  if (!statusVaga(vaga.status).recebe) return {
    erro: `A vaga ${vaga.titulo} não está mais recebendo indicações.`
  };
  const nome = String(dados.nomeCandidato || '').trim();
  if (nome.length < 3) return {
    erro: 'Informe o nome de quem você está indicando.'
  };
  const contato = String(dados.contato || '').trim();
  if (contato.length < 8) return {
    erro: 'Informe um telefone ou e-mail para o RH conseguir falar ' + 'com a pessoa.'
  };

  /* quem indica precisa se identificar: indicação anônima deixa o RH
     sem saber a quem dar retorno, e a pessoa indicada sem referência */
  const quemIndica = String(dados.nomeIndicador || '').trim() || (usuario ? usuario.nome : '');
  if (!quemIndica) return {
    erro: 'Informe seu nome — o RH precisa saber quem indicou para ' + 'dar retorno a você.'
  };
  if (!dados.avisou) return {
    erro: 'Confirme que a pessoa sabe que está sendo indicada. ' + 'Passar o contato de alguém sem que ela saiba não é justo com ela.'
  };

  /* a mesma pessoa indicada duas vezes para a mesma vaga */
  const repetida = (d.indicacoes || []).find(i => i.vagaId === vaga.id && textoSimples(i.nomeCandidato) === textoSimples(nome));
  if (repetida) return {
    erro: `${nome} já foi indicado(a) para esta vaga (protocolo ` + `${repetida.protocolo}). O RH já tem o contato.`
  };
  const reg = {
    id: uid(),
    protocolo: proximoCodigo(d.indicacoes, 'IND'),
    vagaId: vaga.id,
    vagaTitulo: vaga.titulo,
    nomeCandidato: nome,
    contato,
    experiencia: String(dados.experiencia || '').trim(),
    relacao: String(dados.relacao || '').trim(),
    observacao: String(dados.observacao || '').trim(),
    nomeIndicador: quemIndica,
    colaboradorId: dados.colaboradorId || (usuario ? usuario.id || '' : ''),
    avisou: true,
    status: 'recebida',
    historico: [{
      id: uid(),
      quando: agoraISO(),
      quem: quemIndica,
      o_que: 'Indicação registrada',
      status: 'recebida'
    }],
    retornos: [],
    criadaEm: agoraISO()
  };
  d.indicacoes = [...(d.indicacoes || []), reg];
  /* o log não guarda o contato de terceiro */
  registrarLog(d, usuario, 'indicacao.registrada', `${reg.protocolo} · ${vaga.titulo}`);
  return {
    indicacao: reg
  };
}
function moverIndicacao(d, id, novo, nota, usuario) {
  const i = (d.indicacoes || []).find(x => x.id === id);
  if (!i) return {
    erro: 'Indicação não encontrada.'
  };
  if (!STATUS_INDICACAO.some(s => s.id === novo)) return {
    erro: 'Situação inválida.'
  };
  /* recusar sem explicar é o que faz ninguém indicar de novo */
  if (novo === 'nao_seguiu' && !String(nota || '').trim()) return {
    erro: 'Escreva o motivo. Quem indicou vai receber esse retorno, e ' + '"não seguiu" sem explicação desanima a próxima indicação.'
  };
  const antes = i.status;
  i.status = novo;
  i.historico = [...(i.historico || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    o_que: `${statusIndicacao(antes).nome} → ` + statusIndicacao(novo).nome,
    status: novo,
    nota: String(nota || '').trim()
  }];
  registrarLog(d, usuario, 'indicacao.status', `${i.protocolo} · ${statusIndicacao(novo).nome}`);
  return {
    indicacao: i
  };
}
function responderIndicacao(d, id, texto, usuario) {
  const i = (d.indicacoes || []).find(x => x.id === id);
  if (!i) return {
    erro: 'Indicação não encontrada.'
  };
  const t = String(texto || '').trim();
  if (!t) return {
    erro: 'Escreva o retorno.'
  };
  i.retornos = [...(i.retornos || []), {
    id: uid(),
    quando: agoraISO(),
    quem: usuario ? usuario.nome : '',
    texto: t
  }];
  registrarLog(d, usuario, 'indicacao.retorno', i.protocolo);
  return {
    indicacao: i
  };
}

/* O que o RH precisa ver de relance. */
function painelVagas(db) {
  const abertas = vagasAbertas(db);
  const inds = db.indicacoes || [];
  const semRetorno = inds.filter(i => i.status === 'recebida');
  /* indicação parada há mais de 7 dias sem ninguém tocar */
  const paradas = inds.filter(i => {
    if (statusIndicacao(i.status).fim) return false;
    const ultimo = (i.historico || []).map(h => h.quando).sort().pop() || i.criadaEm;
    const d0 = dataValida(ultimo);
    return d0 ? (Date.now() - d0) / 86400000 > 7 : false;
  });
  return {
    vagas: db.vagas || [],
    abertas: abertas.length,
    postosAbertos: abertas.reduce((s, v) => s + num(v.vagas), 0),
    indicacoes: inds.length,
    semRetorno: semRetorno.length,
    paradas: paradas.length,
    contratados: inds.filter(i => i.status === 'contratado').length,
    porVaga: abertas.map(v => ({
      vaga: v,
      total: indicacoesDaVaga(db, v.id).length
    }))
  };
}
function relatorioCanalPeriodo(db, opcoes) {
  const op = opcoes || {};
  let lista = (db.manifestacoes || []).slice();
  if (op.de) lista = lista.filter(m => String(m.criadaEm || '').slice(0, 10) >= op.de);
  if (op.ate) lista = lista.filter(m => String(m.criadaEm || '').slice(0, 10) <= op.ate);
  if (op.categoria) lista = lista.filter(m => m.categoria === op.categoria);
  if (op.departamentoId) lista = lista.filter(m => m.departamentoId === op.departamentoId);
  if (op.status) lista = lista.filter(m => m.status === op.status);
  if (op.soAbertas) lista = lista.filter(m => ABERTOS_CANAL.includes(m.status));
  lista = lista.slice().sort((a, b) => String(a.criadaEm).localeCompare(String(b.criadaEm)));
  const fechadas = lista.filter(m => m.status === 'resolvido' || m.status === 'encerrado');
  const tempos = fechadas.map(m => {
    const fim = (m.historico || []).filter(h => h && (h.status === 'resolvido' || h.status === 'encerrado')).map(h => h.quando).sort().pop();
    const a = dataValida(m.criadaEm),
      b = dataValida(fim);
    return a && b ? Math.max(0, (b - a) / 86400000) : null;
  }).filter(x => x !== null);
  const conta = (chave, valores) => valores.map(v => ({
    id: v.id,
    nome: v.nome,
    total: lista.filter(m => m[chave] === v.id).length
  })).filter(x => x.total > 0);

  /* ações do plano em todas as manifestações do período */
  const acoes = lista.flatMap(m => (m.acoes || []).map(a => ({
    ...a,
    protocolo: m.protocolo
  })));
  return {
    periodo: {
      de: op.de || '',
      ate: op.ate || ''
    },
    lista,
    total: lista.length,
    abertas: lista.filter(m => ABERTOS_CANAL.includes(m.status) && !ehReconhecimento(m)).length,
    /* contados à parte: são coisa boa, não fila de trabalho */
    reconhecimentos: lista.filter(m => ehReconhecimento(m)).length,
    resolvidas: lista.filter(m => m.status === 'resolvido').length,
    encerradas: lista.filter(m => m.status === 'encerrado').length,
    anonimas: lista.filter(m => m.anonima).length,
    sensiveis: lista.filter(m => m.sensivel).length,
    atrasadas: lista.filter(m => prazoManifestacao(m).atrasada).length,
    criticas: lista.filter(m => m.prioridade === 'critica').length,
    tempoMedio: tempos.length ? Number((tempos.reduce((a, b) => a + b, 0) / tempos.length).toFixed(1)) : null,
    noPrazo: fechadas.length ? Math.round(fechadas.filter(m => !prazoManifestacao(m).atrasada).length / fechadas.length * 100) : null,
    porCategoria: conta('categoria', CATEGORIAS_CANAL),
    porStatus: conta('status', STATUS_CANAL),
    porPrioridade: conta('prioridade', PRIORIDADES_CANAL),
    porDepartamento: (db.departamentos || []).map(dp => ({
      id: dp.id,
      nome: dp.nome,
      total: lista.filter(m => m.departamentoId === dp.id).length
    })).filter(x => x.total > 0).sort((a, b) => b.total - a.total),
    porTema: TEMAS_CANAL.map(t => ({
      ...t,
      total: lista.filter(m => (m.temas || []).includes(t.id)).length
    })).filter(t => t.total > 0).sort((a, b) => b.total - a.total),
    acoes,
    acoesAbertas: acoes.filter(a => a.status !== 'concluido' && a.status !== 'cancelado').length,
    /* quem pode ler tudo isso */
    somenteAdmin: true
  };
}

/* Só o administrador vê o relatório completo. O líder trata o que é
   dele; a visão do conjunto, com denúncia dentro, é de quem responde
   pela empresa. */
function podeVerRelatorioCanal(usuario) {
  return !!(usuario && usuario.perfil === 'Administrador');
}

/* ---------------- A caixa do canal ----------------
   O arquivo do canal deposita aqui; o ERP recolhe. Assim o colaborador
   não precisa do sistema aberto, e o sistema não precisa expor o banco
   para quem só vai fazer um relato. */
function lerCaixaCanal() {
  try {
    const bruto = typeof window !== 'undefined' && window.localStorage ? window.localStorage.getItem(CAIXA_CANAL) : null;
    if (!bruto) return [];
    const lista = JSON.parse(bruto);
    return Array.isArray(lista) ? lista : [];
  } catch (e) {
    return [];
  }
}
function limparCaixaCanal() {
  try {
    if (typeof window !== 'undefined' && window.localStorage) window.localStorage.removeItem(CAIXA_CANAL);
  } catch (e) {/* sem acesso ao armazenamento: segue sem limpar */}
}

/* Traz para o banco o que está na caixa. Cada item chega no formato que
   o canal gravou; aqui ele vira manifestação de verdade, com protocolo
   preservado — o papel que o colaborador guardou tem que bater. */
function recolherDoCanal(d, itens, usuario) {
  const lista = itens || lerCaixaCanal();
  if (lista.length === 0) return {
    recolhidas: 0,
    repetidas: 0,
    invalidas: 0
  };
  let novas = 0,
    repetidas = 0,
    invalidas = 0;
  lista.forEach(item => {
    if (!item || typeof item !== 'object') {
      invalidas++;
      return;
    }
    if (item.tipo === 'clima') {
      const nota = num(item.nota);
      if (!(nota >= 1 && nota <= 5)) {
        invalidas++;
        return;
      }
      const jaTem = (d.pesquisasClima || []).some(p => p.id === item.id);
      if (jaTem) {
        repetidas++;
        return;
      }
      d.pesquisasClima = [...(d.pesquisasClima || []), {
        id: item.id || uid(),
        data: item.data || todayISO(),
        quando: item.quando || agoraISO(),
        nota,
        comentario: String(item.comentario || '').trim(),
        departamentoId: item.departamentoId || '',
        via: 'canal'
      }];
      novas++;
      return;
    }
    const descricao = String(item.descricao || '').trim();
    if (descricao.length < 10) {
      invalidas++;
      return;
    }
    if (!item.protocolo) {
      invalidas++;
      return;
    }
    if ((d.manifestacoes || []).some(m => m.protocolo === item.protocolo)) {
      repetidas++;
      return;
    }
    const cat = categoriaCanal(item.categoria);
    const leitura = analisarTextoCanal(descricao);
    const anonima = item.anonima !== false;
    d.manifestacoes = [...(d.manifestacoes || []), {
      id: uid(),
      protocolo: item.protocolo,
      categoria: cat.id,
      departamentoId: item.departamentoId || '',
      local: String(item.local || '').trim(),
      dataOcorrencia: item.dataOcorrencia || todayISO(),
      descricao,
      frequencia: item.frequencia || FREQUENCIAS_CANAL[0],
      impacto: item.impacto || IMPACTOS_CANAL[1],
      envolvidos: anonima ? '' : String(item.envolvidos || '').trim(),
      anexos: item.anexos || [],
      anonima,
      colaboradorId: '',
      nome: anonima ? '' : String(item.nome || '').trim(),
      matricula: anonima ? '' : String(item.matricula || '').trim(),
      querRetorno: !!item.querRetorno,
      temas: leitura.temas,
      grupos: leitura.grupos,
      sinalUrgencia: leitura.critico,
      prioridade: item.prioridade || prioridadeSugerida({
        ...item,
        descricao
      }),
      sensivel: cat.sensivel,
      destino: cat.destino,
      status: 'recebido',
      responsavelId: '',
      acoes: [],
      interacoes: [],
      historico: [{
        id: uid(),
        quando: item.criadaEm || agoraISO(),
        quem: anonima ? 'Anônimo' : item.nome || 'Colaborador',
        o_que: 'Manifestação registrada pelo canal',
        status: 'recebido'
      }],
      criadaEm: item.criadaEm || agoraISO(),
      criadaVia: 'canal'
    }];
    novas++;
  });
  if (novas > 0) registrarLog(d, usuario, 'canal.recolhido', `${novas} registro(s) vindos do canal`);
  return {
    recolhidas: novas,
    repetidas,
    invalidas
  };
}

/* URL base dos QR Codes: cada fábrica hospeda onde quiser. */
function configCanal(db) {
  return {
    baseUrl: '',
    ...(db.configCanal || {})
  };
}
function urlDoSetor(db, departamentoId) {
  const base = String(configCanal(db).baseUrl || '').trim().replace(/\/+$/, '');
  if (!base) return '';
  const dp = (db.departamentos || []).find(x => x.id === departamentoId);
  if (!dp) return base;
  /* o slug vira parte da URL: só letras sem acento, números e hífen */
  const slug = String(dp.nome).normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  return slug ? `${base}?setor=${slug}` : base;
}

/* ==========================================================
   RISCO DE PRODUÇÃO
   Não confundir com a modelagem: a modelagem é a engenharia da
   peça — dela saem as partes e o consumo. O risco de produção é
   o arquivo que vai para a mesa de corte naquela ordem, com o
   encaixe já feito para o lote. Aqui ele é só arquivo: o sistema
   guarda e mostra, não interpreta.
========================================================== */
function etapaDeCorte(passo, db) {
  const dep = (db.departamentos || []).find(d => d.id === (passo && passo.departamentoId));
  const et = (db.etapas || []).find(e => e.id === (passo && passo.etapaId));
  const alvo = normaliza(dep && dep.nome || '') + ' ' + normaliza(et && et.nome || '');
  return alvo.includes('corte') || alvo.includes('enfesto') || alvo.includes('risco');
}
function riscosDeProducao(passo) {
  return Array.isArray(passo && passo.riscosProducao) ? passo.riscosProducao : [];
}

/* As partes que a etapa processa: costurar a frente é diferente de
   costurar a manga, e o cronograma precisa saber o que passa por ali. */
function subprodutosDaEtapa(passo, produto, db) {
  const ids = Array.isArray(passo.subprodutos) ? passo.subprodutos : [];
  if (ids.length === 0) return [];
  const todos = subprodutosDoProduto(produto, db);
  return ids.map(id => todos.find(sp => sp.id === id)).filter(Boolean);
}
function etapasDoSubproduto(subprodutoId, produto, db) {
  return (produto.processo || []).filter(p => (p.subprodutos || []).includes(subprodutoId)).map(p => {
    const et = (db.etapas || []).find(e => e.id === p.etapaId);
    const dep = (db.departamentos || []).find(d => d.id === p.departamentoId);
    return {
      passo: p,
      etapa: et,
      departamento: dep
    };
  });
}

/* Parte que nenhuma etapa processa: alguém vai ter que costurar */
function subprodutosSemEtapa(produto, db) {
  const usados = new Set();
  (produto.processo || []).forEach(p => (p.subprodutos || []).forEach(id => usados.add(id)));
  return subprodutosDoProduto(produto, db).filter(sp => !usados.has(sp.id));
}

/* O outro lado da conta: onde cada material do produto é usado.
   A ficha técnica imprime isso para quem separa o material. */
function etapasDoMaterial(tecidoId, produto, db) {
  return (produto.processo || []).filter(p => (p.materiais || []).includes(tecidoId)).map(p => {
    const et = (db.etapas || []).find(e => e.id === p.etapaId);
    const dep = (db.departamentos || []).find(d => d.id === p.departamentoId);
    return {
      passo: p,
      etapa: et,
      departamento: dep
    };
  });
}

/* Material que ninguém pegou: some no meio do processo */
function materiaisSemEtapa(produto, db) {
  const c = custearProduto(produto, db);
  const usados = new Set();
  (produto.processo || []).forEach(p => (p.materiais || []).forEach(id => usados.add(id)));
  return c.tecidos.filter(t => !usados.has(t.id));
}

/* ==========================================================
   ORDEM DE PRODUÇÃO
   A OP nasce planejada. Ao liberar, o sistema confere material,
   reserva o que existe e cria as etapas com as quantidades.
========================================================== */

/* ==========================================================
   APONTAMENTO DE PRODUÇÃO
   A regra que sustenta tudo: uma etapa só entrega para a
   seguinte o que ela realmente produziu — nunca o planejado.
========================================================== */

/* ==========================================================
   COMPOSIÇÃO DO PRODUTO
   O nome nasce das escolhas: GRUPO + GRAVAÇÃO + TECIDO + MEDIDA.
   Assim dois produtos iguais nunca recebem nomes diferentes.
========================================================== */

/* ==========================================================
   CUSTEIO DO SUBPRODUTO
   Cada peça soma: o tecido que veio do risco, os insumos que
   consome, os subprodutos que recebe prontos e a mão de obra
   das suas etapas — somada por departamento.
========================================================== */

/* ==========================================================
   ETAPA
   Tempo × custo do minuto — de gente e de máquina.
========================================================== */

/* ==========================================================
   LEITURA DE RISCO
   O risco é a medição real da mesa de corte: quantos metros
   de tecido rendem quantas peças. Dele saem as partes — e o
   consumo de cada uma, proporcional à área do molde.
========================================================== */

/* ==========================================================
   CUSTO DO PRODUTO
   Duas listas: o que ele consome e por onde ele passa.
   Do encontro delas sai o custo — e do custo, o preço.
========================================================== */

/* ==========================================================
   LEITOR DE RISCO — HPGL/PLT
   Camada 1: tokenizar · Camada 2: coordenadas
   Camada 3: reconstruir · Camada 4: contornos
   Não é imagem: é geometria de verdade.
========================================================== */

/* --- Camada 1 e 2: comandos e coordenadas --- */
function tokenizarPLT(texto) {
  const corpo = String(texto || '').replace(/[\r\n]/g, '');
  const re = /(IN|IP|SC|PS|SP|QL|PU|PD|PA|PR|PG|BP|LB|VS|LT|WU|PW)([^A-Za-z\x1b]*)/g;
  const cmds = [];
  let m;
  while ((m = re.exec(corpo)) !== null) {
    cmds.push({
      cmd: m[1],
      args: m[2] || ''
    });
  }
  return cmds;
}
function numerosDe(txt) {
  return (String(txt).match(/-?\d+(?:\.\d+)?/g) || []).map(Number);
}

/* --- Camada 3: reconstruir as trajetórias --- */
function reconstruirCaminhos(cmds) {
  let caneta = false,
    pos = [0, 0];
  const caminhos = [];
  let atual = [];
  const fecharAtual = () => {
    if (atual.length > 1) caminhos.push(atual);
    atual = [];
  };
  cmds.forEach(({
    cmd,
    args
  }) => {
    const v = numerosDe(args);
    if (cmd === 'PU') {
      fecharAtual();
      if (v.length >= 2) {
        pos = [v[v.length - 2], v[v.length - 1]];
        atual = [pos];
      }
      caneta = false;
    } else if (cmd === 'PD') {
      caneta = true;
      if (atual.length === 0) atual = [pos];
      for (let i = 0; i + 1 < v.length; i += 2) {
        pos = [v[i], v[i + 1]];
        atual.push(pos);
      }
    } else if (cmd === 'PA') {
      for (let i = 0; i + 1 < v.length; i += 2) {
        pos = [v[i], v[i + 1]];
        if (caneta) atual.push(pos);
      }
    } else if (cmd === 'PG' || cmd === 'SP') {
      fecharAtual();
    }
  });
  fecharAtual();
  return caminhos;
}

/* --- Geometria --- */
function areaPoligono(p) {
  let a = 0;
  for (let i = 0; i < p.length; i++) {
    const [x1, y1] = p[i],
      [x2, y2] = p[(i + 1) % p.length];
    a += x1 * y2 - x2 * y1;
  }
  return Math.abs(a) / 2;
}
function caixaDe(p) {
  let x0 = Infinity,
    y0 = Infinity,
    x1 = -Infinity,
    y1 = -Infinity;
  p.forEach(([x, y]) => {
    if (x < x0) x0 = x;
    if (y < y0) y0 = y;
    if (x > x1) x1 = x;
    if (y > y1) y1 = y;
  });
  return {
    x0,
    y0,
    x1,
    y1,
    largura: x1 - x0,
    altura: y1 - y0
  };
}
function perimetro(p) {
  let d = 0;
  for (let i = 1; i < p.length; i++) d += Math.hypot(p[i][0] - p[i - 1][0], p[i][1] - p[i - 1][1]);
  return d;
}
function ehFechado(p, tol) {
  if (p.length < 3) return false;
  return Math.hypot(p[0][0] - p[p.length - 1][0], p[0][1] - p[p.length - 1][1]) <= (tol || 200);
}
function contido(a, b) {
  return a.x0 >= b.x0 - 1 && a.y0 >= b.y0 - 1 && a.x1 <= b.x1 + 1 && a.y1 <= b.y1 + 1;
}

/* --- Escala: HPGL trabalha em 40 unidades por milímetro --- */
const ESCALAS_PLT = [{
  id: 'hpgl40',
  nome: 'HPGL padrão — 40 un/mm',
  porMm: 40
}, {
  id: 'hpgl20',
  nome: '20 un/mm',
  porMm: 20
}, {
  id: 'mm',
  nome: 'Milímetros',
  porMm: 1
}, {
  id: 'polegada',
  nome: '1016 un/polegada',
  porMm: 1016 / 25.4
}];
function escalaPorId(id) {
  return ESCALAS_PLT.find(e => e.id === id) || ESCALAS_PLT[0];
}

/* Adivinha a escala pelo tamanho da folha: risco de confecção tem
   entre 80 cm e 2,4 m de largura. */
function inferirEscala(caixa) {
  const candidatos = ESCALAS_PLT.map(e => {
    const larguraM = caixa.largura / e.porMm / 1000;
    const alturaM = caixa.altura / e.porMm / 1000;
    const menor = Math.min(larguraM, alturaM);
    const maior = Math.max(larguraM, alturaM);
    const plausivel = menor >= 0.5 && menor <= 3.2 && maior <= 120;
    return {
      escala: e,
      larguraM,
      alturaM,
      plausivel
    };
  });
  const bom = candidatos.find(c => c.plausivel);
  return bom ? bom.escala : ESCALAS_PLT[0];
}

/* --- Camada 4 e 5: classificar cada contorno --- */
const TIPOS_CONTORNO = [{
  id: 'peca',
  nome: 'Peça',
  desc: 'contorno fechado de molde'
}, {
  id: 'interno',
  nome: 'Marca interna',
  desc: 'dentro de uma peça: pence, bolso, dobra'
}, {
  id: 'texto',
  nome: 'Texto',
  desc: 'letra ou número da modelagem'
}, {
  id: 'auxiliar',
  nome: 'Auxiliar',
  desc: 'linha aberta, pique, referência'
}];
function classificarContornos(caminhos, porMm) {
  const mm2 = porMm * porMm; /* unidades² por mm² */
  const AREA_MINIMA_PECA = 2500 * mm2; /* 50 x 50 mm */
  const ALTURA_TEXTO = 10 * porMm; /* letras têm menos de 10 mm */

  const brutos = caminhos.map((pts, i) => {
    const caixa = caixaDe(pts);
    return {
      indice: i,
      pontos: pts,
      caixa,
      area: areaPoligono(pts),
      perimetro: perimetro(pts),
      fechado: ehFechado(pts, porMm * 5),
      nPontos: pts.length
    };
  });

  /* candidatas a peça: fechadas e grandes */
  const candidatas = brutos.filter(c => c.fechado && c.area >= AREA_MINIMA_PECA);
  return brutos.map(c => {
    let tipo = 'auxiliar',
      pai = null;
    if (c.fechado && c.area >= AREA_MINIMA_PECA) {
      /* está dentro de outra peça maior? então é marca interna */
      pai = candidatas.find(o => o !== c && o.area > c.area && contido(c.caixa, o.caixa));
      tipo = pai ? 'interno' : 'peca';
    } else if (c.caixa.altura <= ALTURA_TEXTO && c.caixa.largura <= ALTURA_TEXTO * 2) {
      tipo = 'texto';
      pai = candidatas.find(o => contido(c.caixa, o.caixa));
    } else if (c.fechado) {
      pai = candidatas.find(o => contido(c.caixa, o.caixa));
      tipo = pai ? 'interno' : 'auxiliar';
    } else {
      pai = candidatas.find(o => contido(c.caixa, o.caixa));
    }
    /* guardar a que peça pertence: é o que permite mostrar a marcação junto do molde */
    return {
      ...c,
      tipo,
      paiIndice: pai ? pai.indice : null
    };
  });
}

/* Tudo que está impresso dentro de uma peça: letras, números, piques, pences */
function marcacoesDaPeca(peca, contornos) {
  return contornos.filter(c => c.paiIndice === peca.indice);
}

/* --- Assinatura geométrica: reconhecer a mesma peça repetida --- */
function assinaturaDe(c, porMm) {
  const larguraMm = c.caixa.largura / porMm;
  const alturaMm = c.caixa.altura / porMm;
  const areaMm2 = c.area / (porMm * porMm);
  /* arredonda para tolerar diferença de milímetro entre cópias */
  return [Math.round(larguraMm / 2) * 2, Math.round(alturaMm / 2) * 2, Math.round(areaMm2 / 200) * 200].join('x');
}
function agruparPecas(contornos, porMm) {
  const pecas = contornos.filter(c => c.tipo === 'peca');
  const grupos = {};
  pecas.forEach(c => {
    const chave = assinaturaDe(c, porMm);
    if (!grupos[chave]) grupos[chave] = {
      assinatura: chave,
      ocorrencias: [],
      referencia: c
    };
    grupos[chave].ocorrencias.push(c);
  });
  return Object.values(grupos).map(g => {
    const c = g.referencia;
    const areaMm2 = c.area / (porMm * porMm);
    /* a peça que tiver mais marcação é a melhor para desenhar em miniatura */
    const melhor = g.ocorrencias.map(o => ({
      o,
      marcas: marcacoesDaPeca(o, contornos)
    })).sort((x, y) => y.marcas.length - x.marcas.length)[0];
    return {
      ...g,
      desenho: melhor ? melhor.o : c,
      marcacoes: melhor ? melhor.marcas : [],
      quantidade: g.ocorrencias.length,
      larguraMm: Number((c.caixa.largura / porMm).toFixed(1)),
      alturaMm: Number((c.caixa.altura / porMm).toFixed(1)),
      areaMm2: Number(areaMm2.toFixed(1)),
      areaM2: Number((areaMm2 / 1e6).toFixed(5)),
      proporcao: c.caixa.altura > 0 ? Number((c.caixa.largura / c.caixa.altura).toFixed(3)) : 0,
      posicoes: g.ocorrencias.map(o => ({
        x: Number((o.caixa.x0 / porMm).toFixed(1)),
        y: Number((o.caixa.y0 / porMm).toFixed(1))
      }))
    };
  }).sort((a, b) => b.areaM2 - a.areaM2);
}

/* ==========================================================
   Camada 6 a 8: a análise completa do risco
========================================================== */
function analisarPLT(texto, opcoes) {
  const op = opcoes || {};
  const cmds = tokenizarPLT(texto);
  if (cmds.length === 0) return {
    erro: 'Nenhum comando HPGL encontrado. O arquivo pode não ser um PLT válido.'
  };
  const caminhos = reconstruirCaminhos(cmds);
  if (caminhos.length === 0) return {
    erro: 'Comandos lidos, mas nenhuma trajetória foi desenhada. ' + 'O arquivo pode usar um dialeto que ainda não interpretamos.',
    comandos: resumoComandos(cmds)
  };
  const todos = caminhos.flat();
  const caixa = caixaDe(todos);
  const escala = op.escalaId ? escalaPorId(op.escalaId) : inferirEscala(caixa);
  const porMm = escala.porMm;
  const contornos = classificarContornos(caminhos, porMm);
  const grupos = agruparPecas(contornos, porMm);

  /* o risco é medido pela extensão real do desenho */
  const larguraMm = caixa.largura / porMm;
  const alturaMm = caixa.altura / porMm;
  /* na mesa, a largura do tecido é o menor lado */
  const larguraTecidoM = Number((Math.min(larguraMm, alturaMm) / 1000).toFixed(4));
  const comprimentoM = Number((Math.max(larguraMm, alturaMm) / 1000).toFixed(4));
  const areaRiscoM2 = Number((larguraTecidoM * comprimentoM).toFixed(5));
  const areaPecasM2 = Number(grupos.reduce((a, g) => a + g.areaM2 * g.quantidade, 0).toFixed(5));
  const aproveitamento = areaRiscoM2 > 0 ? Number((areaPecasM2 / areaRiscoM2 * 100).toFixed(2)) : 0;
  const totalPecas = grupos.reduce((a, g) => a + g.quantidade, 0);
  return {
    arquivo: op.nome || '',
    comandos: resumoComandos(cmds),
    escala,
    escalaInferida: !op.escalaId,
    caixa,
    trajetorias: caminhos.length,
    pontos: todos.length,
    contornos,
    contagem: {
      peca: contornos.filter(c => c.tipo === 'peca').length,
      interno: contornos.filter(c => c.tipo === 'interno').length,
      texto: contornos.filter(c => c.tipo === 'texto').length,
      auxiliar: contornos.filter(c => c.tipo === 'auxiliar').length
    },
    grupos,
    totalPecas,
    larguraTecidoM,
    comprimentoM,
    areaRiscoM2,
    areaPecasM2,
    aproveitamento,
    desperdicio: Number((100 - aproveitamento).toFixed(2)),
    consumoPorPeca: totalPecas > 0 ? Number((comprimentoM / totalPecas).toFixed(5)) : 0
  };
}
function resumoComandos(cmds) {
  const c = {};
  cmds.forEach(x => {
    c[x.cmd] = (c[x.cmd] || 0) + 1;
  });
  return Object.entries(c).map(([cmd, n]) => ({
    cmd,
    n
  })).sort((a, b) => b.n - a.n);
}

/* ==========================================================
   Camada 9: a composição do produto a partir das partes


/* Quantos produtos saem do risco, dada a composição */
function produtosDoRisco(risco) {
  const partes = (risco.partes || []).filter(p => num(p.porProduto) > 0);
  if (partes.length === 0) return null;
  const possiveis = partes.map(p => Math.floor(num(p.quantidade) / num(p.porProduto)));
  const produtos = Math.min(...possiveis);
  return {
    produtos,
    gargalo: partes[possiveis.indexOf(produtos)],
    sobra: partes.map((p, i) => ({
      parte: p,
      sobrando: num(p.quantidade) - produtos * num(p.porProduto)
    })).filter(x => x.sobrando > 0)
  };
}

/* O caminho natural: você sabe quantas peças precisa entregar.
   O sistema diz quantas camadas enfestar — e o que vai sobrar. */
function planejarEnfesto(risco, desejado) {
  const alvo = num(desejado);
  const comp = num(risco.comprimentoM);
  const composicao = produtosDoRisco(risco);

  /* se as partes formam produto, a conta é por produto; senão, por peça */
  const porCamada = composicao ? composicao.produtos : num(risco.totalPecas);
  const unidade = composicao ? 'produto' : 'peça';
  if (!(porCamada > 0)) return {
    erro: 'Esta modelagem não rende nenhuma peça inteira. ' + 'Confira as partes e quantas cada produto leva.',
    porCamada: 0,
    unidade
  };
  if (!(alvo > 0)) return {
    porCamada,
    unidade,
    camadas: 0,
    produzido: 0,
    sobra: 0,
    tecidoTotal: 0,
    vazio: true
  };
  const camadas = Math.ceil(alvo / porCamada);
  const produzido = camadas * porCamada;
  const sobra = produzido - alvo;
  const tecidoTotal = Number((comp * camadas).toFixed(2));

  /* a última camada pode ser desnecessária se a sobra for grande */
  const semUltima = camadas > 1 ? (camadas - 1) * porCamada : 0;
  const faltariam = alvo - semUltima;
  return {
    porCamada,
    unidade,
    alvo,
    camadas,
    produzido,
    sobra,
    tecidoTotal,
    pecasCortadas: num(risco.totalPecas) * camadas,
    consumoPorUnidade: produzido > 0 ? Number((tecidoTotal / produzido).toFixed(5)) : 0,
    consumoDoPedido: alvo > 0 ? Number((tecidoTotal / alvo).toFixed(5)) : 0,
    /* quanto de tecido a sobra representa */
    tecidoDaSobra: produzido > 0 ? Number((tecidoTotal * (sobra / produzido)).toFixed(2)) : 0,
    exato: sobra === 0,
    faltariam: camadas > 1 ? faltariam : null
  };
}

/* Palavras que denunciam um tecido, quando o grupo ainda não foi marcado */
const PISTAS_TECIDO = ['tecido', 'malha', 'oxford', 'brim', 'microfibra', 'tnt', 'forro', 'tactel', 'helanca', 'viscose', 'algodao', 'poliester', 'sarja', 'jeans', 'moletom', 'ribana', 'crepe', 'linho', 'lona', 'nylon', 'chiffon', 'cetim', 'tricoline', 'soft', 'plush'];
function pareceTecido(nome) {
  const n = normaliza(nome || '');
  return PISTAS_TECIDO.some(p => n.includes(p));
}

/* Marca os grupos que ainda não têm resposta. Bancos criados antes deste
   campo não sabem o que é tecido — aqui eles descobrem pelo nome. */
function migrarGruposCortaveis(d) {
  const grupos = d.gruposMaterial || [];
  if (grupos.length === 0) return d;
  /* se alguém já respondeu, respeitamos a escolha e não mexemos */
  if (grupos.some(g => g.cortavel !== undefined)) return d;
  const paiCortavel = {};
  grupos.filter(g => !g.paiId).forEach(g => {
    paiCortavel[g.id] = pareceTecido(g.nome);
  });
  d.gruposMaterial = grupos.map(g => ({
    ...g,
    cortavel: g.paiId ? paiCortavel[g.paiId] || pareceTecido(g.nome) : paiCortavel[g.id]
  }));
  return d;
}

/* Tecido é o material que se corta: pertence a um grupo cortável.
   Aviamento, tinta e embalagem entram no produto, mas sem risco. */
function ehTecido(material, db) {
  if (!material) return false;
  if (material.cortavel === true) return true;
  if (material.cortavel === false) return false;
  const g = (db.gruposMaterial || []).find(x => x.id === material.grupoId);
  if (g) {
    if (g.cortavel === true) return true;
    const pai = g.paiId ? (db.gruposMaterial || []).find(x => x.id === g.paiId) : null;
    if (pai && pai.cortavel === true) return true;
    /* grupo existe mas ninguém decidiu: o nome resolve */
    if (g.cortavel === undefined && (!pai || pai.cortavel === undefined)) return pareceTecido(g.nome) || (pai ? pareceTecido(pai.nome) : false) || pareceTecido(material.nome);
    return false;
  }

  /* material sem grupo: só o nome dele resolve */
  return pareceTecido(material.nome);
}

/* ==========================================================
   PRODUTO
   O nome nasce das escolhas — GRUPO + TIPO + MEDIDA + TECIDO —
   e o tecido traz junto o risco, que define as partes.
========================================================== */
const GRUPOS_PRODUTO_PADRAO = ['Avental', 'Capa', 'Camiseta', 'Touca', 'Jaleco', 'Bolsa', 'Almofada'];
const TIPOS_PRODUTO_PADRAO = [{
  nome: 'Liso',
  sigla: ''
}, {
  nome: 'Silk',
  sigla: 'SK'
}, {
  nome: 'Bordado',
  sigla: 'BD'
}, {
  nome: 'Sublimado',
  sigla: 'SB'
}, {
  nome: 'DTF',
  sigla: 'DT'
}];
const FORMATOS_MEDIDA = [{
  id: 'dimensoes',
  nome: 'Dimensões (60x80)'
}, {
  id: 'grade',
  nome: 'Grade (P, M, G)'
}, {
  id: 'unico',
  nome: 'Tamanho único'
}];
function semearProduto(d) {
  if ((d.gruposProduto || []).length === 0) d.gruposProduto = GRUPOS_PRODUTO_PADRAO.map(nome => ({
    id: uid(),
    nome,
    sigla: normaliza(nome).slice(0, 2).toUpperCase(),
    ativo: true
  }));
  if ((d.tiposProduto || []).length === 0) d.tiposProduto = TIPOS_PRODUTO_PADRAO.map(x => ({
    id: uid(),
    ...x,
    ativo: true
  }));
  return d;
}
function textoMedida(produto) {
  const m = produto && produto.medida || {};
  if (m.formato === 'grade') return (m.tamanhos || []).join('/') || 'GRADE';
  if (m.formato === 'unico') return 'ÚNICO';
  const p = [m.largura, m.altura, m.profundidade].filter(x => num(x) > 0).map(x => num(x));
  return p.join('X');
}
function tecidosDoProduto(produto, db) {
  return (produto.tecidos || []).map(t => ({
    ...t,
    material: (db.materiais || []).find(m => m.id === t.materialId) || null,
    risco: (db.riscos || []).find(r => r.id === t.riscoId) || null
  }));
}
function montarNomeProduto(produto, db) {
  if (!produto) return '';
  const g = (db.gruposProduto || []).find(x => x.id === produto.grupoId);
  const tp = (db.tiposProduto || []).find(x => x.id === produto.tipoId);
  /* O nome identifica o produto pelo que ele é feito, não por tudo que
     leva dentro. Um avental é "de nylon" — a linha, a fita e a etiqueta
     estão lá, mas ninguém chama o produto por elas. Se todos os
     materiais entrassem, dois produtos iguais com linha diferente
     teriam nomes diferentes. */
  const nomesUnicos = lista => [...new Set(lista)];
  const cortaveis = tecidosDoProduto(produto, db).filter(t => t.material && ehTecido(t.material, db)).map(t => t.material.nome).filter(Boolean);
  /* produto que não usa tecido nenhum (só aviamento montado) ainda
     precisa de nome: aí valem os materiais que existirem */
  const tecidos = nomesUnicos(cortaveis.length ? cortaveis : tecidosDoProduto(produto, db).map(t => t.material ? t.material.nome : '').filter(Boolean));
  const partes = [g ? g.nome : '', tp && tp.nome !== 'Liso' ? tp.nome : '', textoMedida(produto), tecidos.join(' + '), produto.complemento || ''].filter(Boolean).map(x => String(x).toUpperCase().trim());

  /* Grupo "Avental" com tipo "Avental" dava "AVENTAL AVENTAL ÚNICO".
     Termo que já apareceu não repete — o nome existe para identificar,
     e repetição só atrapalha a leitura. */
  const vistos = new Set();
  const limpas = partes.filter(x => {
    const chave = x.replace(/\s+/g, ' ');
    if (vistos.has(chave)) return false;
    vistos.add(chave);
    return true;
  });
  return limpas.join(' ').replace(/\s+/g, ' ').trim();
}

/* Código sequencial por grupo: olha o maior já usado, não a contagem */
function proximoCodigoProduto(d, grupoId) {
  const g = (d.gruposProduto || []).find(x => x.id === grupoId);
  const sigla = g ? g.sigla || normaliza(g.nome).slice(0, 3).toUpperCase() : 'PRD';
  const usados = (d.produtos || []).map(p => String(p.codigo || '')).filter(c => c.startsWith(sigla)).map(c => num((c.slice(sigla.length).match(/^\d+/) || [])[0])).filter(n => n > 0);
  return `${sigla}${String((usados.length ? Math.max(...usados) : 0) + 1).padStart(3, '0')}`;
}
function codigoCompletoProduto(produto, db) {
  const tp = (db.tiposProduto || []).find(x => x.id === produto.tipoId);
  return `${produto.codigo || ''}${tp && tp.sigla ? '.' + tp.sigla : ''}`;
}

/* ==========================================================
   AS PARTES DO RISCO VIRAM SUBPRODUTOS
========================================================== */

/* Quanto de tecido cabe a cada parte, pela área do molde */
function rateioDasPartes(risco) {
  const partes = risco.partes || [];
  const total = partes.reduce((a, p) => a + num(p.areaM2) * num(p.quantidade), 0);
  return partes.map(p => {
    const area = num(p.areaM2) * num(p.quantidade);
    return {
      parte: p,
      fracao: total > 0 ? area / total : partes.length ? 1 / partes.length : 0
    };
  });
}

/* O consumo de um risco por produto acabado */
function consumoDoRisco(risco) {
  if (!risco) return null;
  const comp = num(risco.comprimentoM);
  const composicao = produtosDoRisco(risco);
  const porRisco = composicao ? composicao.produtos : num(risco.totalPecas);
  if (!(porRisco > 0) || !(comp > 0)) return null;
  return {
    porRisco,
    consumo: Number((comp / porRisco).toFixed(5)),
    unidade: composicao ? 'produto' : 'peça'
  };
}

/* Um aviamento pode virar peça antes de entrar no produto: o rolo de
   viés que vira tiras, a fita que vira alças. A conversão diz quantas
   peças saem de cada unidade comprada. */
function converterAviamento(tecido) {
  const conv = tecido && tecido.conversao || null;
  if (!conv || !conv.ativa) return null;
  const porUnidade = num(conv.pecasPorUnidade);
  const porProduto = num(conv.porProduto) > 0 ? num(conv.porProduto) : 1;
  if (!(porUnidade > 0)) return {
    erro: 'Informe quantas peças saem de cada unidade.'
  };
  const perda = num(conv.perda);
  const aproveitadas = porUnidade * (1 - perda / 100);
  if (!(aproveitadas > 0)) return {
    erro: 'A perda consome toda a produção da unidade.'
  };
  return {
    nome: conv.nome || 'Peça do aviamento',
    pecasPorUnidade: porUnidade,
    perda,
    aproveitadas: Number(aproveitadas.toFixed(4)),
    porProduto,
    /* o consumo por produto é o inverso do rendimento */
    consumo: Number((porProduto / aproveitadas).toFixed(6))
  };
}

/* Cria os subprodutos a partir das partes do risco */
function gerarSubprodutos(d, produtoId, tecidoId, risco, usuario) {
  const existentes = (d.subprodutos || []).filter(sp => sp.produtoId === produtoId && sp.tecidoId === tecidoId);
  const c = consumoDoRisco(risco);
  const rateio = rateioDasPartes(risco);
  const criados = [];
  rateio.forEach(({
    parte,
    fracao
  }) => {
    const ja = existentes.find(sp => sp.assinatura === parte.assinatura);
    const reg = {
      id: ja ? ja.id : uid(),
      codigo: ja ? ja.codigo : `SUB-${String((d.subprodutos || []).length + criados.length + 1).padStart(4, '0')}`,
      produtoId,
      tecidoId,
      riscoId: risco.id,
      assinatura: parte.assinatura,
      nome: parte.nome,
      tamanho: parte.tamanho || '',
      porProduto: num(parte.porProduto) || 1,
      noRisco: num(parte.quantidade),
      larguraMm: num(parte.larguraMm),
      alturaMm: num(parte.alturaMm),
      areaM2: num(parte.areaM2),
      fracaoTecido: Number(fracao.toFixed(5)),
      consumo: c ? Number((c.consumo * fracao).toFixed(5)) : 0,
      etapas: ja ? ja.etapas : [],
      criadoEm: ja ? ja.criadoEm : agoraISO()
    };
    criados.push(reg);
  });

  /* substitui os deste tecido, preserva os dos outros */
  d.subprodutos = [...(d.subprodutos || []).filter(sp => !(sp.produtoId === produtoId && sp.tecidoId === tecidoId)), ...criados];
  return criados;
}
function subprodutosDoProduto(produto, db) {
  return (db.subprodutos || []).filter(sp => sp.produtoId === produto.id);
}

/* O custo de tecido do produto, tecido a tecido */
function custearProduto(produto, db) {
  const tecidos = tecidosDoProduto(produto, db).map(t => {
    const cortavel = ehTecido(t.material, db);
    const preco = t.material ? custoDoMaterial(t.material) : 0;
    if (!cortavel) {
      const conv = converterAviamento(t);
      if (conv && !conv.erro) {
        /* virou peça: o consumo sai do rendimento, não da digitação */
        return {
          ...t,
          cortavel: false,
          calculo: null,
          conversao: conv,
          consumo: conv.consumo,
          custo: Number((conv.consumo * preco).toFixed(8)),
          semRisco: false,
          semQuantidade: false,
          semPreco: !t.material || preco === 0
        };
      }
      /* aviamento: o consumo é informado à mão, não vem de risco */
      const q = num(t.quantidade);
      return {
        ...t,
        cortavel: false,
        calculo: null,
        conversaoErro: conv && conv.erro ? conv.erro : null,
        consumo: q,
        custo: Number((q * preco).toFixed(8)),
        semRisco: false,
        semQuantidade: !(q > 0),
        semPreco: !t.material || preco === 0
      };
    }
    const c = consumoDoRisco(t.risco);
    return {
      ...t,
      cortavel: true,
      calculo: c,
      consumo: c ? c.consumo : 0,
      custo: c ? Number((c.consumo * preco).toFixed(8)) : 0,
      semRisco: !t.risco,
      semPreco: !t.material || preco === 0
    };
  });

  /* o aviamento convertido também é um subproduto: tem nome e é produzido */
  const doAviamento = tecidos.filter(t => t.conversao).map(t => ({
    id: 'conv-' + t.id,
    codigo: 'AVI-' + String(t.id).slice(-4).toUpperCase(),
    produtoId: produto.id,
    tecidoId: t.id,
    nome: t.conversao.nome,
    tamanho: '',
    porProduto: t.conversao.porProduto,
    origem: 'aviamento',
    material: t.material,
    consumo: t.conversao.consumo,
    custo: t.custo,
    conversao: t.conversao
  }));
  const subs = subprodutosDoProduto(produto, db).map(sp => {
    const t = tecidos.find(x => x.id === sp.tecidoId);
    const preco = t && t.material ? custoDoMaterial(t.material) : 0;
    return {
      ...sp,
      tecido: t ? t.material : null,
      origem: 'risco',
      custo: Number((num(sp.consumo) * preco).toFixed(8))
    };
  }).concat(doAviamento);
  const avisos = [];
  const semRisco = tecidos.filter(t => t.cortavel && t.semRisco);
  if (semRisco.length) avisos.push(`Tecido sem modelagem: ${semRisco.map(t => t.material ? t.material.nome : '—').join(', ')}. ` + `Sem a modelagem, não há partes nem consumo.`);
  const convErro = tecidos.filter(t => t.conversaoErro);
  if (convErro.length) avisos.push(`Conversão incompleta: ${convErro.map(t => (t.material ? t.material.nome : '—') + ' — ' + t.conversaoErro).join('; ')}`);
  const semQtd = tecidos.filter(t => !t.cortavel && t.semQuantidade);
  if (semQtd.length) avisos.push(`Sem quantidade em: ${semQtd.map(t => t.material ? t.material.nome : '—').join(', ')}.`);
  const semPreco = tecidos.filter(t => !t.semRisco && !t.semQuantidade && t.semPreco);
  if (semPreco.length) avisos.push(`Sem preço em: ${semPreco.map(t => t.material ? t.material.nome : '—').join(', ')}.`);
  if (tecidos.length === 0) avisos.push('Nenhum material escolhido.');
  const total = tecidos.reduce((a, t) => a + t.custo, 0);

  /* O mesmo material pode entrar várias vezes — corpo e forro do mesmo
     oxford, por exemplo. Para comprar e reservar, o que importa é o total
     por material, não linha a linha. */
  const porMaterial = [];
  tecidos.forEach(t => {
    const chave = t.materialId || t.id;
    let g = porMaterial.find(x => x.materialId === chave);
    if (!g) {
      g = {
        materialId: chave,
        material: t.material,
        nome: t.material ? t.material.nome : 'material removido',
        unidade: t.material ? t.material.unidadeEstoque : '',
        cortavel: t.cortavel,
        consumo: 0,
        custo: 0,
        usos: [],
        indefinido: false
      };
      porMaterial.push(g);
    }
    g.consumo = Number((g.consumo + num(t.consumo)).toFixed(5));
    g.custo = Number((g.custo + num(t.custo)).toFixed(5));
    g.usos.push(t);
    if (num(t.consumo) === 0) g.indefinido = true;
  });
  return {
    produto,
    tecidos,
    subprodutos: subs,
    cortaveis: tecidos.filter(t => t.cortavel),
    aviamentos: tecidos.filter(t => !t.cortavel),
    porMaterial,
    repetidos: porMaterial.filter(g => g.usos.length > 1),
    /* 8 casas no custo unitário: material barato com consumo pequeno
       (linha, tinta) perdia precisão em 5 casas, e o erro aparecia
       multiplicado no total da ordem */
    custoTecido: Number(total.toFixed(8)),
    consumoTotal: Number(tecidos.reduce((a, t) => a + t.consumo, 0).toFixed(5)),
    avisos,
    completo: tecidos.length > 0 && avisos.length === 0
  };
}

/* ==========================================================
   ENGENHARIA
   O mapa da fábrica: onde se trabalha, o que se faz e com quê.
   Só cadastro — tempo e custo entram depois.
========================================================== */
/* Aferir com cronômetro e validar meta faz sentido onde a operação se
   repete peça a peça e a diferença entre pessoas é grande — na costura.
   No corte, na embalagem ou na expedição a meta sai da conta simples:
   o que a jornada produziu dividido pelo tempo dela. Marcar isso no
   departamento deixa a fábrica configurar: uma segunda linha de costura
   ou um acabamento fino entram sem mexer no sistema. */
const DEPARTAMENTOS_PADRAO = [{
  nome: 'Corte',
  sigla: 'CT'
}, {
  nome: 'Estamparia',
  sigla: 'ET'
}, {
  nome: 'Bordado',
  sigla: 'BD'
}, {
  nome: 'Preparação',
  sigla: 'PR'
}, {
  nome: 'Costura',
  sigla: 'CS',
  exigeAfericao: true
}, {
  nome: 'Acabamento',
  sigla: 'AC'
}, {
  nome: 'Qualidade',
  sigla: 'QL'
}, {
  nome: 'Embalagem',
  sigla: 'EM'
}, {
  nome: 'Diversos',
  sigla: 'DV'
}];

/* Etapas que quase toda confecção tem, já no departamento certo */
const ETAPAS_PADRAO = {
  'Corte': ['Encaixe', 'Enfesto', 'Corte', 'Separação', 'Amarração', 'Conferência'],
  'Estamparia': ['Preparação de tela', 'Silk', 'DTF', 'Sublimação', 'Cura'],
  'Bordado': ['Preparação da matriz', 'Bordado', 'Limpeza do bordado'],
  'Preparação': ['Preparação de peças', 'Fusão de entretela', 'Passar borneon'],
  'Costura': ['Fechar ombro', 'Pregar gola', 'Colocar manga', 'Fechar lateral', 'Bainha da manga', 'Bainha da barra', 'Pregar bolso', 'Pregar etiqueta'],
  'Acabamento': ['Limpeza de linhas', 'Revisão', 'Passadoria'],
  'Qualidade': ['Inspeção', 'Reprovação e retrabalho'],
  'Embalagem': ['Dobra', 'Embalar', 'Etiquetar', 'Encaixotar'],
  'Diversos': ['Deslocamento interno', 'Espera', 'Retrabalho', 'Apoio']
};

/* Como o tempo da etapa se converte em custo por peça */
const MODOS_ETAPA = [{
  id: 'pessoa',
  nome: 'Por pessoa',
  desc: 'uma pessoa executa a peça; o tempo é por unidade'
}, {
  id: 'equipe',
  nome: 'Por equipe',
  desc: 'várias pessoas na mesma peça; o tempo-homem multiplica'
}, {
  id: 'projeto',
  nome: 'Por projeto',
  desc: 'gasto uma vez por lote — setup, matriz, tela — e diluído entre as peças'
}];
function modoEtapa(id) {
  return MODOS_ETAPA.find(m => m.id === id) || MODOS_ETAPA[0];
}

/* O minuto-homem que cada peça consome, conforme o modo */
function minutosPorPeca(modo, tempo, pessoas, lote, loteEtapa) {
  const t = num(tempo),
    p = num(pessoas) > 0 ? num(pessoas) : 1;
  const q = num(lote) > 0 ? num(lote) : 1;
  const n = num(loteEtapa);

  /* a etapa pode declarar para quantas peças o tempo rende: "a equipe
     leva 30 min e sai com 50 peças". É propriedade da etapa, não da
     ordem, então vale sempre que estiver preenchido. */
  if (n > 0) return Number((t * p / n).toFixed(6));
  if (modo === 'projeto') return Number((t * p / q).toFixed(6));
  if (modo === 'equipe') return Number((t * p).toFixed(6));
  return Number(t.toFixed(6)); /* por pessoa: pessoas não multiplicam */
}
const TIPOS_EQUIPAMENTO = [{
  id: 'maquina',
  nome: 'Máquina'
}, {
  id: 'ferramenta',
  nome: 'Ferramenta'
}, {
  id: 'movel',
  nome: 'Móvel e bancada'
}, {
  id: 'informatica',
  nome: 'Informática'
}, {
  id: 'outro',
  nome: 'Outro'
}];
const SITUACOES_EQUIPAMENTO = [{
  id: 'operando',
  nome: 'Operando',
  tone: 'ok'
}, {
  id: 'parado',
  nome: 'Parado',
  tone: 'idle'
}, {
  id: 'manutencao',
  nome: 'Manutenção',
  tone: 'warn'
}, {
  id: 'baixado',
  nome: 'Baixado',
  tone: 'bad'
}];
function situacaoEquip(id) {
  return SITUACOES_EQUIPAMENTO.find(x => x.id === id) || SITUACOES_EQUIPAMENTO[0];
}
function semearEngenharia(d) {
  if (!d.jornada) d.jornada = JORNADA_PADRAO;
  if ((d.departamentos || []).length === 0) {
    d.departamentos = DEPARTAMENTOS_PADRAO.map((x, i) => ({
      id: uid(),
      codigo: `DEP-${String(i + 1).padStart(3, '0')}`,
      ...x,
      responsavel: '',
      descricao: '',
      ativo: true,
      criadoEm: agoraISO()
    }));
  }
  if ((d.etapas || []).length === 0) {
    const etapas = [];
    (d.departamentos || []).forEach(dep => {
      (ETAPAS_PADRAO[dep.nome] || []).forEach((nome, i) => {
        etapas.push({
          id: uid(),
          codigo: `ETP-${String(etapas.length + 1).padStart(4, '0')}`,
          nome,
          departamentoId: dep.id,
          sequencia: (i + 1) * 10,
          modo: /tela|matriz|encaixe|enfesto|cura/i.test(nome) ? 'projeto' : 'pessoa',
          descricao: '',
          ativo: true,
          criadoEm: agoraISO()
        });
      });
    });
    d.etapas = etapas;
  }
  return d;
}

/* Consultas que as telas usam */
function etapasDoDepartamento(departamentoId, db) {
  return (db.etapas || []).filter(e => e.departamentoId === departamentoId).sort((a, b) => num(a.sequencia) - num(b.sequencia));
}
function equipamentosDoDepartamento(departamentoId, db) {
  return (db.equipamentos || []).filter(e => e.departamentoId === departamentoId);
}
function proximoCodigo(lista, prefixo, digitos) {
  const n = digitos || 4;
  const usados = (lista || []).map(x => String(x.codigo || '')).filter(c => c.startsWith(prefixo + '-')).map(c => num(c.slice(prefixo.length + 1))).filter(v => v > 0);
  return `${prefixo}-${String((usados.length ? Math.max(...usados) : 0) + 1).padStart(n, '0')}`;
}

/* ==========================================================
   JORNADA
   06:00 às 15:48, de segunda a sexta. Descontados os intervalos,
   sobram os minutos que a fábrica realmente produz.
========================================================== */
const JORNADA_PADRAO = {
  inicio: '06:00:00',
  fim: '15:48:00',
  intervalos: [{
    id: 'cafe',
    nome: 'Café',
    inicio: '09:00:00',
    fim: '09:15:00'
  }, {
    id: 'almoco',
    nome: 'Almoço',
    inicio: '12:00:00',
    fim: '13:00:00'
  }],
  diasSemana: [1, 2, 3, 4, 5],
  extensao: {
    fim: '17:48:00',
    exige: 'Superior'
  },
  sabado: {
    inicio: '06:00:00',
    fim: '12:00:00',
    exige: 'Superior'
  }
};

/* Bancos antigos guardaram 06:00; o campo agora quer 06:00:00 */
function comSegundos(h) {
  const t = String(h || '').trim();
  if (!t) return t;
  const p = t.split(':');
  if (p.length === 2) return t + ':00';
  return t;
}
function migrarJornada(d) {
  const j = d.jornada;
  if (!j) return d;
  d.jornada = {
    ...j,
    inicio: comSegundos(j.inicio),
    fim: comSegundos(j.fim),
    intervalos: (j.intervalos || []).map(i => ({
      ...i,
      inicio: comSegundos(i.inicio),
      fim: comSegundos(i.fim)
    })),
    extensao: j.extensao ? {
      ...j.extensao,
      fim: comSegundos(j.extensao.fim)
    } : j.extensao,
    sabado: j.sabado ? {
      ...j.sabado,
      inicio: comSegundos(j.sabado.inicio),
      fim: comSegundos(j.sabado.fim)
    } : j.sabado
  };
  return d;
}
function minutosEntre(hIni, hFim) {
  return Math.max(horaParaMinutos(hFim) - horaParaMinutos(hIni), 0);
}
function jornadaDe(db) {
  const j = db && db.jornada || JORNADA_PADRAO;
  const bruto = minutosEntre(j.inicio, j.fim);
  const pausas = (j.intervalos || []).reduce((a, i) => a + minutosEntre(i.inicio, i.fim), 0);
  const produtivo = Math.max(bruto - pausas, 0);
  const extra = j.extensao ? minutosEntre(j.fim, j.extensao.fim) : 0;
  const sab = j.sabado ? minutosEntre(j.sabado.inicio, j.sabado.fim) : 0;
  return {
    ...j,
    bruto,
    pausas,
    produtivo,
    minutosExtras: extra,
    comExtras: produtivo + extra,
    minutosSabado: sab,
    diasSemana: j.diasSemana || [1, 2, 3, 4, 5]
  };
}

/* ==========================================================
   PROCESSO PRODUTIVO
   A sequência de etapas do produto, cada uma no seu setor,
   com equipamento e tempo por peça.
========================================================== */
/* Sem ordem aberta, o produto se mede por uma peça. Etapa por
   projeto conta inteira aqui — é na ordem, com a quantidade real,
   que ela se divide entre as peças. */
function loteDoProduto() {
  return 1;
}
function passosDoProcesso(produto, db, lote) {
  const q = num(lote) > 0 ? num(lote) : loteDoProduto(produto);
  return (produto.processo || []).slice().sort((a, b) => num(a.ordem) - num(b.ordem)).map(p => {
    const dep = (db.departamentos || []).find(d => d.id === p.departamentoId);
    const et = (db.etapas || []).find(e => e.id === p.etapaId);
    /* O roteiro pode indicar mais de uma máquina para a mesma operação:
       "fecha no overloque ou na interlock". Guardamos as duas coisas —
       a lista, para o planejamento escolher, e a primeira, para quem
       lê a ficha. */
    const idsEq = Array.isArray(p.equipamentos) && p.equipamentos.length
      ? p.equipamentos
      : (p.equipamentoId ? [p.equipamentoId] : []);
    const listaEq = idsEq
      .map(id => (db.equipamentos || []).find(x => x.id === id))
      .filter(Boolean);
    const eq = listaEq[0] || null;
    const pessoas = num(p.pessoas) > 0 ? num(p.pessoas) : 1;
    /* bancos antigos guardaram "45s" como texto; aqui vira número */
    const tempo = typeof p.tempo === 'string' && /[^\d.,\s]/.test(p.tempo) ? textoParaMinutos(p.tempo, {
      padraoSegundos: true
    }) : num(p.tempo);
    /* o modo vem da etapa cadastrada, mas o produto pode ajustar */
    const modo = p.modo || et && et.modo || 'pessoa';
    const loteEtapa = num(p.loteEtapa);
    const porPeca = minutosPorPeca(modo, tempo, pessoas, q, loteEtapa);
    return {
      passo: p,
      departamento: dep,
      etapa: et,
      equipamento: eq,
      equipamentos: listaEq,
      equipamentoIds: idsEq,
      pessoas,
      tempo,
      modo,
      lote: q,
      loteEtapa,
      rendePara: loteEtapa > 0 ? loteEtapa : null,
      /* o tempo-homem é o que consome a capacidade do setor */
      minutosHomem: Number(porPeca.toFixed(5)),
      /* no projeto, o gasto acontece uma vez só */
      minutosDoLote: modo === 'projeto' && !(loteEtapa > 0) ? Number((tempo * pessoas).toFixed(4)) : Number((porPeca * q).toFixed(4)),
      semTempo: !(tempo > 0),
      semEtapa: !et
    };
  });
}
function resumoProcesso(produto, db, lote) {
  const passos = passosDoProcesso(produto, db, lote);
  const q = passos.length ? passos[0].lote : loteDoProduto(produto);
  /* o tempo "por peça" só faz sentido depois de diluir o projeto */
  const minutos = passos.reduce((a, p) => a + (p.modo === 'projeto' ? p.minutosHomem / Math.max(p.pessoas, 1) : p.tempo), 0);
  const minutosHomem = passos.reduce((a, p) => a + p.minutosHomem, 0);
  const mapa = {};
  passos.forEach(p => {
    const nome = p.departamento ? p.departamento.nome : 'Sem departamento';
    if (!mapa[nome]) mapa[nome] = {
      nome,
      passos: 0,
      minutos: 0,
      minutosHomem: 0
    };
    mapa[nome].passos++;
    mapa[nome].minutos += p.tempo;
    mapa[nome].minutosHomem += p.minutosHomem;
  });
  const avisos = [];
  const semTempo = passos.filter(p => p.semTempo);
  if (semTempo.length) avisos.push(`Sem tempo: ${semTempo.map(p => p.etapa ? p.etapa.nome : 'etapa removida').join(', ')}. ` + `Sem o tempo não há cronograma.`);
  const semEtapa = passos.filter(p => p.semEtapa);
  if (semEtapa.length) avisos.push(`${semEtapa.length} etapa(s) foram removidas do cadastro.`);
  if (passos.length === 0) avisos.push('Nenhuma etapa no processo.');
  return {
    passos,
    lote: q,
    minutos: Number(minutos.toFixed(5)),
    minutosHomem: Number(minutosHomem.toFixed(5)),
    minutosDoLote: Number(passos.reduce((a, p) => a + p.minutosDoLote, 0).toFixed(2)),
    porDepartamento: Object.values(mapa).map(d => ({
      ...d,
      minutos: Number(d.minutos.toFixed(4)),
      minutosHomem: Number(d.minutosHomem.toFixed(4)),
      percentual: minutosHomem > 0 ? Number((d.minutosHomem / minutosHomem * 100).toFixed(1)) : 0
    })).sort((a, b) => b.minutosHomem - a.minutosHomem),
    avisos,
    pronto: passos.length > 0 && avisos.length === 0
  };
}

/* ==========================================================
   CUSTO DA MÃO DE OBRA
   A média salarial de quem trabalha no departamento, dividida
   pelos minutos que ele produz no mês, dá o custo do minuto.
========================================================== */
const ENCARGOS_PADRAO = 80; // % sobre o salário
const DIAS_UTEIS_MES = 22;

/* O vale-transporte entra no custo do minuto: é dinheiro que a
   empresa gasta para a pessoa estar na máquina. */
const DESCONTO_VT_LEGAL = 6; // % do salário, limite da lei

function custoConducao(colab, db) {
  if (!colab || !colab.usaConducao) return {
    usa: false,
    diario: 0,
    mensal: 0,
    descontoLegal: 0,
    custoEmpresa: 0
  };
  const dias = num(colab.diasConducao) || num((db.parametrosMaoDeObra || {}).diasUteis) || DIAS_UTEIS_MES;
  const diario = num(colab.conducoesDia) * num(colab.valorConducao);
  const mensal = diario * dias;
  const teto = num(colab.salario) * (DESCONTO_VT_LEGAL / 100);
  const desconto = Math.min(mensal, teto);
  return {
    usa: true,
    dias,
    diario: Number(diario.toFixed(2)),
    mensal: Number(mensal.toFixed(2)),
    descontoLegal: Number(desconto.toFixed(2)),
    custoEmpresa: Number(Math.max(mensal - desconto, 0).toFixed(2))
  };
}
function colaboradoresDoDepartamento(departamentoId, db) {
  return (db.colaboradores || []).filter(c => c.departamentoId === departamentoId && c.status !== 'Inativo');
}

/* A média da fábrica inteira — a rede de segurança quando o setor
   ainda não tem gente cadastrada. */
function mediaGeralFabrica(db) {
  const todos = (db.colaboradores || []).filter(c => c.status !== 'Inativo' && num(c.salario) > 0);
  if (todos.length === 0) return {
    pessoas: 0,
    media: 0,
    custoMinuto: 0,
    vazia: true
  };
  const j = jornadaDe(db);
  const encargos = num((db.parametrosMaoDeObra || {}).encargos) || ENCARGOS_PADRAO;
  const dias = num((db.parametrosMaoDeObra || {}).diasUteis) || DIAS_UTEIS_MES;
  const media = todos.reduce((a, c) => a + num(c.salario), 0) / todos.length;
  const comEncargos = media * (1 + encargos / 100);
  const vt = todos.reduce((a, c) => a + custoConducao(c, db).custoEmpresa, 0) / todos.length;
  const folha = comEncargos + vt;
  const minutosMes = j.produtivo * dias;
  return {
    pessoas: todos.length,
    media: Number(media.toFixed(2)),
    mediaComEncargos: Number(comEncargos.toFixed(2)),
    vtMedio: Number(vt.toFixed(2)),
    folhaPorPessoa: Number(folha.toFixed(2)),
    minutosMes,
    custoMinuto: minutosMes > 0 ? Number((folha / minutosMes).toFixed(5)) : 0,
    vazia: false
  };
}
function custoMinutoDepartamento(departamentoId, db) {
  const j = jornadaDe(db);
  const equipe = colaboradoresDoDepartamento(departamentoId, db);
  const comSalario = equipe.filter(c => num(c.salario) > 0);
  const encargos = num((db.parametrosMaoDeObra || {}).encargos) || ENCARGOS_PADRAO;
  const dias = num((db.parametrosMaoDeObra || {}).diasUteis) || DIAS_UTEIS_MES;
  if (comSalario.length === 0) return {
    pessoas: equipe.length,
    comSalario: 0,
    media: 0,
    custoMinuto: 0,
    semSalario: true,
    encargos,
    dias,
    minutosMes: j.produtivo * dias
  };
  const soma = comSalario.reduce((a, c) => a + num(c.salario), 0);
  const media = soma / comSalario.length;
  const mediaComEncargos = media * (1 + encargos / 100);
  const minutosMes = j.produtivo * dias;

  /* o transporte que a empresa banca também é custo da hora trabalhada */
  const vt = comSalario.reduce((a, c) => a + custoConducao(c, db).custoEmpresa, 0) / comSalario.length;
  const folhaPorPessoa = mediaComEncargos + vt;
  return {
    pessoas: equipe.length,
    comSalario: comSalario.length,
    media: Number(media.toFixed(2)),
    mediaComEncargos: Number(mediaComEncargos.toFixed(2)),
    vtMedio: Number(vt.toFixed(2)),
    folhaPorPessoa: Number(folhaPorPessoa.toFixed(2)),
    encargos,
    dias,
    minutosMes,
    custoMinuto: minutosMes > 0 ? Number((folhaPorPessoa / minutosMes).toFixed(5)) : 0,
    custoHora: minutosMes > 0 ? Number((folhaPorPessoa / (minutosMes / 60)).toFixed(2)) : 0,
    semSalario: false,
    /* quem tem salário em branco distorce a média — vale avisar */
    incompleto: comSalario.length < equipe.length
  };
}

/* O custo de mão de obra do produto, etapa a etapa */
/* Saber operar a máquina é pré-requisito para receber a tarefa. Uma
   costureira de reta não vira operadora de galoneira porque o
   planejamento precisou — e planejar como se virasse é o caminho mais
   curto para a data não bater. */
function equipamentosDoColaborador(colab){
  return (colab && Array.isArray(colab.equipamentos)) ? colab.equipamentos : null;
}

function podeOperar(db, colaboradorId, equipamentoId){
  if(!equipamentoId) return true;   /* operação sem máquina: qualquer um */
  const c = (db.colaboradores||[]).find(x=>x.id===colaboradorId);
  if(!c) return false;
  const lista = equipamentosDoColaborador(c);
  /* lista não preenchida significa "ainda não informado", não "não sabe
     nada" — senão o cadastro antigo travaria a fábrica inteira */
  if(lista === null || lista.length === 0) return true;
  return lista.includes(equipamentoId);
}

/* Quais das máquinas escolhidas esta pessoa consegue usar. */
function maquinasQueOpera(db, colaboradorId, maquinaIds){
  return (maquinaIds||[]).filter(id=>podeOperar(db, colaboradorId, id));
}

/* Quem sabe operar alguma das máquinas do dia. */
function habilitadosPara(db, colaboradorIds, maquinaIds){
  if(!(maquinaIds||[]).length) return {aptos: colaboradorIds||[], semHabilitacao: []};
  const aptos = [], sem = [];
  (colaboradorIds||[]).forEach(id=>{
    if(maquinasQueOpera(db, id, maquinaIds).length > 0) aptos.push(id);
    else sem.push(id);
  });
  return {aptos, semHabilitacao: sem};
}

function cargaDosEquipamentos(db, programado){
  const uso = {};
  (programado.agenda||[]).forEach(a=>{
    (a.tarefas||[]).forEach(tf=>{
      /* a tarefa não guarda a máquina; o que se sabe é que a pessoa
         ocupou uma das máquinas do dia enquanto trabalhava */
      const chave = a.maquinaId || '_equipe';
      if(!uso[chave]) uso[chave] = {minutos:0, pecas:0, pessoas:new Set()};
      uso[chave].minutos += num(tf.minutos);
      uso[chave].pecas += num(tf.pecas);
      uso[chave].pessoas.add(a.nome);
    });
  });

  /* sem vínculo pessoa-máquina, distribuir o uso entre as escolhidas */
  const maquinas = (programado.maquinaIds||[]);
  const totalMinutos = (programado.agenda||[])
    .reduce((s,a)=>s + num(a.usado) + num(a.extraUsado), 0);
  const totalPecas = (programado.agenda||[])
    .reduce((s,a)=>s + num(a.pecas), 0);
  const capacidade = num(programado.minutosDia);

  return maquinas.map((id,i)=>{
    const eq = (db.equipamentos||[]).find(e=>e.id===id);
    /* cada máquina recebe a fatia proporcional: com N máquinas e N
       pessoas produzindo em paralelo, o uso se divide entre elas */
    const fatia = maquinas.length > 0 ? 1/maquinas.length : 0;
    const minutos = totalMinutos * fatia;
    return {
      id, nome: eq ? eq.nome : 'máquina removida',
      codigo: eq ? (eq.codigo||'') : '',
      minutos: Number(minutos.toFixed(1)),
      pecas: Math.round(totalPecas * fatia),
      capacidade,
      ocupacao: capacidade > 0
        ? Number((minutos/capacidade*100).toFixed(0)) : 0,
      livre: Number(Math.max(0, capacidade - minutos).toFixed(1)),
    };
  });
}

/* Dá para cumprir as entregas com o ritmo deste dia? É a pergunta que
   o supervisor faz no fim da programação, e que ninguém respondia. */
function indicadorDoPeriodo(db, programado, opcoes){
  const op = opcoes || {};
  const demanda = programado.demanda || [];
  if(demanda.length === 0) return null;

  const minutosDia = num(programado.minutosDia) || 480;
  const pessoasDia = (programado.agenda||[]).length;
  const capacidadeDia = (programado.agenda||[])
    .reduce((s,a)=>s + num(a.capacidade), 0);
  if(capacidadeDia <= 0) return null;

  /* o trabalho total que a carteira exige, por data de entrega */
  const porEntrega = {};
  demanda.forEach(l=>{
    const dt = l.entrega || '';
    if(!porEntrega[dt]) porEntrega[dt] = {minutos:0, pecas:0, ordens:new Set()};
    porEntrega[dt].minutos += num(l.quantidade) * num(l.minutosPorPeca);
    porEntrega[dt].pecas += num(l.quantidade);
    porEntrega[dt].ordens.add(l.ordemCodigo);
  });

  const hoje = op.data || todayISO();
  const linhas = Object.entries(porEntrega)
    .filter(([dt])=>!!dt)
    .map(([dt, v])=>{
      /* dias úteis daqui até a entrega */
      const uteis = diasUteisEntre(hoje, dt);
      const capacidade = uteis * capacidadeDia;
      return {
        entrega: dt, uteis,
        minutos: Number(v.minutos.toFixed(1)),
        pecas: v.pecas,
        ordens: [...v.ordens],
        capacidade: Number(capacidade.toFixed(1)),
        cabe: capacidade >= v.minutos,
        folga: Number((capacidade - v.minutos).toFixed(1)),
        /* quantos dias faltam além dos que existem */
        diasFaltando: capacidade < v.minutos && capacidadeDia > 0
          ? Number(((v.minutos - capacidade)/capacidadeDia).toFixed(1)) : 0,
      };
    })
    .sort((a,b)=>a.entrega.localeCompare(b.entrega));

  const apertadas = linhas.filter(l=>!l.cabe);
  const semData = demanda.filter(l=>!l.entrega).length;

  return {
    linhas, apertadas, semData,
    pessoasDia, capacidadeDia: Number(capacidadeDia.toFixed(1)),
    /* o veredicto */
    situacao: apertadas.length === 0 ? 'dentro'
      : apertadas.length === linhas.length ? 'fora' : 'parcial',
  };
}

function diasUteisEntre(deISO, ateISO){
  const a = new Date(deISO), b = new Date(ateISO);
  if(isNaN(a) || isNaN(b) || b < a) return 0;
  let n = 0, g = 0;
  const dia = new Date(a);
  while(dia <= b && g < 800){
    g++;
    const s = dia.getDay();
    if(s !== 0 && s !== 6) n++;
    dia.setDate(dia.getDate() + 1);
  }
  return n;
}

/* A escala por operação vira agenda por pessoa. São duas vistas do
   mesmo plano: a carteira mostra por operação (o que precisa sair), o
   supervisor precisa por pessoa (o que cada um faz hoje). */
function agendaDaCarteira(db, balanceamento, escalas, opcoes){
  const op = opcoes || {};
  const minutosDia = num(op.minutosDia) || jornadaDe(db).produtivo;
  const extra = Math.min(num(op.permitirExtra) || 0, LIMITE_EXTRA_DIA);
  const porPessoa = {};

  (balanceamento.postos||[]).forEach(p=>{
    const esc = (escalas||{})[p.id];
    if(!esc || !(esc.pessoas||[]).length) return;
    const cap = capacidadeDaEquipe(db, esc.pessoas, p.id, p.unitario);
    if(cap.vazia || !(cap.porHora > 0)) return;

    const pecasTotal = p.unitario > 0 ? p.carga / p.unitario : 0;

    cap.membros.forEach(m=>{
      if(!porPessoa[m.id]) porPessoa[m.id] = {
        id: m.id, nome: m.nome,
        capacidade: minutosDia, extraPermitido: extra,
        usado: 0, extraUsado: 0, tarefas: [],
      };
      const a = porPessoa[m.id];
      /* a fatia desta pessoa: proporcional ao ritmo dela na equipe */
      const fatia = cap.porHora > 0 ? m.porHora / cap.porHora : 0;
      const pecas = pecasTotal * fatia;
      const minutos = m.porHora > 0 ? (pecas / m.porHora) * 60 : 0;

      /* o que cabe hoje; o resto fica para os próximos dias */
      const cabeNormal = Math.max(0, a.capacidade - a.usado);
      const cabeExtra = Math.max(0, a.extraPermitido - a.extraUsado);
      const usar = Math.min(minutos, cabeNormal + cabeExtra);
      const noNormal = Math.min(usar, cabeNormal);
      const noExtra = usar - noNormal;
      const pecasHoje = m.porHora > 0 ? (usar / 60) * m.porHora : 0;

      a.usado += noNormal;
      a.extraUsado += noExtra;
      a.tarefas.push({
        etapa: p.etapa, setor: p.setor,
        ordem: (p.produtos||[]).map(x=>x.ordem).join(', '),
        pecas: Math.round(pecasHoje),
        pecasTotal: Math.round(pecas),
        minutos: Number(usar.toFixed(1)),
        emExtra: noExtra > 0 ? Number(noExtra.toFixed(1)) : 0,
        porHora: m.porHora,
        base: m.ritmo.origem,
        sobrou: Math.round(pecas - pecasHoje),
      });
    });
  });

  const agenda = Object.values(porPessoa).map(a=>{
    a.usado = Number(a.usado.toFixed(1));
    a.extraUsado = Number(a.extraUsado.toFixed(1));
    a.livre = Number(Math.max(0, a.capacidade - a.usado).toFixed(1));
    a.ocupacao = a.capacidade > 0
      ? Number((a.usado / a.capacidade * 100).toFixed(0)) : 0;
    a.pecas = a.tarefas.reduce((s,t)=>s + t.pecas, 0);
    a.temFolga = a.livre >= 30;
    return a;
  }).sort((x,y)=>y.usado - x.usado);

  /* o que não coube no dia */
  const sobra = [];
  (balanceamento.postos||[]).forEach(p=>{
    const totalSobrou = agenda.reduce((s,a)=>
      s + a.tarefas.filter(t=>t.etapa===p.etapa)
        .reduce((n,t)=>n + t.sobrou, 0), 0);
    if(totalSobrou > 0)
      sobra.push({
        etapa: p.etapa, setor: p.setor,
        ordemCodigo: (p.produtos||[]).map(x=>x.ordem).join(', '),
        produtoNome: (p.produtos||[]).map(x=>x.produto).join(', '),
        quantidade: totalSobrou,
        minutos: Number((totalSobrou * p.unitario).toFixed(1)),
        entrega: (p.produtos||[])[0] ? (p.produtos[0].entrega||'') : '',
      });
  });

  const totalSobra = sobra.reduce((s,x)=>s + x.minutos, 0);
  const folgaTotal = agenda.reduce((s,a)=>s + a.livre, 0);

  return {
    vazio: agenda.length === 0,
    agenda, sobra, minutosDia,
    maquinaIds: op.maquinaIds || [],
    maquinas: (op.maquinaIds||[]).length,
    demanda: (balanceamento.postos||[]).map(p=>({
      etapa: p.etapa,
      quantidade: p.unitario > 0 ? Math.round(p.carga / p.unitario) : 0,
      minutosPorPeca: p.unitario,
      entrega: (p.produtos||[])[0] ? (p.produtos[0].entrega||'') : '',
      ordemCodigo: (p.produtos||[]).map(x=>x.ordem).join(', '),
    })),
    totalSobra: Number(totalSobra.toFixed(1)),
    totalExtra: Number(agenda.reduce((s,a)=>s + a.extraUsado, 0).toFixed(1)),
    folgaTotal: Number(folgaTotal.toFixed(1)),
    pecasProgramadas: agenda.reduce((s,a)=>s + a.pecas, 0),
    pecasSobrando: sobra.reduce((s,x)=>s + x.quantidade, 0),
    sugestoes: sugerirDestinoDaSobra(db, {agenda, sobra, minutosDia,
      totalSobra, folgaTotal, extra, semMaquina: [], semHabilitacao: []}),
  };
}

/* ==========================================================
   DISPONIBILIDADE
   O calendário não inventa horário: ele lê a jornada cadastrada
   em Engenharia → Jornada e mão de obra. Dia da semana, entrada,
   saída, intervalos e sábado saem de lá. O que o calendário
   acrescenta é o que já está comprometido — programação
   publicada, aferição marcada, máquina parada.
========================================================== */

/* O expediente de um dia, pela jornada da fábrica. */
function expedienteDoDia(db, dataISO){
  const j = jornadaDe(db);
  const d = new Date(dataISO + 'T12:00:00');
  if(isNaN(d)) return {trabalha:false, motivo:'data inválida', minutos:0};
  const semana = d.getDay();

  if(semana === 0)
    return {trabalha:false, motivo:'domingo', minutos:0, blocos:[]};

  if(semana === 6){
    if(!j.sabado || !j.minutosSabado)
      return {trabalha:false, motivo:'sábado sem expediente', minutos:0, blocos:[]};
    return {
      trabalha:true, sabado:true,
      inicio: j.sabado.inicio, fim: j.sabado.fim,
      minutos: j.minutosSabado,
      blocos: [{inicio: j.sabado.inicio, fim: j.sabado.fim,
        minutos: j.minutosSabado}],
      intervalos: [],
    };
  }

  if(!(j.diasSemana||[1,2,3,4,5]).includes(semana))
    return {trabalha:false, motivo:'fora dos dias de trabalho',
      minutos:0, blocos:[]};

  /* o dia partido pelos intervalos: cada bloco é tempo de produção */
  const blocos = [];
  let cursor = j.inicio;
  (j.intervalos||[]).slice()
    .sort((a,b)=>String(a.inicio).localeCompare(String(b.inicio)))
    .forEach(iv=>{
      const m = minutosEntre(cursor, iv.inicio);
      if(m > 0) blocos.push({inicio: cursor, fim: iv.inicio, minutos: m});
      cursor = iv.fim;
    });
  const ultimo = minutosEntre(cursor, j.fim);
  if(ultimo > 0) blocos.push({inicio: cursor, fim: j.fim, minutos: ultimo});

  return {
    trabalha:true, sabado:false,
    inicio: j.inicio, fim: j.fim,
    minutos: j.produtivo,
    blocos,
    intervalos: j.intervalos || [],
    extensao: j.extensao || null,
    minutosExtras: j.minutosExtras || 0,
  };
}

/* O que já está comprometido para esta pessoa ou máquina num dia. */
function compromissosDoRecurso(db, tipo, id, dataISO){
  const itens = [];

  (db.programacoes||[]).forEach(p=>{
    if(p.data !== dataISO || p.status === 'encerrada') return;
    (p.itens||[]).forEach(it=>{
      if(it.concluida) return;
      const meu = tipo === 'pessoa'
        ? (it.pessoas||[]).includes(id)
        : (it.maquinas||[]).includes(id);
      if(!meu) return;
      /* o tempo previsto desta operação para este recurso */
      const divisor = tipo === 'pessoa'
        ? Math.max(1, (it.pessoas||[]).length)
        : Math.max(1, (it.maquinas||[]).length);
      itens.push({
        tipo:'programacao',
        rotulo: it.etapa || 'operação',
        detalhe: `${it.ordemCodigo || ''} · ${p.codigo}`,
        situacao: p.status,
        pecas: Math.round(num(it.quantidadePrevista)/divisor),
      });
    });
  });

  /* aferição marcada ocupa a pessoa e a máquina */
  (db.afericoes||[]).forEach(af=>{
    if(af.fase === 'concluida' || af.fase === 'cancelada') return;
    const meu = tipo === 'pessoa'
      ? af.colaboradorId === id : af.equipamentoId === id;
    if(!meu) return;
    itens.push({tipo:'afericao', rotulo:'Aferição',
      detalhe: af.codigo || '', situacao: af.fase});
  });

  return itens;
}

/* A máquina está em condição de trabalhar? */
function situacaoDoEquipamento(db, id){
  const e = (db.equipamentos||[]).find(x=>x.id===id);
  if(!e) return {ok:false, motivo:'equipamento não encontrado'};
  if(e.ativo === false) return {ok:false, motivo:'inativo no cadastro'};
  if(e.situacao === 'parada') return {ok:false, motivo:'parada'};
  if(e.situacao === 'manutencao') return {ok:false, motivo:'em manutenção'};
  if(e.situacao === 'baixada') return {ok:false, motivo:'baixada'};
  return {ok:true, situacao: e.situacao || 'operando'};
}

/* A pessoa está em condição de trabalhar? */
function situacaoDoColaborador(db, id){
  const c = (db.colaboradores||[]).find(x=>x.id===id);
  if(!c) return {ok:false, motivo:'colaborador não encontrado'};
  if(c.status === 'Inativo') return {ok:false, motivo:'inativo no cadastro'};
  if(c.status && c.status !== 'Ativo')
    return {ok:false, motivo: String(c.status).toLowerCase()};
  if(c.produtivo === false) return {ok:false, motivo:'não é mão de obra direta'};
  return {ok:true, situacao:'ativo'};
}

/* O calendário: dia a dia, o que a jornada oferece e o que já está
   tomado. */
function disponibilidadeDe(db, tipo, id, opcoes){
  const op = opcoes || {};
  const inicio = op.de || todayISO();
  const dias = Math.max(1, Math.min(60, num(op.dias) || 14));

  const cond = tipo === 'pessoa'
    ? situacaoDoColaborador(db, id) : situacaoDoEquipamento(db, id);

  const lista = [];
  const cursor = new Date(inicio + 'T12:00:00');
  for(let i=0; i<dias; i++){
    const iso = cursor.toISOString().slice(0,10);
    const exp = expedienteDoDia(db, iso);
    const comp = compromissosDoRecurso(db, tipo, id, iso);
    const ocupado = comp.length > 0;

    lista.push({
      data: iso,
      semana: cursor.getDay(),
      ...exp,
      compromissos: comp,
      /* livre = a jornada trabalha, o recurso está em condição e
         ninguém reservou */
      livre: exp.trabalha && cond.ok && !ocupado,
      ocupado,
      impedido: !cond.ok,
    });
    cursor.setDate(cursor.getDate() + 1);
  }

  const uteis = lista.filter(d=>d.trabalha);
  const livres = lista.filter(d=>d.livre);

  return {
    tipo, id, condicao: cond,
    dias: lista,
    totalUteis: uteis.length,
    totalLivres: livres.length,
    minutosLivres: livres.reduce((s,d)=>s + d.minutos, 0),
    minutosUteis: uteis.reduce((s,d)=>s + d.minutos, 0),
  };
}

/* ==========================================================
   EXECUÇÃO DA ETAPA
   O plano diz quem faz o quê e a partir de que hora. A execução
   diz se começou, quando começou e o que saiu. A diferença
   entre os dois é o que faz o cronograma alertar antes do
   prazo estourar, e não depois.
========================================================== */
const SITUACAO_EXECUCAO = [
  {id:'prevista',   nome:'Prevista',    cor:'var(--line)'},
  {id:'iniciada',   nome:'Em produção', cor:'var(--thread)'},
  {id:'concluida',  nome:'Concluída',   cor:'var(--ok)'},
];

/* A hora prevista de uma etapa, dentro do expediente do dia. */
function horaPrevistaDoItem(db, item){
  if(item.horaInicio) return item.horaInicio;
  const j = jornadaDe(db);
  return j.inicio;
}

/* Quanto tempo esta etapa deve levar com a equipe escalada. */
function duracaoPrevistaDoItem(db, item){
  /* O tempo do roteiro é o piso do cálculo: sem ele, quem nunca fez a
     operação fica com ritmo zero e a duração não sai. */
  let minutosRoteiro = 0;
  const ordem = (db.ordens||[]).find(o=>o.id === item.ordemId);
  if(ordem){
    const produto = (db.produtos||[]).find(p=>p.id === ordem.produtoId);
    if(produto){
      const passo = (produto.processo||[]).find(x=>x.etapaId === item.etapaId);
      if(passo){
        const r = resumoProcesso(produto, db);
        const achado = (r.passos||[]).find(x=>x.passo.id === passo.id);
        if(achado) minutosRoteiro = num(achado.minutosPorPeca) ||
          num(achado.minutosHomem) / Math.max(1, num(passo.pessoas) || 1);
      }
    }
  }
  const cap = capacidadeDaEquipe(db, item.pessoas || [], item.etapaId,
    minutosRoteiro);
  if(cap.vazia || !(cap.porHora > 0)) return null;
  const pecas = num(item.quantidadePrevista);
  return Number(((pecas / cap.porHora) * 60).toFixed(1));
}

/* Iniciar: registra a hora real em que a etapa começou. */
function iniciarEtapa(d, programacaoId, itemId, dados, usuario){
  const p = (d.programacoes||[]).find(x=>x.id===programacaoId);
  if(!p) return {erro:'Programação não encontrada.'};
  const it = (p.itens||[]).find(x=>x.id===itemId);
  if(!it) return {erro:'Operação não encontrada na programação.'};
  if(it.iniciadoEm) return {erro:'Esta operação já foi iniciada.'};
  if(it.concluida) return {erro:'Esta operação já foi concluída.'};

  const op = dados || {};
  it.iniciadoEm = op.quando || agoraISO();
  it.iniciadoPor = usuario ? usuario.nome : '';
  /* a hora real com segundos: numa operação curta, o minuto redondo
     esconde a diferença entre começar às 8h00 e às 8h00m50s */
  it.horaReal = op.hora || String(new Date().toTimeString().slice(0,8));
  it.situacao = 'iniciada';

  registrarLog(d, usuario, 'producao.inicio',
    `${it.etapa} · ${it.ordemCodigo || ''} · ${it.horaReal}`);
  return {item: it};
}

/* Concluir: fecha a etapa com o que saiu, e gera o apontamento. */
function concluirEtapa(d, programacaoId, itemId, dados, usuario){
  const p = (d.programacoes||[]).find(x=>x.id===programacaoId);
  if(!p) return {erro:'Programação não encontrada.'};
  const it = (p.itens||[]).find(x=>x.id===itemId);
  if(!it) return {erro:'Operação não encontrada.'};
  if(it.concluida) return {erro:'Esta operação já foi concluída.'};
  if(!it.iniciadoEm)
    return {erro:'Esta operação ainda não foi iniciada. Inicie no cronograma.'};

  const boas = num(dados.pecasBoas);
  const defeito = num(dados.pecasDefeito);
  if(boas + defeito <= 0) return {erro:'Informe quantas peças saíram.'};

  const colaboradorId = dados.colaboradorId || (it.pessoas||[])[0] || '';
  if(!colaboradorId) return {erro:'Informe quem produziu.'};

  /* o tempo vem do relógio: início registrado até agora */
  const fim = dados.quando || agoraISO();
  const minutos = num(dados.minutosGastos) > 0
    ? num(dados.minutosGastos)
    : Math.max(1, Math.round(
        (new Date(fim) - new Date(it.iniciadoEm)) / 60000));

  const ordem = (d.ordens||[]).find(o=>o.id === it.ordemId);
  if(!ordem) return {erro:'Ordem não encontrada.'};
  const tarefa = (ordem.tarefas||[]).find(t=>t.etapaId === it.etapaId);
  if(!tarefa) return {erro:'Esta ordem não tem esta operação no roteiro.'};

  const r = apontarProducao(d, {
    ordemId: it.ordemId, tarefaId: tarefa.id, colaboradorId,
    pecasBoas: boas, pecasDefeito: defeito,
    minutosGastos: minutos,
    /* iniciar e concluir no cronograma já marcaram as duas pontas */
    inicioReal: it.iniciadoEm, fimReal: fim,
    paradas: dados.paradas || [],
    data: (it.iniciadoEm||'').slice(0,10) || p.data,
    observacao: dados.observacao || '',
  }, usuario);
  if(r.erro) return {erro: r.erro};

  it.concluida = true;
  it.concluidoEm = fim;
  it.concluidoPor = usuario ? usuario.nome : '';
  it.situacao = 'concluida';
  it.minutosReais = minutos;
  it.pecasFeitas = boas;

  registrarLog(d, usuario, 'producao.conclusao',
    `${it.etapa} · ${boas} peça(s) em ${duracao(minutos)}`);
  return {item: it, apontamento: r.apontamento};
}

/* O cronograma do dia: o que já era para ter começado, o que está
   em produção, o que atrasou. */
function cronogramaDoDia(db, data){
  const p = programacaoDoDia(db, data);
  if(!p) return {vazio:true, itens:[], data};

  const agora = new Date();
  const hoje = todayISO();
  const j = jornadaDe(db);

  const itens = (p.itens||[]).map(it=>{
    const horaPrev = horaPrevistaDoItem(db, it);
    const durPrev = duracaoPrevistaDoItem(db, it);
    const situacao = it.concluida ? 'concluida'
      : it.iniciadoEm ? 'iniciada' : 'prevista';

    /* já passou da hora de começar? */
    let atrasoInicio = 0;
    if(situacao === 'prevista' && p.data <= hoje){
      const alvo = new Date(`${p.data}T${horaPrev}:00`);
      if(p.data < hoje) atrasoInicio = 999;   /* dia passado, nunca começou */
      else if(agora > alvo)
        atrasoInicio = Math.round((agora - alvo) / 60000);
    }

    /* já começou: está demorando mais que o previsto? */
    let excesso = 0;
    if(situacao === 'iniciada' && durPrev){
      const decorrido = Math.round((agora - new Date(it.iniciadoEm)) / 60000);
      if(decorrido > durPrev) excesso = decorrido - durPrev;
    }

    /* A etapa anterior já entregou peça para esta trabalhar? Sem isso
       o supervisor inicia, produz e só descobre na hora de concluir
       que não podia — com a peça já costurada. */
    let bloqueio = null;
    const ordem = (db.ordens||[]).find(o=>o.id === it.ordemId);
    if(ordem && !it.concluida){
      const av = avancoDaOrdem(ordem, db);
      const linha = (av.etapas||[]).find(x=>x.tarefa &&
        x.tarefa.etapaId === it.etapaId);
      if(linha && linha.ordem > 1){
        const disponivel = num(linha.disponivel) - num(linha.produzidas);
        if(disponivel <= 0){
          const ant = (av.etapas||[])[linha.ordem - 2];
          bloqueio = ant
            ? `${ant.etapa} ainda não entregou peça para esta operação`
            : 'a etapa anterior ainda não entregou peça';
        }
      }
    }

    return {
      ...it, situacao,
      horaPrevista: horaPrev,
      duracaoPrevista: durPrev,
      atrasoInicio,
      excesso,
      bloqueio,
      /* a prioridade sobe com o atraso e com a proximidade da entrega */
      urgencia: situacao === 'concluida' ? 0
        : bloqueio ? 0
        : atrasoInicio > 0 ? 3 : excesso > 0 ? 2 : 1,
      alerta: bloqueio ? bloqueio
        : atrasoInicio >= 999 ? 'não foi iniciada no dia previsto'
        : atrasoInicio > 0 ? `${duracao(atrasoInicio)} sem iniciar`
        : excesso > 0 ? `${duracao(excesso)} além do previsto` : '',
    };
  }).sort((a,b)=>(b.urgencia - a.urgencia) ||
    String(a.horaPrevista).localeCompare(String(b.horaPrevista)));

  return {
    vazio:false, data, programacao: p, itens,
    previstas: itens.filter(x=>x.situacao==='prevista').length,
    iniciadas: itens.filter(x=>x.situacao==='iniciada').length,
    concluidas: itens.filter(x=>x.situacao==='concluida').length,
    atrasadas: itens.filter(x=>x.atrasoInicio > 0).length,
    excedidas: itens.filter(x=>x.excesso > 0).length,
    /* o veredicto do dia */
    situacao: itens.some(x=>x.atrasoInicio > 0) ? 'atrasado'
      : itens.some(x=>x.excesso > 0) ? 'apertado' : 'no prazo',
  };
}

/* ==========================================================
   PROGRAMAÇÃO AUTOMÁTICA
   A ordem entra e o sistema já monta o plano: tempo do roteiro,
   quem está livre e habilitado, e a sequência das etapas —
   costura não começa antes de o corte entregar.

   O que ele produz é ponto de partida, não decisão. O supervisor
   ajusta na carteira quando alguém falta, a máquina quebra ou o
   material atrasa; é por isso que cada item nasce marcado como
   automático, e deixa de ser assim quando alguém mexe.
========================================================== */

/* Quem pode assumir uma operação num dia: livre, habilitado e do setor. */
function candidatosLivres(db, etapaId, departamentoId, data, maquinaIds, opcoes){
  const op = opcoes || {};
  return (db.colaboradores||[])
    .filter(c=>c.departamentoId === departamentoId &&
      situacaoDoColaborador(db, c.id).ok)
    .filter(c=>!(maquinaIds||[]).length ||
      maquinasQueOpera(db, c.id, maquinaIds).length > 0)
    .map(c=>{
      const st = colaboradorLivre(db, c.id, data);
      const r = ritmoEsperado(db, c.id, etapaId, num(op.minutosRoteiro),
        {produtoId: op.produtoId, equipamentoId: (maquinaIds||[])[0]});
      const meu = desempenhoNaEtapa(db, c.id, etapaId);
      /* aferição concluída desta pessoa nesta operação */
      const aferido = (db.afericoes||[]).some(x=>
        x.colaboradorId === c.id && x.etapaId === etapaId &&
        x.fase === 'concluida');
      const aferindo = (db.afericoes||[]).some(x=>
        x.colaboradorId === c.id && x.etapaId === etapaId &&
        x.fase !== 'concluida' && x.fase !== 'cancelada');
      /* Quanto se sabe sobre esta pessoa nesta operação. É isso que
         ordena a escolha: medido vale mais que estimado, sempre. */
      const confianca = aferido ? 3
        : (!meu.semDados && meu.confiavel) ? 2
        : !meu.semDados ? 1 : 0;
      const afere = etapaExigeAfericao(db, etapaId);
      return {id: c.id, nome: c.nome, livre: st.livre,
        compromissos: st.compromissos.length, porHora: r.porHora,
        base: r.origem, aferido, aferindo,
        /* setor isento: a confiança vem do histórico, não da aferição */
        confianca: afere ? confianca : Math.min(confianca, 2),
        exigeAfericao: afere,
        apontamentos: meu.apontamentos || 0,
        precisaAferir: afere && confianca === 0 && !aferindo};
    })
    /* livre primeiro; depois quem se conhece melhor; depois quem rende
       mais. Sem isso o sistema escalaria um desconhecido rápido no
       papel à frente de alguém que já provou o ritmo. */
    .sort((a,b)=>(b.livre - a.livre) || (b.confianca - a.confianca) ||
      (b.porHora - a.porHora));
}

/* Máquinas do setor que podem receber a operação. */
function maquinasLivres(db, departamentoId, data){
  return (db.equipamentos||[])
    .filter(e=>e.departamentoId === departamentoId &&
      situacaoDoEquipamento(db, e.id).ok)
    .map(e=>({id: e.id, nome: e.nome,
      livre: compromissosDoRecurso(db,'maquina',e.id,data).length === 0}))
    .sort((a,b)=>b.livre - a.livre);
}

/* Monta o plano da ordem: cada etapa num dia, na sequência do roteiro. */
function programarOrdem(d, ordemId, opcoes, usuario){
  const op = opcoes || {};
  const ordem = (d.ordens||[]).find(o=>o.id===ordemId);
  if(!ordem) return {erro:'Ordem não encontrada.'};
  const produto = (d.produtos||[]).find(p=>p.id===ordem.produtoId);
  if(!produto) return {erro:'Produto da ordem não encontrado.'};

  if(!(produto.processo||[]).length)
    return {erro:'Este produto não tem processo definido. Monte o roteiro antes.'};
  const r = resumoProcesso(produto, d);
  if(!r || !r.passos || r.passos.length === 0)
    return {erro:'Este produto não tem processo definido. Monte o roteiro antes.'};

  const j = jornadaDe(d);
  const minutosDia = num(op.minutosDia) || j.produtivo;
  const quantidade = num(ordem.quantidade);

  /* a data em que cada etapa pode começar: a anterior tem que ter
     entregado. Sem isso o plano nasce impossível. */
  let dataCorrente = op.inicio || todayISO();
  if(!expedienteDoDia(d, dataCorrente).trabalha)
    dataCorrente = proximoDiaUtil(dataCorrente);

  const itens = [];
  const avisos = [];
  const aferidos = [];

  r.passos.forEach((p, ordemEtapa)=>{
    const etapaId = p.passo.etapaId;
    const setor = p.passo.departamentoId;
    const nomeEtapa = p.etapa ? p.etapa.nome : 'operação';

    /* O roteiro manda: se a etapa lista máquinas, só elas servem. Sem
       lista, qualquer uma do setor. */
    const doRoteiro = (p.equipamentoIds||[]).filter(Boolean);
    const disponiveis = maquinasLivres(d, setor, dataCorrente)
      .filter(m=>!doRoteiro.length || doRoteiro.includes(m.id));
    const maqEscolhidas = disponiveis.filter(m=>m.livre).slice(0, 1).map(m=>m.id);
    /* nenhuma livre? usa a que existe e avisa */
    const maquinas = maqEscolhidas.length ? maqEscolhidas
      : disponiveis.slice(0,1).map(m=>m.id);
    if(doRoteiro.length && disponiveis.length === 0)
      avisos.push(`${nomeEtapa}: as máquinas do roteiro estão indisponíveis ` +
        `em ${fmtDate(dataCorrente)}.`);
    else if(!doRoteiro.length && disponiveis.length === 0 && !p.semTempo)
      avisos.push(`${nomeEtapa}: nenhuma máquina no setor — tratada como manual.`);
    else if(maqEscolhidas.length === 0 && disponiveis.length > 0)
      avisos.push(`${nomeEtapa}: nenhuma máquina livre em ${fmtDate(dataCorrente)}.`);

    const porPecaRoteiro = num(p.minutosHomem) /
      Math.max(1, num(p.passo.pessoas) || 1);
    /* Quem este mesmo planejamento já colocou neste dia sai da lista.
       colaboradorLivre olha o que está salvo; as etapas que acabamos de
       montar ainda não foram — sem isso a mesma pessoa entrava duas
       vezes no mesmo dia e o plano nascia impossível. */
    const jaNesteDia = new Set(
      itens.filter(x=>x._data === dataCorrente)
        .flatMap(x=>x.pessoas || []));
    const cands = candidatosLivres(d, etapaId, setor, dataCorrente, maquinas,
      {produtoId: produto.id, minutosRoteiro: porPecaRoteiro})
      .filter(c=>!jaNesteDia.has(c.id));
    if(cands.length === 0){
      avisos.push(`${nomeEtapa}: ninguém disponível no setor — ` +
        `a operação entra sem equipe e precisa de ajuste manual.`);
    }

    /* quantas pessoas o roteiro pede, entre as disponíveis */
    const querem = Math.max(1, num(p.passo.pessoas) || 1);
    const equipe = cands.filter(c=>c.livre).slice(0, querem);
    const pessoas = (equipe.length ? equipe : cands.slice(0, querem))
      .map(c=>c.id);
    if(equipe.length < querem && cands.length > 0)
      avisos.push(`${nomeEtapa}: o roteiro pede ${querem} pessoa(s) e ` +
        `${equipe.length} está(ão) livre(s) em ${fmtDate(dataCorrente)}.`);

    /* quanto tempo leva com quem foi escolhido. O resumo do processo
       chama isso de minutosHomem — é o tempo de uma pessoa por peça. */
    const porPeca = num(p.minutosHomem) / Math.max(1, num(p.passo.pessoas) || 1);
    const cap = capacidadeDaEquipe(d, pessoas, etapaId, porPeca);
    const porHora = (cap.vazia || !cap.porHora)
      ? (porPeca > 0 ? 60/porPeca : 0) : cap.porHora;
    const minutos = porHora > 0 ? (quantidade / porHora) * 60 : 0;
    if(porPeca <= 0)
      avisos.push(`${nomeEtapa}: sem tempo definido no roteiro — ` +
        `a data desta operação é um chute.`);

    /* Quem entra sem medição nesta operação sai daqui com aferição
       aberta. É o que faz a fábrica ir conhecendo o próprio ritmo em
       vez de planejar para sempre no escuro. */
    pessoas.forEach(pid=>{
      const c = cands.find(x=>x.id === pid);
      if(!c || !c.precisaAferir) return;
      /* fora dos setores que aferem, a meta vem da jornada: não há
         cronômetro a marcar nem prova a fazer */
      if(!exigeAfericao(d, setor)) return;
      const ra = novaAfericao(d, {
        colaboradorId: pid, etapaId,
        departamentoId: setor,
        produtoId: produto.id,
        equipamentoId: maquinas[0] || '',
      }, usuario);
      if(ra.afericao){
        aferidos.push({nome: c.nome, etapa: nomeEtapa,
          codigo: ra.afericao.codigo});
      }
    });

    itens.push({
      ordemId: ordem.id, ordemCodigo: ordem.codigo,
      etapaId, etapa: nomeEtapa, departamentoId: setor,
      pessoas, maquinas,
      horaInicio: j.inicio,
      quantidadePrevista: quantidade,
      /* nasce automático: o supervisor vê que ninguém decidiu isso */
      automatico: true,
      _data: dataCorrente,
      _minutos: Number(minutos.toFixed(1)),
    });

    /* a próxima etapa começa quando esta termina */
    const dias = minutosDia > 0 ? Math.ceil(minutos / minutosDia) : 1;
    for(let k=0; k<Math.max(1, dias); k++)
      dataCorrente = proximoDiaUtil(dataCorrente);
  });

  /* agrupar por dia: uma programação por data */
  const porDia = {};
  itens.forEach(it=>{
    if(!porDia[it._data]) porDia[it._data] = [];
    const {_data, _minutos, ...limpo} = it;
    porDia[it._data].push(limpo);
  });

  const criadas = [];
  Object.entries(porDia).forEach(([data, lista])=>{
    const existente = programacaoDoDia(d, data);
    if(existente){
      /* já há programação nesse dia: acrescenta sem mexer no que existe */
      const novos = lista.filter(nl=>
        !(existente.itens||[]).some(x=>
          x.ordemId === nl.ordemId && x.etapaId === nl.etapaId));

      /* Quem já está escalado nesta programação não entra de novo. O
         item novo não pode roubar a pessoa: entra sem ela e o supervisor
         decide quem larga o quê. */
      const ocupados = new Set((existente.itens||[])
        .filter(x=>!x.concluida)
        .flatMap(x=>x.pessoas || []));
      novos.forEach(nl=>{
        const conflito = (nl.pessoas||[]).filter(x=>ocupados.has(x));
        if(conflito.length){
          const nomes = conflito.map(id=>{
            const c = (d.colaboradores||[]).find(y=>y.id===id);
            return c ? c.nome : 'colaborador';
          }).join(', ');
          nl.pessoas = (nl.pessoas||[]).filter(x=>!ocupados.has(x));
          avisos.push(`${nl.etapa} em ${fmtDate(data)}: ${nomes} já ` +
            `está(ão) em outra operação nesse dia. A operação entra sem ` +
            `equipe — escolha quem faz na Programação.`);
        }
        (nl.pessoas||[]).forEach(x=>ocupados.add(x));
      });

      if(novos.length){
        existente.itens = [...(existente.itens||[]), ...novos.map(x=>({
          ...x, id: uid(), concluida:false, situacao:'prevista',
        }))];
        existente.alteradaEm = agoraISO();
        criadas.push({programacao: existente, nova:false, itens: novos.length});
      }
    } else {
      const res = salvarProgramacao(d, {data, itens: lista, minutosDia}, usuario);
      if(res.erro) avisos.push(`${fmtDate(data)}: ${res.erro}`);
      else criadas.push({programacao: res.programacao, nova:true,
        itens: lista.length});
    }
  });

  registrarLog(d, usuario, 'pcp.automatico',
    `${ordem.codigo} · ${itens.length} operação(ões) em ${criadas.length} dia(s)`);

  if(aferidos.length)
    avisos.push(`${aferidos.length} aferição(ões) aberta(s) para quem ainda ` +
      `não tem medição: ` +
      aferidos.map(x=>`${x.nome} em ${x.etapa}`).join(', ') + '.');

  return {
    ordem, itens, avisos, criadas, aferidos,
    primeiroDia: itens.length ? itens[0]._data : null,
    ultimoDia: itens.length ? itens[itens.length-1]._data : null,
    /* comparação com a entrega prometida */
    cabeNaEntrega: ordem.entrega && itens.length
      ? itens[itens.length-1]._data <= ordem.entrega : null,
  };
}

/* ==========================================================
   OCORRÊNCIAS
   O plano é do começo do dia; a fábrica é do meio dele. Máquina
   quebra, gente falta, lote atrasa. Registrar a ocorrência não
   é burocracia: é o que permite realocar o que estava naquele
   recurso sem refazer a programação inteira à mão.
========================================================== */
const TIPOS_OCORRENCIA = [
  {id:'maquina_parada', nome:'Máquina parada', alvo:'maquina',
   ajuda:'quebra, manutenção ou falta de peça', realoca:true},
  {id:'falta', nome:'Falta de colaborador', alvo:'pessoa',
   ajuda:'falta, atestado ou saída antecipada', realoca:true},
  {id:'falta_material', nome:'Falta de material', alvo:'nenhum',
   ajuda:'o lote não chegou ou não passou na conferência', realoca:false},
  {id:'qualidade', nome:'Problema de qualidade', alvo:'nenhum',
   ajuda:'a peça voltou e a operação parou', realoca:false},
  {id:'outro', nome:'Outro motivo', alvo:'nenhum', ajuda:'', realoca:false},
];

/* Registrar o que aconteceu e, quando for o caso, tirar o recurso da
   programação do dia. */
const MOTIVOS_ATRASO = ['Máquina parada', 'Falta de material', 'Retrabalho', 'Falta de pessoal', 'Troca de ordem', 'Manutenção', 'Treinamento', 'Outro'];

const TIPOS_DEFEITO = ['Costura torta', 'Ponto falhado', 'Mancha', 'Furo no tecido', 'Medida fora', 'Estampa deslocada', 'Peça trocada', 'Acabamento', 'Outro'];

function registrarOcorrencia(d, dados, usuario){
  const tipo = TIPOS_OCORRENCIA.find(t=>t.id === dados.tipo);
  if(!tipo) return {erro:'Escolha o que aconteceu.'};
  const data = dados.data || todayISO();
  const descricao = String(dados.descricao||'').trim();
  if(descricao.length < 5)
    return {erro:'Descreva em uma frase o que aconteceu.'};

  if(tipo.alvo === 'maquina' && !dados.equipamentoId)
    return {erro:'Escolha qual máquina parou.'};
  if(tipo.alvo === 'pessoa' && !dados.colaboradorId)
    return {erro:'Escolha quem faltou.'};

  const reg = {
    id: uid(),
    codigo: proximoCodigo(d.ocorrencias||[], 'OCO'),
    data, tipo: tipo.id, tipoNome: tipo.nome,
    equipamentoId: dados.equipamentoId || '',
    colaboradorId: dados.colaboradorId || '',
    ordemId: dados.ordemId || '',
    departamentoId: dados.departamentoId || '',
    descricao,
    minutosParado: num(dados.minutosParado),
    previsaoRetorno: dados.previsaoRetorno || '',
    situacao: 'aberta',
    registradoEm: agoraISO(),
    registradoPor: usuario ? usuario.nome : '',
  };
  d.ocorrencias = [...(d.ocorrencias||[]), reg];

  /* a máquina parada some do planejamento até voltar */
  if(tipo.id === 'maquina_parada' && dados.equipamentoId){
    const eq = (d.equipamentos||[]).find(e=>e.id===dados.equipamentoId);
    if(eq){ eq.situacao = 'manutencao'; eq.ocorrenciaId = reg.id; }
  }

  registrarLog(d, usuario, 'producao.ocorrencia',
    `${reg.codigo} · ${tipo.nome} · ${fmtDate(data)}`);
  return {ocorrencia: reg, tipo};
}

/* O que estava programado no recurso afetado, e portanto precisa de
   outro lugar. */
function itensAfetados(db, ocorrencia){
  const prog = programacaoDoDia(db, ocorrencia.data);
  if(!prog) return {programacao:null, itens:[]};

  const itens = (prog.itens||[]).filter(it=>{
    if(it.concluida) return false;
    if(ocorrencia.equipamentoId)
      return (it.maquinas||[]).includes(ocorrencia.equipamentoId);
    if(ocorrencia.colaboradorId)
      return (it.pessoas||[]).includes(ocorrencia.colaboradorId);
    return false;
  });

  const real = realizadoDaProgramacao(db, prog);
  return {
    programacao: prog,
    itens: itens.map(it=>{
      const r = (real.itens||[]).find(x=>x.id===it.id) || {};
      return {...it, feito: num(r.feito), falta: num(r.falta) || num(it.quantidadePrevista)};
    }),
  };
}

/* Para onde o trabalho pode ir: quem está livre e sabe fazer. */
function alternativasPara(db, ocorrencia, item){
  const data = ocorrencia.data;

  if(ocorrencia.equipamentoId){
    const eq = (db.equipamentos||[]).find(e=>e.id===ocorrencia.equipamentoId);
    const setor = eq ? eq.departamentoId : item.departamentoId;
    return (db.equipamentos||[])
      .filter(e=>e.id !== ocorrencia.equipamentoId &&
        e.departamentoId === setor && situacaoDoEquipamento(db, e.id).ok)
      .map(e=>{
        const ocupada = compromissosDoRecurso(db,'maquina',e.id,data);
        /* quem está escalado no item consegue operar esta máquina? */
        const operam = (item.pessoas||[]).filter(p=>podeOperar(db, p, e.id));
        return {
          id: e.id, nome: e.nome, tipo:'maquina',
          livre: ocupada.length === 0,
          ocupada: ocupada.length,
          operam: operam.length,
          totalPessoas: (item.pessoas||[]).length,
          alerta: operam.length === 0
            ? 'ninguém da equipe opera esta máquina'
            : operam.length < (item.pessoas||[]).length
              ? `${operam.length} de ${(item.pessoas||[]).length} operam` : '',
        };
      })
      .sort((a,b)=>(b.operam - a.operam) || (b.livre - a.livre));
  }

  /* falta de pessoa: quem mais pode assumir */
  const c = (db.colaboradores||[]).find(x=>x.id===ocorrencia.colaboradorId);
  const setor = c ? c.departamentoId : item.departamentoId;
  return (db.colaboradores||[])
    .filter(x=>x.id !== ocorrencia.colaboradorId &&
      x.departamentoId === setor && situacaoDoColaborador(db, x.id).ok)
    .map(x=>{
      const ocupado = compromissosDoRecurso(db,'pessoa',x.id,data);
      const r = ritmoEsperado(db, x.id, item.etapaId, 0);
      const opera = (item.maquinas||[]).length === 0 ||
        (item.maquinas||[]).some(m=>podeOperar(db, x.id, m));
      return {
        id: x.id, nome: x.nome, tipo:'pessoa',
        livre: ocupado.length === 0,
        ocupada: ocupado.length,
        porHora: r.porHora, base: r.origem,
        opera,
        alerta: !opera ? 'não opera a máquina desta operação'
          : ocupado.length > 0 ? `já tem ${ocupado.length} compromisso(s)` : '',
      };
    })
    .sort((a,b)=>(b.opera - a.opera) || (b.livre - a.livre) ||
      (b.porHora - a.porHora));
}

/* Trocar o recurso nos itens escolhidos. */
function realocar(d, ocorrenciaId, trocas, usuario){
  const oc = (d.ocorrencias||[]).find(x=>x.id===ocorrenciaId);
  if(!oc) return {erro:'Ocorrência não encontrada.'};
  const prog = programacaoDoDia(d, oc.data);
  if(!prog) return {erro:'Não há programação aberta para este dia.'};

  const feitas = [];
  (trocas||[]).forEach(tr=>{
    const it = (prog.itens||[]).find(x=>x.id===tr.itemId);
    if(!it || !tr.novoId) return;

    if(oc.equipamentoId){
      it.maquinas = (it.maquinas||[])
        .map(m=>m === oc.equipamentoId ? tr.novoId : m);
      const de = (d.equipamentos||[]).find(e=>e.id===oc.equipamentoId);
      const para = (d.equipamentos||[]).find(e=>e.id===tr.novoId);
      feitas.push(`${it.etapa}: ${de?de.nome:'?'} → ${para?para.nome:'?'}`);
    } else {
      it.pessoas = (it.pessoas||[])
        .map(p=>p === oc.colaboradorId ? tr.novoId : p);
      const de = (d.colaboradores||[]).find(c=>c.id===oc.colaboradorId);
      const para = (d.colaboradores||[]).find(c=>c.id===tr.novoId);
      feitas.push(`${it.etapa}: ${de?de.nome:'?'} → ${para?para.nome:'?'}`);
    }
    it.realocadoPor = oc.codigo;
  });

  if(feitas.length === 0)
    return {erro:'Nenhuma troca foi escolhida.'};

  oc.situacao = 'realocada';
  oc.realocacoes = feitas;
  oc.realocadoEm = agoraISO();
  prog.alteradaEm = agoraISO();

  registrarLog(d, usuario, 'producao.realocacao',
    `${oc.codigo} · ${feitas.join(' · ')}`);
  return {ocorrencia: oc, feitas};
}

/* A máquina voltou, a pessoa retornou. */
function encerrarOcorrencia(d, id, usuario){
  const oc = (d.ocorrencias||[]).find(x=>x.id===id);
  if(!oc) return {erro:'Ocorrência não encontrada.'};
  if(oc.situacao === 'encerrada') return {erro:'Já está encerrada.'};

  oc.situacao = 'encerrada';
  oc.encerradaEm = agoraISO();

  if(oc.equipamentoId){
    const eq = (d.equipamentos||[]).find(e=>e.id===oc.equipamentoId);
    if(eq && eq.ocorrenciaId === oc.id){
      eq.situacao = 'operando';
      delete eq.ocorrenciaId;
    }
  }
  registrarLog(d, usuario, 'producao.ocorrencia.fim', oc.codigo);
  return {ocorrencia: oc};
}

function ocorrenciasAbertas(db, data){
  return (db.ocorrencias||[]).filter(o=>
    o.situacao !== 'encerrada' && (!data || o.data === data));
}

/* ==========================================================
   PROGRAMAÇÃO OFICIAL
   Simular ajuda a decidir; programar compromete. A partir do
   momento em que a programação é publicada, cada pessoa tem
   compromisso naquele dia — e não pode ser escalada em dois
   lugares ao mesmo tempo.

   A realidade não colabora: gente falta, máquina quebra, o
   lote atrasa. Por isso a reprogramação é parte do desenho,
   não conserto: no fim do dia o supervisor abre o que não
   foi concluído e redistribui.
========================================================== */
const STATUS_PROGRAMACAO = [
  {id:'rascunho',  nome:'Rascunho',  ajuda:'ainda pode mudar sem afetar ninguém'},
  {id:'publicada', nome:'Publicada', ajuda:'a fábrica trabalha por ela'},
  {id:'encerrada', nome:'Encerrada', ajuda:'o dia acabou; o que sobrou foi reprogramado'},
];

function programacaoDoDia(db, data){
  return (db.programacoes||[]).find(p=>p.data === data &&
    p.status !== 'encerrada') || null;
}

/* Quem já tem compromisso num dia, e em quê. É isso que impede
   escalar a mesma pessoa em duas operações no mesmo horário. */
function compromissosDoDia(db, data, exceto){
  const mapa = {};
  (db.programacoes||[]).forEach(p=>{
    if(p.data !== data) return;
    if(p.status === 'encerrada') return;
    if(exceto && p.id === exceto) return;
    (p.itens||[]).forEach(it=>{
      if(it.concluida) return;
      (it.pessoas||[]).forEach(id=>{
        if(!mapa[id]) mapa[id] = [];
        mapa[id].push({programacaoId: p.id, item: it});
      });
    });
  });
  return mapa;
}

/* Esta pessoa está livre neste dia? */
function colaboradorLivre(db, colaboradorId, data, opcoes){
  const op = opcoes || {};
  const mapa = compromissosDoDia(db, data, op.exceto);
  const meus = (mapa[colaboradorId] || []).filter(x=>
    !op.exceroItem || x.item.id !== op.exceroItem);
  if(meus.length === 0) return {livre:true, compromissos:[]};

  return {
    livre:false,
    compromissos: meus.map(x=>({
      etapa: x.item.etapa || 'operação',
      ordem: x.item.ordemCodigo || '',
      programacaoId: x.programacaoId,
    })),
  };
}

/* Guardar a programação. Rascunho não compromete ninguém; publicar
   sim — e é aí que a checagem de conflito importa. */
function salvarProgramacao(d, dados, usuario){
  const data = dados.data || todayISO();
  const itens = (dados.itens||[]).filter(x=>x && (x.pessoas||[]).length > 0);
  if(itens.length === 0)
    return {erro:'Nenhuma operação com equipe definida.'};

  const existente = dados.id
    ? (d.programacoes||[]).find(p=>p.id === dados.id) : null;
  if(dados.id && !existente) return {erro:'Programação não encontrada.'};
  if(existente && existente.status === 'encerrada')
    return {erro:'Programação encerrada não pode ser alterada. Crie uma nova.'};

  /* dupla alocação: a mesma pessoa em duas operações do mesmo dia */
  const vistos = {};
  const conflitos = [];
  itens.forEach(it=>{
    (it.pessoas||[]).forEach(id=>{
      if(vistos[id]){
        const c = (d.colaboradores||[]).find(x=>x.id===id);
        conflitos.push({nome: c ? c.nome : 'colaborador',
          onde:[vistos[id], it.etapa || 'operação']});
      } else vistos[id] = it.etapa || 'operação';
    });
  });
  if(conflitos.length)
    return {erro: conflitos.map(c=>
      `${c.nome} está em duas operações ao mesmo tempo: ${c.onde.join(' e ')}.`
    ).join(' ')};

  /* conflito com outra programação já publicada do mesmo dia */
  const externos = [];
  itens.forEach(it=>{
    (it.pessoas||[]).forEach(id=>{
      const st = colaboradorLivre(d, id, data, {exceto: existente ? existente.id : null});
      if(!st.livre){
        const c = (d.colaboradores||[]).find(x=>x.id===id);
        externos.push(`${c ? c.nome : 'colaborador'} já está em ` +
          st.compromissos.map(x=>x.etapa).join(', '));
      }
    });
  });
  if(externos.length && !dados.ignorarConflito)
    return {erro:'Já há programação para este dia com estas pessoas. ' +
      externos.join('; ') + '.', conflito:true};

  const reg = {
    id: existente ? existente.id : uid(),
    codigo: existente ? existente.codigo
      : proximoCodigo(d.programacoes||[], 'PRG'),
    data,
    status: dados.status || (existente ? existente.status : 'rascunho'),
    minutosDia: num(dados.minutosDia) || jornadaDe(d).produtivo,
    itens: itens.map(it=>({
      id: it.id || uid(),
      ordemId: it.ordemId || '',
      ordemCodigo: it.ordemCodigo || '',
      etapaId: it.etapaId || '',
      etapa: it.etapa || '',
      departamentoId: it.departamentoId || '',
      pessoas: it.pessoas || [],
      maquinas: it.maquinas || [],
      quantidadePrevista: num(it.quantidadePrevista),
      /* a hora em que a operação entra: o cronograma compara com o
         relógio para saber se já era para ter começado */
      horaInicio: it.horaInicio || '',
      /* nasce automático quando o sistema montou; deixa de ser quando
         alguém ajusta. É como o supervisor distingue o que ele decidiu
         do que apenas aceitou. */
      automatico: it.automatico === true,
      concluida: !!it.concluida,
      /* a execução, quando houver */
      iniciadoEm: it.iniciadoEm || '',
      concluidoEm: it.concluidoEm || '',
      situacao: it.situacao || 'prevista',
    })),
    origemId: existente ? existente.origemId : (dados.origemId || ''),
    criadaEm: existente ? existente.criadaEm : agoraISO(),
    criadaPor: existente ? existente.criadaPor : (usuario ? usuario.nome : ''),
    alteradaEm: agoraISO(),
  };
  d.programacoes = existente
    ? (d.programacoes||[]).map(p=>p.id===reg.id ? reg : p)
    : [...(d.programacoes||[]), reg];
  registrarLog(d, usuario, 'pcp.programacao',
    `${reg.codigo} · ${fmtDate(data)} · ${reg.itens.length} operação(ões)`);
  return {programacao: reg};
}

function publicarProgramacao(d, id, usuario){
  const p = (d.programacoes||[]).find(x=>x.id===id);
  if(!p) return {erro:'Programação não encontrada.'};
  if(p.status === 'publicada') return {erro:'Já está publicada.'};
  if(p.status === 'encerrada') return {erro:'Programação encerrada.'};
  p.status = 'publicada';
  p.publicadaEm = agoraISO();
  registrarLog(d, usuario, 'pcp.publicada', `${p.codigo} · ${fmtDate(p.data)}`);
  return {programacao: p};
}

/* O que de fato aconteceu em cada item, pelos apontamentos do dia. */
function realizadoDaProgramacao(db, programacao){
  if(!programacao) return {itens:[], concluidas:0, pendentes:0};
  const itens = (programacao.itens||[]).map(it=>{
    const aps = (db.apontamentos||[]).filter(a=>
      a.data === programacao.data &&
      a.ordemId === it.ordemId && a.etapaId === it.etapaId);
    const feito = aps.reduce((s,a)=>s + num(a.pecasBoas), 0);
    const previsto = num(it.quantidadePrevista);
    const falta = Math.max(0, previsto - feito);
    return {
      ...it, feito, previsto, falta,
      apontamentos: aps.length,
      cumprida: previsto > 0 && feito >= previsto,
      percentual: previsto > 0
        ? Number((feito/previsto*100).toFixed(0)) : null,
      /* quem realmente apontou pode ser outro: alguém faltou e
         outra pessoa cobriu */
      quemApontou: [...new Set(aps.map(a=>a.colaboradorId).filter(Boolean))],
    };
  });
  return {
    itens,
    concluidas: itens.filter(x=>x.cumprida).length,
    pendentes: itens.filter(x=>!x.cumprida).length,
    totalPrevisto: itens.reduce((s,x)=>s+x.previsto, 0),
    totalFeito: itens.reduce((s,x)=>s+x.feito, 0),
  };
}

/* Reprogramar: fecha o dia e leva o que faltou para o próximo. A
   equipe vem sugerida, mas tudo pode ser trocado — é justamente
   quando se descobre quem faltou. */
function reprogramar(d, programacaoId, opcoes, usuario){
  const op = opcoes || {};
  const p = (d.programacoes||[]).find(x=>x.id===programacaoId);
  if(!p) return {erro:'Programação não encontrada.'};
  if(p.status === 'encerrada') return {erro:'Esta programação já foi encerrada.'};

  const real = realizadoDaProgramacao(d, p);
  const pendentes = real.itens.filter(x=>!x.cumprida && x.falta > 0);

  /* encerra o dia com o registro do que foi feito */
  p.status = 'encerrada';
  p.encerradaEm = agoraISO();
  p.resultado = {
    previsto: real.totalPrevisto, feito: real.totalFeito,
    cumpridas: real.concluidas, pendentes: real.pendentes,
  };
  (p.itens||[]).forEach(it=>{
    const r = real.itens.find(x=>x.id===it.id);
    if(r){ it.feito = r.feito; it.concluida = r.cumprida; }
  });

  if(pendentes.length === 0){
    registrarLog(d, usuario, 'pcp.encerrada',
      `${p.codigo} · tudo concluído`);
    return {encerrada: p, nova: null,
      aviso:'Tudo que estava programado foi concluído. Nada a reprogramar.'};
  }

  const novaData = op.data || proximoDiaUtil(p.data);
  const nova = {
    id: uid(),
    codigo: proximoCodigo(d.programacoes||[], 'PRG'),
    data: novaData,
    status: 'rascunho',
    minutosDia: p.minutosDia,
    itens: pendentes.map(x=>({
      id: uid(),
      ordemId: x.ordemId, ordemCodigo: x.ordemCodigo,
      etapaId: x.etapaId, etapa: x.etapa,
      departamentoId: x.departamentoId,
      /* a equipe anterior vem sugerida; o supervisor confirma ou troca */
      pessoas: x.pessoas || [], maquinas: x.maquinas || [],
      horaInicio: x.horaInicio || '',
      quantidadePrevista: x.falta,
      concluida: false, situacao: 'prevista',
      veioDe: p.codigo,
      faltouOntem: x.previsto - x.feito,
    })),
    origemId: p.id,
    criadaEm: agoraISO(),
    criadaPor: usuario ? usuario.nome : '',
    alteradaEm: agoraISO(),
  };
  d.programacoes = [...(d.programacoes||[]), nova];
  registrarLog(d, usuario, 'pcp.reprogramada',
    `${p.codigo} → ${nova.codigo} · ${pendentes.length} operação(ões) pendente(s)`);

  return {encerrada: p, nova, pendentes: pendentes.length,
    realizado: real};
}

function proximoDiaUtil(dataISO){
  const dia = new Date(dataISO || todayISO());
  let g = 0;
  do {
    dia.setDate(dia.getDate() + 1);
    g++;
  } while((dia.getDay() === 0 || dia.getDay() === 6) && g < 10);
  return dia.toISOString().slice(0,10);
}

/* ==========================================================
   PROGRAMAÇÃO DO DIA
   Balancear diz quantas pessoas a linha precisa. Programar diz
   o que cada uma faz hoje, até a jornada encher. Numa confecção
   pequena a mesma pessoa passa por várias operações no mesmo
   dia — e é aí que a capacidade individual encontra a
   necessidade da carteira.
========================================================== */

const LIMITE_EXTRA_DIA = 120;   /* 2h é o limite legal diário no Brasil */

/* Quanto tempo cada pessoa tem, considerando a jornada e o que já
   estiver reservado. */
function jornadaDisponivel(db, colaboradorId, opcoes){
  const op = opcoes || {};
  const j = jornadaDe(db);
  const base = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const jaOcupado = num((op.ocupados||{})[colaboradorId]);
  return {
    total: base,
    ocupado: jaOcupado,
    livre: Math.max(0, base - jaOcupado),
    extraPermitido: num(op.permitirExtra) > 0
      ? Math.min(num(op.permitirExtra), LIMITE_EXTRA_DIA) : 0,
  };
}

/* O trabalho que precisa ser feito, vindo das ordens escolhidas.
   Cada linha é uma operação com quantidade pendente. */
function demandaDasOrdens(db, ordemIds, departamentoId){
  const ordens = (db.ordens||[]).filter(o=>
    (ordemIds||[]).includes(o.id) && o.situacao !== 'cancelada');
  const linhas = [];

  ordens.forEach(o=>{
    const produto = (db.produtos||[]).find(p=>p.id===o.produtoId);
    if(!produto) return;
    const av = avancoDaOrdem(o, db);
    const r = resumoProcesso(produto, db);

    r.passos.forEach(p=>{
      if(departamentoId && p.passo.departamentoId !== departamentoId) return;
      const linha = av.etapas.find(x=>x.tarefa &&
        x.tarefa.etapaId === p.passo.etapaId);
      const falta = linha ? Math.max(0, num(linha.saldo)) : num(o.quantidade);
      if(falta <= 0) return;
      const pessoasRoteiro = Math.max(1, num(p.passo.pessoas) || 1);
      linhas.push({
        id: `${o.id}:${p.passo.id}`,
        ordem: o, ordemCodigo: o.codigo,
        produto, produtoNome: montarNomeProduto(produto, db) || produto.codigo,
        etapaId: p.passo.etapaId,
        etapa: p.etapa ? p.etapa.nome : 'etapa removida',
        departamentoId: p.passo.departamentoId,
        setor: p.departamento ? p.departamento.nome : '—',
        quantidade: falta,
        minutosPorPeca: num(p.minutosHomem) / pessoasRoteiro,
        entrega: o.entrega || '',
        prioridade: num(o.prioridade) || 5,
      });
    });
  });

  /* quem entrega antes vem primeiro; sem data, pela prioridade */
  return linhas.sort((a,b)=>{
    if(a.entrega && b.entrega) return a.entrega.localeCompare(b.entrega);
    if(a.entrega) return -1;
    if(b.entrega) return 1;
    return a.prioridade - b.prioridade;
  });
}

/* Distribui o trabalho entre as pessoas escolhidas, respeitando o
   ritmo de cada uma e o tempo que ela tem. */
function programarDia(db, opcoes){
  const op = opcoes || {};
  const j = jornadaDe(db);
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const pessoas = (op.colaboradorIds||[]).filter(Boolean);
  const maquinas = (op.maquinaIds||[]).filter(Boolean);
  const extra = num(op.permitirExtra) || 0;

  const demanda = op.demanda || demandaDasOrdens(db, op.ordemIds, op.departamentoId);

  if(pessoas.length === 0)
    return {vazio:true, motivo:'Escolha quem vai trabalhar.', agenda:[], sobra:[]};
  if(demanda.length === 0)
    return {vazio:true, motivo:'Nenhuma operação pendente nas ordens escolhidas.',
      agenda:[], sobra:[]};

  /* a agenda de cada pessoa começa vazia */
  const agenda = pessoas.map(id=>{
    const c = (db.colaboradores||[]).find(x=>x.id===id);
    const jd = jornadaDisponivel(db, id, {minutosDia, permitirExtra: extra});
    return {
      id, nome: c ? c.nome : 'removido', colaborador: c,
      capacidade: jd.total, extraPermitido: jd.extraPermitido,
      usado: 0, extraUsado: 0, tarefas: [],
    };
  });

  /* Máquina é recurso do turno inteiro, não da tarefa. Com uma reta e
     três costureiras, uma produz o dia todo e duas ficam sem posto —
     não é que a segunda "herda" a máquina quando a primeira enche a
     jornada, porque as três trabalham no mesmo horário. */
  const limiteSimultaneo = maquinas.length > 0
    ? Math.min(pessoas.length, maquinas.length) : pessoas.length;

  const semMaquina = [];
  if(maquinas.length > 0 && agenda.length > limiteSimultaneo){
    /* quem fica com a máquina: os habilitados vêm primeiro */
    const ordenada = agenda.slice().sort((a,b)=>{
      const ha = maquinasQueOpera(db, a.id, maquinas).length > 0 ? 1 : 0;
      const hb = maquinasQueOpera(db, b.id, maquinas).length > 0 ? 1 : 0;
      return hb - ha;
    });
    ordenada.slice(limiteSimultaneo).forEach(a=>{
      a.semMaquina = true;
      semMaquina.push({id:a.id, nome:a.nome});
    });
  }

  const sobra = [];

  demanda.forEach(linha=>{
    let restam = linha.quantidade;
    /* guarda contra laço infinito: se algo der errado no cálculo, é
       melhor parar com o que foi programado que travar a tela */
    let voltas = 0;

    while(restam > 0 && voltas < 2000){
      voltas++;
      /* quem tem mais tempo livre pega o próximo pedaço; empate vai
         para quem é mais rápido nesta operação */
      const livres = agenda
        .filter(a=>!a.cheio && !a.semMaquina)
        /* só entra quem sabe operar alguma das máquinas do dia */
        .filter(a=>maquinas.length === 0 ||
          maquinasQueOpera(db, a.id, maquinas).length > 0)
        .map(a=>{
          const ritmo = ritmoEsperado(db, a.id, linha.etapaId,
            linha.minutosPorPeca, {produtoId: linha.produto.id});
          const disponivel = (a.capacidade - a.usado) +
            (a.extraPermitido - a.extraUsado);
          return {a, ritmo, disponivel};
        })
        .filter(x=>x.disponivel > 0 && x.ritmo.porHora > 0)
        .sort((x,y)=>{
          const d = y.disponivel - x.disponivel;
          return d !== 0 ? d : y.ritmo.porHora - x.ritmo.porHora;
        })
        .slice(0, limiteSimultaneo);

      if(livres.length === 0){
        sobra.push({...linha, quantidade: restam,
          minutos: Number((restam * linha.minutosPorPeca).toFixed(1))});
        break;
      }

      const alvo = livres[0];
      const minutosPorPeca = 60 / alvo.ritmo.porHora;
      const cabeNormal = Math.max(0, alvo.a.capacidade - alvo.a.usado);
      const cabeExtra = Math.max(0, alvo.a.extraPermitido - alvo.a.extraUsado);

      /* quantas peças cabem no tempo que resta */
      const pecasNormais = Math.floor(cabeNormal / minutosPorPeca);
      const pecasExtra = Math.floor(cabeExtra / minutosPorPeca);
      const cabeTudo = pecasNormais + pecasExtra;

      /* Numa linha as pessoas trabalham ao mesmo tempo, não em fila.
         Dar a operação inteira para a primeira encheria uma e deixaria
         as outras paradas — então cada uma leva uma fatia, e a volta
         seguinte reparte o que ficou. */
      const fatia = Math.max(1, Math.ceil(restam / livres.length));
      const pecas = Math.min(restam, cabeTudo, fatia);

      if(pecas <= 0){
        /* não cabe nem uma peça: essa pessoa está cheia */
        alvo.a.cheio = true;
        const outros = agenda.filter(a=>!a.cheio &&
          (a.capacidade - a.usado + a.extraPermitido - a.extraUsado) > 0);
        if(outros.length === 0){
          sobra.push({...linha, quantidade: restam,
            minutos: Number((restam * linha.minutosPorPeca).toFixed(1))});
          break;
        }
        continue;
      }

      const minutos = pecas * minutosPorPeca;
      const noNormal = Math.min(minutos, cabeNormal);
      const noExtra = minutos - noNormal;

      alvo.a.usado += noNormal;
      alvo.a.extraUsado += noExtra;
      alvo.a.tarefas.push({
        linhaId: linha.id,
        ordem: linha.ordemCodigo, produto: linha.produtoNome,
        etapa: linha.etapa, setor: linha.setor,
        pecas, minutos: Number(minutos.toFixed(1)),
        emExtra: noExtra > 0 ? Number(noExtra.toFixed(1)) : 0,
        porHora: alvo.ritmo.porHora,
        base: alvo.ritmo.origem, entrega: linha.entrega,
      });
      restam -= pecas;
    }

    /* a marca de cheio vale só para esta operação: na próxima linha
       a pessoa é reavaliada pelo tempo que realmente tem */
    agenda.forEach(a=>{ delete a.cheio; });
  });

  agenda.forEach(a=>{
    a.usado = Number(a.usado.toFixed(1));
    a.extraUsado = Number(a.extraUsado.toFixed(1));
    a.livre = Number(Math.max(0, a.capacidade - a.usado).toFixed(1));
    a.ocupacao = a.capacidade > 0
      ? Number((a.usado / a.capacidade * 100).toFixed(0)) : 0;
    a.pecas = a.tarefas.reduce((s,t)=>s + t.pecas, 0);
    /* quem tem folga pode receber outra coisa */
    a.temFolga = a.livre >= 30 && !a.semMaquina;
  });

  const hab = habilitadosPara(db, pessoas, maquinas);
  const totalSobra = sobra.reduce((s,x)=>s + x.minutos, 0);
  const totalExtra = agenda.reduce((s,a)=>s + a.extraUsado, 0);
  const folgaTotal = agenda.reduce((s,a)=>s + a.livre, 0);

  return {
    vazio:false, agenda, sobra, demanda, minutosDia,
    maquinas: maquinas.length, limiteSimultaneo,
    totalSobra: Number(totalSobra.toFixed(1)),
    totalExtra: Number(totalExtra.toFixed(1)),
    folgaTotal: Number(folgaTotal.toFixed(1)),
    maquinaIds: maquinas,
    pecasProgramadas: agenda.reduce((s,a)=>s + a.pecas, 0),
    pecasSobrando: sobra.reduce((s,x)=>s + x.quantidade, 0),
    /* quem foi escalado mas não opera nenhuma das máquinas do dia */
    semHabilitacao: hab.semHabilitacao.map(id=>{
      const c = (db.colaboradores||[]).find(x=>x.id===id);
      return {id, nome: c ? c.nome : 'removido'};
    }),
    /* quem ficou sem posto por falta de máquina */
    semMaquina,
    sugestoes: sugerirDestinoDaSobra(db, {agenda, sobra, minutosDia,
      totalSobra, folgaTotal, extra,
      semMaquina,
      semHabilitacao: hab.semHabilitacao.map(id=>{
        const c = (db.colaboradores||[]).find(x=>x.id===id);
        return {id, nome: c ? c.nome : 'removido'};
      })}),
  };
}

/* O que fazer com o que não coube — e com quem sobrou tempo. */
function sugerirDestinoDaSobra(db, dados){
  const {agenda, sobra, minutosDia, totalSobra, folgaTotal, extra} = dados;
  const s = [];

  if(totalSobra > 0){
    /* cabe em hora extra? */
    const extraDisponivel = agenda.reduce((a,x)=>
      a + Math.max(0, LIMITE_EXTRA_DIA - x.extraUsado), 0);
    if(extraDisponivel > 0){
      const cabeTudo = totalSobra <= extraDisponivel;
      const usar = Math.min(totalSobra, extraDisponivel);
      const porPessoa = usar / Math.max(1, agenda.length) / 60;
      s.push({
        tipo:'extra',
        titulo: cabeTudo
          ? `Cabe em hora extra: ${duracao(totalSobra)} no total`
          : `Parte cabe em hora extra: ${duracao(usar)} de ${duracao(totalSobra)}`,
        detalhe:`Cerca de ${porPessoa.toFixed(1)}h por pessoa. ` +
          `O limite legal é 2h por dia, e hora extra sai mais cara que ` +
          `hora normal — vale conferir se o prazo justifica.` +
          (cabeTudo ? '' :
            ` O restante (${duracao(totalSobra - usar)}) fica para outro dia.`),
      });
    }
    /* quantos dias até terminar tudo */
    const capacidadeDia = agenda.reduce((a,x)=>a + x.capacidade, 0);
    if(capacidadeDia > 0){
      const dias = totalSobra / capacidadeDia;
      s.push({
        tipo:'proximoDia',
        titulo: dias <= 1
          ? `Continua amanhã: ${duracao(totalSobra)} de trabalho`
          : `Continua pelos próximos ${Math.ceil(dias)} dia(s) com esta equipe`,
        detalhe:`São ${duracao(totalSobra)} que não couberam hoje, contra ` +
          `${duracao(capacidadeDia)} de capacidade por dia.`,
      });
    }
    /* alguém de fora pode ajudar? */
    const deFora = (db.colaboradores||[]).filter(c=>
      c.status !== 'Inativo' && c.produtivo !== false &&
      !agenda.some(a=>a.id===c.id));
    if(deFora.length > 0)
      s.push({
        tipo:'reforco',
        titulo:`Ou chamar reforço: ${deFora.length} pessoa(s) fora desta escala`,
        detalhe: deFora.slice(0,4).map(c=>c.nome).join(', ') +
          (deFora.length > 4 ? ` e mais ${deFora.length-4}` : '') +
          '. Confira se sabem fazer a operação antes de contar com elas.',
      });
  }

  /* gente com tempo livre e nada para fazer */
  const comFolga = agenda.filter(a=>a.temFolga);
  if(comFolga.length > 0 && totalSobra === 0)
    s.push({
      tipo:'folga',
      titulo:`${comFolga.length} pessoa(s) com tempo livre: ${duracao(folgaTotal)}`,
      detalhe: comFolga.map(a=>`${a.nome} (${duracao(a.livre)})`).join(', ') +
        '. Dá para adiantar ordem seguinte, aferir quem ainda não foi medido, ' +
        'ou treinar em outra operação.',
    });

  const semMaq = dados.semMaquina || [];
  if(semMaq.length > 0)
    s.push({
      tipo:'semMaquina',
      titulo:`${semMaq.length} pessoa(s) sem máquina disponível`,
      detalhe: semMaq.map(x=>x.nome).join(', ') +
        '. Há mais gente escalada que máquina para trabalhar. Elas podem ' +
        'ir para outra operação, outro setor, ou o dia pede menos gente ' +
        'aqui do que foi escalado.',
    });

  const semHab = dados.semHabilitacao || [];
  if(semHab.length > 0)
    s.push({
      tipo:'habilitacao',
      titulo:`${semHab.length} pessoa(s) escalada(s) não operam as máquinas do dia`,
      detalhe: semHab.map(x=>x.nome).join(', ') +
        '. Elas não receberam tarefa. Ou escolha máquina que saibam usar, ' +
        'ou marque no cadastro delas o que já conseguem operar — se for o ' +
        'caso de treinar, o dia com folga é boa hora.',
    });

  if(comFolga.length > 0 && totalSobra > 0)
    s.push({
      tipo:'desequilibrio',
      titulo:'Tem gente com folga e trabalho sobrando ao mesmo tempo',
      detalhe: comFolga.map(a=>a.nome).join(', ') +
        ' não estão cheios, mas o que sobrou é de operação que eles não ' +
        'pegam — por falta de máquina livre ou por não saberem fazer. ' +
        'Treinar cruzado resolve isso melhor que hora extra.',
    });

  return s;
}

/* ==========================================================
   BALANCEAMENTO DA CARTEIRA
   A linha não produz um produto só. Balancear olhando uma
   referência isolada dá a resposta certa para a pergunta
   errada — o que a fábrica precisa saber é se dá conta de
   tudo que está prometido, junto.
========================================================== */

/* O que a fábrica tem para trabalhar num setor: gente e máquina.
   Alocar mais pessoas do que existem é planejar no papel. */
function recursosDoSetor(departamentoId, db){
  const pessoas = (db.colaboradores||[]).filter(c=>
    c.departamentoId === departamentoId && c.status !== 'Inativo' &&
    c.produtivo !== false);
  const maquinas = (db.equipamentos||[]).filter(e=>
    e.departamentoId === departamentoId && e.ativo !== false &&
    e.situacao !== 'parada' && e.situacao !== 'baixada');
  return {
    pessoas: pessoas.length, maquinas: maquinas.length,
    listaPessoas: pessoas, listaMaquinas: maquinas,
    /* o limite real é o menor dos dois quando a operação precisa de
       máquina — não adianta ter cinco costureiras e três máquinas */
    limite: maquinas.length > 0
      ? Math.min(pessoas.length, maquinas.length) : pessoas.length,
  };
}

/* Junta a demanda de várias ordens por operação. Duas ordens que
   passam pela mesma costura disputam a mesma costureira. */
function balancearCarteira(db, opcoes){
  const op = opcoes || {};
  const j = jornadaDe(db);
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const alocacao = op.alocacao || {};
  const ids = op.ordemIds || [];

  const ordens = (db.ordens||[]).filter(o=>
    ids.includes(o.id) && o.situacao !== 'cancelada');
  if(ordens.length === 0)
    return {vazio:true, postos:[], ordens:[], gargalo:null, minutosDia};

  /* cada ordem tem seu produto e sua quantidade; a mesma operação
     pode aparecer em várias */
  const mapa = {};
  const detalhes = [];

  ordens.forEach(o=>{
    const produto = (db.produtos||[]).find(p=>p.id===o.produtoId);
    if(!produto) return;
    const r = resumoProcesso(produto, db);
    /* o que ainda falta produzir, não o total da ordem */
    const av = avancoDaOrdem(o, db);
    detalhes.push({ordem:o, produto, avanco:av});

    r.passos.forEach(p=>{
      const et = p.etapa;
      /* a chave é a operação: é ela que disputa recurso */
      const chave = p.passo.etapaId || (et ? et.id : p.passo.id);
      const pessoasRoteiro = Math.max(1, num(p.passo.pessoas) || 1);
      const unitario = num(p.minutosHomem) / pessoasRoteiro;
      const linha = av.etapas.find(x=>x.tarefa &&
        x.tarefa.etapaId === p.passo.etapaId);
      const falta = linha ? Math.max(0, linha.saldo) : num(o.quantidade);

      if(!mapa[chave]) mapa[chave] = {
        id: chave,
        etapa: et ? et.nome : 'etapa removida',
        setor: p.departamento ? p.departamento.nome : '—',
        departamentoId: p.passo.departamentoId,
        unitario: 0, carga: 0, produtos: [],
        pessoasRoteiro,
      };
      const m = mapa[chave];
      /* o tempo unitário pode variar entre produtos; guardamos o maior,
         que é o que dimensiona o posto */
      m.unitario = Math.max(m.unitario, unitario);
      m.carga += unitario * falta;
      m.produtos.push({
        ordemId: o.id,
        ordem: o.codigo,
        entrega: o.entrega || '',
        produto: montarNomeProduto(produto, db) || produto.codigo,
        falta, unitario: Number(unitario.toFixed(4)),
      });
    });
  });

  let postos = Object.values(mapa);
  /* operação já concluída não disputa recurso nem consome gente do
     planejamento — sem isso, uma etapa pronta continuava "levando" uma
     pessoa e a soma da equipe não batia com o informado */
  postos = postos.filter(p=>p.carga > 0);
  if(op.departamentoId)
    postos = postos.filter(p=>p.departamentoId === op.departamentoId);
  if(postos.length === 0)
    return {vazio:true, postos:[], ordens:detalhes, gargalo:null, minutosDia};

  const informado = op.recursosInformados || {};

  /* Se o supervisor diz "tenho 5 pessoas", o sistema distribui essas 5
     entre os postos — proporcional à carga, porque quem tem mais
     trabalho precisa de mais gente. Antes esse número só servia de
     aviso, e mexer nele não mudava nada: o usuário informava a equipe
     e o prazo continuava igual. */
  const distribuida = {};
  let equipeDividida = null;
  if(num(informado.pessoas) > 0 && !op.semDistribuir){
    const total = num(informado.pessoas);
    const soma = postos.reduce((s,p)=>s + p.carga, 0);
    if(soma > 0 && total > 0){
      /* Distribuição proporcional à carga, aceitando fração. Em costura
         uma pessoa faz várias operações em sequência — forçar uma
         pessoa por posto inventaria gente que a fábrica não tem. Se
         você informa 4, o sistema aloca 4, nem que seja meia pessoa
         por operação. */
      postos.forEach(p=>{
        distribuida[p.id] = Number((total * (p.carga / soma)).toFixed(4));
      });
      /* o que importa não é o total, e sim se algum posto ficou com
         menos de uma pessoa: é aí que alguém divide o tempo entre
         operações. Com 12 pessoas e 8 postos isso ainda acontece se a
         carga for muito desigual. */
      const partidos = postos.filter(p=>distribuida[p.id] < 1);
      if(partidos.length)
        equipeDividida = {informado: total, postos: postos.length,
          partidos: partidos.length};
    }
  }

  /* Escala nominal por operação: quando o usuário diz QUEM faz, o ritmo
     vem do histórico de cada pessoa, não de uma média. É o que
     transforma a carteira em programação de verdade. */
  const escalas = op.escalas || {};

  postos = postos.map(p=>{
    const doCadastro = recursosDoSetor(p.departamentoId, db);
    /* o supervisor sabe quem faltou hoje; o cadastro não */
    const rec = (informado.pessoas !== null && informado.pessoas !== undefined) ||
      (informado.maquinas !== null && informado.maquinas !== undefined)
      ? (()=>{
          const pessoas = informado.pessoas !== null && informado.pessoas !== undefined
            ? informado.pessoas : doCadastro.pessoas;
          const maquinas = informado.maquinas !== null && informado.maquinas !== undefined
            ? informado.maquinas : doCadastro.maquinas;
          return {...doCadastro, pessoas, maquinas, informado:true,
            limite: maquinas > 0 ? Math.min(pessoas, maquinas) : pessoas};
        })()
      : doCadastro;
    /* quem foi escalado nominalmente manda; depois a alocação manual;
       depois a distribuição automática */
    const esc = escalas[p.id];
    const cap = (esc && (esc.pessoas||[]).length)
      ? capacidadeDaEquipe(db, esc.pessoas, p.id, p.unitario) : null;
    const pessoas = (cap && !cap.vazia && cap.porHora > 0) ? cap.pessoas
      : (num(alocacao[p.id]) > 0 ? num(alocacao[p.id])
        : (distribuida[p.id] > 0 ? distribuida[p.id]
          : Math.max(1, p.pessoasRoteiro)));
    /* Com equipe escalada, o tempo vem do ritmo real dela: a carga em
       peças dividida pelo que o time entrega por hora. Sem escala,
       a divisão simples pelo número de pessoas. */
    const pecas = p.unitario > 0 ? p.carga / p.unitario : 0;
    const minutos = (cap && !cap.vazia && cap.porHora > 0)
      ? (pecas / cap.porHora) * 60
      : p.carga / pessoas;
    const dias = minutosDia > 0 ? minutos / minutosDia : null;
    return {
      ...p,
      carga: Number(p.carga.toFixed(1)),
      pessoas,
      recursos: rec,
      /* alocar mais gente do que o setor tem é planejar no papel */
      excede: pessoas > rec.limite,
      minutos: Number(minutos.toFixed(1)),
      dias: dias === null ? null : Number(dias.toFixed(2)),
      escala: (cap && !cap.vazia) ? cap : null,
    };
  });

  /* o gargalo da carteira é a operação que demora mais para dar conta
     de tudo que tem pela frente */
  const gargalo = postos.slice().sort((a,b)=>b.minutos - a.minutos)[0] || null;
  const diasLinha = gargalo ? gargalo.dias : 0;
  const totalPessoas = postos.reduce((s,p)=>s+p.pessoas, 0);
  const cargaTotal = postos.reduce((s,p)=>s+p.carga, 0);

  /* a entrega mais apertada da carteira */
  const entregas = detalhes.map(d=>d.ordem.entrega).filter(Boolean).sort();
  const primeiraEntrega = entregas[0] || null;

  return {
    vazio:false, postos, ordens:detalhes, gargalo, minutosDia,
    diasLinha: diasLinha === null ? null : Number(diasLinha.toFixed(1)),
    totalPessoas,
    cargaTotal: Number(cargaTotal.toFixed(1)),
    /* quanto do tempo alocado é realmente usado */
    ocupacaoMedia: (gargalo && gargalo.minutos > 0 && totalPessoas > 0)
      ? Number((cargaTotal / (totalPessoas * gargalo.minutos) * 100).toFixed(1))
      : null,
    excedidos: postos.filter(p=>p.excede),
    equipeDividida,
    primeiraEntrega,
    /* dá tempo? */
    prazo: primeiraEntrega && diasLinha !== null
      ? (()=>{
          const fim = new Date(todayISO());
          let restam = Math.ceil(diasLinha), g = 0;
          while(restam > 0 && g < 400){
            g++;
            fim.setDate(fim.getDate()+1);
            const s = fim.getDay();
            if(s === 0 || s === 6) continue;
            restam--;
          }
          const data = fim.toISOString().slice(0,10);
          return {data, cabe: data <= primeiraEntrega};
        })()
      : null,
  };
}

/* ==========================================================
   LEAD TIME REAL
   Somar o tempo das operações dá o tempo de trabalho, não o
   tempo de atravessar a fábrica. A peça espera: na fila do
   silk, o lote juntar, a terceirizada devolver. Numa confecção
   essa espera costuma ser várias vezes maior que o processo —
   e é ela que faz a data prometida não bater.
========================================================== */

/* Espera padrão por setor, em horas. Serve de ponto de partida:
   a fábrica descobre o número dela comparando previsto com real. */
const FILA_PADRAO_HORAS = 4;

function filaDoSetor(departamentoId, db){
  const p = (db.parametrosFila || {});
  const especifico = p[departamentoId];
  if(especifico !== undefined && especifico !== null && num(especifico) >= 0)
    return num(especifico);
  const geral = num(p.padrao);
  return geral >= 0 && p.padrao !== undefined && p.padrao !== null
    ? geral : FILA_PADRAO_HORAS;
}

function salvarFilas(d, filas, usuario){
  const limpo = {};
  Object.keys(filas || {}).forEach(k=>{
    const v = num(filas[k]);
    if(v >= 0) limpo[k] = v;
  });
  d.parametrosFila = limpo;
  registrarLog(d, usuario, 'pcp.filas', 'Tempos de fila atualizados');
  return {filas: limpo};
}

/* O caminho da peça pela fábrica: processar, esperar, processar.
   Com lote, o tempo de processo é o do lote inteiro — a próxima
   etapa só começa quando o lote chega. */
function leadTimeProduto(produto, db, opcoes){
  const op = opcoes || {};
  const j = jornadaDe(db);
  if(!produto) return {vazio:true, etapas:[], totalMinutos:0, totalDias:0,
    totalProcesso:0, totalFila:0, proporcaoFila:0, eficienciaFluxo:0};
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const quantidade = Math.max(1, num(op.quantidade) || 1);
  const r = resumoProcesso(produto, db);

  if(r.passos.length === 0)
    return {vazio:true, etapas:[], totalMinutos:0, totalDias:0};

  const etapas = r.passos.map((p, i)=>{
    const pessoas = Math.max(1, num(p.passo.pessoas) || 1);
    /* o lote inteiro precisa passar antes de seguir */
    const processo = num(p.minutosHomem) / pessoas * quantidade;
    const filaHoras = filaDoSetor(p.passo.departamentoId, db);
    /* a primeira etapa não espera fila: o material já está lá */
    const fila = i === 0 ? 0 : filaHoras * 60;
    return {
      ordem: i+1,
      etapa: p.etapa ? p.etapa.nome : 'etapa removida',
      setor: p.departamento ? p.departamento.nome : '—',
      departamentoId: p.passo.departamentoId,
      processo: Number(processo.toFixed(1)),
      fila: Number(fila.toFixed(1)),
      total: Number((processo + fila).toFixed(1)),
      filaHoras,
    };
  });

  const totalProcesso = etapas.reduce((s,e)=>s+e.processo, 0);
  const totalFila = etapas.reduce((s,e)=>s+e.fila, 0);
  const total = totalProcesso + totalFila;

  return {
    vazio:false, etapas, quantidade, minutosDia,
    totalProcesso: Number(totalProcesso.toFixed(1)),
    totalFila: Number(totalFila.toFixed(1)),
    totalMinutos: Number(total.toFixed(1)),
    totalDias: minutosDia > 0 ? Number((total/minutosDia).toFixed(2)) : null,
    /* a proporção que assusta e é verdadeira */
    proporcaoFila: total > 0 ? Number((totalFila/total*100).toFixed(1)) : 0,
    /* quanto do tempo a peça está sendo trabalhada */
    eficienciaFluxo: total > 0
      ? Number((totalProcesso/total*100).toFixed(1)) : 0,
  };
}

/* Datas de entrega prometidas com base no caminho completo. */
function previsaoComFila(produto, db, opcoes){
  const op = opcoes || {};
  const lt = leadTimeProduto(produto, db, op);
  if(lt.vazio) return {vazio:true};
  const minutosDia = lt.minutosDia;
  const inicio = op.inicio || todayISO();

  let faltam = lt.totalMinutos;
  const dia = new Date(inicio);
  let uteis = 0, guarda = 0;
  while(faltam > 0 && guarda < 600){
    guarda++;
    dia.setDate(dia.getDate() + 1);
    const s = dia.getDay();
    if(s === 0 || s === 6) continue;
    faltam -= minutosDia;
    uteis++;
  }
  const data = dia.toISOString().slice(0,10);

  /* a data que sairia contando só o trabalho — a promessa otimista */
  let f2 = lt.totalProcesso;
  const dia2 = new Date(inicio);
  let uteis2 = 0, g2 = 0;
  while(f2 > 0 && g2 < 600){
    g2++;
    dia2.setDate(dia2.getDate() + 1);
    const s = dia2.getDay();
    if(s === 0 || s === 6) continue;
    f2 -= minutosDia;
    uteis2++;
  }
  const dataSemFila = dia2.toISOString().slice(0,10);

  return {
    vazio:false, lead: lt, inicio,
    data, dias: uteis,
    dataSemFila, diasSemFila: uteis2,
    /* quantos dias a espera acrescenta */
    diasDeEspera: uteis - uteis2,
  };
}

/* Comparar o previsto com o que realmente aconteceu. É assim que a
   fábrica descobre o tempo de fila dela, em vez de chutar. */
function leadTimeRealizado(db, opcoes){
  const op = opcoes || {};
  const concluidas = (db.ordens||[]).filter(o=>{
    if(o.situacao !== 'concluida') return false;
    if(op.produtoId && o.produtoId !== op.produtoId) return false;
    return !!o.criadoEm && !!o.concluidaEm;
  });

  const medidas = concluidas.map(o=>{
    const a = dataValida(o.criadoEm), b = dataValida(o.concluidaEm);
    if(!a || !b) return null;
    const dias = Math.max(0, (b - a) / 86400000);
    const aps = apontamentosDaOrdem(db, o.id);
    const minutosTrabalhados = aps.reduce((s,x)=>s+num(x.minutosGastos), 0);
    return {
      ordem: o, dias: Number(dias.toFixed(1)),
      minutosTrabalhados,
      quantidade: num(o.quantidade),
    };
  }).filter(Boolean);

  if(medidas.length === 0)
    return {vazio:true, medidas:[], amostra:0,
      aviso:'Nenhuma ordem concluída ainda. O lead time real aparece quando '+
        'as primeiras ordens fecharem — e aí dá para calibrar a espera com '+
        'o que aconteceu, em vez de estimar.'};

  const dias = medidas.map(m=>m.dias).sort((a,b)=>a-b);
  const media = dias.reduce((a,b)=>a+b,0) / dias.length;
  const mediana = dias.length % 2
    ? dias[(dias.length-1)/2]
    : (dias[dias.length/2 - 1] + dias[dias.length/2]) / 2;

  return {
    vazio:false, medidas, amostra: medidas.length,
    media: Number(media.toFixed(1)),
    /* a mediana resiste melhor a uma ordem que travou por outro motivo */
    mediana: Number(mediana.toFixed(1)),
    menor: dias[0],
    maior: dias[dias.length-1],
    /* amostra pequena não sustenta conclusão */
    confiavel: medidas.length >= 5,
  };
}

/* O previsto bate com o real? Se não, a espera configurada está errada. */
function calibrarFila(db, produtoId){
  const real = leadTimeRealizado(db, {produtoId});
  if(real.vazio) return {vazio:true, aviso: real.aviso};

  const produto = (db.produtos||[]).find(p=>p.id===produtoId);
  if(!produto) return {vazio:true, aviso:'Produto não encontrado.'};

  /* usa a quantidade média das ordens medidas */
  const qtdMedia = real.medidas.reduce((s,m)=>s+m.quantidade, 0) / real.medidas.length;
  const previsto = previsaoComFila(produto, db, {quantidade: qtdMedia});
  if(previsto.vazio) return {vazio:true, aviso:'Produto sem processo produtivo.'};

  const diferenca = real.mediana - previsto.dias;
  return {
    vazio:false, real, previsto, quantidadeMedia: Math.round(qtdMedia),
    diferenca: Number(diferenca.toFixed(1)),
    /* a espera precisa subir ou descer? */
    ajuste: Math.abs(diferenca) < 1 ? 'ok'
      : diferenca > 0 ? 'aumentar' : 'diminuir',
    confiavel: real.confiavel,
  };
}

/* ==========================================================
   EQUIPE POR OPERAÇÃO
   Balancear por "quantas pessoas" supõe que todas produzem
   igual. Não produzem: quem já fez aquela operação cem vezes
   é mais rápido que quem nunca fez. Escolher QUEM vai para
   cada posto dá um plano que a fábrica consegue cumprir.
========================================================== */

/* O que esta pessoa já produziu nesta operação, pelos apontamentos. */
function desempenhoNaEtapa(db, colaboradorId, etapaId){
  const aps = (db.apontamentos||[]).filter(a=>
    a.colaboradorId === colaboradorId && a.etapaId === etapaId &&
    num(a.minutosGastos) > 0);
  const boas = aps.reduce((s,a)=>s + num(a.pecasBoas), 0);
  const defeito = aps.reduce((s,a)=>s + num(a.pecasDefeito), 0);
  const minutos = aps.reduce((s,a)=>s + num(a.minutosGastos), 0);
  const total = boas + defeito;

  if(aps.length === 0 || minutos <= 0)
    return {semDados:true, apontamentos:0, porHora:null, qualidade:null};

  return {
    semDados:false,
    apontamentos: aps.length,
    boas, defeito, minutos,
    porHora: Number((boas / (minutos/60)).toFixed(1)),
    minutosPorPeca: boas > 0 ? Number((minutos / boas).toFixed(4)) : null,
    qualidade: total > 0 ? Number((boas/total*100).toFixed(1)) : null,
    /* poucos apontamentos não sustentam conclusão */
    confiavel: aps.length >= 3,
  };
}

/* A média da fábrica nesta operação — a referência para quem nunca fez. */
function mediaDaEtapa(db, etapaId){
  const aps = (db.apontamentos||[]).filter(a=>
    a.etapaId === etapaId && num(a.minutosGastos) > 0);
  const boas = aps.reduce((s,a)=>s + num(a.pecasBoas), 0);
  const minutos = aps.reduce((s,a)=>s + num(a.minutosGastos), 0);
  const pessoas = new Set(aps.map(a=>a.colaboradorId).filter(Boolean)).size;

  if(minutos <= 0) return {semDados:true, porHora:null, pessoas:0};
  return {
    semDados:false,
    porHora: Number((boas / (minutos/60)).toFixed(1)),
    minutosPorPeca: boas > 0 ? Number((minutos / boas).toFixed(4)) : null,
    apontamentos: aps.length, pessoas,
  };
}

/* Quanto rende uma pessoa nesta operação: o histórico dela, a média da
   fábrica, ou o tempo do roteiro — nessa ordem de preferência. */
function ritmoEsperado(db, colaboradorId, etapaId, minutosRoteiro, opcoes){
  const op = opcoes || {};
  const meu = desempenhoNaEtapa(db, colaboradorId, etapaId);
  if(!meu.semDados && meu.porHora > 0)
    return {porHora: meu.porHora, origem:'historico',
      label:'já fez esta operação', confiavel: meu.confiavel,
      apontamentos: meu.apontamentos, qualidade: meu.qualidade};

  /* Onde não se afere, a referência é a jornada do setor: o que ele
     entregou por hora trabalhada. É menos preciso que a aferição, e é
     o que existe — melhor isso que exigir cronômetro no corte. */
  if(!etapaExigeAfericao(db, etapaId)){
    const j = metaPelaJornada(db, etapaId, {});
    if(!j.semDados && j.porHora > 0)
      return {porHora: j.porHora, origem:'jornada',
        label:'média do setor por hora trabalhada', confiavel: j.confiavel,
        apontamentos: j.apontamentos};
  }

  /* Meta aferida vale mais que média de apontamento: ela foi medida com
     método, cronometrada e assinada. A média bruta mistura dia bom com
     dia ruim, máquina quebrada e treinamento. */
  const meta = metaVigente(db, etapaId, op.produtoId, op.equipamentoId);
  if(meta && num(meta.valorHora) > 0)
    return {porHora: num(meta.valorHora), origem:'meta',
      label:'meta aferida para a operação', confiavel:true,
      apontamentos:0, meta};

  const geral = mediaDaEtapa(db, etapaId);
  if(!geral.semDados && geral.porHora > 0)
    return {porHora: geral.porHora, origem:'media',
      label:'média da fábrica nesta operação', confiavel:false,
      apontamentos:0};

  const rot = num(minutosRoteiro);
  if(rot > 0)
    return {porHora: Number((60/rot).toFixed(1)), origem:'roteiro',
      label:'tempo do roteiro, sem histórico', confiavel:false,
      apontamentos:0};

  return {porHora:0, origem:'nenhum', label:'sem base para estimar',
    confiavel:false, apontamentos:0};
}

/* Quem está disponível para uma operação, com o desempenho de cada um. */
function candidatosDaEtapa(db, departamentoId, etapaId, minutosRoteiro, opcoes){
  const op = opcoes || {};
  const geral = mediaDaEtapa(db, etapaId);
  return (db.colaboradores||[])
    .filter(c=>c.departamentoId === departamentoId &&
      c.status !== 'Inativo' && c.produtivo !== false)
    .map(c=>{
      const meu = desempenhoNaEtapa(db, c.id, etapaId);
      const r = ritmoEsperado(db, c.id, etapaId, minutosRoteiro, op);
      /* já existe aferição desta pessoa nesta operação? */
      const af = (db.afericoes||[]).filter(x=>
        x.colaboradorId === c.id && x.etapaId === etapaId);
      const concluida = af.find(x=>x.fase === 'concluida');
      const emAndamento = af.find(x=>x.fase !== 'concluida' && x.fase !== 'cancelada');
      return {
        colaborador: c, nome: c.nome,
        desempenho: meu, ritmo: r,
        porHora: r.porHora,
        /* comparação com a média: acima ou abaixo, e quanto */
        contraMedia: (!meu.semDados && !geral.semDados && geral.porHora > 0)
          ? Number(((meu.porHora / geral.porHora - 1) * 100).toFixed(0)) : null,
        afericao: concluida || emAndamento || null,
        afericaoEmAndamento: !!emAndamento && !concluida,
        /* sem histórico e sem aferição: o número dele é chute */
        precisaAferir: meu.semDados && !concluida,
      };
    })
    .sort((a,b)=>{
      /* quem já fez vem primeiro, do mais rápido para o mais lento */
      if(a.desempenho.semDados !== b.desempenho.semDados)
        return a.desempenho.semDados ? 1 : -1;
      return b.porHora - a.porHora;
    });
}

/* A capacidade da equipe escolhida: soma do que cada um rende. */
function capacidadeDaEquipe(db, colaboradorIds, etapaId, minutosRoteiro, opcoes){
  const ids = (colaboradorIds||[]).filter(Boolean);
  if(ids.length === 0) return {vazia:true, porHora:0, pessoas:0, membros:[]};

  const membros = ids.map(id=>{
    const c = (db.colaboradores||[]).find(x=>x.id===id);
    const r = ritmoEsperado(db, id, etapaId, minutosRoteiro, opcoes);
    return {id, nome: c ? c.nome : 'removido', ritmo:r, porHora:r.porHora,
      /* quem entra só com estimativa merece ser aferido */
      estimado: r.origem === 'media' || r.origem === 'roteiro'};
  });
  const porHora = membros.reduce((s,m)=>s + m.porHora, 0);
  /* histórico próprio ou meta aferida contam como base medida */
  const comHistorico = membros.filter(m=>
    m.ritmo.origem==='historico' || m.ritmo.origem==='meta').length;

  return {
    vazia:false, membros, pessoas: membros.length,
    porHora: Number(porHora.toFixed(1)),
    /* o ciclo da equipe: quanto tempo por peça, somando todo mundo */
    ciclo: porHora > 0 ? Number((60/porHora).toFixed(5)) : 0,
    comHistorico,
    aAferir: membros.filter(m=>m.estimado),
    /* quanto do plano se apoia em dado real */
    baseReal: membros.length > 0
      ? Number((comHistorico/membros.length*100).toFixed(0)) : 0,
  };
}

/* A meta por hora que equilibra a linha: todo posto no ritmo do gargalo
   não adianta — o que interessa é o ritmo que a linha inteira precisa. */
function metaHoraDaLinha(postos, opcoes){
  const op = opcoes || {};
  if(!postos || postos.length === 0) return null;
  const demanda = num(op.demandaDia);
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : 480;

  /* se há demanda, a meta é ela; senão, o ritmo do gargalo atual */
  const alvoHora = demanda > 0
    ? Number((demanda / (minutosDia/60)).toFixed(1))
    : Math.min(...postos.map(p=>num(p.porHora)||Infinity));

  if(!isFinite(alvoHora) || alvoHora <= 0) return null;

  return {
    alvoHora,
    origem: demanda > 0 ? 'demanda' : 'gargalo',
    postos: postos.map(p=>{
      const atual = num(p.porHora);
      return {
        id: p.id, etapa: p.etapa,
        atual,
        meta: alvoHora,
        /* sobra ou falta capacidade neste posto */
        folga: Number((atual - alvoHora).toFixed(1)),
        atende: atual >= alvoHora,
        /* quantas pessoas a mais para alcançar a meta */
        faltamPessoas: atual > 0 && atual < alvoHora
          ? Number(((alvoHora/atual - 1) * (num(p.pessoas)||1)).toFixed(2)) : 0,
      };
    }),
  };
}

/* ==========================================================
   BALANCEAMENTO DE LINHA
   A linha produz no ritmo da operação mais lenta. Se o
   fechamento faz 60/h e o resto faz 90/h, a linha faz 60/h e
   oito pessoas ficam esperando — sem que ninguém pareça
   estar parado. É o desperdício mais caro e mais invisível
   de uma confecção.
========================================================== */

/* Quanto tempo cada peça tem para sair, dada a demanda do dia.
   É o compasso da linha: operação mais lenta que o takt atrasa
   tudo; mais rápida acumula estoque na frente da próxima. */
function taktTime(minutosDia, demandaDia){
  const m = num(minutosDia), q = num(demandaDia);
  if(!(m > 0) || !(q > 0)) return null;
  return Number((m / q).toFixed(4));
}

/* O retrato da linha: quem faz o quê, em quanto tempo, com
   quantas pessoas — e onde ela trava. */
function balancearLinha(produto, db, opcoes){
  const op = opcoes || {};
  const j = jornadaDe(db);
  if(!produto) return {vazio:true, postos:[], gargalo:null,
    minutosDia: num(op.minutosDia) || j.produtivo};
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const r = resumoProcesso(produto, db);
  const alocacao = op.alocacao || {};

  if(r.passos.length === 0)
    return {vazio:true, postos:[], gargalo:null, minutosDia};

  let postos = r.passos.map((p, i) => {
    const et = p.etapa;
    /* quantas pessoas nesta operação: o que foi simulado, o que o
       roteiro pede, ou uma */
    const pessoas = Math.max(1,
      num(alocacao[p.passo.id]) || num(p.passo.pessoas) || 1);
    /* o tempo de uma peça nesta operação, com uma pessoa */
    const tempoUnitario = num(p.minutosHomem) / Math.max(1, num(p.passo.pessoas) || 1);
    /* com N pessoas, o ciclo cai proporcionalmente */
    const ciclo = pessoas > 0 ? tempoUnitario / pessoas : 0;
    const porHora = ciclo > 0 ? Number((60 / ciclo).toFixed(1)) : 0;
    const porDia = ciclo > 0 ? Math.floor(minutosDia / ciclo) : 0;

    return {
      id: p.passo.id,
      ordem: i + 1,
      etapa: et ? et.nome : 'etapa removida',
      setor: p.departamento ? p.departamento.nome : '—',
      equipamento: p.equipamento ? p.equipamento.nome : '',
      tempoUnitario: Number(tempoUnitario.toFixed(5)),
      pessoas,
      pessoasOriginais: Math.max(1, num(p.passo.pessoas) || 1),
      ciclo: Number(ciclo.toFixed(5)),
      porHora, porDia,
    };
  });

  /* filtrar por setor mostra a linha daquele departamento apenas */
  if(op.departamentoId)
    postos = postos.filter(p=>{
      const passo = (r.passos.find(x=>x.passo.id===p.id)||{}).passo;
      return passo && passo.departamentoId === op.departamentoId;
    });
  if(postos.length === 0)
    return {vazio:true, postos:[], gargalo:null, minutosDia};

  /* Escala nominal: quando o usuário diz QUEM faz a operação, o ritmo
     vem do histórico de cada pessoa, não de uma média genérica. É a
     diferença entre "duas pessoas" e "a Rita e o João". */
  const escalas = op.escalas || {};
  postos.forEach(p=>{
    const esc = escalas[p.id];
    if(!esc || !(esc.pessoas||[]).length) return;
    const passo = (r.passos.find(x=>x.passo.id===p.id)||{}).passo;
    const cap = capacidadeDaEquipe(db, esc.pessoas,
      passo ? passo.etapaId : null, p.tempoUnitario);
    if(cap.vazia || !(cap.porHora > 0)) return;
    p.pessoas = cap.pessoas;
    p.escala = cap;
    p.maquinasEscaladas = (esc.maquinas||[]).length;
    /* o ciclo passa a ser o da equipe real */
    p.ciclo = Number(cap.ciclo.toFixed(5));
    p.porHora = cap.porHora;
    p.porDia = p.ciclo > 0 ? Math.floor(minutosDia / p.ciclo) : 0;
  });

  /* recursos: o cadastro do setor, ou o que o supervisor informou */
  const informado = op.recursosInformados || {};

  /* pessoas informadas viram alocação de verdade, distribuídas por
     tempo de operação — senão informar a equipe não mudaria nada */
  let equipeDividida = null;
  const semEscala = postos.filter(p=>!p.escala);
  if(num(informado.pessoas) > 0 && !op.alocacaoManual && semEscala.length===postos.length){
    const total = num(informado.pessoas);
    const soma = postos.reduce((s,p)=>s + p.tempoUnitario, 0);
    if(soma > 0 && total > 0){
      /* proporcional ao tempo da operação, com fração: uma pessoa pode
         cobrir duas operações rápidas */
      postos.forEach(p=>{
        p.pessoas = Number((total * (p.tempoUnitario / soma)).toFixed(4));
      });
      const partidos = postos.filter(p=>p.pessoas < 1);
      if(partidos.length)
        equipeDividida = {informado: total, postos: postos.length,
          partidos: partidos.length};
      postos.forEach(p=>{
        p.ciclo = p.pessoas > 0
          ? Number((p.tempoUnitario / p.pessoas).toFixed(5)) : 0;
        p.porHora = p.ciclo > 0 ? Number((60 / p.ciclo).toFixed(1)) : 0;
        p.porDia = p.ciclo > 0 ? Math.floor(minutosDia / p.ciclo) : 0;
      });
    }
  }

  postos.forEach(p=>{
    const passo = (r.passos.find(x=>x.passo.id===p.id)||{}).passo;
    const doCadastro = passo
      ? recursosDoSetor(passo.departamentoId, db)
      : {pessoas:0, maquinas:0, limite:0};
    const temInformado = (informado.pessoas !== null && informado.pessoas !== undefined) ||
      (informado.maquinas !== null && informado.maquinas !== undefined);
    if(temInformado){
      const pessoas = informado.pessoas !== null && informado.pessoas !== undefined
        ? informado.pessoas : doCadastro.pessoas;
      const maquinas = informado.maquinas !== null && informado.maquinas !== undefined
        ? informado.maquinas : doCadastro.maquinas;
      p.recursos = {...doCadastro, pessoas, maquinas, informado:true,
        limite: maquinas > 0 ? Math.min(pessoas, maquinas) : pessoas};
    } else {
      p.recursos = doCadastro;
    }
    p.excede = p.pessoas > p.recursos.limite && p.recursos.limite > 0;
  });

  /* o gargalo é o maior ciclo — a operação que segura a fila */
  const gargalo = postos.slice().sort((a,b)=>b.ciclo - a.ciclo)[0] || null;
  const cicloLinha = gargalo ? gargalo.ciclo : 0;
  const saidaDia = cicloLinha > 0 ? Math.floor(minutosDia / cicloLinha) : 0;
  const saidaHora = cicloLinha > 0 ? Number((60 / cicloLinha).toFixed(1)) : 0;

  const totalPessoas = postos.reduce((s,p)=>s + p.pessoas, 0);
  const somaTrabalho = postos.reduce((s,p)=>s + p.tempoUnitario, 0);

  /* Eficiência de balanceamento: quanto do tempo pago vira produto.
     100% seria todo mundo ocupado o tempo inteiro — não existe, mas
     a distância até lá mede o desperdício. */
  const eficiencia = (cicloLinha > 0 && totalPessoas > 0)
    ? Number((somaTrabalho / (totalPessoas * cicloLinha) * 100).toFixed(1))
    : null;

  /* ocioso por posto: quanto cada um espera a cada peça */
  postos.forEach(p=>{
    p.ocioso = Number(Math.max(0, cicloLinha - p.ciclo).toFixed(5));
    p.ocupacao = cicloLinha > 0
      ? Number((p.ciclo / cicloLinha * 100).toFixed(1)) : 0;
    p.gargalo = !!gargalo && p.id === gargalo.id;
    /* quanto essa espera custa por dia, em minutos de gente parada */
    p.minutosPerdidosDia = Number((p.ocioso * saidaDia * p.pessoas).toFixed(0));
  });

  const takt = op.demandaDia ? taktTime(minutosDia, op.demandaDia) : null;
  const atende = takt !== null ? cicloLinha <= takt : null;

  return {
    vazio:false, postos, gargalo, cicloLinha, minutosDia,
    saidaDia, saidaHora, totalPessoas,
    somaTrabalho: Number(somaTrabalho.toFixed(5)),
    eficiencia,
    /* o tempo pago que não virou peça */
    minutosOciososDia: postos.reduce((s,p)=>s + p.minutosPerdidosDia, 0),
    equipeDividida,
    takt, demandaDia: num(op.demandaDia) || null, atende,
    faltaPorDia: (takt !== null && !atende)
      ? Math.max(0, num(op.demandaDia) - saidaDia) : 0,
  };
}

/* Onde colocar a próxima pessoa. A resposta quase nunca é óbvia:
   reforçar o gargalo só ajuda até ele deixar de ser o gargalo. */
function sugerirBalanceamento(produto, db, opcoes){
  const op = opcoes || {};
  const base = balancearLinha(produto, db, op);
  if(base.vazio) return {vazio:true, sugestoes:[]};

  const sugestoes = [];
  const alocacao = {...(op.alocacao||{})};
  base.postos.forEach(p=>{ alocacao[p.id] = p.pessoas; });

  /* simula acrescentar uma pessoa em cada posto e vê qual rende mais */
  base.postos.forEach(p=>{
    const teste = {...alocacao};
    teste[p.id] = p.pessoas + 1;
    const novo = balancearLinha(produto, db, {...op, alocacao: teste});
    const ganho = novo.saidaDia - base.saidaDia;
    if(ganho > 0) sugestoes.push({
      posto: p,
      pessoasDepois: p.pessoas + 1,
      saidaAntes: base.saidaDia,
      saidaDepois: novo.saidaDia,
      ganho,
      ganhoPercentual: base.saidaDia > 0
        ? Number((ganho / base.saidaDia * 100).toFixed(1)) : null,
      eficienciaDepois: novo.eficiencia,
      /* quanto cada peça a mais custa em gente */
      pecasPorPessoa: Number((novo.saidaDia / novo.totalPessoas).toFixed(1)),
    });
  });

  sugestoes.sort((a,b)=>b.ganho - a.ganho);

  /* tirar gente de onde sobra também é balanceamento — e é o que
     ninguém lembra de fazer */
  const podeReduzir = base.postos
    .filter(p=>p.pessoas > 1 && !p.gargalo)
    .map(p=>{
      const teste = {...alocacao};
      teste[p.id] = p.pessoas - 1;
      const novo = balancearLinha(produto, db, {...op, alocacao: teste});
      return {
        posto: p,
        pessoasDepois: p.pessoas - 1,
        perda: base.saidaDia - novo.saidaDia,
        liberaPessoa: true,
      };
    })
    .filter(x=>x.perda === 0)
    .sort((a,b)=>a.posto.ocupacao - b.posto.ocupacao);

  return {
    vazio:false, base,
    sugestoes: sugestoes.slice(0, 4),
    podeReduzir: podeReduzir.slice(0, 3),
    melhor: sugestoes[0] || null,
  };
}

/* Quantas pessoas para atender a demanda, e como distribuí-las.
   Divisão simples por carga: quem tem mais trabalho leva mais gente. */
function dimensionarLinha(produto, db, opcoes){
  const op = opcoes || {};
  const demanda = num(op.demandaDia);
  const j = jornadaDe(db);
  const minutosDia = num(op.minutosDia) > 0 ? num(op.minutosDia) : j.produtivo;
  const takt = taktTime(minutosDia, demanda);
  if(takt === null) return {erro:'Informe a demanda por dia.'};

  const base = balancearLinha(produto, db, {minutosDia});
  if(base.vazio) return {erro:'Este produto não tem processo produtivo.'};

  const postos = base.postos.map(p=>{
    /* pessoas necessárias para o ciclo caber no takt */
    const necessario = Math.max(1, Math.ceil(p.tempoUnitario / takt));
    return {...p, necessario, diferenca: necessario - p.pessoas};
  });

  const total = postos.reduce((s,p)=>s + p.necessario, 0);
  const alocacao = {};
  postos.forEach(p=>{ alocacao[p.id] = p.necessario; });
  const resultado = balancearLinha(produto, db, {minutosDia, alocacao,
    demandaDia: demanda});

  return {
    takt, demanda, minutosDia,
    postos, total,
    totalAtual: base.totalPessoas,
    diferenca: total - base.totalPessoas,
    resultado,
    /* dá para atender com o time proposto? */
    atende: resultado.saidaDia >= demanda,
    sobra: resultado.saidaDia - demanda,
  };
}

/* ==========================================================
   CUSTO INDIRETO
   Material e mão de obra direta não são o custo da peça: são
   parte dele. Aluguel, energia, manutenção e supervisão
   existem mesmo quando ninguém está costurando, e alguém paga.
   Ignorar isso é formar preço achando que a margem é maior do
   que ela é.
========================================================== */
const TIPOS_CUSTO_FIXO = [
  {id:'instalacao',  nome:'Instalação',   ajuda:'aluguel, IPTU, condomínio'},
  {id:'energia',     nome:'Energia e água', ajuda:'conta de luz, água, gás'},
  {id:'manutencao',  nome:'Manutenção',   ajuda:'mecânico, peças, revisão de máquina'},
  {id:'depreciacao', nome:'Depreciação',  ajuda:'o desgaste das máquinas ao longo do tempo'},
  {id:'indireta',    nome:'Mão de obra indireta',
   ajuda:'supervisão, PCP, limpeza — quem não costura mas é necessário'},
  {id:'administrativo', nome:'Administrativo',
   ajuda:'contabilidade, sistema, telefone, material de escritório'},
  {id:'outros',      nome:'Outros',       ajuda:'o que não coube acima'},
];
function tipoCustoFixo(id){
  return TIPOS_CUSTO_FIXO.find(t=>t.id===id) || TIPOS_CUSTO_FIXO[TIPOS_CUSTO_FIXO.length-1];
}

/* A ocupação real muda tudo. Uma fábrica a 50% da capacidade tem o
   dobro de custo fixo por peça — e é justamente quando ela está vazia
   que o preço parece bom. */
const OCUPACAO_PADRAO = 85;

function salvarCustoFixo(d, dados, usuario){
  const nome = String(dados.nome||'').trim();
  if(!nome) return {erro:'Dê um nome ao custo.'};
  const valor = num(dados.valorMensal);
  if(!(valor > 0)) return {erro:'Informe o valor mensal.'};

  const existente = dados.id ? (d.custosFixos||[]).find(c=>c.id===dados.id) : null;
  if(dados.id && !existente) return {erro:'Custo não encontrado.'};

  const reg = {
    id: existente ? existente.id : uid(),
    nome,
    tipo: dados.tipo || 'outros',
    valorMensal: valor,
    observacao: String(dados.observacao||'').trim(),
    ativo: dados.ativo !== false,
    criadoEm: existente ? existente.criadoEm : agoraISO(),
    alteradoEm: agoraISO(),
  };
  d.custosFixos = existente
    ? (d.custosFixos||[]).map(c=>c.id===reg.id ? reg : c)
    : [...(d.custosFixos||[]), reg];
  registrarLog(d, usuario, existente ? 'custo.editado' : 'custo.criado',
    `${reg.nome} · ${money(valor)}/mês`);
  return {custo: reg};
}

function removerCustoFixo(d, id, usuario){
  const c = (d.custosFixos||[]).find(x=>x.id===id);
  if(!c) return {erro:'Custo não encontrado.'};
  d.custosFixos = (d.custosFixos||[]).filter(x=>x.id!==id);
  registrarLog(d, usuario, 'custo.removido', c.nome);
  return {removido:true};
}

/* Quantos minutos de produção a fábrica tem por mês. É o denominador
   da conta — e é onde mora a armadilha: dividir pela capacidade
   teórica dá um custo por minuto otimista demais. */
function capacidadeProdutivaMes(db){
  const j = jornadaDe(db);
  const p = db.parametrosCustoIndireto || {};
  const dias = num(p.diasUteis) || num((db.parametrosMaoDeObra||{}).diasUteis) ||
    DIAS_UTEIS_MES;
  const ocupacao = num(p.ocupacao) > 0 ? num(p.ocupacao) : OCUPACAO_PADRAO;

  /* só quem produz entra na conta: supervisão e administrativo já são
     custo indireto, contá-los como capacidade seria contar duas vezes */
  const diretos = (db.colaboradores||[]).filter(c=>{
    if(c.status === 'Inativo') return false;
    if(c.produtivo === false) return false;
    return !!c.departamentoId;
  });

  const minutosTeoricos = diretos.length * j.produtivo * dias;
  const minutosReais = minutosTeoricos * (ocupacao/100);
  return {
    pessoas: diretos.length,
    minutosDia: j.produtivo,
    dias, ocupacao,
    minutosTeoricos,
    minutosReais: Math.round(minutosReais),
    horasMes: Number((minutosReais/60).toFixed(1)),
    vazia: diretos.length === 0,
  };
}

/* Quanto custa cada minuto de fábrica, além de quem está costurando. */
function taxaCustoIndireto(db){
  const lista = (db.custosFixos||[]).filter(c=>c.ativo !== false);
  const total = lista.reduce((s,c)=>s + num(c.valorMensal), 0);
  const cap = capacidadeProdutivaMes(db);

  const porTipo = TIPOS_CUSTO_FIXO.map(t=>({
    ...t,
    total: lista.filter(c=>c.tipo===t.id).reduce((s,c)=>s+num(c.valorMensal), 0),
  })).filter(x=>x.total > 0).sort((a,b)=>b.total-a.total);

  const avisos = [];
  if(lista.length === 0)
    avisos.push('Nenhum custo fixo cadastrado. O custo do produto está '+
      'incompleto: aluguel, energia e manutenção existem mesmo sem aparecer '+
      'na conta, e quem forma preço sem eles trabalha com margem menor do '+
      'que imagina.');
  if(cap.vazia)
    avisos.push('Nenhum colaborador de produção cadastrado — sem base para '+
      'dividir o custo fixo.');
  if(cap.ocupacao >= 100)
    avisos.push('Ocupação em 100% supõe que a fábrica nunca para. Na prática '+
      'ninguém opera assim, e o custo por peça sai menor do que o real.');

  return {
    lista, total, porTipo,
    capacidade: cap,
    /* o número que interessa */
    porMinuto: cap.minutosReais > 0
      ? Number((total / cap.minutosReais).toFixed(6)) : 0,
    porHora: cap.minutosReais > 0
      ? Number((total / (cap.minutosReais/60)).toFixed(2)) : 0,
    avisos,
    configurado: lista.length > 0 && !cap.vazia,
  };
}

/* O custo indireto de um produto: o tempo que ele ocupa a fábrica
   vezes o que a fábrica custa por minuto. */
function custoIndiretoProduto(produto, db, lote){
  const taxa = taxaCustoIndireto(db);
  if(!produto) return {taxa, minutosOcupados:0, custo:0, configurado:false};
  const r = resumoProcesso(produto, db, lote);
  /* o tempo que ocupa a fábrica é o de relógio, não o de mão de obra:
     duas pessoas na mesma operação usam uma bancada, não duas */
  const minutos = num(r.minutos);
  return {
    taxa,
    minutosOcupados: minutos,
    custo: Number((minutos * taxa.porMinuto).toFixed(8)),
    configurado: taxa.configurado,
  };
}

/* A conta completa da peça. Antes disso, o sistema mostrava material
   e mão de obra e chamava de "custo" — o que era verdade parcial. */
function custoCompletoProduto(produto, db, lote){
  /* produto que sumiu do cadastro não pode derrubar a tela de custo */
  if(!produto) return {material:0, maoDeObra:0, indireto:0, total:0,
    fatias:[], completo:false, avisos:['Produto não encontrado.'],
    detalheMaterial:null, detalheMaoDeObra:null, detalheIndireto:null};
  const mat = custearProduto(produto, db);
  const mo = custoMaoDeObra(produto, db, lote);
  const ind = custoIndiretoProduto(produto, db, lote);

  const material = num(mat.custoTecido);
  const maoDeObra = num(mo.total);
  const indireto = num(ind.custo);
  const total = material + maoDeObra + indireto;

  const avisos = [...(mat.avisos||[]), ...(mo.avisos||[]), ...(ind.taxa.avisos||[])];

  return {
    material, maoDeObra, indireto,
    total: Number(total.toFixed(8)),
    detalheMaterial: mat,
    detalheMaoDeObra: mo,
    detalheIndireto: ind,
    /* a participação de cada parte ajuda a saber onde mexer */
    fatias: total > 0 ? [
      {nome:'Material',      valor:material,  perc: Number((material/total*100).toFixed(1))},
      {nome:'Mão de obra',   valor:maoDeObra, perc: Number((maoDeObra/total*100).toFixed(1))},
      {nome:'Custo da fábrica', valor:indireto, perc: Number((indireto/total*100).toFixed(1))},
    ] : [],
    completo: ind.configurado,
    avisos,
  };
}

/* Formação de preço: a margem sobre o custo completo, e o que ela
   seria se o indireto tivesse sido esquecido. */
function precoSugerido(produto, db, opcoes){
  const op = opcoes || {};
  const c = custoCompletoProduto(produto, db, op.lote);
  if(!produto) return {erro:'Produto não encontrado.', custo:c};
  const margem = num(op.margem);
  const impostos = num(op.impostos);
  const comissao = num(op.comissao);

  /* margem sobre o preço de venda, não sobre o custo: é assim que se
     forma preço, e confundir os dois é o erro mais comum */
  const carga = (margem + impostos + comissao) / 100;
  if(carga >= 1)
    return {erro:'Margem, impostos e comissão somam 100% ou mais do preço — '+
      'não sobra nada para pagar o custo.', custo:c};

  const preco = c.total / (1 - carga);
  /* o preço que sairia sem contar o custo da fábrica */
  const precoSemIndireto = (c.material + c.maoDeObra) / (1 - carga);
  const margemReal = preco > 0
    ? Number(((preco - c.total) / preco * 100).toFixed(2)) : 0;
  /* quem esquece o indireto vende por precoSemIndireto e acha que tem
     a margem cheia; a real é esta */
  const margemIludida = precoSemIndireto > 0
    ? Number(((precoSemIndireto - c.total) / precoSemIndireto * 100).toFixed(2)) : 0;

  return {
    custo: c,
    margem, impostos, comissao,
    preco: Number(preco.toFixed(4)),
    precoSemIndireto: Number(precoSemIndireto.toFixed(4)),
    margemReal,
    margemIludida,
    /* vender sem contar o indireto dá prejuízo? */
    prejuizo: margemIludida < 0,
    diferenca: Number((preco - precoSemIndireto).toFixed(4)),
  };
}

function custoMaoDeObra(produto, db, lote) {
  const r = resumoProcesso(produto, db, lote);
  const cache = {};
  const custoDe = id => {
    if (!cache[id]) cache[id] = custoMinutoDepartamento(id, db);
    return cache[id];
  };

  /* setor sem ninguém cadastrado usa a média da fábrica, sinalizada */
  const geral = mediaGeralFabrica(db);
  const passos = r.passos.map(p => {
    const c = p.departamento ? custoDe(p.departamento.id) : null;
    const proprio = c && !c.semSalario;
    const minuto = proprio ? c.custoMinuto : geral.vazia ? 0 : geral.custoMinuto;
    const custo = p.minutosHomem * minuto;
    return {
      ...p,
      custoDep: c,
      custoMinuto: minuto,
      estimado: !proprio && !geral.vazia,
      custo: Number(custo.toFixed(8))
    };
  });
  const mapa = {};
  passos.forEach(p => {
    const dep = p.departamento;
    const nome = dep ? dep.nome : 'Sem departamento';
    if (!mapa[nome]) mapa[nome] = {
      nome,
      departamento: dep,
      etapas: [],
      minutos: 0,
      minutosHomem: 0,
      custo: 0,
      custoDep: p.custoDep,
      estimado: p.estimado,
      custoMinuto: p.custoMinuto
    };
    mapa[nome].etapas.push(p);
    mapa[nome].minutos += p.tempo;
    mapa[nome].minutosHomem += p.minutosHomem;
    mapa[nome].custo += p.custo;
  });
  const total = passos.reduce((a, p) => a + p.custo, 0);
  const avisos = [];
  const estimados = [];
  Object.values(mapa).forEach(d => {
    if (!d.departamento) return;
    if (d.custoDep && d.custoDep.semSalario) {
      estimados.push(d.nome);
      avisos.push(geral.vazia ? `${d.nome}: nenhum colaborador com salário — sem base para calcular.` : `${d.nome}: sem colaborador próprio — o custo usa a média da fábrica. ` + `Cadastre a equipe do setor para o valor ficar exato.`);
    } else if (d.custoDep && d.custoDep.incompleto) avisos.push(`${d.nome}: ${d.custoDep.pessoas - d.custoDep.comSalario} ` + `colaborador(es) sem salário — a média usa apenas os ${d.custoDep.comSalario} informados.`);
  });
  if (geral.vazia) avisos.push('Nenhum colaborador com salário no sistema. ' + 'Cadastre em Cadastros → Colaboradores, com salário e departamento.');
  return {
    passos,
    lote: r.lote,
    geral,
    estimados,
    temEstimativa: estimados.length > 0,
    porDepartamento: Object.values(mapa).map(d => ({
      ...d,
      minutos: Number(d.minutos.toFixed(4)),
      minutosHomem: Number(d.minutosHomem.toFixed(4)),
      custo: Number(d.custo.toFixed(5)),
      percentual: total > 0 ? Number((d.custo / total * 100).toFixed(1)) : 0
    })).sort((a, b) => b.custo - a.custo),
    minutos: r.minutos,
    minutosHomem: r.minutosHomem,
    total: Number(total.toFixed(8)),
    avisos,
    semProcesso: r.passos.length === 0
  };
}

/* ==========================================================
   ORDEM DE PRODUÇÃO
   A ordem é a unidade do sistema: nasce direta, com o produto,
   a quantidade e o cliente. Carrega as etapas do processo.
========================================================== */
const SITUACOES_OP = [{
  id: 'aberta',
  nome: 'Aberta',
  tone: 'idle'
}, {
  id: 'liberada',
  nome: 'Liberada',
  tone: 'info'
}, {
  id: 'producao',
  nome: 'Em produção',
  tone: 'warn'
}, {
  id: 'concluida',
  nome: 'Concluída',
  tone: 'ok'
}, {
  id: 'cancelada',
  nome: 'Cancelada',
  tone: 'bad'
}];
function situacaoOP(id) {
  return SITUACOES_OP.find(x => x.id === id) || SITUACOES_OP[0];
}

/* As tarefas saem do processo do produto, na quantidade da ordem.
   Se já houver tarefas, preserva o que a fábrica decidiu. */
function tarefasDoProduto(produto, db, quantidade, antigas) {
  const r = resumoProcesso(produto, db, quantidade);
  const anteriores = antigas || [];
  return r.passos.map((p, i) => {
    const velha = anteriores.find(t => t.etapaId === p.passo.etapaId) || {};
    return {
      id: velha.id || uid(),
      ordem: i + 1,
      etapaId: p.passo.etapaId,
      departamentoId: p.passo.departamentoId,
      equipamentoId: velha.equipamentoId || p.passo.equipamentoId || '',
      colaboradorIds: velha.colaboradorIds || (velha.colaboradorId ? [velha.colaboradorId] : []),
      modo: p.modo,
      tempo: p.tempo,
      pessoas: p.pessoas,
      /* o que a etapa consome e processa vai junto para a ordem */
      materiais: velha.materiais || p.passo.materiais || [],
      subprodutos: velha.subprodutos || p.passo.subprodutos || [],
      anexos: p.passo.anexos || [],
      riscosProducao: p.passo.riscosProducao || [],
      minutosPorPeca: p.minutosHomem,
      minutosTotais: Number((p.minutosHomem * num(quantidade)).toFixed(3)),
      concluida: !!velha.concluida
    };
  });
}
function criarOrdem(d, dados, usuario) {
  const produto = (d.produtos || []).find(p => p.id === dados.produtoId);
  if (!produto) return {
    erro: 'Escolha um produto.'
  };
  const qtd = num(dados.quantidade);
  if (!(qtd > 0)) return {
    erro: 'Informe a quantidade a produzir.'
  };
  if ((produto.processo || []).length === 0) return {
    erro: 'Este produto ainda não tem processo produtivo. ' + 'Cadastre as etapas na aba Produtos antes de abrir a ordem.'
  };

  /* Produto que ainda não fechou a engenharia não vai para a linha —
     mas amostra pode, porque é justamente amostra que fecha a engenharia. */
  const st = statusProduto(statusDoProduto(produto));
  const amostra = !!dados.amostra;
  if (amostra && !st.amostra) return {
    erro: `Produto ${st.nome.toLowerCase()}: não é possível produzir, ` + 'nem como amostra.'
  };
  if (!amostra && !st.produz) {
    const ck = checklistEngenharia(produto, d);
    const falta = ck.pendentesObrigatorios.map(i => i.nome.toLowerCase()).join(', ');
    return {
      erro: `Produto ${st.nome.toLowerCase()} — só produtos liberados abrem ` + `ordem normal.${falta ? ' Falta: ' + falta + '.' : ''} Se for para aprovar ` + 'modelo, marque a ordem como amostra.'
    };
  }

  /* a ordem nasce amarrada à versão da engenharia que existia agora */
  const versao = versaoVigente(d, produto.id);
  const reg = {
    id: uid(),
    codigo: proximoCodigo(d.ordens, 'OP'),
    produtoId: dados.produtoId,
    /* qual engenharia gerou esta ordem — mudar o produto depois não
       reescreve o que foi combinado aqui */
    versaoId: versao ? versao.id : '',
    versaoCodigo: versao ? versao.codigo : '',
    amostra,
    quantidade: qtd,
    clienteId: dados.clienteId || '',
    entrega: dados.entrega || '',
    prioridade: num(dados.prioridade) || 5,
    situacao: 'aberta',
    observacao: dados.observacao || '',
    anexos: dados.anexos || [],
    tarefas: tarefasDoProduto(produto, d, qtd),
    criadoEm: agoraISO(),
    criadoPor: usuario ? usuario.nome : ''
  };
  d.ordens = [...(d.ordens || []), reg];
  registrarLog(d, usuario, 'Abriu ordem de produção', `${reg.codigo} · ${montarNomeProduto(produto, d) || produto.codigo} · ${qtd} peça(s)` + `${amostra ? ' · amostra' : ''}${reg.versaoCodigo ? ' · ' + reg.versaoCodigo : ''}`);
  return {
    ordem: reg
  };
}

/* A OP é uma fotografia das etapas no momento em que nasceu. Se a
   engenharia do produto mudar depois, ela fica velha e ninguém
   percebe — a não ser que o sistema conte. */
function ordemDesatualizada(ordem, db) {
  const produto = (db.produtos || []).find(p => p.id === ordem.produtoId);
  const motivos = [];
  if (!produto) {
    motivos.push('o produto foi removido do cadastro');
    return {
      desatualizada: true,
      motivos,
      produto: null
    };
  }
  const atuais = (produto.processo || []).length;
  const naOrdem = (ordem.tarefas || []).length;
  if (atuais !== naOrdem) motivos.push(`o produto tem ${atuais} etapa(s) e a ordem, ${naOrdem}`);else {
    const r = resumoProcesso(produto, db, ordem.quantidade);
    const mudou = r.passos.some(p => {
      const t = (ordem.tarefas || []).find(x => x.etapaId === p.passo.etapaId);
      return t && Math.abs(num(t.minutosPorPeca) - p.minutosHomem) > 0.0001;
    });
    if (mudou) motivos.push('os tempos das etapas mudaram no produto');
  }

  /* os minutos precisam bater com a quantidade da ordem */
  const desalinhada = (ordem.tarefas || []).some(t => Math.abs(num(t.minutosPorPeca) * num(ordem.quantidade) - num(t.minutosTotais)) > 0.01);
  if (desalinhada) motivos.push('os minutos não batem com a quantidade da ordem');
  return {
    desatualizada: motivos.length > 0,
    motivos,
    produto
  };
}

/* Refaz as etapas pelo que o produto diz hoje, preservando quem
   foi designado e o que já está feito. */
function ressincronizarOrdem(d, ordemId, usuario) {
  const ordem = (d.ordens || []).find(o => o.id === ordemId);
  if (!ordem) return {
    erro: 'Ordem não encontrada.'
  };
  const produto = (d.produtos || []).find(p => p.id === ordem.produtoId);
  if (!produto) return {
    erro: 'O produto desta ordem não existe mais.'
  };
  const info = ordemDesatualizada(ordem, d);
  const tarefas = tarefasDoProduto(produto, d, ordem.quantidade, ordem.tarefas);
  d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
    ...o,
    tarefas,
    sincronizadoEm: agoraISO()
  });
  registrarLog(d, usuario, 'Atualizou a ordem', `${ordem.codigo} · ${info.motivos.join('; ')}`);
  return {
    ok: true,
    motivos: info.motivos,
    tarefas: tarefas.length
  };
}

/* Mudar a quantidade recalcula os minutos de todas as etapas */
function alterarQuantidadeDaOrdem(d, ordemId, quantidade, usuario) {
  const ordem = (d.ordens || []).find(o => o.id === ordemId);
  if (!ordem) return {
    erro: 'Ordem não encontrada.'
  };
  const qtd = num(quantidade);
  if (!(qtd > 0)) return {
    erro: 'A quantidade precisa ser maior que zero.'
  };
  const produto = (d.produtos || []).find(p => p.id === ordem.produtoId);
  if (!produto) return {
    erro: 'O produto desta ordem não existe mais.'
  };
  if (ordem.materialReservado) return {
    erro: 'O material já foi reservado nesta quantidade. ' + 'Cancele a reserva antes de mudar.'
  };
  const anterior = num(ordem.quantidade);
  d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
    ...o,
    quantidade: qtd,
    tarefas: tarefasDoProduto(produto, d, qtd, o.tarefas)
  });
  registrarLog(d, usuario, 'Alterou a quantidade da ordem', `${ordem.codigo} · ${anterior} → ${qtd}`);
  return {
    ok: true,
    quantidade: qtd
  };
}

/* ==========================================================
   MATERIAL PARA A ORDEM
   Programar produção sem tecido é planejar parada.
========================================================== */
function materiaisDaOrdem(ordem, db) {
  const produto = (db.produtos || []).find(p => p.id === ordem.produtoId);
  if (!produto) return {
    linhas: [],
    falta: false,
    semProduto: true
  };
  const c = custearProduto(produto, db);
  const qtd = num(ordem.quantidade);

  /* uma linha por material, não por uso: o mesmo tecido usado duas vezes
     no produto se compra e se reserva uma vez só, somando o consumo */
  const linhas = (c.porMaterial || []).filter(g => g.material).map(g => {
    const necessario = Number((num(g.consumo) * qtd).toFixed(4));
    const s = saldoTotal(g.material.id, db);
    const livre = num(s.fisico) - num(s.reservado);
    return {
      material: g.material,
      unidade: g.material.unidadeEstoque,
      porPeca: num(g.consumo),
      usos: g.usos.length,
      necessario,
      fisico: num(s.fisico),
      reservado: num(s.reservado),
      livre: Number(livre.toFixed(4)),
      falta: Number(Math.max(necessario - livre, 0).toFixed(4)),
      suficiente: livre >= necessario - 0.0001,
      semConsumo: !(num(g.consumo) > 0)
    };
  });
  const faltantes = linhas.filter(l => !l.suficiente && !l.semConsumo);
  /* consumo zero quase sempre quer dizer tecido sem risco importado:
     dizer "disponível" nesse caso seria mentira por omissão */
  const indefinidos = linhas.filter(l => l.semConsumo);
  let resumo;
  if (faltantes.length) resumo = `Falta material para ${faltantes.length} item(ns): ` + faltantes.map(l => `${l.material.nome} (${l.falta.toFixed(3)} ${l.unidade})`).join(', ');else if (indefinidos.length) resumo = `${indefinidos.length} material(is) sem consumo definido: ` + indefinidos.map(l => l.material.nome).join(', ') + '. Importe a modelagem ou informe a quantidade por peça.';else resumo = 'Material disponível para toda a ordem.';
  return {
    linhas,
    faltantes,
    falta: faltantes.length > 0,
    indefinido: indefinidos.length > 0,
    semConsumo: indefinidos,
    resumo
  };
}

/* Reserva o que a ordem precisa, para outro pedido não levar */
function reservarMateriaisDaOrdem(d, ordemId, usuario) {
  const ordem = (d.ordens || []).find(o => o.id === ordemId);
  if (!ordem) return {
    erro: 'Ordem não encontrada.'
  };
  if (ordem.materialReservado) return {
    erro: 'O material desta ordem já foi reservado.'
  };
  const info = materiaisDaOrdem(ordem, d);
  if (info.linhas.length === 0) return {
    erro: 'A ordem não tem material definido.'
  };
  if (info.falta) return {
    erro: info.resumo
  };
  const estoque = (d.estoques || []).find(e => e.padrao) || (d.estoques || [])[0];
  if (!estoque) return {
    erro: 'Nenhum estoque cadastrado.'
  };
  const feitas = [];
  for (const l of info.linhas) {
    if (l.semConsumo) continue;
    const r = reservar(d, {
      materialId: l.material.id,
      estoqueId: estoque.id,
      quantidade: l.necessario,
      motivo: `${ordem.codigo}`
    }, usuario);
    if (r.erro) return {
      erro: `${l.material.nome}: ${r.erro}`
    };
    feitas.push({
      material: l.material.nome,
      quantidade: l.necessario
    });
  }
  d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
    ...o,
    materialReservado: true,
    reservadoEm: agoraISO()
  });
  registrarLog(d, usuario, 'Reservou material da ordem', `${ordem.codigo} · ${feitas.length} material(is)`);
  return {
    ok: true,
    reservas: feitas
  };
}
function minutosDaOrdem(ordem) {
  return (ordem.tarefas || []).reduce((a, t) => a + num(t.minutosTotais), 0);
}

/* ==========================================================
   CRONOGRAMA
   O trabalho não cabe num dia só: aqui ele se espalha pelos
   dias seguintes, respeitando a jornada.
========================================================== */
function ehDiaUtil(data, jornada) {
  return (jornada.diasSemana || []).includes(data.getDay());
}
function ehSabado(data) {
  return data.getDay() === 6;
}
function montarCronograma(produto, db, opcoes) {
  const op = opcoes || {};
  const quantidade = num(op.quantidade);
  /* o cronograma dilui o projeto pela quantidade que vai produzir */
  const r = resumoProcesso(produto, db, quantidade);
  const j = jornadaDe(db);
  if (!(quantidade > 0)) return {
    vazio: true,
    motivo: 'Informe a quantidade do lote.',
    jornada: j,
    resumo: r
  };
  if (r.passos.length === 0) return {
    vazio: true,
    motivo: 'Cadastre as etapas do processo antes.',
    jornada: j,
    resumo: r
  };
  if (!(r.minutosHomem > 0)) return {
    vazio: true,
    motivo: 'Nenhuma etapa tem tempo informado.',
    jornada: j,
    resumo: r
  };
  const totalMinutos = r.minutosHomem * quantidade;
  const capacidadeNormal = j.produtivo;
  const capacidadeComExtra = op.horaExtra ? j.comExtras : j.produtivo;

  /* fila de trabalho: cada etapa com seus minutos totais */
  const fila = r.passos.filter(p => p.tempo > 0).map(p => ({
    passo: p,
    restante: Number((p.minutosHomem * quantidade).toFixed(4))
  }));
  const dias = [];
  let data = op.inicio ? new Date(op.inicio + 'T00:00:00') : new Date();
  data.setHours(0, 0, 0, 0);
  let i = 0,
    guarda = 0;
  while (fila.some(f => f.restante > 0.0001) && guarda < 400) {
    guarda++;
    const util = ehDiaUtil(data, j);
    const sabado = op.sabado && ehSabado(data);
    if (!util && !sabado) {
      data = new Date(data.getTime() + 86400000);
      continue;
    }
    let capacidade = sabado ? j.minutosSabado : capacidadeComExtra;
    const normal = sabado ? j.minutosSabado : capacidadeNormal;
    const doDia = [];
    let usado = 0;
    while (capacidade > 0.0001 && i < fila.length) {
      const f = fila[i];
      if (f.restante <= 0.0001) {
        i++;
        continue;
      }
      const aloca = Math.min(f.restante, capacidade);
      doDia.push({
        etapa: f.passo.etapa ? f.passo.etapa.nome : 'etapa removida',
        departamento: f.passo.departamento ? f.passo.departamento.nome : '—',
        equipamento: f.passo.equipamento ? f.passo.equipamento.nome : '',
        minutos: Number(aloca.toFixed(2)),
        pecas: f.passo.minutosHomem > 0 ? Math.floor(aloca / f.passo.minutosHomem) : 0,
        parcial: aloca < f.restante - 0.0001
      });
      f.restante = Number((f.restante - aloca).toFixed(4));
      capacidade -= aloca;
      usado += aloca;
      if (f.restante <= 0.0001) i++;
    }
    if (doDia.length > 0) {
      dias.push({
        data: new Date(data),
        iso: data.toISOString().slice(0, 10),
        sabado,
        util,
        minutos: Number(usado.toFixed(2)),
        minutosNormais: Number(Math.min(usado, normal).toFixed(2)),
        minutosExtras: Number(Math.max(usado - normal, 0).toFixed(2)),
        capacidade: sabado ? j.minutosSabado : op.horaExtra ? j.comExtras : j.produtivo,
        etapas: doDia
      });
    }
    data = new Date(data.getTime() + 86400000);
  }
  const totalExtras = dias.reduce((a, d) => a + d.minutosExtras, 0);
  const diasSabado = dias.filter(d => d.sabado);
  const semExtra = Math.ceil(totalMinutos / Math.max(capacidadeNormal, 1));
  return {
    vazio: false,
    jornada: j,
    resumo: r,
    quantidade,
    totalMinutos: Number(totalMinutos.toFixed(2)),
    totalHoras: Number((totalMinutos / 60).toFixed(2)),
    dias,
    diasNecessarios: dias.length,
    diasSemHoraExtra: semExtra,
    primeiroDia: dias.length ? dias[0].iso : null,
    ultimoDia: dias.length ? dias[dias.length - 1].iso : null,
    minutosExtras: Number(totalExtras.toFixed(2)),
    usaExtras: totalExtras > 0,
    diasSabado: diasSabado.length,
    /* passa de um dia? então o cronograma vale a pena */
    passaDoDia: totalMinutos > capacidadeNormal,
    incompleto: guarda >= 400
  };
}

/* ==========================================================
   CRONOGRAMA DA FÁBRICA
   Todas as ordens disputando as mesmas pessoas e máquinas.
   Cada recurso tem a sua carga diária; ninguém trabalha em dois
   lugares ao mesmo tempo.
========================================================== */
/* Quem foi designado: aceita um, vários, ou ninguém (aí é o setor todo) */
function designadosDaTarefa(tarefa) {
  if (Array.isArray(tarefa.colaboradorIds) && tarefa.colaboradorIds.length) return tarefa.colaboradorIds;
  return tarefa.colaboradorId ? [tarefa.colaboradorId] : [];
}

/* A agenda de um colaborador: quanto já está comprometido e o que
   sobra. Sem isso, designar alguém é apostar. */
function cargaDoColaborador(colaboradorId, db, opcoes) {
  const op = opcoes || {};
  const j = jornadaDe(db);
  const prog = agendarFabrica(db, {
    inicio: op.inicio || todayISO(),
    horaExtra: op.horaExtra,
    sabado: op.sabado
  });
  if (prog.vazio) return {
    ocupado: 0,
    livre: j.produtivo,
    dias: [],
    porDia: {},
    capacidadeDia: j.produtivo,
    semProgramacao: true
  };
  const dias = prog.diasCompletos || prog.dias || [];
  const porDia = {};
  dias.forEach(d => {
    const meu = (d.porPessoa || []).find(p => p.id === colaboradorId);
    if (!meu) return;
    porDia[d.iso] = {
      iso: d.iso,
      ocupado: meu.usado,
      capacidade: meu.capacidade,
      livre: Number((meu.capacidade - meu.usado).toFixed(2)),
      entrada: meu.entrada,
      saida: meu.saida,
      ocupacao: meu.ocupacao,
      /* onde ele está, para quem precisa remanejar */
      tarefas: (d.tarefas || []).filter(t => (t.pessoas || []).some(q => q.id === colaboradorId)).map(t => {
        const meuBloco = (t.pessoas || []).find(q => q.id === colaboradorId) || {};
        return {
          ordem: t.ordem,
          etapa: t.etapa,
          departamento: t.departamento,
          inicio: meuBloco.inicio,
          fim: meuBloco.fim,
          minutos: meuBloco.minutos
        };
      })
    };
  });
  const lista = Object.values(porDia).sort((a, b) => a.iso.localeCompare(b.iso));
  const primeiro = lista[0] || null;
  return {
    porDia,
    dias: lista,
    /* o retrato do primeiro dia programado: é onde a decisão pesa */
    ocupado: primeiro ? primeiro.ocupado : 0,
    livre: primeiro ? primeiro.livre : j.produtivo,
    capacidadeDia: primeiro ? primeiro.capacidade : j.produtivo,
    entrada: primeiro ? primeiro.entrada : null,
    saida: primeiro ? primeiro.saida : null,
    diasOcupados: lista.length,
    totalOcupado: Number(lista.reduce((a, x) => a + x.ocupado, 0).toFixed(2)),
    semProgramacao: lista.length === 0
  };
}

function apontamentosDoDia(data, db) {
  return (db.apontamentos || []).filter(a => a.data === data);
}
function apontamentosDoColaborador(colaboradorId, db, de, ate) {
  return (db.apontamentos || []).filter(a => {
    if (a.colaboradorId !== colaboradorId) return false;
    if (de && a.data < de) return false;
    if (ate && a.data > ate) return false;
    return true;
  });
}

/* O retrato de um colaborador no período: o que produziu, o que
   perdeu e o quanto ficou perto do programado. */
function desempenhoColaborador(colaboradorId, db, periodo) {
  const p = periodo || {};
  const lancamentos = apontamentosDoColaborador(colaboradorId, db, p.de, p.ate);
  const colab = (db.colaboradores || []).find(c => c.id === colaboradorId);
  const previsto = lancamentos.reduce((a, x) => a + num(x.minutosPrevistos), 0);
  const realizado = lancamentos.reduce((a, x) => a + num(x.minutosGastos), 0);
  const boas = lancamentos.reduce((a, x) => a + num(x.pecasBoas), 0);
  const defeitos = lancamentos.reduce((a, x) => a + num(x.pecasDefeito), 0);
  const total = boas + defeitos;

  /* dias em que a produção atrasou, com o motivo */
  const atrasos = lancamentos.filter(x => num(x.minutosGastos) > num(x.minutosPrevistos) * 1.05 || String(x.motivoAtraso || '').trim());
  const porMotivo = {};
  lancamentos.forEach(x => {
    const m = String(x.motivoAtraso || '').trim();
    if (!m) return;
    porMotivo[m] = (porMotivo[m] || 0) + 1;
  });
  const porDefeito = {};
  lancamentos.forEach(x => {
    (x.defeitos || []).forEach(d => {
      const tipo = d.tipo || 'Outro';
      porDefeito[tipo] = (porDefeito[tipo] || 0) + num(d.quantidade);
    });
  });
  return {
    colaborador: colab,
    lancamentos: lancamentos.sort((a, b) => String(b.data).localeCompare(String(a.data))),
    dias: new Set(lancamentos.map(x => x.data)).size,
    previsto: Number(previsto.toFixed(1)),
    realizado: Number(realizado.toFixed(1)),
    /* acima de 100% é mais rápido que o previsto */
    eficiencia: realizado > 0 ? Number((previsto / realizado * 100).toFixed(1)) : 0,
    boas,
    defeitos,
    total,
    qualidade: total > 0 ? Number((boas / total * 100).toFixed(2)) : null,
    refugo: total > 0 ? Number((defeitos / total * 100).toFixed(2)) : 0,
    atrasos: atrasos.length,
    porMotivo: Object.entries(porMotivo).map(([nome, n]) => ({
      nome,
      n
    })).sort((a, b) => b.n - a.n),
    porDefeito: Object.entries(porDefeito).map(([nome, n]) => ({
      nome,
      n
    })).sort((a, b) => b.n - a.n),
    semDados: lancamentos.length === 0
  };
}

/* A mesma leitura para a fábrica inteira */
function desempenhoGeral(db, periodo) {
  const equipe = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
  const linhas = equipe.map(c => desempenhoColaborador(c.id, db, periodo)).filter(x => !x.semDados);
  const boas = linhas.reduce((a, x) => a + x.boas, 0);
  const defeitos = linhas.reduce((a, x) => a + x.defeitos, 0);
  const total = boas + defeitos;
  return {
    linhas: linhas.sort((a, b) => b.boas - a.boas),
    pessoas: linhas.length,
    boas,
    defeitos,
    total,
    qualidade: total > 0 ? Number((boas / total * 100).toFixed(2)) : null,
    previsto: Number(linhas.reduce((a, x) => a + x.previsto, 0).toFixed(1)),
    realizado: Number(linhas.reduce((a, x) => a + x.realizado, 0).toFixed(1)),
    eficiencia: (() => {
      const p = linhas.reduce((a, x) => a + x.previsto, 0);
      const r = linhas.reduce((a, x) => a + x.realizado, 0);
      return r > 0 ? Number((p / r * 100).toFixed(1)) : 0;
    })(),
    atrasos: linhas.reduce((a, x) => a + x.atrasos, 0)
  };
}

/* ==========================================================
   O RELÓGIO DO DIA
   A jornada não é um bloco de 513 minutos: é 06:00 às 09:00,
   café, 09:15 às 12:00, almoço, e 13:00 até o fim. O trabalho
   entra nessas janelas, na ordem em que chega.
========================================================== */
/* O relógio da fábrica trabalha em minutos decimais, mas quem lê
   precisa de hora:minuto:segundo — muita etapa dura segundos. */
function minutosParaHora(m) {
  const total = Math.round(num(m) * 60); // em segundos
  const h = Math.floor(total / 3600);
  const min = Math.floor(total % 3600 / 60);
  const seg = total % 60;
  return String(h).padStart(2, '0') + ':' + String(min).padStart(2, '0') + ':' + String(seg).padStart(2, '0');
}
function horaParaMinutos(h) {
  const [a, b, c] = String(h || '0:0:0').split(':');
  return num(a) * 60 + num(b) + num(c) / 60;
}

/* Uma duração: 0,75 min vira 45s; 92 min viram 1h32m */
function duracao(min) {
  const total = Math.round(num(min) * 60);
  if (total < 60) return total + 's';
  const h = Math.floor(total / 3600);
  const m = Math.floor(total % 3600 / 60);
  const s = total % 60;
  if (h > 0) return h + 'h' + String(m).padStart(2, '0') + (s ? ' ' + s + 's' : '');
  return m + 'min' + (s ? ' ' + String(s).padStart(2, '0') + 's' : '');
}

/* O campo digitado aceita 45s, 1:30, 2min ou 0,75 — tudo vira minutos.
   Na bancada se cronometra em segundos, então nos campos de tempo de
   etapa um número solto é lido como SEGUNDOS: quem digita 27 quer 27s. */
function textoParaMinutos(txt, opcoes) {
  const op = opcoes || {};
  const t = String(txt || '').trim().toLowerCase().replace(',', '.');
  if (!t) return 0;
  if (t.includes(':')) {
    const p = t.split(':').map(x => num(x));
    if (p.length === 3) return p[0] * 60 + p[1] + p[2] / 60;
    if (p.length === 2) return p[0] + p[1] / 60; // mm:ss, o mais comum na bancada
  }
  const seg = t.match(/^([\d.]+)\s*s(eg)?$/);
  if (seg) return num(seg[1]) / 60;
  const min = t.match(/^([\d.]+)\s*m(in)?$/);
  if (min) return num(min[1]);
  const hora = t.match(/^([\d.]+)\s*h$/);
  if (hora) return num(hora[1]) * 60;
  /* número solto: segundos onde o cronômetro manda, minutos no resto */
  return op.padraoSegundos ? num(t) / 60 : num(t);
}

/* ==========================================================
   UNIDADE DE TEMPO
   Cronometrar em segundos e planejar em minutos convivem mal:
   "15" pode ser um quarto de minuto ou um quarto de hora. O
   número anda junto da unidade escolhida.
========================================================== */
const UNIDADES_TEMPO = [{
  id: 's',
  nome: 'seg',
  curto: 's',
  porMinuto: 60
}, {
  id: 'min',
  nome: 'min',
  curto: 'min',
  porMinuto: 1
}, {
  id: 'h',
  nome: 'hora',
  curto: 'h',
  porMinuto: 1 / 60
}];
function unidadeTempo(id) {
  return UNIDADES_TEMPO.find(u => u.id === id) || UNIDADES_TEMPO[0];
}

/* De minutos para o número que se mostra na unidade escolhida */
function tempoNaUnidade(minutos, unidadeId) {
  const u = unidadeTempo(unidadeId);
  const v = num(minutos) * u.porMinuto;
  if (v === 0) return '';
  /* casas só quando fazem falta: 1,5 min sim; 90,0000 s não */
  const arred = Math.round(v * 1000) / 1000;
  return String(arred);
}
/* E de volta: o que foi digitado naquela unidade vale quantos minutos */
function tempoParaMinutos(valor, unidadeId) {
  const u = unidadeTempo(unidadeId);
  return num(String(valor).replace(',', '.')) / u.porMinuto;
}

/* A unidade que menos incomoda para um tempo já existente */
function unidadeSugerida(minutos) {
  const m = num(minutos);
  if (m <= 0) return 's';
  if (m < 1) return 's';
  if (m >= 60) return 'h';
  return 'min';
}

/* Quantos segundos, para quem precisa do número cru */
function emSegundos(min) {
  return Math.round(num(min) * 60);
}

/* O número em segundos só ajuda quando a duração já não é em segundos:
   mostrar "27s" embaixo de "27s" é ruído. */
function segundosSeAjudar(min) {
  const d = duracao(min);
  const seg = emSegundos(min) + 's';
  return d === seg ? null : seg;
}

/* As janelas em que se produz, já descontados os intervalos */
function janelasDoDia(jornada, opcoes) {
  const op = opcoes || {};
  const ini = horaParaMinutos(op.sabado && jornada.sabado ? jornada.sabado.inicio : jornada.inicio);
  const fim = horaParaMinutos(op.sabado && jornada.sabado ? jornada.sabado.fim : op.horaExtra && jornada.extensao ? jornada.extensao.fim : jornada.fim);

  /* os intervalos que caem dentro do expediente */
  const pausas = (jornada.intervalos || []).map(i => ({
    nome: i.nome,
    ini: horaParaMinutos(i.inicio),
    fim: horaParaMinutos(i.fim)
  })).filter(p => p.fim > ini && p.ini < fim).sort((a, b) => a.ini - b.ini);
  const janelas = [];
  let cursor = ini;
  pausas.forEach(p => {
    if (p.ini > cursor) janelas.push({
      ini: cursor,
      fim: Math.min(p.ini, fim)
    });
    cursor = Math.max(cursor, p.fim);
  });
  if (cursor < fim) janelas.push({
    ini: cursor,
    fim
  });
  return janelas.filter(x => x.fim > x.ini);
}

/* A agenda de uma pessoa no dia: onde ela está livre */
function novaAgenda(jornada, opcoes) {
  return {
    janelas: janelasDoDia(jornada, opcoes),
    indice: 0,
    cursor: null,
    capacidade: janelasDoDia(jornada, opcoes).reduce((a, j) => a + (j.fim - j.ini), 0)
  };
}
function saldoDaAgenda(ag) {
  let total = 0;
  ag.janelas.forEach((j, i) => {
    if (i < ag.indice) return;
    const de = i === ag.indice && ag.cursor !== null ? ag.cursor : j.ini;
    total += Math.max(j.fim - de, 0);
  });
  return total;
}
/* Ocupa minutos na agenda e devolve os horários em que aconteceu */
function ocuparAgenda(ag, minutos) {
  let falta = minutos;
  const blocos = [];
  while (falta > 0.0001 && ag.indice < ag.janelas.length) {
    const j = ag.janelas[ag.indice];
    const de = ag.cursor !== null ? ag.cursor : j.ini;
    const disponivel = j.fim - de;
    if (disponivel <= 0.0001) {
      ag.indice++;
      ag.cursor = null;
      continue;
    }
    const usa = Math.min(falta, disponivel);
    blocos.push({
      ini: de,
      fim: de + usa
    });
    ag.cursor = de + usa;
    falta -= usa;
    if (ag.cursor >= j.fim - 0.0001) {
      ag.indice++;
      ag.cursor = null;
    }
  }
  return {
    blocos,
    alocado: minutos - falta,
    inicio: blocos.length ? minutosParaHora(blocos[0].ini) : null,
    fim: blocos.length ? minutosParaHora(blocos[blocos.length - 1].fim) : null,
    atravessouIntervalo: blocos.length > 1
  };
}
function recursosDaTarefa(tarefa, db) {
  const ids = designadosDaTarefa(tarefa);
  /* quem executa: os designados, ou toda a equipe do setor */
  const designado = ids.length ? (db.colaboradores || []).filter(c => ids.includes(c.id) && c.status !== 'Inativo') : colaboradoresDoDepartamento(tarefa.departamentoId, db);
  const equipamento = tarefa.equipamentoId ? (db.equipamentos || []).find(e => e.id === tarefa.equipamentoId && e.ativo !== false) : null;
  return {
    pessoas: designado,
    equipamento,
    /* uma máquina parada não produz */
    equipamentoParado: !!(equipamento && equipamento.situacao !== 'operando')
  };
}
function agendarFabrica(db, opcoes) {
  const op = opcoes || {};
  const j = jornadaDe(db);

  /* as ordens entram pela urgência: prioridade e depois entrega */
  const situacoes = op.situacoes && op.situacoes.length ? op.situacoes : ['aberta', 'liberada', 'producao'];
  const ordens = (db.ordens || []).filter(o => situacoes.includes(o.situacao)).filter(o => !op.somenteOrdem || o.id === op.somenteOrdem).slice().sort((a, b) => {
    const pa = num(a.prioridade),
      pb = num(b.prioridade);
    if (pa !== pb) return pa - pb;
    return String(a.entrega || '9999').localeCompare(String(b.entrega || '9999'));
  });
  if (ordens.length === 0) return {
    vazio: true,
    motivo: 'Nenhuma ordem aberta para programar.',
    jornada: j
  };

  /* fila por ordem: as tarefas se sucedem, uma só começa quando a anterior acaba */
  /* os filtros de setor e pessoa recortam o que se vê, não o que se agenda:
     a fila continua inteira para as datas não mentirem */
  const filas = ordens.map(o => ({
    ordem: o,
    tarefas: (o.tarefas || []).slice().sort((a, b) => num(a.ordem) - num(b.ordem)).filter(t => {
      if (op.incluirConcluidas) return num(t.minutosTotais) > 0;
      return !t.concluida && num(t.minutosTotais) > 0;
    }).map(t => ({
      tarefa: t,
      restante: num(t.minutosTotais),
      recursos: recursosDaTarefa(t, db)
    })),
    indice: 0
  })).filter(f => f.tarefas.length > 0);
  const dias = [];
  const semRecurso = [];
  let data = op.inicio ? new Date(op.inicio + 'T00:00:00') : new Date();
  data.setHours(0, 0, 0, 0);
  let guarda = 0;
  const pendente = () => filas.some(f => f.indice < f.tarefas.length);
  while (pendente() && guarda < 400) {
    guarda++;
    const util = (j.diasSemana || []).includes(data.getDay());
    const sabado = op.sabado && data.getDay() === 6;
    if (!util && !sabado) {
      data = new Date(data.getTime() + 86400000);
      continue;
    }
    const normal = sabado ? j.minutosSabado : j.produtivo;
    /* cada recurso tem a sua agenda de horários no dia */
    const opJanela = {
      sabado,
      horaExtra: op.horaExtra
    };
    const agendaPessoa = {};
    const agendaMaquina = {};
    const pegarAgenda = (mapa, id) => {
      if (!mapa[id]) mapa[id] = novaAgenda(j, opJanela);
      return mapa[id];
    };
    const doDia = [];
    filas.forEach(f => {
      /* dentro da ordem, uma tarefa por vez */
      while (f.indice < f.tarefas.length) {
        const t = f.tarefas[f.indice];
        if (t.restante <= 0.0001) {
          f.indice++;
          continue;
        }
        const r = t.recursos;
        if (r.pessoas.length === 0 || r.equipamentoParado) {
          const motivo = r.pessoas.length === 0 ? 'sem colaborador no setor' : 'equipamento parado';
          if (!semRecurso.some(x => x.tarefaId === t.tarefa.id)) semRecurso.push({
            ordem: f.ordem,
            tarefa: t.tarefa,
            motivo
          });
          f.indice++;
          continue;
        }

        /* quanto ainda cabe hoje, somando as agendas disponíveis */
        let capacidadePessoas = 0;
        r.pessoas.forEach(c => {
          capacidadePessoas += saldoDaAgenda(pegarAgenda(agendaPessoa, c.id));
        });
        let capacidade = capacidadePessoas;
        const agMaq = r.equipamento ? pegarAgenda(agendaMaquina, r.equipamento.id) : null;
        if (agMaq) capacidade = Math.min(capacidade, saldoDaAgenda(agMaq));
        if (capacidade <= 0.0001) break; // o recurso lotou hoje

        const aloca = Math.min(t.restante, capacidade);

        /* ocupa a agenda de cada pessoa, na ordem, marcando o relógio */
        let falta = aloca;
        const quem = [];
        r.pessoas.forEach(c => {
          if (falta <= 0.0001) return;
          const ag2 = pegarAgenda(agendaPessoa, c.id);
          const livre = saldoDaAgenda(ag2);
          if (livre <= 0.0001) return;
          const usa = Math.min(livre, falta);
          const res = ocuparAgenda(ag2, usa);
          if (res.alocado <= 0.0001) return;
          falta -= res.alocado;
          quem.push({
            id: c.id,
            nome: c.nome,
            minutos: Number(res.alocado.toFixed(2)),
            inicio: res.inicio,
            fim: res.fim,
            blocos: res.blocos,
            atravessouIntervalo: res.atravessouIntervalo
          });
        });
        /* a máquina acompanha o mesmo intervalo de tempo */
        let horarioMaquina = null;
        if (agMaq) {
          const res = ocuparAgenda(agMaq, aloca - falta);
          horarioMaquina = {
            inicio: res.inicio,
            fim: res.fim
          };
        }
        t.restante = Number((t.restante - (aloca - falta)).toFixed(4));
        const etapa = (db.etapas || []).find(e => e.id === t.tarefa.etapaId);
        const dep = (db.departamentos || []).find(d => d.id === t.tarefa.departamentoId);
        const produto = (db.produtos || []).find(p => p.id === f.ordem.produtoId);
        doDia.push({
          ordemId: f.ordem.id,
          ordem: f.ordem.codigo,
          departamentoId: t.tarefa.departamentoId,
          produto: produto ? montarNomeProduto(produto, db) || produto.codigo : '—',
          etapa: etapa ? etapa.nome : 'etapa removida',
          departamento: dep ? dep.nome : '—',
          equipamento: r.equipamento ? r.equipamento.nome : '',
          pessoas: quem,
          minutos: Number((aloca - falta).toFixed(2)),
          /* o horário em que a etapa aconteceu: do primeiro ao último */
          inicio: quem.length ? quem.map(q => q.inicio).filter(Boolean).sort()[0] : null,
          fim: quem.length ? quem.map(q => q.fim).filter(Boolean).sort().slice(-1)[0] : null,
          horarioMaquina,
          pecas: num(t.tarefa.minutosPorPeca) > 0 ? Math.floor((aloca - falta) / num(t.tarefa.minutosPorPeca)) : 0,
          parcial: t.restante > 0.0001
        });
        if (t.restante <= 0.0001) f.indice++;
        break; // esta ordem já ocupou o que podia; passa para a próxima
      }
    });
    if (doDia.length > 0) {
      const usado = doDia.reduce((a, x) => a + x.minutos, 0);
      const resumoAgenda = (mapa, colecao) => Object.entries(mapa).map(([id, ag2]) => {
        const item = (colecao || []).find(x => x.id === id);
        const cap = ag2.capacidade;
        const usado = cap - saldoDaAgenda(ag2);
        /* o primeiro e o último minuto trabalhado no dia */
        const primeiro = doDia.flatMap(x => (x.pessoas || []).filter(q => q.id === id).flatMap(q => q.blocos || []).concat(x.horarioMaquina && item && x.equipamento === item.nome ? [{
          ini: horaParaMinutos(x.horarioMaquina.inicio),
          fim: horaParaMinutos(x.horarioMaquina.fim)
        }] : [])).sort((a, b) => a.ini - b.ini);
        return {
          id,
          nome: item ? item.nome : '—',
          usado: Number(usado.toFixed(2)),
          capacidade: cap,
          entrada: primeiro.length ? minutosParaHora(primeiro[0].ini) : null,
          saida: primeiro.length ? minutosParaHora(primeiro[primeiro.length - 1].fim) : null,
          ocupacao: cap > 0 ? Number((usado / cap * 100).toFixed(1)) : 0
        };
      }).filter(x => x.usado > 0.01).sort((a, b) => b.usado - a.usado);
      const porPessoa = resumoAgenda(agendaPessoa, db.colaboradores);
      const porMaquina = resumoAgenda(agendaMaquina, db.equipamentos);
      dias.push({
        iso: data.toISOString().slice(0, 10),
        data: new Date(data),
        sabado,
        minutos: Number(usado.toFixed(2)),
        minutosExtras: Number(Math.max(usado - normal * Math.max(porPessoa.length, 1), 0).toFixed(2)),
        tarefas: doDia,
        porPessoa,
        porMaquina
      });
    }
    data = new Date(data.getTime() + 86400000);
  }

  /* o recorte que o usuário pediu para olhar */
  const filtrar = dias.map(d => {
    const tarefas = d.tarefas.filter(x => {
      if (op.departamentoId && x.departamentoId !== op.departamentoId) return false;
      if (op.colaboradorId && !x.pessoas.some(q => q.id === op.colaboradorId)) return false;
      if (op.de && d.iso < op.de) return false;
      if (op.ate && d.iso > op.ate) return false;
      return true;
    });
    return {
      ...d,
      tarefas
    };
  }).filter(d => d.tarefas.length > 0);
  const recortado = !!(op.departamentoId || op.colaboradorId || op.de || op.ate);
  const diasVisiveis = recortado ? filtrar : dias;

  /* quando cada ordem termina */
  const conclusao = {};
  dias.forEach(d => d.tarefas.forEach(t => {
    conclusao[t.ordemId] = d.iso;
  }));
  const resumoOrdens = ordens.map(o => {
    const fim = conclusao[o.id] || null;
    const atraso = fim && o.entrega && fim > o.entrega;
    return {
      ordem: o,
      fim,
      atraso,
      diasDeAtraso: atraso ? Math.round((new Date(fim) - new Date(o.entrega)) / 86400000) : 0
    };
  });
  return {
    vazio: false,
    jornada: j,
    dias: diasVisiveis,
    diasCompletos: dias,
    recortado,
    ordens: resumoOrdens,
    totalMinutos: Number(dias.reduce((a, d) => a + d.minutos, 0).toFixed(2)),
    primeiroDia: dias.length ? dias[0].iso : null,
    ultimoDia: dias.length ? dias[dias.length - 1].iso : null,
    atrasadas: resumoOrdens.filter(x => x.atraso),
    semRecurso,
    incompleto: guarda >= 400
  };
}

/* ==========================================================
   QUANDO VALE PEDIR HORA EXTRA
   Não se oferece extra por qualquer atraso: só quando o excedente
   cabe nas 2 horas do dia, ou quando o sábado fecha a semana.
========================================================== */
function analisarNecessidadeExtra(db, opcoes) {
  const op = opcoes || {};
  const j = jornadaDe(db);
  const base = agendarFabrica(db, {
    ...op,
    horaExtra: false,
    sabado: false
  });
  if (base.vazio) return {
    vazio: true,
    motivo: base.motivo
  };

  /* as ordens que não fecham no prazo são o problema a resolver */
  const atrasadas = base.atrasadas || [];
  if (atrasadas.length === 0) return {
    vazio: false,
    precisa: false,
    base,
    motivo: 'Todas as ordens fecham no prazo com a jornada normal.'
  };
  const comExtra = agendarFabrica(db, {
    ...op,
    horaExtra: true,
    sabado: false
  });
  const comSabado = agendarFabrica(db, {
    ...op,
    horaExtra: true,
    sabado: true
  });
  const atrasoExtra = (comExtra.atrasadas || []).length;
  const atrasoSabado = (comSabado.atrasadas || []).length;

  /* quanto trabalho está sobrando por dia, em média */
  const diasBase = base.diasCompletos ? base.diasCompletos.length : base.dias.length;
  const pessoas = new Set();
  (base.diasCompletos || base.dias).forEach(d => d.porPessoa.forEach(p => pessoas.add(p.id)));
  const capacidadeDia = j.produtivo * Math.max(pessoas.size, 1);
  const maiorAtraso = Math.max(...atrasadas.map(x => x.diasDeAtraso));
  const excedente = maiorAtraso * capacidadeDia;
  const porDia = diasBase > 0 ? excedente / diasBase : excedente;
  const porPessoaDia = pessoas.size > 0 ? porDia / pessoas.size : porDia;
  const opcoesDisponiveis = [];

  /* 1) cabe nas 2 horas extras do dia? */
  if (atrasoExtra === 0 || atrasoExtra < atrasadas.length) {
    opcoesDisponiveis.push({
      id: 'horaExtra',
      titulo: 'Estender a jornada até ' + j.extensao.fim,
      ganho: `${atrasadas.length - atrasoExtra} ordem(ns) voltam ao prazo`,
      detalhe: `mais ${j.minutosExtras} min por pessoa, por dia`,
      resolve: atrasoExtra === 0,
      restam: atrasoExtra,
      exige: j.extensao.exige,
      cenario: comExtra
    });
  }

  /* 2) o sábado fecha o que a hora extra não fechou? */
  if (atrasoSabado < atrasoExtra || atrasoExtra > 0 && atrasoSabado === 0) {
    opcoesDisponiveis.push({
      id: 'sabado',
      titulo: `Trabalhar no sábado, ${j.sabado.inicio} às ${j.sabado.fim}`,
      ganho: `${atrasoExtra - atrasoSabado} ordem(ns) a mais no prazo`,
      detalhe: `${j.minutosSabado} min por pessoa, com a hora extra junto`,
      resolve: atrasoSabado === 0,
      restam: atrasoSabado,
      exige: j.sabado.exige,
      cenario: comSabado
    });
  }
  return {
    vazio: false,
    precisa: true,
    base,
    atrasadas,
    maiorAtraso,
    excedenteDiario: Number(porPessoaDia.toFixed(0)),
    cabeNaHoraExtra: porPessoaDia <= j.minutosExtras,
    opcoes: opcoesDisponiveis,
    semSolucao: opcoesDisponiveis.length === 0,
    motivo: opcoesDisponiveis.length === 0 ? 'Nem a hora extra nem o sábado resolvem: o volume passa da capacidade da fábrica. ' + 'Reveja prazos, prioridades ou contrate.' : `${atrasadas.length} ordem(ns) fora do prazo. Falta cerca de ` + `${Math.round(porPessoaDia)} min por pessoa, por dia.`
  };
}

/* Quanto a hora extra encurta o prazo */
function ganhoComHoraExtra(produto, db, opcoes) {
  const sem = montarCronograma(produto, db, {
    ...opcoes,
    horaExtra: false,
    sabado: false
  });
  const com = montarCronograma(produto, db, {
    ...opcoes,
    horaExtra: true
  });
  if (sem.vazio || com.vazio) return null;
  return {
    sem: sem.diasNecessarios,
    com: com.diasNecessarios,
    ganho: sem.diasNecessarios - com.diasNecessarios,
    minutosExtras: com.minutosExtras,
    horasExtras: Number((com.minutosExtras / 60).toFixed(2))
  };
}
const {
  useState,
  useEffect,
  useCallback,
  useRef
} = React;
const todayISO = () => new Date().toISOString().slice(0, 10);
const num = v => {
  const n = parseFloat(v);
  return isNaN(n) ? 0 : n;
};
const PERFIS = ['Administrador', 'Gestor', 'Colaborador'];
// permissões por perfil
/* Áreas do sistema — usadas na tela de Perfis de acesso */
const ABAS_SISTEMA = [{
  id: 'painel',
  label: 'Painel'
}, {
  id: 'pessoas',
  label: 'Cadastros'
}, {
  id: 'materiais',
  label: 'Materiais'
}, {
  id: 'engenharia',
  label: 'Engenharia'
}, {
  id: 'produtos',
  label: 'Produtos'
}, {
  id: 'producao',
  label: 'Produção'
}, {
  id: 'canal',
  label: 'Conversa aberta'
}, {
  id: 'recados',
  label: 'Chat'
}];
const ACOES_SISTEMA = [{
  id: 'cadastros',
  label: 'Criar e editar cadastros',
  ajuda: 'clientes e fornecedores'
}, {
  id: 'pessoas',
  label: 'Gerenciar colaboradores',
  ajuda: 'cadastrar, editar e excluir pessoas'
}, {
  id: 'financeiro',
  label: 'Ver valores financeiros',
  ajuda: 'salários e demais valores sensíveis'
}, {
  id: 'admin',
  label: 'Administrar o sistema',
  ajuda: 'perfis de acesso, exclusões críticas e backup'
}];

/* Níveis de acesso prontos — cada função recebe um deles */
/* As áreas que se pode liberar uma a uma. Antes eram quatro blocos
   grandes; agora cada operação sensível tem a sua chave. */
const AREAS_PERMISSAO = [{
  grupo: 'Cadastros',
  itens: [{
    id: 'cadastros',
    nome: 'Clientes e fornecedores'
  }, {
    id: 'materiais.editar',
    nome: 'Cadastrar e editar materiais'
  }, {
    id: 'materiais.mover',
    nome: 'Lançar movimentações de estoque'
  }, {
    id: 'materiais.estornar',
    nome: 'Estornar movimentações'
  }, {
    id: 'materiais.custo',
    nome: 'Ver custos e preços de material'
  }]
}, {
  grupo: 'Pessoas',
  itens: [{
    id: 'pessoas',
    nome: 'Cadastrar colaboradores'
  }, {
    id: 'pessoas.salario',
    nome: 'Ver e editar salários'
  }, {
    id: 'pessoas.permissoes',
    nome: 'Alterar permissões de acesso'
  }]
}, {
  grupo: 'Produto',
  itens: [{
    id: 'produtos.editar',
    nome: 'Criar e editar produtos'
  }, {
    id: 'produtos.risco',
    nome: 'Importar e vincular modelagens'
  }, {
    id: 'produtos.processo',
    nome: 'Montar o processo produtivo'
  }, {
    id: 'produtos.custo',
    nome: 'Ver o custo do produto'
  }]
}, {
  grupo: 'Engenharia',
  itens: [{
    id: 'engenharia.editar',
    nome: 'Departamentos, etapas e equipamentos'
  }, {
    id: 'engenharia.jornada',
    nome: 'Alterar jornada e encargos'
  }]
}, {
  grupo: 'Produção',
  itens: [{
    id: 'producao.extra',
    nome: 'Autorizar horas extras e sábado'
  }]
}, {
  grupo: 'Sistema',
  itens: [{
    id: 'financeiro',
    nome: 'Informações financeiras'
  }, {
    id: 'admin',
    nome: 'Administração do sistema'
  }]
}];
const TODAS_AREAS = AREAS_PERMISSAO.flatMap(g => g.itens.map(i => i.id));
function nomeDaArea(id) {
  for (const g of AREAS_PERMISSAO) {
    const i = g.itens.find(x => x.id === id);
    if (i) return i.nome;
  }
  return id;
}
const NIVEIS_ACESSO = [{
  id: 'total',
  nome: 'Acesso total',
  cor: 'bad',
  desc: 'Vê e altera tudo, inclusive permissões, colaboradores e dados sensíveis.',
  perfil: 'Administrador',
  abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
  acoes: TODAS_AREAS
}, {
  id: 'gerencial',
  nome: 'Acesso gerencial',
  cor: 'warn',
  desc: 'Administra os cadastros e acompanha a operação, sem alterar permissões.',
  perfil: 'Gestor',
  abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
  acoes: TODAS_AREAS.filter(a => a !== 'admin' && a !== 'pessoas.permissoes')
}, {
  id: 'cadastro',
  nome: 'Cadastros',
  cor: 'info',
  desc: 'Inclui e edita clientes, fornecedores e colaboradores.',
  perfil: 'Colaborador',
  abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
  acoes: ['cadastros', 'materiais.editar', 'materiais.mover', 'produtos.editar', 'produtos.risco', 'produtos.processo']
}, {
  id: 'consulta',
  nome: 'Somente consulta',
  cor: 'idle',
  desc: 'Vê as informações, mas não altera nada.',
  perfil: 'Colaborador',
  abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
  acoes: []
}, {
  id: 'sem_acesso',
  nome: 'Sem acesso',
  cor: 'idle',
  desc: 'Não entra no sistema.',
  perfil: 'Colaborador',
  abas: [],
  acoes: []
}];
function nivelPorId(id) {
  return NIVEIS_ACESSO.find(n => n.id === id) || null;
}

/* Permissões padrão de cada perfil. Podem ser alteradas em Cadastros → Perfis de acesso. */
/* Monta o objeto de ações a partir da lista de áreas liberadas */
function acoesDe(liberadas) {
  const o = {};
  TODAS_AREAS.forEach(a => {
    o[a] = liberadas.includes(a);
  });
  return o;
}
const PERM_PADRAO = {
  'Administrador': {
    abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
    acoes: acoesDe(TODAS_AREAS)
  },
  'Gestor': {
    abas: ['painel', 'pessoas', 'materiais', 'engenharia', 'produtos', 'producao', 'canal', 'recados'],
    acoes: acoesDe(TODAS_AREAS.filter(a => a !== 'admin' && a !== 'pessoas.permissoes'))
  },
  'Colaborador': {
    abas: ['painel', 'canal'],
    acoes: acoesDe([])
  }
};
const PERM = {
  'Administrador': {
    ...PERM_PADRAO['Administrador'],
    financeiro: true,
    pessoas: true
  },
  'Gestor': {
    ...PERM_PADRAO['Gestor'],
    financeiro: false,
    pessoas: false
  },
  'Colaborador': {
    ...PERM_PADRAO['Colaborador'],
    financeiro: false,
    pessoas: false
  }
};

/* Resolve as permissões efetivas: padrão do perfil → configuração salva → exceção do colaborador. */
/* Ordem de resolução das permissões:
   1) padrão do perfil  →  2) configuração salva do perfil
   3) definição por cargo/função (soma o acesso de todas as funções do colaborador)
   4) exceção individual do colaborador                                            */
function resolverPermissoes(colab, db) {
  const perfil = colab && colab.perfil || 'Colaborador';
  const base = db && db.permissoes && db.permissoes[perfil] || PERM_PADRAO[perfil] || PERM_PADRAO['Colaborador'];
  let abas = [...(base.abas || [])];
  let acoes = {
    ...(base.acoes || {})
  };

  // função: cada uma tem um nível de acesso; quem tem várias recebe a soma
  const porCargo = db && db.perfisPorCargo || {};
  const funcoes = funcoesColaborador(colab);
  funcoes.forEach(fn => {
    const cfg = porCargo[normaliza(fn)];
    if (!cfg) return;
    const nvBase = nivelPorId(cfg.nivel);
    const cst = (db && db.niveisCustom || {})[cfg.nivel];
    const nv = nvBase ? {
      ...nvBase,
      ...(cst || {})
    } : null;
    const fonteAbas = nv ? nv.abas : cfg.abas || [];
    const fonteAcoes = nv ? nv.acoes : cfg.acoes || [];
    fonteAbas.forEach(x => {
      if (!abas.includes(x)) abas.push(x);
    });
    // as ações vêm como lista nos níveis e como objeto em bases antigas
    if (Array.isArray(fonteAcoes)) fonteAcoes.forEach(k => {
      acoes[k] = true;
    });else Object.entries(fonteAcoes).forEach(([k, v]) => {
      if (v) acoes[k] = true;
    });
  });
  const excecao = colab && colab.permissoes || null;
  if (excecao && Array.isArray(excecao.abas)) abas = excecao.abas;
  if (excecao && excecao.acoes) acoes = {
    ...acoes,
    ...excecao.acoes
  };
  return {
    abas,
    acoes,
    financeiro: !!acoes.financeiro,
    pessoas: !!acoes.pessoas,
    perfil,
    funcoes
  };
}
/* Perfil sugerido a partir do cargo cadastrado */
function perfilDoCargo(colab, db) {
  const porCargo = db && db.perfisPorCargo || {};
  const fns = funcoesColaborador(colab);
  const ordem = {
    'Administrador': 3,
    'Gestor': 2,
    'Colaborador': 1
  };
  let melhor = null;
  fns.forEach(fn => {
    const cfg = porCargo[normaliza(fn)];
    if (!cfg) return;
    const nv = nivelPorId(cfg.nivel);
    const p = nv ? nv.perfil : cfg.perfil;
    if (p && (!melhor || ordem[p] > ordem[melhor])) melhor = p;
  });
  return melhor;
}
/* --- validação de duplicidade de cadastro --- */
function normaliza(v) {
  return String(v || '').trim().toLowerCase().replace(/[.\-\/\s]/g, '');
}
// campos: [{key:'nome', label:'Nome', soDigitos:false}, ...]
// retorna mensagem de erro se encontrar duplicado, ou null
function checarDuplicidade(lista, registro, campos) {
  for (const campo of campos) {
    const valor = normaliza(registro[campo.key]);
    if (!valor) continue; // campo vazio não bloqueia
    // ignora o próprio registro apenas quando ele já tem identificação (edição);
    // cadastro novo (sem id) é sempre confrontado com todos os existentes
    const conflito = lista.find(item => !(registro.id && item.id === registro.id) && normaliza(item[campo.key]) === valor);
    if (conflito) {
      return `Já existe um cadastro com o mesmo ${campo.label}: "${conflito.nome || conflito[campo.key]}". Não é permitido cadastro duplicado.`;
    }
  }
  return null;
}
function permDe(usuario) {
  if (usuario && usuario._perm) return usuario._perm; // resolvidas no login
  return usuario && PERM[usuario.perfil] || PERM['Colaborador'];
}
/* Autorização aplicada na ação, não só na tela.
   area: 'pessoas' | 'financeiro' | 'cadastros' | 'admin' */
function podeExecutar(usuario, area) {
  const perm = permDe(usuario);
  if (perm && perm.acoes) {
    if (Object.prototype.hasOwnProperty.call(perm.acoes, area)) return !!perm.acoes[area];
    /* quem pode administrar o sistema passa em qualquer porta */
    if (perm.acoes.admin) return true;
    /* Área conhecida e ausente do nível significa negada de propósito:
       é o caso de 'pessoas.permissoes' num perfil que tem 'pessoas'.
       A herança abaixo vale só para áreas que o cadastro ainda não conhece. */
    if (TODAS_AREAS.includes(area)) return false;
    const raiz = String(area).split('.')[0];
    if (raiz !== area && Object.prototype.hasOwnProperty.call(perm.acoes, raiz)) return !!perm.acoes[raiz];
    if (['materiais', 'engenharia', 'produtos'].includes(raiz) && Object.prototype.hasOwnProperty.call(perm.acoes, 'cadastros')) return !!perm.acoes.cadastros;
  }
  // segurança: se a área não foi configurada, só administrador passa
  return (usuario && usuario.perfil) === 'Administrador';
}
// usada dentro das funções de salvar/excluir; bloqueia e registra a tentativa
function exigirPermissao(usuario, area, update, oQue) {
  if (podeExecutar(usuario, area)) return true;
  alert(`Seu perfil (${usuario && usuario.perfil || 'Colaborador'}) não tem permissão para ${oQue}.` + `

A permissão necessária é "${nomeDaArea(area)}", em Cadastros → Níveis de acesso.`);
  if (update) update(d => {
    registrarLog(d, usuario, 'Ação bloqueada por permissão', oQue);
    return d;
  });
  return false;
}
/* O momento exato, com segundos. Sem eles, duas ações no mesmo minuto
   ficam indistinguíveis no log e a duração de uma operação curta some
   no arredondamento. O separador continua sendo espaço (e não T) por
   compatibilidade com o que já está gravado — new Date() lê os dois. */
function agoraISO() {
  const d = new Date();
  const p = n => String(n).padStart(2, '0');
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}` +
    ` ${p(d.getHours())}:${p(d.getMinutes())}:${p(d.getSeconds())}`;
}
// registra uma entrada no log; recebe o objeto db já em modo de escrita
function registrarLog(d, usuario, acao, detalhe) {
  if (!d.logs) d.logs = [];
  d.logs.push({
    id: uid(),
    quando: agoraISO(),
    data: todayISO(),
    quem: usuario ? usuario.nome : 'Sistema',
    perfil: usuario ? usuario.perfil : '—',
    acao,
    detalhe: detalhe || ''
  });
  if (d.logs.length > 500) d.logs = d.logs.slice(-500);
}
/* ---------------- persistence ---------------- */
const VERSAO_APP = '2026.09.03-1532';
/* O canal do colaborador roda num arquivo próprio e grava aqui, numa
   caixa separada. Quem abrir o canal no chão de fábrica não alcança o
   banco da empresa — só esta caixa, que só tem manifestação. */
const CAIXA_CANAL = 'conversa-aberta-caixa-v1';
/* ==========================================================
   SEGURANÇA — senhas com hash, primeiro acesso e sessão
   Nenhuma senha fica no código nem é gravada em texto puro.
========================================================== */
const ADMIN_PADRAO_NOME = 'Administrador';
const SESSAO_INATIVIDADE_MIN = 30; // desconecta após 30 min sem uso
const MAX_TENTATIVAS = 5; // bloqueio temporário após 5 erros
const BLOQUEIO_MIN = 10;
function gerarSalt() {
  const a = new Uint8Array(16);
  (window.crypto || window.msCrypto).getRandomValues(a);
  return Array.from(a).map(b => b.toString(16).padStart(2, '0')).join('');
}

/* Hash SHA-256 com salt. Sem crypto.subtle (contexto não seguro),
   cai para um hash local — mais fraco, mas ainda não guarda texto puro. */
async function hashSenha(senha, salt) {
  const dado = salt + '::' + senha;
  try {
    if (window.crypto && window.crypto.subtle) {
      const buf = await window.crypto.subtle.digest('SHA-256', new TextEncoder().encode(dado));
      return Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2, '0')).join('');
    }
  } catch (e) {/* segue para o alternativo */}
  let h1 = 0x811c9dc5,
    h2 = 0x01000193;
  for (let i = 0; i < dado.length; i++) {
    h1 = (h1 ^ dado.charCodeAt(i)) * 16777619 >>> 0;
    h2 = (h2 + dado.charCodeAt(i) * (i + 7)) * 2654435761 >>> 0;
  }
  return 'fb' + h1.toString(16).padStart(8, '0') + h2.toString(16).padStart(8, '0');
}
async function definirSenha(colab, senha) {
  const salt = gerarSalt();
  const hash = await hashSenha(senha, salt);
  return {
    ...colab,
    senhaSalt: salt,
    senhaHash: hash,
    senha: undefined,
    precisaTrocarSenha: false,
    senhaDefinidaEm: agoraISO()
  };
}

/* Confere a senha. Cadastros antigos guardavam texto puro: nesse caso
   valida uma única vez e sinaliza para migrar para hash. */
async function conferirSenha(colab, senha) {
  if (colab.senhaHash && colab.senhaSalt) {
    const h = await hashSenha(senha, colab.senhaSalt);
    return {
      ok: h === colab.senhaHash,
      migrar: false
    };
  }
  if (colab.senha && String(colab.senha).length > 0) {
    return {
      ok: String(colab.senha) === senha,
      migrar: true
    }; // legado
  }
  return {
    ok: false,
    semSenha: true
  };
}
function temSenhaDefinida(c) {
  if (!c) return false;
  return !!(c.senhaHash && c.senhaSalt || c.senha && String(c.senha).length > 0);
}
function forcaSenha(s) {
  const t = String(s || '');
  if (t.length < 6) return {
    ok: false,
    msg: 'A senha precisa ter ao menos 6 caracteres.'
  };
  if (!/[A-Za-z]/.test(t) || !/[0-9]/.test(t)) return {
    ok: false,
    msg: 'Use ao menos uma letra e um número.'
  };
  return {
    ok: true,
    msg: 'Senha válida.'
  };
}

/* Garante que sempre exista UM administrador para não travar o acesso.
   Se não houver nenhum, cria um sem senha — que será definida no primeiro acesso.
   Nunca sobrescreve a senha de um administrador existente. */
function garantirAdminPadrao(db) {
  const temAdmin = db.colaboradores.some(c => c.perfil === 'Administrador' && c.status !== 'Inativo');
  if (temAdmin) return db;
  const legado = db.colaboradores.find(c => (c.nome || '').trim().toLowerCase() === 'renato monteiro');
  if (legado) {
    legado.perfil = 'Administrador';
    legado.status = 'Ativo';
    if (!temSenhaDefinida(legado)) legado.precisaTrocarSenha = true;
    return db;
  }
  db.colaboradores.unshift({
    id: uid(),
    nome: ADMIN_PADRAO_NOME,
    cpf: '',
    rg: '',
    dataNascimento: '',
    telefone: '',
    celular: '',
    email: '',
    cargo: '',
    funcoes: ['Administrador do sistema'],
    departamentoId: '',
    dataAdmissao: todayISO(),
    salario: 0,
    status: 'Ativo',
    perfil: 'Administrador',
    precisaTrocarSenha: true,
    cep: '',
    endereco: '',
    numero: '',
    complemento: '',
    bairro: '',
    cidade: '',
    uf: '',
    observacoes: ''
  });
  return db;
}
/* ==========================================================
   PERSISTÊNCIA — um registro por chave (não mais um blob único)
   ----------------------------------------------------------
   O window.storage aqui é sempre o Supabase (instalado por
   storage.js antes deste app montar). Cada item de cada coleção
   vira uma linha própria na tabela app_storage, na chave
   `confeccao-erp:<colecao>:<id>`, do mesmo jeito que o resto do
   sistema já faz. Isso elimina o teto de 4,5 MB do blob único:
   cada registro é independente.
========================================================== */
const PREFIXO = 'confeccao-erp';
const COLECOES_ARRAY = ['colaboradores', 'clientes', 'fornecedores', 'logs', 'materiais', 'gruposMaterial', 'unidades', 'conversoes', 'estoques', 'saldos', 'movimentacoes', 'reservas', 'centrosCusto', 'programacoes', 'ocorrencias', 'custosFixos', 'riscos', 'gruposProduto', 'tiposProduto', 'ordens', 'apontamentos', 'avaliacoes', 'produtos', 'subprodutos', 'departamentos', 'etapas', 'equipamentos', 'versoesProduto', 'afericoes', 'metasOperacionais', 'objetivosMelhoria', 'conversas', 'mensagens', 'notas', 'vagas', 'indicacoes', 'manifestacoes', 'pesquisasClima'];
const CAMPOS_UNICOS = ['permissoes', 'perfisPorCargo', 'niveisCustom', 'parametrosMateriais', 'parametrosMaoDeObra', 'parametrosCustoIndireto', 'parametrosFila', 'jornada', 'configCanal', 'seq'];

function tamanhoBase(db) {
  try {
    return JSON.stringify(db).length;
  } catch (e) {
    return 0;
  }
}

async function loadDb() {
  const modelo = emptyDb();
  const base = {};
  try {
    await Promise.all(COLECOES_ARRAY.map(async (colecao) => {
      const { keys } = await window.storage.list(`${PREFIXO}:${colecao}:`);
      const itens = await Promise.all((keys || []).map(async (chave) => {
        try {
          const r = await window.storage.get(chave);
          return r && r.value ? JSON.parse(r.value) : null;
        } catch (e) {
          return null;
        }
      }));
      base[colecao] = itens.filter(Boolean);
    }));
    await Promise.all(CAMPOS_UNICOS.map(async (campo) => {
      try {
        const r = await window.storage.get(`${PREFIXO}:${campo}`);
        base[campo] = r && r.value ? JSON.parse(r.value) : modelo[campo];
      } catch (e) {
        base[campo] = modelo[campo];
      }
    }));
  } catch (e) {/* base parcialmente carregada — completa com o padrão abaixo */}
  Object.keys(modelo).forEach(k => {
    if (base[k] === undefined) base[k] = modelo[k];
  });
  base.seq = {
    ...modelo.seq,
    ...(base.seq || {})
  };
  return migrarJornada(semearEngenharia(migrarGruposCortaveis(semearProduto(semearMateriais(garantirAdminPadrao(base))))));
}

async function persistDb(next, prev) {
  const anterior = prev || emptyDb();
  const falhas = [];
  await Promise.all(COLECOES_ARRAY.map(async (colecao) => {
    const antigos = new Map((anterior[colecao] || []).map(it => [it.id, it]));
    const novos = new Map((next[colecao] || []).map(it => [it.id, it]));
    const tarefas = [];
    for (const [id, item] of novos) {
      const anteriorItem = antigos.get(id);
      if (!anteriorItem || JSON.stringify(anteriorItem) !== JSON.stringify(item)) {
        tarefas.push(window.storage.set(`${PREFIXO}:${colecao}:${id}`, JSON.stringify(item)));
      }
    }
    for (const id of antigos.keys()) {
      if (!novos.has(id)) tarefas.push(window.storage.delete(`${PREFIXO}:${colecao}:${id}`));
    }
    if (tarefas.length) {
      try {
        await Promise.all(tarefas);
      } catch (e) {
        falhas.push(colecao);
      }
    }
  }));
  await Promise.all(CAMPOS_UNICOS.map(async (campo) => {
    if (JSON.stringify(anterior[campo]) !== JSON.stringify(next[campo])) {
      try {
        await window.storage.set(`${PREFIXO}:${campo}`, JSON.stringify(next[campo]));
      } catch (e) {
        falhas.push(campo);
      }
    }
  }));
  if (falhas.length) {
    return `Não foi possível salvar: ${falhas.join(', ')}. Verifique a conexão e tente novamente.`;
  }
  return null;
}

function rotuloArmazenamento() {
  return {
    texto: 'Salvo no banco de dados compartilhado',
    tone: 'ok'
  };
}

/* ---------------- small UI atoms ---------------- */
function Field({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "field"
  }, /*#__PURE__*/React.createElement("label", null, label), children);
}
function Badge({
  tone,
  children
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "badge " + tone
  }, /*#__PURE__*/React.createElement("span", {
    className: "dot"
  }), children);
}
function Modal({
  title,
  onClose,
  children,
  wide
}) {
  /* O dropdown de um <select> é desenhado pelo sistema, fora do modal.
     Fechar no mousedown fazia o modal sumir quando a lista era longa e o
     clique na opção caía sobre o fundo — o campo parecia não funcionar. */
  const comecouNoFundo = useRef(false);
  return /*#__PURE__*/React.createElement("div", {
    className: "modal-backdrop",
    onMouseDown: e => {
      comecouNoFundo.current = e.target === e.currentTarget;
    },
    onMouseUp: e => {
      if (comecouNoFundo.current && e.target === e.currentTarget) onClose();
      comecouNoFundo.current = false;
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "modal",
    style: wide ? {
      maxWidth: 820
    } : {}
  }, /*#__PURE__*/React.createElement("h3", null, title), children));
}
function Empty({
  text
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "empty"
  }, text);
}

/* ==========================================================
   APP
========================================================== */
function App() {
  const [db, setDb] = useState(null);
  const [tab, setTab] = useState('painel');
  const [ready, setReady] = useState(false);
  const [usuario, setUsuario] = useState(null);
  /* a porta do canal, aberta antes de qualquer senha */
  const [portaCanal, setPortaCanal] = useState(() => {
    try {
      const p = new URLSearchParams(window.location.search);
      /* o QR do setor cai direto aqui, sem passar pelo login */
      return p.get('canal') === '1' || p.has('setor');
    } catch (e) {
      return false;
    }
  });
  const [erroSalvar, setErroSalvar] = useState(null);
  const [trocarSenha, setTrocarSenha] = useState(false);
  const [ultimaAtividade, setUltimaAtividade] = useState(Date.now());

  // controle de sessão: encerra após inatividade
  useEffect(() => {
    if (!usuario) return;
    const marcar = () => setUltimaAtividade(Date.now());
    ['click', 'keydown', 'mousemove', 'touchstart'].forEach(ev => window.addEventListener(ev, marcar));
    const timer = setInterval(() => {
      if (Date.now() - ultimaAtividade > SESSAO_INATIVIDADE_MIN * 60000) {
        setUsuario(null);
        alert(`Sessão encerrada após ${SESSAO_INATIVIDADE_MIN} minutos sem uso. Entre novamente.`);
      }
    }, 30000);
    return () => {
      ['click', 'keydown', 'mousemove', 'touchstart'].forEach(ev => window.removeEventListener(ev, marcar));
      clearInterval(timer);
    };
  }, [usuario, ultimaAtividade]);
  useEffect(() => {
    loadDb().then(d => {
      setDb(d);
      setReady(true);
    });
  }, []);

  /* A base viva fica numa referência para que o mutator rode NA HORA.
     Com a forma diferida do setState, quem chama update não conseguia ler
     o retorno da operação — e todo aviso de erro se perdia pelo caminho. */
  const dbRef = useRef(null);
  dbRef.current = db;
  const update = useCallback(mutator => {
    const base = dbRef.current;
    if (!base) return null;
    const copia = JSON.parse(JSON.stringify(base));
    const devolvido = mutator(copia);
    const next = devolvido || copia;
    dbRef.current = next; // encadeia updates no mesmo clique
    persistDb(next, base).then(erro => setErroSalvar(erro));
    setDb(next);
    return next;
  }, []);
  if (!ready || !db) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: 60,
        fontFamily: 'var(--mono)',
        color: '#8a8577'
      }
    }, "Carregando ERP…");
  }

  // acesso sempre autenticado — não há mais entrada livre
  if (!usuario) {
    if (portaCanal) {
      return /*#__PURE__*/React.createElement(PortaConversaAberta, {
        db: db,
        update: update,
        onVoltar: () => setPortaCanal(false)
      });
    }
    return /*#__PURE__*/React.createElement(Login, {
      db: db,
      update: update,
      onCanal: () => setPortaCanal(true),
      onEntrar: u => {
        setUsuario(u);
        setUltimaAtividade(Date.now());
        setTab(permDe(u).abas[0]);
      }
    });
  }
  const base = db.colaboradores.find(c => c.id === usuario.id) || usuario;
  const usuarioAtual = {
    ...base,
    _perm: resolverPermissoes(base, db)
  };
  // usuário desativado durante a sessão perde o acesso na hora
  if (usuarioAtual.status === 'Inativo') {
    setTimeout(() => setUsuario(null), 0);
    return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: 60,
        fontFamily: 'var(--mono)'
      }
    }, "Acesso revogado. Recarregando…");
  }
  const perm = permDe(usuarioAtual);
  const NAV_TODOS = ABAS_SISTEMA.map((a, i) => ({
    ...a,
    n: String(i + 1)
  }));
  const NAV = NAV_TODOS.filter(n => perm.abas.includes(n.id));
  const tabAtual = perm.abas.includes(tab) ? tab : perm.abas[0];
  const curLabel = (NAV.find(n => n.id === tabAtual) || {}).label || '';
  function sair() {
    if (!confirm('Sair do sistema?')) return;
    update(d => {
      registrarLog(d, usuarioAtual, 'Saiu do sistema', '');
      return d;
    });
    setUsuario(null);
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "app"
  }, /*#__PURE__*/React.createElement("div", {
    className: "sidebar"
  }, /*#__PURE__*/React.createElement("div", {
    className: "brand"
  }, /*#__PURE__*/React.createElement("div", {
    className: "tag"
  }, "Sistema Interno"), /*#__PURE__*/React.createElement("h1", null, "Confecção ERP"), /*#__PURE__*/React.createElement("div", {
    className: "versao-app",
    title: "versão do arquivo aberto"
  }, "v", VERSAO_APP)), /*#__PURE__*/React.createElement("div", {
    className: "nav"
  }, NAV.map(item => /*#__PURE__*/React.createElement("button", {
    key: item.id,
    className: tabAtual === item.id ? 'active' : '',
    onClick: () => setTab(item.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: "num"
  }, item.n), item.label))), /*#__PURE__*/React.createElement("div", {
    className: "sidebar-foot"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      color: '#EDEAE4',
      fontFamily: 'var(--body)',
      fontSize: 12,
      fontWeight: 600
    }
  }, usuarioAtual.nome), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 8
    }
  }, usuarioAtual.perfil), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: '#EDEAE4',
      borderColor: 'rgba(255,255,255,0.25)'
    },
    onClick: () => setTrocarSenha(true)
  }, "Senha"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: '#EDEAE4',
      borderColor: 'rgba(255,255,255,0.25)'
    },
    onClick: sair
  }, "Sair")))), /*#__PURE__*/React.createElement("div", {
    className: "mobile-header"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "tag"
  }, "Confecção ERP · ", usuarioAtual.perfil, ' · ', "v", VERSAO_APP), /*#__PURE__*/React.createElement("div", {
    className: "cur"
  }, curLabel)), usuario && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: '#EDEAE4',
      borderColor: 'rgba(255,255,255,0.25)'
    },
    onClick: sair
  }, "Sair")), /*#__PURE__*/React.createElement("div", {
    className: "main"
  }, erroSalvar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--bad)',
      background: 'var(--bad-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      color: 'var(--bad)',
      fontWeight: 600,
      flex: 1
    }
  }, "⚠ ", erroSalvar), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setErroSalvar(null)
  }, "Dispensar"))), tabAtual === 'painel' && /*#__PURE__*/React.createElement(Painel, {
    db: db,
    update: update,
    usuario: usuarioAtual
  }), tabAtual === 'pessoas' && /*#__PURE__*/React.createElement(GrupoCadastros, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'materiais' && /*#__PURE__*/React.createElement(GrupoMateriais, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'produtos' && /*#__PURE__*/React.createElement(GrupoProdutos, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'engenharia' && /*#__PURE__*/React.createElement(GrupoEngenharia, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'producao' && /*#__PURE__*/React.createElement(GrupoProducao, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'canal' && /*#__PURE__*/React.createElement(GrupoCanal, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  }), tabAtual === 'recados' && /*#__PURE__*/React.createElement(GrupoRecados, {
    db: db,
    update: update,
    usuario: usuarioAtual,
    perm: perm
  })), trocarSenha && /*#__PURE__*/React.createElement(TrocarSenhaModal, {
    usuario: usuarioAtual,
    update: update,
    onClose: () => setTrocarSenha(false)
  }), /*#__PURE__*/React.createElement("div", {
    className: "bottom-nav"
  }, NAV.map(item => /*#__PURE__*/React.createElement("button", {
    key: item.id,
    className: tabAtual === item.id ? 'active' : '',
    onClick: () => setTab(item.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: "num"
  }, item.n), item.label.split(' ')[0]))));
}
function TrocarSenhaModal({
  usuario,
  update,
  onClose
}) {
  const [atual, setAtual] = useState('');
  const [nova1, setNova1] = useState('');
  const [nova2, setNova2] = useState('');
  const [erro, setErro] = useState('');
  const [ok, setOk] = useState(false);
  const [ocupado, setOcupado] = useState(false);
  async function salvar() {
    setErro('');
    if (temSenhaDefinida(usuario)) {
      const r = await conferirSenha(usuario, atual);
      if (!r.ok) {
        setErro('Senha atual incorreta.');
        return;
      }
    }
    const v = forcaSenha(nova1);
    if (!v.ok) {
      setErro(v.msg);
      return;
    }
    if (nova1 !== nova2) {
      setErro('As senhas não conferem.');
      return;
    }
    setOcupado(true);
    const atualizado = await definirSenha(usuario, nova1);
    setOcupado(false);
    update(d => {
      d.colaboradores = d.colaboradores.map(x => x.id === usuario.id ? {
        ...x,
        ...atualizado
      } : x);
      registrarLog(d, usuario, 'Alterou a própria senha', '');
      return d;
    });
    setOk(true);
  }
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Alterar senha",
    onClose: onClose
  }, ok ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600,
      marginBottom: 12
    }
  }, "✓ Senha alterada com sucesso."), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: onClose
  }, "Fechar"))) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Usuário: ", /*#__PURE__*/React.createElement("strong", null, usuario.nome)), temSenhaDefinida(usuario) && /*#__PURE__*/React.createElement(Field, {
    label: "Senha atual"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: atual,
    onChange: e => {
      setAtual(e.target.value);
      setErro('');
    }
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nova senha"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: nova1,
    onChange: e => {
      setNova1(e.target.value);
      setErro('');
    }
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Repita a nova senha"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: nova2,
    onChange: e => {
      setNova2(e.target.value);
      setErro('');
    },
    onKeyDown: e => {
      if (e.key === 'Enter') salvar();
    }
  })), nova1 && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: forcaSenha(nova1).ok ? 'var(--ok)' : 'var(--warn)',
      marginBottom: 8
    }
  }, forcaSenha(nova1).msg), erro && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      marginBottom: 10,
      fontWeight: 600
    }
  }, erro), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: salvar,
    disabled: ocupado
  }, ocupado ? 'Salvando…' : 'Alterar senha'))));
}
/* ==========================================================
   LOGIN
========================================================== */
function Login({
  db,
  update,
  onEntrar,
  onCanal
}) {
  const [nomeId, setNomeId] = useState('');
  const [senha, setSenha] = useState('');
  const [erro, setErro] = useState('');
  const [ocupado, setOcupado] = useState(false);
  // primeiro acesso / troca obrigatória
  const [definindo, setDefinindo] = useState(null); // colaborador
  const [nova1, setNova1] = useState('');
  const [nova2, setNova2] = useState('');
  const ativos = db.colaboradores.filter(c => c.status !== 'Inativo');
  const selecionado = db.colaboradores.find(x => x.id === nomeId);
  function registrar(colab, acao, detalhe) {
    update(d => {
      registrarLog(d, colab ? {
        nome: colab.nome,
        perfil: colab.perfil
      } : null, acao, detalhe);
      return d;
    });
  }

  // bloqueio temporário por tentativas seguidas
  function bloqueioAtivo(c) {
    if (!c || !c.bloqueadoAte) return null;
    const agora = new Date();
    const ate = new Date(c.bloqueadoAte);
    return ate > agora ? ate : null;
  }
  async function entrar() {
    setErro('');
    const c = selecionado;
    if (!c) {
      setErro('Selecione um usuário.');
      return;
    }
    if (c.status === 'Inativo') {
      setErro('Usuário inativo — acesso bloqueado. Procure um administrador.');
      registrar(c, 'Acesso negado', 'usuário inativo');
      return;
    }
    const ate = bloqueioAtivo(c);
    if (ate) {
      setErro(`Muitas tentativas. Tente novamente após ${ate.toLocaleTimeString('pt-BR').slice(0, 5)}.`);
      return;
    }
    if (!temSenhaDefinida(c)) {
      setDefinindo(c); // primeiro acesso: define a senha agora
      return;
    }
    setOcupado(true);
    const r = await conferirSenha(c, senha);
    setOcupado(false);
    if (!r.ok) {
      const tentativas = num(c.tentativasFalhas) + 1;
      update(d => {
        d.colaboradores = d.colaboradores.map(x => {
          if (x.id !== c.id) return x;
          const bloq = tentativas >= MAX_TENTATIVAS ? new Date(Date.now() + BLOQUEIO_MIN * 60000).toISOString() : x.bloqueadoAte;
          return {
            ...x,
            tentativasFalhas: tentativas,
            bloqueadoAte: bloq
          };
        });
        registrarLog(d, {
          nome: c.nome,
          perfil: c.perfil
        }, 'Acesso negado', `senha incorreta (tentativa ${tentativas} de ${MAX_TENTATIVAS})`);
        return d;
      });
      setErro(tentativas >= MAX_TENTATIVAS ? `Senha incorreta. Usuário bloqueado por ${BLOQUEIO_MIN} minutos.` : `Senha incorreta. Restam ${MAX_TENTATIVAS - tentativas} tentativa(s).`);
      return;
    }
    if (c.precisaTrocarSenha) {
      setDefinindo(c);
      return;
    }
    update(d => {
      d.colaboradores = d.colaboradores.map(x => x.id === c.id ? {
        ...x,
        tentativasFalhas: 0,
        bloqueadoAte: null,
        ultimoAcesso: agoraISO(),
        ...(r.migrar ? {} : {})
      } : x);
      registrarLog(d, c, 'Entrou no sistema', `perfil ${c.perfil || 'Colaborador'}`);
      return d;
    });

    // migra senha legada em texto puro para hash
    if (r.migrar) {
      const atualizado = await definirSenha(c, senha);
      update(d => {
        d.colaboradores = d.colaboradores.map(x => x.id === c.id ? {
          ...x,
          ...atualizado
        } : x);
        registrarLog(d, c, 'Senha migrada', 'senha antiga convertida para hash');
        return d;
      });
    }
    onEntrar(c);
  }
  async function salvarNovaSenha() {
    const v = forcaSenha(nova1);
    if (!v.ok) {
      setErro(v.msg);
      return;
    }
    if (nova1 !== nova2) {
      setErro('As senhas não conferem.');
      return;
    }
    setOcupado(true);
    const atualizado = await definirSenha(definindo, nova1);
    setOcupado(false);
    update(d => {
      d.colaboradores = d.colaboradores.map(x => x.id === definindo.id ? {
        ...x,
        ...atualizado,
        tentativasFalhas: 0,
        bloqueadoAte: null,
        ultimoAcesso: agoraISO()
      } : x);
      registrarLog(d, definindo, 'Senha definida', 'primeiro acesso ou troca obrigatória');
      registrarLog(d, definindo, 'Entrou no sistema', `perfil ${definindo.perfil || 'Colaborador'}`);
      return d;
    });
    onEntrar({
      ...definindo,
      ...atualizado
    });
  }
  const semSenhaAinda = selecionado && !temSenhaDefinida(selecionado);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      maxWidth: 420
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'center',
      marginBottom: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--mono)',
      fontSize: 10,
      letterSpacing: '.16em',
      color: 'var(--thread-dark)',
      textTransform: 'uppercase'
    }
  }, "Sistema Interno"), /*#__PURE__*/React.createElement("h1", {
    style: {
      fontFamily: 'var(--display)',
      fontSize: 30,
      margin: '4px 0 0 0'
    }
  }, "Confecção ERP")), definindo ? /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Definir senha — ", definindo.nome), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, temSenhaDefinida(definindo) ? 'É necessário trocar a senha para continuar.' : 'Primeiro acesso: crie sua senha para entrar no sistema.', ' ', "Mínimo de 6 caracteres, com letras e números."), /*#__PURE__*/React.createElement(Field, {
    label: "Nova senha"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: nova1,
    onChange: e => {
      setNova1(e.target.value);
      setErro('');
    }
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Repita a nova senha"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: nova2,
    onChange: e => {
      setNova2(e.target.value);
      setErro('');
    },
    onKeyDown: e => {
      if (e.key === 'Enter') salvarNovaSenha();
    }
  })), nova1 && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: forcaSenha(nova1).ok ? 'var(--ok)' : 'var(--warn)',
      marginBottom: 8
    }
  }, forcaSenha(nova1).msg), erro && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      marginBottom: 10,
      fontWeight: 600
    }
  }, erro), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => {
      setDefinindo(null);
      setNova1('');
      setNova2('');
      setErro('');
    }
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    onClick: salvarNovaSenha,
    disabled: ocupado
  }, ocupado ? 'Salvando…' : 'Salvar e entrar'))) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Entrar"), /*#__PURE__*/React.createElement(Field, {
    label: "Usuário"
  }, /*#__PURE__*/React.createElement("select", {
    value: nomeId,
    onChange: e => {
      setNomeId(e.target.value);
      setErro('');
      setSenha('');
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Selecione…"), ativos.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome, " — ", c.perfil || 'Colaborador')))), semSenhaAinda ? /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      background: 'var(--warn-bg)',
      color: 'var(--warn)',
      padding: '8px 10px',
      borderRadius: 6,
      marginBottom: 10,
      fontWeight: 600
    }
  }, "Este usuário ainda não tem senha. Clique em “Definir senha” para criar a sua no primeiro acesso.") : /*#__PURE__*/React.createElement(Field, {
    label: "Senha"
  }, /*#__PURE__*/React.createElement("input", {
    type: "password",
    value: senha,
    onChange: e => {
      setSenha(e.target.value);
      setErro('');
    },
    onKeyDown: e => {
      if (e.key === 'Enter') entrar();
    },
    placeholder: "••••••"
  })), erro && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      marginBottom: 10,
      fontWeight: 600
    }
  }, erro), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    style: {
      width: '100%',
      justifyContent: 'center'
    },
    onClick: entrar,
    disabled: ocupado || !nomeId
  }, ocupado ? 'Verificando…' : semSenhaAinda ? 'Definir senha' : 'Entrar')), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      textAlign: 'center',
      marginTop: 12
    }
  }, "Senhas são guardadas com hash, nunca em texto puro. Após ", MAX_TENTATIVAS, " tentativas o usuário fica bloqueado por ", BLOQUEIO_MIN, " minutos."), /*#__PURE__*/React.createElement("div", {
    className: "divisor-login"
  }, /*#__PURE__*/React.createElement("span", null, "ou")), /*#__PURE__*/React.createElement("button", {
    className: "porta-canal",
    onClick: onCanal
  }, /*#__PURE__*/React.createElement("span", {
    className: "ic"
  }, "💬"), /*#__PURE__*/React.createElement("span", {
    className: "texto"
  }, /*#__PURE__*/React.createElement("strong", null, "Conversa aberta"), /*#__PURE__*/React.createElement("span", {
    className: "lema"
  }, "Fale. Sugira. Aponte. Melhore.")), /*#__PURE__*/React.createElement("span", {
    className: "seta"
  }, "→")), /*#__PURE__*/React.createElement("div", {
    className: "small muted porta-nota"
  }, "Sugestão, problema, risco ou relato — sem precisar entrar no sistema.")));
}
function SubTabs({
  tabs,
  active,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "tabs-strip",
    style: {
      marginBottom: 18
    }
  }, tabs.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.id,
    className: active === t.id ? 'active' : '',
    onClick: () => onChange(t.id)
  }, t.label)));
}

/* ==========================================================
   MÓDULO DE MATERIAIS — interface
========================================================== */
function GrupoMateriais({
  db,
  update,
  usuario,
  perm
}) {
  const [sub, setSub] = useState('materiais');
  const podeEditar = podeExecutar(usuario, 'cadastros');
  const abaixo = (db.materiais || []).filter(m => m.ativo !== false).filter(m => ['abaixo', 'zerado'].includes(situacaoMaterial(m, db).id)).length;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Suprimentos"), /*#__PURE__*/React.createElement("h2", null, "Materiais e estoque"))), /*#__PURE__*/React.createElement(SubTabs, {
    active: sub,
    onChange: setSub,
    tabs: [{
      id: 'materiais',
      label: `Materiais (${(db.materiais || []).filter(m => m.ativo !== false).length})`
    }, {
      id: 'posicao',
      label: `Posição de estoque${abaixo ? ` ⚠ ${abaixo}` : ''}`
    }, {
      id: 'movimentos',
      label: `Movimentações (${(db.movimentacoes || []).length})`
    }, {
      id: 'reservas',
      label: `Reservas (${(db.reservas || []).filter(r => r.status === 'Ativa').length})`
    }]
  }), sub === 'materiais' && /*#__PURE__*/React.createElement(Materiais, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'posicao' && /*#__PURE__*/React.createElement(PosicaoEstoque, {
    db: db,
    update: update,
    usuario: usuario
  }), sub === 'movimentos' && /*#__PURE__*/React.createElement(Movimentacoes, {
    db: db,
    update: update,
    usuario: usuario
  }), sub === 'reservas' && /*#__PURE__*/React.createElement(Reservas, {
    db: db,
    update: update,
    usuario: usuario
  }));
}

/* ---------------- Cadastro de materiais ---------------- */
function Materiais({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [modal, setModal] = useState(null);
  const [q, setQ] = useState('');
  const [fGrupo, setFGrupo] = useState('');
  const [fSit, setFSit] = useState('');
  const [verInativos, setVerInativos] = useState(false);
  const [de, setDe] = useState('');
  const [ate, setAte] = useState('');
  const [campoData, setCampoData] = useState('cadastro');
  const [pagina, setPagina] = useState(1);

  /* o período pode olhar duas datas diferentes: quando o material entrou
     no cadastro, ou quando teve movimento pela última vez */
  const ultimoMovimento = React.useMemo(() => {
    const mapa = {};
    (db.movimentacoes || []).forEach(mv => {
      const d = String(mv.data || mv.criadoEm || '').slice(0, 10);
      if (!d) return;
      if (!mapa[mv.materialId] || d > mapa[mv.materialId]) mapa[mv.materialId] = d;
    });
    return mapa;
  }, [db.movimentacoes]);
  const dataDoMaterial = m => campoData === 'movimento' ? ultimoMovimento[m.id] || '' : String(m.criadoEm || '').slice(0, 10);
  const dentroDoPeriodo = m => {
    if (!de && !ate) return true;
    const d = dataDoMaterial(m);
    if (!d) return false;
    if (de && d < de) return false;
    if (ate && d > ate) return false;
    return true;
  };
  const porPagina = 25;
  const grupos = (db.gruposMaterial || []).filter(g => !g.paiId);
  const nomeGrupo = id => {
    const g = (db.gruposMaterial || []).find(x => x.id === id);
    if (!g) return '';
    const pai = g.paiId ? (db.gruposMaterial || []).find(x => x.id === g.paiId) : null;
    return pai ? `${pai.nome} → ${g.nome}` : g.nome;
  };
  const lista = (db.materiais || []).filter(m => verInativos ? true : m.ativo !== false).filter(m => {
    const t = normaliza(q);
    if (t && !normaliza(m.nome).includes(t) && !normaliza(m.codigo || '').includes(t) && !normaliza(m.sku || '').includes(t) && !normaliza(m.codigoBarras || '').includes(t)) return false;
    if (fGrupo) {
      const g = (db.gruposMaterial || []).find(x => x.id === m.grupoId);
      if (!g) return false;
      if (g.id !== fGrupo && g.paiId !== fGrupo) return false;
    }
    if (fSit && situacaoMaterial(m, db).id !== fSit) return false;
    if (!dentroDoPeriodo(m)) return false;
    return true;
  }).sort((a, b) => String(a.nome).localeCompare(String(b.nome)));
  const totalPag = Math.max(Math.ceil(lista.length / porPagina), 1);
  const pag = Math.min(pagina, totalPag);
  const visiveis = lista.slice((pag - 1) * porPagina, pag * porPagina);
  function salvar(mat) {
    if (!String(mat.nome || '').trim()) return alert('Informe o nome do material.');
    if (!mat.unidadeEstoque) return alert('Todo material precisa de uma unidade de estoque.');
    const erro = checarDuplicidade(db.materiais || [], mat, [{
      key: 'codigo',
      label: 'Código'
    }, {
      key: 'sku',
      label: 'SKU'
    }, {
      key: 'codigoBarras',
      label: 'Código de barras'
    }]);
    if (erro) return alert(erro);
    const novo = !mat.id;
    /* o estoque inicial é um campo do formulário, não do cadastro:
       vira movimentação e some do registro */
    const qtdInicial = novo ? num(mat.estoqueInicial) : 0;
    const custoInicial = num(mat.custoInicial) || num(mat.precoCompra);
    const localInicial = mat.estoqueInicialLocal || ((db.estoques || []).filter(x => x.ativo !== false)[0] || {}).id;
    update(d => {
      const reg = {
        ...mat,
        id: mat.id || uid(),
        codigo: mat.codigo || codigoMaterial(d, mat.grupoId),
        criadoEm: mat.criadoEm || agoraISO()
      };
      delete reg.estoqueInicial;
      delete reg.custoInicial;
      delete reg.estoqueInicialLocal;
      delete reg._aba;
      d.materiais = novo ? [...(d.materiais || []), reg] : d.materiais.map(x => x.id === reg.id ? reg : x);
      registrarLog(d, usuario, novo ? 'Cadastrou material' : 'Editou material', `${reg.codigo} · ${reg.nome}`);
      if (qtdInicial > 0 && localInicial) {
        const r = aplicarMovimento(d, {
          materialId: reg.id,
          estoqueId: localInicial,
          tipo: 'entrada_ajuste',
          quantidade: qtdInicial,
          custoUnitario: custoInicial,
          motivo: 'Saldo inicial do cadastro'
        }, usuario);
        if (r.erro) alert('Material salvo, mas o estoque inicial não entrou: ' + r.erro);
        /* ajuste não forma custo médio — mas o saldo inicial é a primeira
           compra na prática, então o custo informado passa a valer */else if (custoInicial > 0) {
          d.materiais = d.materiais.map(x => x.id === reg.id ? {
            ...x,
            custoMedio: custoInicial,
            ultimoCusto: custoInicial
          } : x);
        }
      }
      return d;
    });
    setModal(null);
  }
  function inativar(m) {
    const saldo = saldoTotal(m.id, db);
    if (m.ativo !== false && saldo.fisico > 0 && !confirm(`${m.nome} ainda tem ${fmtQtd(saldo.fisico, m.unidadeEstoque, db)} em estoque. Inativar mesmo assim?`)) return;
    update(d => {
      d.materiais = d.materiais.map(x => x.id === m.id ? {
        ...x,
        ativo: x.ativo === false
      } : x);
      registrarLog(d, usuario, m.ativo === false ? 'Reativou material' : 'Inativou material', m.nome);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, lista.length, " material(is)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({
      ativo: true,
      unidadeEstoque: 'UN'
    })
  }, "+ Novo material")), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Buscar"
  }, /*#__PURE__*/React.createElement("input", {
    value: q,
    onChange: e => {
      setQ(e.target.value);
      setPagina(1);
    },
    placeholder: "nome, código, SKU, código de barras…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Grupo"
  }, /*#__PURE__*/React.createElement("select", {
    value: fGrupo,
    onChange: e => {
      setFGrupo(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), grupos.map(g => /*#__PURE__*/React.createElement("option", {
    key: g.id,
    value: g.id
  }, g.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Situação do estoque"
  }, /*#__PURE__*/React.createElement("select", {
    value: fSit,
    onChange: e => {
      setFSit(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas"), /*#__PURE__*/React.createElement("option", {
    value: "zerado"
  }, "Sem estoque"), /*#__PURE__*/React.createElement("option", {
    value: "abaixo"
  }, "Abaixo do mínimo"), /*#__PURE__*/React.createElement("option", {
    value: "proximo"
  }, "Próximo do mínimo"), /*#__PURE__*/React.createElement("option", {
    value: "excesso"
  }, "Acima do máximo"), /*#__PURE__*/React.createElement("option", {
    value: "ok"
  }, "Normal")))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Período por"
  }, /*#__PURE__*/React.createElement("select", {
    value: campoData,
    onChange: e => {
      setCampoData(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: "cadastro"
  }, "Data de cadastro"), /*#__PURE__*/React.createElement("option", {
    value: "movimento"
  }, "Último movimento"))), /*#__PURE__*/React.createElement(Field, {
    label: "De"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: de,
    onChange: e => {
      setDe(e.target.value);
      setPagina(1);
    }
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Até"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: ate,
    onChange: e => {
      setAte(e.target.value);
      setPagina(1);
    }
  }))), (de || ate) && /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 200
    }
  }, campoData === 'movimento' ? 'Materiais sem nenhum movimento ficam de fora do período.' : 'Materiais cadastrados antes do sistema podem não ter data.'), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setDe('');
      setAte('');
      setPagina(1);
    }
  }, "Limpar período")), /*#__PURE__*/React.createElement("label", {
    className: "small",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    style: {
      width: 'auto'
    },
    checked: verInativos,
    onChange: e => setVerInativos(e.target.checked)
  }), "Mostrar materiais inativos")), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum material encontrado."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Código"), /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", null, "Grupo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Físico"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Disponível"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Mínimo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo médio"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 100
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, visiveis.map(m => {
    const st = situacaoMaterial(m, db);
    const parado = diasSemMovimento(m.id, db);
    const limite = num((db.parametrosMateriais || {}).diasSemMovimento) || 60;
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id,
      className: st.tone === 'bad' ? 'linha-atraso' : '',
      style: m.ativo === false ? {
        opacity: .55
      } : {}
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, m.codigo), m.ativo === false && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "inativo")), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, m.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, [m.cor, m.gramatura && `${m.gramatura}g`, m.largura && `${m.largura}m`, m.composicao].filter(Boolean).join(' · '))), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, nomeGrupo(m.grupoId) || '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, fmtQtd(st.saldo.fisico, m.unidadeEstoque, db)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, fmtQtd(st.saldo.disponivel, m.unidadeEstoque, db)), st.saldo.reservado > 0 && /*#__PURE__*/React.createElement("div", {
      className: "muted",
      style: {
        fontSize: 10
      }
    }, fmtQtd(st.saldo.reservado, '', db), " reservado")), /*#__PURE__*/React.createElement("td", {
      className: "num small muted"
    }, num(m.estoqueMinimo) > 0 ? fmtQtd(m.estoqueMinimo, '', db) : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, (() => {
      const oc = origemDoCusto(m);
      if (oc.valor === 0) return /*#__PURE__*/React.createElement("span", {
        className: "muted"
      }, "—");
      return /*#__PURE__*/React.createElement(React.Fragment, null, money(oc.valor), oc.origem === 'cadastro' && /*#__PURE__*/React.createElement("div", {
        className: "muted",
        style: {
          fontSize: 9.5
        }
      }, "de cadastro"));
    })()), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
      tone: st.tone
    }, st.label), parado !== null && parado >= limite && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "parado há ", parado, "d")), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("div", {
      className: "acoes-icone"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      title: "Ficha e histórico",
      onClick: () => setModal({
        ...m,
        _aba: 'ficha'
      })
    }, "👁"), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      title: "Editar",
      onClick: () => setModal(m)
    }, "✎"), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      title: m.ativo === false ? 'Reativar' : 'Inativar',
      onClick: () => inativar(m)
    }, m.ativo === false ? '↺' : '⊘'))));
  })))), totalPag > 1 && /*#__PURE__*/React.createElement("div", {
    className: "row-actions no-print",
    style: {
      justifyContent: 'center',
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: pag <= 1,
    onClick: () => setPagina(pag - 1)
  }, "← Anterior"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "página ", pag, " de ", totalPag), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: pag >= totalPag,
    onClick: () => setPagina(pag + 1)
  }, "Próxima →"))), modal && /*#__PURE__*/React.createElement(MaterialModal, {
    mat: modal,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    onClose: () => setModal(null),
    onSave: salvar
  }));
}

/* Código automático: prefixo do grupo + sequencial */
function codigoMaterial(d, grupoId) {
  const g = (d.gruposMaterial || []).find(x => x.id === grupoId);
  const pai = g && g.paiId ? (d.gruposMaterial || []).find(x => x.id === g.paiId) : g;
  const sigla = t => normaliza(t || 'MAT').replace(/[^a-z]/g, '').slice(0, 3).toUpperCase() || 'MAT';
  const pref = g ? `${sigla(pai && pai.nome)}-${sigla(g.nome)}` : 'MAT';
  const usados = (d.materiais || []).map(m => String(m.codigo || '')).filter(c => c.startsWith(pref + '-')).map(c => parseInt(c.slice(pref.length + 1), 10)).filter(n => !isNaN(n));
  return `${pref}-${String((usados.length ? Math.max(...usados) : 0) + 1).padStart(4, '0')}`;
}

/* ---------------- Ajuste manual de estoque ----------------
   O caminho natural é "contei X, o sistema diz Y": o operador informa o
   que existe e o sistema deduz se é entrada ou saída. Digitar diferença
   com sinal é fonte de erro. */
function AjusteDeEstoque({
  material,
  db,
  usuario,
  onAjustar
}) {
  const mat = material || {};
  const locais = (db.estoques || []).filter(x => x.ativo !== false);
  const [estoqueId, setEstoqueId] = useState((locais[0] || {}).id || '');
  const [contado, setContado] = useState('');
  const [motivo, setMotivo] = useState('');
  const linha = (db.saldos || []).find(x => x.materialId === mat.id && x.estoqueId === estoqueId);
  const atual = num(linha && linha.fisico);
  const informado = contado === '' ? null : num(contado);
  const diferenca = informado === null ? 0 : Number((informado - atual).toFixed(4));
  const reservado = num(linha && linha.reservado);
  /* não dá para baixar abaixo do que já está prometido a uma ordem */
  const abaixoDoReservado = informado !== null && informado < reservado;
  const pronto = informado !== null && diferenca !== 0 && motivo.trim() && !abaixoDoReservado;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Local"
  }, /*#__PURE__*/React.createElement("select", {
    value: estoqueId,
    onChange: e => setEstoqueId(e.target.value)
  }, locais.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Sistema diz"
  }, /*#__PURE__*/React.createElement("input", {
    value: fmtQtd(atual, mat.unidadeEstoque, db),
    disabled: true
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Contagem física *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    min: "0",
    value: contado,
    placeholder: "0",
    onChange: e => setContado(e.target.value)
  }))), informado !== null && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginTop: -4,
      marginBottom: 8,
      color: diferenca === 0 ? '#8a8577' : diferenca > 0 ? 'var(--ok)' : 'var(--danger)',
      fontWeight: 600
    }
  }, diferenca === 0 ? 'A contagem bate com o sistema — nada a lançar.' : diferenca > 0 ? `Sobra de ${fmtQtd(diferenca, mat.unidadeEstoque, db)} — entrará como ajuste positivo.` : `Falta de ${fmtQtd(Math.abs(diferenca), mat.unidadeEstoque, db)} — sairá como ajuste negativo.`), abaixoDoReservado && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      marginTop: -4,
      marginBottom: 8
    }
  }, "⚠ Há ", fmtQtd(reservado, mat.unidadeEstoque, db), " reservado(s) para ordens neste local. A contagem não pode ficar abaixo disso sem liberar as reservas."), /*#__PURE__*/React.createElement(Field, {
    label: "Motivo do ajuste *"
  }, /*#__PURE__*/React.createElement("input", {
    value: motivo,
    placeholder: "inventário de janeiro, perda no corte, sobra de rolo…",
    onChange: e => setMotivo(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    disabled: !pronto,
    onClick: () => {
      onAjustar({
        materialId: mat.id,
        estoqueId,
        tipo: diferenca > 0 ? 'entrada_ajuste' : 'saida_ajuste',
        quantidade: Math.abs(diferenca),
        custoUnitario: num(mat.custoMedio) || num(mat.precoCompra),
        motivo: motivo.trim()
      });
      setContado('');
      setMotivo('');
    }
  }, "Lançar ajuste"), informado !== null && diferenca !== 0 && !motivo.trim() && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "informe o motivo para lançar")));
}
function MaterialModal({
  mat,
  db,
  update,
  usuario,
  podeEditar,
  onClose,
  onSave
}) {
  const [aba, setAba] = useState(mat._aba || 'dados');
  const [f, setF] = useState({
    codigo: '',
    nome: '',
    descricao: '',
    sku: '',
    codigoBarras: '',
    grupoId: '',
    marca: '',
    modelo: '',
    cor: '',
    pantone: '',
    tamanho: '',
    composicao: '',
    gramatura: '',
    largura: '',
    espessura: '',
    rendimento: '',
    tipoTecido: '',
    tubular: '',
    unidadeEstoque: 'UN',
    unidadeCompra: '',
    unidadeConsumo: '',
    estoqueMinimo: 0,
    estoqueMaximo: 0,
    pontoReposicao: 0,
    leadTimeDias: 0,
    custoMedio: 0,
    ultimoCusto: 0,
    precoCompra: '',
    fornecedorPadraoId: '',
    precoValidade: '',
    controlaLote: false,
    controlaValidade: false,
    localizacao: '',
    observacoes: '',
    ativo: true,
    ...mat
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const grupos = (db.gruposMaterial || []).filter(g => !g.paiId);
  const subgrupos = (db.gruposMaterial || []).filter(g => g.paiId);
  const unidades = (db.unidades || []).filter(u => u.ativo !== false);
  const ehTecido = (() => {
    const g = (db.gruposMaterial || []).find(x => x.id === f.grupoId);
    const pai = g && g.paiId ? (db.gruposMaterial || []).find(x => x.id === g.paiId) : g;
    return normaliza(pai && pai.nome || '').includes('tecid');
  })();
  const st = mat.id ? situacaoMaterial(f, db) : null;
  const movs = mat.id ? (db.movimentacoes || []).filter(m => m.materialId === mat.id).slice().sort((a, b) => String(b.quando).localeCompare(String(a.quando))) : [];
  return /*#__PURE__*/React.createElement(Modal, {
    title: mat.id ? `${f.codigo} · ${f.nome}` : 'Novo material',
    onClose: onClose,
    wide: true
  }, mat.id && /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'dados',
      label: 'Dados'
    }, {
      id: 'ficha',
      label: 'Posição e custos'
    }, {
      id: 'historico',
      label: `Movimentações (${movs.length})`
    }]
  }), aba === 'dados' && !mat.id && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, "Estoque atual"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      margin: '3px 0 8px'
    }
  }, "O que já existe na prateleira hoje. Vira a primeira entrada do material, registrada como saldo inicial — deixe em branco se for comprar depois."), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    min: "0",
    value: f.estoqueInicial || '',
    placeholder: "0",
    disabled: !podeEditar,
    onChange: e => set('estoqueInicial', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Custo unitário"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    min: "0",
    value: f.custoInicial || '',
    placeholder: f.precoCompra ? String(f.precoCompra) : '0,00',
    disabled: !podeEditar,
    onChange: e => set('custoInicial', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Local"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.estoqueInicialLocal || (db.estoques || [])[0]?.id || '',
    disabled: !podeEditar,
    onChange: e => set('estoqueInicialLocal', e.target.value)
  }, (db.estoques || []).filter(x => x.ativo !== false).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome))))), num(f.estoqueInicial) > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Entra ", fmtQtd(num(f.estoqueInicial), f.unidadeEstoque, db), ' ', "a ", money(num(f.custoInicial) || num(f.precoCompra)), " =", ' ', /*#__PURE__*/React.createElement("strong", null, money(num(f.estoqueInicial) * (num(f.custoInicial) || num(f.precoCompra)))), " em estoque.")), aba === 'dados' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Código"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.codigo,
    onChange: e => set('codigo', e.target.value.toUpperCase()),
    placeholder: "gerado automaticamente",
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome do material *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Grupo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.grupoId,
    onChange: e => set('grupoId', e.target.value),
    disabled: !podeEditar
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), grupos.map(g => /*#__PURE__*/React.createElement("optgroup", {
    key: g.id,
    label: g.nome
  }, /*#__PURE__*/React.createElement("option", {
    value: g.id
  }, g.nome, " (geral)"), subgrupos.filter(x => x.paiId === g.id).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))))), /*#__PURE__*/React.createElement(Field, {
    label: "SKU"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.sku,
    onChange: e => set('sku', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Código de barras"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.codigoBarras,
    onChange: e => set('codigoBarras', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Marca"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.marca,
    onChange: e => set('marca', e.target.value),
    disabled: !podeEditar
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Descrição"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.descricao,
    onChange: e => set('descricao', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement("h3", null, "Unidades"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Unidade de estoque *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.unidadeEstoque,
    onChange: e => set('unidadeEstoque', e.target.value),
    disabled: !podeEditar
  }, unidades.map(u => /*#__PURE__*/React.createElement("option", {
    key: u.id,
    value: u.sigla
  }, u.sigla, " · ", u.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Unidade de compra"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.unidadeCompra,
    onChange: e => set('unidadeCompra', e.target.value),
    disabled: !podeEditar
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "igual à de estoque"), unidades.map(u => /*#__PURE__*/React.createElement("option", {
    key: u.id,
    value: u.sigla
  }, u.sigla, " · ", u.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Unidade de consumo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.unidadeConsumo,
    onChange: e => set('unidadeConsumo', e.target.value),
    disabled: !podeEditar
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "igual à de estoque"), unidades.map(u => /*#__PURE__*/React.createElement("option", {
    key: u.id,
    value: u.sigla
  }, u.sigla, " · ", u.nome))))), f.unidadeCompra && f.unidadeCompra !== f.unidadeEstoque && (() => {
    const c = converter(1, f.unidadeCompra, f.unidadeEstoque, db, f.id);
    return c === null ? /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginTop: -6,
        marginBottom: 10
      }
    }, "⚠ Não há conversão de ", f.unidadeCompra, " para ", f.unidadeEstoque, ". Cadastre em Grupos, unidades e locais → Conversões, senão a compra não entra no estoque.") : /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -6,
        marginBottom: 10
      }
    }, "1 ", f.unidadeCompra, " = ", c, " ", f.unidadeEstoque);
  })(), ehTecido && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "Características do tecido"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Tipo de tecido"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.tipoTecido,
    onChange: e => set('tipoTecido', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Cor"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.cor,
    onChange: e => set('cor', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Pantone"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.pantone,
    onChange: e => set('pantone', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Composição"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.composicao,
    onChange: e => set('composicao', e.target.value),
    placeholder: "100% algodão",
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Gramatura (g/m²)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.gramatura,
    onChange: e => set('gramatura', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Largura (m)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    value: f.largura,
    onChange: e => set('largura', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Rendimento (m/kg)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    value: f.rendimento,
    onChange: e => set('rendimento', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Acabamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tubular,
    onChange: e => set('tubular', e.target.value),
    disabled: !podeEditar
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), /*#__PURE__*/React.createElement("option", {
    value: "Tubular"
  }, "Tubular"), /*#__PURE__*/React.createElement("option", {
    value: "Aberto"
  }, "Aberto"))))), !ehTecido && /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Cor"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.cor,
    onChange: e => set('cor', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Tamanho"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.tamanho,
    onChange: e => set('tamanho', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Modelo"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.modelo,
    onChange: e => set('modelo', e.target.value),
    disabled: !podeEditar
  }))), /*#__PURE__*/React.createElement("h3", null, "Preço"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: `Preço do material (R$/${f.unidadeEstoque || 'un'})`
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    min: "0",
    value: f.precoCompra,
    onChange: e => set('precoCompra', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Fornecedor de referência"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.fornecedorPadraoId,
    onChange: e => set('fornecedorPadraoId', e.target.value),
    disabled: !podeEditar
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.fornecedores || []).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Preço válido até"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.precoValidade,
    onChange: e => set('precoValidade', e.target.value),
    disabled: !podeEditar
  }))), (() => {
    if (num(f.custoMedio) > 0) return /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 10
      }
    }, "Este material já tem ", /*#__PURE__*/React.createElement("strong", null, money(f.custoMedio)), " de custo médio, calculado pelas entradas. O preço acima serve de referência para compras, mas quem manda no custo é o histórico.");
    if (num(f.precoCompra) > 0) return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--ok)',
        fontWeight: 600,
        marginTop: -4,
        marginBottom: 10
      }
    }, "Sem compras ainda — o custo do produto usará ", money(f.precoCompra), "/", f.unidadeEstoque || 'un', ". Na primeira entrada, o custo médio real assume o lugar.");
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginTop: -4,
        marginBottom: 10
      }
    }, "⚠ Sem preço informado e sem compras: este material entra como R$ 0,00 no custo dos produtos.");
  })(), /*#__PURE__*/React.createElement("h3", null, "Estoque"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Estoque mínimo"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    value: f.estoqueMinimo,
    onChange: e => set('estoqueMinimo', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Estoque máximo"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    value: f.estoqueMaximo,
    onChange: e => set('estoqueMaximo', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Ponto de reposição"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    value: f.pontoReposicao,
    onChange: e => set('pontoReposicao', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Prazo de entrega (dias)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.leadTimeDias,
    onChange: e => set('leadTimeDias', e.target.value),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Localização"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.localizacao,
    onChange: e => set('localizacao', e.target.value),
    placeholder: "Rua A · Prateleira 03",
    disabled: !podeEditar
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 16,
      flexWrap: 'wrap',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "small",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    style: {
      width: 'auto'
    },
    checked: !!f.controlaLote,
    onChange: e => set('controlaLote', e.target.checked),
    disabled: !podeEditar
  }), "Controlar por lote"), /*#__PURE__*/React.createElement("label", {
    className: "small",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    style: {
      width: 'auto'
    },
    checked: !!f.controlaValidade,
    onChange: e => set('controlaValidade', e.target.checked),
    disabled: !podeEditar
  }), "Controlar validade")), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacoes,
    onChange: e => set('observacoes', e.target.value),
    disabled: !podeEditar
  }))), aba === 'ficha' && st && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Estoque físico"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, fmtQtd(st.saldo.fisico, f.unidadeEstoque, db))), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Reservado"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, fmtQtd(st.saldo.reservado, '', db))), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Disponível"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, fmtQtd(st.saldo.disponivel, '', db))), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Valor em estoque"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, money(st.saldo.fisico * num(f.custoMedio))))), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, "Ajuste manual de estoque"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      margin: '3px 0 8px'
    }
  }, "Para acertar o sistema com a contagem física. Informe quanto existe de verdade e o sistema lança a diferença — sobra vira entrada, falta vira saída, e tudo fica no histórico com o motivo."), /*#__PURE__*/React.createElement(AjusteDeEstoque, {
    material: f,
    db: db,
    usuario: usuario,
    onAjustar: dados => update(d => {
      const r = aplicarMovimento(d, dados, usuario);
      if (r.erro) alert(r.erro);
      return d;
    })
  })), /*#__PURE__*/React.createElement("h3", null, "Por local de estoque"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Físico"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Reservado"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Disponível"))), /*#__PURE__*/React.createElement("tbody", null, (db.saldos || []).filter(x => x.materialId === f.id).map(x => {
    const e = (db.estoques || []).find(y => y.id === x.estoqueId);
    return /*#__PURE__*/React.createElement("tr", {
      key: x.estoqueId
    }, /*#__PURE__*/React.createElement("td", null, e ? e.nome : '(local removido)'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, fmtQtd(x.fisico, '', db)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, fmtQtd(x.reservado, '', db)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, fmtQtd(num(x.fisico) - num(x.reservado) - num(x.comprometido), '', db))));
  }), (db.saldos || []).filter(x => x.materialId === f.id).length === 0 && /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    colSpan: "4",
    className: "small muted"
  }, "Sem saldo em nenhum local.")))), /*#__PURE__*/React.createElement("h3", null, "Custos"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Custo médio"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(f.custoMedio)))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Último custo"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(f.ultimoCusto))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Menor custo"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(f.menorCusto))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Maior custo"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(f.maiorCusto))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Última compra"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, f.ultimaCompra ? fmtDate(f.ultimaCompra) : '—'))))), aba === 'historico' && (movs.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma movimentação registrada."
  }) : /*#__PURE__*/React.createElement("div", {
    style: {
      maxHeight: 420,
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Quando"), /*#__PURE__*/React.createElement("th", null, "Movimento"), /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Qtd."), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"), /*#__PURE__*/React.createElement("th", null, "Documento"), /*#__PURE__*/React.createElement("th", null, "Usuário"))), /*#__PURE__*/React.createElement("tbody", null, movs.map(m => {
    const t = tipoMov(m.tipo) || {
      nome: m.tipo,
      sinal: 0
    };
    const e = (db.estoques || []).find(x => x.id === m.estoqueId);
    const estornada = (db.movimentacoes || []).some(x => x.estornoDe === m.id);
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id,
      style: estornada ? {
        opacity: .5
      } : {}
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, m.quando, /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, m.numero)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: t.sinal > 0 ? 'var(--ok)' : 'var(--bad)',
        fontWeight: 600
      }
    }, t.sinal > 0 ? '▲' : '▼', " ", t.nome), estornada && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "estornada"), m.motivo && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, m.motivo)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, e ? e.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: {
        color: t.sinal > 0 ? 'var(--ok)' : 'var(--bad)'
      }
    }, t.sinal > 0 ? '+' : '−', fmtQtd(m.quantidade, '', db)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, num(m.custoUnitario) > 0 ? money(m.custoUnitario) : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, m.documento || '—'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, m.usuario));
  }))))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Fechar"), podeEditar && aba === 'dados' && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome || !f.unidadeEstoque
  }, "Salvar")));
}

/* ---------------- Posição de estoque ---------------- */
function PosicaoEstoque({
  db,
  update,
  usuario
}) {
  const [fEstoque, setFEstoque] = useState('');
  const [q, setQ] = useState('');
  const [soAlerta, setSoAlerta] = useState(false);
  const materiais = (db.materiais || []).filter(m => m.ativo !== false);
  const linhas = materiais.map(m => {
    const st = situacaoMaterial(m, db);
    const s = fEstoque ? (() => {
      const x = saldoDe(m.id, fEstoque, db);
      return {
        fisico: num(x.fisico),
        reservado: num(x.reservado),
        comprometido: num(x.comprometido),
        transito: num(x.transito),
        disponivel: num(x.fisico) - num(x.reservado) - num(x.comprometido)
      };
    })() : st.saldo;
    return {
      m,
      st,
      s,
      valor: num(s.fisico) * num(m.custoMedio)
    };
  }).filter(l => {
    const t = normaliza(q);
    if (t && !normaliza(l.m.nome).includes(t) && !normaliza(l.m.codigo || '').includes(t)) return false;
    if (soAlerta && !['zerado', 'abaixo', 'proximo'].includes(l.st.id)) return false;
    if (fEstoque && l.s.fisico === 0 && l.s.reservado === 0) return false;
    return true;
  }).sort((a, b) => b.valor - a.valor);
  const valorTotal = linhas.reduce((acc, l) => acc + l.valor, 0);
  const emAlerta = materiais.filter(m => ['zerado', 'abaixo'].includes(situacaoMaterial(m, db).id));
  const limite = num((db.parametrosMateriais || {}).diasSemMovimento) || 60;
  const parados = materiais.filter(m => {
    const dd = diasSemMovimento(m.id, db);
    return dd !== null && dd >= limite;
  });
  function recalcular() {
    if (!confirm('Reconstruir todos os saldos a partir das movimentações?\n\nIsso confere se algum saldo foi alterado por fora do sistema.')) return;
    let div = [];
    update(d => {
      div = recalcularSaldos(d);
      registrarLog(d, usuario, 'Recalculou saldos', `${div.length} divergência(s)`);
      return d;
    });
    alert(div.length === 0 ? 'Todos os saldos conferem com as movimentações.' : `${div.length} saldo(s) corrigido(s). O histórico de movimentações é a fonte da verdade.`);
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Valor em estoque"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, money(valorTotal)), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, linhas.length, " material(is)")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Abaixo do mínimo"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, emAlerta.length)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Parados há ", limite, "+ dias"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, parados.length)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Locais de estoque"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (db.estoques || []).filter(e => e.ativo !== false).length))), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Buscar"
  }, /*#__PURE__*/React.createElement("input", {
    value: q,
    onChange: e => setQ(e.target.value),
    placeholder: "material ou código"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Local de estoque"
  }, /*#__PURE__*/React.createElement("select", {
    value: fEstoque,
    onChange: e => setFEstoque(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos os locais (consolidado)"), (db.estoques || []).filter(e => e.ativo !== false).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      gap: 8,
      paddingBottom: 12
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "small",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    style: {
      width: 'auto'
    },
    checked: soAlerta,
    onChange: e => setSoAlerta(e.target.checked)
  }), "Só os que precisam de atenção"))), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: recalcular
  }, "↻ Recalcular saldos"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => window.print()
  }, "🖨️ Imprimir posição"))), linhas.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nada a mostrar com estes filtros."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Físico"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Reservado"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Disponível"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Mínimo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo médio"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Valor"), /*#__PURE__*/React.createElement("th", null, "Situação"))), /*#__PURE__*/React.createElement("tbody", null, linhas.map(l => /*#__PURE__*/React.createElement("tr", {
    key: l.m.id,
    className: l.st.tone === 'bad' ? 'linha-atraso' : ''
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, l.m.nome), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, l.m.codigo)), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, l.m.localizacao || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, fmtQtd(l.s.fisico, l.m.unidadeEstoque, db)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, l.s.reservado > 0 ? fmtQtd(l.s.reservado, '', db) : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, fmtQtd(l.s.disponivel, '', db))), /*#__PURE__*/React.createElement("td", {
    className: "num small muted"
  }, num(l.m.estoqueMinimo) > 0 ? fmtQtd(l.m.estoqueMinimo, '', db) : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, money(custoDoMaterial(l.m))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(l.valor))), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
    tone: l.st.tone
  }, l.st.label)))), /*#__PURE__*/React.createElement("tr", {
    style: {
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "7"
  }, /*#__PURE__*/React.createElement("strong", null, "Total")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(valorTotal))), /*#__PURE__*/React.createElement("td", null))))));
}

/* ---------------- Movimentações ---------------- */
function Movimentacoes({
  db,
  update,
  usuario
}) {
  const [modal, setModal] = useState(null);
  const [transf, setTransf] = useState(false);
  const [estorno, setEstorno] = useState(null);
  const [fTipo, setFTipo] = useState('');
  const [fMat, setFMat] = useState('');
  const [fEst, setFEst] = useState('');
  const [de, setDe] = useState('');
  const [ate, setAte] = useState('');
  const [pagina, setPagina] = useState(1);
  const porPagina = 30;
  const podeMover = podeExecutar(usuario, 'cadastros');
  const lista = (db.movimentacoes || []).filter(m => {
    if (fTipo && m.tipo !== fTipo) return false;
    if (fMat && m.materialId !== fMat) return false;
    if (fEst && m.estoqueId !== fEst) return false;
    const dia = String(m.quando || '').slice(0, 10);
    if (de && dia < de) return false;
    if (ate && dia > ate) return false;
    return true;
  }).slice().sort((a, b) => String(b.quando).localeCompare(String(a.quando)));
  const totalPag = Math.max(Math.ceil(lista.length / porPagina), 1);
  const pag = Math.min(pagina, totalPag);
  const visiveis = lista.slice((pag - 1) * porPagina, pag * porPagina);
  function lancar(dados, aoFechar) {
    let res = null;
    update(d => {
      res = aplicarMovimento(d, dados, usuario);
      return d;
    });
    if (res && res.erro) {
      if (res.podeAutorizar && podeExecutar(usuario, 'admin')) {
        if (confirm(res.erro + '\n\nAutorizar o saldo negativo? Fica registrado no seu nome.')) {
          update(d => {
            res = aplicarMovimento(d, {
              ...dados,
              autorizadoNegativo: true
            }, usuario);
            return d;
          });
          if (res && res.erro) {
            alert(res.erro);
            return;
          }
          aoFechar && aoFechar();
          return;
        }
        return;
      }
      alert(res.erro + (res.podeAutorizar ? '\n\nSomente um administrador pode autorizar saldo negativo.' : ''));
      return;
    }
    aoFechar && aoFechar();
  }
  function estornar(m, motivo) {
    let res = null;
    update(d => {
      res = estornarMovimento(d, m.id, usuario, motivo);
      return d;
    });
    if (res && res.erro) {
      alert(res.erro);
      return;
    }
    setEstorno(null);
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, lista.length, " movimentação(ões)"), podeMover && /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setTransf(true)
  }, "⇄ Transferir"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({
      tipo: 'entrada_compra'
    })
  }, "+ Nova movimentação"))), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: fTipo,
    onChange: e => {
      setFTipo(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), TIPOS_MOV.map(t => /*#__PURE__*/React.createElement("option", {
    key: t.id,
    value: t.id
  }, t.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Material"
  }, /*#__PURE__*/React.createElement("select", {
    value: fMat,
    onChange: e => {
      setFMat(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.materiais || []).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Local"
  }, /*#__PURE__*/React.createElement("select", {
    value: fEst,
    onChange: e => {
      setFEst(e.target.value);
      setPagina(1);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.estoques || []).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "De"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: de,
    onChange: e => {
      setDe(e.target.value);
      setPagina(1);
    }
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Até"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: ate,
    onChange: e => {
      setAte(e.target.value);
      setPagina(1);
    }
  })))), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma movimentação registrada."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Nº / quando"), /*#__PURE__*/React.createElement("th", null, "Movimento"), /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Quantidade"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo total"), /*#__PURE__*/React.createElement("th", null, "Origem"), /*#__PURE__*/React.createElement("th", null, "Usuário"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 60
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, visiveis.map(m => {
    const t = tipoMov(m.tipo) || {
      nome: m.tipo,
      sinal: 0
    };
    const mat = (db.materiais || []).find(x => x.id === m.materialId);
    const est = (db.estoques || []).find(x => x.id === m.estoqueId);
    const estornada = (db.movimentacoes || []).some(x => x.estornoDe === m.id);
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id,
      style: estornada ? {
        opacity: .5
      } : {}
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, m.numero), /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, fmtDataHora(m.quando))), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: t.sinal > 0 ? 'var(--ok)' : 'var(--bad)',
        fontWeight: 600
      }
    }, t.sinal > 0 ? '▲' : '▼', " ", t.nome), m.motivo && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, m.motivo), m.autorizadoNegativo && /*#__PURE__*/React.createElement(Badge, {
      tone: "warn"
    }, "saldo negativo autorizado"), estornada && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "estornada"), m.estornoDe && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "é um estorno")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, mat ? mat.nome : '(removido)'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, est ? est.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: {
        color: t.sinal > 0 ? 'var(--ok)' : 'var(--bad)',
        fontWeight: 600
      }
    }, t.sinal > 0 ? '+' : '−', fmtQtd(m.quantidade, m.unidade, db)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, num(m.custoTotal) > 0 ? money(m.custoTotal) : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, m.documento || m.origemTipo || '—', m.parId && /*#__PURE__*/React.createElement("div", null, "transferência")), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, m.usuario), /*#__PURE__*/React.createElement("td", null, podeMover && !estornada && !m.estornoDe && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      title: "Estornar",
      onClick: () => setEstorno(m)
    }, "↩")));
  })))), totalPag > 1 && /*#__PURE__*/React.createElement("div", {
    className: "row-actions no-print",
    style: {
      justifyContent: 'center',
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: pag <= 1,
    onClick: () => setPagina(pag - 1)
  }, "← Anterior"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "página ", pag, " de ", totalPag), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: pag >= totalPag,
    onClick: () => setPagina(pag + 1)
  }, "Próxima →"))), modal && /*#__PURE__*/React.createElement(MovimentacaoModal, {
    dados: modal,
    db: db,
    onClose: () => setModal(null),
    onSalvar: d2 => lancar(d2, () => setModal(null))
  }), transf && /*#__PURE__*/React.createElement(TransferenciaModal, {
    db: db,
    usuario: usuario,
    update: update,
    onClose: () => setTransf(false)
  }), estorno && /*#__PURE__*/React.createElement(EstornoModal, {
    mov: estorno,
    db: db,
    onClose: () => setEstorno(null),
    onConfirmar: motivo => estornar(estorno, motivo)
  }));
}
function MovimentacaoModal({
  dados,
  db,
  onClose,
  onSalvar
}) {
  const [f, setF] = useState({
    tipo: 'entrada_compra',
    materialId: '',
    estoqueId: '',
    quantidade: '',
    unidadeInformada: '',
    custoUnitario: '',
    fornecedorId: '',
    centroCustoId: '',
    documento: '',
    motivo: '',
    observacao: '',
    ...dados
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const t = tipoMov(f.tipo) || {};
  const mat = (db.materiais || []).find(m => m.id === f.materialId);
  const estoquePadrao = (db.estoques || []).find(e => e.padrao) || (db.estoques || [])[0];
  useEffect(() => {
    if (!f.estoqueId && estoquePadrao) set('estoqueId', estoquePadrao.id);
  }, []);

  // quantidade pode ser informada em outra unidade (ex.: comprar em ROLO, estocar em M)
  const unidades = mat ? Array.from(new Set([mat.unidadeEstoque, mat.unidadeCompra, mat.unidadeConsumo].filter(Boolean))) : [];
  const unInformada = f.unidadeInformada || (mat ? mat.unidadeEstoque : '');
  const convertida = mat && unInformada !== mat.unidadeEstoque ? converter(f.quantidade, unInformada, mat.unidadeEstoque, db, mat.id) : num(f.quantidade);
  const disp = mat && f.estoqueId ? disponivelEm(f.materialId, f.estoqueId, db) : null;
  const custoTotal = num(convertida) * num(f.custoUnitario);

  // alerta de aumento de preço
  const par = db.parametrosMateriais || {};
  const limiteAlta = num(par.alertaAumentoPreco) || 10;
  const ultimo = mat ? num(mat.ultimoCusto) : 0;
  const alta = t.grupo === 'entrada' && ultimo > 0 && num(f.custoUnitario) > 0 ? (num(f.custoUnitario) - ultimo) / ultimo * 100 : null;
  function salvar() {
    if (convertida === null) return alert(`Não há conversão de ${unInformada} para ${mat.unidadeEstoque}. Cadastre em Grupos, unidades e locais.`);
    onSalvar({
      ...f,
      quantidade: convertida
    });
  }
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Nova movimentação",
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Toda alteração de estoque nasce aqui. O saldo é o acumulado das movimentações — nunca um número digitado."), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo de movimento *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tipo,
    onChange: e => set('tipo', e.target.value)
  }, ['entrada', 'saida', 'ajuste'].map(g => /*#__PURE__*/React.createElement("optgroup", {
    key: g,
    label: g === 'entrada' ? 'Entradas' : g === 'saida' ? 'Saídas' : 'Ajustes'
  }, TIPOS_MOV.filter(x => x.grupo === g).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Material *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.materialId,
    onChange: e => setF(p => ({
      ...p,
      materialId: e.target.value,
      unidadeInformada: ''
    }))
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.materiais || []).filter(m => m.ativo !== false).sort((a, b) => String(a.nome).localeCompare(String(b.nome))).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.codigo, " · ", m.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Local de estoque *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.estoqueId,
    onChange: e => set('estoqueId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.estoques || []).filter(e => e.ativo !== false).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome))))), mat && disp !== null && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "Disponível hoje neste local: ", /*#__PURE__*/React.createElement("strong", null, fmtQtd(disp, mat.unidadeEstoque, db))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    min: "0",
    value: f.quantidade,
    onChange: e => set('quantidade', e.target.value)
  })), unidades.length > 1 && /*#__PURE__*/React.createElement(Field, {
    label: "Unidade informada"
  }, /*#__PURE__*/React.createElement("select", {
    value: unInformada,
    onChange: e => set('unidadeInformada', e.target.value)
  }, unidades.map(u => /*#__PURE__*/React.createElement("option", {
    key: u,
    value: u
  }, u)))), /*#__PURE__*/React.createElement(Field, {
    label: "Custo unitário (R$)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    value: f.custoUnitario,
    onChange: e => set('custoUnitario', e.target.value),
    placeholder: mat ? String(custoDoMaterial(mat).toFixed(4)) : ''
  }))), mat && unInformada !== mat.unidadeEstoque && (convertida === null ? /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      fontWeight: 600,
      marginTop: -6,
      marginBottom: 10
    }
  }, "⚠ Sem conversão de ", unInformada, " para ", mat.unidadeEstoque, ".") : /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, num(f.quantidade), " ", unInformada, " = ", /*#__PURE__*/React.createElement("strong", null, fmtQtd(convertida, mat.unidadeEstoque, db)), " no estoque")), alta !== null && Math.abs(alta) >= limiteAlta && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      background: alta > 0 ? 'var(--bad-bg)' : 'var(--ok-bg)',
      color: alta > 0 ? 'var(--bad)' : 'var(--ok)',
      padding: '8px 10px',
      borderRadius: 6,
      marginBottom: 10,
      fontWeight: 600
    }
  }, alta > 0 ? '⚠ AUMENTO DE PREÇO' : '↓ Redução de preço', ": ", Math.abs(alta).toFixed(1), "% em relação à última compra (", money(ultimo), " → ", money(f.custoUnitario), ")."), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, (t.exigeFornecedor || t.grupo === 'entrada') && /*#__PURE__*/React.createElement(Field, {
    label: t.exigeFornecedor ? 'Fornecedor *' : 'Fornecedor'
  }, /*#__PURE__*/React.createElement("select", {
    value: f.fornecedorId,
    onChange: e => set('fornecedorId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.fornecedores || []).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), t.grupo === 'saida' && /*#__PURE__*/React.createElement(Field, {
    label: "Centro de custo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.centroCustoId,
    onChange: e => set('centroCustoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.centrosCusto || []).filter(c => c.ativo !== false).map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Documento"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.documento,
    onChange: e => set('documento', e.target.value),
    placeholder: "NF, pedido, OP…"
  }))), /*#__PURE__*/React.createElement(Field, {
    label: t.exigeMotivo ? 'Motivo *' : 'Motivo'
  }, f.tipo === 'saida_devolucao' ? /*#__PURE__*/React.createElement("select", {
    value: f.motivo,
    onChange: e => set('motivo', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), MOTIVOS_DEVOLUCAO.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x))) : /*#__PURE__*/React.createElement("input", {
    value: f.motivo,
    onChange: e => set('motivo', e.target.value),
    placeholder: t.exigeMotivo ? 'obrigatório — fica na auditoria' : 'opcional'
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacao,
    onChange: e => set('observacao', e.target.value)
  })), num(custoTotal) > 0 && /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Quantidade"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, fmtQtd(convertida, mat ? mat.unidadeEstoque : '', db))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Custo unitário"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, money(f.custoUnitario))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Custo total"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, money(custoTotal)))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: salvar,
    disabled: !f.materialId || !f.estoqueId || !(num(f.quantidade) > 0)
  }, "Lançar movimentação")));
}
function EstornoModal({
  mov,
  db,
  onClose,
  onConfirmar
}) {
  const [motivo, setMotivo] = useState('');
  const t = tipoMov(mov.tipo) || {
    nome: mov.tipo,
    sinal: 0
  };
  const mat = (db.materiais || []).find(m => m.id === mov.materialId);
  return /*#__PURE__*/React.createElement(Modal, {
    title: `Estornar ${mov.numero}`,
    onClose: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "A movimentação original não é apagada. O estorno gera uma contramovimentação no sentido oposto, e as duas ficam visíveis no histórico."), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Movimento"), /*#__PURE__*/React.createElement("td", null, t.nome)), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Material"), /*#__PURE__*/React.createElement("td", null, mat ? mat.nome : '—')), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Quantidade"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, fmtQtd(mov.quantidade, mov.unidade, db))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Lançado por"), /*#__PURE__*/React.createElement("td", null, mov.usuario, " em ", mov.quando)))), /*#__PURE__*/React.createElement(Field, {
    label: "Motivo do estorno *"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: motivo,
    onChange: e => setMotivo(e.target.value),
    placeholder: "erro de lançamento, quantidade errada, material trocado…"
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn danger",
    onClick: () => onConfirmar(motivo),
    disabled: !motivo.trim()
  }, "Estornar")));
}
function TransferenciaModal({
  db,
  usuario,
  update,
  onClose
}) {
  const [f, setF] = useState({
    materialId: '',
    origemId: '',
    destinoId: '',
    quantidade: '',
    motivo: '',
    observacao: ''
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const mat = (db.materiais || []).find(m => m.id === f.materialId);
  const disp = mat && f.origemId ? disponivelEm(f.materialId, f.origemId, db) : null;
  function transferirAgora() {
    let res = null;
    update(d => {
      res = transferir(d, f, usuario);
      return d;
    });
    if (res && res.erro) {
      if (res.podeAutorizar && podeExecutar(usuario, 'admin') && confirm(res.erro + '\n\nAutorizar saldo negativo na origem?')) {
        update(d => {
          res = transferir(d, {
            ...f,
            autorizadoNegativo: true
          }, usuario);
          return d;
        });
        if (res && res.erro) return alert(res.erro);
      } else return alert(res.erro);
    }
    onClose();
  }
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Transferir entre estoques",
    onClose: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "A transferência gera duas movimentações vinculadas: uma saída na origem e uma entrada no destino."), /*#__PURE__*/React.createElement(Field, {
    label: "Material *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.materialId,
    onChange: e => set('materialId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.materiais || []).filter(m => m.ativo !== false).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.codigo, " · ", m.nome)))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Origem *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.origemId,
    onChange: e => set('origemId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.estoques || []).filter(e => e.ativo !== false).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Destino *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.destinoId,
    onChange: e => set('destinoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.estoques || []).filter(e => e.ativo !== false && e.id !== f.origemId).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome))))), disp !== null && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "Disponível na origem: ", /*#__PURE__*/React.createElement("strong", null, fmtQtd(disp, mat.unidadeEstoque, db))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    min: "0",
    value: f.quantidade,
    onChange: e => set('quantidade', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Motivo"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.motivo,
    onChange: e => set('motivo', e.target.value),
    placeholder: "realocação, produção em outro local…"
  }))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: transferirAgora,
    disabled: !f.materialId || !f.origemId || !f.destinoId || !(num(f.quantidade) > 0)
  }, "Transferir")));
}

/* ---------------- Reservas ---------------- */
function Reservas({
  db,
  update,
  usuario
}) {
  const [modal, setModal] = useState(false);
  const [verEncerradas, setVerEncerradas] = useState(false);
  const lista = (db.reservas || []).filter(r => verEncerradas ? true : r.status === 'Ativa').slice().sort((a, b) => String(b.quando).localeCompare(String(a.quando)));
  function criar(f) {
    let res = null;
    update(d => {
      res = reservar(d, f, usuario);
      return d;
    });
    if (res && res.erro) return alert(res.erro);
    setModal(false);
  }
  function liberar(r) {
    if (!confirm('Liberar esta reserva? O material volta ao disponível.')) return;
    let res = null;
    update(d => {
      res = liberarReserva(d, r.id, usuario);
      return d;
    });
    if (res && res.erro) alert(res.erro);
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, lista.filter(r => r.status === 'Ativa').length, " reserva(s) ativa(s)"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal(true)
  }, "+ Nova reserva")), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A reserva não tira o material do estoque físico — ela apenas o retira do disponível, para que ninguém use o que já está comprometido. Quando produção voltar ao sistema, a liberação da OP passa a reservar sozinha."), /*#__PURE__*/React.createElement("label", {
    className: "small",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer',
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    style: {
      width: 'auto'
    },
    checked: verEncerradas,
    onChange: e => setVerEncerradas(e.target.checked)
  }), "Mostrar reservas encerradas")), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma reserva."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Nº"), /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Quantidade"), /*#__PURE__*/React.createElement("th", null, "Origem"), /*#__PURE__*/React.createElement("th", null, "Quando"), /*#__PURE__*/React.createElement("th", null, "Status"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 60
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lista.map(r => {
    const m = (db.materiais || []).find(x => x.id === r.materialId);
    const e = (db.estoques || []).find(x => x.id === r.estoqueId);
    return /*#__PURE__*/React.createElement("tr", {
      key: r.id,
      style: r.status !== 'Ativa' ? {
        opacity: .55
      } : {}
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, r.numero)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, m ? m.nome : '(removido)'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, e ? e.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, fmtQtd(r.quantidade, m ? m.unidadeEstoque : '', db)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, r.descricao || r.origemTipo), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, r.quando, /*#__PURE__*/React.createElement("div", null, r.usuario)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
      tone: r.status === 'Ativa' ? 'warn' : 'idle'
    }, r.status)), /*#__PURE__*/React.createElement("td", null, r.status === 'Ativa' && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      title: "Liberar",
      onClick: () => liberar(r)
    }, "↺")));
  })))), modal && /*#__PURE__*/React.createElement(ReservaModal, {
    db: db,
    onClose: () => setModal(false),
    onSalvar: criar
  }));
}
function ReservaModal({
  db,
  onClose,
  onSalvar
}) {
  const [f, setF] = useState({
    materialId: '',
    estoqueId: '',
    quantidade: '',
    descricao: '',
    origemTipo: 'manual'
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const mat = (db.materiais || []).find(m => m.id === f.materialId);
  const disp = mat && f.estoqueId ? disponivelEm(f.materialId, f.estoqueId, db) : null;
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Reservar material",
    onClose: onClose
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Material *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.materialId,
    onChange: e => set('materialId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.materiais || []).filter(m => m.ativo !== false).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.codigo, " · ", m.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Local *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.estoqueId,
    onChange: e => set('estoqueId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.estoques || []).filter(e => e.ativo !== false).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))), disp !== null && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "Disponível para reserva: ", /*#__PURE__*/React.createElement("strong", null, fmtQtd(disp, mat.unidadeEstoque, db))), /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.001",
    min: "0",
    value: f.quantidade,
    onChange: e => set('quantidade', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Para quê"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.descricao,
    onChange: e => set('descricao', e.target.value),
    placeholder: "Pedido do cliente X, amostra, OP futura…"
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSalvar(f),
    disabled: !f.materialId || !f.estoqueId || !(num(f.quantidade) > 0)
  }, "Reservar")));
}

/* ---------------- Grupos, unidades, locais e parâmetros ---------------- */
function ConfigMateriais({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [aba, setAba] = useState('grupos');
  const [novo, setNovo] = useState({
    grupo: '',
    sub: {},
    unidade: {
      sigla: '',
      nome: '',
      decimais: 2
    },
    estoque: {
      codigo: '',
      nome: '',
      localizacao: '',
      responsavel: ''
    },
    conv: {
      de: '',
      para: '',
      fator: '',
      materialId: ''
    },
    cc: ''
  });
  const set = (k, v) => setNovo(p => ({
    ...p,
    [k]: v
  }));
  const par = db.parametrosMateriais || {};
  const [p2, setP2] = useState(par);
  const grupos = (db.gruposMaterial || []).filter(g => !g.paiId);
  const usoGrupo = id => (db.materiais || []).filter(m => m.grupoId === id).length;
  const usoUnidade = sig => (db.materiais || []).filter(m => [m.unidadeEstoque, m.unidadeCompra, m.unidadeConsumo].includes(sig)).length;
  const usoEstoque = id => (db.saldos || []).filter(s => s.estoqueId === id && num(s.fisico) !== 0).length;
  function addGrupo() {
    const n = String(novo.grupo || '').trim();
    if (!n) return;
    if ((db.gruposMaterial || []).some(g => !g.paiId && normaliza(g.nome) === normaliza(n))) return alert('Já existe um grupo com este nome.');
    update(d => {
      d.gruposMaterial = [...(d.gruposMaterial || []), {
        id: uid(),
        nome: n,
        paiId: '',
        ativo: true
      }];
      registrarLog(d, usuario, 'Criou grupo de material', n);
      return d;
    });
    set('grupo', '');
  }
  function addSub(paiId) {
    const n = String((novo.sub || {})[paiId] || '').trim();
    if (!n) return;
    update(d => {
      d.gruposMaterial = [...(d.gruposMaterial || []), {
        id: uid(),
        nome: n,
        paiId,
        ativo: true
      }];
      return d;
    });
    set('sub', {
      ...novo.sub,
      [paiId]: ''
    });
  }
  function rmGrupo(g) {
    if (usoGrupo(g.id) > 0) return alert(`${usoGrupo(g.id)} material(is) usam este grupo. Reclassifique antes de remover.`);
    const filhos = (db.gruposMaterial || []).filter(x => x.paiId === g.id);
    if (filhos.some(x => usoGrupo(x.id) > 0)) return alert('Há materiais em subgrupos deste grupo.');
    if (!confirm(`Remover "${g.nome}"${filhos.length ? ` e seus ${filhos.length} subgrupo(s)` : ''}?`)) return;
    update(d => {
      d.gruposMaterial = d.gruposMaterial.filter(x => x.id !== g.id && x.paiId !== g.id);
      return d;
    });
  }
  function addUnidade() {
    const u = novo.unidade;
    if (!u.sigla.trim() || !u.nome.trim()) return;
    if ((db.unidades || []).some(x => normaliza(x.sigla) === normaliza(u.sigla))) return alert('Já existe uma unidade com esta sigla.');
    update(d => {
      d.unidades = [...(d.unidades || []), {
        id: uid(),
        sigla: u.sigla.toUpperCase(),
        nome: u.nome,
        decimais: num(u.decimais),
        ativo: true
      }];
      return d;
    });
    set('unidade', {
      sigla: '',
      nome: '',
      decimais: 2
    });
  }
  function addEstoque() {
    const e = novo.estoque;
    if (!e.nome.trim()) return;
    update(d => {
      const cod = e.codigo.trim() || `EST-${String((d.estoques || []).length + 1).padStart(2, '0')}`;
      d.estoques = [...(d.estoques || []), {
        id: uid(),
        codigo: cod,
        nome: e.nome,
        localizacao: e.localizacao,
        responsavel: e.responsavel,
        ativo: true,
        padrao: (d.estoques || []).length === 0
      }];
      registrarLog(d, usuario, 'Criou local de estoque', e.nome);
      return d;
    });
    set('estoque', {
      codigo: '',
      nome: '',
      localizacao: '',
      responsavel: ''
    });
  }
  function rmEstoque(e) {
    if (usoEstoque(e.id) > 0) return alert('Este local ainda tem saldo. Transfira o material antes de removê-lo.');
    if (!confirm(`Remover o local "${e.nome}"?`)) return;
    update(d => {
      d.estoques = d.estoques.filter(x => x.id !== e.id);
      return d;
    });
  }
  function addConversao() {
    const c = novo.conv;
    if (!c.de || !c.para || !(num(c.fator) > 0)) return alert('Informe as duas unidades e o fator.');
    if (normaliza(c.de) === normaliza(c.para)) return alert('As unidades precisam ser diferentes.');
    update(d => {
      d.conversoes = [...(d.conversoes || []), {
        id: uid(),
        de: c.de,
        para: c.para,
        fator: num(c.fator),
        materialId: c.materialId || ''
      }];
      return d;
    });
    set('conv', {
      de: '',
      para: '',
      fator: '',
      materialId: ''
    });
  }
  function addCC() {
    const n = String(novo.cc || '').trim();
    if (!n) return;
    update(d => {
      d.centrosCusto = [...(d.centrosCusto || []), {
        id: uid(),
        codigo: `CC-${String((d.centrosCusto || []).length + 1).padStart(2, '0')}`,
        nome: n,
        ativo: true
      }];
      return d;
    });
    set('cc', '');
  }
  function salvarParametros() {
    update(d => {
      d.parametrosMateriais = {
        ...d.parametrosMateriais,
        ...p2
      };
      registrarLog(d, usuario, 'Alterou parâmetros de materiais', '');
      return d;
    });
    alert('Parâmetros salvos.');
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'grupos',
      label: `Grupos (${grupos.length})`
    }, {
      id: 'unidades',
      label: `Unidades (${(db.unidades || []).length})`
    }, {
      id: 'conv',
      label: `Conversões (${(db.conversoes || []).length})`
    }, {
      id: 'locais',
      label: `Locais (${(db.estoques || []).length})`
    }, {
      id: 'cc',
      label: `Centros de custo (${(db.centrosCusto || []).length})`
    }, {
      id: 'param',
      label: 'Parâmetros'
    }]
  }), aba === 'grupos' && /*#__PURE__*/React.createElement("div", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.grupo,
    onChange: e => set('grupo', e.target.value),
    placeholder: "Novo grupo",
    style: {
      maxWidth: 240
    },
    onKeyDown: e => {
      if (e.key === 'Enter') addGrupo();
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addGrupo,
    disabled: !novo.grupo.trim()
  }, "+ Grupo"))), grupos.map(g => {
    const subs = (db.gruposMaterial || []).filter(x => x.paiId === g.id);
    return /*#__PURE__*/React.createElement("div", {
      key: g.id,
      className: "panel"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        flexWrap: 'wrap',
        marginBottom: 8
      }
    }, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0
      }
    }, g.nome), g.cortavel && /*#__PURE__*/React.createElement("span", {
      className: "tag",
      style: {
        background: 'var(--ok-bg)',
        color: 'var(--ok)'
      }
    }, "cortado na modelagem"), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1
      }
    }), /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, usoGrupo(g.id), " material(is) · ", subs.length, " subgrupo(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      title: "Remover",
      onClick: () => rmGrupo(g)
    }, "×")), podeEditar && /*#__PURE__*/React.createElement("label", {
      className: "small linha-check",
      style: {
        marginBottom: 8
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      checked: !!g.cortavel,
      onChange: e => {
        const v = e.target.checked;
        update(d => {
          d.gruposMaterial = d.gruposMaterial.map(x => x.id === g.id || x.paiId === g.id ? {
            ...x,
            cortavel: v
          } : x);
          return d;
        });
      }
    }), "Material deste grupo é cortado na modelagem"), g.cortavel && /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 8
      }
    }, "No produto, estes materiais pedem a modelagem de corte. Os demais entram com a quantidade informada."), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 5,
        flexWrap: 'wrap',
        marginBottom: 8
      }
    }, subs.map(sg => /*#__PURE__*/React.createElement("span", {
      key: sg.id,
      className: "tag"
    }, sg.nome, usoGrupo(sg.id) > 0 ? ` (${usoGrupo(sg.id)})` : '', podeEditar && usoGrupo(sg.id) === 0 && /*#__PURE__*/React.createElement("button", {
      style: {
        marginLeft: 5,
        border: 'none',
        background: 'none',
        cursor: 'pointer'
      },
      onClick: () => update(d => {
        d.gruposMaterial = d.gruposMaterial.filter(x => x.id !== sg.id);
        return d;
      })
    }, "×"))), subs.length === 0 && /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, "nenhum subgrupo")), podeEditar && /*#__PURE__*/React.createElement("div", {
      className: "row-actions"
    }, /*#__PURE__*/React.createElement("input", {
      value: (novo.sub || {})[g.id] || '',
      onChange: e => set('sub', {
        ...novo.sub,
        [g.id]: e.target.value
      }),
      placeholder: "Novo subgrupo",
      style: {
        maxWidth: 200
      },
      onKeyDown: e => {
        if (e.key === 'Enter') addSub(g.id);
      }
    }), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => addSub(g.id)
    }, "+ Subgrupo")));
  })), aba === 'unidades' && /*#__PURE__*/React.createElement("div", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Sigla"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.unidade.sigla,
    maxLength: "6",
    onChange: e => set('unidade', {
      ...novo.unidade,
      sigla: e.target.value.toUpperCase()
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.unidade.nome,
    onChange: e => set('unidade', {
      ...novo.unidade,
      nome: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Casas decimais"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    max: "4",
    value: novo.unidade.decimais,
    onChange: e => set('unidade', {
      ...novo.unidade,
      decimais: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addUnidade
  }, "+ Unidade")), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Sigla"), /*#__PURE__*/React.createElement("th", null, "Nome"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Decimais"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Materiais"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, (db.unidades || []).map(u => /*#__PURE__*/React.createElement("tr", {
    key: u.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, u.sigla)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, u.nome), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, num(u.decimais)), /*#__PURE__*/React.createElement("td", {
    className: "num small muted"
  }, usoUnidade(u.sigla) || '—'), /*#__PURE__*/React.createElement("td", null, podeEditar && usoUnidade(u.sigla) === 0 && /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    title: "Remover",
    onClick: () => update(d => {
      d.unidades = d.unidades.filter(x => x.id !== u.id);
      return d;
    })
  }, "×")))))))), aba === 'conv' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "A conversão permite comprar numa unidade e estocar em outra. Exemplo: 1 ROLO = 50 M. Deixando o material em branco, a regra vale para todos."), podeEditar && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "1 unidade de"
  }, /*#__PURE__*/React.createElement("select", {
    value: novo.conv.de,
    onChange: e => set('conv', {
      ...novo.conv,
      de: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.unidades || []).map(u => /*#__PURE__*/React.createElement("option", {
    key: u.id,
    value: u.sigla
  }, u.sigla)))), /*#__PURE__*/React.createElement(Field, {
    label: "equivale a"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    value: novo.conv.fator,
    onChange: e => set('conv', {
      ...novo.conv,
      fator: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "unidades de"
  }, /*#__PURE__*/React.createElement("select", {
    value: novo.conv.para,
    onChange: e => set('conv', {
      ...novo.conv,
      para: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.unidades || []).map(u => /*#__PURE__*/React.createElement("option", {
    key: u.id,
    value: u.sigla
  }, u.sigla)))), /*#__PURE__*/React.createElement(Field, {
    label: "Apenas para o material"
  }, /*#__PURE__*/React.createElement("select", {
    value: novo.conv.materialId,
    onChange: e => set('conv', {
      ...novo.conv,
      materialId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "todos os materiais"), (db.materiais || []).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome))))), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addConversao
  }, "+ Conversão"))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Regra"), /*#__PURE__*/React.createElement("th", null, "Aplica-se a"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, (db.conversoes || []).length === 0 ? /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    colSpan: "3",
    className: "small muted",
    style: {
      padding: 16
    }
  }, "Nenhuma conversão cadastrada.")) : (db.conversoes || []).map(c => {
    const m = c.materialId ? (db.materiais || []).find(x => x.id === c.materialId) : null;
    return /*#__PURE__*/React.createElement("tr", {
      key: c.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, "1 ", c.de, " = ", num(c.fator), " ", c.para)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, m ? m.nome : 'todos os materiais'), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => update(d => {
        d.conversoes = d.conversoes.filter(x => x.id !== c.id);
        return d;
      })
    }, "×")));
  }))))), aba === 'locais' && /*#__PURE__*/React.createElement("div", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Código"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.estoque.codigo,
    onChange: e => set('estoque', {
      ...novo.estoque,
      codigo: e.target.value.toUpperCase()
    }),
    placeholder: "automático"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome do local *"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.estoque.nome,
    onChange: e => set('estoque', {
      ...novo.estoque,
      nome: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Endereço interno"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.estoque.localizacao,
    onChange: e => set('estoque', {
      ...novo.estoque,
      localizacao: e.target.value
    }),
    placeholder: "Galpão 2, mezanino…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Responsável"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.estoque.responsavel,
    onChange: e => set('estoque', {
      ...novo.estoque,
      responsavel: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addEstoque,
    disabled: !novo.estoque.nome.trim()
  }, "+ Local")), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Código"), /*#__PURE__*/React.createElement("th", null, "Local"), /*#__PURE__*/React.createElement("th", null, "Endereço"), /*#__PURE__*/React.createElement("th", null, "Responsável"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Materiais com saldo"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, (db.estoques || []).map(e => /*#__PURE__*/React.createElement("tr", {
    key: e.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, e.codigo)), /*#__PURE__*/React.createElement("td", null, e.nome, " ", e.padrao && /*#__PURE__*/React.createElement("span", {
    className: "tag"
  }, "padrão")), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, e.localizacao || '—'), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, e.responsavel || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, usoEstoque(e.id)), /*#__PURE__*/React.createElement("td", null, podeEditar && !e.padrao && /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => rmEstoque(e)
  }, "×")))))))), aba === 'cc' && /*#__PURE__*/React.createElement("div", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.cc,
    onChange: e => set('cc', e.target.value),
    placeholder: "Novo centro de custo",
    style: {
      maxWidth: 240
    },
    onKeyDown: e => {
      if (e.key === 'Enter') addCC();
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addCC,
    disabled: !novo.cc.trim()
  }, "+ Centro de custo"))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Código"), /*#__PURE__*/React.createElement("th", null, "Nome"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Saídas"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, (db.centrosCusto || []).map(c => {
    const uso = (db.movimentacoes || []).filter(m => m.centroCustoId === c.id).length;
    return /*#__PURE__*/React.createElement("tr", {
      key: c.id
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, c.codigo)), /*#__PURE__*/React.createElement("td", null, c.nome), /*#__PURE__*/React.createElement("td", {
      className: "num small muted"
    }, uso || '—'), /*#__PURE__*/React.createElement("td", null, podeEditar && uso === 0 && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => update(d => {
        d.centrosCusto = d.centrosCusto.filter(x => x.id !== c.id);
        return d;
      })
    }, "×")));
  }))))), aba === 'param' && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Parâmetros do módulo"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Alerta de aumento de preço (%)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.1",
    value: p2.alertaAumentoPreco,
    onChange: e => setP2({
      ...p2,
      alertaAumentoPreco: e.target.value
    }),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Material parado após (dias)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: p2.diasSemMovimento,
    onChange: e => setP2({
      ...p2,
      diasSemMovimento: e.target.value
    }),
    disabled: !podeEditar
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Método de custo"
  }, /*#__PURE__*/React.createElement("select", {
    value: p2.metodoCusto,
    onChange: e => setP2({
      ...p2,
      metodoCusto: e.target.value
    }),
    disabled: true
  }, /*#__PURE__*/React.createElement("option", {
    value: "medio"
  }, "Custo médio ponderado")))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O aumento de preço é avisado no momento da entrada, comparando com a última compra. O saldo negativo sempre exige autorização de um administrador, caso a caso."), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: salvarParametros
  }, "Salvar parâmetros")));
}

/* ==========================================================
   MÓDULO PRODUÇÃO
========================================================== */
function GrupoProducao({
  db,
  update,
  usuario,
  perm
}) {
  /* a torre abre primeiro: o dia começa perguntando o que está acontecendo */
  const [sub, setSub] = useState('torre');
  const podeEditar = podeExecutar(usuario, 'cadastros') || podeExecutar(usuario, 'admin');
  const abertas = (db.ordens || []).filter(o => ['aberta', 'liberada', 'producao'].includes(o.situacao)).length;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Comercial e PCP"), /*#__PURE__*/React.createElement("h2", null, "Produção"))), /*#__PURE__*/React.createElement(SubTabs, {
    active: sub,
    onChange: setSub,
    tabs: [{
      id: 'torre',
      label: 'Hoje'
    }, {
      id: 'ordens',
      label: `Ordens (${abertas})`
    }, {
      id: 'analise',
      label: 'Programação'
    }]
  }), sub === 'torre' && /*#__PURE__*/React.createElement(TorreDeControle, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'ordens' && /*#__PURE__*/React.createElement(TelaOrdens, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'analise' && /*#__PURE__*/React.createElement(AnaliseProducao, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }));
}

/* As ferramentas de estudo, juntas. Antes estavam espalhadas: aferição
   em Produção, balanceamento e lead time dentro do produto. São todas a
   mesma coisa — entender a fábrica para planejar melhor. */
function AnaliseProducao({ db, update, usuario, podeEditar }) {
  const [qual, setQual] = React.useState('carteira');
  const [produtoId, setProdutoId] = React.useState(
    ((db.produtos || [])[0] || {}).id || '');
  const produto = (db.produtos || []).find(p => p.id === produtoId) || null;

  const precisaProduto = qual === 'balanceamento' || qual === 'leadtime';

  return React.createElement("div", null,
    React.createElement(SubTabs, {
      active: qual, onChange: setQual, tabs: [
        { id: 'ocorrencias', label: 'Ocorrências' },
        { id: 'afericao', label: 'Aferição e metas' },
        { id: 'carteira', label: 'Balanceamento da carteira' },
        { id: 'cronograma', label: 'Cronograma' },
        { id: 'apontar', label: `Apontamentos (${(db.apontamentos || []).length})` },
        { id: 'avaliacao', label: 'Avaliação' },
      ]
    }),

    precisaProduto && React.createElement("div", { className: "panel no-print" },
      React.createElement(Field, { label: "Produto a estudar" },
        React.createElement("select", {
          value: produtoId, onChange: e => setProdutoId(e.target.value)
        },
          React.createElement("option", { value: "" }, "— escolha o produto —"),
          (db.produtos || []).map(p => React.createElement("option", {
            key: p.id, value: p.id
          }, codigoCompletoProduto(p, db), " · ",
            montarNomeProduto(p, db) || '(sem nome)'))))),

    precisaProduto && !produto && React.createElement(Empty, {
      text: "Escolha um produto para estudar a linha."
    }),

    /* Programação do dia: consolidada no Balanceamento da carteira, que
       agora faz o plano inteiro num lugar só — escolher ordens, escalar
       cada operação com nome e máquina, ver a carga de pessoas e de
       equipamentos, conferir contra as entregas e publicar. A tela
       antiga distribuía automaticamente em vez de deixar você escolher
       quem faz o quê; fica no código porque essa distribuição pode
       voltar a ser útil. */
    qual === 'programacao' && React.createElement(ProgramacaoDoDia, {
      db, update, usuario
    }),
    qual === 'ocorrencias' && React.createElement(TelaOcorrencias, {
      db, update, usuario, podeEditar
    }),
    qual === 'afericao' && React.createElement(AfericaoEMetas, {
      db, update, usuario, podeEditar
    }),
    /* Balanceamento por produto: oculto do menu, mas a tela continua
       viva. A carteira responde "quando fica pronto e quem faz"; esta
       responde "a linha está equilibrada e onde mexer" — meta por hora,
       onde colocar a próxima pessoa, onde sobra gente e dimensionamento
       pela demanda. Nada disso existe em outro lugar, então o código
       fica: reativar é trocar uma linha, refazer seria semanas. */
    qual === 'balanceamento' && produto && React.createElement(BalanceamentoLinha, {
      produto, db, update, usuario
    }),
    qual === 'carteira' && React.createElement(BalanceamentoCarteira, {
      db, update, usuario, podeEditar
    }),
    /* Lead time: oculto do menu. A carteira responde a mesma pergunta
       com dados melhores — ela sabe quem faz cada operação e confronta
       com a entrega. Esta tela calculava a travessia por fila estimada,
       o que era o melhor possível antes da programação nominal existir. */
    qual === 'leadtime' && produto && React.createElement(LeadTimeProduto, {
      produto, db, update, usuario, podeEditar
    }),
    qual === 'cronograma' && React.createElement(CronogramaDoDia, {
      db, update, usuario, podeEditar
    }),
    qual === 'apontar' && React.createElement(FimDeJornada, {
      db, update, usuario, podeEditar
    }),
    qual === 'avaliacao' && React.createElement(AvaliacaoColaborador, {
      db, update, usuario, podeEditar
    }));
}

/* ---------------- Ordens de produção ---------------- */
function TelaOrdens({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [sel, setSel] = useState(null);
  const [fSit, setFSit] = useState('');
  const [nova, setNova] = useState(null);
  function abrirOrdem(dados) {
    let r = null;
    update(d => {
      r = criarOrdem(d, dados, usuario);
      return d;
    });
    if (r && r.erro) return alert(r.erro);
    setNova(null);
    if (r && r.ordem) setSel(r.ordem.id);
  }
  const lista = (db.ordens || []).filter(o => !fSit || o.situacao === fSit).sort((a, b) => {
    const pa = num(a.prioridade),
      pb = num(b.prioridade);
    if (pa !== pb) return pa - pb;
    return String(a.entrega || '9999').localeCompare(String(b.entrega || '9999'));
  });
  if (sel) return /*#__PURE__*/React.createElement(OrdemDetalhe, {
    ordemId: sel,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    onVoltar: () => setSel(null)
  });
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, (db.ordens || []).length, " ordem(ns)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setNova({
      produtoId: '',
      quantidade: '',
      clienteId: '',
      entrega: '',
      prioridade: 5,
      observacao: '',
      anexos: []
    })
  }, "+ Ordem de produção")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "A ordem nasce do produto: escolha, diga quantas peças e ela já vem com as etapas do processo. Indique quem executa cada uma para o cronograma reservar a pessoa certa."), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: fSit,
    onChange: e => setFSit(e.target.value),
    style: {
      maxWidth: 220
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas"), SITUACOES_OP.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome))))), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma ordem. Clique em + Ordem de produção para abrir a primeira."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "OP"), /*#__PURE__*/React.createElement("th", null, "Cliente"), /*#__PURE__*/React.createElement("th", null, "Produto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Qtd"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Horas"), /*#__PURE__*/React.createElement("th", null, "Alertas"), /*#__PURE__*/React.createElement("th", null, "Entrega"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 44
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lista.map(o => {
    const cli = (db.clientes || []).find(c => c.id === o.clienteId);
    const prod = (db.produtos || []).find(p => p.id === o.produtoId);
    const sit = situacaoOP(o.situacao);
    const semResp = (o.tarefas || []).filter(t => !t.colaboradorId).length;
    return /*#__PURE__*/React.createElement("tr", {
      key: o.id
    }, /*#__PURE__*/React.createElement("td", {
      className: "small",
      style: {
        fontFamily: 'var(--mono)'
      }
    }, /*#__PURE__*/React.createElement("strong", null, o.codigo), /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "prior. ", num(o.prioridade) || 5)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, cli ? cli.nome : /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "interno")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, prod ? montarNomeProduto(prod, db) || prod.codigo : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, o.quantidade), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, (minutosDaOrdem(o) / 60).toFixed(1)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, (() => {
      const sync = ordemDesatualizada(o, db);
      const mat = materiaisDaOrdem(o, db);
      const alertas = [];
      if (sync.desatualizada) alertas.push(/*#__PURE__*/React.createElement("div", {
        key: "s",
        style: {
          color: 'var(--warn)'
        }
      }, "desatualizada"));
      if (mat.falta) alertas.push(/*#__PURE__*/React.createElement("div", {
        key: "m",
        style: {
          color: 'var(--bad)'
        }
      }, "sem material"));else if (mat.indefinido) alertas.push(/*#__PURE__*/React.createElement("div", {
        key: "i",
        style: {
          color: 'var(--warn)'
        }
      }, "consumo indefinido"));
      if (semResp > 0) alertas.push(/*#__PURE__*/React.createElement("div", {
        key: "r",
        className: "muted"
      }, semResp, " sem quem faça"));
      return alertas.length ? alertas : /*#__PURE__*/React.createElement("span", {
        style: {
          color: 'var(--ok)'
        }
      }, "✓ pronta");
    })()), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, o.entrega ? fmtDate(o.entrega) : '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
      tone: sit.tone
    }, sit.nome)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-accent",
      title: "Abrir",
      onClick: () => setSel(o.id)
    }, "👁")));
  })))), nova && /*#__PURE__*/React.createElement(OrdemNovaModal, {
    dados: nova,
    db: db,
    onChange: setNova,
    onClose: () => setNova(null),
    onSalvar: abrirOrdem
  }));
}

/* A abertura da ordem: o mínimo para a fábrica começar */
function OrdemNovaModal({
  dados,
  db,
  onChange,
  onClose,
  onSalvar
}) {
  const f = dados;
  const set = (k, v) => onChange({
    ...f,
    [k]: v
  });
  const produto = (db.produtos || []).find(p => p.id === f.produtoId);
  const qtd = num(f.quantidade);

  /* o que a escolha implica, antes de confirmar */
  const previa = produto && qtd > 0 ? (() => {
    const r = resumoProcesso(produto, db, qtd);
    const mo = custoMaoDeObra(produto, db, qtd);
    const c = custearProduto(produto, db);
    const fingida = {
      id: 'previa',
      produtoId: produto.id,
      quantidade: qtd,
      tarefas: []
    };
    const mat = materiaisDaOrdem(fingida, db);
    return {
      r,
      mo,
      c,
      mat,
      trabalho: r.minutosHomem * qtd,
      custo: (c.custoTecido + mo.total) * qtd
    };
  })() : null;
  const LIMITE = 900 * 1024;
  async function anexar(e) {
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > LIMITE) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa do ` + `limite de 900 KB. Reduza o arquivo.`);
    try {
      const url = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
        r.readAsDataURL(file);
      });
      onChange({
        ...f,
        anexos: [...(f.anexos || []), {
          id: uid(),
          nome: file.name,
          tamanho: file.size,
          tipo: file.type || '',
          url,
          quando: agoraISO()
        }]
      });
    } catch (err) {
      alert(err.message);
    }
  }
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Nova ordem de produção",
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-form"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-num"
  }, "1"), /*#__PURE__*/React.createElement("div", {
    className: "passo-corpo"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-rot"
  }, "O que produzir"), /*#__PURE__*/React.createElement(Field, {
    label: "Produto *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.produtoId,
    autoFocus: true,
    onChange: e => set('produtoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha o produto —"), (() => {
    /* liberados primeiro: é o que se produz no dia a dia */
    const todos = db.produtos || [];
    const liberados = todos.filter(p => statusProduto(statusDoProduto(p)).produz);
    const resto = todos.filter(p => !liberados.includes(p));
    const opcao = p => /*#__PURE__*/React.createElement("option", {
      key: p.id,
      value: p.id
    }, codigoCompletoProduto(p, db), " · ", montarNomeProduto(p, db) || '(sem nome)', (p.processo || []).length === 0 ? ' — sem processo' : '');
    if (resto.length === 0) return todos.map(opcao);
    return [liberados.length ? /*#__PURE__*/React.createElement("optgroup", {
      key: "lib",
      label: "Liberados para produção"
    }, liberados.map(opcao)) : null, /*#__PURE__*/React.createElement("optgroup", {
      key: "out",
      label: "Ainda não liberados"
    }, resto.map(p => /*#__PURE__*/React.createElement("option", {
      key: p.id,
      value: p.id
    }, codigoCompletoProduto(p, db), " · ", montarNomeProduto(p, db) || '(sem nome)', ` — ${statusProduto(statusDoProduto(p)).nome.toLowerCase()}`)))];
  })())), produto && (() => {
    const st = statusProduto(statusDoProduto(produto));
    const semProcesso = (produto.processo || []).length === 0;
    if (st.produz && !semProcesso) return null;
    const ck = checklistEngenharia(produto, db);
    const bloqueado = !st.amostra || semProcesso;
    return /*#__PURE__*/React.createElement("div", {
      className: "aviso-passo",
      style: {
        borderColor: bloqueado ? 'var(--danger)' : 'var(--warn)',
        background: bloqueado ? 'rgba(176,58,46,.06)' : 'var(--warn-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        fontWeight: 600,
        color: bloqueado ? 'var(--danger)' : 'var(--warn)'
      }
    }, semProcesso ? 'Produto sem processo produtivo' : `Produto ${st.nome.toLowerCase()}`), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, semProcesso ? /*#__PURE__*/React.createElement(React.Fragment, null, "Sem etapas cadastradas não há o que programar. Abra Produtos → ", montarNomeProduto(produto, db) || produto.codigo, ' ', "→ Processo produtivo.") : st.amostra ? /*#__PURE__*/React.createElement(React.Fragment, null, "Ordem normal não pode ser aberta ainda.", ck.pendentesObrigatorios.length > 0 && ` Falta na engenharia: ${ck.pendentesObrigatorios.map(i => i.nome.toLowerCase()).join(', ')}.`, ' ', "Para aprovar o modelo, marque como amostra abaixo.") : 'Este produto não pode ser produzido, nem como amostra.'), st.amostra && !semProcesso && /*#__PURE__*/React.createElement("label", {
      className: "check",
      style: {
        marginTop: 8
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      checked: !!f.amostra,
      onChange: e => set('amostra', e.target.checked)
    }), /*#__PURE__*/React.createElement("span", null, "Esta é uma ordem de amostra / protótipo")));
  })(), /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    style: {
      maxWidth: 180
    },
    value: f.quantidade,
    onChange: e => set('quantidade', e.target.value)
  })), produto && qtd > 0 && previa && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, previa.r.passos.length, " etapa(s) ·", ' ', duracao(previa.trabalho), " de trabalho ·", ' ', previa.r.passos[0] && previa.r.passos[0].departamento ? `começa em ${previa.r.passos[0].departamento.nome}` : ''))), /*#__PURE__*/React.createElement("div", {
    className: "passo-form"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-num"
  }, "2"), /*#__PURE__*/React.createElement("div", {
    className: "passo-corpo"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-rot"
  }, "Para quem e para quando"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Cliente"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.clienteId,
    onChange: e => set('clienteId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— produção interna —"), (db.clientes || []).filter(c => c.ativo !== false).map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Entrega"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.entrega,
    onChange: e => set('entrega', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Prioridade (1 = mais urgente)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    max: "9",
    value: f.prioridade,
    onChange: e => set('prioridade', e.target.value)
  }))), produto && qtd > 0 && f.entrega && (() => {
    /* a entrega vai junto: é ela que diz se atrasa e por quantos dias */
    const fingida = {
      id: 'previa',
      produtoId: produto.id,
      quantidade: qtd,
      entrega: f.entrega,
      tarefas: tarefasDoProduto(produto, db)
    };
    const prev = previsaoTermino(fingida, db);
    if (!prev.data) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        marginTop: -4,
        color: prev.atrasada ? 'var(--danger)' : 'var(--ok)',
        fontWeight: prev.atrasada ? 600 : 400
      }
    }, prev.atrasada ? `Atenção: pelo ritmo atual só ficaria pronta em ${fmtDate(prev.data)}, ` + `${prev.diasDeAtraso} dia(s) depois do combinado.` : `Pelo ritmo atual, a ordem fica pronta em ${fmtDate(prev.data)} — cabe no prazo.`);
  })(), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.observacao,
    placeholder: "o que a fábrica precisa saber",
    onChange: e => set('observacao', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Arquivos (arte, referência)"), /*#__PURE__*/React.createElement("div", {
    className: "anexos"
  }, (f.anexos || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", Math.round(num(x.tamanho) / 1024), " KB")), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => onChange({
      ...f,
      anexos: (f.anexos || []).filter(y => y.id !== x.id)
    })
  }, "×"))), (f.anexos || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum arquivo"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📎 Anexar", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: anexar
  }))))), previa && /*#__PURE__*/React.createElement("div", {
    className: "passo-form"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-num"
  }, "3"), /*#__PURE__*/React.createElement("div", {
    className: "passo-corpo"
  }, /*#__PURE__*/React.createElement("div", {
    className: "passo-rot"
  }, "O que esta ordem significa"), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Etapas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, previa.r.passos.length)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Trabalho"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(previa.trabalho))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Material"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, money(previa.c.custoTecido * qtd))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Mão de obra"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, money(previa.mo.total * qtd))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Custo total"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, money(previa.custo)))), previa.mat.falta && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      fontWeight: 600,
      marginTop: 6
    }
  }, "⛔ ", previa.mat.resumo), previa.mat.indefinido && !previa.mat.falta && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      marginTop: 6
    }
  }, "⚠ ", previa.mat.resumo))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !f.produtoId || !(qtd > 0) || !!(produto && (produto.processo || []).length === 0),
    onClick: () => onSalvar(f)
  }, "Abrir ordem")));
}
function OrdemDetalhe({
  ordemId,
  db,
  update,
  usuario,
  podeEditar,
  onVoltar
}) {
  const [inicio, setInicio] = useState(todayISO());
  const [qtdTexto, setQtdTexto] = useState(null);
  const LIMITE_ANEXO = 900 * 1024;
  async function anexarNaOrdem(e) {
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > LIMITE_ANEXO) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa do ` + `limite de 900 KB. Reduza o arquivo.`);
    try {
      const url = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
        r.readAsDataURL(file);
      });
      update(d => {
        d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
          ...o,
          anexos: [...(o.anexos || []), {
            id: uid(),
            nome: file.name,
            tamanho: file.size,
            tipo: file.type || '',
            url,
            quando: agoraISO()
          }]
        });
        return d;
      });
    } catch (err) {
      alert(err.message);
    }
  }
  const ordem = (db.ordens || []).find(o => o.id === ordemId);
  if (!ordem) return /*#__PURE__*/React.createElement(Empty, {
    text: "Ordem não encontrada."
  });
  const prod = (db.produtos || []).find(p => p.id === ordem.produtoId);
  const cli = (db.clientes || []).find(c => c.id === ordem.clienteId);
  const prog = agendarFabrica(db, {
    somenteOrdem: ordemId,
    inicio
  });
  const meu = prog.vazio ? null : (prog.ordens || [])[0];
  const setO = (k, v) => update(d => {
    d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
      ...o,
      [k]: v
    });
    return d;
  });
  const setT = (id, patch) => update(d => {
    d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
      ...o,
      tarefas: (o.tarefas || []).map(t => t.id === id ? {
        ...t,
        ...patch
      } : t)
    });
    return d;
  });
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, ordem.codigo, cli && ` · ${cli.nome}`), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0
    }
  }, prod ? montarNomeProduto(prod, db) || prod.codigo : 'Produto removido')), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar")), (() => {
    const sync = ordemDesatualizada(ordem, db);
    if (!sync.desatualizada) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--warn)',
        background: 'var(--warn-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginBottom: 5
      }
    }, "⚠ Esta ordem não reflete mais o produto:"), sync.motivos.map((x, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      className: "small"
    }, "· ", x)), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn accent sm",
      style: {
        marginTop: 8
      },
      onClick: () => {
        let r = null;
        update(d => {
          r = ressincronizarOrdem(d, ordemId, usuario);
          return d;
        });
        if (r && r.erro) alert(r.erro);
      }
    }, "Atualizar a ordem"), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 5
      }
    }, "Atualizar refaz as etapas e a quantidade, mantendo quem foi designado e o que já está concluído."));
  })(), /*#__PURE__*/React.createElement(AndamentoDaOrdem, {
    ordem: ordem,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), (() => {
    const mat = materiaisDaOrdem(ordem, db);
    if (mat.semProduto || mat.linhas.length === 0) return null;
    /* A lista de materiais é o que vai para o almoxarifado buscar o
       tecido. Sem botão de impressão, alguém copiava à mão. */
    const imprimir = () => {
      const w = window.open('', '_blank');
      if (!w) return alert('O navegador bloqueou a janela de impressão.');
      const linhas = mat.linhas.map(l => {
        const nome = l.material ? (l.material.nome || l.material.codigo || '—') : '—';
        const n = v => Number(v || 0).toLocaleString('pt-BR',
          { maximumFractionDigits: 2 });
        const sit = l.semConsumo ? 'consumo não definido'
          : l.falta > 0 ? `FALTAM ${n(l.falta)} ${l.unidade}` : 'ok';
        return `<tr><td>${nome}</td>` +
          `<td class="n">${l.semConsumo ? '—' : n(l.necessario) + ' ' + l.unidade}</td>` +
          `<td class="n">${n(l.livre)} ${l.unidade}</td>` +
          `<td>${sit}</td></tr>`;
      }).join('');
      const prod = (db.produtos || []).find(p => p.id === ordem.produtoId);
      w.document.write(`<!doctype html><html lang="pt-BR"><head>
        <meta charset="utf-8"><title>Materiais · ${ordem.codigo}</title>
        <style>
          body{font-family:Georgia,serif; padding:26px; color:#1a1a1a;}
          h1{font-size:19px; margin:0 0 2px;}
          .sub{font-size:12px; color:#666; margin-bottom:16px;}
          table{width:100%; border-collapse:collapse; font-size:12.5px;}
          th{text-align:left; border-bottom:2px solid #333; padding:6px 8px;
             font-size:10px; text-transform:uppercase; letter-spacing:.06em;}
          td{border-bottom:1px solid #ddd; padding:6px 8px;}
          .n{text-align:right; font-variant-numeric:tabular-nums;}
          .rodape{margin-top:22px; font-size:11px; color:#666;}
          .assina{margin-top:34px; display:flex; gap:40px;}
          .assina div{flex:1; border-top:1px solid #333; padding-top:5px;
            font-size:10.5px; color:#666;}
          @media print{ body{padding:12px;} }
        </style></head><body>
        <h1>Materiais · ${ordem.codigo}</h1>
        <div class="sub">${prod ? montarNomeProduto(prod, db) : ''} ·
          ${ordem.quantidade} peça(s)${ordem.entrega ? ' · entrega ' + fmtDate(ordem.entrega) : ''}</div>
        <table><thead><tr><th>Material</th><th class="n">Necessário</th>
          <th class="n">Em estoque</th><th>Situação</th></tr></thead>
          <tbody>${linhas}</tbody></table>
        <div class="rodape">${mat.resumo}</div>
        <div class="assina"><div>Separado por</div><div>Conferido por</div>
          <div>Data</div></div>
        </body></html>`);
      w.document.close();
      w.focus();
      setTimeout(() => w.print(), 250);
    };
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: mat.falta ? 'var(--bad)' : mat.indefinido ? 'var(--warn)' : 'var(--ok)',
        background: mat.falta ? 'var(--bad-bg)' : mat.indefinido ? 'var(--warn-bg)' : 'var(--ok-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: mat.falta ? 'var(--bad)' : mat.indefinido ? 'var(--warn)' : 'var(--ok)',
        fontWeight: 600,
        marginBottom: 6
      }
    }, mat.falta ? '⛔ ' : mat.indefinido ? '⚠ ' : '✓ ', mat.resumo,
      /*#__PURE__*/React.createElement("button", {
        className: "btn ghost sm no-print",
        style: { marginLeft: 10 },
        onClick: imprimir
      }, "Imprimir lista")), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Por peça"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Necessário"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Livre"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Falta"))), /*#__PURE__*/React.createElement("tbody", null, mat.linhas.map(l => /*#__PURE__*/React.createElement("tr", {
      key: l.material.id,
      className: l.suficiente && !l.semConsumo ? '' : 'linha-atraso'
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, l.material.nome)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, l.porPeca.toFixed(4), " ", l.unidade), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, l.necessario.toFixed(3)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, l.livre.toFixed(3), l.reservado > 0 && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, l.reservado.toFixed(2), " reservado")), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: l.falta > 0 ? {
        color: 'var(--bad)',
        fontWeight: 600
      } : {}
    }, l.semConsumo ? /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--warn)'
      }
    }, "sem consumo") : l.falta > 0 ? l.falta.toFixed(3) : '—'))))), podeEditar && !mat.falta && !mat.indefinido && !ordem.materialReservado && /*#__PURE__*/React.createElement("button", {
      className: "btn accent sm",
      style: {
        marginTop: 8
      },
      onClick: () => {
        let r = null;
        update(d => {
          r = reservarMateriaisDaOrdem(d, ordemId, usuario);
          return d;
        });
        if (r && r.erro) alert(r.erro);
      }
    }, "Reservar o material desta ordem"), ordem.materialReservado && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--ok)',
        fontWeight: 600,
        marginTop: 6
      }
    }, "✓ material reservado em ", fmtDate(String(ordem.reservadoEm || '').slice(0, 10))));
  })(), (() => {
    /* A ordem entra e o plano nasce: o sistema usa o tempo do roteiro,
       vê quem está livre e habilitado, e encadeia as etapas. É ponto de
       partida — o ajuste fino é na Programação. */
    if (!podeEditar || ordem.situacao === 'concluida') return null;
    const jaTem = (db.programacoes || []).some(p =>
      p.status !== 'encerrada' &&
      (p.itens || []).some(x => x.ordemId === ordem.id));
    return /*#__PURE__*/React.createElement("div", {
      className: "panel no-print",
      style: { borderColor: jaTem ? 'var(--line)' : 'var(--thread)' }
    },
      /*#__PURE__*/React.createElement("div", { className: "row-actions" },
        /*#__PURE__*/React.createElement("div", { style: { flex: 1 } },
          /*#__PURE__*/React.createElement("div", { className: "small",
            style: { fontWeight: 600 } },
            jaTem ? "Esta ordem já está programada."
                  : "Esta ordem ainda não foi programada."),
          /*#__PURE__*/React.createElement("div", { className: "small muted" },
            jaTem
              ? "Veja e ajuste em Produção → Programação. Programar de novo "
                + "acrescenta o que faltar, sem mexer no que já existe."
              : "O sistema monta o plano pelo tempo do roteiro, com quem "
                + "está livre e habilitado, encadeando as etapas.")),
        /*#__PURE__*/React.createElement(Field, { label: "Começar em" },
          /*#__PURE__*/React.createElement("input", {
            type: "date", value: inicio,
            onChange: e => setInicio(e.target.value)
          })),
        /*#__PURE__*/React.createElement("button", {
          className: jaTem ? "btn ghost" : "btn accent",
          onClick: () => {
            let msg = null;
            update(d => {
              const r = programarOrdem(d, ordem.id, { inicio }, usuario);
              if (r.erro) { msg = r.erro; return d; }
              const partes = [];
              partes.push(`${r.itens.length} operação(ões) programada(s), `
                + `de ${fmtDate(r.primeiroDia)} a ${fmtDate(r.ultimoDia)}.`);
              if (r.cabeNaEntrega === false)
                partes.push(`\nATENÇÃO: termina depois da entrega `
                  + `(${fmtDate(ordem.entrega)}). Ajuste a equipe ou o prazo.`);
              if (r.avisos.length)
                partes.push('\n\n' + r.avisos.join('\n'));
              partes.push('\n\nO plano é um ponto de partida: ajuste em '
                + 'Produção → Programação antes de publicar.');
              msg = partes.join('');
              return d;
            });
            if (msg) alert(msg);
          }
        }, jaTem ? "Programar novamente" : "Programar produção")));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Quantidade"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, ordem.quantidade)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Trabalho"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (minutosDaOrdem(ordem) / 60).toFixed(1)), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "horas-homem")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Previsão"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      fontSize: 17
    }
  }, meu && meu.fim ? fmtDate(meu.fim) : '—'), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "se começar hoje")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Entrega"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      fontSize: 17,
      ...(meu && meu.atraso ? {
        color: 'var(--bad)'
      } : {})
    }
  }, ordem.entrega ? fmtDate(ordem.entrega) : '—'), meu && meu.atraso && /*#__PURE__*/React.createElement("div", {
    className: "sub",
    style: {
      color: 'var(--bad)'
    }
  }, meu.diasDeAtraso, " dia(s) de atraso"))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: ordem.situacao,
    disabled: !podeEditar,
    onChange: e => setO('situacao', e.target.value)
  }, SITUACOES_OP.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Prioridade"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    max: "9",
    value: num(ordem.prioridade) || 5,
    disabled: !podeEditar,
    onChange: e => setO('prioridade', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Entrega"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: ordem.entrega || '',
    disabled: !podeEditar,
    onChange: e => setO('entrega', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Cliente"
  }, /*#__PURE__*/React.createElement("select", {
    value: ordem.clienteId || '',
    disabled: !podeEditar,
    onChange: e => setO('clienteId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— produção interna —"), (db.clientes || []).filter(c => c.ativo !== false).map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: qtdTexto !== null ? qtdTexto : num(ordem.quantidade),
    disabled: !podeEditar,
    onChange: e => setQtdTexto(e.target.value),
    onBlur: () => {
      if (qtdTexto === null) return;
      if (num(qtdTexto) === num(ordem.quantidade)) return setQtdTexto(null);
      let r = null;
      update(d => {
        r = alterarQuantidadeDaOrdem(d, ordemId, qtdTexto, usuario);
        return d;
      });
      if (r && r.erro) alert(r.erro);
      setQtdTexto(null);
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "mudar recalcula os minutos das etapas")), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: ordem.observacao || '',
    disabled: !podeEditar,
    onChange: e => setO('observacao', e.target.value)
  })))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Arquivos"), /*#__PURE__*/React.createElement("div", {
    className: "anexos"
  }, (ordem.anexos || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", Math.round(num(x.tamanho) / 1024), " KB")), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => update(d => {
      d.ordens = d.ordens.map(o => o.id !== ordemId ? o : {
        ...o,
        anexos: (o.anexos || []).filter(y => y.id !== x.id)
      });
      return d;
    })
  }, "×"))), (ordem.anexos || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum arquivo anexado"), podeEditar && /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📎 Anexar", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: anexarNaOrdem
  })))));
}

/* ---------------- Avaliação do colaborador ---------------- */
function AvaliacaoColaborador({
  db,
  update,
  usuario,
  podeEditar
}) {
  const hoje = todayISO();
  const trintaDias = new Date(Date.now() - 30 * 86400000).toISOString().slice(0, 10);
  const [de, setDe] = useState(trintaDias);
  const [ate, setAte] = useState(hoje);
  const [sel, setSel] = useState('');
  const [nota, setNota] = useState(null);
  const periodo = {
    de,
    ate
  };
  const geral = desempenhoGeral(db, periodo);
  const foco = sel ? desempenhoColaborador(sel, db, periodo) : null;
  const vePessoal = podeExecutar(usuario, 'pessoas') || podeExecutar(usuario, 'admin');
  function salvarNota(f) {
    if (!f.colaboradorId) return;
    update(d => {
      d.avaliacoes = [...(d.avaliacoes || []), {
        ...f,
        id: uid(),
        de,
        ate,
        criadoEm: agoraISO(),
        avaliador: usuario ? usuario.nome : ''
      }];
      registrarLog(d, usuario, 'Registrou avaliação', `${f.colaborador || ''} · nota ${f.nota}`);
      return d;
    });
    setNota(null);
  }
  const historico = (db.avaliacoes || []).filter(a => !sel || a.colaboradorId === sel).sort((a, b) => String(b.criadoEm).localeCompare(String(a.criadoEm)));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O que sai do fim de jornada: quanto cada pessoa produziu, com que qualidade e quão perto do tempo previsto. Os números são da fábrica, não opinião."), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "De"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: de,
    onChange: e => setDe(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Até"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: ate,
    onChange: e => setAte(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Colaborador"
  }, /*#__PURE__*/React.createElement("select", {
    value: sel,
    onChange: e => setSel(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.colaboradores || []).filter(c => c.status !== 'Inativo').map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 200
    }
  }, "Eficiência acima de 100% significa terminar antes do previsto."), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => window.print()
  }, "🖨 Imprimir"))), geral.pessoas === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum apontamento no período. Os dados vêm da aba Fim de jornada."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Pessoas com registro"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, geral.pessoas)), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Peças boas"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, geral.boas)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Qualidade"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: geral.qualidade !== null && geral.qualidade < 95 ? {
      color: 'var(--warn)'
    } : {}
  }, geral.qualidade !== null ? geral.qualidade + '%' : '—'), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, geral.defeitos, " com defeito")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Eficiência"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: geral.eficiencia < 90 ? {
      color: 'var(--warn)'
    } : {}
  }, geral.eficiencia, "%"), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, geral.atrasos, " atraso(s)"))), /*#__PURE__*/React.createElement("h3", null, "Por colaborador"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0,
      overflowX: 'auto'
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Colaborador"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Dias"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Previsto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Gasto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Eficiência"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Defeitos"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Qualidade"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Atrasos"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 60
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, geral.linhas.map(x => /*#__PURE__*/React.createElement("tr", {
    key: x.colaborador ? x.colaborador.id : x.boas,
    className: x.qualidade !== null && x.qualidade < 95 ? 'linha-atraso' : ''
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, x.colaborador ? x.colaborador.nome : '—')), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, x.dias), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, duracao(x.previsto)), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, duracao(x.realizado)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", {
    style: x.eficiencia < 90 ? {
      color: 'var(--warn)'
    } : x.eficiencia >= 100 ? {
      color: 'var(--ok)'
    } : {}
  }, x.eficiencia, "%")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, x.boas), /*#__PURE__*/React.createElement("td", {
    className: "num",
    style: x.defeitos > 0 ? {
      color: 'var(--bad)'
    } : {}
  }, x.defeitos || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, x.qualidade !== null ? x.qualidade + '%' : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, x.atrasos || '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-accent",
    title: "Detalhar",
    onClick: () => setSel(x.colaborador ? x.colaborador.id : '')
  }, "👁")))))))), foco && !foco.semDados && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, foco.colaborador ? foco.colaborador.nome : 'Colaborador'), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Defeitos por tipo"), foco.porDefeito.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "✓ Nenhum defeito no período.") : foco.porDefeito.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.nome,
    className: "row-actions",
    style: {
      marginBottom: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      flex: 1
    }
  }, d.nome), /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, d.n)))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Motivos de atraso"), foco.porMotivo.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "✓ Nenhum atraso registrado.") : foco.porMotivo.map(m => /*#__PURE__*/React.createElement("div", {
    key: m.nome,
    className: "row-actions",
    style: {
      marginBottom: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      flex: 1
    }
  }, m.nome), /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, m.n, "×"))))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Dia"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Previsto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Gasto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Defeitos"), /*#__PURE__*/React.createElement("th", null, "Motivo"))), /*#__PURE__*/React.createElement("tbody", null, foco.lancamentos.map(a => /*#__PURE__*/React.createElement("tr", {
    key: a.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, fmtDate(a.data)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, a.etapa || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, duracao(a.minutosPrevistos)), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, duracao(a.minutosGastos)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, num(a.pecasBoas)), /*#__PURE__*/React.createElement("td", {
    className: "num",
    style: num(a.pecasDefeito) > 0 ? {
      color: 'var(--bad)'
    } : {}
  }, num(a.pecasDefeito) || '—'), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, a.motivoAtraso || '—', a.observacao && /*#__PURE__*/React.createElement("div", null, a.observacao))))))), vePessoal && podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setNota({
      colaboradorId: foco.colaborador ? foco.colaborador.id : '',
      colaborador: foco.colaborador ? foco.colaborador.nome : '',
      nota: '',
      pontos: '',
      melhorar: ''
    })
  }, "Registrar avaliação do período"))), historico.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "Avaliações registradas"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Colaborador"), /*#__PURE__*/React.createElement("th", null, "Período"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Nota"), /*#__PURE__*/React.createElement("th", null, "Pontos fortes"), /*#__PURE__*/React.createElement("th", null, "A melhorar"), /*#__PURE__*/React.createElement("th", null, "Avaliador"))), /*#__PURE__*/React.createElement("tbody", null, historico.map(a => /*#__PURE__*/React.createElement("tr", {
    key: a.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, a.colaborador)), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, fmtDate(a.de), " a ", fmtDate(a.ate)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, a.nota)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, a.pontos || '—'), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, a.melhorar || '—'), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, a.avaliador))))))), nota && /*#__PURE__*/React.createElement(Modal, {
    title: `Avaliação de ${nota.colaborador}`,
    onClose: () => setNota(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Período de ", fmtDate(de), " a ", fmtDate(ate), ". Os números da produção ficam registrados junto; aqui vai a leitura de quem acompanha."), /*#__PURE__*/React.createElement(Field, {
    label: "Nota de 1 a 5 *"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 5
    }
  }, [1, 2, 3, 4, 5].map(n => /*#__PURE__*/React.createElement("button", {
    key: n,
    type: "button",
    className: "comp-chip" + (num(nota.nota) === n ? ' on' : ''),
    onClick: () => setNota(x => ({
      ...x,
      nota: n
    }))
  }, n)))), /*#__PURE__*/React.createElement(Field, {
    label: "Pontos fortes"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: nota.pontos,
    onChange: e => setNota(x => ({
      ...x,
      pontos: e.target.value
    }))
  })), /*#__PURE__*/React.createElement(Field, {
    label: "A melhorar"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: nota.melhorar,
    onChange: e => setNota(x => ({
      ...x,
      melhorar: e.target.value
    }))
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setNota(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !num(nota.nota),
    onClick: () => salvarNota(nota)
  }, "Salvar avaliação"))));
}

/* ---------------- Fim de jornada ----------------
   A pessoa (ou o líder) fecha o dia: o que rendeu, o que perdeu,
   e por que atrasou. É daqui que sai a avaliação. */
function FimDeJornada({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [data, setData] = useState(todayISO());
  const [modal, setModal] = useState(null);
  const [concluir, setConcluir] = useState(null);
  const [fColab, setFColab] = useState('');
  const prog = agendarFabrica(db, {
    inicio: data
  });
  const doDia = (prog.dias || []).find(d => d.iso === data);
  const lancados = apontamentosDoDia(data, db).filter(a => !fColab || a.colaboradorId === fColab);

  /* o que a programação previu para cada pessoa naquele dia */
  const previstos = [];
  if (doDia) doDia.tarefas.forEach(t => {
    t.pessoas.forEach(p => {
      previstos.push({
        colaboradorId: p.id,
        colaborador: p.nome,
        ordemId: t.ordemId,
        ordem: t.ordem,
        produto: t.produto,
        etapa: t.etapa,
        departamento: t.departamento,
        equipamento: t.equipamento,
        inicio: p.inicio,
        fim: p.fim,
        minutos: p.minutos,
        pecas: t.pecas,
        jaLancado: lancados.some(a => a.colaboradorId === p.id && a.ordemId === t.ordemId && a.etapa === t.etapa)
      });
    });
  });
  const lista = previstos.filter(x => !fColab || x.colaboradorId === fColab);
  function salvar(f) {
    if (!f.colaboradorId) return alert('Escolha o colaborador.');
    const boas = num(f.pecasBoas),
      def = num(f.pecasDefeito);
    if (def > 0 && (f.defeitos || []).length === 0) return alert('Informe o tipo dos defeitos — é isso que a avaliação usa.');
    const somaDef = (f.defeitos || []).reduce((a, d) => a + num(d.quantidade), 0);
    if (def > 0 && Math.abs(somaDef - def) > 0.01) return alert(`As quantidades por tipo somam ${somaDef}, mas você informou ${def} ` + `peça(s) com defeito.`);
    update(d => {
      const reg = {
        ...f,
        id: f.id || uid(),
        data: f.data || data,
        pecasBoas: boas,
        pecasDefeito: def,
        minutosPrevistos: num(f.minutosPrevistos),
        minutosGastos: num(f.minutosGastos),
        lancadoEm: agoraISO(),
        lancadoPor: usuario ? usuario.nome : ''
      };
      d.apontamentos = f.id ? (d.apontamentos || []).map(x => x.id === reg.id ? reg : x) : [...(d.apontamentos || []), reg];
      registrarLog(d, usuario, 'Apontou fim de jornada', `${reg.data} · ${reg.colaborador || ''} · ${reg.etapa || ''}`);
      return d;
    });
    setModal(null);
  }
  function remover(a) {
    if (!confirm('Remover este apontamento?')) return;
    update(d => {
      d.apontamentos = (d.apontamentos || []).filter(x => x.id !== a.id);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Ao fim do dia, cada pessoa fecha o que fez: peças boas, defeitos e o motivo de qualquer atraso. O programado aparece ao lado para comparar."),

  /* As operações que foram iniciadas no cronograma e ainda não
     fecharam. É aqui que a produção se conclui. */
  (() => {
    const c = cronogramaDoDia(db, data);
    if (c.vazio) return null;
    const emCurso = c.itens.filter(x => x.situacao === 'iniciada');
    if (emCurso.length === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--thread)' }
    },
      /*#__PURE__*/React.createElement("h3", { style: { marginTop: 0 } },
        `Em produção agora (${emCurso.length})`),
      /*#__PURE__*/React.createElement("table", null,
        /*#__PURE__*/React.createElement("thead", null,
          /*#__PURE__*/React.createElement("tr", null,
            /*#__PURE__*/React.createElement("th", null, "Operação"),
            /*#__PURE__*/React.createElement("th", null, "Quem"),
            /*#__PURE__*/React.createElement("th", { className: "num" }, "Desde"),
            /*#__PURE__*/React.createElement("th", { className: "no-print" }, ""))),
        /*#__PURE__*/React.createElement("tbody", null,
          emCurso.map(it => /*#__PURE__*/React.createElement("tr", { key: it.id },
            /*#__PURE__*/React.createElement("td", null,
              /*#__PURE__*/React.createElement("strong", null, it.etapa),
              /*#__PURE__*/React.createElement("div", { className: "small muted" },
                (it.ordemCodigo || '') + " · " + it.quantidadePrevista + " peça(s)")),
            /*#__PURE__*/React.createElement("td", { className: "small" },
              (it.pessoas || []).map(id => {
                const p = (db.colaboradores || []).find(x => x.id === id);
                return p ? p.nome.split(' ')[0] : '?';
              }).join(', ') || '—'),
            /*#__PURE__*/React.createElement("td", { className: "num small" },
              fmtHora(it.iniciadoEm),
              it.excesso > 0
                ? /*#__PURE__*/React.createElement("div", {
                    style: { color: 'var(--warn)', fontWeight: 600 }
                  }, "+" + duracao(it.excesso))
                : null),
            podeEditar
              ? /*#__PURE__*/React.createElement("td", { className: "no-print" },
                  /*#__PURE__*/React.createElement("button", {
                    className: "btn accent sm",
                    onClick: () => setConcluir({ item: it, progId: c.programacao.id })
                  }, "Concluir"))
              : null)))),
      /*#__PURE__*/React.createElement("div", { className: "small muted" },
        "O tempo vem do relógio: da hora em que a operação foi iniciada no ",
        "cronograma até agora."));
  })(),

  /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Dia"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: data,
    onChange: e => setData(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Colaborador"
  }, /*#__PURE__*/React.createElement("select", {
    value: fColab,
    onChange: e => setFColab(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.colaboradores || []).filter(c => c.status !== 'Inativo').map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      paddingBottom: 10
    }
  }, podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setModal({
      data
    })
  }, "+ Apontamento avulso")))), /*#__PURE__*/React.createElement("h3", null, "Programado para o dia"), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nada programado para esta data. Confira o cronograma ou lance um apontamento avulso."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0,
      overflowX: 'auto'
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Horário"), /*#__PURE__*/React.createElement("th", null, "Quem"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", null, "OP"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Min"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Peças"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 110
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lista.map((x, i) => /*#__PURE__*/React.createElement("tr", {
    key: i,
    className: x.jaLancado ? 'linha-inativa' : ''
  }, /*#__PURE__*/React.createElement("td", {
    className: "small",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, x.inicio, "–", x.fim), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, x.colaborador)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, x.etapa, /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, x.departamento, x.equipamento && ` · ${x.equipamento}`)), /*#__PURE__*/React.createElement("td", {
    className: "small",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, x.ordem), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(x.minutos)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, x.pecas), /*#__PURE__*/React.createElement("td", null, x.jaLancado ? /*#__PURE__*/React.createElement(Badge, {
    tone: "ok"
  }, "lançado") : podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setModal({
      data,
      colaboradorId: x.colaboradorId,
      colaborador: x.colaborador,
      ordemId: x.ordemId,
      ordem: x.ordem,
      etapa: x.etapa,
      departamento: x.departamento,
      minutosPrevistos: x.minutos,
      minutosGastos: x.minutos,
      pecasBoas: x.pecas,
      pecasDefeito: '',
      defeitos: []
    })
  }, "Apontar"))))))), /*#__PURE__*/React.createElement("h3", null, "Apontado (", lancados.length, ")"), lancados.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum apontamento neste dia."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0,
      overflowX: 'auto'
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Quem"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Previsto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Gasto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Defeitos"), /*#__PURE__*/React.createElement("th", null, "Motivo"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lancados.map(a => {
    const atrasou = num(a.minutosGastos) > num(a.minutosPrevistos) * 1.05;
    return /*#__PURE__*/React.createElement("tr", {
      key: a.id,
      className: atrasou ? 'linha-atraso' : ''
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", null, a.colaborador)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, a.etapa || '—', a.ordem && /*#__PURE__*/React.createElement("div", {
      className: "muted",
      style: {
        fontFamily: 'var(--mono)'
      }
    }, a.ordem)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, duracao(a.minutosPrevistos)), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: atrasou ? {
        color: 'var(--warn)',
        fontWeight: 600
      } : {}
    }, duracao(a.minutosGastos)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, num(a.pecasBoas)), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: num(a.pecasDefeito) > 0 ? {
        color: 'var(--bad)'
      } : {}
    }, num(a.pecasDefeito) || '—'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, a.motivoAtraso || '—', a.observacao && /*#__PURE__*/React.createElement("div", null, a.observacao)), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("div", {
      className: "acoes-icone"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      onClick: () => setModal(a)
    }, "✎"), /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => remover(a)
    }, "×"))));
  })))), modal && /*#__PURE__*/React.createElement(ApontamentoModal, {
    dados: modal,
    db: db,
    onClose: () => setModal(null),
    onSave: salvar
  }),

    concluir && /*#__PURE__*/React.createElement(ConcluirEtapaModal, {
      db, item: concluir.item, usuario,
      onClose: () => setConcluir(null),
      onConfirmar: dados => {
        let erro = null;
        update(d => {
          const r = concluirEtapa(d, concluir.progId, concluir.item.id,
            dados, usuario);
          erro = r.erro || null;
          return d;
        });
        if (erro) alert(erro); else setConcluir(null);
      }
    }));
}
function ApontamentoModal({
  dados,
  db,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    colaboradorId: '',
    colaborador: '',
    etapa: '',
    minutosPrevistos: '',
    minutosGastos: '',
    pecasBoas: '',
    pecasDefeito: '',
    defeitos: [],
    motivoAtraso: '',
    observacao: '',
    ...dados
  });
  /* a unidade acompanha o que já veio da programação */
  const [unPrevisto, setUnPrevisto] = useState(() => unidadeSugerida((dados || {}).minutosPrevistos));
  const [unGasto, setUnGasto] = useState(() => unidadeSugerida((dados || {}).minutosGastos || (dados || {}).minutosPrevistos));
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  /* O previsto sai do roteiro: minutos por peça vezes o que foi
     produzido. Não é campo de digitação — é o que a engenharia definiu,
     e mexer nele à mão faria a comparação com o real perder o sentido. */
  const previstoDoRoteiro = (() => {
    const ordem = (db.ordens || []).find(o => o.id === f.ordemId);
    if (!ordem) return null;
    const produto = (db.produtos || []).find(p => p.id === ordem.produtoId);
    if (!produto) return null;
    const tarefa = (ordem.tarefas || []).find(t => t.id === f.tarefaId);
    const etapaId = tarefa ? tarefa.etapaId : null;
    if (!etapaId) return null;
    const r = resumoProcesso(produto, db);
    const passo = (r.passos || []).find(x => x.passo.etapaId === etapaId);
    if (!passo) return null;
    const porPeca = num(passo.minutosHomem) /
      Math.max(1, num(passo.passo.pessoas) || 1);
    const pecas = num(f.pecasBoas) + num(f.pecasDefeito);
    return {
      porPeca,
      minutos: pecas > 0 ? Number((porPeca * pecas).toFixed(1)) : 0,
      pecas,
    };
  })();

  /* O gasto vem do relógio: início e fim reais. Digitar duração
     enquanto se marca horário é pedir para os dois discordarem. */
  const gastoDoRelogio = (f.inicioReal && f.fimReal)
    ? Math.max(0, Math.round(
        (new Date(f.fimReal) - new Date(f.inicioReal)) / 60000))
    : null;

  const previsto = previstoDoRoteiro ? previstoDoRoteiro.minutos
    : num(f.minutosPrevistos);
  const gasto = gastoDoRelogio !== null ? gastoDoRelogio
    : num(f.minutosGastos);
  const atraso = gasto - previsto;
  const boas = num(f.pecasBoas),
    def = num(f.pecasDefeito);
  const total = boas + def;
  const addDefeito = () => set('defeitos', [...(f.defeitos || []), {
    id: uid(),
    tipo: TIPOS_DEFEITO[0],
    quantidade: '',
    observacao: ''
  }]);
  const setDef = (id, patch) => set('defeitos', (f.defeitos || []).map(d => d.id === id ? {
    ...d,
    ...patch
  } : d));
  const rmDef = id => set('defeitos', (f.defeitos || []).filter(d => d.id !== id));
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Fim de jornada",
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Data"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.data || '',
    onChange: e => set('data', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Colaborador *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.colaboradorId,
    onChange: e => {
      const c = (db.colaboradores || []).find(x => x.id === e.target.value);
      setF(p => ({
        ...p,
        colaboradorId: e.target.value,
        colaborador: c ? c.nome : ''
      }));
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.colaboradores || []).filter(c => c.status !== 'Inativo').map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Etapa"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.etapa || '',
    onChange: e => set('etapa', e.target.value)
  }))), /*#__PURE__*/React.createElement("h3", null, "Quando aconteceu"),
  /*#__PURE__*/React.createElement("div", { className: "grid2" },
    /*#__PURE__*/React.createElement(Field, { label: "Início real" },
      /*#__PURE__*/React.createElement("input", {
        type: "datetime-local", step: "1",
        value: f.inicioReal || '',
        onChange: e => {
          const ini = e.target.value;
          /* marcou os dois pontas? o tempo é a diferença — não faz
             sentido digitar duração e horário e deixar os dois brigarem */
          const min = (ini && f.fimReal)
            ? Math.max(0, Math.round(
                (new Date(f.fimReal) - new Date(ini)) / 60000))
            : f.minutosGastos;
          setF(p => ({ ...p, inicioReal: ini,
            minutosGastos: min, gastoTexto: undefined }));
        }
      })),
    /*#__PURE__*/React.createElement(Field, { label: "Fim real" },
      /*#__PURE__*/React.createElement("input", {
        type: "datetime-local", step: "1",
        value: f.fimReal || '',
        onChange: e => {
          const fim = e.target.value;
          const min = (f.inicioReal && fim)
            ? Math.max(0, Math.round(
                (new Date(fim) - new Date(f.inicioReal)) / 60000))
            : f.minutosGastos;
          setF(p => ({ ...p, fimReal: fim,
            minutosGastos: min, gastoTexto: undefined }));
        }
      }))),
  (() => {
    if (!f.inicioReal || !f.fimReal) return /*#__PURE__*/React.createElement("div", {
      className: "small muted", style: { marginTop: -6, marginBottom: 10 }
    }, "Marcando os dois horários, o tempo gasto é calculado pela diferença. ",
      "Sem eles, informe o tempo abaixo.");
    const min = Math.round(
      (new Date(f.fimReal) - new Date(f.inicioReal)) / 60000);
    if (min < 0) return /*#__PURE__*/React.createElement("div", {
      className: "small", style: { color: 'var(--danger)', fontWeight: 600,
        marginTop: -6, marginBottom: 10 }
    }, "O fim está antes do início.");
    return /*#__PURE__*/React.createElement("div", {
      className: "small muted", style: { marginTop: -6, marginBottom: 10 }
    }, `Das ${fmtHora(f.inicioReal)} às ${fmtHora(f.fimReal)} — `,
      /*#__PURE__*/React.createElement("strong", null, duracao(min)),
      ". O tempo gasto abaixo veio dessa conta.");
  })(),
  /*#__PURE__*/React.createElement("h3", null, "Tempo"),
  /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  },
    /*#__PURE__*/React.createElement(Field, {
      label: "Tempo previsto"
    },
      /*#__PURE__*/React.createElement("div", {
        className: "campo-leitura" + (previsto > 0 ? "" : " vazio")
      }, previsto > 0 ? duracao(previsto) : "—"),
      /*#__PURE__*/React.createElement("div", {
        className: "small muted"
      }, previstoDoRoteiro
        ? (previstoDoRoteiro.pecas > 0
            ? duracao(previstoDoRoteiro.porPeca) + " por peça × "
              + previstoDoRoteiro.pecas + " — vem do roteiro"
            : "informe as peças abaixo para calcular")
        : "esta operação não tem tempo no roteiro")
    ),
    /*#__PURE__*/React.createElement(Field, {
      label: "Tempo gasto"
    },
      /*#__PURE__*/React.createElement("div", {
        className: "campo-leitura" + (gasto > 0 ? "" : " vazio")
      }, gasto > 0 ? duracao(gasto) : "—"),
      /*#__PURE__*/React.createElement("div", {
        className: "small muted"
      }, gastoDoRelogio !== null
        ? "do início ao fim marcados acima"
        : "marque o início e o fim reais acima")
    )
  ),
  /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Diferença"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: atraso > 0 ? {
      color: 'var(--warn)'
    } : atraso < 0 ? {
      color: 'var(--ok)'
    } : {}
  }, atraso > 0 ? '+' : '', duracao(Math.abs(atraso)))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Eficiência"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, gasto > 0 ? Math.round(previsto / gasto * 100) : 0, "%"))), atraso > previsto * 0.05 && /*#__PURE__*/React.createElement(Field, {
    label: "Motivo do atraso *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.motivoAtraso || '',
    onChange: e => set('motivoAtraso', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), MOTIVOS_ATRASO.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x)))), /*#__PURE__*/React.createElement("h3", null, "Produção"), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Peças boas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.pecasBoas,
    onChange: e => set('pecasBoas', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Peças com defeito"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.pecasDefeito,
    onChange: e => set('pecasDefeito', e.target.value)
  }))), total > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, boas, " boas de ", total, " — qualidade de ", (boas / total * 100).toFixed(1), "%."), def > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Que defeitos foram — é o que entra na avaliação"), (f.defeitos || []).map(d => /*#__PURE__*/React.createElement("div", {
    key: d.id,
    className: "linha-campos"
  }, /*#__PURE__*/React.createElement("select", {
    value: d.tipo,
    style: {
      maxWidth: 180,
      margin: 0
    },
    onChange: e => setDef(d.id, {
      tipo: e.target.value
    })
  }, TIPOS_DEFEITO.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x))), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: d.quantidade,
    placeholder: "qtd",
    style: {
      maxWidth: 90,
      margin: 0
    },
    onChange: e => setDef(d.id, {
      quantidade: e.target.value
    })
  }), /*#__PURE__*/React.createElement("input", {
    value: d.observacao,
    placeholder: "detalhe (opcional)",
    style: {
      flex: 1,
      minWidth: 120,
      margin: 0
    },
    onChange: e => setDef(d.id, {
      observacao: e.target.value
    })
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => rmDef(d.id)
  }, "×"))), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: addDefeito
  }, "+ Tipo de defeito"), (() => {
    const soma = (f.defeitos || []).reduce((a, d) => a + num(d.quantidade), 0);
    if (Math.abs(soma - def) < 0.01) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginTop: 6
      }
    }, "⚠ Os tipos somam ", soma, ", mas você informou ", def, " peça(s) com defeito.");
  })()), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacao || '',
    onChange: e => set('observacao', e.target.value),
    placeholder: "o que aconteceu no dia"
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    /* o que vai para o banco é o calculado, não o que sobrou de um
       campo antigo: previsto do roteiro, gasto do relógio */
    onClick: () => onSave({
      ...f,
      minutosPrevistos: previsto,
      minutosGastos: gasto
    }),
    disabled: !f.colaboradorId || !(gasto > 0) ||
      !(num(f.pecasBoas) + num(f.pecasDefeito) > 0)
  }, "Salvar")));
}

/* ---------------- Relatório por recurso ----------------
   A folha que vai para a bancada: o que cada pessoa (ou cada
   máquina) faz, dia a dia, com o que precisa saber para executar. */
function RelatorioRecurso({
  tipo,
  itens,
  jornada,
  prog,
  db,
  diaSemana
}) {
  const [aberto, setAberto] = useState({});
  const [soUm, setSoUm] = useState('');
  const lista = itens.slice().sort((a, b) => b.minutos - a.minutos);
  const mostrar = soUm ? lista.filter(x => x.id === soUm) : lista;
  const rotulo = tipo === 'pessoa' ? 'Colaborador' : 'Equipamento';
  const plural = tipo === 'pessoa' ? 'colaboradores' : 'equipamentos';
  const alternar = id => setAberto(a => ({
    ...a,
    [id]: !a[id]
  }));
  const todos = () => {
    const abrirTudo = mostrar.some(x => !aberto[x.id]);
    const novo = {};
    mostrar.forEach(x => {
      novo[x.id] = abrirTudo;
    });
    setAberto(a => ({
      ...a,
      ...novo
    }));
  };
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: `Ver apenas um ${tipo === 'pessoa' ? 'colaborador' : 'equipamento'}`
  }, /*#__PURE__*/React.createElement("select", {
    value: soUm,
    onChange: e => setSoUm(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos (", lista.length, ")"), lista.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      gap: 8,
      paddingBottom: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: todos
  }, mostrar.some(x => !aberto[x.id]) ? 'Abrir o dia a dia' : 'Fechar tudo'), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => window.print()
  }, "🖨 Imprimir relatório"))), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A impressão sai com o dia a dia de quem estiver aberto — abra tudo para o relatório completo, ou escolha um ", tipo === 'pessoa' ? 'colaborador' : 'equipamento', " só.")), /*#__PURE__*/React.createElement("div", {
    className: "so-print ficha-cab"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "ficha-selo"
  }, "Cronograma por ", tipo === 'pessoa' ? 'colaborador' : 'equipamento'), /*#__PURE__*/React.createElement("div", {
    className: "ficha-nome"
  }, "Programação da fábrica"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, fmtDate(prog.primeiroDia), " a ", fmtDate(prog.ultimoDia), " ·", ' ', mostrar.length, " ", plural)), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", null, "Emitido em ", fmtDate(todayISO())), /*#__PURE__*/React.createElement("div", null, "Jornada ", jornada.inicio, " às ", jornada.fim))), mostrar.map(x => {
    const media = x.dias.length > 0 ? x.minutos / x.dias.length : 0;
    const pct = jornada.produtivo > 0 ? Math.min(media / jornada.produtivo * 100, 100) : 0;
    const setores = Array.from(x.setores || []);
    const totalPecas = x.dias.reduce((a, d) => a + d.tarefas.reduce((b, t2) => b + num(t2.pecas), 0), 0);
    return /*#__PURE__*/React.createElement("div", {
      key: x.id,
      className: "recurso-bloco"
    }, /*#__PURE__*/React.createElement("div", {
      className: "recurso-topo"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 180
      }
    }, /*#__PURE__*/React.createElement("strong", {
      style: {
        fontSize: 15
      }
    }, x.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, rotulo, setores.length > 0 && ` · ${setores.join(', ')}`)), /*#__PURE__*/React.createElement("div", {
      className: "painel-meta",
      style: {
        flex: 2,
        minWidth: 260,
        margin: 0
      }
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Horas"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, (x.minutos / 60).toFixed(1))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Dias"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, x.dias.length)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Média/dia"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        fontSize: 15
      }
    }, duracao(media))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Ocupação"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: pct > 95 ? {
        color: 'var(--warn)'
      } : {}
    }, pct.toFixed(0), "%"))), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm no-print",
      onClick: () => alternar(x.id)
    }, aberto[x.id] ? 'Fechar' : 'Ver dia a dia')), /*#__PURE__*/React.createElement("div", {
      className: "barra-ocupacao no-print"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: pct + '%',
        background: pct > 95 ? 'var(--warn)' : 'var(--thread)'
      }
    })), aberto[x.id] && /*#__PURE__*/React.createElement("div", {
      className: "recurso-dias"
    }, x.dias.map(d => /*#__PURE__*/React.createElement("div", {
      key: d.iso,
      className: "dia-linha" + (d.sabado ? ' sabado' : '')
    }, /*#__PURE__*/React.createElement("div", {
      className: "dia-cab"
    }, /*#__PURE__*/React.createElement("strong", null, fmtDate(d.iso)), /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, diaSemana[d.data.getDay()], d.sabado && ' · autorizado'), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1
      }
    }), /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, tipo === 'pessoa' ? `${duracao(d.usado)} de ${duracao(d.capacidade)} · ${d.ocupacao}%` : `${duracao(d.usado)} · ${d.ocupacao}% de uso`)), d.tarefas.length === 0 ? /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Sem etapa registrada neste dia.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", null, "OP · Produto"), tipo === 'pessoa' ? /*#__PURE__*/React.createElement("th", null, "Máquina") : /*#__PURE__*/React.createElement("th", null, "Quem"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Minutos"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Peças"))), /*#__PURE__*/React.createElement("tbody", null, d.tarefas.map((t2, i) => /*#__PURE__*/React.createElement("tr", {
      key: i
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, t2.etapa), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, t2.departamento)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--mono)'
      }
    }, t2.ordem), /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, t2.produto)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, tipo === 'pessoa' ? t2.equipamento || '—' : t2.pessoas.map(q => q.nome).join(', ')), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, duracao(tipo === 'pessoa' ? t2.meusMinutos : t2.minutos)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, t2.pecas, t2.parcial && /*#__PURE__*/React.createElement("div", {
      style: {
        color: 'var(--warn)'
      }
    }, "continua")))))))), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 6
      }
    }, "Total no período: ", (x.minutos / 60).toFixed(1), " h · ", totalPecas, " peça(s).")));
  }), /*#__PURE__*/React.createElement("div", {
    className: "so-print ficha-assina"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "linha"
  }), "Responsável"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "linha"
  }), rotulo)));
}

function GrupoEngenharia({
  db,
  update,
  usuario,
  perm
}) {
  const [sub, setSub] = useState('departamentos');
  const podeEditar = podeExecutar(usuario, 'engenharia') || podeExecutar(usuario, 'cadastros') || podeExecutar(usuario, 'admin');
  const abas = [{
    id: 'departamentos',
    label: `Departamentos (${(db.departamentos || []).length})`
  }, {
    id: 'etapas',
    label: `Etapas (${(db.etapas || []).length})`
  }, {
    id: 'equipamentos',
    label: `Máquinas e equipamentos (${(db.equipamentos || []).length})`
  }, {
    id: 'jornada',
    label: 'Jornada e mão de obra'
  }, {
    id: 'custos',
    label: 'Custo da fábrica'
  }, {
    id: 'listas',
    label: 'Grupos e tipos de produto'
  }, {
    id: 'materiais',
    label: 'Grupos, unidades e locais'
  }];
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Industrial"), /*#__PURE__*/React.createElement("h2", null, "Engenharia"))), /*#__PURE__*/React.createElement(SubTabs, {
    active: sub,
    onChange: setSub,
    tabs: abas
  }), sub === 'departamentos' && /*#__PURE__*/React.createElement(TelaDepartamentos, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'etapas' && /*#__PURE__*/React.createElement(TelaEtapas, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'equipamentos' && /*#__PURE__*/React.createElement(TelaEquipamentos, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'custos' && /*#__PURE__*/React.createElement(CustosFixos, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'jornada' && /*#__PURE__*/React.createElement(TelaMaoDeObra, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'listas' && /*#__PURE__*/React.createElement(ListasProduto, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'materiais' && /*#__PURE__*/React.createElement(ConfigMateriais, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }));
}

/* ---------------- Jornada e mão de obra ---------------- */
function TelaMaoDeObra({
  db,
  update,
  usuario,
  podeEditar
}) {
  /* salário é informação restrita: só quem administra pessoas enxerga */
  const vePessoal = podeExecutar(usuario, 'pessoas') || podeExecutar(usuario, 'admin');
  const j = jornadaDe(db);
  const par = db.parametrosMaoDeObra || {
    encargos: ENCARGOS_PADRAO,
    diasUteis: DIAS_UTEIS_MES
  };
  const setPar = (k, v) => update(d => {
    d.parametrosMaoDeObra = {
      ...(d.parametrosMaoDeObra || {
        encargos: ENCARGOS_PADRAO,
        diasUteis: DIAS_UTEIS_MES
      }),
      [k]: v
    };
    return d;
  });
  const setJ = (k, v) => update(d => {
    d.jornada = {
      ...(d.jornada || JORNADA_PADRAO),
      [k]: v
    };
    return d;
  });
  const setInt = (id, k, v) => update(d => {
    const jd = d.jornada || JORNADA_PADRAO;
    d.jornada = {
      ...jd,
      intervalos: (jd.intervalos || []).map(i => i.id === id ? {
        ...i,
        [k]: v
      } : i)
    };
    return d;
  });
  const linhas = (db.departamentos || []).map(dep => ({
    dep,
    custo: custoMinutoDepartamento(dep.id, db),
    equipe: colaboradoresDoDepartamento(dep.id, db)
  }));
  const semDepto = (db.colaboradores || []).filter(c => c.status !== 'Inativo' && !c.departamentoId);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "O custo do minuto de trabalho: a folha do departamento, com encargos, dividida pelos minutos que a fábrica produz no mês.", !vePessoal && ' Os salários ficam visíveis apenas para quem administra pessoas.'), /*#__PURE__*/React.createElement("h3", null, "Jornada"), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Entrada"
  }, /*#__PURE__*/React.createElement("input", {
    type: "time",
    step: "1",
    value: j.inicio,
    disabled: !podeEditar,
    onChange: e => setJ('inicio', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Saída"
  }, /*#__PURE__*/React.createElement("input", {
    type: "time",
    step: "1",
    value: j.fim,
    disabled: !podeEditar,
    onChange: e => setJ('fim', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Dias úteis no mês"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    max: "31",
    value: num(par.diasUteis) || DIAS_UTEIS_MES,
    disabled: !podeEditar,
    onChange: e => setPar('diasUteis', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginBottom: 6
    }
  }, "Intervalos"), (j.intervalos || []).map(i => /*#__PURE__*/React.createElement("div", {
    key: i.id,
    className: "linha-campos"
  }, /*#__PURE__*/React.createElement("input", {
    value: i.nome,
    disabled: !podeEditar,
    style: {
      maxWidth: 150,
      margin: 0
    },
    onChange: e => setInt(i.id, 'nome', e.target.value)
  }), /*#__PURE__*/React.createElement("input", {
    type: "time",
    step: "1",
    value: i.inicio,
    disabled: !podeEditar,
    style: {
      maxWidth: 130,
      margin: 0
    },
    onChange: e => setInt(i.id, 'inicio', e.target.value)
  }), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "até"), /*#__PURE__*/React.createElement("input", {
    type: "time",
    step: "1",
    value: i.fim,
    disabled: !podeEditar,
    style: {
      maxWidth: 130,
      margin: 0
    },
    onChange: e => setInt(i.id, 'fim', e.target.value)
  }), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, duracao(minutosEntre(i.inicio, i.fim))))), /*#__PURE__*/React.createElement(Field, {
    label: "Encargos sobre o salário (%)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.1",
    value: num(par.encargos) || ENCARGOS_PADRAO,
    disabled: !podeEditar,
    onChange: e => setPar('encargos', e.target.value),
    style: {
      maxWidth: 130
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6
    }
  }, "INSS, FGTS, férias, 13º e demais — o quanto o salário custa acima do que é pago.")), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Jornada"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (j.bruto / 60).toFixed(2), "h"), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, j.inicio, " às ", j.fim)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Intervalos"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, j.pausas, " min")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Minutos produtivos"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, j.produtivo), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "por dia, por pessoa")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "No mês"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, j.produtivo * (num(par.diasUteis) || DIAS_UTEIS_MES)), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, num(par.diasUteis) || DIAS_UTEIS_MES, " dias úteis"))), /*#__PURE__*/React.createElement("h3", null, "Custo por departamento"), (() => {
    const g = mediaGeralFabrica(db);
    if (g.vazia) return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--bad)',
        background: 'var(--bad-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--bad)',
        fontWeight: 600
      }
    }, "⛔ Nenhum colaborador com salário cadastrado. Sem isso, nenhum produto recebe custo de mão de obra."));
    const semEquipe = (db.departamentos || []).filter(d => custoMinutoDepartamento(d.id, db).semSalario);
    return /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, vePessoal && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Média da fábrica"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(g.media))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Com encargos"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        fontSize: 16
      }
    }, money(g.mediaComEncargos)))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Custo/minuto geral"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: 'var(--thread-dark)'
      }
    }, money(g.custoMinuto))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Colaboradores"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, g.pessoas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Setores sem equipe"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: semEquipe.length ? {
        color: 'var(--warn)'
      } : {}
    }, semEquipe.length)));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Setor sem colaborador cadastrado usa a média da fábrica, marcada com asterisco na ficha do produto — o custo aparece, mas como estimativa."), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Departamento"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Equipe"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Com salário"), vePessoal && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Média"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Com encargos")), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo/minuto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo/hora"))), /*#__PURE__*/React.createElement("tbody", null, linhas.map(({
    dep,
    custo,
    equipe
  }) => /*#__PURE__*/React.createElement("tr", {
    key: dep.id,
    className: custo.semSalario ? 'linha-atraso' : ''
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, dep.nome), vePessoal && equipe.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, equipe.slice(0, 3).map(c => c.nome).join(', '), equipe.length > 3 && ` e mais ${equipe.length - 3}`)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, custo.pessoas || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, custo.comSalario || '—', custo.incompleto && /*#__PURE__*/React.createElement("div", {
    style: {
      color: 'var(--warn)'
    }
  }, custo.pessoas - custo.comSalario, " sem")), vePessoal && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, custo.media > 0 ? money(custo.media) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--warn)'
    }
  }, "usa a média geral")), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, custo.mediaComEncargos > 0 ? money(custo.mediaComEncargos) : '—')), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, custo.custoMinuto > 0 ? money(custo.custoMinuto) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--warn)'
    }
  }, "média geral"))), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, custo.custoHora > 0 ? money(custo.custoHora) : '—')))))), semDepto.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", semDepto.length, " colaborador(es) sem departamento", vePessoal && /*#__PURE__*/React.createElement(React.Fragment, null, ": ", semDepto.slice(0, 5).map(c => c.nome).join(', '), semDepto.length > 5 && ` e mais ${semDepto.length - 5}`), ". Eles não entram em nenhuma média."), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 4
    }
  }, "Ajuste em Cadastros → Colaboradores, no campo Departamento.")));
}

/* ---------------- Máquinas e equipamentos ---------------- */
function TelaEquipamentos({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [modal, setModal] = useState(null);
  const [fDep, setFDep] = useState('');
  const [fSit, setFSit] = useState('');
  const [busca, setBusca] = useState('');
  const lista = (db.equipamentos || []).filter(e => !fDep || e.departamentoId === fDep).filter(e => !fSit || (e.situacao || 'operando') === fSit).filter(e => {
    const t = normaliza(busca);
    return !t || [e.nome, e.codigo, e.patrimonio, e.marca, e.modelo, e.serie].some(x => normaliza(x || '').includes(t));
  }).sort((a, b) => String(a.nome).localeCompare(String(b.nome)));
  function salvar(f) {
    if (!String(f.nome || '').trim()) return alert('Informe o nome do equipamento.');
    const pat = String(f.patrimonio || '').trim();
    if (pat && (db.equipamentos || []).some(e => e.id !== f.id && String(e.patrimonio || '').trim() === pat)) return alert(`O patrimônio "${pat}" já está em outro equipamento.`);
    update(d => {
      const reg = {
        ...f,
        nome: f.nome.trim(),
        id: f.id || uid(),
        codigo: f.codigo || proximoCodigo(d.equipamentos, 'EQP'),
        tipo: f.tipo || 'maquina',
        situacao: f.situacao || 'operando',
        ativo: f.ativo !== false,
        criadoEm: f.criadoEm || agoraISO()
      };
      d.equipamentos = f.id ? d.equipamentos.map(x => x.id === reg.id ? reg : x) : [...(d.equipamentos || []), reg];
      registrarLog(d, usuario, f.id ? 'Editou equipamento' : 'Cadastrou equipamento', reg.nome);
      return d;
    });
    setModal(null);
  }
  function remover(eq) {
    if (!confirm(`Remover "${eq.nome}"?`)) return;
    update(d => {
      d.equipamentos = d.equipamentos.filter(x => x.id !== eq.id);
      return d;
    });
  }
  const porSituacao = SITUACOES_EQUIPAMENTO.map(s => ({
    ...s,
    n: (db.equipamentos || []).filter(e => (e.situacao || 'operando') === s.id).length
  }));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, (db.equipamentos || []).length, " equipamento(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({
      tipo: 'maquina',
      situacao: 'operando',
      departamentoId: fDep || ''
    })
  }, "+ Equipamento")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "O parque de máquinas: onde cada uma está, o que faz e em que estado."), (db.equipamentos || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, porSituacao.map(s => /*#__PURE__*/React.createElement("div", {
    key: s.id,
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, s.nome), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: s.n > 0 && s.tone === 'bad' ? {
      color: 'var(--bad)'
    } : s.n > 0 && s.tone === 'warn' ? {
      color: 'var(--warn)'
    } : {}
  }, s.n)))), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Departamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: fDep,
    onChange: e => setFDep(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.departamentos || []).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: fSit,
    onChange: e => setFSit(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas"), SITUACOES_EQUIPAMENTO.map(s => /*#__PURE__*/React.createElement("option", {
    key: s.id,
    value: s.id
  }, s.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Buscar"
  }, /*#__PURE__*/React.createElement("input", {
    value: busca,
    onChange: e => setBusca(e.target.value),
    placeholder: "nome, patrimônio, marca, série"
  })))), (db.equipamentos || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum equipamento. Comece pelas máquinas do seu setor mais movimentado."
  }) : lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum equipamento com esses filtros."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Equipamento"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Departamento"), /*#__PURE__*/React.createElement("th", null, "Patrimônio"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lista.map(eq => {
    const dep = (db.departamentos || []).find(d => d.id === eq.departamentoId);
    const sit = situacaoEquip(eq.situacao);
    const tipo = TIPOS_EQUIPAMENTO.find(x => x.id === eq.tipo);
    return /*#__PURE__*/React.createElement("tr", {
      key: eq.id,
      className: eq.ativo === false ? 'linha-inativa' : ''
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, eq.nome), (eq.marca || eq.modelo) && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, [eq.marca, eq.modelo].filter(Boolean).join(' ')), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, eq.codigo)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, tipo ? tipo.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, dep ? dep.nome : /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--warn)'
      }
    }, "sem setor")), /*#__PURE__*/React.createElement("td", {
      className: "small",
      style: {
        fontFamily: 'var(--mono)'
      }
    }, eq.patrimonio || '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
      tone: sit.tone
    }, sit.nome)), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("div", {
      className: "acoes-icone"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      onClick: () => setModal(eq)
    }, "✎"), /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => remover(eq)
    }, "×"))));
  })))), modal && /*#__PURE__*/React.createElement(EquipamentoModal, {
    eq: modal,
    db: db,
    onClose: () => setModal(null),
    onSave: salvar,
    /* marcar aqui grava na lista do colaborador: o dado é dele, esta
       tela é só outra porta de entrada */
    onOperador: (colaboradorId, passaAOperar) => update(d => {
      const c = (d.colaboradores || []).find(x => x.id === colaboradorId);
      if (!c) return d;
      const atual = Array.isArray(c.equipamentos) ? c.equipamentos : [];
      const doSetor = (d.equipamentos || [])
        .filter(e => e.departamentoId === c.departamentoId && e.ativo !== false)
        .map(e => e.id);
      if (passaAOperar) {
        /* lista vazia já significava "opera tudo": nada a fazer */
        if (atual.length === 0) return d;
        c.equipamentos = [...new Set([...atual, modal.id])];
      } else {
        /* desmarcar quem não tinha lista exige materializá-la primeiro,
           senão "não opera esta" viraria "não opera nenhuma" */
        const base = atual.length === 0 ? doSetor : atual;
        c.equipamentos = base.filter(x => x !== modal.id);
      }
      registrarLog(d, usuario, 'engenharia.operador',
        `${c.nome} · ${modal.nome} · ${passaAOperar ? 'passa a operar' : 'deixa de operar'}`);
      return d;
    })
  }));
}
function EquipamentoModal({
  eq,
  db,
  onClose,
  onSave,
  onOperador
}) {
  const [f, setF] = useState({
    nome: '',
    tipo: 'maquina',
    departamentoId: '',
    marca: '',
    modelo: '',
    serie: '',
    patrimonio: '',
    situacao: 'operando',
    aquisicao: '',
    fornecedorId: '',
    localizacao: '',
    observacao: '',
    ativo: true,
    ...eq
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const dep = (db.departamentos || []).find(d => d.id === f.departamentoId);
  return /*#__PURE__*/React.createElement(Modal, {
    title: eq.id ? `Equipamento ${f.nome}` : 'Novo equipamento',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value),
    placeholder: "Reta industrial, Overloque…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tipo,
    onChange: e => set('tipo', e.target.value)
  }, TIPOS_EQUIPAMENTO.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Departamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId,
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem setor —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome))))), /*#__PURE__*/React.createElement("h3", null, "Identificação"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Marca"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.marca,
    onChange: e => set('marca', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Modelo"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.modelo,
    onChange: e => set('modelo', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Número de série"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.serie,
    onChange: e => set('serie', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Patrimônio"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.patrimonio,
    onChange: e => set('patrimonio', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Data de aquisição"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.aquisicao,
    onChange: e => set('aquisicao', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Fornecedor"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.fornecedorId,
    onChange: e => set('fornecedorId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "—"), (db.fornecedores || []).filter(x => x.ativo !== false).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome))))), /*#__PURE__*/React.createElement("h3", null, "Situação"), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Estado atual"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.situacao,
    onChange: e => set('situacao', e.target.value)
  }, SITUACOES_EQUIPAMENTO.map(s => /*#__PURE__*/React.createElement("option", {
    key: s.id,
    value: s.id
  }, s.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Localização"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.localizacao,
    onChange: e => set('localizacao', e.target.value),
    placeholder: dep ? `dentro de ${dep.nome}` : 'onde a máquina está'
  }))), (() => {
    /* Quem opera esta máquina. O mesmo dado que existe no cadastro do
       colaborador, visto pelo outro lado — marcar aqui é mais natural
       quando a máquina é nova e várias pessoas já sabem usá-la. */
    const doSetor = (db.colaboradores || []).filter(c =>
      c.departamentoId === f.departamentoId &&
      c.status !== 'Inativo' && c.produtivo !== false);
    if (!f.departamentoId)
      return /*#__PURE__*/React.createElement(Field, { label: "Quem opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Escolha o departamento acima para ver os colaboradores."));
    if (doSetor.length === 0)
      return /*#__PURE__*/React.createElement(Field, { label: "Quem opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Nenhum colaborador cadastrado neste departamento."));
    /* a lista vive no colaborador; aqui só se lê e se marca */
    const opera = id => {
      const c = doSetor.find(x => x.id === id);
      const l = Array.isArray(c && c.equipamentos) ? c.equipamentos : null;
      /* sem lista significa "ainda não informado": opera tudo */
      return l === null || l.length === 0 || l.includes(f.id);
    };
    const explicito = id => {
      const c = doSetor.find(x => x.id === id);
      return Array.isArray(c && c.equipamentos) && c.equipamentos.length > 0;
    };
    return /*#__PURE__*/React.createElement("div", null,
      /*#__PURE__*/React.createElement("div", { className: "etp-rot",
        style: { marginBottom: 6 } },
        `Quem opera (${doSetor.filter(c => opera(c.id)).length} de ${doSetor.length})`),
      /*#__PURE__*/React.createElement("div", { className: "gente-grade" },
        doSetor.map(c => /*#__PURE__*/React.createElement("label", {
          key: c.id,
          className: "gente-check" + (opera(c.id) ? " on" : "")
        },
          /*#__PURE__*/React.createElement("input", {
            type: "checkbox", checked: opera(c.id),
            onChange: () => onOperador && onOperador(c.id, !opera(c.id))
          }),
          /*#__PURE__*/React.createElement("span", null,
            /*#__PURE__*/React.createElement("strong", null, c.nome),
            /*#__PURE__*/React.createElement("span", { className: "small muted" },
              explicito(c.id) ? `${c.equipamentos.length} máquina(s)`
                : 'não informado'))))),
      /*#__PURE__*/React.createElement("div", { className: "small muted",
        style: { marginTop: 6, marginBottom: 10 } },
        "Quem aparece como “não informado” ainda não teve as máquinas ",
        "marcadas no cadastro dele — o planejamento o considera apto a ",
        "qualquer uma do setor. Marcar aqui grava no cadastro do ",
        "colaborador; é o mesmo dado, visto pelo outro lado."));
  })(), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacao,
    onChange: e => set('observacao', e.target.value)
  })), /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.ativo !== false,
    onChange: e => set('ativo', e.target.checked)
  }), "Equipamento ativo no cadastro"), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome
  }, "Salvar")));
}

/* ---------------- Etapas ---------------- */
function TelaEtapas({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [modal, setModal] = useState(null);
  const [fDep, setFDep] = useState('');
  const [busca, setBusca] = useState('');
  const lista = (db.etapas || []).filter(e => !fDep || e.departamentoId === fDep).filter(e => {
    const t = normaliza(busca);
    return !t || normaliza(e.nome).includes(t) || normaliza(e.codigo || '').includes(t);
  });

  /* agrupa por departamento — é assim que a fábrica enxerga */
  const porDep = (db.departamentos || []).map(dep => ({
    dep,
    etapas: lista.filter(e => e.departamentoId === dep.id).sort((a, b) => num(a.sequencia) - num(b.sequencia))
  })).filter(g => g.etapas.length > 0 || !fDep && !busca);
  const orfas = lista.filter(e => !(db.departamentos || []).some(d => d.id === e.departamentoId));
  function salvar(f) {
    if (!String(f.nome || '').trim()) return alert('Informe o nome da etapa.');
    if (!f.departamentoId) return alert('Escolha o departamento — toda etapa pertence a um setor.');
    update(d => {
      const irmas = etapasDoDepartamento(f.departamentoId, d).filter(x => x.id !== f.id);
      const reg = {
        ...f,
        nome: f.nome.trim(),
        id: f.id || uid(),
        codigo: f.codigo || proximoCodigo(d.etapas, 'ETP'),
        sequencia: num(f.sequencia) || (irmas.length + 1) * 10,
        ativo: f.ativo !== false,
        criadoEm: f.criadoEm || agoraISO()
      };
      d.etapas = f.id ? d.etapas.map(x => x.id === reg.id ? reg : x) : [...(d.etapas || []), reg];
      registrarLog(d, usuario, f.id ? 'Editou etapa' : 'Cadastrou etapa', reg.nome);
      return d;
    });
    setModal(null);
  }
  function remover(et) {
    if (!confirm(`Remover a etapa "${et.nome}"?`)) return;
    update(d => {
      d.etapas = d.etapas.filter(x => x.id !== et.id);
      return d;
    });
  }
  function mover(et, dir) {
    const irmas = etapasDoDepartamento(et.departamentoId, db);
    const i = irmas.findIndex(x => x.id === et.id);
    const j = i + dir;
    if (i < 0 || j < 0 || j >= irmas.length) return;
    const l = irmas.slice();
    [l[i], l[j]] = [l[j], l[i]];
    update(d => {
      d.etapas = d.etapas.map(x => {
        const k = l.findIndex(y => y.id === x.id);
        return k >= 0 ? {
          ...x,
          sequencia: (k + 1) * 10
        } : x;
      });
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, (db.etapas || []).length, " etapa(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({
      departamentoId: fDep || ''
    })
  }, "+ Etapa")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "As operações que a fábrica executa, na ordem em que costumam acontecer dentro de cada setor. Tempo e custo entram depois."), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Departamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: fDep,
    onChange: e => setFDep(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.departamentos || []).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Buscar"
  }, /*#__PURE__*/React.createElement("input", {
    value: busca,
    onChange: e => setBusca(e.target.value),
    placeholder: "nome ou código"
  })))), (db.etapas || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma etapa cadastrada."
  }) : lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma etapa com esse filtro."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, porDep.map(({
    dep,
    etapas
  }) => /*#__PURE__*/React.createElement("div", {
    key: dep.id,
    className: "dep-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "dep-topo"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 5,
      height: 17,
      background: 'var(--thread)',
      borderRadius: 3
    }
  }), /*#__PURE__*/React.createElement("strong", {
    style: {
      flex: 1
    }
  }, dep.nome), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, etapas.length, " etapa(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setModal({
      departamentoId: dep.id
    })
  }, "+ Etapa")), etapas.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma etapa neste setor.") : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0,
      marginBottom: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    style: {
      width: 32
    }
  }, "#"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", null, "Modo"), /*#__PURE__*/React.createElement("th", null, "Código"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 100
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, etapas.map((et, i) => /*#__PURE__*/React.createElement("tr", {
    key: et.id,
    className: et.ativo === false ? 'linha-inativa' : ''
  }, /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, i + 1), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, et.nome), et.descricao && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, et.descricao)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, et.modo === 'projeto' ? /*#__PURE__*/React.createElement(Badge, {
    tone: "warn"
  }, "por projeto") : et.modo === 'equipe' ? /*#__PURE__*/React.createElement(Badge, {
    tone: "info"
  }, "por equipe") : /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "por pessoa")), /*#__PURE__*/React.createElement("td", {
    className: "small muted",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, et.codigo), /*#__PURE__*/React.createElement("td", null, et.ativo === false ? /*#__PURE__*/React.createElement(Badge, {
    tone: "idle"
  }, "inativa") : /*#__PURE__*/React.createElement(Badge, {
    tone: "ok"
  }, "ativa")), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "acoes-icone"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn-ic",
    disabled: i === 0,
    onClick: () => mover(et, -1)
  }, "↑"), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic",
    disabled: i === etapas.length - 1,
    onClick: () => mover(et, 1)
  }, "↓"), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic",
    onClick: () => setModal(et)
  }, "✎"), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => remover(et)
  }, "×")))))))))), orfas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", orfas.length, " etapa(s) sem departamento: ", orfas.map(e => e.nome).join(', '), "."))), modal && /*#__PURE__*/React.createElement(EtapaModal, {
    et: modal,
    db: db,
    onClose: () => setModal(null),
    onSave: salvar
  }));
}
function EtapaModal({
  et,
  db,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    nome: '',
    departamentoId: '',
    sequencia: '',
    descricao: '',
    modo: 'pessoa',
    ativo: true,
    ...et
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const dep = (db.departamentos || []).find(d => d.id === f.departamentoId);
  const equips = f.departamentoId ? equipamentosDoDepartamento(f.departamentoId, db) : [];
  return /*#__PURE__*/React.createElement(Modal, {
    title: et.id ? `Etapa ${f.nome}` : 'Nova etapa',
    onClose: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value),
    placeholder: "Fechar lateral, Pregar gola…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Departamento *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId,
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Ordem dentro do setor"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.sequencia,
    onChange: e => set('sequencia', e.target.value),
    placeholder: "automática"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Como o tempo é gasto"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.modo || 'pessoa',
    onChange: e => set('modo', e.target.value)
  }, MODOS_ETAPA.map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, modoEtapa(f.modo).desc, ".", f.modo === 'projeto' && ' O custo se divide entre as peças do lote — quanto maior o lote, menor por peça.'), /*#__PURE__*/React.createElement(Field, {
    label: "Descrição"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.descricao,
    onChange: e => set('descricao', e.target.value),
    placeholder: "como a operação é feita"
  })), dep && equips.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Equipamentos disponíveis em ", dep.nome, ": ", equips.map(x => x.nome).join(', '), "."), /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.ativo !== false,
    onChange: e => set('ativo', e.target.checked)
  }), "Etapa ativa"), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome || !f.departamentoId
  }, "Salvar")));
}

/* ---------------- Departamentos ---------------- */
function TelaDepartamentos({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [modal, setModal] = useState(null);
  function salvar(f) {
    if (!String(f.nome || '').trim()) return alert('Informe o nome do departamento.');
    const sigla = String(f.sigla || '').trim().toUpperCase();
    const repetida = (db.departamentos || []).some(d => d.id !== f.id && d.sigla === sigla && sigla);
    if (repetida) return alert(`A sigla "${sigla}" já é usada por outro departamento.`);
    update(d => {
      const reg = {
        ...f,
        nome: f.nome.trim(),
        sigla,
        id: f.id || uid(),
        codigo: f.codigo || proximoCodigo(d.departamentos, 'DEP', 3),
        ativo: f.ativo !== false,
        criadoEm: f.criadoEm || agoraISO()
      };
      d.departamentos = f.id ? d.departamentos.map(x => x.id === reg.id ? reg : x) : [...(d.departamentos || []), reg];
      registrarLog(d, usuario, f.id ? 'Editou departamento' : 'Cadastrou departamento', reg.nome);
      return d;
    });
    setModal(null);
  }
  function remover(dep) {
    const et = etapasDoDepartamento(dep.id, db).length;
    const eq = equipamentosDoDepartamento(dep.id, db).length;
    if (et || eq) return alert(`"${dep.nome}" tem ${et} etapa(s) e ${eq} equipamento(s). ` + `Realoque ou remova antes.`);
    if (!confirm(`Remover "${dep.nome}"?`)) return;
    update(d => {
      d.departamentos = d.departamentos.filter(x => x.id !== dep.id);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, (db.departamentos || []).length, " departamento(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({})
  }, "+ Departamento")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Os setores por onde a produção passa. Cada etapa e cada máquina pertence a um deles."), (db.departamentos || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum departamento."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Departamento"), /*#__PURE__*/React.createElement("th", null, "Sigla"), /*#__PURE__*/React.createElement("th", null, "Responsável"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Etapas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Equipamentos"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.departamentos || []).map(dep => /*#__PURE__*/React.createElement("tr", {
    key: dep.id,
    className: dep.ativo === false ? 'linha-inativa' : ''
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, dep.nome), dep.descricao && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, dep.descricao), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, dep.codigo)), /*#__PURE__*/React.createElement("td", {
    className: "small",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, dep.sigla || '—'), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, dep.responsavel || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, etapasDoDepartamento(dep.id, db).length), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, equipamentosDoDepartamento(dep.id, db).length), /*#__PURE__*/React.createElement("td", null, dep.ativo === false ? /*#__PURE__*/React.createElement(Badge, {
    tone: "idle"
  }, "inativo") : /*#__PURE__*/React.createElement(Badge, {
    tone: "ok"
  }, "ativo")), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "acoes-icone"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn-ic",
    onClick: () => setModal(dep)
  }, "✎"), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => remover(dep)
  }, "×")))))))), modal && /*#__PURE__*/React.createElement(DepartamentoModal, {
    dep: modal,
    db: db,
    onClose: () => setModal(null),
    onSave: salvar
  }));
}
function DepartamentoModal({
  dep,
  db,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    nome: '',
    sigla: '',
    responsavel: '',
    descricao: '',
    ativo: true,
    ...dep
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const colaboradores = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
  return /*#__PURE__*/React.createElement(Modal, {
    title: dep.id ? `Departamento ${f.nome}` : 'Novo departamento',
    onClose: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value),
    placeholder: "Costura, Corte…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Sigla"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.sigla,
    maxLength: "4",
    onChange: e => set('sigla', e.target.value),
    placeholder: "CS"
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Responsável"
  }, colaboradores.length > 0 ? /*#__PURE__*/React.createElement("select", {
    value: f.responsavel,
    onChange: e => set('responsavel', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem responsável —"), colaboradores.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.nome
  }, c.nome))) : /*#__PURE__*/React.createElement("input", {
    value: f.responsavel,
    onChange: e => set('responsavel', e.target.value),
    placeholder: "nome de quem responde pelo setor"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Descrição"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.descricao,
    onChange: e => set('descricao', e.target.value),
    placeholder: "o que este setor faz"
  })), /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.ativo !== false,
    onChange: e => set('ativo', e.target.checked)
  }), "Departamento ativo"), /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.exigeAfericao === true,
    onChange: e => set('exigeAfericao', e.target.checked)
  }), "Este setor trabalha com aferição de meta"),
  /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: { marginTop: -4, marginBottom: 10 }
  }, f.exigeAfericao
    ? "Cada pessoa é cronometrada nas operações deste setor, e a meta " +
      "passa por prova e validação. É o caso da costura, onde a mesma " +
      "operação se repete peça a peça e o ritmo varia muito entre pessoas."
    : "A meta deste setor sai da jornada: peças produzidas sobre tempo " +
      "trabalhado, pelos apontamentos. Sem cronômetro e sem prova — é o " +
      "que faz sentido no corte, na embalagem ou na expedição."),
  /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome
  }, "Salvar")));
}

/* ==========================================================
   MÓDULO PRODUTOS
========================================================== */
function GrupoProdutos({
  db,
  update,
  usuario,
  perm
}) {
  const podeEditar = podeExecutar(usuario, 'cadastros') || podeExecutar(usuario, 'admin');
  /* Sem sub-abas: subproduto e modelagem pertencem a um produto e só
     fazem sentido dentro dele. Listá-los aqui sugeria que existiam por
     conta própria — e a pessoa acabava procurando ali o que só se
     resolve abrindo o produto. */
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Engenharia de produto"), /*#__PURE__*/React.createElement("h2", null, "Produtos"))), /*#__PURE__*/React.createElement(ListaProdutos, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }));
}

/* ---------------- Detalhe do produto ---------------- */
function ProdutoDetalhe({
  produtoId,
  db,
  update,
  usuario,
  podeEditar,
  onVoltar
}) {
  /* a engenharia abre primeiro: é dela que sai tudo o mais */
  const [aba, setAba] = useState('identidade');
  /* dentro de "como é feito", a ordem do fluxo */
  const [passo, setPasso] = useState('materiais');
  const [importando, setImportando] = useState(null); // {tecidoId, analise}
  const [verRisco, setVerRisco] = useState(null);
  const produto = (db.produtos || []).find(p => p.id === produtoId);
  if (!produto) return /*#__PURE__*/React.createElement(Empty, {
    text: "Produto não encontrado."
  });
  const c = custearProduto(produto, db);
  const nome = montarNomeProduto(produto, db);
  const setP = (k, v) => update(d => {
    d.produtos = d.produtos.map(p => {
      if (p.id !== produtoId) return p;
      const novo = {
        ...p,
        [k]: v
      };
      novo.nome = montarNomeProduto(novo, d);
      return novo;
    });
    return d;
  });
  function addTecido(materialId) {
    if (!materialId) return;
    /* o mesmo tecido pode entrar mais de uma vez: corpo e forro saem do
       mesmo oxford, mas de modelagens diferentes. O apelido separa os dois
       na tela; o consumo se junta de novo na ficha. */
    const repetidos = (produto.tecidos || []).filter(t => t.materialId === materialId).length;
    const mat = (db.materiais || []).find(m => m.id === materialId);
    setP('tecidos', [...(produto.tecidos || []), {
      id: uid(),
      materialId,
      riscoId: '',
      apelido: repetidos > 0 ? `${mat && mat.nome || 'material'} (${repetidos + 1}º uso)` : ''
    }]);
  }
  function rmTecido(tecidoId) {
    const subs = (db.subprodutos || []).filter(sp => sp.tecidoId === tecidoId).length;
    if (subs && !confirm(`Este tecido gerou ${subs} subproduto(s). Remover tudo?`)) return;
    update(d => {
      d.subprodutos = (d.subprodutos || []).filter(sp => sp.tecidoId !== tecidoId);
      d.produtos = d.produtos.map(p => {
        if (p.id !== produtoId) return p;
        const novo = {
          ...p,
          tecidos: (p.tecidos || []).filter(t => t.id !== tecidoId)
        };
        novo.nome = montarNomeProduto(novo, d);
        return novo;
      });
      return d;
    });
  }
  const setTecido = (tecidoId, patch) => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
      ...p,
      tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
        ...t,
        ...patch
      } : t)
    });
    return d;
  });
  const setQuantidade = (tecidoId, v) => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
      ...p,
      tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
        ...t,
        quantidade: v
      } : t)
    });
    return d;
  });
  const setConversao = (tecidoId, v) => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
      ...p,
      tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
        ...t,
        conversao: v
      } : t)
    });
    return d;
  });
  const setPerdaAviamento = (tecidoId, v) => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
      ...p,
      tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
        ...t,
        perda: v
      } : t)
    });
    return d;
  });

  /* aqui o risco vira produto: as partes nomeadas viram subprodutos */
  function vincularRisco(tecidoId, riscoId) {
    update(d => {
      d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
        ...p,
        tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
          ...t,
          riscoId
        } : t)
      });
      if (riscoId) {
        const risco = (d.riscos || []).find(r => r.id === riscoId);
        if (risco) gerarSubprodutos(d, produtoId, tecidoId, risco, usuario);
      } else {
        d.subprodutos = (d.subprodutos || []).filter(sp => sp.tecidoId !== tecidoId);
      }
      return d;
    });
  }

  /* o risco importado aqui já nasce vinculado ao tecido que o pediu */
  function salvarRiscoDoTecido(tecidoId, dados) {
    update(d => {
      const risco = gravarRisco(d, dados, usuario, produtoId);
      d.produtos = d.produtos.map(p => p.id !== produtoId ? p : {
        ...p,
        tecidos: (p.tecidos || []).map(t => t.id === tecidoId ? {
          ...t,
          riscoId: risco.id
        } : t)
      });
      gerarSubprodutos(d, produtoId, tecidoId, risco, usuario);
      return d;
    });
    setImportando(null);
  }
  const materiais = (db.materiais || []).filter(m => m.ativo !== false);
  /* cada risco é exclusivo do produto que o importou */
  const riscosDisponiveis = (db.riscos || []).filter(r => r.produtoId === produtoId);
  const riscosDeOutros = (db.riscos || []).filter(r => r.produtoId && r.produtoId !== produtoId).length;

  /* leitura do risco sem sair do produto */
  if (importando) return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "trilha"
  }, /*#__PURE__*/React.createElement("button", {
    className: "link-btn",
    onClick: () => setImportando(null)
  }, "← ", nome || produto.codigo), /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " / importando modelagem")), /*#__PURE__*/React.createElement(AnaliseRisco, {
    analise: importando.analise,
    db: db,
    update: update,
    usuario: usuario,
    onCancelar: () => setImportando(null),
    onSalvar: dados => salvarRiscoDoTecido(importando.tecidoId, dados)
  }));

  /* o risco já vinculado, aberto de dentro do produto */
  if (verRisco) return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "trilha"
  }, /*#__PURE__*/React.createElement("button", {
    className: "link-btn",
    onClick: () => setVerRisco(null)
  }, "← ", nome || produto.codigo), /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " / modelagem")), /*#__PURE__*/React.createElement(RiscoSalvo, {
    riscoId: verRisco,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    onVoltar: () => setVerRisco(null)
  }));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, codigoCompletoProduto(produto, db)), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0
    }
  }, nome || '(sem tecido ainda)')), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar")), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Tecidos"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, c.cortaveis.length), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, c.aviamentos.length, " aviamento(s)")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Subprodutos"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, c.subprodutos.length)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Consumo"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, c.consumoTotal > 0 ? c.consumoTotal.toFixed(4) : '—'), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "m por produto")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Custo do material"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, money(c.custoTecido)))), c.avisos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, c.avisos.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", x))), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    /* Três abas, na ordem em que a pessoa trabalha: o que é, como se
       faz, e o documento que sai disso. Antes eram sete, sem ordem
       aparente — cada pedido virava uma aba nova. */
    tabs: [{
      id: 'identidade',
      label: 'O que é'
    }, {
      id: 'comofeito',
      label: `Como é feito (${c.tecidos.length + (produto.processo || []).length})`
    }, {
      id: 'ficha',
      label: 'Ficha técnica'
    }]
  }),

  /* A engenharia sai da aba e vira faixa: em que pé está o produto
     precisa estar sempre à vista, não escondido atrás de um clique. */
  /*#__PURE__*/React.createElement(FaixaEngenharia, {
    produto: produto,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    aberta: aba === 'identidade'
  }), aba === 'identidade' && /*#__PURE__*/React.createElement(EngenhariaDoProduto, {
    produto: produto,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), aba === 'comofeito' && /*#__PURE__*/React.createElement(SubTabs, {
    active: passo,
    onChange: setPasso,
    tabs: [{
      id: 'materiais',
      label: `1. Materiais (${c.tecidos.length})`
    }, {
      id: 'partes',
      label: `2. Subprodutos (${c.subprodutos.length})`
    }, {
      id: 'processo',
      label: `3. Processo (${(produto.processo || []).length})`
    }]
  }), aba === 'comofeito' && passo === 'processo' && /*#__PURE__*/React.createElement(ProcessoProdutivo, {
    produto: produto,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), aba === 'comofeito' && passo === 'materiais' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Tecido pede modelagem — é dela que saem as partes e o consumo. Aviamento, tinta e embalagem entram com a quantidade informada."), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Incluir material"
  }, /*#__PURE__*/React.createElement("select", {
    value: "",
    onChange: e => addTecido(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha um material cadastrado —"), /*#__PURE__*/React.createElement("optgroup", {
    label: "Tecidos — cortados na modelagem"
  }, materiais.filter(m => ehTecido(m, db)).sort((x, y) => String(x.nome).localeCompare(String(y.nome))).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.codigo, " · ", m.nome, m.largura ? ` · ${m.largura} m` : '', m.composicao ? ` · ${m.composicao}` : ''))), /*#__PURE__*/React.createElement("optgroup", {
    label: "Aviamentos e demais — consumo informado"
  }, materiais.filter(m => !ehTecido(m, db)).sort((x, y) => String(x.nome).localeCompare(String(y.nome))).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.codigo, " · ", m.nome, " (", m.unidadeEstoque, ")")))))), c.tecidos.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum material. O tecido traz a modelagem; os aviamentos, a quantidade."
  }) : c.tecidos.map(t => {
    const subs = c.subprodutos.filter(sp => sp.tecidoId === t.id);
    return /*#__PURE__*/React.createElement("div", {
      key: t.id,
      className: "tecido-bloco"
    }, /*#__PURE__*/React.createElement("div", {
      className: "tecido-topo"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 5,
        height: 18,
        background: 'var(--thread)',
        borderRadius: 3
      }
    }), /*#__PURE__*/React.createElement("strong", {
      style: {
        flex: 1
      }
    }, t.material ? t.material.nome : '(material removido)', !t.cortavel && /*#__PURE__*/React.createElement("span", {
      className: "tag",
      style: {
        marginLeft: 7
      }
    }, "aviamento"), (() => {
      /* mesmo material duas vezes: dizer qual uso é qual */
      const irmaos = (produto.tecidos || []).filter(x => x.materialId === t.materialId);
      if (irmaos.length < 2) return null;
      const n = irmaos.findIndex(x => x.id === t.id) + 1;
      return /*#__PURE__*/React.createElement("span", {
        className: "tag",
        style: {
          marginLeft: 7
        }
      }, n, "º de ", irmaos.length, " usos");
    })()), podeEditar && t.material && /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      title: "Trocar entre tecido e aviamento",
      onClick: () => update(d => {
        d.materiais = d.materiais.map(m => m.id === t.materialId ? {
          ...m,
          cortavel: !ehTecido(m, d)
        } : m);
        return d;
      })
    }, t.cortavel ? 'É aviamento' : 'É tecido'), t.material && /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, money(custoDoMaterial(t.material)), "/", t.material.unidadeEstoque, t.material.largura ? ` · ${t.material.largura} m` : ''), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => rmTecido(t.id)
    }, "×")), (() => {
      const irmaos = (produto.tecidos || []).filter(x => x.materialId === t.materialId);
      if (irmaos.length < 2) return null;
      return /*#__PURE__*/React.createElement(Field, {
        label: "Para que serve este uso"
      }, /*#__PURE__*/React.createElement("input", {
        value: t.apelido || '',
        disabled: !podeEditar,
        placeholder: "corpo, forro, vista, punho…",
        onChange: e => setTecido(t.id, {
          apelido: e.target.value
        })
      }));
    })(), t.cortavel ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
      className: "risco-campo"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 220
      }
    }, /*#__PURE__*/React.createElement(Field, {
      label: "Modelagem *"
    }, /*#__PURE__*/React.createElement("select", {
      value: t.riscoId || '',
      disabled: !podeEditar,
      onChange: e => vincularRisco(t.id, e.target.value)
    }, /*#__PURE__*/React.createElement("option", {
      value: ""
    }, riscosDisponiveis.length ? '— escolha uma modelagem importada —' : '— nenhuma modelagem importada —'), riscosDisponiveis.map(r => /*#__PURE__*/React.createElement("option", {
      key: r.id,
      value: r.id
    }, r.codigo, " · ", r.arquivo, " · ", r.comprimentoM, " m ·", ' ', (r.partes || []).length, " parte(s)"))))), podeEditar && /*#__PURE__*/React.createElement("div", {
      className: "risco-campo-acoes"
    }, /*#__PURE__*/React.createElement(BotaoImportarPLT, {
      classe: "btn accent sm",
      rotulo: "+ Importar modelagem PLT",
      onAnalisado: a2 => setImportando({
        tecidoId: t.id,
        analise: a2
      })
    }), t.risco && /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setVerRisco(t.risco.id)
    }, "Abrir modelagem"))), riscosDisponiveis.length === 0 && /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 8
      }
    }, "Nenhuma modelagem neste produto — suba o PLT aqui mesmo.", riscosDeOutros > 0 && ` As ${riscosDeOutros} modelagem(ns) de outros produtos ` + `não aparecem: cada peça é única do seu produto.`)) :
    /*#__PURE__*/
    /* aviamento não se corta em risco: a quantidade é informada */
    React.createElement(React.Fragment, null, podeEditar && /*#__PURE__*/React.createElement("label", {
      className: "small linha-check",
      style: {
        marginBottom: 10
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      checked: !!(t.conversao && t.conversao.ativa !== false),
      onChange: e => setConversao(t.id, e.target.checked ? {
        ativa: true,
        nome: '',
        pecasPorUnidade: '',
        perda: '',
        porProduto: 1
      } : null)
    }), "Este aviamento vira peça antes de entrar no produto"), t.conversao ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
      className: "grid2"
    }, /*#__PURE__*/React.createElement(Field, {
      label: "Nome da peça produzida *"
    }, /*#__PURE__*/React.createElement("input", {
      value: t.conversao.nome || '',
      disabled: !podeEditar,
      placeholder: "Alça, Viés da barra, Tira…",
      onChange: e => setConversao(t.id, {
        ...t.conversao,
        nome: e.target.value
      })
    })), /*#__PURE__*/React.createElement(Field, {
      label: `Peças por ${t.material ? t.material.unidadeEstoque : 'unidade'} *`
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      step: "0.01",
      value: t.conversao.pecasPorUnidade || '',
      disabled: !podeEditar,
      onChange: e => setConversao(t.id, {
        ...t.conversao,
        pecasPorUnidade: e.target.value
      })
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Perda no corte (%)"
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      step: "0.1",
      value: t.conversao.perda || '',
      disabled: !podeEditar,
      onChange: e => setConversao(t.id, {
        ...t.conversao,
        perda: e.target.value
      })
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Quantas por produto"
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      min: "1",
      value: t.conversao.porProduto || 1,
      disabled: !podeEditar,
      onChange: e => setConversao(t.id, {
        ...t.conversao,
        porProduto: e.target.value
      })
    }))), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 8
      }
    }, "Cada ", t.material ? t.material.unidadeEstoque : 'unidade', " rende as peças informadas; o sistema calcula o consumo sozinho."), t.conversaoErro && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginBottom: 8
      }
    }, "⚠ ", t.conversaoErro), t.conversao && !t.conversaoErro && t.consumo > 0 && /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Rende por unidade"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, num(t.conversao.aproveitadas).toFixed(2)), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, t.conversao.pecasPorUnidade, " − ", t.conversao.perda || 0, "% de perda")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Consumo"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: 'var(--thread-dark)'
      }
    }, t.consumo.toFixed(5)), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, t.material ? t.material.unidadeEstoque : '', " por produto")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Custo"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(t.custo))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Vira subproduto"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        fontSize: 14
      }
    }, t.conversao.nome || '—')))) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
      className: "grid2"
    }, /*#__PURE__*/React.createElement(Field, {
      label: `Consumo por produto${t.material ? ` (${t.material.unidadeEstoque})` : ''} *`
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      step: "0.0001",
      value: t.quantidade || '',
      disabled: !podeEditar,
      onChange: e => setQuantidade(t.id, e.target.value)
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Perda prevista (%)"
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      step: "0.1",
      value: t.perda || '',
      disabled: !podeEditar,
      onChange: e => setPerdaAviamento(t.id, e.target.value)
    }))), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 8
      }
    }, "Aviamento não passa pela modelagem — informe quanto cada produto consome."), t.consumo > 0 && /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Consumo"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: 'var(--thread-dark)'
      }
    }, t.consumo.toFixed(4), " ", t.material ? t.material.unidadeEstoque : '')), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Custo"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(t.custo)))))), t.risco && t.calculo && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Rende por modelagem"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, t.calculo.porRisco)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Consumo"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: 'var(--thread-dark)'
      }
    }, t.consumo.toFixed(4), " m")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Custo"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(t.custo))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Aproveitamento"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: num(t.risco.aproveitamento) >= 75 ? 'var(--ok)' : 'var(--warn)'
      }
    }, t.risco.aproveitamento, "%"))), subs.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
      className: "etp-rot",
      style: {
        marginBottom: 6
      }
    }, "Subprodutos gerados por esta modelagem"), /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        padding: 0,
        marginBottom: 0
      }
    }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Subproduto"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Por produto"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Rateio"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Consumo"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Custo"))), /*#__PURE__*/React.createElement("tbody", null, subs.map(sp => /*#__PURE__*/React.createElement("tr", {
      key: sp.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, sp.nome), sp.tamanho && /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, " · ", sp.tamanho), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, sp.codigo, " ·", ' ', sp.larguraMm, " × ", sp.alturaMm, " mm")), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, sp.porProduto), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, (num(sp.fracaoTecido) * 100).toFixed(1), "%"), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, num(sp.consumo).toFixed(4), " m"), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, money(sp.custo))))), /*#__PURE__*/React.createElement("tr", {
      style: {
        background: 'var(--ok-bg)'
      }
    }, /*#__PURE__*/React.createElement("td", {
      colSpan: "3"
    }, /*#__PURE__*/React.createElement("strong", null, "Total do tecido")), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, t.consumo.toFixed(4), " m")), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, money(t.custo)))))))), subs.length === 0 && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "⚠ Esta modelagem não tem partes nomeadas. Abra-a em Corte e identifique as peças.")));
  })), aba === 'comofeito' && passo === 'partes' && /*#__PURE__*/React.createElement(ListaSubprodutos, {
    db: db,
    update: update,
    usuario: usuario,
    produtoId: produtoId,
    podeEditar: podeEditar
  }), aba === 'ficha' && /*#__PURE__*/React.createElement(FichaTecnica, {
    produto: produto,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    custo: c
  }));
}
const CUIDADOS_PADRAO = ['Lavar à mão', 'Lavar à máquina', 'Água fria', 'Água morna até 40°C', 'Não usar alvejante', 'Não torcer', 'Secar à sombra', 'Não secar em máquina', 'Passar em ferro morno', 'Não passar', 'Não limpar a seco'];

/* ---------------- Grade de tamanhos ----------------
   Livre: você cria as linhas de medida e as colunas de tamanho
   que a sua peça pede — nenhuma delas vem imposta. */
function GradeTamanhos({
  grade,
  onChange,
  podeEditar
}) {
  const g = grade || {
    colunas: [],
    linhas: []
  };
  const colunas = g.colunas || [];
  const linhas = g.linhas || [];
  const set = patch => onChange({
    ...g,
    ...patch
  });
  const addColuna = () => set({
    colunas: [...colunas, ''],
    linhas: linhas.map(l => ({
      ...l,
      valores: [...(l.valores || []), '']
    }))
  });
  const setColuna = (i, v) => set({
    colunas: colunas.map((c, j) => j === i ? v : c)
  });
  const rmColuna = i => set({
    colunas: colunas.filter((_, j) => j !== i),
    linhas: linhas.map(l => ({
      ...l,
      valores: (l.valores || []).filter((_, j) => j !== i)
    }))
  });
  const addLinha = () => set({
    linhas: [...linhas, {
      id: uid(),
      nome: '',
      valores: colunas.map(() => '')
    }]
  });
  const setLinha = (id, patch) => set({
    linhas: linhas.map(l => l.id === id ? {
      ...l,
      ...patch
    } : l)
  });
  const setCelula = (id, i, v) => set({
    linhas: linhas.map(l => l.id !== id ? l : {
      ...l,
      valores: (l.valores || []).map((x, j) => j === i ? v : x)
    })
  });
  const rmLinha = id => set({
    linhas: linhas.filter(l => l.id !== id)
  });

  /* atalhos que economizam digitação, sem obrigar */
  const sugestoes = [{
    nome: 'P M G',
    cols: ['P', 'M', 'G']
  }, {
    nome: 'PP a XG',
    cols: ['PP', 'P', 'M', 'G', 'GG', 'XG']
  }, {
    nome: 'Numérico',
    cols: ['36', '38', '40', '42', '44', '46']
  }, {
    nome: 'Único',
    cols: ['Único']
  }];
  if (colunas.length === 0 && linhas.length === 0) {
    return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginBottom: 8
      }
    }, "Nenhuma grade. Comece por um conjunto de tamanhos ou monte o seu."), podeEditar && /*#__PURE__*/React.createElement("div", {
      className: "row-actions"
    }, sugestoes.map(x => /*#__PURE__*/React.createElement("button", {
      key: x.nome,
      type: "button",
      className: "btn ghost sm",
      onClick: () => set({
        colunas: x.cols,
        linhas: [{
          id: uid(),
          nome: '',
          valores: x.cols.map(() => '')
        }]
      })
    }, x.nome)), /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "btn ghost sm",
      onClick: () => set({
        colunas: [''],
        linhas: [{
          id: uid(),
          nome: '',
          valores: ['']
        }]
      })
    }, "Do zero")));
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "grade-rolagem"
  }, /*#__PURE__*/React.createElement("table", {
    className: "grade"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    style: {
      minWidth: 150
    }
  }, "Medida"), colunas.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    className: "num",
    style: {
      minWidth: 74
    }
  }, podeEditar ? /*#__PURE__*/React.createElement("input", {
    value: c,
    placeholder: "tam.",
    style: {
      margin: 0,
      textAlign: 'center'
    },
    onChange: e => setColuna(i, e.target.value)
  }) : c || '—', podeEditar && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn-ic ic-danger grade-x",
    title: "Remover tamanho",
    onClick: () => rmColuna(i)
  }, "×"))), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 36
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, linhas.map(l => /*#__PURE__*/React.createElement("tr", {
    key: l.id
  }, /*#__PURE__*/React.createElement("td", null, podeEditar ? /*#__PURE__*/React.createElement("input", {
    value: l.nome,
    placeholder: "Largura do busto, Comprimento…",
    style: {
      margin: 0
    },
    onChange: e => setLinha(l.id, {
      nome: e.target.value
    })
  }) : /*#__PURE__*/React.createElement("strong", null, l.nome || '—')), colunas.map((_, i) => /*#__PURE__*/React.createElement("td", {
    key: i,
    className: "num"
  }, podeEditar ? /*#__PURE__*/React.createElement("input", {
    value: (l.valores || [])[i] || '',
    style: {
      margin: 0,
      textAlign: 'center'
    },
    onChange: e => setCelula(l.id, i, e.target.value)
  }) : (l.valores || [])[i] || '—')), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn-ic ic-danger",
    onClick: () => rmLinha(l.id)
  }, "×"))))))), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "row-actions no-print",
    style: {
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn ghost sm",
    onClick: addLinha
  }, "+ Medida"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn ghost sm",
    onClick: addColuna
  }, "+ Tamanho"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "O campo aceita qualquer texto — centímetros, polegadas, tolerância.")));
}

/* ---------------- Processo produtivo ---------------- */
function ProcessoProdutivo({
  produto,
  db,
  update,
  usuario,
  podeEditar
}) {
  const [aba, setAba] = useState('etapas');
  const [editando, setEditando] = useState(null);
  const [quantidade, setQuantidade] = useState('100');
  const [inicio, setInicio] = useState(todayISO());
  const [horaExtra, setHoraExtra] = useState(false);
  const [sabado, setSabado] = useState(false);
  const [aprovando, setAprovando] = useState(null);
  const [quemAutoriza, setQuemAutoriza] = useState('');
  const r = resumoProcesso(produto, db);
  const mo = custoMaoDeObra(produto, db);
  /* materiais que ninguém pegou: aparecem como aviso no rodapé */
  const orfaos = materiaisSemEtapa(produto, db);
  const j = jornadaDe(db);
  const autorizacao = produto.autorizacao || {};
  const setProc = v => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produto.id ? p : {
      ...p,
      processo: v
    });
    return d;
  });
  const passos = (produto.processo || []).slice().sort((a, b) => num(a.ordem) - num(b.ordem));

  /* a etapa vem do catálogo da Engenharia e traz o setor junto */
  const salvarPasso = dados => {
    const limpo = {
      id: dados.id,
      departamentoId: dados.departamentoId,
      etapaId: dados.etapaId,
      equipamentoId: dados.equipamentoId || '',
      modo: dados.modo || 'pessoa',
      tempo: num(dados.tempo),
      unidadeTempo: dados.unidadeTempo || 's',
      pessoas: num(dados.pessoas) || 1,
      loteEtapa: num(dados.loteEtapa) > 0 ? num(dados.loteEtapa) : '',
      materiais: dados.materiais || [],
      subprodutos: dados.subprodutos || [],
      anexos: dados.anexos || [],
      riscosProducao: dados.riscosProducao || []
    };
    if (dados.novo) {
      const lista = passos.slice();
      const onde = dados.posicao === undefined || dados.posicao > lista.length ? lista.length : Math.max(num(dados.posicao), 0);
      lista.splice(onde, 0, limpo);
      setProc(lista.map((p, i) => ({
        ...p,
        ordem: i + 1
      })));
    } else {
      setProc(passos.map(p => p.id === limpo.id ? {
        ...p,
        ...limpo
      } : p).map((p, i) => ({
        ...p,
        ordem: i + 1
      })));
    }
  };

  /* incluir uma etapa numa posição certa: é assim que o bloco do
     setor cresce sem embaralhar a sequência da peça */
  const rm = id => setProc(passos.filter(p => p.id !== id).map((p, i) => ({
    ...p,
    ordem: i + 1
  })));
  function mover(id, dir) {
    const i = passos.findIndex(p => p.id === id);
    const k = i + dir;
    if (i < 0 || k < 0 || k >= passos.length) return;
    const l = passos.slice();
    [l[i], l[k]] = [l[k], l[i]];
    setProc(l.map((p, n) => ({
      ...p,
      ordem: n + 1
    })));
  }

  /* o cronograma respeita o que já foi autorizado */
  const usaExtra = horaExtra && autorizacao.horaExtra;
  const usaSabado = sabado && autorizacao.sabado;
  const crono = montarCronograma(produto, db, {
    quantidade,
    inicio,
    horaExtra: usaExtra,
    sabado: usaSabado
  });
  const ganho = ganhoComHoraExtra(produto, db, {
    quantidade,
    inicio
  });
  function autorizar(tipo, quem) {
    if (!String(quem || '').trim()) return alert('Informe quem está autorizando.');
    update(d => {
      d.produtos = d.produtos.map(p => p.id !== produto.id ? p : {
        ...p,
        autorizacao: {
          ...(p.autorizacao || {}),
          [tipo]: {
            quem: quem.trim(),
            quando: agoraISO(),
            por: usuario ? usuario.nome : ''
          }
        }
      });
      registrarLog(d, usuario, tipo === 'horaExtra' ? 'Autorizou hora extra' : 'Autorizou trabalho no sábado', `${produto.nome || produto.codigo} · aprovado por ${quem.trim()}`);
      return d;
    });
    if (tipo === 'horaExtra') setHoraExtra(true);else setSabado(true);
    setAprovando(null);
    setQuemAutoriza('');
  }
  function revogar(tipo) {
    if (!confirm('Revogar esta autorização?')) return;
    update(d => {
      d.produtos = d.produtos.map(p => {
        if (p.id !== produto.id) return p;
        const a2 = {
          ...(p.autorizacao || {})
        };
        delete a2[tipo];
        return {
          ...p,
          autorizacao: a2
        };
      });
      return d;
    });
    if (tipo === 'horaExtra') setHoraExtra(false);else setSabado(false);
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "A sequência que o produto percorre na fábrica. Cada etapa tem setor, equipamento e tempo por peça — e é daí que sai o cronograma."), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'etapas',
      label: `Etapas (${passos.length})`
    }, {
      id: 'cronograma',
      label: 'Cronograma'
    }]
  }), aba === 'etapas' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    style: {
      marginBottom: 10
    },
    onClick: () => setEditando({
      novo: true,
      id: uid(),
      departamentoId: '',
      etapaId: '',
      equipamentoId: '',
      modo: 'pessoa',
      tempo: '',
      unidadeTempo: 's',
      pessoas: 1,
      materiais: []
    })
  }, "+ Incluir etapa"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Os tempos aqui são ", /*#__PURE__*/React.createElement("strong", null, "por peça"), ". Etapas ", /*#__PURE__*/React.createElement("strong", null, "por projeto"), ' ', "— preparar tela, gravar matriz, montar o enfesto — aparecem com o tempo inteiro; é na ordem de produção, com a quantidade real, que elas se dividem entre as peças."), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, /*#__PURE__*/React.createElement("strong", null, "No campo Tempo, número solto é segundo:"), " digite", ' ', /*#__PURE__*/React.createElement("strong", null, "27"), " para 27 segundos. Para minutos use", ' ', /*#__PURE__*/React.createElement("strong", null, "20min"), ", ou ", /*#__PURE__*/React.createElement("strong", null, "1:30"), " para um minuto e meio.")), r.avisos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, r.avisos.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", x))), passos.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma etapa. Use o botão acima para incluir a primeira operação do processo."
  }) : /*#__PURE__*/React.createElement("div", null, (() => {
    const blocos = [];
    r.passos.forEach((p, i) => {
      const depId = p.passo.departamentoId || '';
      const ultimo = blocos[blocos.length - 1];
      if (ultimo && ultimo.depId === depId) ultimo.itens.push({
        p,
        i
      });else blocos.push({
        depId,
        itens: [{
          p,
          i
        }]
      });
    });
    return blocos.map((bloco, nb) => {
      const depto = (db.departamentos || []).find(d => d.id === bloco.depId);
      const minutos = bloco.itens.reduce((a, x) => a + num(x.p.minutosHomem), 0);
      const custo = bloco.itens.reduce((a, x) => a + num(((mo.passos || [])[x.i] || {}).custo), 0);
      const equipe = bloco.depId ? colaboradoresDoDepartamento(bloco.depId, db) : [];
      const repetido = blocos.filter(b => b.depId === bloco.depId).length > 1;
      const passagem = repetido ? blocos.slice(0, nb + 1).filter(b => b.depId === bloco.depId).length : 0;
      return /*#__PURE__*/React.createElement("div", {
        key: nb,
        className: "setor-bloco"
      }, /*#__PURE__*/React.createElement("div", {
        className: "setor-topo"
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          flex: 1,
          minWidth: 170
        }
      }, /*#__PURE__*/React.createElement("strong", null, depto ? depto.nome : 'Sem departamento'), passagem > 0 && /*#__PURE__*/React.createElement("span", {
        className: "setor-passagem"
      }, passagem, "ª passagem"), /*#__PURE__*/React.createElement("div", {
        className: "small muted"
      }, bloco.itens.length, " etapa(s)", equipe.length > 0 ? ` · ${equipe.length} pessoa(s) no setor` : bloco.depId ? ' · ninguém no setor' : '')), /*#__PURE__*/React.createElement("div", {
        className: "painel-meta",
        style: {
          margin: 0,
          flex: '0 1 260px'
        }
      }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
        className: "k"
      }, "Por peça"), /*#__PURE__*/React.createElement("div", {
        className: "v",
        style: {
          fontSize: 15
        }
      }, duracao(minutos))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
        className: "k"
      }, "Custo"), /*#__PURE__*/React.createElement("div", {
        className: "v",
        style: {
          fontSize: 15
        }
      }, money(custo))))), bloco.itens.map(({
        p,
        i
      }) => {
        const etapa = p.etapa;
        const equip = p.equipamento;
        const linhaCusto = (mo.passos || [])[i];
        const mats = materiaisDaEtapa(p.passo, produto, db);
        return /*#__PURE__*/React.createElement("div", {
          key: p.passo.id,
          className: "etapa-linha" + (p.semTempo ? ' sem-tempo' : '')
        }, /*#__PURE__*/React.createElement("span", {
          className: "etapa-n"
        }, i + 1), /*#__PURE__*/React.createElement("div", {
          style: {
            flex: 1,
            minWidth: 170
          }
        }, /*#__PURE__*/React.createElement("strong", null, etapa ? etapa.nome : 'etapa não escolhida'), /*#__PURE__*/React.createElement("div", {
          className: "small muted"
        }, modoEtapa(p.modo).nome, num(p.passo.pessoas) > 1 && ` · ${num(p.passo.pessoas)} pessoas`, p.rendePara && ` · rende ${p.rendePara} peças`, equip ? ` · ${equip.nome}` : ''), mats.length > 0 && /*#__PURE__*/React.createElement("div", {
          className: "small muted"
        }, mats.map(m => m.nome + (num(m.consumo) > 0 ? ` (${num(m.consumo).toFixed(4)} ${m.unidade})` : '')).join(' · ')), (() => {
          const sps = subprodutosDaEtapa(p.passo, produto, db);
          if (sps.length === 0) return null;
          return /*#__PURE__*/React.createElement("div", {
            className: "small muted"
          }, "Partes: ", sps.map(sp => sp.nome + (sp.tamanho ? ` ${sp.tamanho}` : '')).join(', '));
        })(), riscosDeProducao(p.passo).length > 0 && /*#__PURE__*/React.createElement("div", {
          className: "anexos",
          style: {
            marginTop: 4
          }
        }, /*#__PURE__*/React.createElement("span", {
          className: "rot-risco"
        }, "risco de produção"), riscosDeProducao(p.passo).map(x => /*#__PURE__*/React.createElement("a", {
          key: x.id,
          className: "anexo-chip risco-prod",
          href: x.url,
          target: "_blank",
          rel: "noreferrer",
          download: x.nome
        }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
          src: x.url,
          alt: x.nome
        }) : /*#__PURE__*/React.createElement("span", {
          className: "anexo-ic"
        }, "📐"), /*#__PURE__*/React.createElement("span", {
          className: "small"
        }, x.nome)))), (p.passo.anexos || []).length > 0 && /*#__PURE__*/React.createElement("div", {
          className: "anexos",
          style: {
            marginTop: 4
          }
        }, (p.passo.anexos || []).map(x => /*#__PURE__*/React.createElement("a", {
          key: x.id,
          className: "anexo-chip",
          href: x.url,
          target: "_blank",
          rel: "noreferrer",
          download: x.nome
        }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
          src: x.url,
          alt: x.nome
        }) : /*#__PURE__*/React.createElement("span", {
          className: "anexo-ic"
        }, "📄"), /*#__PURE__*/React.createElement("span", {
          className: "small"
        }, x.nome))))), /*#__PURE__*/React.createElement("div", {
          className: "etapa-nums"
        }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
          className: "k"
        }, "Tempo"), /*#__PURE__*/React.createElement("div", {
          className: "v"
        }, p.tempo > 0 ? duracao(p.tempo) : /*#__PURE__*/React.createElement("span", {
          style: {
            color: 'var(--warn)'
          }
        }, "sem tempo")), /*#__PURE__*/React.createElement("div", {
          className: "k"
        }, p.rendePara ? `por ${p.rendePara} peças` : p.modo === 'projeto' ? 'por lote' : 'por peça')), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
          className: "k"
        }, "Por peça"), /*#__PURE__*/React.createElement("div", {
          className: "v"
        }, duracao(p.minutosHomem))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
          className: "k"
        }, "Custo"), /*#__PURE__*/React.createElement("div", {
          className: "v"
        }, linhaCusto && linhaCusto.custo > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, money(linhaCusto.custo), linhaCusto.estimado && /*#__PURE__*/React.createElement("span", {
          className: "est"
        }, "*")) : '—'))), podeEditar && /*#__PURE__*/React.createElement("div", {
          className: "acoes-icone"
        }, /*#__PURE__*/React.createElement("button", {
          className: "btn-ic",
          title: "Subir",
          disabled: i === 0,
          onClick: () => mover(p.passo.id, -1)
        }, "↑"), /*#__PURE__*/React.createElement("button", {
          className: "btn-ic",
          title: "Descer",
          disabled: i === r.passos.length - 1,
          onClick: () => mover(p.passo.id, 1)
        }, "↓"), /*#__PURE__*/React.createElement("button", {
          className: "btn-ic ic-accent",
          title: "Editar",
          onClick: () => setEditando({
            ...p.passo
          })
        }, "✎"), /*#__PURE__*/React.createElement("button", {
          className: "btn-ic ic-danger",
          title: "Excluir",
          onClick: () => {
            if (!confirm(`Excluir a etapa ${etapa ? etapa.nome : ''} do processo?`)) return;
            rm(p.passo.id);
          }
        }, "×")));
      }), podeEditar && bloco.depId && /*#__PURE__*/React.createElement("button", {
        className: "btn ghost sm",
        style: {
          marginTop: 9
        },
        onClick: () => setEditando({
          novo: true,
          id: uid(),
          departamentoId: bloco.depId,
          etapaId: '',
          equipamentoId: '',
          modo: 'pessoa',
          tempo: '',
          unidadeTempo: 's',
          pessoas: 1,
          materiais: [],
          posicao: bloco.itens[bloco.itens.length - 1].i + 1
        })
      }, "+ Etapa em ", depto ? depto.nome : 'este setor'));
    });
  })(), (() => {
    const semEtapa = subprodutosSemEtapa(produto, db);
    const marcadas = subprodutosDoProduto(produto, db).length - semEtapa.length;
    /* só cobra depois que alguém começou a marcar: um processo
       que trata a peça inteira não precisa de partes por etapa */
    if (semEtapa.length === 0 || marcadas === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--warn)',
        background: 'var(--warn-bg)',
        marginBottom: 12
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "⚠ ", semEtapa.length, " parte(s) sem etapa:", ' ', semEtapa.map(sp => sp.nome).join(', '), "."), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Como você já marcou partes em outras etapas, essas ficaram sem quem as processe."));
  })(), orfaos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", orfaos.length, " material(is) sem etapa:", ' ', orfaos.map(t => t.material ? t.material.nome : 'sem nome').join(', '), "."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Edite a etapa onde cada um entra e marque o material lá.")), /*#__PURE__*/React.createElement("div", {
    className: "setor-total"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 150
    }
  }, /*#__PURE__*/React.createElement("strong", null, "Total por peça"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "uma peça, sem diluição")), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta",
    style: {
      margin: 0,
      flex: '0 1 380px'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Etapas"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, r.passos.length)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Relógio"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(r.minutos))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Trabalho"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(r.minutosHomem))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Mão de obra"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, money(mo.total)))))), mo.temEstimativa && !mo.geral.vazia && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      marginTop: 6
    }
  }, "* Custo estimado pela média da fábrica: ", mo.estimados.join(', '), mo.estimados.length === 1 ? ' ainda não tem' : ' ainda não têm', " equipe própria cadastrada."), editando && /*#__PURE__*/React.createElement(EtapaDoProcessoModal, {
    passo: editando,
    produto: produto,
    db: db,
    onClose: () => setEditando(null),
    onSalvar: dados => {
      salvarPasso(dados);
      setEditando(null);
    }
  })), aba === 'cronograma' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quantidade do lote"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: quantidade,
    onChange: e => setQuantidade(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Início da produção"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: inicio,
    onChange: e => setInicio(e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Jornada de ", j.inicio, " às ", j.fim, ", segunda a sexta. Descontados ", j.pausas, " min de intervalo, sobram ", /*#__PURE__*/React.createElement("strong", null, j.produtivo, " minutos"), " por dia.")), crono.vazio ? /*#__PURE__*/React.createElement(Empty, {
    text: crono.motivo
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Trabalho total"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, crono.totalHoras), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "horas (", crono.totalMinutos, " min)")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Dias necessários"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, crono.diasNecessarios)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Entrega"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      fontSize: 17
    }
  }, fmtDate(crono.ultimoDia)), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "começando ", fmtDate(crono.primeiroDia))), crono.usaExtras && /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Horas extras"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      color: 'var(--warn)'
    }
  }, (crono.minutosExtras / 60).toFixed(1)))), crono.passaDoDia && ganho && ganho.ganho > 0 && !autorizacao.horaExtra && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      marginBottom: 6
    }
  }, "O trabalho não cabe na jornada normal: são ", ganho.sem, " dias úteis."), /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginBottom: 8
    }
  }, "Estendendo até ", j.extensao.fim, " — mais ", j.minutosExtras, " min por dia — a produção cai para ", /*#__PURE__*/React.createElement("strong", null, ganho.com, " dia(s)"), ", com ", ganho.horasExtras, " h extras no total. Isso exige aprovação de ", j.extensao.exige, "."), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setAprovando('horaExtra')
  }, "Programar horas extras")), autorizacao.horaExtra && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "✓ Horas extras autorizadas por ", /*#__PURE__*/React.createElement("strong", null, autorizacao.horaExtra.quem), ' ', "em ", fmtDataHora(autorizacao.horaExtra.quando), autorizacao.horaExtra.por && ` · registrado por ${autorizacao.horaExtra.por}`), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginTop: 7
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: horaExtra,
    onChange: e => setHoraExtra(e.target.checked)
  }), "Usar no cronograma"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => revogar('horaExtra')
  }, "Revogar"))), crono.diasNecessarios > 5 && !autorizacao.sabado && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 7
    }
  }, "A produção atravessa mais de uma semana. Trabalhar no sábado (", j.sabado.inicio, " às ", j.sabado.fim, ", ", j.minutosSabado, " min) exige autorização de ", j.sabado.exige, "."), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setAprovando('sabado')
  }, "Programar sábado")), autorizacao.sabado && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "✓ Sábado autorizado por ", /*#__PURE__*/React.createElement("strong", null, autorizacao.sabado.quem), ' ', "em ", fmtDataHora(autorizacao.sabado.quando)), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginTop: 7
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "small linha-check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: sabado,
    onChange: e => setSabado(e.target.checked)
  }), "Usar no cronograma"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => revogar('sabado')
  }, "Revogar"))), /*#__PURE__*/React.createElement("h3", null, "Cronograma dia a dia"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Dia"), /*#__PURE__*/React.createElement("th", null, "O que se produz"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Normal"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Extra"), /*#__PURE__*/React.createElement("th", {
    style: {
      minWidth: 110
    }
  }, "Ocupação"))), /*#__PURE__*/React.createElement("tbody", null, crono.dias.map(d => /*#__PURE__*/React.createElement("tr", {
    key: d.iso,
    className: d.sabado ? 'linha-sabado' : ''
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, fmtDate(d.iso)), /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, ['domingo', 'segunda', 'terça', 'quarta', 'quinta', 'sexta', 'sábado'][d.data.getDay()], d.sabado && ' · autorizado')), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, d.etapas.map((e, k) => /*#__PURE__*/React.createElement("div", {
    key: k
  }, e.etapa, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", e.departamento), e.equipamento && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", e.equipamento), /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", e.minutos, " min"), e.parcial && /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--warn)'
    }
  }, " (continua)")))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, d.minutosNormais), /*#__PURE__*/React.createElement("td", {
    className: "num",
    style: d.minutosExtras > 0 ? {
      color: 'var(--warn)'
    } : {}
  }, d.minutosExtras > 0 ? d.minutosExtras : '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("div", {
    style: {
      background: '#e8e4da',
      borderRadius: 5,
      height: 10,
      display: 'flex',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: d.minutosNormais / d.capacidade * 100 + '%',
      background: 'var(--thread)'
    }
  }), d.minutosExtras > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      width: d.minutosExtras / d.capacidade * 100 + '%',
      background: 'var(--warn)'
    }
  })))))))), crono.incompleto && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--bad)',
      fontWeight: 600,
      marginTop: 6
    }
  }, "⛔ O lote passa de 400 dias de produção — o cronograma foi interrompido. Divida o pedido.")), aprovando && /*#__PURE__*/React.createElement(Modal, {
    title: aprovando === 'horaExtra' ? 'Autorizar horas extras' : 'Autorizar sábado',
    onClose: () => setAprovando(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, aprovando === 'horaExtra' ? `A jornada se estende até ${j.extensao.fim}, somando ${j.minutosExtras} minutos por dia.` : `O sábado rende ${j.minutosSabado} minutos, das ${j.sabado.inicio} às ${j.sabado.fim}.`, ' ', "A autorização fica registrada com o nome de quem aprovou."), /*#__PURE__*/React.createElement(Field, {
    label: "Autorizado por *"
  }, (db.colaboradores || []).length > 0 ? /*#__PURE__*/React.createElement("select", {
    value: quemAutoriza,
    onChange: e => setQuemAutoriza(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— quem está autorizando —"), (db.colaboradores || []).filter(x => x.status !== 'Inativo').map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.nome
  }, x.nome))) : /*#__PURE__*/React.createElement("input", {
    value: quemAutoriza,
    placeholder: "nome do superior",
    onChange: e => setQuemAutoriza(e.target.value),
    onKeyDown: e => {
      if (e.key === 'Enter') autorizar(aprovando, quemAutoriza);
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setAprovando(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !String(quemAutoriza || '').trim(),
    onClick: () => autorizar(aprovando, quemAutoriza)
  }, "Autorizar")))));
}

/* ---------------- Uma etapa do processo, no modal ----------------
   A tabela mostra; aqui se decide. Separar as duas coisas deixa a
   lista legível e o formulário com espaço para explicar. */
function EtapaDoProcessoModal({
  passo,
  produto,
  db,
  onClose,
  onSalvar
}) {
  const [f, setF] = useState({
    ...passo
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const etapasDep = f.departamentoId ? etapasDoDepartamento(f.departamentoId, db) : [];
  const equipsDep = f.departamentoId ? equipamentosDoDepartamento(f.departamentoId, db) : [];
  const equipe = f.departamentoId ? colaboradoresDoDepartamento(f.departamentoId, db) : [];
  const lote = 1; /* o produto se mede por peça; a ordem dilui depois */

  const materiais = custearProduto(produto, db).tecidos;
  const marcados = f.materiais || [];
  const alternarMat = tid => set('materiais', marcados.includes(tid) ? marcados.filter(x => x !== tid) : [...marcados, tid]);
  const LIMITE_ANEXO = 900 * 1024;
  async function anexar(e, destino) {
    const campo = destino || 'anexos';
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > LIMITE_ANEXO) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa do ` + `limite de 900 KB. Reduza o arquivo — tudo fica guardado no navegador.`);
    try {
      const url = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
        r.readAsDataURL(file);
      });
      setF(p => ({
        ...p,
        [campo]: [...(p[campo] || []), {
          id: uid(),
          nome: file.name,
          tamanho: file.size,
          tipo: file.type || '',
          url,
          quando: agoraISO()
        }]
      }));
    } catch (err) {
      alert(err.message);
    }
  }

  /* as partes que saíram dos riscos deste produto */
  const partes = subprodutosDoProduto(produto, db);
  const alternarParte = spid => {
    const atuais = f.subprodutos || [];
    set('subprodutos', atuais.includes(spid) ? atuais.filter(x => x !== spid) : [...atuais, spid]);
  };
  const unidade = f.unidadeTempo || 's';
  const minutos = num(f.tempo);
  const porPeca = minutosPorPeca(f.modo, minutos, num(f.pessoas) || 1, lote, num(f.loteEtapa));
  const pronto = !!f.departamentoId && !!f.etapaId;
  return /*#__PURE__*/React.createElement(Modal, {
    title: f.novo ? 'Incluir etapa no processo' : 'Editar etapa',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Departamento *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId || '',
    onChange: e => setF(p => ({
      ...p,
      departamentoId: e.target.value,
      etapaId: '',
      equipamentoId: ''
    }))
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha o setor —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => {
    const n = etapasDoDepartamento(d.id, db).filter(e => e.ativo !== false).length;
    return /*#__PURE__*/React.createElement("option", {
      key: d.id,
      value: d.id
    }, d.nome, n === 0 ? ' — sem etapas cadastradas' : ` · ${n} etapa(s)`);
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Etapa *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.etapaId || '',
    disabled: !f.departamentoId,
    onChange: e => {
      const et = (db.etapas || []).find(x => x.id === e.target.value);
      setF(p => ({
        ...p,
        etapaId: e.target.value,
        modo: et && et.modo ? et.modo : p.modo
      }));
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, f.departamentoId ? etapasDep.length ? '— escolha a operação —' : '— setor sem etapas —' : '— escolha o setor antes —'), etapasDep.filter(e => e.ativo !== false).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome))))), f.departamentoId && etapasDep.length === 0 && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      marginTop: -6
    }
  }, "⚠ Este setor não tem etapas cadastradas. Vá em", ' ', /*#__PURE__*/React.createElement("strong", null, "Engenharia → Etapas"), " e cadastre as operações dele."), f.departamentoId && equipe.length === 0 && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      marginTop: -4,
      marginBottom: 8
    }
  }, "⚠ Ninguém alocado neste setor: o custo sairá pela média da fábrica e o cronograma não conseguirá programar esta etapa."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, (() => {
    /* A operação pode sair em mais de uma máquina — "fecha no overloque
       ou na interlock". Marcar todas dá ao planejamento onde encaixar
       quando uma estiver ocupada ou parada. */
    const lista = Array.isArray(f.equipamentos) && f.equipamentos.length
      ? f.equipamentos
      : (f.equipamentoId ? [f.equipamentoId] : []);
    const opcoes = equipsDep.filter(x => x.ativo !== false);
    if (!f.departamentoId)
      return /*#__PURE__*/React.createElement(Field, { label: "Equipamentos" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Escolha o setor acima para ver as máquinas."));
    if (opcoes.length === 0)
      return /*#__PURE__*/React.createElement(Field, { label: "Equipamentos" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Nenhuma máquina cadastrada neste setor — a operação é manual."));
    const marcar = id => {
      const nova = lista.includes(id)
        ? lista.filter(x => x !== id) : [...lista, id];
      set('equipamentos', nova);
      /* o primeiro da lista continua sendo o equipamento da ficha */
      set('equipamentoId', nova[0] || '');
    };
    return /*#__PURE__*/React.createElement("div", null,
      /*#__PURE__*/React.createElement("div", { className: "etp-rot",
        style: { marginBottom: 6 } },
        `Equipamentos (${lista.length} de ${opcoes.length})`),
      /*#__PURE__*/React.createElement("div", { className: "gente-grade" },
        opcoes.map(x => /*#__PURE__*/React.createElement("label", {
          key: x.id,
          className: "gente-check" + (lista.includes(x.id) ? " on" : "")
        },
          /*#__PURE__*/React.createElement("input", {
            type: "checkbox", checked: lista.includes(x.id),
            onChange: () => marcar(x.id)
          }),
          /*#__PURE__*/React.createElement("span", null,
            /*#__PURE__*/React.createElement("strong", null, x.nome),
            /*#__PURE__*/React.createElement("span", { className: "small muted" },
              x.codigo || x.tipo || ''))))),
      /*#__PURE__*/React.createElement("div", { className: "small muted",
        style: { marginTop: 6, marginBottom: 10 } },
        lista.length === 0
          ? "Nenhuma marcada: a operação é tratada como manual e qualquer "
            + "pessoa do setor pode fazê-la."
          : lista.length === 1
            ? "Só esta máquina. Se ela parar, a operação para junto — marcar "
              + "as alternativas dá ao planejamento onde encaixar."
            : `${lista.length} alternativas. O planejamento usa a que estiver `
              + "livre no dia."));
  })(), false && /*#__PURE__*/React.createElement(Field, {
    label: "Equipamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.equipamentoId || '',
    disabled: !f.departamentoId,
    onChange: e => set('equipamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem equipamento —"), equipsDep.filter(x => x.ativo !== false).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome, x.situacao !== 'operando' ? ` — ${situacaoEquip(x.situacao).nome}` : '')))), /*#__PURE__*/React.createElement(Field, {
    label: "Modo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.modo || 'pessoa',
    onChange: e => set('modo', e.target.value)
  }, MODOS_ETAPA.map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, (MODOS_ETAPA.find(m => m.id === (f.modo || 'pessoa')) || {}).desc), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: `Tempo ${f.modo === 'projeto' ? 'por lote' : 'por peça'} *`
  }, /*#__PURE__*/React.createElement("div", {
    className: "campo-tempo largo"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.tempoTexto !== undefined ? f.tempoTexto : tempoNaUnidade(f.tempo, unidade),
    inputMode: "decimal",
    placeholder: "0",
    onChange: e => setF(p => ({
      ...p,
      tempoTexto: e.target.value,
      tempo: tempoParaMinutos(e.target.value, unidade)
    }))
  }), /*#__PURE__*/React.createElement("div", {
    className: "unidades-tempo"
  }, UNIDADES_TEMPO.map(u => /*#__PURE__*/React.createElement("button", {
    key: u.id,
    type: "button",
    className: "un-btn" + (unidade === u.id ? ' on' : ''),
    onClick: () => {
      const naTela = f.tempoTexto !== undefined ? f.tempoTexto : tempoNaUnidade(f.tempo, unidade);
      setF(p => ({
        ...p,
        unidadeTempo: u.id,
        tempoTexto: naTela,
        tempo: tempoParaMinutos(naTela, u.id)
      }));
    }
  }, u.curto)), minutos > 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      marginLeft: 6
    }
  }, duracao(minutos))))), /*#__PURE__*/React.createElement(Field, {
    label: "Pessoas na operação"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: num(f.pessoas) || 1,
    onChange: e => set('pessoas', e.target.value)
  }))), (f.modo === 'equipe' || f.modo === 'projeto') && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Field, {
    label: f.modo === 'projeto' ? 'Este tempo rende para quantas peças?' : 'A equipe produz quantas peças nesse tempo?'
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: f.loteEtapa || '',
    placeholder: f.modo === 'projeto' ? 'em branco: dilui pela quantidade da ordem' : 'em branco: o tempo é de uma peça só',
    onChange: e => set('loteEtapa', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, f.modo === 'projeto' ? 'Preparar uma tela serve para toda a ordem. Deixando em branco, ' + 'o custo se divide pela quantidade que a ordem pedir; preenchendo, ' + 'o sistema usa esse número mesmo antes de existir ordem.' : 'Se a equipe leva o tempo informado para sair com um lote de peças, ' + 'diga quantas — é assim que o custo por unidade fecha.')), minutos > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Minutos-homem por peça"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(porPeca))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Em 100 peças"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(minutosPorPeca(f.modo, minutos, num(f.pessoas) || 1, 100, num(f.loteEtapa)) * 100))), num(f.loteEtapa) > 0 && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Rende"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, num(f.loteEtapa), " peças"))), num(f.loteEtapa) > 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -4,
      marginBottom: 10
    }
  }, duracao(minutos), " × ", num(f.pessoas) || 1, " pessoa(s) ÷ ", num(f.loteEtapa), " peças", ' ', "= ", /*#__PURE__*/React.createElement("strong", null, duracao(porPeca)), " por peça.") : f.modo === 'projeto' ? /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -4,
      marginBottom: 10
    }
  }, "Sem quantidade informada, este tempo se divide pela ordem: em 100 peças vira ", duracao(minutos / 100), " por peça; em 1.000, ", duracao(minutos / 1000), ".") : null), /*#__PURE__*/React.createElement("h3", null, "Material consumido nesta etapa"), materiais.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "O produto ainda não tem material. Inclua na aba ", /*#__PURE__*/React.createElement("strong", null, "Materiais"), ".") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "mats-etapa"
  }, materiais.map(t => {
    const on = marcados.includes(t.id);
    return /*#__PURE__*/React.createElement("button", {
      key: t.id,
      type: "button",
      className: "mat-chip" + (on ? ' on' : '') + (num(t.consumo) > 0 ? '' : ' indefinido'),
      onClick: () => alternarMat(t.id)
    }, t.material ? t.material.nome : 'sem material', num(t.consumo) > 0 && t.material && ` · ${num(t.consumo).toFixed(4)} ${t.material.unidadeEstoque}`);
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, marcados.length === 0 ? 'Nenhum material marcado — a etapa não consome nada.' : `${marcados.length} material(is) nesta etapa.`)), etapaDeCorte(f, db) && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "Risco de produção"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 7
    }
  }, "O encaixe que vai para a mesa de corte. Diferente da", ' ', /*#__PURE__*/React.createElement("strong", null, "modelagem"), ", que é a engenharia da peça e traz as partes e o consumo: aqui é só o arquivo, guardado para quem corta abrir na hora."), /*#__PURE__*/React.createElement("div", {
    className: "anexos",
    style: {
      marginBottom: 12
    }
  }, (f.riscosProducao || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📐"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", Math.round(num(x.tamanho) / 1024), " KB")), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => set('riscosProducao', (f.riscosProducao || []).filter(y => y.id !== x.id))
  }, "×"))), (f.riscosProducao || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum risco de produção"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📐 Anexar risco", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: e => anexar(e, 'riscosProducao')
  })))), /*#__PURE__*/React.createElement("h3", null, "Arquivos da etapa"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 7
    }
  }, "Esquema de costura, foto de referência, instrução de máquina — o que a bancada precisa ver para fazer certo."), /*#__PURE__*/React.createElement("div", {
    className: "anexos",
    style: {
      marginBottom: 12
    }
  }, (f.anexos || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", Math.round(num(x.tamanho) / 1024), " KB")), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => set('anexos', (f.anexos || []).filter(y => y.id !== x.id))
  }, "×"))), (f.anexos || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum arquivo"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📎 Anexar", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: anexar
  }))), /*#__PURE__*/React.createElement("h3", null, "Partes processadas nesta etapa"), partes.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "O produto ainda não tem partes. Elas nascem quando uma modelagem é vinculado a um tecido, na aba ", /*#__PURE__*/React.createElement("strong", null, "Materiais"), ".") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "mats-etapa"
  }, partes.map(sp => {
    const on = (f.subprodutos || []).includes(sp.id);
    return /*#__PURE__*/React.createElement("button", {
      key: sp.id,
      type: "button",
      className: "mat-chip" + (on ? ' on' : ''),
      onClick: () => alternarParte(sp.id)
    }, sp.nome, sp.tamanho ? ` · ${sp.tamanho}` : '', num(sp.porProduto) > 1 ? ` · ${num(sp.porProduto)}×` : '');
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, (f.subprodutos || []).length === 0 ? 'Nenhuma parte marcada — a etapa vale para a peça inteira.' : `${(f.subprodutos || []).length} de ${partes.length} parte(s) passam por aqui.`)), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !pronto,
    onClick: () => onSalvar(f)
  }, f.novo ? 'Incluir etapa' : 'Salvar')));
}

/* ==========================================================
   CONVERSA ABERTA — telas
========================================================== */

/* A porta de entrada: seis botões grandes e nada mais. */
function CanalColaborador({
  db,
  update,
  usuario,
  setorInicial
}) {
  const [passo, setPasso] = useState('inicio'); // inicio · formulario · pronto
  const [verVagas, setVerVagas] = useState(false);
  const [categoria, setCategoria] = useState('');
  const [protocolo, setProtocolo] = useState('');
  const [consulta, setConsulta] = useState('');
  const [achada, setAchada] = useState(null);
  const [copiado, setCopiado] = useState(false);
  const eu = (db.colaboradores || []).find(c => usuario && c.nome === usuario.nome);
  const meuSetor = setorInicial || eu && eu.departamentoId || '';
  const minhas = eu ? (db.manifestacoes || []).filter(m => m.colaboradorId === eu.id) : [];
  if (verVagas) {
    return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setVerVagas(false)
    }, "← Voltar ao canal"), /*#__PURE__*/React.createElement(VagasParaColaborador, {
      db: db,
      update: update,
      usuario: usuario
    }));
  }
  if (passo === 'formulario') {
    return /*#__PURE__*/React.createElement(CanalFormulario, {
      db: db,
      update: update,
      usuario: usuario,
      categoria: categoria,
      setorInicial: meuSetor,
      onCancelar: () => setPasso('inicio'),
      onPronto: p => {
        setProtocolo(p);
        setPasso('pronto');
      }
    });
  }
  if (passo === 'pronto') {
    return /*#__PURE__*/React.createElement("div", {
      className: "canal-pronto"
    }, /*#__PURE__*/React.createElement("div", {
      className: "canal-selo"
    }, "✓"), /*#__PURE__*/React.createElement("h2", null, "Recebemos sua manifestação"), /*#__PURE__*/React.createElement("p", {
      className: "canal-sub"
    }, "Obrigado por contribuir para melhorar o nosso ambiente de trabalho."), /*#__PURE__*/React.createElement("div", {
      className: "canal-protocolo"
    }, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Guarde este protocolo para acompanhar"), /*#__PURE__*/React.createElement("strong", null, protocolo)), /*#__PURE__*/React.createElement("div", {
      className: "row-actions",
      style: {
        justifyContent: 'center'
      }
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => {
        const nav = typeof window !== 'undefined' && window.navigator || null;
        if (nav && nav.clipboard && nav.clipboard.writeText) {
          nav.clipboard.writeText(protocolo);
          setCopiado(true);
          setTimeout(() => setCopiado(false), 2500);
        }
      }
    }, copiado ? '✓ Copiado' : 'Copiar protocolo')), /*#__PURE__*/React.createElement("p", {
      className: "small muted",
      style: {
        maxWidth: 460,
        margin: '0 auto'
      }
    }, "Anote ou tire uma foto desta tela. Com o protocolo você acompanha a situação da sua manifestação sem precisar se identificar."), /*#__PURE__*/React.createElement("div", {
      className: "row-actions",
      style: {
        justifyContent: 'center',
        marginTop: 16
      }
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn accent",
      onClick: () => {
        setPasso('inicio');
        setProtocolo('');
      }
    }, "Voltar ao início")));
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "canal-inicio"
  }, /*#__PURE__*/React.createElement("div", {
    className: "canal-cabecalho"
  }, /*#__PURE__*/React.createElement("h2", null, "💬 Conversa aberta"), /*#__PURE__*/React.createElement("div", {
    className: "canal-lema"
  }, "Fale. Sugira. Aponte. Melhore."), /*#__PURE__*/React.createElement("p", {
    className: "canal-sub"
  }, "Tem alguma coisa no trabalho que podemos melhorar? Conte para nós.")), /*#__PURE__*/React.createElement("div", {
    className: "canal-opcoes"
  }, CATEGORIAS_CANAL.map(c => /*#__PURE__*/React.createElement("button", {
    key: c.id,
    className: "canal-opcao",
    onClick: () => {
      setCategoria(c.id);
      setPasso('formulario');
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ic"
  }, c.icone), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("strong", null, c.nome), /*#__PURE__*/React.createElement("span", {
    className: "frase"
  }, c.frase))))), /*#__PURE__*/React.createElement("div", {
    className: "canal-aviso"
  }, "Relatos feitos de boa-fé são tratados com respeito e sigilo. Este canal não deve ser usado para perseguir alguém, ameaçar ou acusar sabendo que não é verdade."), vagasAbertas(db).length > 0 && /*#__PURE__*/React.createElement("button", {
    className: "atalho-vagas",
    onClick: () => setVerVagas(true)
  }, /*#__PURE__*/React.createElement("span", {
    className: "ic"
  }, "🤝"), /*#__PURE__*/React.createElement("span", {
    className: "texto"
  }, /*#__PURE__*/React.createElement("strong", null, vagasAbertas(db).length, " vaga(s) aberta(s)"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "Conhece alguém que se daria bem aqui? Indique.")), /*#__PURE__*/React.createElement("span", {
    className: "seta"
  }, "→")), /*#__PURE__*/React.createElement(TermometroDia, {
    db: db,
    update: update,
    setorInicial: meuSetor
  }), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Acompanhar uma manifestação"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Digite o protocolo que você guardou."), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: consulta,
    placeholder: "CA-2026-00001",
    style: {
      maxWidth: 220,
      margin: 0
    },
    onChange: e => setConsulta(e.target.value.toUpperCase())
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      const alvo = (db.manifestacoes || []).find(m => String(m.protocolo || '').toUpperCase() === consulta.trim());
      setAchada(alvo || {
        naoAchou: true
      });
    }
  }, "Consultar")), achada && achada.naoAchou && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      marginTop: 8
    }
  }, "Não encontramos esse protocolo. Confira os números e tente de novo."), achada && !achada.naoAchou && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      marginTop: 10,
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      flex: 1
    }
  }, achada.protocolo), /*#__PURE__*/React.createElement("span", {
    className: "tag"
  }, statusCanal(achada.status).nome)), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, categoriaCanal(achada.categoria).nome, " ·", ' ', "aberta em ", fmtDataHora(achada.criadaEm)), (achada.interacoes || []).filter(i => !i.interno).map(i => /*#__PURE__*/React.createElement("div", {
    key: i.id,
    className: "canal-resposta"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, fmtDataHora(i.quando)), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, i.texto))), (achada.interacoes || []).filter(i => !i.interno).length === 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Ainda não há resposta registrada. Assim que houver, ela aparece aqui."))), minhas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Minhas manifestações"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "As que você registrou identificado. As anônimas só pelo protocolo."), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Protocolo"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", null, "Atualizada"))), /*#__PURE__*/React.createElement("tbody", null, minhas.slice().reverse().map(m => {
    const ultima = (m.historico || []).map(h => h.quando).sort().pop();
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, m.protocolo)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, categoriaCanal(m.categoria).nome), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, statusCanal(m.status).nome)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, fmtDate(String(ultima || '').slice(0, 10))));
  })))));
}

/* O formulário: poucos campos, linguagem simples, nada obrigatório
   além do essencial. */
function CanalFormulario({
  db,
  update,
  usuario,
  categoria,
  setorInicial,
  onCancelar,
  onPronto
}) {
  const cat = categoriaCanal(categoria);
  const eu = (db.colaboradores || []).find(c => usuario && c.nome === usuario.nome);
  const [f, setF] = useState({
    categoria,
    departamentoId: setorInicial || '',
    local: '',
    dataOcorrencia: todayISO(),
    descricao: '',
    frequencia: FREQUENCIAS_CANAL[0],
    impacto: IMPACTOS_CANAL[1],
    envolvidos: '',
    anexos: [],
    anonima: cat.sensivel,
    /* sensível começa anônimo */
    nome: eu ? eu.nome : '',
    matricula: eu && eu.matricula || '',
    querRetorno: true
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const LIMITE = 900 * 1024;
  async function anexar(e) {
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > LIMITE) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa do ` + `limite de 900 KB. Tire uma foto menor ou corte o vídeo.`);
    try {
      const url = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
        r.readAsDataURL(file);
      });
      setF(p => ({
        ...p,
        anexos: [...(p.anexos || []), {
          id: uid(),
          nome: file.name,
          tamanho: file.size,
          tipo: file.type || '',
          url
        }]
      }));
    } catch (err) {
      alert(err.message);
    }
  }
  const prioridade = prioridadeSugerida(f);
  const pronto = String(f.descricao || '').trim().length >= 10;
  return /*#__PURE__*/React.createElement("div", {
    className: "canal-form"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      if (String(f.descricao || '').trim() && !confirm('Você escreveu algo que ainda não foi enviado. Sair mesmo assim?')) return;
      onCancelar();
    }
  }, "← Voltar"), /*#__PURE__*/React.createElement("div", {
    className: "canal-cabecalho",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("h2", null, cat.icone, " ", cat.nome), /*#__PURE__*/React.createElement("p", {
    className: "canal-sub"
  }, cat.frase)), cat.sensivel && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, "Este tipo de relato tem acesso restrito: só o RH e a direção conseguem abrir. Seu líder e seus colegas não veem.")), cat.reconhecimento && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, "Um reconhecimento registrado chega a quem foi citado e à liderança. Vale para pessoa, equipe ou para uma mudança que deu certo — e não precisa ser nada grandioso: a ajuda de um colega num dia corrido conta.")), /*#__PURE__*/React.createElement(Field, {
    label: cat.reconhecimento ? 'Quem ou o que você quer reconhecer? *' : 'O que aconteceu? *'
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 5,
    value: f.descricao,
    placeholder: cat.reconhecimento ? 'Ex: a Maria ficou depois da hora para me ensinar a regular a máquina.' : 'Conte com suas palavras. Não precisa escrever bonito — só precisa ficar claro.',
    onChange: e => set('descricao', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Setor"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId,
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— não informar —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Local"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.local,
    placeholder: "máquina 4, corredor, refeitório…",
    onChange: e => set('local', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Quando foi"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.dataOcorrencia,
    onChange: e => set('dataOcorrencia', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Acontece com que frequência?"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.frequencia,
    onChange: e => set('frequencia', e.target.value)
  }, FREQUENCIAS_CANAL.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x)))), /*#__PURE__*/React.createElement(Field, {
    label: "O quanto atrapalha?"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.impacto,
    onChange: e => set('impacto', e.target.value)
  }, IMPACTOS_CANAL.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x))))), !f.anonima && /*#__PURE__*/React.createElement(Field, {
    label: "Pessoas envolvidas (se fizer diferença)"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.envolvidos,
    placeholder: "deixe em branco se preferir",
    onChange: e => set('envolvidos', e.target.value)
  })), /*#__PURE__*/React.createElement("h3", null, "Fotos, vídeos ou documentos"), /*#__PURE__*/React.createElement("div", {
    className: "anexos",
    style: {
      marginBottom: 12
    }
  }, (f.anexos || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => set('anexos', f.anexos.filter(y => y.id !== x.id))
  }, "×"))), (f.anexos || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum arquivo"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📎 Anexar", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: anexar
  }))), /*#__PURE__*/React.createElement("h3", null, "Você quer se identificar?"), /*#__PURE__*/React.createElement("div", {
    className: "canal-escolha"
  }, /*#__PURE__*/React.createElement("button", {
    className: "canal-botao" + (!f.anonima ? ' on' : ''),
    onClick: () => set('anonima', false)
  }, /*#__PURE__*/React.createElement("strong", null, "Sim, pode saber que fui eu"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "Facilita conversar sobre o assunto e dar retorno direto.")), /*#__PURE__*/React.createElement("button", {
    className: "canal-botao" + (f.anonima ? ' on' : ''),
    onClick: () => set('anonima', true)
  }, /*#__PURE__*/React.createElement("strong", null, "Prefiro não me identificar"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "Seu nome não é gravado em lugar nenhum. O acompanhamento é pelo protocolo."))), f.anonima && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "O que o sistema não guarda: seu nome, sua matrícula, o vínculo com o seu cadastro e os nomes que você citaria. O que fica: o texto, o setor, a data e os arquivos que anexar — cuide para que eles não identifiquem você sem querer (uma foto da sua bancada, por exemplo)."), !f.anonima && /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Matrícula"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.matricula,
    onChange: e => set('matricula', e.target.value)
  }))), /*#__PURE__*/React.createElement("label", {
    className: "check",
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.querRetorno,
    onChange: e => set('querRetorno', e.target.checked)
  }), /*#__PURE__*/React.createElement("span", null, "Quero receber retorno sobre o que foi feito")), prioridade === 'critica' && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--danger)',
      background: 'rgba(176,58,46,.06)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 700
    }
  }, "Pelo que você escreveu, isto parece urgente."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Se houver risco agora — alguém em perigo, fogo, alguém passando mal — procure imediatamente um responsável no local. Este canal não substitui o socorro imediato.")), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => {
      if (String(f.descricao || '').trim() && !confirm('Você escreveu algo que ainda não foi enviado. Sair mesmo assim?')) return;
      onCancelar();
    }
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !pronto,
    onClick: () => {
      let erro = null,
        prot = '';
      update(d => {
        const r = registrarManifestacao(d, {
          ...f,
          colaboradorId: !f.anonima && eu ? eu.id : '',
          via: 'sistema'
        }, usuario);
        if (r.erro) erro = r.erro;else prot = r.manifestacao.protocolo;
        return d;
      });
      if (erro) alert(erro);else onPronto(prot);
    }
  }, "Enviar")), !pronto && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      textAlign: 'right'
    }
  }, "escreva pelo menos uma frase para enviar"));
}

/* Uma pergunta por dia, cinco carinhas. Anônimo sempre. */
function TermometroDia({
  db,
  update,
  setorInicial
}) {
  const [nota, setNota] = useState(0);
  const [comentario, setComentario] = useState('');
  const [enviado, setEnviado] = useState(false);
  const carinhas = [{
    n: 1,
    face: '😟'
  }, {
    n: 2,
    face: '😕'
  }, {
    n: 3,
    face: '😐'
  }, {
    n: 4,
    face: '🙂'
  }, {
    n: 5,
    face: '😀'
  }];
  if (enviado) {
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--ok)',
        background: 'var(--ok-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        fontWeight: 600
      }
    }, "Obrigado! Sua resposta é anônima e ajuda a entender como anda a fábrica."));
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Como está o seu dia de trabalho?"), /*#__PURE__*/React.createElement("div", {
    className: "termometro"
  }, carinhas.map(c => /*#__PURE__*/React.createElement("button", {
    key: c.n,
    className: "carinha" + (nota === c.n ? ' on' : ''),
    onClick: () => setNota(c.n)
  }, /*#__PURE__*/React.createElement("span", null, c.face), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, c.n)))), nota > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Field, {
    label: "Tem alguma coisa que poderíamos melhorar? (opcional)"
  }, /*#__PURE__*/React.createElement("input", {
    value: comentario,
    placeholder: "pode escrever à vontade",
    onChange: e => setComentario(e.target.value)
  })), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => {
      update(d => {
        registrarPesquisaClima(d, {
          nota,
          comentario,
          departamentoId: setorInicial || ''
        });
        return d;
      });
      setEnviado(true);
    }
  }, "Enviar resposta")));
}

/* O grupo do módulo: canal, tratamento, indicadores e QR Codes. */
/* ---------------- Recados: as telas ---------------- */
function GrupoRecados({db, update, usuario, perm}) {
  const eu = (db.colaboradores || []).find(c => usuario && c.nome === usuario.nome);
  const [aberta, setAberta] = React.useState(null);
  const [novo, setNovo] = React.useState(null);      // 'pessoa' | 'grupo'
  const [painel, setPainel] = React.useState('conversas');
  const servidor = chatFuncionaDeVerdade();

  if (!eu) {
    return React.createElement("div", { className: "panel", style: { borderColor: 'var(--warn)', background: 'var(--warn-bg)' } },
      React.createElement("div", { className: "small", style: { fontWeight: 600 } },
        "Recados precisam de um cadastro de colaborador."),
      React.createElement("div", { className: "small muted" },
        "Seu usuário não está ligado a nenhuma pessoa no cadastro, então não há " +
        "de quem nem para quem mandar. Peça ao administrador para vincular.")
    );
  }

  const lista = conversasDe(db, eu.id);
  const conversa = aberta ? (db.conversas || []).find(c => c.id === aberta) : null;

  return React.createElement("div", null,
    React.createElement("div", { className: "page-head" },
      React.createElement("div", null,
        React.createElement("div", { className: "eyebrow" }, "Comunicação interna"),
        React.createElement("h2", null, "Chat"))),

    React.createElement(SubTabs, {
      active: painel, onChange: setPainel, tabs: [
        { id: 'conversas', label: `Conversas${naoLidasDe(db, eu.id) ? ' (' + naoLidasDe(db, eu.id) + ')' : ''}` },
        { id: 'notas', label: `Minhas notas (${notasDe(db, eu.id).length})` },
      ]
    }),

    painel === 'notas' && React.createElement(BlocoDeNotas, { db, update, eu }),

    painel === 'conversas' && React.createElement(React.Fragment, null,

    React.createElement("div", { className: "row-actions", style: { marginBottom: 10 } },
      React.createElement("button", { className: "btn ghost sm", onClick: () => setNovo('pessoa') },
        "+ Conversa"),
      React.createElement("button", { className: "btn ghost sm", onClick: () => setNovo('grupo') },
        "+ Grupo"),
      React.createElement("span", { style: { flex: 1 } }),
      React.createElement("span", { className: "small muted" },
        `${lista.length} conversa(s)`)),

    React.createElement("div", { className: "recados-tela" },
      React.createElement("div", { className: "recados-lista" },
        lista.length === 0
          ? React.createElement("div", { className: "small muted", style: { padding: 14 } },
              "Nenhuma conversa ainda. Use “+ Conversa” para falar com alguém.")
          : lista.map(c => React.createElement("button", {
              key: c.id,
              className: "recado-item" + (aberta === c.id ? " on" : ""),
              onClick: () => {
                setAberta(c.id);
                update(d => { marcarLidas(d, c.id, eu.id); return d; });
              }
            },
            React.createElement("div", { className: "recado-topo" },
              React.createElement("strong", null, nomeConversa(db, c, eu.id)),
              c.naoLidas > 0 && React.createElement("span", { className: "bolha" }, c.naoLidas)),
            React.createElement("div", { className: "small muted recado-previa" },
              c.grupo ? `${(c.participantes || []).length} pessoas · ` : '',
              c.ultima
                ? (c.ultima.texto ||
                   `${(c.ultima.anexos||[]).length} arquivo(s)`)
                : 'sem mensagens'),
            c.ultima && React.createElement("div", { className: "recado-hora" },
              fmtDataHora(c.ultima.quando))))),

      React.createElement("div", { className: "recados-conversa" },
        conversa
          ? React.createElement(Conversa, { conversa, db, update, eu })
          : React.createElement("div", { className: "small muted", style: { padding: 20, textAlign: 'center' } },
              "Escolha uma conversa ao lado."))),

    ),

    /* Os avisos ficam no pé: precisam existir, não precisam dominar a tela.
       E são sobre o chat — na aba de notas não fazem sentido. */
    painel === 'conversas' && React.createElement("div", { className: "chat-avisos" },
      !servidor && React.createElement("div", { className: "chat-aviso grave" },
        React.createElement("span", { className: "ic" }, "⚠"),
        React.createElement("span", null,
          React.createElement("strong", null, "As mensagens não saem deste computador. "),
          "O sistema está aberto como arquivo local, então cada navegador tem o " +
          "seu próprio banco. Para o chat funcionar entre aparelhos, ele precisa " +
          "estar publicado num endereço com banco compartilhado.")),
      React.createElement("div", { className: "chat-aviso" },
        React.createElement("span", { className: "ic" }, "🔓"),
        React.createElement("span", null,
          "Conversa aqui não é conversa privada: quem exporta o backup lê tudo. " +
          "Para assunto delicado, use o canal ",
          React.createElement("strong", null, "Conversa aberta"),
          " — lá o sigilo é tratado como regra."))),

    novo === 'pessoa' && painel === 'conversas' && React.createElement(NovaConversaModal, {
      db, eu, update, onClose: () => setNovo(null),
      onAberta: id => { setNovo(null); setAberta(id); }
    }),
    novo === 'grupo' && painel === 'conversas' && React.createElement(NovoGrupoModal, {
      db, eu, update, usuario, onClose: () => setNovo(null),
      onCriado: id => { setNovo(null); setAberta(id); }
    })
  );
}

function Conversa({ conversa, db, update, eu }) {
  const [texto, setTexto] = React.useState('');
  const [anexos, setAnexos] = React.useState([]);
  const [verGente, setVerGente] = React.useState(false);
  const msgs = mensagensDa(db, conversa.id);
  const gente = participantesDa(db, conversa);

  const enviar = () => {
    if (!texto.trim() && anexos.length === 0) return;
    let erro = null;
    update(d => {
      const r = enviarMensagem(d, conversa.id, eu.id, texto, null, anexos);
      erro = r.erro || null;
      return d;
    });
    if (erro) alert(erro); else { setTexto(''); setAnexos([]); }
  };

  const anexar = ev => {
    const file = ev.target.files && ev.target.files[0];
    ev.target.value = '';
    if (!file) return;
    if (file.size > LIMITE_ARQUIVO_CHAT)
      return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa ` +
        `do limite de ${Math.round(LIMITE_ARQUIVO_CHAT / 1024)} KB. ` +
        `Sem servidor, arquivo grande deixa o sistema lento para todo mundo.`);
    const leitor = new FileReader();
    leitor.onload = () => setAnexos(a => [...a, {
      id: uid(), nome: file.name, tipo: file.type,
      tamanho: file.size, url: leitor.result
    }]);
    leitor.readAsDataURL(file);
  };

  /* iniciais para o avatar — dois caracteres bastam para reconhecer */
  const iniciais = txt => String(txt || '?').trim().split(/\s+/)
    .slice(0, 2).map(p => p[0]).join('').toUpperCase();
  const titulo = nomeConversa(db, conversa, eu.id);

  return React.createElement("div", { className: "conversa-coluna" },
    React.createElement("div", { className: "conversa-topo" },
      React.createElement("div", { className: "conversa-avatar" }, iniciais(titulo)),
      React.createElement("div", { style: { flex: 1, minWidth: 0 } },
        React.createElement("strong", null, titulo),
        React.createElement("div", { className: "small muted" },
          conversa.grupo
            ? gente.map(g => g.nome).join(', ')
            : (() => {
                const outro = gente.find(g => g.id !== eu.id);
                const dp = outro && (db.departamentos || []).find(x => x.id === outro.departamentoId);
                return dp ? dp.nome : '';
              })())),
      conversa.grupo && React.createElement("button", {
        className: "btn ghost sm", onClick: () => setVerGente(true)
      }, "Participantes")),

    React.createElement("div", { className: "conversa-corpo" },
      msgs.length === 0
        ? React.createElement("div", { className: "small muted vazio" },
            "Nenhuma mensagem ainda. Escreva a primeira.")
        : msgs.map((m, i) => {
            const meu = m.autorId === eu.id;
            const autor = (db.colaboradores || []).find(c => c.id === m.autorId);
            const antes = msgs[i - 1];
            /* mensagens seguidas da mesma pessoa se agrupam; o nome só
               aparece na primeira do bloco */
            const seguida = antes && antes.autorId === m.autorId &&
              String(antes.quando || '').slice(0, 10) === String(m.quando || '').slice(0, 10);
            const diaNovo = !antes ||
              String(antes.quando || '').slice(0, 10) !== String(m.quando || '').slice(0, 10);
            const lida = meu && (m.lidaPor || []).length > 1;

            return React.createElement(React.Fragment, { key: m.id },
              diaNovo && React.createElement("div", { className: "dia-marca" },
                (() => {
                  const dia = String(m.quando || '').slice(0, 10);
                  if (dia === todayISO()) return 'HOJE';
                  const ontem = new Date(); ontem.setDate(ontem.getDate() - 1);
                  if (dia === ontem.toISOString().slice(0, 10)) return 'ONTEM';
                  return fmtDate(dia);
                })()),
              React.createElement("div", {
                className: "msg" + (meu ? " minha" : "") + (seguida ? " seguida" : "")
              },
                !meu && conversa.grupo && !seguida &&
                  React.createElement("div", { className: "msg-autor" },
                    autor ? autor.nome : 'removido'),
                m.texto && React.createElement("div", { className: "msg-texto" }, m.texto),
                (m.anexos || []).length > 0 && React.createElement("div", { className: "msg-anexos" },
                  (m.anexos || []).map(x =>
                    React.createElement("a", {
                      key: x.id, href: x.url, download: x.nome, className: "msg-anexo"
                    },
                      (x.tipo || '').startsWith('image/')
                        ? React.createElement("img", { src: x.url, alt: x.nome })
                        : React.createElement("span", { className: "anexo-ic" }, "📄"),
                      React.createElement("span", { className: "small" }, x.nome,
                        React.createElement("span", { className: "muted" },
                          ` · ${Math.round(num(x.tamanho) / 1024)} KB`))))),
                React.createElement("div", { className: "msg-hora" },
                  fmtHora(m.quando),
                  lida && React.createElement("span", { className: "lida" }, " ✓✓"))));
          })),

    React.createElement("div", { className: "conversa-rodape" },
      anexos.length > 0 && React.createElement("div", { className: "anexos-fila" },
        anexos.map(x => React.createElement("span", { key: x.id, className: "anexo-chip" },
          (x.tipo || '').startsWith('image/')
            ? React.createElement("img", { src: x.url, alt: x.nome })
            : React.createElement("span", { className: "anexo-ic" }, "📄"),
          React.createElement("span", { className: "small" }, x.nome),
          React.createElement("button", {
            className: "btn-ic ic-danger",
            onClick: () => setAnexos(a => a.filter(y => y.id !== x.id))
          }, "×")))),

      React.createElement("div", { className: "conversa-escrever" },
        React.createElement("label", { className: "btn-redondo", title: "Anexar arquivo" },
          "📎",
          React.createElement("input", {
            type: "file", style: { display: 'none' }, onChange: anexar
          })),
        React.createElement("textarea", {
          rows: 1, value: texto, placeholder: "Mensagem",
          maxLength: LIMITE_MENSAGEM,
          onChange: e => setTexto(e.target.value),
          onKeyDown: e => {
            if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); enviar(); }
          }
        }),
        React.createElement("button", {
          className: "btn-redondo enviar", title: "Enviar",
          disabled: !texto.trim() && anexos.length === 0, onClick: enviar
        }, "➤")),
      React.createElement("div", { className: "dica-envio" },
        `Enter envia · Shift+Enter quebra a linha · arquivo até ` +
        `${Math.round(LIMITE_ARQUIVO_CHAT / 1024)} KB`)),

    verGente && React.createElement(ParticipantesModal, {
      conversa, db, update, eu, onClose: () => setVerGente(false)
    })
  );
}

function NovaConversaModal({ db, eu, update, onClose, onAberta }) {
  const [busca, setBusca] = React.useState('');
  const pessoas = pessoasDisponiveis(db, eu.id).filter(p =>
    !busca.trim() || textoSimples(p.nome).includes(textoSimples(busca)));

  return React.createElement(Modal, { title: "Falar com quem?", onClose },
    React.createElement(Field, { label: "Procurar" },
      React.createElement("input", {
        value: busca, autoFocus: true, placeholder: "nome da pessoa",
        onChange: e => setBusca(e.target.value)
      })),
    pessoas.length === 0
      ? React.createElement("div", { className: "small muted" }, "Ninguém encontrado.")
      : React.createElement("div", { className: "gente-lista" },
          pessoas.map(p => {
            const dp = (db.departamentos || []).find(x => x.id === p.departamentoId);
            return React.createElement("button", {
              key: p.id, className: "gente-item",
              onClick: () => {
                let id = null, erro = null;
                update(d => {
                  const r = abrirConversa(d, eu.id, p.id, null);
                  erro = r.erro || null;
                  id = r.conversa ? r.conversa.id : null;
                  return d;
                });
                if (erro) alert(erro); else onAberta(id);
              }
            },
              React.createElement("strong", null, p.nome),
              React.createElement("span", { className: "small muted" },
                dp ? dp.nome : 'sem setor'));
          })),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Fechar"))
  );
}

function NovoGrupoModal({ db, eu, update, usuario, onClose, onCriado }) {
  const [nome, setNome] = React.useState('');
  const [descricao, setDescricao] = React.useState('');
  const [membros, setMembros] = React.useState([eu.id]);
  const pessoas = pessoasDisponiveis(db, eu.id);

  const alternar = id => setMembros(m =>
    m.includes(id) ? m.filter(x => x !== id) : [...m, id]);

  return React.createElement(Modal, { title: "Novo grupo", onClose, wide: true },
    React.createElement(Field, { label: "Nome do grupo *" },
      React.createElement("input", {
        value: nome, autoFocus: true, placeholder: "ex: Turno da manhã",
        onChange: e => setNome(e.target.value)
      })),
    React.createElement(Field, { label: "Para que serve" },
      React.createElement("input", {
        value: descricao, placeholder: "avisos do turno, combinações do setor…",
        onChange: e => setDescricao(e.target.value)
      })),
    React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
      `Quem participa (${membros.length})`),
    React.createElement("div", { className: "gente-grade" },
      pessoas.map(p => {
        const dp = (db.departamentos || []).find(x => x.id === p.departamentoId);
        return React.createElement("label", {
          key: p.id, className: "gente-check" + (membros.includes(p.id) ? " on" : "")
        },
          React.createElement("input", {
            type: "checkbox", checked: membros.includes(p.id),
            onChange: () => alternar(p.id)
          }),
          React.createElement("span", null,
            React.createElement("strong", null, p.nome),
            React.createElement("span", { className: "small muted" },
              dp ? dp.nome : 'sem setor')));
      })),
    React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
      "Você entra no grupo automaticamente. Quem for adicionado vê as mensagens " +
      "a partir de agora, não as anteriores."),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        disabled: !nome.trim() || membros.length < 2,
        onClick: () => {
          let id = null, erro = null;
          update(d => {
            const r = criarGrupo(d, {
              nome, descricao, participantes: membros, criadorId: eu.id
            }, usuario);
            erro = r.erro || null;
            id = r.conversa ? r.conversa.id : null;
            return d;
          });
          if (erro) alert(erro); else onCriado(id);
        }
      }, "Criar grupo"))
  );
}

function ParticipantesModal({ conversa, db, update, eu, onClose }) {
  const [membros, setMembros] = React.useState(conversa.participantes || []);
  const pessoas = pessoasDisponiveis(db, eu.id);
  const alternar = id => setMembros(m =>
    m.includes(id) ? m.filter(x => x !== id) : [...m, id]);

  return React.createElement(Modal, { title: `Participantes de ${conversa.nome}`, onClose, wide: true },
    React.createElement("div", { className: "gente-grade" },
      [eu, ...pessoas].map(p => {
        const dp = (db.departamentos || []).find(x => x.id === p.departamentoId);
        const souEu = p.id === eu.id;
        return React.createElement("label", {
          key: p.id,
          className: "gente-check" + (membros.includes(p.id) ? " on" : "")
        },
          React.createElement("input", {
            type: "checkbox", checked: membros.includes(p.id), disabled: souEu,
            onChange: () => alternar(p.id)
          }),
          React.createElement("span", null,
            React.createElement("strong", null, p.nome + (souEu ? ' (você)' : '')),
            React.createElement("span", { className: "small muted" },
              dp ? dp.nome : 'sem setor')));
      })),
    React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
      "Quem sair para de receber as próximas mensagens, mas o que já foi dito " +
      "continua no histórico de quem ficou."),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        onClick: () => {
          let erro = null;
          update(d => {
            const r = mudarParticipantes(d, conversa.id, membros, null);
            erro = r.erro || null;
            return d;
          });
          if (erro) alert(erro); else onClose();
        }
      }, "Salvar"))
  );
}

/* O bloco de notas: anotação de trabalho, só de quem escreveu. */
function BlocoDeNotas({ db, update, eu }) {
  const [editando, setEditando] = React.useState(null);
  const [busca, setBusca] = React.useState('');
  const todas = notasDe(db, eu.id);
  const lista = busca.trim()
    ? todas.filter(n => textoSimples(n.titulo + ' ' + n.texto)
        .includes(textoSimples(busca)))
    : todas;

  return React.createElement("div", null,
    React.createElement("div", { className: "panel", style: { borderColor: 'var(--thread)' } },
      React.createElement("div", { className: "small muted" },
        "Suas anotações de trabalho — o que lembrar, o que combinar, o rascunho ",
        "de uma conversa. Só aparecem para você no sistema. ",
        React.createElement("strong", null, "Mas quem exporta o backup lê tudo"),
        ", então não guarde aqui o que não pode ser lido por outra pessoa.")),

    React.createElement("div", { className: "row-actions", style: { marginBottom: 12 } },
      React.createElement("button", {
        className: "btn accent sm",
        onClick: () => setEditando({ titulo: '', texto: '', fixada: false })
      }, "+ Nota"),
      React.createElement("input", {
        value: busca, placeholder: "procurar nas notas", style: { flex: 1, maxWidth: 280 },
        onChange: e => setBusca(e.target.value)
      }),
      React.createElement("span", { style: { flex: 1 } }),
      React.createElement("span", { className: "small muted" },
        `${todas.length} nota(s)`)),

    lista.length === 0
      ? React.createElement(Empty, {
          text: busca.trim() ? "Nenhuma nota com esse texto."
            : "Nenhuma nota ainda. Use “+ Nota” para começar."
        })
      : React.createElement("div", { className: "notas-grade" },
          lista.map(n => React.createElement("div", {
            key: n.id, className: "nota-card" + (n.fixada ? " fixada" : "")
          },
            React.createElement("div", { className: "nota-topo" },
              React.createElement("strong", { style: { flex: 1 } },
                n.titulo || 'sem título'),
              React.createElement("button", {
                className: "btn-ic", title: n.fixada ? "Soltar" : "Fixar no topo",
                onClick: () => update(d => {
                  const alvo = (d.notas || []).find(x => x.id === n.id);
                  if (alvo) { alvo.fixada = !alvo.fixada; alvo.alteradaEm = instanteISO(); }
                  return d;
                })
              }, n.fixada ? "📌" : "📍")),
            React.createElement("div", { className: "nota-texto" }, n.texto),
            React.createElement("div", { className: "nota-rodape" },
              React.createElement("span", { className: "small muted" },
                fmtDataHora(n.alteradaEm)),
              React.createElement("span", { style: { flex: 1 } }),
              React.createElement("button", {
                className: "btn ghost sm", onClick: () => setEditando({ ...n })
              }, "Abrir"),
              React.createElement("button", {
                className: "btn-ic ic-danger",
                onClick: () => {
                  if (!confirm(`Apagar "${n.titulo || 'esta nota'}"? Não dá para desfazer.`)) return;
                  update(d => { removerNota(d, n.id, eu.id); return d; });
                }
              }, "×"))))),

    editando && React.createElement(NotaModal, {
      nota: editando, eu, update, onClose: () => setEditando(null)
    })
  );
}

function NotaModal({ nota, eu, update, onClose }) {
  const [f, setF] = React.useState({ ...nota });
  const set = (k, v) => setF(p => ({ ...p, [k]: v }));
  const restam = LIMITE_NOTA - String(f.texto || '').length;

  return React.createElement(Modal, {
    title: nota.id ? "Editar nota" : "Nova nota", onClose, wide: true
  },
    React.createElement(Field, { label: "Título" },
      React.createElement("input", {
        value: f.titulo || '', autoFocus: true,
        placeholder: "deixe em branco para usar a primeira linha",
        onChange: e => set('titulo', e.target.value)
      })),
    React.createElement(Field, { label: "Anotação" },
      React.createElement("textarea", {
        rows: 12, value: f.texto || '', maxLength: LIMITE_NOTA,
        placeholder: "escreva à vontade",
        onChange: e => set('texto', e.target.value)
      })),
    React.createElement("div", { className: "row-actions" },
      React.createElement("label", { className: "check" },
        React.createElement("input", {
          type: "checkbox", checked: !!f.fixada,
          onChange: e => set('fixada', e.target.checked)
        }),
        React.createElement("span", null, "Fixar no topo")),
      React.createElement("span", { style: { flex: 1 } }),
      restam < 2000 && React.createElement("span", { className: "small muted" },
        `${restam} caracteres restantes`)),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        disabled: !String(f.texto || '').trim() && !String(f.titulo || '').trim(),
        onClick: () => {
          let erro = null;
          update(d => {
            const r = salvarNota(d, { ...f, colaboradorId: eu.id }, null);
            erro = r.erro || null;
            return d;
          });
          if (erro) alert(erro); else onClose();
        }
      }, "Salvar"))
  );
}

/* O custo da fábrica: o que existe mesmo quando ninguém está costurando. */
/* O balanceamento: onde a linha trava e o que fazer a respeito. */
/* Balanceamento da carteira: várias ordens disputando a mesma linha. */
/* Barra de filtros: mesmo desenho nas telas de análise, para quem
   aprendeu numa não precisar reaprender na outra. */
function recursosDisponiveis(db, departamentoId){
  const ativos = c => c.status !== 'Inativo' && c.produtivo !== false;
  const maqOk = e => e.ativo !== false && e.situacao !== 'parada' &&
    e.situacao !== 'baixada';

  const pessoas = (db.colaboradores||[]).filter(c=>
    ativos(c) && (!departamentoId || c.departamentoId === departamentoId));
  const maquinas = (db.equipamentos||[]).filter(e=>
    maqOk(e) && (!departamentoId || e.departamentoId === departamentoId));

  const nomes = (lista, limite) => {
    if(lista.length === 0) return 'nenhum cadastrado';
    if(lista.length <= limite)
      return lista.map(x=>x.nome).join(', ');
    return lista.slice(0,limite).map(x=>x.nome).join(', ') +
      ` e mais ${lista.length-limite}`;
  };

  return {
    pessoas, maquinas,
    rotuloPessoas: nomes(pessoas, 3),
    rotuloMaquinas: nomes(maquinas, 3),
  };
}

/* Escolher os recursos pelo nome, não pela quantidade. "4 máquinas" não
   diz quais, e quais importa: quem opera o quê muda o que a linha
   entrega. */
/* O calendário de um recurso: o que a jornada oferece e o que já está
   tomado, dia a dia. */
function CalendarioRecurso({ db, tipo, id, nome, onClose, onEscolherDia,
  diaAtual }) {
  const [dias, setDias] = React.useState(14);
  const d = disponibilidadeDe(db, tipo, id, { dias });
  const j = jornadaDe(db);

  const DIAS_NOME = ['dom', 'seg', 'ter', 'qua', 'qui', 'sex', 'sáb'];

  const cabecalho = React.createElement("div", { className: "small muted",
    style: { marginBottom: 10 } },
    "Os horários vêm da jornada cadastrada em Engenharia → Jornada e mão de "
    + "obra: entrada " + j.inicio + ", saída " + j.fim
    + (j.intervalos && j.intervalos.length
        ? ", com " + j.intervalos.length + " intervalo(s)" : "")
    + ". O calendário mostra o que sobra depois do que já está reservado."
  );

  const impedido = !d.condicao.ok ? React.createElement("div", {
    className: "panel",
    style: { borderColor: 'var(--danger)', background: 'rgba(176,58,46,.06)' }
  },
    React.createElement("div", { className: "small", style: { fontWeight: 600 } },
      (tipo === 'pessoa' ? "Colaborador" : "Equipamento") + " " + d.condicao.motivo + "."),
    React.createElement("div", { className: "small muted" },
      "Enquanto estiver assim, nenhum dia aparece como livre.")
  ) : null;

  const faixa = React.createElement("div", { className: "faixa-dados" },
    React.createElement("div", { className: "linha-rotulos" },
      React.createElement("span", null, "Dias livres"),
      React.createElement("span", null, "Dias úteis"),
      React.createElement("span", null, "Horas livres"),
      React.createElement("span", null, "Por dia")
    ),
    React.createElement("div", { className: "linha-valores" },
      React.createElement("span", { className: d.totalLivres > 0 ? "destaque" : "alerta" },
        d.totalLivres,
        React.createElement("small", null, "sem compromisso")
      ),
      React.createElement("span", null, d.totalUteis,
        React.createElement("small", null, "no período")
      ),
      React.createElement("span", null, duracao(d.minutosLivres),
        React.createElement("small", null, "de produção")
      ),
      React.createElement("span", null, duracao(j.produtivo),
        React.createElement("small", null, "pela jornada")
      )
    )
  );

  const celula = (dia) => {
    /* só faz sentido escolher um dia em que dá para trabalhar */
    const pode = onEscolherDia && dia.trabalha && !dia.impedido;
    const classe = "cal-dia"
      + (dia.livre ? " livre" : "")
      + (dia.ocupado ? " ocupado" : "")
      + (!dia.trabalha ? " fechado" : "")
      + (pode ? " clicavel" : "")
      + (diaAtual === dia.data ? " escolhido" : "");
    return React.createElement("div", {
      key: dia.data, className: classe,
      onClick: pode ? () => onEscolherDia(dia.data) : undefined,
      title: pode ? "Programar para este dia" : undefined
    },
      React.createElement("div", { className: "cal-topo" },
        React.createElement("span", { className: "cal-num" },
          dia.data.slice(8, 10)),
        React.createElement("span", { className: "cal-sem" },
          DIAS_NOME[dia.semana])
      ),
      !dia.trabalha
        ? React.createElement("div", { className: "cal-vazio" }, dia.motivo)
        : React.createElement(React.Fragment, null,
            React.createElement("div", { className: "cal-hora" },
              dia.inicio + "–" + dia.fim),
            (dia.blocos || []).length > 1
              ? React.createElement("div", { className: "cal-blocos" },
                  dia.blocos.map((b, k) => React.createElement("span", { key: k },
                    b.inicio + "–" + b.fim)))
              : null,
            React.createElement("div", { className: "cal-min" },
              duracao(dia.minutos)),
            dia.compromissos.length > 0
              ? React.createElement("div", { className: "cal-comp" },
                  dia.compromissos.map((c, k) => React.createElement("div", { key: k },
                    React.createElement("strong", null, c.rotulo),
                    c.detalhe ? React.createElement("div", { className: "muted" },
                      c.detalhe) : null)))
              : React.createElement("div", { className: "cal-ok" },
                  diaAtual === dia.data ? "programado" : "livre")
          )
    );
  };

  return React.createElement(Modal, {
    title: (tipo === 'pessoa' ? "Agenda de " : "Agenda da ") + nome,
    onClose, wide: true
  },
    cabecalho,
    impedido,
    faixa,
    React.createElement("div", { className: "row-actions", style: { marginBottom: 10 } },
      [7, 14, 30].map(n => React.createElement("button", {
        key: n,
        className: "btn " + (dias === n ? "accent" : "ghost") + " sm",
        onClick: () => setDias(n)
      }, n + " dias"))
    ),
    onEscolherDia ? React.createElement("div", { className: "small muted",
      style: { marginBottom: 6 } },
      "Clique num dia para programar a produção nele. Dias já ocupados "
      + "podem ser escolhidos — o sistema avisa o conflito na hora de salvar."
    ) : null,
    React.createElement("div", { className: "cal-grade" }, d.dias.map(celula)),
    React.createElement("div", { className: "small muted", style: { marginTop: 10 } },
      "Feriado não entra nesta conta — a jornada não guarda calendário de "
      + "feriados, então um dia útil aqui pode não existir na prática."),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn accent", onClick: onClose },
        "Fechar"))
  );
}

function SeletorRecursos({ titulo, itens, escolhidos, onChange, vazio, rodape,
  db, tipoAgenda, onEscolherDia, diaAtual }) {
  const lista = escolhidos || [];
  const [agenda, setAgenda] = React.useState(null);
  const alternar = id => onChange(lista.includes(id)
    ? lista.filter(x => x !== id) : [...lista, id]);

  return React.createElement("div", { className: "sel-recursos" },
    React.createElement("div", { className: "sel-topo" },
      React.createElement("span", { className: "filtro-rot" },
        `${titulo} (${lista.length} de ${itens.length})`),
      itens.length > 0 && React.createElement("span", { className: "row-actions" },
        React.createElement("button", {
          className: "btn ghost sm", onClick: () => onChange(itens.map(i => i.id))
        }, "Todos"),
        lista.length > 0 && React.createElement("button", {
          className: "btn ghost sm", onClick: () => onChange([])
        }, "Limpar"))),

    itens.length === 0
      ? React.createElement("div", { className: "small muted" },
          vazio || "Nada cadastrado.")
      : React.createElement("div", { className: "sel-grade" },
          itens.map(i => React.createElement("label", {
            key: i.id, className: "sel-item" + (lista.includes(i.id) ? " on" : "") +
              (i.alerta ? " alerta" : "")
          },
            React.createElement("input", {
              type: "checkbox", checked: lista.includes(i.id),
              onChange: () => alternar(i.id)
            }),
            React.createElement("span", { className: "sel-nome" }, i.nome),
            i.detalhe && React.createElement("span", { className: "sel-detalhe" },
              i.detalhe),
            /* o nome abre a agenda: dias e horários que sobram */
            db && tipoAgenda && React.createElement("button", {
              className: "sel-agenda", title: "Ver dias e horários disponíveis",
              onClick: ev => { ev.preventDefault(); ev.stopPropagation();
                setAgenda({ id: i.id, nome: i.nome }); }
            }, "agenda")))),

    rodape && React.createElement("div", { className: "small muted",
      style: { marginTop: 6 } }, rodape),

    agenda && React.createElement(CalendarioRecurso, {
      db, tipo: tipoAgenda, id: agenda.id, nome: agenda.nome, diaAtual,
      onEscolherDia: onEscolherDia
        ? (data) => { onEscolherDia(data); setAgenda(null); } : null,
      onClose: () => setAgenda(null)
    }));
}

function BarraFiltros({ campos }) {
  return React.createElement("div", { className: "barra-filtros" },
    campos.filter(Boolean).map((c, i) =>
      React.createElement("div", { key: c.id || i, className: "filtro-campo" },
        React.createElement("label", { className: "filtro-rot" }, c.label),
        c.tipo === 'select'
          ? React.createElement("select", {
              value: c.valor, onChange: e => c.onChange(e.target.value)
            },
              (c.opcoes || []).map(o => React.createElement("option", {
                key: o.id, value: o.id
              }, o.nome)))
          : React.createElement("input", {
              type: c.tipo || 'text',
              min: c.min, max: c.max, value: c.valor,
              placeholder: c.placeholder || '',
              onChange: e => c.onChange(e.target.value)
            }),
        c.ajuda && React.createElement("span", { className: "filtro-ajuda" }, c.ajuda))));
}

/* A programação do dia: quem faz o quê, até a jornada encher. */
function ProgramacaoDoDia({ db, update, usuario }) {
  const j = jornadaDe(db);
  const [departamentoId, setDepartamentoId] = React.useState('');
  const [minutosDia, setMinutosDia] = React.useState(j.produtivo);
  const [extra, setExtra] = React.useState('0');
  const [pessoas, setPessoas] = React.useState([]);
  const [maquinas, setMaquinas] = React.useState([]);

  const disp = recursosDisponiveis(db, departamentoId);
  const abertas = (db.ordens || []).filter(o =>
    o.situacao !== 'cancelada' && o.situacao !== 'concluida');

  const p = programarDia(db, {
    ordemIds: abertas.map(o => o.id),
    colaboradorIds: pessoas, maquinaIds: maquinas,
    minutosDia, permitirExtra: num(extra), departamentoId
  });

  const alternar = (lista, set, id) => set(l =>
    l.includes(id) ? l.filter(x => x !== id) : [...l, id]);

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "Escolha as máquinas e as pessoas do dia. O sistema distribui as ",
      "operações pendentes entre elas, no ritmo de cada uma, até a jornada ",
      "encher — e diz o que fazer com o que sobrar ou com quem sobrar tempo."),

    React.createElement(BarraFiltros, { campos: [
      { id: 'dep', label: 'Departamento', tipo: 'select', valor: departamentoId,
        onChange: v => { setDepartamentoId(v); setPessoas([]); setMaquinas([]); },
        opcoes: [{ id: '', nome: 'Todos' }].concat(
          (db.departamentos || []).filter(d => d.ativo !== false)
            .map(d => ({ id: d.id, nome: d.nome }))) },
      { id: 'min', label: 'Minutos por dia', tipo: 'number', min: '1',
        valor: minutosDia, onChange: v => setMinutosDia(num(v)) },
      { id: 'extra', label: 'Hora extra permitida', tipo: 'select',
        valor: extra, onChange: setExtra,
        opcoes: [{ id: '0', nome: 'Sem hora extra' },
          { id: '30', nome: '30 minutos' }, { id: '60', nome: '1 hora' },
          { id: '120', nome: '2 horas (limite legal)' }],
        ajuda: num(extra) > 0 ? 'hora extra custa mais que hora normal' : '' },
    ] }),

    React.createElement("div", { className: "panel no-print" },
      React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
        `Máquinas do dia (${maquinas.length} de ${disp.maquinas.length})`),
      disp.maquinas.length === 0
        ? React.createElement("div", { className: "small muted" },
            "Nenhuma máquina cadastrada neste setor.")
        : React.createElement("div", { className: "gente-grade" },
            disp.maquinas.map(m => React.createElement("label", {
              key: m.id, className: "gente-check" + (maquinas.includes(m.id) ? " on" : "")
            },
              React.createElement("input", {
                type: "checkbox", checked: maquinas.includes(m.id),
                onChange: () => alternar(maquinas, setMaquinas, m.id)
              }),
              React.createElement("span", null,
                React.createElement("strong", null, m.nome),
                React.createElement("span", { className: "small muted" },
                  m.codigo || ''))))),

      React.createElement("div", { className: "etp-rot",
        style: { marginTop: 14, marginBottom: 6 } },
        `Quem trabalha hoje (${pessoas.length} de ${disp.pessoas.length})`),
      disp.pessoas.length === 0
        ? React.createElement("div", { className: "small muted" },
            "Nenhum colaborador cadastrado neste setor.")
        : React.createElement("div", { className: "gente-grade" },
            disp.pessoas.map(c => React.createElement("label", {
              key: c.id, className: "gente-check" + (pessoas.includes(c.id) ? " on" : "")
            },
              React.createElement("input", {
                type: "checkbox", checked: pessoas.includes(c.id),
                onChange: () => alternar(pessoas, setPessoas, c.id)
              }),
              React.createElement("span", null,
                React.createElement("strong", null, c.nome),
                React.createElement("span", { className: "small muted" },
                  (db.departamentos || []).find(d => d.id === c.departamentoId)?.nome
                    || 'sem setor'))))),

      maquinas.length > 0 && pessoas.length > maquinas.length &&
        React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
          `${pessoas.length} pessoas para ${maquinas.length} máquina(s): `,
          `só ${maquinas.length} trabalham ao mesmo tempo.`)),

    p.vazio
      ? React.createElement(Empty, { text: p.motivo })
      : React.createElement(React.Fragment, null,
          React.createElement("div", { className: "faixa-dados" },
            React.createElement("div", { className: "linha-rotulos" },
              React.createElement("span", null, "Peças programadas"),
              React.createElement("span", null, "Continua amanhã"),
              React.createElement("span", null, "Hora extra"),
              React.createElement("span", null, "Tempo livre"),
              React.createElement("span", null, "Pessoas")),
            React.createElement("div", { className: "linha-valores" },
              React.createElement("span", { className: "destaque" }, p.pecasProgramadas,
                React.createElement("small", null, "para hoje")),
              React.createElement("span", {
                className: p.pecasSobrando > 0 ? "alerta" : ""
              }, p.pecasSobrando,
                React.createElement("small", null,
                  p.totalSobra > 0 ? duracao(p.totalSobra) : 'tudo coube')),
              React.createElement("span", null,
                p.totalExtra > 0 ? duracao(p.totalExtra) : '—',
                React.createElement("small", null,
                  p.totalExtra > 0 ? 'já contada acima' : 'não foi preciso')),
              React.createElement("span", null, duracao(p.folgaTotal),
                React.createElement("small", null, "somando as folgas")),
              React.createElement("span", null, p.agenda.length,
                React.createElement("small", null,
                  p.maquinas > 0 ? `${p.maquinas} máquina(s)` : 'sem máquina')))),

          React.createElement("div", { className: "panel", style: { padding: 0 } },
            React.createElement("table", null,
              React.createElement("thead", null,
                React.createElement("tr", null,
                  React.createElement("th", null, "Pessoa"),
                  React.createElement("th", null, "O que faz hoje"),
                  React.createElement("th", { className: "num" }, "Tempo"),
                  React.createElement("th", null, "Ocupação"))),
              React.createElement("tbody", null,
                p.agenda.map(a => React.createElement("tr", { key: a.id },
                  React.createElement("td", null,
                    React.createElement("strong", null, a.nome),
                    a.extraUsado > 0 && React.createElement("div", {
                      className: "small", style: { color: 'var(--warn)' }
                    }, `+${duracao(a.extraUsado)} extra`),
                    a.temFolga && React.createElement("div", { className: "small muted" },
                      `${duracao(a.livre)} livre(s)`)),
                  React.createElement("td", { className: "small" },
                    a.tarefas.length === 0
                      ? React.createElement("span", { className: "muted" },
                          "nada programado")
                      : a.tarefas.map((tf, i) => React.createElement("div", { key: i },
                          React.createElement("strong", null, `${tf.pecas}× `),
                          `${tf.etapa} · ${tf.ordem}`,
                          React.createElement("span", { className: "muted" },
                            ` (${tf.porHora}/h`,
                            tf.base !== 'historico' ? ', estimado' : '',
                            tf.emExtra > 0 ? `, ${duracao(tf.emExtra)} em extra` : '',
                            ')')))),
                  React.createElement("td", { className: "num" },
                    duracao(a.usado + a.extraUsado),
                    React.createElement("div", { className: "small muted" },
                      `de ${duracao(a.capacidade)}`)),
                  React.createElement("td", null,
                    React.createElement("span", { className: "trilho",
                      style: { display: 'block' } },
                      React.createElement("span", {
                        className: "preenche",
                        style: {
                          width: `${Math.min(100, a.ocupacao)}%`,
                          background: a.ocupacao >= 95 ? 'var(--ok)'
                            : a.ocupacao < 60 ? 'var(--danger)' : 'var(--thread)'
                        }
                      })),
                    React.createElement("span", { className: "small muted" },
                      `${a.ocupacao}%`)))))),

          /* ---- o veredicto do período ---- */
          (() => {
            const ind = indicadorDoPeriodo(db, p, {});
            if (!ind) return null;
            const cor = ind.situacao === 'dentro' ? 'var(--ok)'
              : ind.situacao === 'fora' ? 'var(--danger)' : 'var(--warn)';
            const fundo = ind.situacao === 'dentro' ? 'var(--ok-bg)'
              : ind.situacao === 'fora' ? 'rgba(176,58,46,.06)' : 'var(--warn-bg)';
            return React.createElement("div", { className: "panel",
              style: { borderColor: cor, background: fundo } },
              React.createElement("div", { className: "small",
                style: { fontWeight: 600, color: cor, marginBottom: 6 } },
                ind.situacao === 'dentro'
                  ? "As entregas cabem no ritmo desta equipe."
                  : ind.situacao === 'fora'
                    ? "Nenhuma entrega cabe no ritmo desta equipe."
                    : `${ind.apertadas.length} de ${ind.linhas.length} entrega(s) não cabem no ritmo desta equipe.`),
              React.createElement("table", null,
                React.createElement("thead", null,
                  React.createElement("tr", null,
                    React.createElement("th", null, "Entrega"),
                    React.createElement("th", { className: "num" }, "Dias úteis"),
                    React.createElement("th", { className: "num" }, "Precisa"),
                    React.createElement("th", { className: "num" }, "Capacidade"),
                    React.createElement("th", null, ""))),
                React.createElement("tbody", null,
                  ind.linhas.map(l => React.createElement("tr", { key: l.entrega },
                    React.createElement("td", null,
                      React.createElement("strong", null, fmtDate(l.entrega)),
                      React.createElement("div", { className: "small muted" },
                        l.ordens.join(', '), ` · ${l.pecas} peças`)),
                    React.createElement("td", { className: "num" }, l.uteis),
                    React.createElement("td", { className: "num" }, duracao(l.minutos)),
                    React.createElement("td", { className: "num" }, duracao(l.capacidade)),
                    React.createElement("td", { className: "small",
                      style: { color: l.cabe ? 'var(--ok)' : 'var(--danger)',
                        fontWeight: 600 } },
                      l.cabe ? `sobra ${duracao(l.folga)}`
                        : `faltam ${l.diasFaltando} dia(s)`))))),
              ind.semData > 0 && React.createElement("div", {
                className: "small muted", style: { marginTop: 6 }
              }, `${ind.semData} operação(ões) sem data de entrega ficaram fora `,
                "desta conta — sem prazo, não há como dizer se cabe."),
              React.createElement("div", { className: "small muted",
                style: { marginTop: 6 } },
                `A conta usa ${ind.pessoasDia} pessoa(s) × ${duracao(ind.capacidadeDia / Math.max(1, ind.pessoasDia))} `,
                "por dia útil, do dia de hoje até a entrega. Feriado e falta ",
                "não entram — se houver, sobra menos do que aparece aqui."));
          })(),

          /* ---- a carga das máquinas ---- */
          p.maquinas > 0 && (() => {
            const cargas = cargaDosEquipamentos(db, p);
            if (cargas.length === 0) return null;
            return React.createElement("div", { className: "panel", style: { padding: 0 } },
              React.createElement("table", null,
                React.createElement("thead", null,
                  React.createElement("tr", null,
                    React.createElement("th", null, "Máquina"),
                    React.createElement("th", { className: "num" }, "Uso do dia"),
                    React.createElement("th", { className: "num" }, "Peças"),
                    React.createElement("th", null, "Ocupação"))),
                React.createElement("tbody", null,
                  cargas.map(c => React.createElement("tr", { key: c.id },
                    React.createElement("td", null,
                      React.createElement("strong", null, c.nome),
                      c.codigo && React.createElement("div", { className: "small muted" },
                        c.codigo)),
                    React.createElement("td", { className: "num" },
                      duracao(c.minutos),
                      React.createElement("div", { className: "small muted" },
                        `de ${duracao(c.capacidade)}`)),
                    React.createElement("td", { className: "num" }, c.pecas),
                    React.createElement("td", null,
                      React.createElement("span", { className: "trilho",
                        style: { display: 'block' } },
                        React.createElement("span", {
                          className: "preenche",
                          style: {
                            width: `${Math.min(100, c.ocupacao)}%`,
                            background: c.ocupacao >= 95 ? 'var(--ok)'
                              : c.ocupacao < 60 ? 'var(--danger)' : 'var(--thread)'
                          }
                        })),
                      React.createElement("span", { className: "small muted" },
                        `${c.ocupacao}%`,
                        c.livre > 30 ? ` · ${duracao(c.livre)} livre` : '')))))),
              React.createElement("div", { className: "small muted",
                style: { padding: '8px 12px' } },
                "O uso é dividido entre as máquinas escolhidas — o sistema não ",
                "sabe qual pessoa ficou em qual máquina, só quantas estão em uso."));
          })(),

          p.sugestoes.length > 0 && React.createElement("div", { className: "panel" },
            React.createElement("h3", { style: { marginTop: 0 } },
              p.pecasSobrando > 0 ? "O que fazer com o que não coube hoje"
                : "O que fazer com o tempo que sobrou"),
            p.sugestoes.map((sg, i) => React.createElement("div", {
              key: i, style: { marginBottom: 10 }
            },
              React.createElement("div", { className: "small",
                style: { fontWeight: 600 } }, sg.titulo),
              React.createElement("div", { className: "small muted" }, sg.detalhe)))),

          p.sobra.length > 0 && React.createElement("div", { className: "panel",
            style: { padding: 0 } },
            React.createElement("table", null,
              React.createElement("thead", null,
                React.createElement("tr", null,
                  React.createElement("th", null, "Continua amanhã"),
                  React.createElement("th", { className: "num" }, "Peças"),
                  React.createElement("th", { className: "num" }, "Tempo"),
                  React.createElement("th", null, "Entrega"))),
              React.createElement("tbody", null,
                p.sobra.map((x, i) => React.createElement("tr", { key: i },
                  React.createElement("td", null,
                    React.createElement("strong", null, x.etapa),
                    React.createElement("div", { className: "small muted" },
                      `${x.ordemCodigo} · ${x.produtoNome}`)),
                  React.createElement("td", { className: "num" }, x.quantidade),
                  React.createElement("td", { className: "num" }, duracao(x.minutos)),
                  React.createElement("td", { className: "small" },
                    x.entrega ? fmtDate(x.entrega) : '—')))))))));
}

/* O dia de cada pessoa, a carga das máquinas e o confronto com as
   entregas. Antes vivia numa tela separada; a carteira já sabe quem
   faz cada operação, então é aqui que a informação pertence. */
function ResumoDoDia({ db, balanceamento, escalas, minutosDia, extra, maquinas }) {
  /* as máquinas em uso são as escolhidas em cada operação, mais as do
     filtro geral — sem juntar as duas, a carga dos equipamentos ficava
     vazia sempre que o supervisor escalava direto na operação */
  const usadas = [...new Set([
    ...(maquinas || []),
    ...Object.values(escalas || {}).flatMap(e => (e && e.maquinas) || [])
  ])];

  const ag = agendaDaCarteira(db, balanceamento, escalas,
    { minutosDia, permitirExtra: num(extra), maquinaIds: usadas });
  if (ag.vazio) return null;

  const ind = indicadorDoPeriodo(db, ag, {});
  const cargas = usadas.length > 0 ? cargaDosEquipamentos(db, ag) : [];
  const cor = !ind ? null
    : ind.situacao === 'dentro' ? 'var(--ok)'
    : ind.situacao === 'fora' ? 'var(--danger)' : 'var(--warn)';

  const barra = (pct) => React.createElement("span", {
    className: "trilho", style: { display: 'block' }
  }, React.createElement("span", {
    className: "preenche",
    style: {
      width: Math.min(100, pct) + '%',
      background: pct >= 95 ? 'var(--ok)'
        : pct < 60 ? 'var(--danger)' : 'var(--thread)'
    }
  }));

  const faixa = React.createElement("div", {
    className: "faixa-dados", style: { marginTop: 14 }
  },
    React.createElement("div", { className: "linha-rotulos" },
      React.createElement("span", null, "Peças no dia"),
      React.createElement("span", null, "Continua amanhã"),
      React.createElement("span", null, "Hora extra"),
      React.createElement("span", null, "Tempo livre"),
      React.createElement("span", null, "Pessoas")
    ),
    React.createElement("div", { className: "linha-valores" },
      React.createElement("span", { className: "destaque" },
        ag.pecasProgramadas,
        React.createElement("small", null, "com esta equipe")
      ),
      React.createElement("span", { className: ag.pecasSobrando > 0 ? "alerta" : "" },
        ag.pecasSobrando,
        React.createElement("small", null,
          ag.totalSobra > 0 ? duracao(ag.totalSobra) : 'tudo coube')
      ),
      React.createElement("span", null,
        ag.totalExtra > 0 ? duracao(ag.totalExtra) : '—',
        React.createElement("small", null,
          ag.totalExtra > 0 ? 'já contada' : 'não foi preciso')
      ),
      React.createElement("span", null,
        duracao(ag.folgaTotal),
        React.createElement("small", null, "somando as folgas")
      ),
      React.createElement("span", null,
        ag.agenda.length,
        React.createElement("small", null,
          ag.maquinas > 0 ? ag.maquinas + " máquina(s)" : 'sem máquina')
      )
    )
  );

  const linhaPessoa = (a) => React.createElement("tr", { key: a.id },
    React.createElement("td", null,
      React.createElement("strong", null, a.nome),
      a.extraUsado > 0 ? React.createElement("div", {
        className: "small", style: { color: 'var(--warn)' }
      }, "+" + duracao(a.extraUsado) + " extra") : null,
      a.temFolga ? React.createElement("div", { className: "small muted" },
        duracao(a.livre) + " livre(s)") : null
    ),
    React.createElement("td", { className: "small" },
      a.tarefas.map((tf, k) => React.createElement("div", { key: k },
        React.createElement("strong", null, tf.pecas + "× "),
        tf.etapa + " · " + tf.ordem,
        React.createElement("span", { className: "muted" },
          " (" + tf.porHora + "/h"
            + (tf.base !== 'historico' ? ', estimado' : '')
            + (tf.emExtra > 0 ? ', ' + duracao(tf.emExtra) + ' em extra' : '')
            + ")")
      ))
    ),
    React.createElement("td", { className: "num" },
      duracao(a.usado + a.extraUsado),
      React.createElement("div", { className: "small muted" },
        "de " + duracao(a.capacidade))
    ),
    React.createElement("td", null,
      barra(a.ocupacao),
      React.createElement("span", { className: "small muted" }, a.ocupacao + "%")
    )
  );

  const tabelaPessoas = React.createElement("div", {
    className: "panel", style: { padding: 0 }
  },
    React.createElement("table", null,
      React.createElement("thead", null,
        React.createElement("tr", null,
          React.createElement("th", null, "Pessoa"),
          React.createElement("th", null, "O que faz hoje"),
          React.createElement("th", { className: "num" }, "Tempo"),
          React.createElement("th", null, "Ocupação")
        )
      ),
      React.createElement("tbody", null, ag.agenda.map(linhaPessoa))
    )
  );

  const linhaMaquina = (c) => React.createElement("tr", { key: c.id },
    React.createElement("td", null,
      React.createElement("strong", null, c.nome),
      c.codigo ? React.createElement("div", { className: "small muted" }, c.codigo) : null
    ),
    React.createElement("td", { className: "num" },
      duracao(c.minutos),
      React.createElement("div", { className: "small muted" },
        "de " + duracao(c.capacidade))
    ),
    React.createElement("td", { className: "num" }, c.pecas),
    React.createElement("td", null,
      barra(c.ocupacao),
      React.createElement("span", { className: "small muted" },
        c.ocupacao + "%" + (c.livre > 30 ? ' · ' + duracao(c.livre) + ' livre' : ''))
    )
  );

  const tabelaMaquinas = cargas.length === 0 ? null
    : React.createElement("div", { className: "panel", style: { padding: 0 } },
        React.createElement("table", null,
          React.createElement("thead", null,
            React.createElement("tr", null,
              React.createElement("th", null, "Máquina"),
              React.createElement("th", { className: "num" }, "Uso do dia"),
              React.createElement("th", { className: "num" }, "Peças"),
              React.createElement("th", null, "Ocupação")
            )
          ),
          React.createElement("tbody", null, cargas.map(linhaMaquina))
        ),
        React.createElement("div", { className: "small muted",
          style: { padding: '8px 12px' } },
          "O uso é dividido entre as máquinas escolhidas — o sistema não sabe "
          + "qual pessoa ficou em qual máquina, só quantas estão em uso.")
      );

  const veredicto = !ind ? null : (() => {
    const titulo = ind.situacao === 'dentro'
      ? "As entregas cabem no ritmo desta equipe."
      : ind.situacao === 'fora'
        ? "Nenhuma entrega cabe no ritmo desta equipe."
        : ind.apertadas.length + " de " + ind.linhas.length
          + " entrega(s) não cabem no ritmo desta equipe.";

    const linha = (l) => React.createElement("tr", { key: l.entrega },
      React.createElement("td", null,
        React.createElement("strong", null, fmtDate(l.entrega)),
        React.createElement("div", { className: "small muted" },
          l.ordens.join(', ') + " · " + l.pecas + " peças")
      ),
      React.createElement("td", { className: "num" }, l.uteis),
      React.createElement("td", { className: "num" }, duracao(l.minutos)),
      React.createElement("td", { className: "num" }, duracao(l.capacidade)),
      React.createElement("td", {
        className: "small",
        style: { color: l.cabe ? 'var(--ok)' : 'var(--danger)', fontWeight: 600 }
      }, l.cabe ? "sobra " + duracao(l.folga)
                : "faltam " + l.diasFaltando + " dia(s)")
    );

    return React.createElement("div", { className: "panel", style: { borderColor: cor } },
      React.createElement("div", {
        className: "small",
        style: { fontWeight: 600, color: cor, marginBottom: 6 }
      }, titulo),
      React.createElement("table", null,
        React.createElement("thead", null,
          React.createElement("tr", null,
            React.createElement("th", null, "Entrega"),
            React.createElement("th", { className: "num" }, "Dias úteis"),
            React.createElement("th", { className: "num" }, "Precisa"),
            React.createElement("th", { className: "num" }, "Capacidade"),
            React.createElement("th", null, "")
          )
        ),
        React.createElement("tbody", null, ind.linhas.map(linha))
      ),
      ind.semData > 0 ? React.createElement("div", {
        className: "small muted", style: { marginTop: 6 }
      }, ind.semData + " operação(ões) sem data de entrega ficaram fora desta "
        + "conta — sem prazo, não há como dizer se cabe.") : null,
      React.createElement("div", { className: "small muted", style: { marginTop: 6 } },
        "A conta usa os dias úteis do calendário, de hoje até a entrega. "
        + "Feriado e falta não entram — se houver, sobra menos.")
    );
  })();

  const sugestoes = ag.sugestoes.length === 0 ? null
    : React.createElement("div", { className: "panel" },
        React.createElement("h3", { style: { marginTop: 0 } },
          ag.pecasSobrando > 0 ? "O que fazer com o que não coube hoje"
                               : "O que fazer com o tempo que sobrou"),
        ag.sugestoes.map((sg, k) => React.createElement("div", {
          key: k, style: { marginBottom: 10 }
        },
          React.createElement("div", { className: "small", style: { fontWeight: 600 } },
            sg.titulo),
          React.createElement("div", { className: "small muted" }, sg.detalhe)
        ))
      );

  return React.createElement(React.Fragment, null,
    faixa, tabelaPessoas, tabelaMaquinas, veredicto, sugestoes);
}

/* Registrar o que interrompeu e mandar o trabalho para outro lugar. */
function TelaOcorrencias({ db, update, usuario, podeEditar }) {
  const [novo, setNovo] = React.useState(null);
  const [realocando, setRealocando] = React.useState(null);
  const abertas = ocorrenciasAbertas(db);
  const historico = (db.ocorrencias || [])
    .filter(o => o.situacao === 'encerrada')
    .slice(-10).reverse();

  const nome = (tipo, id) => {
    const l = tipo === 'maquina' ? (db.equipamentos || []) : (db.colaboradores || []);
    const x = l.find(y => y.id === id);
    return x ? x.nome : '—';
  };

  const cartao = (o) => {
    const t = TIPOS_OCORRENCIA.find(x => x.id === o.tipo) || {};
    const alvo = o.equipamentoId ? nome('maquina', o.equipamentoId)
      : o.colaboradorId ? nome('pessoa', o.colaboradorId) : '';
    const af = itensAfetados(db, o);
    return React.createElement("div", {
      key: o.id, className: "panel",
      style: { borderColor: o.situacao === 'aberta' ? 'var(--warn)' : 'var(--line)' }
    },
      React.createElement("div", { className: "row-actions" },
        React.createElement("div", { style: { flex: 1, minWidth: 0 } },
          React.createElement("strong", null, o.codigo + " · " + o.tipoNome),
          alvo ? React.createElement("span", { className: "muted" }, " — " + alvo) : null,
          React.createElement("div", { className: "small muted" },
            fmtDate(o.data) + " · " + o.descricao),
          o.minutosParado > 0 ? React.createElement("div", { className: "small muted" },
            duracao(o.minutosParado) + " de parada") : null,
          o.realocacoes && o.realocacoes.length
            ? React.createElement("div", { className: "small",
                style: { color: 'var(--ok)', marginTop: 4 } },
                "Realocado: " + o.realocacoes.join(' · '))
            : null
        ),
        podeEditar && o.situacao !== 'encerrada' && af.itens.length > 0
          ? React.createElement("button", {
              className: "btn accent sm",
              onClick: () => setRealocando(o)
            }, af.itens.length + " operação(ões) para realocar")
          : null,
        podeEditar && o.situacao !== 'encerrada'
          ? React.createElement("button", {
              className: "btn ghost sm",
              onClick: () => {
                let erro = null;
                update(d => {
                  const r = encerrarOcorrencia(d, o.id, usuario);
                  erro = r.erro || null;
                  return d;
                });
                if (erro) alert(erro);
              }
            }, "Encerrar")
          : null
      ),
      o.situacao !== 'encerrada' && af.itens.length === 0
        ? React.createElement("div", { className: "small muted", style: { marginTop: 6 } },
            "Nada programado neste recurso hoje — não há o que realocar.")
        : null
    );
  };

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "O que interrompeu a produção: máquina parada, gente que faltou, "
      + "material que não chegou. Quando o recurso tinha operação programada, "
      + "o sistema mostra para onde ela pode ir."),

    podeEditar ? React.createElement("div", { className: "row-actions",
      style: { marginBottom: 12 } },
      React.createElement("button", {
        className: "btn accent",
        onClick: () => setNovo({ tipo: '', data: todayISO(), descricao: '' })
      }, "Registrar ocorrência")
    ) : null,

    abertas.length === 0 && historico.length === 0
      ? React.createElement(Empty, { text: "Nenhuma ocorrência registrada." })
      : null,

    abertas.length > 0 ? React.createElement("div", null,
      React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
        "Em aberto (" + abertas.length + ")"),
      abertas.map(cartao)
    ) : null,

    historico.length > 0 ? React.createElement("div", { style: { marginTop: 14 } },
      React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
        "Encerradas"),
      historico.map(cartao)
    ) : null,

    novo ? React.createElement(OcorrenciaModal, {
      db, dados: novo, onChange: setNovo,
      onClose: () => setNovo(null),
      onSalvar: () => {
        let erro = null;
        update(d => {
          const r = registrarOcorrencia(d, novo, usuario);
          erro = r.erro || null;
          return d;
        });
        if (erro) alert(erro); else setNovo(null);
      }
    }) : null,

    realocando ? React.createElement(RealocarModal, {
      db, ocorrencia: realocando,
      onClose: () => setRealocando(null),
      onConfirmar: (trocas) => {
        let msg = null;
        update(d => {
          const r = realocar(d, realocando.id, trocas, usuario);
          msg = r.erro || null;
          return d;
        });
        if (msg) alert(msg); else setRealocando(null);
      }
    }) : null
  );
}

function OcorrenciaModal({ db, dados, onChange, onClose, onSalvar }) {
  const set = (k, v) => onChange({ ...dados, [k]: v });
  const t = TIPOS_OCORRENCIA.find(x => x.id === dados.tipo);

  return React.createElement(Modal, { title: "Registrar ocorrência", onClose },
    React.createElement(Field, { label: "O que aconteceu" },
      React.createElement("select", {
        value: dados.tipo, onChange: e => set('tipo', e.target.value)
      },
        React.createElement("option", { value: "" }, "escolha..."),
        TIPOS_OCORRENCIA.map(x => React.createElement("option",
          { key: x.id, value: x.id }, x.nome))
      ),
      t && t.ajuda ? React.createElement("div", { className: "small muted" },
        t.ajuda) : null
    ),

    React.createElement(Field, { label: "Dia" },
      React.createElement("input", {
        type: "date", value: dados.data,
        onChange: e => set('data', e.target.value)
      })
    ),

    t && t.alvo === 'maquina' ? React.createElement(Field, { label: "Qual máquina" },
      React.createElement("select", {
        value: dados.equipamentoId || '',
        onChange: e => set('equipamentoId', e.target.value)
      },
        React.createElement("option", { value: "" }, "escolha..."),
        (db.equipamentos || []).filter(e => e.ativo !== false)
          .map(e => React.createElement("option", { key: e.id, value: e.id },
            e.nome + (e.codigo ? " · " + e.codigo : "")))
      ),
      React.createElement("div", { className: "small muted" },
        "A máquina passa a constar em manutenção e sai do planejamento "
        + "até a ocorrência ser encerrada.")
    ) : null,

    t && t.alvo === 'pessoa' ? React.createElement(Field, { label: "Quem faltou" },
      React.createElement("select", {
        value: dados.colaboradorId || '',
        onChange: e => set('colaboradorId', e.target.value)
      },
        React.createElement("option", { value: "" }, "escolha..."),
        (db.colaboradores || []).filter(c => c.status !== 'Inativo')
          .map(c => React.createElement("option", { key: c.id, value: c.id }, c.nome))
      )
    ) : null,

    React.createElement(Field, { label: "O que houve" },
      React.createElement("textarea", {
        rows: "2", value: dados.descricao,
        placeholder: "uma frase basta",
        onChange: e => set('descricao', e.target.value)
      })
    ),

    React.createElement(Field, { label: "Minutos de parada (se souber)" },
      React.createElement("input", {
        type: "number", min: "0", value: dados.minutosParado || '',
        onChange: e => set('minutosParado', e.target.value)
      })
    ),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose },
        "Cancelar"),
      React.createElement("button", { className: "btn accent", onClick: onSalvar },
        "Registrar")
    )
  );
}

function RealocarModal({ db, ocorrencia, onClose, onConfirmar }) {
  const af = itensAfetados(db, ocorrencia);
  const [trocas, setTrocas] = React.useState({});
  const paraMaquina = !!ocorrencia.equipamentoId;

  const linha = (it) => {
    const alt = alternativasPara(db, ocorrencia, it);
    const escolhido = trocas[it.id] || '';
    return React.createElement("div", { key: it.id, className: "panel" },
      React.createElement("div", { className: "small", style: { fontWeight: 600 } },
        it.etapa + " · " + (it.ordemCodigo || '')),
      React.createElement("div", { className: "small muted", style: { marginBottom: 8 } },
        it.falta + " peça(s) ainda por fazer"
        + (it.feito > 0 ? " · " + it.feito + " já saíram" : "")),

      alt.length === 0
        ? React.createElement("div", { className: "small",
            style: { color: 'var(--danger)' } },
            paraMaquina
              ? "Nenhuma outra máquina disponível neste setor."
              : "Ninguém mais disponível neste setor.")
        : React.createElement("div", { className: "sel-grade" },
            alt.map(a => React.createElement("label", {
              key: a.id,
              className: "sel-item" + (escolhido === a.id ? " on" : "")
                + (a.alerta ? " alerta" : "")
            },
              React.createElement("input", {
                type: "radio", name: "tr-" + it.id,
                checked: escolhido === a.id,
                onChange: () => setTrocas(t => ({ ...t, [it.id]: a.id }))
              }),
              React.createElement("span", { className: "sel-nome" }, a.nome),
              React.createElement("span", { className: "sel-detalhe" },
                (a.porHora ? a.porHora + "/h" : "")
                + (a.livre ? "" : " · ocupado"))
            ))
          ),

      alt.filter(a => a.alerta).length > 0
        ? React.createElement("div", { className: "small muted", style: { marginTop: 6 } },
            alt.filter(a => a.alerta)
              .map(a => a.nome + ": " + a.alerta).join(' · '))
        : null
    );
  };

  return React.createElement(Modal, {
    title: "Realocar · " + ocorrencia.codigo, onClose, wide: true
  },
    React.createElement("div", { className: "small muted", style: { marginBottom: 10 } },
      paraMaquina
        ? "A máquina parou. Escolha para onde vai cada operação que estava nela."
        : "A pessoa não veio. Escolha quem assume cada operação."),

    af.itens.length === 0
      ? React.createElement(Empty, { text: "Nada programado neste recurso." })
      : af.itens.map(linha),

    React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
      "Trocar aqui altera a programação do dia. O que já foi apontado "
      + "continua no nome de quem produziu."),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose },
        "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        disabled: Object.keys(trocas).length === 0,
        onClick: () => onConfirmar(
          Object.entries(trocas).map(([itemId, novoId]) => ({ itemId, novoId })))
      }, "Confirmar realocação")
    )
  );
}

/* Apontar produção pela operação: a carteira já sabe qual ordem, qual
   etapa e quem foi escalado. O que falta é quanto saiu. */
function ApontarNaOperacao({ db, posto, escala, usuario, onClose, onSalvar }) {
  const ordens = posto.produtos || [];
  const [ordemId, setOrdemId] = React.useState(
    ordens.length === 1 ? (ordens[0].ordemId || '') : '');
  const [colaboradorId, setColaboradorId] = React.useState(
    (escala.pessoas || []).length === 1 ? escala.pessoas[0] : '');
  const [boas, setBoas] = React.useState('');
  const [defeito, setDefeito] = React.useState('');
  const [minutos, setMinutos] = React.useState('');
  const [data, setData] = React.useState(todayISO());

  const ordem = (db.ordens || []).find(o => o.id === ordemId);
  const tarefa = ordem
    ? (ordem.tarefas || []).find(t => t.etapaId === posto.id) : null;

  /* quem pode apontar: os escalados primeiro, depois o resto do setor */
  const escalados = (escala.pessoas || [])
    .map(id => (db.colaboradores || []).find(c => c.id === id))
    .filter(Boolean);
  const outros = (db.colaboradores || []).filter(c =>
    c.departamentoId === posto.departamentoId &&
    c.status !== 'Inativo' &&
    !(escala.pessoas || []).includes(c.id));

  const ritmo = colaboradorId
    ? ritmoEsperado(db, colaboradorId, posto.id, posto.unitario) : null;
  const esperado = (ritmo && ritmo.porHora > 0 && num(minutos) > 0)
    ? Math.round(ritmo.porHora * num(minutos) / 60) : null;

  return React.createElement(Modal, {
    title: "Apontar · " + posto.etapa, onClose
  },
    ordens.length > 1
      ? React.createElement(Field, { label: "De qual ordem" },
          React.createElement("select", {
            value: ordemId, onChange: e => setOrdemId(e.target.value)
          },
            React.createElement("option", { value: "" }, "escolha..."),
            ordens.map(o => React.createElement("option",
              { key: o.ordemId, value: o.ordemId },
              o.ordem + " · " + (o.produto || "")))
          ),
          React.createElement("div", { className: "small muted" },
            "Esta operação atende " + ordens.length + " ordens ao mesmo tempo.")
        )
      : React.createElement("div", { className: "small muted",
          style: { marginBottom: 10 } },
          ordens.length === 1
            ? ordens[0].ordem + " · " + (ordens[0].produto || "")
            : "Nenhuma ordem nesta operação."),

    React.createElement(Field, { label: "Quem produziu" },
      React.createElement("select", {
        value: colaboradorId, onChange: e => setColaboradorId(e.target.value)
      },
        React.createElement("option", { value: "" }, "escolha..."),
        escalados.length > 0
          ? React.createElement("optgroup", { label: "Escalados nesta operação" },
              escalados.map(c => React.createElement("option",
                { key: c.id, value: c.id }, c.nome)))
          : null,
        outros.length > 0
          ? React.createElement("optgroup", { label: "Outros do setor" },
              outros.map(c => React.createElement("option",
                { key: c.id, value: c.id }, c.nome)))
          : null
      ),
      escalados.length === 0
        ? React.createElement("div", { className: "small muted" },
            "Ninguém escalado nesta operação — o apontamento vale igual, "
            + "mas escalar antes deixa o plano e o realizado comparáveis.")
        : null,
      colaboradorId && !(escala.pessoas || []).includes(colaboradorId)
        ? React.createElement("div", { className: "small",
            style: { color: 'var(--warn)' } },
            "Esta pessoa não estava escalada aqui. O apontamento fica no "
            + "nome dela, como deve ser.")
        : null
    ),

    React.createElement("div", { className: "grid3" },
      React.createElement(Field, { label: "Peças boas" },
        React.createElement("input", {
          type: "number", min: "0", value: boas,
          onChange: e => setBoas(e.target.value)
        })),
      React.createElement(Field, { label: "Rejeitadas" },
        React.createElement("input", {
          type: "number", min: "0", value: defeito,
          onChange: e => setDefeito(e.target.value)
        })),
      React.createElement(Field, { label: "Minutos gastos" },
        React.createElement("input", {
          type: "number", min: "0", value: minutos,
          onChange: e => setMinutos(e.target.value)
        }))
    ),

    esperado !== null
      ? React.createElement("div", { className: "small muted",
          style: { marginBottom: 10 } },
          "No ritmo " + (ritmo.origem === 'historico' ? "desta pessoa"
            : ritmo.origem === 'meta' ? "da meta" : "estimado")
          + " (" + ritmo.porHora + "/h), " + duracao(num(minutos))
          + " dariam cerca de " + esperado + " peças."
          + (num(boas) > 0 && Math.abs(num(boas) - esperado) > esperado * 0.3
              ? " O apontado está bem diferente disso — vale conferir." : ""))
      : null,

    React.createElement(Field, { label: "Dia" },
      React.createElement("input", {
        type: "date", value: data, onChange: e => setData(e.target.value)
      })),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose },
        "Cancelar"),
      !ordemId ? React.createElement("div", { className: "small",
        style: { color: 'var(--warn)' } },
        "Escolha a ordem para apontar.") : null,
      ordemId && !tarefa ? React.createElement("div", { className: "small",
        style: { color: 'var(--danger)' } },
        "Esta ordem não tem esta operação no roteiro — não dá para apontar aqui.")
        : null,

      React.createElement("button", {
        className: "btn accent",
        disabled: !tarefa || !colaboradorId || num(boas) + num(defeito) <= 0
          || num(minutos) <= 0,
        onClick: () => onSalvar({
          ordemId, tarefaId: tarefa ? tarefa.id : '',
          colaboradorId,
          pecasBoas: num(boas), pecasDefeito: num(defeito),
          minutosGastos: num(minutos), paradas: [], data
        })
      }, "Apontar")
    )
  );
}

/* O cronograma do dia: o que já era para ter começado e o que está
   demorando mais que o previsto. É aqui que a produção começa. */
function CronogramaDoDia({ db, update, usuario, podeEditar }) {
  const [data, setData] = React.useState(todayISO());
  const [concluindo, setConcluindo] = React.useState(null);
  const c = cronogramaDoDia(db, data);

  const cabecalho = React.createElement("div", { className: "small muted",
    style: { marginBottom: 12 } },
    "As operações do dia, na ordem de urgência. Quem já passou da hora de "
    + "entrar sobe para o topo. Iniciar aqui marca a hora real — é o que "
    + "permite saber depois se a etapa demorou mais do que devia.");

  const filtro = React.createElement(BarraFiltros, { campos: [
    { id: 'dia', label: 'Dia', tipo: 'date', valor: data, onChange: setData }
  ] });

  if (c.vazio)
    return React.createElement("div", null, cabecalho, filtro,
      React.createElement(Empty, {
        text: "Nenhuma programação para " + fmtDate(data)
          + ". Monte a programação no Balanceamento da carteira."
      }));

  const cor = c.situacao === 'no prazo' ? 'var(--ok)'
    : c.situacao === 'atrasado' ? 'var(--danger)' : 'var(--warn)';

  const veredicto = React.createElement("div", {
    className: "panel", style: { borderColor: cor }
  },
    React.createElement("div", { className: "small",
      style: { fontWeight: 600, color: cor } },
      c.situacao === 'no prazo' ? "O dia está no ritmo previsto."
        : c.situacao === 'atrasado'
          ? c.atrasadas + " operação(ões) já deviam ter começado."
          : c.excedidas + " operação(ões) estão demorando mais que o previsto."),
    React.createElement("div", { className: "small muted" },
      c.concluidas + " concluída(s) · " + c.iniciadas + " em produção · "
      + c.previstas + " ainda não iniciada(s)")
  );

  const linha = (it) => {
    const st = SITUACAO_EXECUCAO.find(x => x.id === it.situacao) || {};
    return React.createElement("tr", {
      key: it.id,
      style: it.atrasoInicio > 0
        ? { background: 'rgba(176,58,46,.05)' } : undefined
    },
      React.createElement("td", { className: "num small" },
        React.createElement("strong", null, it.horaPrevista),
        it.duracaoPrevista
          ? React.createElement("div", { className: "muted" },
              duracao(it.duracaoPrevista))
          : null
      ),
      React.createElement("td", null,
        React.createElement("strong", null, it.etapa),
        React.createElement("div", { className: "small muted" },
          (it.ordemCodigo || '') + " · " + it.quantidadePrevista + " peça(s)"
          + (it.automatico ? " · sugerido pelo sistema" : "")),
        it.alerta
          ? React.createElement("div", {
              className: "small",
              style: { color: it.atrasoInicio > 0 ? 'var(--danger)' : 'var(--warn)',
                fontWeight: 600 }
            }, it.alerta)
          : null
      ),
      React.createElement("td", { className: "small" },
        (it.pessoas || []).map(id => {
          const p = (db.colaboradores || []).find(x => x.id === id);
          return p ? p.nome.split(' ')[0] : '?';
        }).join(', ') || '—',
        (it.maquinas || []).length > 0
          ? React.createElement("div", { className: "muted" },
              (it.maquinas || []).map(id => {
                const e = (db.equipamentos || []).find(x => x.id === id);
                return e ? e.nome : '?';
              }).join(', '))
          : null
      ),
      React.createElement("td", { className: "small" },
        React.createElement("span", { style: { color: st.cor, fontWeight: 600 } },
          st.nome || it.situacao),
        it.iniciadoEm
          ? React.createElement("div", { className: "muted" },
              "às " + (fmtHora(it.iniciadoEm)))
          : null
      ),
      podeEditar
        ? React.createElement("td", { className: "no-print" },
            it.situacao === 'prevista' && it.bloqueio
              ? React.createElement("span", { className: "small muted" },
                  "aguardando")
              : it.situacao === 'prevista'
              ? React.createElement("button", {
                  className: "btn accent sm",
                  onClick: () => {
                    let erro = null;
                    update(d => {
                      const r = iniciarEtapa(d, c.programacao.id, it.id, {}, usuario);
                      erro = r.erro || null;
                      return d;
                    });
                    if (erro) alert(erro);
                  }
                }, "Iniciar")
              : it.situacao === 'iniciada'
                ? React.createElement("button", {
                    className: "btn ghost sm",
                    onClick: () => setConcluindo(it)
                  }, "Concluir")
                : React.createElement("span", { className: "small muted" },
                    it.pecasFeitas + " peça(s)"))
        : null
    );
  };

  return React.createElement("div", null,
    cabecalho, filtro, veredicto,

    React.createElement("div", { className: "panel", style: { padding: 0 } },
      React.createElement("table", null,
        React.createElement("thead", null,
          React.createElement("tr", null,
            React.createElement("th", { className: "num" }, "Entra às"),
            React.createElement("th", null, "Operação"),
            React.createElement("th", null, "Quem faz"),
            React.createElement("th", null, "Situação"),
            podeEditar ? React.createElement("th", { style: { width: 96 } }) : null
          )),
        React.createElement("tbody", null, c.itens.map(linha)))),

    c.atrasadas > 0
      ? React.createElement("div", { className: "small muted" },
          "Operação atrasada não se resolve sozinha: ou entra agora, ou vale "
          + "registrar a ocorrência e realocar para quem está livre.")
      : null,

    concluindo
      ? React.createElement(ConcluirEtapaModal, {
          db, item: concluindo, programacaoId: c.programacao.id, usuario,
          onClose: () => setConcluindo(null),
          onConfirmar: (dados) => {
            let erro = null;
            update(d => {
              const r = concluirEtapa(d, c.programacao.id, concluindo.id,
                dados, usuario);
              erro = r.erro || null;
              return d;
            });
            if (erro) alert(erro); else setConcluindo(null);
          }
        })
      : null
  );
}

/* Concluir a etapa: quanto saiu, de quem, e o que atrapalhou. */
function ConcluirEtapaModal({ db, item, usuario, onClose, onConfirmar }) {
  const [boas, setBoas] = React.useState('');
  const [defeito, setDefeito] = React.useState('');
  const [colaboradorId, setColaboradorId] = React.useState(
    (item.pessoas || [])[0] || '');
  const [observacao, setObservacao] = React.useState('');

  const decorrido = item.iniciadoEm
    ? Math.max(1, Math.round((Date.now() - new Date(item.iniciadoEm)) / 60000))
    : 0;
  const equipe = (item.pessoas || [])
    .map(id => (db.colaboradores || []).find(c => c.id === id))
    .filter(Boolean);
  const outros = (db.colaboradores || []).filter(c =>
    c.departamentoId === item.departamentoId && c.status !== 'Inativo' &&
    !(item.pessoas || []).includes(c.id));

  const previsto = item.quantidadePrevista;
  const total = num(boas) + num(defeito);
  const parcial = total > 0 && total < previsto;

  return React.createElement(Modal, {
    title: "Concluir · " + item.etapa, onClose
  },
    React.createElement("div", { className: "small muted", style: { marginBottom: 10 } },
      "Iniciada às " + (fmtHora(item.iniciadoEm))
      + " · " + duracao(decorrido) + " até agora"
      + (item.duracaoPrevista
          ? " · previsto " + duracao(item.duracaoPrevista) : "")),

    React.createElement("div", { className: "grid3" },
      React.createElement(Field, { label: "Peças boas" },
        React.createElement("input", {
          type: "number", min: "0", value: boas,
          placeholder: String(previsto),
          onChange: e => setBoas(e.target.value)
        })),
      React.createElement(Field, { label: "Rejeitadas" },
        React.createElement("input", {
          type: "number", min: "0", value: defeito,
          onChange: e => setDefeito(e.target.value)
        })),
      React.createElement(Field, { label: "Tempo" },
        React.createElement("div", { className: "small",
          style: { paddingTop: 8, fontWeight: 600 } }, duracao(decorrido)),
        React.createElement("div", { className: "small muted" },
          "pelo relógio"))),

    equipe.length > 1 || outros.length > 0
      ? React.createElement(Field, { label: "Quem produziu" },
          React.createElement("select", {
            value: colaboradorId, onChange: e => setColaboradorId(e.target.value)
          },
            equipe.length > 0
              ? React.createElement("optgroup", { label: "Escalados" },
                  equipe.map(c => React.createElement("option",
                    { key: c.id, value: c.id }, c.nome)))
              : null,
            outros.length > 0
              ? React.createElement("optgroup", { label: "Outros do setor" },
                  outros.map(c => React.createElement("option",
                    { key: c.id, value: c.id }, c.nome)))
              : null))
      : null,

    parcial
      ? React.createElement("div", { className: "panel",
          style: { borderColor: 'var(--warn)', background: 'var(--warn-bg)' } },
          React.createElement("div", { className: "small" },
            total + " de " + previsto + " peças. A operação será dada como "
            + "concluída e as " + (previsto - total) + " restantes voltam para "
            + "a reprogramação do dia."))
      : null,

    React.createElement(Field, { label: "Observação" },
      React.createElement("textarea", {
        rows: "2", value: observacao,
        placeholder: "o que atrapalhou, se atrapalhou",
        onChange: e => setObservacao(e.target.value)
      })),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose },
        "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        disabled: total <= 0 || !colaboradorId,
        onClick: () => onConfirmar({
          pecasBoas: num(boas), pecasDefeito: num(defeito),
          colaboradorId, observacao
        })
      }, "Concluir operação"))
  );
}

function BalanceamentoCarteira({ db, update, usuario, podeEditar }) {
  const j = jornadaDe(db);
  const abertas = (db.ordens || []).filter(o =>
    o.situacao !== 'cancelada' && o.situacao !== 'concluida');
  /* as ordens entram pela regra dos filtros, não por seleção manual:
     marcar caixinha uma a uma cansa quando a carteira cresce */
  const [minutosDia, setMinutosDia] = React.useState(j.produtivo);
  const [departamentoId, setDepartamentoId] = React.useState('');
  const [grupoId, setGrupoId] = React.useState('');
  /* máquinas e pessoas escolhidas pelo nome, não por quantidade */
  const [maquinas, setMaquinas] = React.useState([]);
  const [pessoas, setPessoas] = React.useState([]);
  /* quem faz cada operação: { operacaoId: {pessoas:[], maquinas:[]} } */
  const [escalasCarteira, setEscalasCarteira] = React.useState({});
  const [escalando, setEscalando] = React.useState(null);
  const [apontando, setApontando] = React.useState(null);
  const [dataProg, setDataProg] = React.useState(todayISO());
  const [extra, setExtra] = React.useState('0');
  const [de, setDe] = React.useState('');
  const [ate, setAte] = React.useState('');
  const [alocacao, setAlocacao] = React.useState({});

  /* os filtros de período e grupo escolhem quais ordens entram na conta */
  const naFaixa = o => {
    if (de && (!o.entrega || o.entrega < de)) return false;
    if (ate && (!o.entrega || o.entrega > ate)) return false;
    if (grupoId) {
      const p = (db.produtos || []).find(x => x.id === o.produtoId);
      if (!p || p.grupoId !== grupoId) return false;
    }
    return true;
  };
  const naConta = abertas.filter(naFaixa);
  const idsFiltrados = naConta.map(o => o.id);

  const disponivel = recursosDisponiveis(db, departamentoId);

  /* só conta quem sabe operar as máquinas do dia */
  const aptos = maquinas.length > 0
    ? pessoas.filter(id => maquinasQueOpera(db, id, maquinas).length > 0)
    : pessoas;
  /* máquina é recurso do turno: mais gente que máquina não produz mais */
  const efetivos = maquinas.length > 0
    ? aptos.slice(0, maquinas.length) : aptos;

  const b = balancearCarteira(db, {
    ordemIds: idsFiltrados, minutosDia, departamentoId, alocacao,
    escalas: escalasCarteira,
    semDistribuir: Object.keys(alocacao).length > 0 ||
      Object.keys(escalasCarteira).length > 0,
    recursosInformados: {
      pessoas: efetivos.length > 0 ? efetivos.length : null,
      maquinas: maquinas.length > 0 ? maquinas.length : null,
    }
  });

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "A linha não produz uma referência só. Aqui as ordens escolhidas dividem ",
      "os mesmos postos — é assim que dá para saber se a fábrica dá conta de ",
      "tudo que está prometido, e não de uma coisa de cada vez."),

    React.createElement(BarraFiltros, { campos: [
      { id: 'grupo', label: 'Grupo de produto', tipo: 'select', valor: grupoId,
        onChange: setGrupoId, opcoes: [{ id: '', nome: 'Todos' }].concat(
          (db.gruposProduto || []).map(g => ({ id: g.id, nome: g.nome }))) },
      { id: 'dep', label: 'Departamento', tipo: 'select', valor: departamentoId,
        onChange: setDepartamentoId, opcoes: [{ id: '', nome: 'Todos' }].concat(
          (db.departamentos || []).filter(d => d.ativo !== false)
            .map(d => ({ id: d.id, nome: d.nome }))) },
      { id: 'de', label: 'Entrega de', tipo: 'date', valor: de, onChange: setDe },
      { id: 'ate', label: 'Até', tipo: 'date', valor: ate, onChange: setAte },
      { id: 'min', label: 'Minutos por dia', tipo: 'number', min: '1',
        valor: minutosDia, onChange: v => setMinutosDia(num(v)) },
      { id: 'extra', label: 'Hora extra permitida', tipo: 'select',
        valor: extra, onChange: setExtra,
        opcoes: [{ id: '0', nome: 'Sem hora extra' },
          { id: '30', nome: '30 minutos' }, { id: '60', nome: '1 hora' },
          { id: '120', nome: '2 horas (limite legal)' }],
        ajuda: num(extra) > 0 ? 'custa mais que hora normal' : '' },
    ] }),

    React.createElement(SeletorRecursos, {
      db, tipoAgenda: 'maquina',
      onEscolherDia: setDataProg, diaAtual: dataProg,
      titulo: "Máquinas", itens: disponivel.maquinas.map(m => ({
        id: m.id, nome: m.nome, detalhe: m.codigo || m.tipo || '' })),
      escolhidos: maquinas, onChange: setMaquinas,
      vazio: "Nenhuma máquina cadastrada neste setor.",
      rodape: maquinas.length > 0 && pessoas.length > maquinas.length
        ? `${pessoas.length} pessoas para ${maquinas.length} máquina(s): ` +
          `${pessoas.length - maquinas.length} ficaria(m) sem posto.`
        : null
    }),

    React.createElement(SeletorRecursos, {
      db, tipoAgenda: 'pessoa',
      onEscolherDia: setDataProg, diaAtual: dataProg,
      titulo: "Colaboradores",
      itens: disponivel.pessoas.map(c => {
        const opera = maquinas.length === 0 ||
          maquinasQueOpera(db, c.id, maquinas).length > 0;
        return { id: c.id, nome: c.nome,
          detalhe: opera ? '' : 'não opera',
          alerta: !opera };
      }),
      escolhidos: pessoas, onChange: v => { setPessoas(v); setAlocacao({}); },
      vazio: "Nenhum colaborador cadastrado neste setor.",
      rodape: "O ritmo de cada pessoa vem do histórico dela nas operações. " +
        "Quem aparece como “não opera” não sabe usar nenhuma das máquinas marcadas."
    }),

    (() => {
      const prog = programacaoDoDia(db, dataProg);
      const real = prog ? realizadoDaProgramacao(db, prog) : null;
      const st = prog ? (STATUS_PROGRAMACAO.find(x => x.id === prog.status) || {}) : null;

      const montarItens = () => b.postos
        .filter(p => escalasCarteira[p.id] &&
          (escalasCarteira[p.id].pessoas || []).length > 0)
        .map(p => ({
          ordemId: (p.produtos[0] || {}).ordemId || '',
          ordemCodigo: (p.produtos[0] || {}).ordem || '',
          etapaId: p.id, etapa: p.etapa,
          departamentoId: p.departamentoId,
          pessoas: escalasCarteira[p.id].pessoas,
          maquinas: escalasCarteira[p.id].maquinas || [],
          horaInicio: escalasCarteira[p.id].hora || '',
          quantidadePrevista: p.unitario > 0
            ? Math.round(p.carga / p.unitario) : 0,
        }));

      return React.createElement("div", { className: "panel no-print" },
        React.createElement("div", { className: "row-actions",
          style: { marginBottom: prog ? 8 : 0 } },
          React.createElement(Field, { label: "Programar para o dia" },
            React.createElement("input", {
              type: "date", value: dataProg,
              onChange: e => setDataProg(e.target.value)
            })),
          React.createElement("span", { style: { flex: 1 } }),

          !prog && React.createElement("button", {
            className: "btn accent",
            disabled: montarItens().length === 0,
            onClick: () => {
              let erro = null;
              update(d => {
                const r = salvarProgramacao(d, { data: dataProg,
                  itens: montarItens(), minutosDia }, usuario);
                erro = r.erro || null;
                return d;
              });
              if (erro) alert(erro);
            }
          }, "Salvar programação"),

          prog && prog.status === 'rascunho' && React.createElement("button", {
            className: "btn accent",
            onClick: () => {
              let erro = null;
              update(d => {
                const r = publicarProgramacao(d, prog.id, usuario);
                erro = r.erro || null;
                return d;
              });
              if (erro) alert(erro);
            }
          }, "Publicar"),

          prog && React.createElement("button", {
            className: "btn ghost",
            onClick: () => {
              if (!confirm(
                `Encerrar a programação de ${fmtDate(prog.data)} e levar o que ` +
                `não foi concluído para o próximo dia?\n\n` +
                `A equipe vem sugerida, mas você pode trocar antes de publicar.`
              )) return;
              let msg = null;
              update(d => {
                const r = reprogramar(d, prog.id, {}, usuario);
                msg = r.erro || r.aviso ||
                  (r.nova ? `${r.pendentes} operação(ões) foram para ` +
                    `${fmtDate(r.nova.data)} na ${r.nova.codigo}.` : null);
                return d;
              });
              if (msg) alert(msg);
            }
          }, "Reprogramar o dia")),

        prog && React.createElement("div", { className: "small" },
          React.createElement("strong", null, `${prog.codigo} · ${st.nome}`),
          React.createElement("span", { className: "muted" }, ` — ${st.ajuda}`),
          real && React.createElement("div", { className: "muted" },
            `${real.totalFeito} de ${real.totalPrevisto} peças · `,
            `${real.concluidas} de ${prog.itens.length} operação(ões) concluída(s)`)),

        prog && prog.status === 'publicada' && React.createElement("div", {
          className: "small muted", style: { marginTop: 6 }
        }, "A fábrica trabalha por esta programação. Se alguém faltar ou a ",
          "máquina parar, use “Reprogramar o dia” no fim do turno — ele leva ",
          "o que não saiu para amanhã e deixa você trocar a equipe."),

        !prog && montarItens().length === 0 && React.createElement("div", {
          className: "small muted", style: { marginTop: 6 }
        }, "Escale ao menos uma operação acima para poder salvar a programação."));
    })(),

    React.createElement("div", { className: "small muted", style: { marginBottom: 10 } },
      naConta.length === 0
        ? "Nenhuma ordem em aberto no filtro."
        : React.createElement(React.Fragment, null,
            React.createElement("strong", null,
              `${naConta.length} ordem(ns) na conta`),
            ": ", naConta.map(o => o.codigo).join(', '),
            naConta.length < abertas.length &&
              ` · ${abertas.length - naConta.length} fora do filtro`)),

    b.vazio
      ? React.createElement(Empty, {
          text: naConta.length === 0
            ? "Ajuste os filtros para incluir alguma ordem."
            : "Nenhuma operação nas ordens filtradas para este setor."
        })
      : React.createElement(React.Fragment, null,
          React.createElement("div", { className: "faixa-dados" },
            React.createElement("div", { className: "linha-rotulos" },
              React.createElement("span", null, "Ordens na conta"),
              React.createElement("span", null, "Carga total"),
              React.createElement("span", null, "Dias para terminar"),
              React.createElement("span", null, "Pessoas alocadas"),
              React.createElement("span", null, "Ocupação média")),
            React.createElement("div", { className: "linha-valores" },
              React.createElement("span", null, b.ordens.length,
                React.createElement("small", null,
                  b.primeiraEntrega ? `1ª entrega ${fmtDate(b.primeiraEntrega)}` : 'sem prazo')),
              React.createElement("span", null, duracao(b.cargaTotal),
                React.createElement("small", null, "de trabalho pela frente")),
              React.createElement("span", { className: "destaque" },
                b.diasLinha !== null ? `${b.diasLinha}d` : '—',
                React.createElement("small", null,
                  b.gargalo ? `pelo ${b.gargalo.etapa}` : '')),
              React.createElement("span", null, b.totalPessoas,
                React.createElement("small", null, "somando os postos")),
              React.createElement("span", {
                className: b.ocupacaoMedia !== null && b.ocupacaoMedia < 70 ? "alerta" : ""
              }, b.ocupacaoMedia !== null ? `${b.ocupacaoMedia}%` : '—',
                React.createElement("small", null, "do tempo alocado")))),

          b.equipeDividida && React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--thread)' }
    },
      React.createElement("div", { className: "small muted" },
        React.createElement("strong", null,
          `${b.equipeDividida.informado} pessoa(s) para `,
          `${b.equipeDividida.postos} operações — `,
          `${b.equipeDividida.partidos} delas com menos de uma pessoa. `),
        "O tempo de cada uma é dividido entre as operações, proporcional ",
        "ao trabalho. Na prática isso custa trocas de posto, que o prazo ",
        "abaixo não considera.")),

    b.prazo && React.createElement("div", {
            className: "panel",
            style: b.prazo.cabe
              ? { borderColor: 'var(--ok)', background: 'var(--ok-bg)' }
              : { borderColor: 'var(--danger)', background: 'rgba(176,58,46,.06)' }
          },
            React.createElement("div", {
              className: "small",
              style: { fontWeight: 600, color: b.prazo.cabe ? 'var(--ok)' : 'var(--danger)' }
            },
              b.prazo.cabe
                ? `Tudo fica pronto em ${fmtDate(b.prazo.data)} — antes da primeira entrega.`
                : `Tudo só fica pronto em ${fmtDate(b.prazo.data)}, depois da entrega de ${fmtDate(b.primeiraEntrega)}.`),
            React.createElement("div", { className: "small muted" },
              b.prazo.cabe
                ? "Considerando as ordens escolhidas e as pessoas alocadas."
                : "Reforce o gargalo, tire ordem da conta ou renegocie o prazo.")),

          b.excedidos.length > 0 && React.createElement("div", {
            className: "panel", style: { borderColor: 'var(--danger)' }
          },
            React.createElement("div", {
              className: "small", style: { fontWeight: 600, color: 'var(--danger)' }
            }, "Alocação acima do que a fábrica tem:"),
            b.excedidos.map(p => React.createElement("div", {
              key: p.id, className: "small muted"
            }, `${p.etapa} (${p.setor}): ${p.pessoas} pessoa(s) alocada(s), `,
              `mas o setor tem ${p.recursos.pessoas} pessoa(s)`,
              p.recursos.maquinas > 0 ? ` e ${p.recursos.maquinas} máquina(s)` : '',
              `. Planejar com gente que não existe é planejar no papel.`))),

          React.createElement("div", { className: "panel", style: { padding: 0 } },
            React.createElement("table", null,
              React.createElement("thead", null,
                React.createElement("tr", null,
                  React.createElement("th", null, "Operação"),
                  React.createElement("th", { className: "num" }, "Carga"),
                  React.createElement("th", null, "Quem faz"),
                  React.createElement("th", { className: "num" }, "Dias"),
                  React.createElement("th", null, "Vem de"))),
              React.createElement("tbody", null,
                b.postos.slice().sort((x, y) => y.minutos - x.minutos).map(p =>
                  React.createElement("tr", {
                    key: p.id,
                    style: p.gargalo || (b.gargalo && p.id === b.gargalo.id)
                      ? { background: 'var(--warn-bg)' } : undefined
                  },
                    React.createElement("td", null,
                      React.createElement("strong", null, p.etapa),
                      React.createElement("div", { className: "small muted" }, p.setor),
                      b.gargalo && p.id === b.gargalo.id &&
                        React.createElement("div", {
                          className: "small", style: { color: 'var(--warn)', fontWeight: 600 }
                        }, "gargalo da carteira")),
                    React.createElement("td", { className: "num" }, duracao(p.carga)),
                    React.createElement("td", null,
                      /* quem faz esta operação: escolhido aqui, não abstraído
                         num número de pessoas */
                      React.createElement("button", {
                        className: "btn ghost sm",
                        onClick: () => setEscalando(p.id)
                      }, p.escala ? "Trocar" : "Escalar"),
                      /* Apontar saiu daqui: iniciar é no cronograma,
                         concluir é em apontamentos. A carteira planeja;
                         a execução acontece nas outras duas telas. */
                      p.escala
                        ? React.createElement("div", { className: "small",
                            style: { marginTop: 4 } },
                            p.escala.membros.map(m => m.nome.split(' ')[0]).join(', '),
                            React.createElement("div", { className: "muted" },
                              `${p.escala.porHora}/h`,
                              p.escala.baseReal < 100
                                ? ` · base real ${p.escala.baseReal}%` : ''),
                            (escalasCarteira[p.id] || {}).maquinas &&
                              (escalasCarteira[p.id].maquinas || []).length > 0 &&
                              React.createElement("div", { className: "muted" },
                                (escalasCarteira[p.id].maquinas || []).map(mid => {
                                  const eq = (db.equipamentos || []).find(e => e.id === mid);
                                  return eq ? eq.nome : '';
                                }).filter(Boolean).join(', ')))
                        : React.createElement("div", { className: "small muted",
                            style: { marginTop: 4 } },
                            `${p.recursos.pessoas} disponível(is)`,
                            p.recursos.maquinas > 0
                              ? ` · ${p.recursos.maquinas} máquina(s)` : '')),
                    React.createElement("td", { className: "num" },
                      React.createElement("strong", null,
                        p.dias !== null ? `${p.dias}d` : '—')),
                    React.createElement("td", { className: "small muted" },
                      p.produtos.map(x => x.ordem).join(', ')))))),

          React.createElement("div", { className: "small muted" },
            "A carga soma o que falta produzir em cada ordem, não o total dela — ",
            "o que já saiu não disputa mais a linha."),

          React.createElement(ResumoDoDia, {
            db, balanceamento: b, escalas: escalasCarteira,
            minutosDia, extra, maquinas
          }),

          apontando && React.createElement(ApontarNaOperacao, {
            db, posto: apontando,
            escala: (escalasCarteira[apontando.id] || {}),
            usuario,
            onClose: () => setApontando(null),
            onSalvar: (dados) => {
              let erro = null;
              update(d => {
                const r = apontarProducao(d, dados, usuario);
                erro = r.erro || null;
                return d;
              });
              if (erro) alert(erro); else setApontando(null);
            }
          }),

          escalando && (() => {
            const p = b.postos.find(x => x.id === escalando);
            if (!p) return null;
            const esc = escalasCarteira[p.id] || { pessoas: [], maquinas: [] };
            return React.createElement(EscalaDaOperacao, {
              posto: { id: p.id, etapa: p.etapa, etapaId: p.id,
                departamentoId: p.departamentoId,
                tempoUnitario: p.unitario, unitario: p.unitario,
                carga: p.carga },
              db, escala: esc.pessoas, maquinas: esc.maquinas,
              opcoesEscala: { data: esc.data || dataProg, hora: esc.hora },
              produtoId: (p.produtos[0] || {}).produtoId, update, usuario,
              onChange: v => setEscalasCarteira(e => ({ ...e, [p.id]: v })),
              onClose: () => setEscalando(null)
            });
          })())));
}

/* Escolher quem vai para cada posto, vendo o que cada um já rendeu ali. */
function EscalaDaOperacao({ posto, db, escala, maquinas, onChange, onClose,
  update, usuario, produtoId, opcoesEscala }) {
  const opc = { produtoId, equipamentoId: (maquinas || [])[0] };
  const cands = candidatosDaEtapa(db, posto.departamentoId, posto.etapaId,
    posto.tempoUnitario, opc);
  const rec = recursosDoSetor(posto.departamentoId, db);
  const media = mediaDaEtapa(db, posto.etapaId);
  const meta = metaVigente(db, posto.etapaId, produtoId);
  const escolhidos = escala || [];
  const maqEscolhidas = maquinas || [];
  const dataEsc = (opcoesEscala || {}).data || '';
  const horaEsc = (opcoesEscala || {}).hora || '';
  const cap = capacidadeDaEquipe(db, escolhidos, posto.etapaId,
    posto.tempoUnitario, opc);

  /* Abrir a aferição já preenchida: quem vai ser medido, em qual
     operação, com qual máquina. O resto o supervisor completa na
     tela de aferição. */
  const aferir = colaboradorId => {
    if (!update) return;
    let erro = null, codigo = null;
    update(d => {
      const r = novaAfericao(d, {
        colaboradorId, etapaId: posto.etapaId,
        departamentoId: posto.departamentoId,
        produtoId: produtoId || '',
        equipamentoId: maqEscolhidas[0] || ''
      }, usuario);
      erro = r.erro || null;
      codigo = r.afericao ? r.afericao.codigo : null;
      return d;
    });
    if (erro) alert(erro);
    else alert(`Aferição ${codigo} aberta.\n\n` +
      `Ela aparece em Produção → Análise → Aferição e metas, na fase de ` +
      `preparação. Enquanto não for concluída, o cálculo desta pessoa ` +
      `continua sendo estimativa.`);
  };

  const alternar = id => onChange({
    pessoas: escolhidos.includes(id)
      ? escolhidos.filter(x => x !== id) : [...escolhidos, id],
    maquinas: maqEscolhidas, data: dataEsc, hora: horaEsc
  });
  const alternarMaquina = id => onChange({
    pessoas: escolhidos,
    maquinas: maqEscolhidas.includes(id)
      ? maqEscolhidas.filter(x => x !== id) : [...maqEscolhidas, id],
    data: dataEsc, hora: horaEsc
  });

  const faltaGente = maqEscolhidas.length > escolhidos.length;
  const sobraGente = escolhidos.length > maqEscolhidas.length && maqEscolhidas.length > 0;

  return React.createElement(Modal, {
    title: `Equipe · ${posto.etapa}`, onClose, wide: true
  },
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "Escolha quem vai fazer esta operação. O número ao lado de cada nome é o ",
      "que a pessoa já produziu aqui, pelos apontamentos — não uma estimativa. ",
      "Quem nunca fez aparece com a média da fábrica."),

    /* Quando esta operação entra. O cronograma usa esta hora para
       saber se já era para ter começado. */
    React.createElement("div", { className: "grid3", style: { marginBottom: 12 } },
      React.createElement(Field, { label: "Entra em" },
        React.createElement("input", {
          type: "datetime-local",
          value: (dataEsc || todayISO()) + 'T'
            + (horaEsc || jornadaDe(db).inicio),
          onChange: e => {
            const [dd, hh] = String(e.target.value || '').split('T');
            onChange({
              pessoas: escolhidos, maquinas: maqEscolhidas,
              data: dd || dataEsc, hora: (hh || '').slice(0, 5) || horaEsc
            });
          }
        }),
        React.createElement("div", { className: "small muted" },
          "dia e hora em que esta operação entra na linha")),
      React.createElement(Field, { label: "Deve levar" },
        React.createElement("div", { className: "small",
          style: { paddingTop: 8, fontWeight: 600 } },
          (() => {
            const capH = cap.vazia ? 0 : cap.porHora;
            const pecas = posto.unitario > 0
              ? Math.round(posto.carga / posto.unitario) : 0;
            if (!capH || !pecas) return '—';
            return duracao((pecas / capH) * 60);
          })()),
        React.createElement("div", { className: "small muted" },
          "no ritmo desta equipe"))),

    rec.listaMaquinas.length > 0 && React.createElement(React.Fragment, null,
      React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
        `Máquinas (${maqEscolhidas.length} de ${rec.listaMaquinas.length})`),
      React.createElement("div", { className: "gente-grade", style: { marginBottom: 14 } },
        rec.listaMaquinas.map(m => React.createElement("label", {
          key: m.id,
          className: "gente-check" + (maqEscolhidas.includes(m.id) ? " on" : "")
        },
          React.createElement("input", {
            type: "checkbox", checked: maqEscolhidas.includes(m.id),
            onChange: () => alternarMaquina(m.id)
          }),
          React.createElement("span", null,
            React.createElement("strong", null, m.nome),
            React.createElement("span", { className: "small muted" },
              m.codigo || m.tipo || '')))))),

    React.createElement("div", { className: "etp-rot", style: { marginBottom: 6 } },
      `Colaboradores (${escolhidos.length} de ${cands.length})`),

    (faltaGente || sobraGente) && React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--warn)',
        background: 'var(--warn-bg)', marginBottom: 10 }
    },
      React.createElement("div", { className: "small" },
        faltaGente
          ? `${maqEscolhidas.length} máquina(s) para ${escolhidos.length} pessoa(s) — ` +
            `${maqEscolhidas.length - escolhidos.length} ficaria(m) parada(s).`
          : `${escolhidos.length} pessoa(s) para ${maqEscolhidas.length} máquina(s) — ` +
            `${escolhidos.length - maqEscolhidas.length} sem máquina para trabalhar.`)),

    cands.length === 0
      ? React.createElement(Empty, {
          text: "Nenhum colaborador cadastrado neste setor."
        })
      : React.createElement("div", { className: "escala-lista" },
          cands.map(c => {
            const on = escolhidos.includes(c.colaborador.id);
            const d = c.desempenho;
            return React.createElement("label", {
              key: c.colaborador.id, className: "escala-item" + (on ? " on" : "")
            },
              React.createElement("input", {
                type: "checkbox", checked: on,
                onChange: () => alternar(c.colaborador.id)
              }),
              React.createElement("span", { className: "escala-nome" }, c.nome),
              React.createElement("span", { className: "escala-num" },
                React.createElement("strong", null, `${c.porHora}/h`),
                React.createElement("span", { className: "small muted" },
                  d.semDados ? 'estimado pela média'
                    : `${d.apontamentos} apontamento(s)`)),
              c.contraMedia !== null && React.createElement("span", {
                className: "escala-comp",
                style: { color: c.contraMedia >= 0 ? 'var(--ok)' : 'var(--warn)' }
              }, c.contraMedia >= 0 ? `+${c.contraMedia}%` : `${c.contraMedia}%`),
              d.qualidade !== null && React.createElement("span", {
                className: "small muted escala-qual"
              }, `${d.qualidade}% boas`),
              c.precisaAferir && exigeAfericao(db, posto.departamentoId) &&
                update && React.createElement("button", {
                className: "btn ghost sm escala-aferir",
                title: meta
                  ? `A meta da operação é ${meta.valorHora}/h — aferir mostra se esta pessoa a alcança`
                  : "Abrir uma aferição para medir esta pessoa nesta operação",
                onClick: ev => { ev.preventDefault(); ev.stopPropagation();
                  aferir(c.colaborador.id); }
              }, c.afericaoEmAndamento ? "aferindo…" : "Aferir"),
              !c.precisaAferir && c.afericao && React.createElement("span", {
                className: "small muted escala-qual"
              }, c.afericao.codigo));
          })),

    !media.semDados && React.createElement("div", {
      className: "small muted", style: { marginTop: 8 }
    },
      `A média da fábrica nesta operação é ${media.porHora}/h, `,
      `de ${media.pessoas} pessoa(s) que já a fizeram. `,
      "A comparação ao lado do nome é contra esse número."),

    !cap.vazia && React.createElement("div", { className: "faixa-dados",
      style: { marginTop: 12, marginBottom: 0 } },
      React.createElement("div", { className: "linha-rotulos" },
        React.createElement("span", null, "Equipe escolhida"),
        React.createElement("span", null, "Capacidade"),
        React.createElement("span", null, "Ciclo"),
        React.createElement("span", null, "Base do cálculo")),
      React.createElement("div", { className: "linha-valores" },
        React.createElement("span", null, cap.pessoas,
          React.createElement("small", null, "pessoa(s)")),
        React.createElement("span", { className: "destaque" }, `${cap.porHora}/h`,
          React.createElement("small", null, "somando o ritmo de cada um")),
        React.createElement("span", null, duracao(cap.ciclo),
          React.createElement("small", null, "por peça")),
        React.createElement("span", {
          className: cap.baseReal < 50 ? "alerta" : ""
        }, `${cap.baseReal}%`,
          React.createElement("small", null,
            `${cap.comHistorico} de ${cap.pessoas} com histórico`)))),

    !cap.vazia && cap.aAferir.length > 0 && React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--warn)',
        background: 'var(--warn-bg)', marginTop: 10 }
    },
      React.createElement("div", { className: "small", style: { fontWeight: 600 } },
        `${cap.aAferir.length} pessoa(s) na equipe sem medição nesta operação.`),
      React.createElement("div", { className: "small muted" },
        cap.aAferir.map(m => m.nome).join(', ') +
        ". O ritmo delas é estimativa. Aferir leva algumas horas e troca o " +
        "chute por número medido — vale a pena antes de prometer prazo com " +
        "base nesta equipe.")),

    !exigeAfericao(db, posto.departamentoId) && React.createElement("div", {
      className: "small muted", style: { marginTop: 8 }
    }, "Este setor não trabalha com aferição: a referência de ritmo vem ",
      "do que a jornada entregou, pelos apontamentos. Aferição com ",
      "cronômetro faz sentido onde a operação se repete peça a peça — ",
      "quem define isso é o cadastro do departamento."),

    meta && exigeAfericao(db, posto.departamentoId) &&
      React.createElement("div", { className: "small muted",
      style: { marginTop: 8 } },
      `Existe meta aferida para esta operação: ${meta.valorHora}/h. `,
      "Ela é usada no cálculo de quem ainda não tem histórico próprio — ",
      "mas é a meta da operação, não a capacidade da pessoa. Aferir quem ",
      "nunca fez mostra se ela alcança esse ritmo."),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn accent", onClick: onClose },
        "Pronto"))
  );
}

function BalanceamentoLinha({ produto, db, update, usuario }) {
  const j = jornadaDe(db);
  const [minutosDia, setMinutosDia] = React.useState(j.produtivo);
  const [demanda, setDemanda] = React.useState('');
  const [alocacao, setAlocacao] = React.useState({});
  const [simulando, setSimulando] = React.useState(false);
  const [departamentoId, setDepartamentoId] = React.useState('');
  const [maquinas, setMaquinas] = React.useState([]);
  const [pessoas, setPessoas] = React.useState([]);
  /* quem foi escalado para cada posto: { postoId: {pessoas:[], maquinas:[]} } */
  const [escalas, setEscalas] = React.useState({});
  const [escalando, setEscalando] = React.useState(null);

  const disponivel = recursosDisponiveis(db, departamentoId);

  const b = balancearLinha(produto, db, {
    escalas,
    minutosDia, alocacao, demandaDia: num(demanda) || null,
    departamentoId,
    /* mexeu na tabela? então a mão do usuário manda */
    alocacaoManual: Object.keys(alocacao).length > 0,
    recursosInformados: {
      pessoas: (() => {
        const aptos = maquinas.length > 0
          ? pessoas.filter(id => maquinasQueOpera(db, id, maquinas).length > 0)
          : pessoas;
        const efetivos = maquinas.length > 0
          ? aptos.slice(0, maquinas.length) : aptos;
        return efetivos.length > 0 ? efetivos.length : null;
      })(),
      maquinas: maquinas.length > 0 ? maquinas.length : null,
    }
  });

  if (b.vazio) {
    return React.createElement(Empty, {
      text: "Este produto ainda não tem processo produtivo. Sem as operações, não há linha para balancear."
    });
  }

  const s = sugerirBalanceamento(produto, db, { minutosDia, alocacao });
  const dim = num(demanda) > 0
    ? dimensionarLinha(produto, db, { minutosDia, demandaDia: num(demanda) })
    : null;

  const mudarPessoas = (id, valor) => {
    setAlocacao(a => ({ ...a, [id]: Math.max(1, num(valor)) }));
    setSimulando(true);
  };

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "A linha produz no ritmo da operação mais lenta. Quem vem antes ou depois ",
      "dela espera — e essa espera é paga sem virar peça. Aqui dá para ver onde ",
      "a linha trava e simular o efeito de mexer nas pessoas."),

    React.createElement(BarraFiltros, { campos: [
      { id: 'dep', label: 'Departamento', tipo: 'select', valor: departamentoId,
        onChange: setDepartamentoId, opcoes: [{ id: '', nome: 'Todos' }].concat(
          (db.departamentos || []).filter(d => d.ativo !== false)
            .map(d => ({ id: d.id, nome: d.nome }))) },
      { id: 'demanda', label: 'Precisa por dia', tipo: 'number', min: '0',
        valor: demanda, onChange: setDemanda, placeholder: 'opcional' },
      { id: 'min', label: 'Minutos por dia', tipo: 'number', min: '1',
        valor: minutosDia, onChange: v => setMinutosDia(num(v)) },
    ] }),

    React.createElement(SeletorRecursos, {
      db, tipoAgenda: 'maquina',
      titulo: "Máquinas", itens: disponivel.maquinas.map(m => ({
        id: m.id, nome: m.nome, detalhe: m.codigo || m.tipo || '' })),
      escolhidos: maquinas, onChange: setMaquinas,
      vazio: "Nenhuma máquina cadastrada neste setor."
    }),

    React.createElement(SeletorRecursos, {
      db, tipoAgenda: 'pessoa',
      titulo: "Colaboradores",
      itens: disponivel.pessoas.map(c => {
        const opera = maquinas.length === 0 ||
          maquinasQueOpera(db, c.id, maquinas).length > 0;
        return { id: c.id, nome: c.nome,
          detalhe: opera ? '' : 'não opera', alerta: !opera };
      }),
      escolhidos: pessoas,
      onChange: v => { setPessoas(v); setAlocacao({}); setSimulando(false); },
      vazio: "Nenhum colaborador cadastrado neste setor.",
      rodape: "Marque quem trabalha nesta linha. O ritmo de cada um vem do " +
        "histórico dele; quem nunca fez a operação usa a meta ou a média."
    }),

    simulando && React.createElement("div", { className: "row-actions",
      style: { marginBottom: 10 } },
      React.createElement("span", { className: "small muted", style: { flex: 1 } },
        "Você está simulando uma alocação diferente da cadastrada."),
      React.createElement("button", {
        className: "btn ghost sm",
        onClick: () => { setAlocacao({}); setSimulando(false); }
      }, "Voltar ao real")),

    React.createElement("div", { className: "faixa-dados" },
      React.createElement("div", { className: "linha-rotulos" },
        React.createElement("span", null, "Sai por dia"),
        React.createElement("span", null, "Pessoas na linha"),
        React.createElement("span", null, "Equilíbrio da linha"),
        React.createElement("span", null, "Espera por dia")),
      React.createElement("div", { className: "linha-valores" },
        React.createElement("span", { className: "destaque" }, b.saidaDia,
          React.createElement("small", null,
            `${b.saidaHora}/hora · ciclo ${duracao(b.cicloLinha)}`)),
        React.createElement("span", null, b.totalPessoas,
          React.createElement("small", null,
            `${(b.saidaDia / Math.max(1, b.totalPessoas)).toFixed(0)} peças por pessoa`)),
        React.createElement("span", {
          className: b.eficiencia !== null && b.eficiencia < 70 ? "alerta" : ""
        }, b.eficiencia !== null ? `${b.eficiencia}%` : '—',
          React.createElement("small", null, "do tempo pago vira peça")),
        React.createElement("span", null, duracao(b.minutosOciososDia),
          React.createElement("small", null,
            `${(b.minutosOciososDia / Math.max(1, minutosDia)).toFixed(1)} pessoa(s) parada(s)`)))),

    b.equipeDividida && React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--thread)' }
    },
      React.createElement("div", { className: "small muted" },
        React.createElement("strong", null,
          `${b.equipeDividida.informado} pessoa(s) para `,
          `${b.equipeDividida.postos} operações — `,
          `${b.equipeDividida.partidos} delas com menos de uma pessoa. `),
        "A conta divide o tempo de cada uma entre as operações, ",
        "proporcional ao trabalho — é o que acontece quando a costureira ",
        "faz duas coisas em sequência. Na prática isso custa trocas de ",
        "posto, que o cálculo não considera.")),

    b.takt !== null && React.createElement("div", {
      className: "panel",
      style: b.atende
        ? { borderColor: 'var(--ok)', background: 'var(--ok-bg)' }
        : { borderColor: 'var(--danger)', background: 'rgba(176,58,46,.06)' }
    },
      React.createElement("div", { className: "small", style: { fontWeight: 600,
        color: b.atende ? 'var(--ok)' : 'var(--danger)' } },
        b.atende
          ? `A linha dá conta: ${b.saidaDia} peças contra ${b.demandaDia} necessárias.`
          : `Faltam ${b.faltaPorDia} peças por dia.`),
      React.createElement("div", { className: "small muted" },
        `Cada peça precisa sair a cada ${duracao(b.takt)} para dar conta ` +
        `da demanda. Hoje a linha entrega uma a cada `,
        `${duracao(b.cicloLinha)}`,
        b.atende ? '.' : ` — ${duracao(b.cicloLinha - b.takt)} acima do necessário.`)),

    React.createElement("div", { className: "panel", style: { padding: 0 } },
      React.createElement("table", null,
        React.createElement("thead", null,
          React.createElement("tr", null,
            React.createElement("th", null, "Operação"),
            React.createElement("th", { className: "num" }, "Tempo"),
            React.createElement("th", { className: "num" }, "Pessoas"),
            React.createElement("th", { className: "num" }, "Disponível"),
            React.createElement("th", { className: "num" }, "Ciclo"),
            React.createElement("th", null, "Ocupação"),
            React.createElement("th", { className: "num" }, "Espera/dia"))),
        React.createElement("tbody", null,
          b.postos.map(p => React.createElement("tr", {
            key: p.id,
            style: p.gargalo ? { background: 'var(--warn-bg)' } : undefined
          },
            React.createElement("td", null,
              React.createElement("strong", null, `${p.ordem}. ${p.etapa}`),
              React.createElement("div", { className: "small muted" },
                p.setor, p.equipamento ? ` · ${p.equipamento}` : ''),
              p.gargalo && React.createElement("div", {
                className: "small", style: { color: 'var(--warn)', fontWeight: 600 }
              }, "trava a linha")),
            React.createElement("td", { className: "num small" },
              duracao(p.tempoUnitario)),
            React.createElement("td", { className: "num" },
              React.createElement("input", {
                type: "number", min: "1", value: p.pessoas,
                style: {
                  width: 58, textAlign: 'right',
                  borderColor: p.excede ? 'var(--danger)' : undefined
                },
                onChange: e => mudarPessoas(p.id, e.target.value)
              }),
              p.pessoas !== p.pessoasOriginais && React.createElement("div", {
                className: "small muted"
              }, `era ${p.pessoasOriginais}`)),
            React.createElement("td", { className: "num small muted" },
              React.createElement("button", {
                className: "btn ghost sm",
                onClick: () => setEscalando(p.id)
              }, p.escala ? "Trocar equipe" : "Escalar"),
              p.escala && React.createElement("div", { style: { marginTop: 3 } },
                p.escala.membros.map(m => m.nome.split(' ')[0]).join(', '),
                React.createElement("div", null,
                  `base real ${p.escala.baseReal}%`)),
              !p.escala && React.createElement("div", { style: { marginTop: 3 } },
                p.recursos ? `${p.recursos.pessoas} disponível(is)` : '—',
                p.recursos && p.recursos.maquinas > 0 && React.createElement("div", null,
                  `${p.recursos.maquinas} máquina(s)`)),
              p.excede && React.createElement("div", {
                style: { color: 'var(--danger)', fontWeight: 600 }
              }, "acima do limite")),
            React.createElement("td", { className: "num" },
              React.createElement("strong", null, duracao(p.ciclo)),
              React.createElement("div", { className: "small muted" },
                `${p.porHora}/h`)),
            React.createElement("td", null,
              React.createElement("span", { className: "trilho", style: { display: 'block' } },
                React.createElement("span", {
                  className: "preenche",
                  style: {
                    width: `${p.ocupacao}%`,
                    background: p.gargalo ? 'var(--thread)'
                      : p.ocupacao < 60 ? 'var(--danger)' : 'var(--ok)'
                  }
                })),
              React.createElement("span", { className: "small muted" }, `${p.ocupacao}%`)),
            React.createElement("td", { className: "num small muted" },
              p.minutosPerdidosDia > 0 ? duracao(p.minutosPerdidosDia) : '—'))))),

    (() => {
      const meta = metaHoraDaLinha(b.postos, { minutosDia, demandaDia: num(demanda) || null });
      if (!meta) return null;
      const faltam = meta.postos.filter(x => !x.atende);
      return React.createElement("div", { className: "panel" },
        React.createElement("h3", { style: { marginTop: 0 } },
          `Meta por hora para equilibrar: ${meta.alvoHora}/h`),
        React.createElement("div", { className: "small muted", style: { marginBottom: 8 } },
          meta.origem === 'demanda'
            ? `Vem da demanda de ${num(demanda)} peças por dia.`
            : "Vem do gargalo atual — é o ritmo que a linha consegue hoje. Informe a demanda acima para calcular pela necessidade."),
        React.createElement("table", null,
          React.createElement("thead", null,
            React.createElement("tr", null,
              React.createElement("th", null, "Operação"),
              React.createElement("th", { className: "num" }, "Faz"),
              React.createElement("th", { className: "num" }, "Meta"),
              React.createElement("th", { className: "num" }, "Folga"),
              React.createElement("th", null, ""))),
          React.createElement("tbody", null,
            meta.postos.map(x => React.createElement("tr", { key: x.id },
              React.createElement("td", null, x.etapa),
              React.createElement("td", { className: "num" }, `${x.atual}/h`),
              React.createElement("td", { className: "num" }, `${x.meta}/h`),
              React.createElement("td", {
                className: "num",
                style: { color: x.atende ? 'var(--ok)' : 'var(--warn)', fontWeight: 600 }
              }, x.folga > 0 ? `+${x.folga}` : x.folga),
              React.createElement("td", { className: "small muted" },
                x.atende ? 'dá conta'
                  : `faltam ${x.faltamPessoas} pessoa(s) no ritmo atual`))))),
        faltam.length === 0 && React.createElement("div", {
          className: "small muted", style: { marginTop: 8 }
        }, "Todas as operações alcançam a meta. A linha está equilibrada para este ritmo."));
    })(),

    s.melhor && React.createElement("div", { className: "panel", style: { borderColor: 'var(--thread)' } },
      React.createElement("h3", { style: { marginTop: 0 } }, "Onde colocar a próxima pessoa"),
      s.sugestoes.map((x, i) => React.createElement("div", {
        key: x.posto.id, className: "small", style: { marginBottom: 6 }
      },
        React.createElement("strong", null,
          i === 0 ? "→ " : "   ", x.posto.etapa),
        `: ${x.posto.pessoas} → ${x.pessoasDepois} pessoa(s) leva a linha de `,
        `${x.saidaAntes} para ${x.saidaDepois} peças/dia `,
        React.createElement("strong", null, `(+${x.ganho})`),
        x.ganhoPercentual !== null && ` · +${x.ganhoPercentual}%`)),
      React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
        "Reforçar o gargalo funciona até ele deixar de ser o gargalo. Depois ",
        "disso, a pessoa a mais não faz a linha andar mais rápido.")),

    s.podeReduzir.length > 0 && React.createElement("div", { className: "panel" },
      React.createElement("h3", { style: { marginTop: 0 } }, "Onde sobra gente"),
      s.podeReduzir.map(x => React.createElement("div", {
        key: x.posto.id, className: "small", style: { marginBottom: 5 }
      },
        React.createElement("strong", null, x.posto.etapa),
        `: ${x.posto.pessoas} → ${x.pessoasDepois} pessoa(s) sem perder produção `,
        React.createElement("span", { className: "muted" },
          `(ocupação de ${x.posto.ocupacao}%)`))),
      React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
        "Tirar de onde sobra e pôr no gargalo costuma render mais que contratar. ",
        "Confira antes se a pessoa sabe fazer a outra operação.")),

    escalando && (() => {
      const p = b.postos.find(x => x.id === escalando);
      if (!p) return null;
      const passo = (produto.processo || []).find(x => x.id === p.id);
      const esc = escalas[p.id] || { pessoas: [], maquinas: [] };
      return React.createElement(EscalaDaOperacao, {
        posto: { ...p, etapaId: passo ? passo.etapaId : null,
          departamentoId: passo ? passo.departamentoId : null },
        db, escala: esc.pessoas, maquinas: esc.maquinas,
        produtoId: produto.id, update, usuario,
        onChange: v => setEscalas(e => ({ ...e, [p.id]: v })),
        onClose: () => setEscalando(null)
      });
    })(),

    dim && !dim.erro && React.createElement("div", { className: "panel" },
      React.createElement("h3", { style: { marginTop: 0 } },
        `Para produzir ${dim.demanda} por dia`),
      React.createElement("div", { className: "painel-meta" },
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Uma peça a cada"),
          React.createElement("div", { className: "v", style: { fontSize: 15 } },
            duracao(dim.takt))),
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Pessoas hoje"),
          React.createElement("div", { className: "v" }, dim.totalAtual)),
        React.createElement("div", { className: "destaque" },
          React.createElement("div", { className: "k" }, "Pessoas necessárias"),
          React.createElement("div", { className: "v" }, dim.total)),
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Diferença"),
          React.createElement("div", { className: "v" },
            dim.diferenca > 0 ? `+${dim.diferenca}` : dim.diferenca))),
      React.createElement("table", { style: { marginTop: 10 } },
        React.createElement("tbody", null,
          dim.postos.filter(p => p.diferenca !== 0).map(p =>
            React.createElement("tr", { key: p.id },
              React.createElement("td", null, p.etapa),
              React.createElement("td", { className: "num small" },
                `${p.pessoas} → ${p.necessario}`),
              React.createElement("td", { className: "num small muted" },
                p.diferenca > 0 ? `+${p.diferenca}` : p.diferenca))))),
      dim.postos.every(p => p.diferenca === 0) && React.createElement("div", {
        className: "small muted"
      }, "A linha atual já atende esta demanda.")))
  );
}

/* O caminho da peça pela fábrica: trabalhar e esperar. */
function LeadTimeProduto({ produto, db, update, usuario, podeEditar }) {
  const j = jornadaDe(db);
  const [quantidade, setQuantidade] = React.useState(100);
  const [minutosDia, setMinutosDia] = React.useState(j.produtivo);
  const [ajustando, setAjustando] = React.useState(false);

  const lt = leadTimeProduto(produto, db, { quantidade, minutosDia });
  if (lt.vazio) {
    return React.createElement(Empty, {
      text: "Este produto não tem processo produtivo. Sem as operações, não há caminho a percorrer."
    });
  }
  const prev = previsaoComFila(produto, db, { quantidade, minutosDia });
  const cal = calibrarFila(db, produto.id);

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "Somar o tempo das operações dá o tempo de trabalho, não o tempo de ",
      "atravessar a fábrica. Entre uma operação e outra a peça espera — na fila ",
      "do setor, o lote juntar, a máquina liberar. É essa espera que faz a data ",
      "prometida não bater."),

    React.createElement("div", { className: "grid3" },
      React.createElement(Field, { label: "Quantidade da ordem" },
        React.createElement("input", {
          type: "number", min: "1", value: quantidade,
          onChange: e => setQuantidade(Math.max(1, num(e.target.value)))
        })),
      React.createElement(Field, { label: "Minutos por dia" },
        React.createElement("input", {
          type: "number", min: "1", value: minutosDia,
          onChange: e => setMinutosDia(num(e.target.value))
        })),
      podeEditar && React.createElement(Field, { label: " " },
        React.createElement("button", {
          className: "btn ghost", onClick: () => setAjustando(true)
        }, "Ajustar esperas"))),

    /* Duas linhas: os rótulos em cima, os números embaixo. Lado a lado
       fica mais fácil comparar do que em cartões separados. */
    React.createElement("div", { className: "faixa-dados" },
      React.createElement("div", { className: "linha-rotulos" },
        React.createElement("span", null, "Tempo de trabalho"),
        React.createElement("span", null, "Tempo de espera"),
        React.createElement("span", null, "Lead time"),
        React.createElement("span", null, "Tempo sendo trabalhada")),
      React.createElement("div", { className: "linha-valores" },
        React.createElement("span", null, duracao(lt.totalProcesso),
          React.createElement("small", null,
            `${(lt.totalProcesso / minutosDia).toFixed(1)} dia(s)`)),
        React.createElement("span", { className: "alerta" }, duracao(lt.totalFila),
          React.createElement("small", null, `${lt.proporcaoFila}% do total`)),
        React.createElement("span", { className: "destaque" }, `${lt.totalDias} dias`,
          React.createElement("small", null, "atravessar a fábrica")),
        React.createElement("span", null, `${lt.eficienciaFluxo}%`,
          React.createElement("small", null, "a peça é trabalhada")))),

    prev.diasDeEspera > 0 && React.createElement("div", {
      className: "panel", style: { borderColor: 'var(--warn)', background: 'var(--warn-bg)' }
    },
      React.createElement("div", { className: "small", style: { fontWeight: 600 } },
        `A espera acrescenta ${prev.diasDeEspera} dia(s) à data de entrega.`),
      React.createElement("div", { className: "small muted" },
        `Contando só o trabalho, a ordem ficaria pronta em `,
        React.createElement("strong", null, fmtDate(prev.dataSemFila)),
        `. Contando a fábrica como ela é, `,
        React.createElement("strong", null, fmtDate(prev.data)),
        `. Prometer a primeira data é prometer o que não se cumpre.`)),

    React.createElement("div", { className: "panel", style: { padding: 0 } },
      React.createElement("table", null,
        React.createElement("thead", null,
          React.createElement("tr", null,
            React.createElement("th", null, "Etapa"),
            React.createElement("th", { className: "num" }, "Trabalho"),
            React.createElement("th", { className: "num" }, "Espera"),
            React.createElement("th", null, "Proporção"),
            React.createElement("th", { className: "num" }, "Total"))),
        React.createElement("tbody", null,
          lt.etapas.map(e => {
            const pFila = e.total > 0 ? (e.fila / e.total * 100) : 0;
            return React.createElement("tr", { key: e.ordem },
              React.createElement("td", null,
                React.createElement("strong", null, `${e.ordem}. ${e.etapa}`),
                React.createElement("div", { className: "small muted" }, e.setor)),
              React.createElement("td", { className: "num" }, duracao(e.processo)),
              React.createElement("td", { className: "num" },
                e.fila > 0 ? duracao(e.fila) : '—',
                e.fila > 0 && React.createElement("div", { className: "small muted" },
                  `${e.filaHoras}h de fila`)),
              React.createElement("td", null,
                React.createElement("span", { className: "trilho", style: { display: 'block' } },
                  React.createElement("span", {
                    className: "preenche",
                    style: { width: `${100 - pFila}%`, background: 'var(--ok)' }
                  })),
                React.createElement("span", { className: "small muted" },
                  pFila > 0 ? `${(100 - pFila).toFixed(0)}% trabalho` : 'só trabalho')),
              React.createElement("td", { className: "num" },
                React.createElement("strong", null, duracao(e.total))));
          }))),

    !cal.vazio && React.createElement("div", {
      className: "panel",
      style: { borderColor: cal.ajuste === 'ok' ? 'var(--ok)' : 'var(--thread)' }
    },
      React.createElement("h3", { style: { marginTop: 0 } },
        "O previsto bate com o que aconteceu?"),
      React.createElement("div", { className: "painel-meta" },
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Previsto"),
          React.createElement("div", { className: "v" }, `${cal.previsto.dias}d`)),
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Real (mediana)"),
          React.createElement("div", { className: "v" }, `${cal.real.mediana}d`)),
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Diferença"),
          React.createElement("div", { className: "v" },
            `${cal.diferenca > 0 ? '+' : ''}${cal.diferenca}d`)),
        React.createElement("div", null,
          React.createElement("div", { className: "k" }, "Ordens medidas"),
          React.createElement("div", { className: "v" }, cal.real.amostra))),
      React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
        cal.ajuste === 'ok'
          ? "A espera configurada está batendo com a realidade."
          : cal.ajuste === 'aumentar'
            ? `As ordens estão levando ${cal.diferenca} dia(s) a mais que o previsto. A espera configurada está baixa — aumente, ou a data prometida vai continuar errando.`
            : `As ordens estão saindo ${Math.abs(cal.diferenca)} dia(s) antes do previsto. A espera configurada está alta — dá para prometer prazo menor.`,
        !cal.confiavel && ` Atenção: só ${cal.real.amostra} ordem(ns) medida(s) — pouco para concluir.`)),

    cal.vazio && cal.aviso && React.createElement("div", { className: "panel" },
      React.createElement("div", { className: "small muted" }, cal.aviso)),

    ajustando && React.createElement(FilasModal, {
      db, update, usuario, onClose: () => setAjustando(false)
    }))
  );
}

function FilasModal({ db, update, usuario, onClose }) {
  const atual = db.parametrosFila || {};
  const [filas, setFilas] = React.useState({ ...atual });
  const set = (k, v) => setFilas(p => ({ ...p, [k]: num(v) }));

  return React.createElement(Modal, {
    title: "Tempo de espera entre setores", onClose, wide: true
  },
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "Quanto tempo, em média, uma peça espera antes de começar a ser ",
      "trabalhada em cada setor. Se você não sabe o número, comece com 4 horas ",
      "e deixe as primeiras ordens mostrarem o real — o sistema compara e avisa ",
      "quando o valor está errado."),

    React.createElement(Field, { label: "Padrão para todos os setores (horas)" },
      React.createElement("input", {
        type: "number", min: "0", step: "0.5", style: { maxWidth: 140 },
        value: filas.padrao !== undefined ? filas.padrao : FILA_PADRAO_HORAS,
        onChange: e => set('padrao', e.target.value)
      })),

    React.createElement("div", { className: "etp-rot", style: { marginTop: 12, marginBottom: 6 } },
      "Ou por setor, quando a diferença importa"),
    React.createElement("div", { className: "grid2" },
      (db.departamentos || []).filter(d => d.ativo !== false).map(dep =>
        React.createElement(Field, { key: dep.id, label: dep.nome },
          React.createElement("input", {
            type: "number", min: "0", step: "0.5",
            placeholder: `${filas.padrao !== undefined ? filas.padrao : FILA_PADRAO_HORAS} (padrão)`,
            value: filas[dep.id] !== undefined ? filas[dep.id] : '',
            onChange: e => {
              const v = e.target.value;
              setFilas(p => {
                const n = { ...p };
                if (v === '') delete n[dep.id]; else n[dep.id] = num(v);
                return n;
              });
            }
          })))),

    React.createElement("div", { className: "small muted", style: { marginTop: 10 } },
      "Setor que trabalha por lote grande — estamparia, bordado, terceirizado — ",
      "costuma ter espera bem maior. Vale medir antes de estimar."),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        onClick: () => {
          update(d => { salvarFilas(d, filas, usuario); return d; });
          onClose();
        }
      }, "Salvar"))
  );
}

function CustosFixos({ db, update, usuario, podeEditar }) {
  const [editando, setEditando] = React.useState(null);
  const [config, setConfig] = React.useState(false);
  const tx = taxaCustoIndireto(db);
  const cap = tx.capacidade;

  return React.createElement("div", null,
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "Aluguel, energia, manutenção e supervisão existem mesmo quando a fábrica ",
      "está parada. Dividir esses custos pelos minutos de produção dá quanto ",
      "cada peça carrega de fábrica — e é o que faltava para o custo ser o custo."),

    React.createElement("div", { className: "cards" },
      React.createElement("div", { className: "card" },
        React.createElement("div", { className: "k" }, "Custo fixo mensal"),
        React.createElement("div", { className: "v" }, money(tx.total)),
        React.createElement("div", { className: "small muted" },
          `${tx.lista.length} item(ns)`)),
      React.createElement("div", { className: "card" },
        React.createElement("div", { className: "k" }, "Capacidade do mês"),
        React.createElement("div", { className: "v" },
          cap.minutosReais.toLocaleString('pt-BR')),
        React.createElement("div", { className: "small muted" },
          `minutos · ${cap.pessoas} pessoa(s) · ${cap.ocupacao}% de ocupação`)),
      React.createElement("div", { className: "card destaque" },
        React.createElement("div", { className: "k" }, "Custo por minuto"),
        React.createElement("div", { className: "v" },
          tx.porMinuto > 0 ? `R$ ${tx.porMinuto.toFixed(4)}` : '—'),
        React.createElement("div", { className: "small muted" },
          tx.porHora > 0 ? `${money(tx.porHora)} por hora de fábrica` : 'sem base')),
      React.createElement("div", { className: "card" },
        React.createElement("div", { className: "k" }, "Numa peça de 10 min"),
        React.createElement("div", { className: "v" },
          tx.porMinuto > 0 ? money(tx.porMinuto * 10) : '—'),
        React.createElement("div", { className: "small muted" }, "só de fábrica"))),

    tx.avisos.length > 0 && React.createElement("div", { className: "panel", style: { borderColor: 'var(--warn)', background: 'var(--warn-bg)' } },
      tx.avisos.map((a, i) => React.createElement("div", {
        key: i, className: "small muted", style: { marginBottom: 4 }
      }, a))),

    React.createElement("div", { className: "row-actions", style: { marginBottom: 10 } },
      podeEditar && React.createElement("button", {
        className: "btn accent sm",
        onClick: () => setEditando({ tipo: 'instalacao', ativo: true })
      }, "+ Custo fixo"),
      podeEditar && React.createElement("button", {
        className: "btn ghost sm", onClick: () => setConfig(true)
      }, "Ajustar capacidade"),
      React.createElement("span", { style: { flex: 1 } })),

    tx.lista.length === 0
      ? React.createElement(Empty, {
          text: "Nenhum custo fixo cadastrado. Sem eles, o custo do produto fica incompleto."
        })
      : React.createElement("div", { className: "panel", style: { padding: 0 } },
          React.createElement("table", null,
            React.createElement("thead", null,
              React.createElement("tr", null,
                React.createElement("th", null, "Custo"),
                React.createElement("th", null, "Tipo"),
                React.createElement("th", { className: "num" }, "Por mês"),
                React.createElement("th", { className: "num" }, "Por minuto"),
                React.createElement("th", { className: "num" }, "Peso"),
                podeEditar && React.createElement("th", { style: { width: 70 } }))),
            React.createElement("tbody", null,
              (db.custosFixos || []).map(c => {
                const inativo = c.ativo === false;
                const peso = tx.total > 0 && !inativo
                  ? (num(c.valorMensal) / tx.total * 100) : 0;
                return React.createElement("tr", {
                  key: c.id, style: inativo ? { opacity: .5 } : undefined
                },
                  React.createElement("td", null,
                    React.createElement("strong", null, c.nome),
                    c.observacao && React.createElement("div", { className: "small muted" },
                      c.observacao),
                    inativo && React.createElement("div", { className: "small muted" },
                      "fora da conta")),
                  React.createElement("td", { className: "small" },
                    tipoCustoFixo(c.tipo).nome),
                  React.createElement("td", { className: "num" }, money(c.valorMensal)),
                  React.createElement("td", { className: "num small" },
                    cap.minutosReais > 0 && !inativo
                      ? `R$ ${(num(c.valorMensal) / cap.minutosReais).toFixed(4)}` : '—'),
                  React.createElement("td", { className: "num small" },
                    peso > 0 ? `${peso.toFixed(1)}%` : '—'),
                  podeEditar && React.createElement("td", null,
                    React.createElement("button", {
                      className: "btn-ic", onClick: () => setEditando({ ...c })
                    }, "✎"),
                    React.createElement("button", {
                      className: "btn-ic ic-danger",
                      onClick: () => {
                        if (!confirm(`Remover "${c.nome}" da conta de custos?`)) return;
                        update(d => { removerCustoFixo(d, c.id, usuario); return d; });
                      }
                    }, "×")));
              })))),

    tx.porTipo.length > 0 && React.createElement("div", { className: "panel" },
      React.createElement("h3", null, "Para onde vai o custo fixo"),
      tx.porTipo.map(t => React.createElement("div", {
        key: t.id, className: "barra-linha"
      },
        React.createElement("span", { className: "rot" }, t.nome),
        React.createElement("span", { className: "trilho" },
          React.createElement("span", {
            className: "preenche",
            style: { width: `${(t.total / tx.porTipo[0].total * 100)}%` }
          })),
        React.createElement("span", { className: "qtd" }, money(t.total)))),
      React.createElement("div", { className: "small muted", style: { marginTop: 8 } },
        "O maior deles costuma ser o melhor lugar para procurar economia — ",
        "mas confira se dá para mexer antes de contar com isso.")),

    editando && React.createElement(CustoFixoModal, {
      custo: editando, update, usuario, onClose: () => setEditando(null)
    }),
    config && React.createElement(CapacidadeModal, {
      db, update, usuario, onClose: () => setConfig(false)
    })
  );
}

function CustoFixoModal({ custo, update, usuario, onClose }) {
  const [f, setF] = React.useState({ ...custo });
  const set = (k, v) => setF(p => ({ ...p, [k]: v }));

  return React.createElement(Modal, {
    title: custo.id ? "Editar custo fixo" : "Novo custo fixo", onClose
  },
    React.createElement(Field, { label: "Do que se trata *" },
      React.createElement("input", {
        value: f.nome || '', autoFocus: true,
        placeholder: "ex: aluguel do galpão",
        onChange: e => set('nome', e.target.value)
      })),
    React.createElement(Field, { label: "Tipo" },
      React.createElement("select", {
        value: f.tipo, onChange: e => set('tipo', e.target.value)
      },
        TIPOS_CUSTO_FIXO.map(t => React.createElement("option", {
          key: t.id, value: t.id
        }, t.nome)))),
    React.createElement("div", { className: "small muted", style: { marginTop: -6, marginBottom: 10 } },
      tipoCustoFixo(f.tipo).ajuda),
    React.createElement(Field, { label: "Valor por mês *" },
      React.createElement("input", {
        type: "number", min: "0", step: "0.01", style: { maxWidth: 180 },
        value: f.valorMensal || '',
        onChange: e => set('valorMensal', e.target.value)
      })),
    React.createElement("div", { className: "small muted", style: { marginTop: -6, marginBottom: 10 } },
      "Use a média dos últimos meses. Conta que varia muito — energia no ",
      "verão, por exemplo — vale usar o valor mais alto: errar para cima no ",
      "custo é menos perigoso que errar para baixo."),
    React.createElement(Field, { label: "Observação" },
      React.createElement("input", {
        value: f.observacao || '',
        onChange: e => set('observacao', e.target.value)
      })),
    React.createElement("label", { className: "check" },
      React.createElement("input", {
        type: "checkbox", checked: f.ativo !== false,
        onChange: e => set('ativo', e.target.checked)
      }),
      React.createElement("span", null, "Entra na conta do custo")),
    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        disabled: !String(f.nome || '').trim() || !(num(f.valorMensal) > 0),
        onClick: () => {
          let erro = null;
          update(d => { const r = salvarCustoFixo(d, f, usuario); erro = r.erro || null; return d; });
          if (erro) alert(erro); else onClose();
        }
      }, "Salvar"))
  );
}

function CapacidadeModal({ db, update, usuario, onClose }) {
  const atual = db.parametrosCustoIndireto || {};
  const [ocupacao, setOcupacao] = React.useState(
    num(atual.ocupacao) || OCUPACAO_PADRAO);
  const [dias, setDias] = React.useState(
    num(atual.diasUteis) || num((db.parametrosMaoDeObra || {}).diasUteis) || DIAS_UTEIS_MES);

  const previa = capacidadeProdutivaMes({ ...db,
    parametrosCustoIndireto: { ocupacao, diasUteis: dias } });
  const total = (db.custosFixos || []).filter(c => c.ativo !== false)
    .reduce((s, c) => s + num(c.valorMensal), 0);

  return React.createElement(Modal, { title: "Capacidade da fábrica", onClose, wide: true },
    React.createElement("div", { className: "small muted", style: { marginBottom: 12 } },
      "É por este número que o custo fixo é dividido. Quanto menor a ",
      "capacidade, mais caro fica cada minuto — e é por isso que fábrica ",
      "vazia tem custo por peça mais alto."),

    React.createElement("div", { className: "grid2" },
      React.createElement(Field, { label: "Dias úteis por mês" },
        React.createElement("input", {
          type: "number", min: "1", max: "31", value: dias,
          onChange: e => setDias(num(e.target.value))
        })),
      React.createElement(Field, { label: "Ocupação real (%)" },
        React.createElement("input", {
          type: "number", min: "1", max: "100", value: ocupacao,
          onChange: e => setOcupacao(num(e.target.value))
        }))),

    React.createElement("div", { className: "small muted", style: { marginTop: -6, marginBottom: 12 } },
      React.createElement("strong", null, "Sobre a ocupação: "),
      "ninguém produz 100% do tempo disponível. Falta material, troca-se de ",
      "produto, a máquina para. Se você não sabe o número, 85% é um ponto de ",
      "partida razoável — e o próprio sistema vai mostrar o real conforme os ",
      "apontamentos entrarem."),

    React.createElement("div", { className: "painel-meta" },
      React.createElement("div", null,
        React.createElement("div", { className: "k" }, "Pessoas na produção"),
        React.createElement("div", { className: "v" }, previa.pessoas)),
      React.createElement("div", null,
        React.createElement("div", { className: "k" }, "Minutos teóricos"),
        React.createElement("div", { className: "v", style: { fontSize: 15 } },
          previa.minutosTeoricos.toLocaleString('pt-BR'))),
      React.createElement("div", null,
        React.createElement("div", { className: "k" }, "Minutos reais"),
        React.createElement("div", { className: "v", style: { fontSize: 15 } },
          previa.minutosReais.toLocaleString('pt-BR'))),
      React.createElement("div", { className: "destaque" },
        React.createElement("div", { className: "k" }, "Custo por minuto"),
        React.createElement("div", { className: "v", style: { fontSize: 15 } },
          previa.minutosReais > 0
            ? `R$ ${(total / previa.minutosReais).toFixed(4)}` : '—'))),

    React.createElement("div", { className: "modal-actions" },
      React.createElement("button", { className: "btn ghost", onClick: onClose }, "Cancelar"),
      React.createElement("button", {
        className: "btn accent",
        onClick: () => {
          update(d => {
            d.parametrosCustoIndireto = { ...(d.parametrosCustoIndireto || {}),
              ocupacao, diasUteis: dias };
            registrarLog(d, usuario, 'custo.capacidade',
              `ocupação ${ocupacao}% · ${dias} dias`);
            return d;
          });
          onClose();
        }
      }, "Salvar"))
  );
}

function GrupoCanal({
  db,
  update,
  usuario,
  perm
}) {
  const [sub, setSub] = useState('canal');
  const pode = a => podeExecutar(perm, a);
  const abas = [{
    id: 'canal',
    label: '💬 Conversa aberta'
  }, {
    id: 'tratamento',
    label: `Manifestações (${manifestacoesVisiveis(db, usuario).filter(m => ABERTOS_CANAL.includes(m.status)).length})`
  }, {
    id: 'indicadores',
    label: 'Indicadores'
  }, {
    id: 'vagas',
    label: `Vagas (${vagasAbertas(db).length})`
  }, {
    id: 'qrcodes',
    label: 'QR Codes'
  }];
  /* a relação completa reúne denúncia com sugestão: é do administrador */
  if (podeVerRelatorioCanal(usuario)) abas.push({
    id: 'relatorios',
    label: 'Relatórios'
  });
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Canal do colaborador"), /*#__PURE__*/React.createElement("h2", null, "Conversa aberta"))), /*#__PURE__*/React.createElement("div", {
    className: "subtabs"
  }, abas.map(a => /*#__PURE__*/React.createElement("button", {
    key: a.id,
    className: sub === a.id ? 'on' : '',
    onClick: () => setSub(a.id)
  }, a.label))), sub === 'canal' && /*#__PURE__*/React.createElement(CanalColaborador, {
    db: db,
    update: update,
    usuario: usuario
  }), sub === 'tratamento' && /*#__PURE__*/React.createElement(CanalTratamento, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: pode('pessoas')
  }), sub === 'indicadores' && /*#__PURE__*/React.createElement(CanalIndicadores, {
    db: db,
    usuario: usuario
  }), sub === 'vagas' && /*#__PURE__*/React.createElement(GestaoVagas, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: pode('config') || pode('tratar')
  }), sub === 'qrcodes' && /*#__PURE__*/React.createElement(CanalQRCodes, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: pode('config')
  }), sub === 'relatorios' && /*#__PURE__*/React.createElement(RelatoriosCanal, {
    db: db,
    usuario: usuario
  }));
}

/* A fila de quem trata: o que chegou, o que está atrasado, o que falta. */
function CanalTratamento({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [aberta, setAberta] = useState(null);
  const [fStatus, setFStatus] = useState('abertas');
  const [fCategoria, setFCategoria] = useState('');
  const [fDep, setFDep] = useState('');
  const [busca, setBusca] = useState('');
  const visiveis = manifestacoesVisiveis(db, usuario);
  const lista = visiveis.filter(m => {
    if (fStatus === 'abertas' && !ABERTOS_CANAL.includes(m.status)) return false;
    if (fStatus === 'atrasadas' && !prazoManifestacao(m).atrasada) return false;
    if (fStatus !== 'abertas' && fStatus !== 'atrasadas' && fStatus && m.status !== fStatus) return false;
    if (fCategoria && m.categoria !== fCategoria) return false;
    if (fDep && m.departamentoId !== fDep) return false;
    const t = normaliza(busca);
    if (t && !normaliza(`${m.protocolo} ${m.descricao} ${m.local}`).includes(t)) return false;
    return true;
  }).sort((a, b) => {
    const ordem = PRIORIDADES_CANAL.map(p => p.id);
    const d = ordem.indexOf(b.prioridade) - ordem.indexOf(a.prioridade);
    return d !== 0 ? d : String(b.criadaEm).localeCompare(String(a.criadaEm));
  });
  if (aberta) {
    const m = (db.manifestacoes || []).find(x => x.id === aberta);
    if (!m) return /*#__PURE__*/React.createElement(Empty, {
      text: "Manifestação não encontrada."
    });
    return /*#__PURE__*/React.createElement(CanalDetalhe, {
      manifestacao: m,
      db: db,
      update: update,
      usuario: usuario,
      podeEditar: podeEditar,
      onVoltar: () => setAberta(null)
    });
  }
  const escondidas = (db.manifestacoes || []).length - visiveis.length;
  return /*#__PURE__*/React.createElement("div", null, (() => {
    const abertas = visiveis.filter(m => ABERTOS_CANAL.includes(m.status));
    const atrasadas = abertas.filter(m => prazoManifestacao(m).atrasada);
    const criticas = abertas.filter(m => m.prioridade === 'critica');
    const novas = abertas.filter(m => m.status === 'recebido');
    if (abertas.length === 0) return null;
    return /*#__PURE__*/React.createElement("div", { className: "faixa-dados" },
    /*#__PURE__*/React.createElement("div", { className: "linha-rotulos" },
      /*#__PURE__*/React.createElement("span", null, "Em aberto"),
      /*#__PURE__*/React.createElement("span", null, "Críticas"),
      /*#__PURE__*/React.createElement("span", null, "Fora do prazo")),
    /*#__PURE__*/React.createElement("div", { className: "linha-valores" },
      
      /*#__PURE__*/React.createElement("span", null,
        abertas.length,
        /*#__PURE__*/React.createElement("small", null, novas.length, " sem leitura")),
      /*#__PURE__*/React.createElement("span", { className: (criticas.length ? 'alerta' : '') },
        criticas.length,
        /*#__PURE__*/React.createElement("small", null, "prazo de 1 dia")),
      /*#__PURE__*/React.createElement("span", { className: (atrasadas.length ? 'alerta' : '') },
        atrasadas.length,
        /*#__PURE__*/React.createElement("small", null, atrasadas.length ? 'precisam de resposta' : 'tudo em dia'))));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Ordenadas por prioridade. Relatos confidenciais e denúncias só aparecem para quem tem acesso a eles."), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: fStatus,
    onChange: e => setFStatus(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: "abertas"
  }, "Em aberto"), /*#__PURE__*/React.createElement("option", {
    value: "atrasadas"
  }, "Fora do prazo"), /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas"), STATUS_CANAL.map(s => /*#__PURE__*/React.createElement("option", {
    key: s.id,
    value: s.id
  }, s.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: fCategoria,
    onChange: e => setFCategoria(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), CATEGORIAS_CANAL.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Setor"
  }, /*#__PURE__*/React.createElement("select", {
    value: fDep,
    onChange: e => setFDep(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.departamentos || []).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome))))), /*#__PURE__*/React.createElement(Field, {
    label: "Buscar"
  }, /*#__PURE__*/React.createElement("input", {
    value: busca,
    placeholder: "protocolo, palavra do relato, local…",
    onChange: e => setBusca(e.target.value)
  }))), escondidas > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, escondidas, " manifestação(ões) não aparecem para o seu perfil."), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma manifestação com esses filtros."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Protocolo"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Relato"), /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", null, "Prazo"))), /*#__PURE__*/React.createElement("tbody", null, lista.map(m => {
    const p = prazoManifestacao(m);
    const dep = (db.departamentos || []).find(d => d.id === m.departamentoId);
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id,
      onClick: () => setAberta(m.id),
      style: {
        cursor: 'pointer'
      }
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, m.protocolo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, prioridadeCanal(m.prioridade).marca, ' ', prioridadeCanal(m.prioridade).nome, m.anonima && ' · anônima')), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, categoriaCanal(m.categoria).icone, ' ', categoriaCanal(m.categoria).nome, m.sensivel && /*#__PURE__*/React.createElement("div", {
      className: "tag"
    }, "restrito")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, String(m.descricao || '').slice(0, 80), String(m.descricao || '').length > 80 ? '…' : '', (m.temas || []).length > 0 && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, (m.temas || []).map(t => {
      const x = TEMAS_CANAL.find(y => y.id === t);
      return x ? x.nome : t;
    }).join(' · '))), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, dep ? dep.nome : '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, statusCanal(m.status).nome)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, p.atrasada ? /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--danger)',
        fontWeight: 600
      }
    }, "venceu ", fmtDate(p.limite)) : /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, fmtDate(p.limite))));
  })))));
}

/* A manifestação por dentro: o relato, o plano e a conversa. */
function CanalDetalhe({
  manifestacao,
  db,
  update,
  usuario,
  podeEditar,
  onVoltar
}) {
  const m = manifestacao;
  const [nota, setNota] = useState('');
  const [resposta, setResposta] = useState('');
  const [acao, setAcao] = useState(null);
  const cat = categoriaCanal(m.categoria);
  const p = prazoManifestacao(m);
  const dep = (db.departamentos || []).find(d => d.id === m.departamentoId);
  const equipe = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
  const mudar = novo => update(d => {
    const r = moverManifestacao(d, m.id, novo, usuario, nota);
    if (r.erro) alert(r.erro);else setNota('');
    return d;
  });
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar"), /*#__PURE__*/React.createElement("div", {
    className: "page-head",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, cat.icone, " ", cat.nome), /*#__PURE__*/React.createElement("h2", null, m.protocolo), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "aberta em ", fmtDataHora(m.criadaEm), ' · ', m.anonima ? 'anônima' : `${m.nome || 'sem nome'}${m.matricula ? ' · ' + m.matricula : ''}`))), m.sinalUrgencia && ABERTOS_CANAL.includes(m.status) && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--danger)',
      background: 'rgba(176,58,46,.06)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 700
    }
  }, "O texto tem palavras que sugerem urgência."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Isto é um sinal para leitura humana imediata — não é uma conclusão do sistema sobre o que aconteceu.")), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Situação"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, statusCanal(m.status).nome)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Prioridade"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, prioridadeCanal(m.prioridade).marca, " ", prioridadeCanal(m.prioridade).nome)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Prazo"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15,
      color: p.atrasada ? 'var(--danger)' : undefined
    }
  }, fmtDate(p.limite))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Setor"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, dep ? dep.nome : '—'))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -4,
      marginBottom: 10
    }
  }, "Encaminhamento sugerido: ", /*#__PURE__*/React.createElement("strong", null, {
    setor: 'responsável do setor',
    producao: 'produção / PCP',
    sst: 'responsável pela segurança do trabalho',
    rh: 'RH',
    direcao: 'RH e direção'
  }[m.destino] || 'responsável designado'), ".", m.querRetorno && ' A pessoa pediu retorno.'), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "O relato"), /*#__PURE__*/React.createElement("div", {
    style: {
      whiteSpace: 'pre-wrap'
    }
  }, m.descricao), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Quando"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, fmtDate(m.dataOcorrencia))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Local"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, m.local || '—')), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Frequência"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, m.frequencia)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Impacto"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, m.impacto))), m.envolvidos && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 8
    }
  }, "Pessoas citadas: ", m.envolvidos), (m.anexos || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "anexos",
    style: {
      marginTop: 10
    }
  }, (m.anexos || []).map(x => /*#__PURE__*/React.createElement("a", {
    key: x.id,
    className: "anexo-chip",
    href: x.url,
    target: "_blank",
    rel: "noreferrer",
    download: x.nome
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome)))), (m.temas || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      marginTop: 10,
      background: 'var(--canvas-panel)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, /*#__PURE__*/React.createElement("strong", null, "Possível classificação — precisa de análise humana."), ' ', "O sistema achou palavras ligadas a:", ' ', (m.temas || []).map(t => {
    const x = TEMAS_CANAL.find(y => y.id === t);
    return x ? x.nome : t;
  }).join(', '), ". Isso serve para orientar a leitura, não para afirmar o que houve."))), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Andamento"), /*#__PURE__*/React.createElement(Field, {
    label: "Nota do andamento (fica no histórico interno)"
  }, /*#__PURE__*/React.createElement("input", {
    value: nota,
    placeholder: "o que foi apurado, o que foi decidido…",
    onChange: e => setNota(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, STATUS_CANAL.filter(s => s.id !== m.status).map(s => /*#__PURE__*/React.createElement("button", {
    key: s.id,
    className: "btn ghost sm",
    onClick: () => mudar(s.id)
  }, s.nome))), (() => {
    const pendentes = (m.acoes || []).filter(a => a.status !== 'concluido' && a.status !== 'cancelado').length;
    if (pendentes === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 6
      }
    }, pendentes, " ação(ões) do plano em aberto — resolver e encerrar só depois que elas forem concluídas ou canceladas.");
  })()), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      flex: 1,
      margin: 0
    }
  }, "Plano de ação"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setAcao({
      oQue: '',
      responsavelId: '',
      prazo: '',
      status: 'nao_iniciado',
      evidencia: '',
      anexos: []
    })
  }, "+ Ação")), (m.acoes || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma ação registrada. Toda providência precisa de responsável e prazo.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "O que será feito"), /*#__PURE__*/React.createElement("th", null, "Responsável"), /*#__PURE__*/React.createElement("th", null, "Prazo"), /*#__PURE__*/React.createElement("th", null, "Situação"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 50
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (m.acoes || []).map(a => {
    const resp = (db.colaboradores || []).find(c => c.id === a.responsavelId);
    const atrasada = a.status !== 'concluido' && a.status !== 'cancelado' && a.prazo && todayISO() > a.prazo;
    return /*#__PURE__*/React.createElement("tr", {
      key: a.id
    }, /*#__PURE__*/React.createElement("td", null, a.oQue, a.evidencia && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, a.evidencia), (a.anexos || []).length > 0 && /*#__PURE__*/React.createElement("div", {
      className: "anexos",
      style: {
        marginTop: 5
      }
    }, (a.anexos || []).map(x => /*#__PURE__*/React.createElement("a", {
      key: x.id,
      className: "anexo-chip",
      href: x.url,
      target: "_blank",
      rel: "noreferrer",
      download: x.nome
    }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
      src: x.url,
      alt: x.nome
    }) : /*#__PURE__*/React.createElement("span", {
      className: "anexo-ic"
    }, "📄"), /*#__PURE__*/React.createElement("span", {
      className: "small"
    }, x.nome))))), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, resp ? resp.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small",
      style: atrasada ? {
        color: 'var(--danger)',
        fontWeight: 600
      } : undefined
    }, fmtDate(a.prazo)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, (STATUS_ACAO.find(x => x.id === a.status) || {}).nome)), podeEditar && /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-accent",
      title: "Editar",
      onClick: () => setAcao(a)
    }, "✎")));
  })))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Retorno ao colaborador"), m.querRetorno ? /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "A pessoa pediu retorno. O que você escrever aqui aparece para ela na consulta pelo protocolo.") : /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "A pessoa não pediu retorno, mas a resposta fica disponível pelo protocolo."), (m.interacoes || []).map(i => /*#__PURE__*/React.createElement("div", {
    key: i.id,
    className: "canal-resposta" + (i.interno ? ' interno' : '')
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, fmtDataHora(i.quando), " · ", i.quem, i.interno && ' · nota interna'), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, i.texto))), podeEditar && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Field, {
    label: "Escrever"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: resposta,
    placeholder: "fale do que foi feito, sem expor terceiros",
    onChange: e => setResposta(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 8
    }
  }, "O que for enviado ao colaborador fica visível para quem tiver o protocolo. Conte o que foi feito sem detalhar situação de outras pessoas nem resultado de apuração sobre alguém."), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    disabled: !resposta.trim(),
    onClick: () => {
      update(d => {
        const r = responderManifestacao(d, m.id, resposta, usuario, true);
        if (r.erro) alert(r.erro);
        return d;
      });
      setResposta('');
    }
  }, "Responder ao colaborador"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: !resposta.trim(),
    onClick: () => {
      update(d => {
        const r = responderManifestacao(d, m.id, resposta, usuario, false);
        if (r.erro) alert(r.erro);
        return d;
      });
      setResposta('');
    }
  }, "Guardar como nota interna")))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Histórico"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, (m.historico || []).slice().reverse().map(h => /*#__PURE__*/React.createElement("tr", {
    key: h.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, h.o_que, h.nota && /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, h.nota)), /*#__PURE__*/React.createElement("td", {
    className: "small muted",
    style: {
      width: 150
    }
  }, h.quem, /*#__PURE__*/React.createElement("div", null, fmtDataHora(h.quando))))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Manifestações não são apagadas. O histórico guarda cada mudança.")), acao && /*#__PURE__*/React.createElement(Modal, {
    title: acao.id ? 'Editar ação' : 'Nova ação',
    onClose: () => setAcao(null)
  }, /*#__PURE__*/React.createElement(Field, {
    label: "O que será feito *"
  }, /*#__PURE__*/React.createElement("input", {
    value: acao.oQue,
    autoFocus: true,
    onChange: e => setAcao({
      ...acao,
      oQue: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Responsável *"
  }, /*#__PURE__*/React.createElement("select", {
    value: acao.responsavelId,
    onChange: e => setAcao({
      ...acao,
      responsavelId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), equipe.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Prazo *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: acao.prazo,
    onChange: e => setAcao({
      ...acao,
      prazo: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: acao.status,
    onChange: e => setAcao({
      ...acao,
      status: e.target.value
    })
  }, STATUS_ACAO.map(s => /*#__PURE__*/React.createElement("option", {
    key: s.id,
    value: s.id
  }, s.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Evidência"
  }, /*#__PURE__*/React.createElement("input", {
    value: acao.evidencia,
    placeholder: "o que comprova a execução",
    onChange: e => setAcao({
      ...acao,
      evidencia: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot"
  }, "Arquivos que comprovam"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 6
    }
  }, "Foto do que foi feito, nota fiscal do reparo, lista de presença do treinamento. Até 900 KB por arquivo."), /*#__PURE__*/React.createElement("div", {
    className: "anexos",
    style: {
      marginBottom: 10
    }
  }, (acao.anexos || []).map(x => /*#__PURE__*/React.createElement("span", {
    key: x.id,
    className: "anexo-chip"
  }, (x.tipo || '').startsWith('image/') ? /*#__PURE__*/React.createElement("img", {
    src: x.url,
    alt: x.nome
  }) : /*#__PURE__*/React.createElement("span", {
    className: "anexo-ic"
  }, "📄"), /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, x.nome), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    title: "Remover",
    onClick: () => setAcao({
      ...acao,
      anexos: (acao.anexos || []).filter(y => y.id !== x.id)
    })
  }, "×"))), (acao.anexos || []).length === 0 && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "nenhum arquivo"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📎 Anexar", /*#__PURE__*/React.createElement("input", {
    type: "file",
    style: {
      display: 'none'
    },
    onChange: async e => {
      const file = (e.target.files || [])[0];
      e.target.value = '';
      if (!file) return;
      if (file.size > 900 * 1024) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e ` + `passa do limite de 900 KB. Use uma foto menor.`);
      try {
        const url = await new Promise((res, rej) => {
          const r = new FileReader();
          r.onload = () => res(r.result);
          r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
          r.readAsDataURL(file);
        });
        setAcao(p => ({
          ...p,
          anexos: [...(p.anexos || []), {
            id: uid(),
            nome: file.name,
            tamanho: file.size,
            tipo: file.type || '',
            url
          }]
        }));
      } catch (err) {
        alert(err.message);
      }
    }
  }))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setAcao(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = salvarAcaoCanal(d, m.id, acao, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setAcao(null);
    }
  }, "Salvar"))));
}

/* O que o RH e a SST precisam ver para agir — e o que o sistema
   deliberadamente não conclui sozinho. */
function CanalIndicadores({
  db,
  usuario
}) {
  const [dias, setDias] = useState(30);
  const corte = (() => {
    const x = new Date(todayISO());
    x.setDate(x.getDate() - num(dias));
    return x.toISOString().slice(0, 10);
  })();
  const ind = indicadoresCanal(db, {
    de: corte
  });
  const tend = tendenciasCanal(db, dias);
  const reinc = reincidenciasCanal(db, dias);
  const clima = climaResumo(db, dias);
  const podeVerTudo = usuario && (usuario.perfil === 'Administrador' || usuario.perfil === 'Gestor');
  const barra = (lista, chave) => {
    const maior = Math.max(1, ...lista.map(x => x.total));
    return lista.map(x => /*#__PURE__*/React.createElement("div", {
      key: x.id || x.nome,
      className: "barra-linha"
    }, /*#__PURE__*/React.createElement("span", {
      className: "rot"
    }, x.nome), /*#__PURE__*/React.createElement("span", {
      className: "trilho"
    }, /*#__PURE__*/React.createElement("span", {
      className: "preenche",
      style: {
        width: `${x.total / maior * 100}%`
      }
    })), /*#__PURE__*/React.createElement("span", {
      className: "qtd"
    }, x.total)));
  };
  if (!podeVerTudo) {
    return /*#__PURE__*/React.createElement(Empty, {
      text: "Os indicadores do canal são vistos pelo RH e pela direção."
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Período"
  }, /*#__PURE__*/React.createElement("select", {
    value: dias,
    onChange: e => setDias(num(e.target.value))
  }, /*#__PURE__*/React.createElement("option", {
    value: "30"
  }, "Últimos 30 dias"), /*#__PURE__*/React.createElement("option", {
    value: "60"
  }, "Últimos 60 dias"), /*#__PURE__*/React.createElement("option", {
    value: "90"
  }, "Últimos 90 dias"), /*#__PURE__*/React.createElement("option", {
    value: "365"
  }, "Último ano")))), /*#__PURE__*/React.createElement("div", {
    className: "cards"
  }, /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Manifestações"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.total), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.anonimas, " anônima(s)")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Em aberto"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.abertas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.novas, " sem leitura")), /*#__PURE__*/React.createElement("div", {
    className: "card" + (ind.atrasadas > 0 ? ' alerta' : '')
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Fora do prazo"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.atrasadas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.criticas > 0 ? `${ind.criticas} crítica(s) aberta(s)` : 'nenhuma crítica')), /*#__PURE__*/React.createElement("div", {
    className: "card destaque"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Tempo de resolução"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.tempoMedioResolucao !== null ? `${ind.tempoMedioResolucao}d` : '—'), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.percentualNoPrazo !== null ? `${ind.percentualNoPrazo}% no prazo` : 'sem histórico'))), tend.alertas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      marginTop: 0
    }
  }, "⚠️ O que subiu neste período"), tend.alertas.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("strong", null, a.rotulo), " — período anterior: ", a.anterior, ",", ' ', "período atual: ", a.atual, a.variacao !== null && ` (+${a.variacao}%)`)), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Aumento de registros não quer dizer que o risco aumentou: pode ser que as pessoas passaram a confiar mais no canal. Serve para escolher onde olhar.")), reinc.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Assunto que se repete"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", null, "Tema"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Registros"), /*#__PURE__*/React.createElement("th", null, "Protocolos"))), /*#__PURE__*/React.createElement("tbody", null, reinc.map(r => /*#__PURE__*/React.createElement("tr", {
    key: r.chave
  }, /*#__PURE__*/React.createElement("td", null, r.departamento), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, r.tema)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, r.total), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, r.protocolos.slice(0, 4).join(', '), r.protocolos.length > 4 ? '…' : ''))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Recomenda-se avaliação do responsável. O sistema aponta a repetição; quem conclui se existe risco é a análise técnica.")), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Por tipo"), ind.porCategoria.some(x => x.total > 0) ? barra(ind.porCategoria.filter(x => x.total > 0)) : /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "sem registros")), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Por setor"), ind.porDepartamento.length ? barra(ind.porDepartamento) : /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "sem registros"))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Por prioridade"), barra(ind.porPrioridade.filter(x => x.total > 0))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Por situação"), barra(ind.porStatus.filter(x => x.total > 0)))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "🛡️ Indicadores de riscos"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Temas que apareceram nos relatos, agrupados como a NR-1 organiza: segurança, saúde, psicossocial e conduta."), ind.porTema.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhum tema identificado no período.") : ['Segurança', 'Saúde', 'Psicossocial', 'Conduta', 'Operacional'].map(g => {
    const doGrupo = ind.porTema.filter(t => t.grupo === g);
    if (doGrupo.length === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      key: g,
      style: {
        marginBottom: 10
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "etp-rot"
    }, g), barra(doGrupo));
  }), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: 'var(--canvas-panel)',
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Estes números são ", /*#__PURE__*/React.createElement("strong", null, "fonte de informação"), " para o gerenciamento de riscos. Não substituem PGR, GRO, AEP, AET, PCMSO nem avaliação de profissional habilitado — apontam onde a avaliação deve olhar primeiro."))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Como está o dia de trabalho"), clima.vazio ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Ninguém respondeu o termômetro no período.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Respostas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, clima.total)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Média"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, clima.media, " ", /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "de 5")))), clima.porSetor.length > 0 && /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Respostas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Média"))), /*#__PURE__*/React.createElement("tbody", null, clima.porSetor.map(s => /*#__PURE__*/React.createElement("tr", {
    key: s.id
  }, /*#__PURE__*/React.createElement("td", null, s.nome), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, s.total), /*#__PURE__*/React.createElement("td", {
    className: "num",
    style: s.media < 3 ? {
      color: 'var(--warn)',
      fontWeight: 600
    } : undefined
  }, s.media))))), clima.comentarios.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 10
    }
  }, "O que escreveram"), clima.comentarios.slice(0, 12).map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "canal-resposta"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, c)))))));
}

/* O que o arquivo do canal deixou para o sistema recolher. */
function CaixaDoCanal({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [caixa, setCaixa] = useState(() => lerCaixaCanal());
  const [resultado, setResultado] = useState(null);
  const manifestacoes = caixa.filter(x => x && x.tipo !== 'clima');
  const climas = caixa.filter(x => x && x.tipo === 'clima');

  /* o canal grava o setor pelo nome; aqui vira o id do cadastro */
  const casarSetor = nome => {
    if (!nome) return '';
    const alvo = String(nome).trim().toLowerCase();
    const dp = (db.departamentos || []).find(d => String(d.nome).trim().toLowerCase() === alvo);
    return dp ? dp.id : '';
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      flex: 1,
      margin: 0
    }
  }, "Caixa do canal"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setCaixa(lerCaixaCanal());
      setResultado(null);
    }
  }, "Verificar")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "O arquivo do canal roda sozinho, sem o sistema aberto, e deixa aqui o que o pessoal registrou. Recolher traz para as manifestações — o protocolo que a pessoa guardou continua o mesmo."), caixa.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nada na caixa deste computador. Manifestações feitas em outro aparelho só aparecem lá — sem servidor, cada navegador tem a sua caixa.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Manifestações"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, manifestacoes.length)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Termômetro"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, climas.length))), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    style: {
      marginTop: 8
    },
    onClick: () => {
      let r = null;
      const itens = caixa.map(x => ({
        ...x,
        departamentoId: casarSetor(x.departamento)
      }));
      update(d => {
        r = recolherDoCanal(d, itens, usuario);
        return d;
      });
      limparCaixaCanal();
      setCaixa([]);
      setResultado(r);
    }
  }, "Recolher ", caixa.length, " registro(s)")), resultado && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600
    }
  }, resultado.recolhidas, " registro(s) recolhido(s)."), (resultado.repetidas > 0 || resultado.invalidas > 0) && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, resultado.repetidas > 0 && `${resultado.repetidas} já estavam no sistema. `, resultado.invalidas > 0 && `${resultado.invalidas} vieram incompletos e foram ignorados.`)));
}

/* QR Codes por setor. O desenho é gerado aqui mesmo, sem depender de
   serviço externo — cartaz de fábrica não pode depender de internet. */
function CanalQRCodes({
  db,
  update,
  usuario,
  podeEditar
}) {
  const cfg = configCanal(db);
  const [base, setBase] = useState(cfg.baseUrl || '');
  const [cartaz, setCartaz] = useState(null);
  const salvarBase = () => update(d => {
    d.configCanal = {
      ...(d.configCanal || {}),
      baseUrl: base.trim()
    };
    registrarLog(d, usuario, 'canal.config', `URL do canal: ${base.trim() || '(vazia)'}`);
    return d;
  });
  const setores = [{
    id: '',
    nome: 'Geral (sem setor)'
  }].concat((db.departamentos || []).filter(d => d.ativo !== false).map(d => ({
    id: d.id,
    nome: d.nome
  })));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Endereço do canal"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "O QR Code aponta para o endereço onde o sistema está publicado. Enquanto o ERP rodar como arquivo no computador, o celular do chão de fábrica não consegue abrir — publique numa rede acessível e cole o endereço aqui."), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: base,
    placeholder: "https://sistema.suaempresa.com.br/conversa-aberta",
    style: {
      flex: 1,
      minWidth: 260,
      margin: 0
    },
    disabled: !podeEditar,
    onChange: e => setBase(e.target.value)
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    disabled: !podeEditar,
    onClick: salvarBase
  }, "Salvar endereço")), !cfg.baseUrl && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Sem endereço, os QR Codes abaixo não levam a lugar nenhum."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "O celular do chão de fábrica não abre um arquivo que está no seu computador. Para o QR funcionar de verdade, o sistema precisa estar publicado numa rede que os celulares alcancem — pode ser um servidor da própria empresa. Enquanto isso, o canal funciona normalmente pelo menu, para quem acessa o ERP."))), /*#__PURE__*/React.createElement(CaixaDoCanal, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), /*#__PURE__*/React.createElement("div", {
    className: "qr-lista"
  }, setores.map(s => {
    const url = s.id ? urlDoSetor(db, s.id) : cfg.baseUrl || '';
    return /*#__PURE__*/React.createElement("div", {
      key: s.id || 'geral',
      className: "qr-cartao"
    }, /*#__PURE__*/React.createElement(QRCode, {
      texto: url,
      tamanho: 132
    }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("strong", null, s.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted qr-url"
    }, url || 'sem endereço'), /*#__PURE__*/React.createElement("div", {
      className: "row-actions",
      style: {
        marginTop: 6
      }
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      disabled: !url,
      onClick: () => setCartaz({
        nome: s.nome,
        url
      })
    }, "Cartaz A4"))));
  })), cartaz && /*#__PURE__*/React.createElement(Modal, {
    title: `Cartaz — ${cartaz.nome}`,
    onClose: () => setCartaz(null),
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "cartaz"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cartaz-topo"
  }, "💬 CONVERSA ABERTA"), /*#__PURE__*/React.createElement("div", {
    className: "cartaz-lema"
  }, "Fale. Sugira. Aponte. Melhore."), /*#__PURE__*/React.createElement(QRCode, {
    texto: cartaz.url,
    tamanho: 280
  }), /*#__PURE__*/React.createElement("div", {
    className: "cartaz-chamada"
  }, "Escaneie e participe"), /*#__PURE__*/React.createElement("div", {
    className: "cartaz-setor"
  }, cartaz.nome), /*#__PURE__*/React.createElement("div", {
    className: "cartaz-rodape"
  }, "Sua manifestação pode ser anônima. Relatos de boa-fé são tratados com respeito e sigilo.")), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions no-print"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setCartaz(null)
  }, "Fechar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => window.print()
  }, "Imprimir"))));
}

/* ==========================================================
   AFERIÇÃO E METAS — telas
========================================================== */
function AfericaoEMetas({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [sub, setSub] = useState('andamento');
  const [aberta, setAberta] = useState(null);
  const afs = db.afericoes || [];
  const emAndamento = afs.filter(a => a.fase !== 'concluida');
  const abas = [{
    id: 'andamento',
    label: `Aferições (${emAndamento.length})`
  }, {
    id: 'metas',
    label: `Metas (${(db.metasOperacionais || []).filter(m => m.status === 'validada').length})`
  }, {
    id: 'objetivos',
    label: `Objetivos (${(db.objetivosMelhoria || []).filter(o => o.status === 'ativo').length})`
  }, {
    id: 'historico',
    label: 'Histórico'
  }, {
    id: 'indicadores',
    label: 'Indicadores'
  }];
  if (aberta) {
    const a = (db.afericoes || []).find(x => x.id === aberta);
    if (!a) return /*#__PURE__*/React.createElement(Empty, {
      text: "Aferição não encontrada."
    });
    return /*#__PURE__*/React.createElement(AfericaoDetalhe, {
      afericao: a,
      db: db,
      update: update,
      usuario: usuario,
      podeEditar: podeEditar,
      onVoltar: () => setAberta(null)
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "A meta nasce da capacidade comprovada: prova de ", PECAS_PROVA, " peças para estimar, um período de validação para confirmar, e só então alguém propõe a meta — com justificativa registrada. O período é escolhido por quem afere: 3 horas para operação de rotina, menos para lote pequeno."), /*#__PURE__*/React.createElement("div", {
    className: "subtabs"
  }, abas.map(x => /*#__PURE__*/React.createElement("button", {
    key: x.id,
    className: sub === x.id ? 'on' : '',
    onClick: () => setSub(x.id)
  }, x.label))), sub === 'andamento' && /*#__PURE__*/React.createElement(AfericoesLista, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    onAbrir: setAberta
  }), sub === 'metas' && /*#__PURE__*/React.createElement(MetasLista, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'objetivos' && /*#__PURE__*/React.createElement(ObjetivosLista, {
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar
  }), sub === 'historico' && /*#__PURE__*/React.createElement(HistoricoCapacidade, {
    db: db
  }), sub === 'indicadores' && /*#__PURE__*/React.createElement(IndicadoresCapacidade, {
    db: db
  }));
}

/* A lista das aferições, com o botão que começa tudo. */
function AfericoesLista({
  db,
  update,
  usuario,
  podeEditar,
  onAbrir
}) {
  const [nova, setNova] = useState(null);
  const [verTodas, setVerTodas] = useState(false);
  const afs = (db.afericoes || []).slice().sort((a, b) => String(b.criadaEm).localeCompare(String(a.criadaEm)));
  const lista = verTodas ? afs : afs.filter(a => a.fase !== 'concluida');
  const nomeDe = (col, id) => {
    const x = (db[col] || []).find(y => y.id === id);
    return x ? x.nome : '—';
  };
  return /*#__PURE__*/React.createElement("div", null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setNova({
      colaboradorId: '',
      etapaId: '',
      departamentoId: '',
      equipamentoId: '',
      produtoId: '',
      ordemId: '',
      supervisorId: '',
      turno: '',
      data: todayISO()
    })
  }, "+ Nova aferição"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setVerTodas(!verTodas)
  }, verTodas ? 'Ver só as abertas' : 'Ver todas')), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: afs.length === 0 ? 'Nenhuma aferição. Comece medindo uma operação que você já conhece — ' + 'é a forma de ver se o número bate com a percepção da fábrica.' : 'Nenhuma aferição em andamento.'
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Aferição"), /*#__PURE__*/React.createElement("th", null, "Operação"), /*#__PURE__*/React.createElement("th", null, "Operador"), /*#__PURE__*/React.createElement("th", null, "Fase"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Resultado"))), /*#__PURE__*/React.createElement("tbody", null, lista.map(a => {
    const an = analiseAfericao(a);
    const et = (db.etapas || []).find(e => e.id === a.etapaId);
    const f = faseAfericao(a.fase);
    return /*#__PURE__*/React.createElement("tr", {
      key: a.id,
      style: {
        cursor: 'pointer'
      },
      onClick: () => onAbrir(a.id)
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, a.codigo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, fmtDate(a.data), a.turno ? ` · ${a.turno}` : '')), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, et ? et.nome : '—', a.equipamentoId && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, nomeDe('equipamentos', a.equipamentoId))), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, nomeDe('colaboradores', a.colaboradorId)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, f.nome), (() => {
      const as = a.assinaturas || {};
      if (!as.supervisor && !as.colaborador) return null;
      return /*#__PURE__*/React.createElement("div", {
        className: "small muted"
      }, as.supervisor && as.colaborador ? 'assinado' : as.supervisor ? 'falta o colaborador' : 'falta o supervisor');
    })()), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, an.vazio ? /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "—") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("strong", null, an.boasPorHora, "/h"), /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, "boas por hora"))));
  })))), nova && /*#__PURE__*/React.createElement(Modal, {
    title: "Nova aferição",
    onClose: () => setNova(null),
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Comece pela ordem ou pelo produto — as operações sugeridas são as do processo produtivo dele, na ordem em que acontecem. ",
    "Só aparecem operações de setores que trabalham com aferição; nos ",
    "demais a meta sai da jornada."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Ordem de produção"
  }, /*#__PURE__*/React.createElement("select", {
    value: nova.ordemId,
    onChange: e => {
      const o = (db.ordens || []).find(x => x.id === e.target.value);
      /* a ordem já sabe o produto: preencher poupa um passo e
         evita aferir a operação de outro produto por engano */
      setNova({
        ...nova,
        ordemId: e.target.value,
        produtoId: o ? o.produtoId : nova.produtoId,
        etapaId: '',
        departamentoId: '',
        equipamentoId: ''
      });
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem ordem —"), (db.ordens || []).filter(o => o.situacao !== 'concluida').map(o => {
    const p = (db.produtos || []).find(x => x.id === o.produtoId);
    return /*#__PURE__*/React.createElement("option", {
      key: o.id,
      value: o.id
    }, o.codigo, " · ", p ? montarNomeProduto(p, db) || p.codigo : 'produto removido');
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Produto"
  }, /*#__PURE__*/React.createElement("select", {
    value: nova.produtoId,
    onChange: e => setNova({
      ...nova,
      produtoId: e.target.value,
      etapaId: '',
      departamentoId: '',
      equipamentoId: ''
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— todas as operações —"), (db.produtos || []).map(p => /*#__PURE__*/React.createElement("option", {
    key: p.id,
    value: p.id
  }, montarNomeProduto(p, db) || p.codigo))))), (() => {
    const prod = (db.produtos || []).find(p => p.id === nova.produtoId);
    const doProduto = prod ? (prod.processo || []).map(passo => {
      const et = (db.etapas || []).find(e => e.id === passo.etapaId);
      const dp = (db.departamentos || []).find(x => x.id === passo.departamentoId);
      return et ? {
        etapa: et,
        passo,
        setor: dp ? dp.nome : ''
      } : null;
    }).filter(Boolean) : [];
    return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Field, {
      label: "Operação a aferir *"
    }, doProduto.length > 0 ? /*#__PURE__*/React.createElement("select", {
      value: nova.etapaId,
      onChange: e => {
        const alvo = doProduto.find(x => x.etapa.id === e.target.value);
        setNova({
          ...nova,
          etapaId: e.target.value,
          departamentoId: alvo ? alvo.passo.departamentoId : '',
          equipamentoId: alvo && alvo.passo.equipamentoId || ''
        });
      }
    }, /*#__PURE__*/React.createElement("option", {
      value: ""
    }, "— escolha a operação —"), doProduto
      /* aferição é dos setores que medem peça a peça; nos demais a meta
         sai da jornada e não há o que cronometrar */
      .filter(x => exigeAfericao(db, x.passo.departamentoId))
      .map((x, i) => /*#__PURE__*/React.createElement("option", {
      key: x.passo.id,
      value: x.etapa.id
    }, i + 1, ". ", x.etapa.nome, x.setor ? ` (${x.setor})` : ''))) : /*#__PURE__*/React.createElement("select", {
      value: nova.etapaId,
      onChange: e => {
        const et = (db.etapas || []).find(x => x.id === e.target.value);
        setNova({
          ...nova,
          etapaId: e.target.value,
          departamentoId: et ? et.departamentoId : ''
        });
      }
    }, /*#__PURE__*/React.createElement("option", {
      value: ""
    }, "— escolha a operação —"), (db.departamentos || [])
      /* só os setores que trabalham com aferição aparecem aqui */
      .filter(dp => exigeAfericao(db, dp.id)).map(dp => {
      const doSetor = (db.etapas || []).filter(e => e.departamentoId === dp.id && e.ativo !== false);
      if (doSetor.length === 0) return null;
      return /*#__PURE__*/React.createElement("optgroup", {
        key: dp.id,
        label: dp.nome
      }, doSetor.map(e => /*#__PURE__*/React.createElement("option", {
        key: e.id,
        value: e.id
      }, e.nome)));
    }))), doProduto.length > 0 ? /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -6,
        marginBottom: 10
      }
    }, doProduto.length, " operação(ões) no processo deste produto, na sequência de produção.") : nova.produtoId ? /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        marginTop: -6,
        marginBottom: 10,
        color: 'var(--warn)'
      }
    }, "Este produto ainda não tem processo produtivo cadastrado — escolhendo qualquer operação da fábrica.") : /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -6,
        marginBottom: 10
      }
    }, "Sem produto escolhido, a lista traz todas as operações da fábrica."), (() => {
      /* dizer o que já se sabe daquela operação evita refazer
         medição que já existe */
      const jaTem = nova.etapaId ? metaVigente(db, nova.etapaId, nova.produtoId) : null;
      if (!jaTem) return null;
      return /*#__PURE__*/React.createElement("div", {
        className: "panel",
        style: {
          borderColor: 'var(--thread)',
          marginBottom: 10
        }
      }, /*#__PURE__*/React.createElement("div", {
        className: "small"
      }, "Esta operação já tem meta validada de", ' ', /*#__PURE__*/React.createElement("strong", null, jaTem.valorHora, " peças/hora"), ". Uma nova aferição substitui a anterior, que fica no histórico."));
    })());
  })(), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Operador *"
  }, /*#__PURE__*/React.createElement("select", {
    value: nova.colaboradorId,
    onChange: e => setNova({
      ...nova,
      colaboradorId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha quem será aferido —"), (() => {
    /* quem trabalha no setor da operação vem primeiro */
    const ativos = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
    const doSetor = ativos.filter(c => nova.departamentoId && c.departamentoId === nova.departamentoId);
    const resto = ativos.filter(c => !doSetor.includes(c));
    if (doSetor.length === 0) return ativos.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome));
    const dp = (db.departamentos || []).find(x => x.id === nova.departamentoId);
    /* array, não fragmento: <select> só aceita option e
       optgroup como filhos diretos */
    return [/*#__PURE__*/React.createElement("optgroup", {
      key: "setor",
      label: dp ? dp.nome : 'do setor'
    }, doSetor.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome))), resto.length > 0 ? /*#__PURE__*/React.createElement("optgroup", {
      key: "outros",
      label: "Outros setores"
    }, resto.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome))) : null];
  })())), /*#__PURE__*/React.createElement(Field, {
    label: "Máquina"
  }, /*#__PURE__*/React.createElement("select", {
    value: nova.equipamentoId,
    onChange: e => setNova({
      ...nova,
      equipamentoId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem máquina —"), (db.equipamentos || []).filter(q => q.ativo !== false && (!nova.departamentoId || q.departamentoId === nova.departamentoId)).map(q => /*#__PURE__*/React.createElement("option", {
    key: q.id,
    value: q.id
  }, q.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Supervisor responsável"
  }, /*#__PURE__*/React.createElement("select", {
    value: nova.supervisorId,
    onChange: e => setNova({
      ...nova,
      supervisorId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— não informar —"), (db.colaboradores || []).filter(c => c.status !== 'Inativo').map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Data"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: nova.data,
    onChange: e => setNova({
      ...nova,
      data: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Turno"
  }, /*#__PURE__*/React.createElement("input", {
    value: nova.turno,
    placeholder: "manhã, tarde…",
    onChange: e => setNova({
      ...nova,
      turno: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setNova(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !nova.etapaId || !nova.colaboradorId,
    onClick: () => {
      let erro = null,
        id = null;
      update(d => {
        const r = novaAfericao(d, nova, usuario);
        if (r.erro) erro = r.erro;else id = r.afericao.id;
        return d;
      });
      if (erro) alert(erro);else {
        setNova(null);
        onAbrir(id);
      }
    }
  }, "Começar"))));
}

/* O passo a passo: preparação, prova, validação e análise.
   Cada fase mostra só o que precisa ser feito agora. */
function AfericaoDetalhe({
  afericao,
  db,
  update,
  usuario,
  podeEditar,
  onVoltar
}) {
  const a = afericao;
  const an = analiseAfericao(a);
  const cond = conferirCondicoes(a);
  const et = (db.etapas || []).find(e => e.id === a.etapaId);
  const col = (db.colaboradores || []).find(c => c.id === a.colaboradorId);
  const eq = (db.equipamentos || []).find(x => x.id === a.equipamentoId);
  const pr = (db.produtos || []).find(p => p.id === a.produtoId);
  const meta = (db.metasOperacionais || []).find(m => m.id === a.metaProposta);
  const [form, setForm] = useState({});
  const [hora, setHora] = useState(null);
  const [parada, setParada] = useState(null);
  const [propor, setPropor] = useState(null);
  const [relatorio, setRelatorio] = useState(false);
  const setF = (k, v) => setForm(p => ({
    ...p,
    [k]: v
  }));
  const agir = fn => update(d => {
    const r = fn(d);
    if (r && r.erro) alert(r.erro);
    return d;
  });
  if (relatorio) {
    return /*#__PURE__*/React.createElement(RelatorioAfericao, {
      afericao: a,
      db: db,
      update: update,
      usuario: usuario,
      podeEditar: podeEditar,
      onFechar: () => setRelatorio(false)
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), (a.fase === 'analise' || a.fase === 'concluida') && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setRelatorio(true)
  }, "📄 Relatório para assinatura")), /*#__PURE__*/React.createElement("div", {
    className: "page-head",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, et ? et.nome : 'operação'), /*#__PURE__*/React.createElement("h2", null, a.codigo), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, col ? col.nome : '—', eq ? ` · ${eq.nome}` : '', pr ? ` · ${montarNomeProduto(pr, db) || pr.codigo}` : '', ' · ', fmtDate(a.data), a.turno ? ` · ${a.turno}` : ''))), /*#__PURE__*/React.createElement("div", {
    className: "passos-afericao"
  }, FASES_AFERICAO.filter(f => f.id !== 'concluida').map((f, i) => {
    const ordem = FASES_AFERICAO.map(x => x.id);
    const atual = ordem.indexOf(a.fase);
    const este = ordem.indexOf(f.id);
    const estado = a.fase === 'concluida' || este < atual ? 'feito' : este === atual ? 'agora' : '';
    return /*#__PURE__*/React.createElement("div", {
      key: f.id,
      className: "passo " + estado
    }, /*#__PURE__*/React.createElement("span", {
      className: "n"
    }, estado === 'feito' ? '✓' : i + 1), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("strong", null, f.nome), /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, f.ajuda)));
  })), a.fase === 'preparacao' && podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "1. Preparação"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "O tempo de preparar a máquina não entra na conta da produção — mas precisa ser medido, porque também é tempo da fábrica."), !a.preparacao.inicio ? /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => agir(d => iniciarPreparacao(d, a.id, usuario))
  }, "▶ Iniciar preparação") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginBottom: 10
    }
  }, "Preparação em andamento desde ", fmtHora(a.preparacao.inicio), "."), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Regulagem"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.regulagem || '',
    onChange: e => setF('regulagem', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Linha"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.linha || '',
    onChange: e => setF('linha', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Agulha"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.agulha || '',
    onChange: e => setF('agulha', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Acessórios"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.acessorios || '',
    onChange: e => setF('acessorios', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Material separado"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.material || '',
    onChange: e => setF('material', e.target.value)
  }))), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      agir(d => finalizarPreparacao(d, a.id, form, usuario));
      setForm({});
    }
  }, "✓ Preparação concluída"))), a.preparacao.fim && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small",
    style: {
      flex: 1
    }
  }, "Preparação"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, duracao(a.preparacao.minutos))), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, [a.preparacao.regulagem && `regulagem ${a.preparacao.regulagem}`, a.preparacao.linha && `linha ${a.preparacao.linha}`, a.preparacao.agulha && `agulha ${a.preparacao.agulha}`, a.preparacao.material].filter(Boolean).join(' · ') || 'sem detalhes')), a.fase === 'prova' && podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "2. Prova de ", PECAS_PROVA, " peças"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Produza ", PECAS_PROVA, " peças em ritmo normal. Serve para estimar — o número daqui ainda não é meta."), !a.prova.inicio ? /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => agir(d => iniciarProva(d, a.id))
  }, "▶ Iniciar a prova") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginBottom: 10
    }
  }, "Cronômetro rodando desde ", fmtHora(a.prova.inicio), "."), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Peças produzidas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: form.quantidade ?? PECAS_PROVA,
    onChange: e => setF('quantidade', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Peças boas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: form.boas ?? PECAS_PROVA,
    onChange: e => setF('boas', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Com defeito"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: form.defeito || '',
    onChange: e => setF('defeito', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: form.observacao || '',
    onChange: e => setF('observacao', e.target.value)
  })), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      agir(d => finalizarProva(d, a.id, form, usuario));
      setForm({});
    }
  }, "✓ Encerrar a prova"))), a.prova.fim && (() => {
    const p = resultadoProva(a);
    return /*#__PURE__*/React.createElement("div", {
      className: "panel"
    }, /*#__PURE__*/React.createElement("h3", null, "Resultado da prova"), /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Tempo por peça"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, duracao(p.tempoPorPeca))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Capacidade estimada"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, p.capacidadeHora, /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, "/h"))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Projeção em 3h"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, p.projecao3h, /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, " peças")))), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 6
      }
    }, "Estimativa a partir de ", p.quantidade, " peças em ", duracao(p.minutos), ". É o ponto de partida da validação, não uma meta."));
  })(), a.fase === 'validacao' && podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "3. Validação"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Produção normal, em blocos. Toda parada é registrada com o motivo — é isso que separa o desempenho de quem trabalha das falhas do processo."), !a.validacao.inicio ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Quanto tempo a medição vai correr? Escolha pelo que a operação pede: um lote de 40 peças acaba antes de 3 horas, e esperar mais só inventaria produção que não existe."), /*#__PURE__*/React.createElement("div", {
    className: "canal-escolha"
  }, /*#__PURE__*/React.createElement("button", {
    className: "canal-botao" + ((form._modo || 'tempo') === 'tempo' ? ' on' : ''),
    onClick: () => setForm(p => ({
      ...p,
      _modo: 'tempo'
    }))
  }, /*#__PURE__*/React.createElement("strong", null, "Por tempo"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "medir durante um período definido")), /*#__PURE__*/React.createElement("button", {
    className: "canal-botao" + (form._modo === 'lote' ? ' on' : ''),
    onClick: () => setForm(p => ({
      ...p,
      _modo: 'lote'
    }))
  }, /*#__PURE__*/React.createElement("strong", null, "Por lote"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "medir até fechar uma quantidade"))), (form._modo || 'tempo') === 'tempo' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "periodos"
  }, PERIODOS_VALIDACAO.map(p => /*#__PURE__*/React.createElement("button", {
    key: p.minutos,
    className: "periodo" + ((num(form._minutos) || MINUTOS_VALIDACAO) === p.minutos ? ' on' : ''),
    onClick: () => setForm(x => ({
      ...x,
      _minutos: p.minutos
    }))
  }, /*#__PURE__*/React.createElement("strong", null, p.nome), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, p.ajuda)))), /*#__PURE__*/React.createElement(Field, {
    label: "Ou informe os minutos"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "5",
    style: {
      maxWidth: 160
    },
    value: form._minutos ?? MINUTOS_VALIDACAO,
    onChange: e => setForm(x => ({
      ...x,
      _minutos: e.target.value
    }))
  })), (() => {
    const q = qualidadeDoPeriodo(num(form._minutos) || MINUTOS_VALIDACAO);
    if (!q.aviso) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        marginTop: -6,
        marginBottom: 10
      }
    }, q.aviso);
  })()) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Field, {
    label: "Quantas peças fecham a validação"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    style: {
      maxWidth: 160
    },
    value: form._lote || '',
    placeholder: "40",
    onChange: e => setForm(x => ({
      ...x,
      _lote: e.target.value
    }))
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "A medição termina quando o lote fechar. O tempo que levar é o tempo da conta — pode dar 25 minutos ou 2 horas.")), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => agir(d => iniciarValidacao(d, a.id, {
      modo: form._modo || 'tempo',
      minutosPlanejados: num(form._minutos) || MINUTOS_VALIDACAO,
      loteAlvo: num(form._lote)
    }))
  }, "▶ Iniciar a validação")) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 180
    }
  }, "Em andamento desde ", fmtHora(a.validacao.inicio), a.validacao.modo === 'lote' ? ` · até fechar ${a.validacao.loteAlvo} peça(s)` : ` · ${duracao(a.validacao.minutosPlanejados)} combinados`, ".", (() => {
    const feito = (a.validacao.horas || []).reduce((s, h) => s + num(h.boas), 0);
    const tempo = (a.validacao.horas || []).reduce((s, h) => s + (num(h.minutos) || 60), 0);
    if (a.validacao.modo === 'lote' && a.validacao.loteAlvo > 0) return ` Faltam ${Math.max(0, a.validacao.loteAlvo - feito)}.`;
    if (tempo > 0) return ` ${duracao(tempo)} apontado(s).`;
    return '';
  })()), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setHora({
      hora: (a.validacao.horas || []).length + 1,
      minutos: a.validacao.modo === 'lote' ? 30 : Math.min(60, num(a.validacao.minutosPlanejados) || 60),
      produzidas: '',
      boas: '',
      rejeitadas: '',
      retrabalho: ''
    })
  }, "+ Bloco"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setParada({
      motivo: '',
      minutos: '',
      hora: (a.validacao.horas || []).length || 1
    })
  }, "+ Parada")), (a.validacao.horas || []).length > 0 && /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Bloco"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Duração"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Produzidas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Rejeitadas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Retrabalho"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Ritmo"))), /*#__PURE__*/React.createElement("tbody", null, (a.validacao.horas || []).map(h => {
    const min = num(h.minutos) || 60;
    const ritmo = min > 0 ? Number((num(h.boas) / (min / 60)).toFixed(1)) : null;
    return /*#__PURE__*/React.createElement("tr", {
      key: h.id
    }, /*#__PURE__*/React.createElement("td", null, h.hora, "º"), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, duracao(min)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.produzidas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.boas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.rejeitadas || 0), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.retrabalho || 0), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, ritmo !== null ? ritmo + '/h' : '—'));
  }))), (a.validacao.paradas || []).length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 10
    }
  }, "Paradas"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, (a.validacao.paradas || []).map(p => {
    const m = motivoParada(p.motivo);
    const o = ORIGENS_PERDA.find(x => x.id === p.origem);
    return /*#__PURE__*/React.createElement("tr", {
      key: p.id
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, m.nome, p.observacao && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, p.observacao)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, o ? o.nome : ''), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, duracao(p.minutos)), /*#__PURE__*/React.createElement("td", {
      style: {
        width: 40
      }
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => agir(d => removerParada(d, a.id, p.id))
    }, "×")));
  })))), (a.validacao.horas || []).length > 0 && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    style: {
      marginTop: 10
    },
    onClick: () => agir(d => finalizarValidacao(d, a.id, {}, usuario))
  }, "✓ Encerrar a validação"))), !an.vazio && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "cards"
  }, /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Produzidas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.produzidas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, an.boas, " boas")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Qualidade"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.qualidade, "%"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, an.rejeitadas, " rejeitada(s)")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Utilização"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.utilizacao, "%"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "parou ", duracao(an.paradaTotal))), /*#__PURE__*/React.createElement("div", {
    className: "card destaque"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Capacidade validada"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.boasPorHora, /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, "/h")), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "medido em ", duracao(an.periodo)))), an.confianca && an.confianca.fraco && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Medição curta"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, an.confianca.aviso)), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "De onde veio a perda"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Culpar o operador por falta de tecido é o erro que esta separação evita."), an.paradaTotal === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma parada registrada.") : ORIGENS_PERDA.map(o => {
    const m = (an.porOrigem.find(x => x.id === o.id) || {}).minutos || 0;
    if (m === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      key: o.id,
      className: "barra-linha"
    }, /*#__PURE__*/React.createElement("span", {
      className: "rot"
    }, o.nome), /*#__PURE__*/React.createElement("span", {
      className: "trilho"
    }, /*#__PURE__*/React.createElement("span", {
      className: "preenche",
      style: {
        width: `${m / an.paradaTotal * 100}%`,
        background: o.id === 'operador' ? 'var(--warn)' : 'var(--thread)'
      }
    })), /*#__PURE__*/React.createElement("span", {
      className: "qtd"
    }, duracao(m)));
  }))), (a.fase === 'analise' || a.fase === 'concluida') && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Condições da aferição"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Sem condições normais, o número medido não representa o processo — e a meta sai torta. Marque só o que era verdade durante a medição."), /*#__PURE__*/React.createElement("div", {
    className: "condicoes"
  }, CONDICOES_NORMAIS.map(c => /*#__PURE__*/React.createElement("label", {
    key: c.id,
    className: "check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    disabled: !podeEditar || a.fase === 'concluida',
    checked: !!(a.condicoes || {})[c.id],
    onChange: e => update(d => {
      const x = (d.afericoes || []).find(y => y.id === a.id);
      if (x) x.condicoes = {
        ...(x.condicoes || {}),
        [c.id]: e.target.checked
      };
      return d;
    })
  }), /*#__PURE__*/React.createElement("span", null, c.nome, c.essencial && /*#__PURE__*/React.createElement("span", {
    className: "tag",
    style: {
      marginLeft: 6
    }
  }, "essencial"))))), !cond.ok && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ Meta não validada: existem condições que podem distorcer a aferição."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Faltam: ", cond.faltando.map(c => c.nome.toLowerCase()).join(', '), "."))), a.fase === 'analise' && podeEditar && cond.ok && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "4. Propor a meta"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "A capacidade validada foi de ", /*#__PURE__*/React.createElement("strong", null, an.boasPorHora, " peças boas por hora"), ". A meta pode ser esse número ou menos — acima disso, a fábrica passa a prometer o que não comprovou."), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setPropor({
      valorHora: an.metaSugerida,
      justificativa: ''
    })
  }, "Propor meta")), meta && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      flex: 1
    }
  }, "Meta ", meta.codigo, " · ", meta.valorHora, " peças/hora"), /*#__PURE__*/React.createElement("span", {
    className: "tag"
  }, meta.porTurno, "/turno")), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, meta.justificativa), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "validada por ", meta.validadaPor, " · capacidade medida ", meta.capacidadeValidada, "/h")), hora && /*#__PURE__*/React.createElement(Modal, {
    title: `Produção do ${hora.hora}º bloco`,
    onClose: () => setHora(null)
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Duração deste bloco (minutos) *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    style: {
      maxWidth: 160
    },
    value: hora.minutos,
    onChange: e => setHora({
      ...hora,
      minutos: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "Blocos não precisam ter uma hora. Numa validação de 30 minutos existe um bloco de 30; num lote curto, blocos de 15."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Peças produzidas *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    autoFocus: true,
    value: hora.produzidas,
    onChange: e => setHora({
      ...hora,
      produzidas: e.target.value,
      boas: hora.boas === '' ? e.target.value : hora.boas
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Peças boas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: hora.boas,
    onChange: e => setHora({
      ...hora,
      boas: e.target.value
    })
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Rejeitadas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: hora.rejeitadas,
    onChange: e => setHora({
      ...hora,
      rejeitadas: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Retrabalho"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: hora.retrabalho,
    onChange: e => setHora({
      ...hora,
      retrabalho: e.target.value
    })
  }))), (() => {
    const min = num(hora.minutos),
      boas = num(hora.boas);
    if (!(min > 0 && boas > 0)) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Ritmo deste bloco: ", /*#__PURE__*/React.createElement("strong", null, (boas / (min / 60)).toFixed(1), " peças boas por hora"), ".");
  })(), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setHora(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !(num(hora.minutos) > 0),
    onClick: () => {
      let erro = null;
      update(d => {
        const r = registrarHora(d, a.id, hora, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setHora(null);
    }
  }, "Registrar"))), parada && /*#__PURE__*/React.createElement(Modal, {
    title: "Registrar parada",
    onClose: () => setParada(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O motivo define de quem é a perda. Isso muda a leitura do resultado."), /*#__PURE__*/React.createElement(Field, {
    label: "Motivo *"
  }, /*#__PURE__*/React.createElement("select", {
    value: parada.motivo,
    autoFocus: true,
    onChange: e => setParada({
      ...parada,
      motivo: e.target.value,
      origem: motivoParada(e.target.value).origem
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), ORIGENS_PERDA.map(o => /*#__PURE__*/React.createElement("optgroup", {
    key: o.id,
    label: o.nome
  }, MOTIVOS_PARADA.filter(m => m.origem === o.id).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome)))))), parada.motivo && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 10
    }
  }, "Contabilizada como ", /*#__PURE__*/React.createElement("strong", null, (ORIGENS_PERDA.find(o => o.id === parada.origem) || {}).nome), "."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Minutos parados *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: parada.minutos,
    onChange: e => setParada({
      ...parada,
      minutos: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Em qual hora"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: parada.hora,
    onChange: e => setParada({
      ...parada,
      hora: e.target.value
    })
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: parada.observacao || '',
    onChange: e => setParada({
      ...parada,
      observacao: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setParada(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = registrarParada(d, a.id, parada, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setParada(null);
    }
  }, "Registrar"))), propor && /*#__PURE__*/React.createElement(Modal, {
    title: "Propor meta",
    onClose: () => setPropor(null),
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Capacidade validada"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.boasPorHora, /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, "/h"))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Estimativa da prova"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.prova.vazio ? '—' : an.prova.capacidadeHora, /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, "/h"))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Qualidade"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, an.qualidade, "%")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Medido em"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(an.periodo)))), an.confianca && an.confianca.fraco && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      marginBottom: 8
    }
  }, "⚠ ", an.confianca.aviso), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Meta em peças boas por hora *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    autoFocus: true,
    value: propor.valorHora,
    onChange: e => setPropor({
      ...propor,
      valorHora: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Por turno de 8h"
  }, /*#__PURE__*/React.createElement("input", {
    value: Math.round(num(propor.valorHora) * 8),
    disabled: true
  }))), num(propor.valorHora) > an.boasPorHora && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Esta meta está acima da capacidade que foi medida."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A validação mostrou ", an.boasPorHora, "/h. Prometer mais do que se comprovou é o começo da meta que ninguém alcança — se ainda assim for o caso, explique o porquê na justificativa.")), /*#__PURE__*/React.createElement(Field, {
    label: "Justificativa *"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: propor.justificativa,
    placeholder: "por que esta meta, com base em quê",
    onChange: e => setPropor({
      ...propor,
      justificativa: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setPropor(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !num(propor.valorHora) || !String(propor.justificativa).trim(),
    onClick: () => {
      let erro = null;
      update(d => {
        const r = proporMeta(d, a.id, propor, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setPropor(null);
    }
  }, "Validar meta"))));
}

/* As metas vigentes: o que a fábrica pode prometer. */
function MetasLista({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [editar, setEditar] = useState(null);
  const [suspender, setSuspender] = useState(null);
  const [verSuspensas, setVerSuspensas] = useState(false);
  const metas = (db.metasOperacionais || []).filter(m => verSuspensas || m.status === 'validada').sort((a, b) => String(b.validadaEm).localeCompare(String(a.validadaEm)));
  const alertas = alertasAfericao(db).filter(x => x.tipo === 'meta_acima');
  return /*#__PURE__*/React.createElement("div", null, alertas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, alertas.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", x.texto))), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 220
    }
  }, "Metas nascem de aferição. Toda alteração exige justificativa e fica no histórico."), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setVerSuspensas(!verSuspensas)
  }, verSuspensas ? 'Só as vigentes' : 'Ver suspensas também')), metas.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma meta validada. Conclua uma aferição para propor a primeira."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Meta"), /*#__PURE__*/React.createElement("th", null, "Operação"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por hora"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por turno"), /*#__PURE__*/React.createElement("th", null, "Situação"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, metas.map(m => {
    const et = (db.etapas || []).find(e => e.id === m.etapaId);
    const pr = (db.produtos || []).find(p => p.id === m.produtoId);
    const acima = num(m.capacidadeValidada) > 0 && m.valorHora > num(m.capacidadeValidada) * 1.1;
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, m.codigo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, m.justificativa.slice(0, 60), m.justificativa.length > 60 ? '…' : '')), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, et ? et.nome : '—', pr && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, montarNomeProduto(pr, db) || pr.codigo)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", {
      style: acima ? {
        color: 'var(--warn)'
      } : undefined
    }, m.valorHora), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "medido ", m.capacidadeValidada)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, m.porTurno), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, (STATUS_META.find(s => s.id === m.status) || {}).nome), m.valorAnterior !== null && m.valorAnterior !== undefined && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "era ", m.valorAnterior, "/h")), podeEditar && /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("div", {
      className: "acoes-icone"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-accent",
      title: "Alterar",
      onClick: () => setEditar({
        id: m.id,
        valor: m.valorHora,
        motivo: ''
      })
    }, "✎"), m.status === 'validada' && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      title: "Suspender",
      onClick: () => setSuspender({
        id: m.id,
        motivo: ''
      })
    }, "×"))));
  })))), metas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Histórico das alterações"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, metas.flatMap(m => (m.historico || []).map(h => ({
    ...h,
    meta: m.codigo
  }))).sort((a, b) => String(b.quando).localeCompare(String(a.quando))).slice(0, 20).map(h => /*#__PURE__*/React.createElement("tr", {
    key: h.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, h.meta), " · ", h.o_que, h.de !== null && h.de !== undefined && h.para !== undefined && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " (", h.de, "/h → ", h.para, "/h)"), h.motivo && /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, h.motivo)), /*#__PURE__*/React.createElement("td", {
    className: "small muted",
    style: {
      width: 150
    }
  }, h.quem, /*#__PURE__*/React.createElement("div", null, fmtDataHora(h.quando)))))))), editar && /*#__PURE__*/React.createElement(Modal, {
    title: "Alterar meta",
    onClose: () => setEditar(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Mudar meta sem explicar é o começo da meta que ninguém entende. A justificativa fica registrada com o seu nome."), /*#__PURE__*/React.createElement(Field, {
    label: "Nova meta por hora *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    autoFocus: true,
    value: editar.valor,
    onChange: e => setEditar({
      ...editar,
      valor: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Justificativa *"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: editar.motivo,
    placeholder: "o que mudou no processo para justificar",
    onChange: e => setEditar({
      ...editar,
      motivo: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setEditar(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !String(editar.motivo).trim(),
    onClick: () => {
      let erro = null;
      update(d => {
        const r = alterarMeta(d, editar.id, editar.valor, editar.motivo, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setEditar(null);
    }
  }, "Salvar"))), suspender && /*#__PURE__*/React.createElement(Modal, {
    title: "Suspender meta",
    onClose: () => setSuspender(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Meta suspensa deixa de valer para o planejamento. Use quando o processo mudou e a aferição não representa mais a realidade."), /*#__PURE__*/React.createElement(Field, {
    label: "Motivo *"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: suspender.motivo,
    autoFocus: true,
    onChange: e => setSuspender({
      ...suspender,
      motivo: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setSuspender(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !String(suspender.motivo).trim(),
    onClick: () => {
      let erro = null;
      update(d => {
        const r = suspenderMeta(d, suspender.id, suspender.motivo, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setSuspender(null);
    }
  }, "Suspender"))));
}

/* Objetivo é onde se quer chegar — nunca vira meta sozinho. */
function ObjetivosLista({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [novo, setNovo] = useState(null);
  const [acao, setAcao] = useState(null);
  const objetivos = db.objetivosMelhoria || [];
  const metas = (db.metasOperacionais || []).filter(m => m.status === 'validada');
  const equipe = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 220
    }
  }, "O objetivo mostra onde se quer chegar. A meta continua sendo o que foi comprovado — só muda com nova aferição."), podeEditar && metas.length > 0 && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setNovo({
      metaId: '',
      objetivoHora: '',
      prazo: '',
      responsavelId: '',
      observacao: ''
    })
  }, "+ Objetivo")), metas.length === 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Sem meta validada não há de onde partir — conclua uma aferição primeiro."), objetivos.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum objetivo de melhoria."
  }) : objetivos.map(o => {
    const meta = (db.metasOperacionais || []).find(m => m.id === o.metaId);
    const et = meta && (db.etapas || []).find(e => e.id === meta.etapaId);
    const resp = (db.colaboradores || []).find(c => c.id === o.responsavelId);
    return /*#__PURE__*/React.createElement("div", {
      key: o.id,
      className: "panel"
    }, /*#__PURE__*/React.createElement("div", {
      className: "row-actions"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 200
      }
    }, /*#__PURE__*/React.createElement("strong", null, o.codigo, " · ", et ? et.nome : 'operação'), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "meta ", o.metaAtual, "/h → objetivo ", o.objetivoHora, "/h", ' ', "(+", o.ganho, "/h)", o.prazo && ` · até ${fmtDate(o.prazo)}`, resp && ` · ${resp.nome}`)), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setAcao({
        objetivoId: o.id,
        tipo: ACOES_MELHORIA[0],
        oQue: '',
        responsavelId: '',
        prazo: '',
        status: 'nao_iniciado'
      })
    }, "+ Ação")), (o.acoes || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 6
      }
    }, "Nenhuma ação — objetivo sem ação é só um número maior na parede.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Ação"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Responsável"), /*#__PURE__*/React.createElement("th", null, "Prazo"), /*#__PURE__*/React.createElement("th", null, "Situação"))), /*#__PURE__*/React.createElement("tbody", null, (o.acoes || []).map(x => {
      const r = (db.colaboradores || []).find(c => c.id === x.responsavelId);
      return /*#__PURE__*/React.createElement("tr", {
        key: x.id
      }, /*#__PURE__*/React.createElement("td", {
        className: "small"
      }, x.oQue), /*#__PURE__*/React.createElement("td", {
        className: "small muted"
      }, x.tipo), /*#__PURE__*/React.createElement("td", {
        className: "small"
      }, r ? r.nome : '—'), /*#__PURE__*/React.createElement("td", {
        className: "small"
      }, fmtDate(x.prazo)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
        className: "tag"
      }, (STATUS_ACAO.find(s => s.id === x.status) || {}).nome)));
    }))));
  }), novo && /*#__PURE__*/React.createElement(Modal, {
    title: "Novo objetivo de melhoria",
    onClose: () => setNovo(null),
    wide: true
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Meta de referência *"
  }, /*#__PURE__*/React.createElement("select", {
    value: novo.metaId,
    autoFocus: true,
    onChange: e => setNovo({
      ...novo,
      metaId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha a meta —"), metas.map(m => {
    const et = (db.etapas || []).find(x => x.id === m.etapaId);
    return /*#__PURE__*/React.createElement("option", {
      key: m.id,
      value: m.id
    }, m.codigo, " · ", et ? et.nome : '', " · ", m.valorHora, "/h");
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Objetivo por hora *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: novo.objetivoHora,
    onChange: e => setNovo({
      ...novo,
      objetivoHora: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Prazo"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: novo.prazo,
    onChange: e => setNovo({
      ...novo,
      prazo: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Responsável"
  }, /*#__PURE__*/React.createElement("select", {
    value: novo.responsavelId,
    onChange: e => setNovo({
      ...novo,
      responsavelId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), equipe.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome))))), (() => {
    const m = metas.find(x => x.id === novo.metaId);
    if (!m || !num(novo.objetivoHora)) return null;
    const ganho = num(novo.objetivoHora) - m.valorHora;
    if (ganho <= 0) return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "O objetivo precisa ser maior que a meta atual (", m.valorHora, "/h).");
    return /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Ganho pretendido: ", /*#__PURE__*/React.createElement("strong", null, "+", ganho.toFixed(1), "/h"), ' ', "(", Math.round(ganho / m.valorHora * 100), "% acima da meta). Isso exige ação — treinamento, método, máquina ou layout.");
  })(), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: novo.observacao,
    onChange: e => setNovo({
      ...novo,
      observacao: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setNovo(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = salvarObjetivo(d, novo, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setNovo(null);
    }
  }, "Salvar"))), acao && /*#__PURE__*/React.createElement(Modal, {
    title: "Ação do plano",
    onClose: () => setAcao(null)
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: acao.tipo,
    onChange: e => setAcao({
      ...acao,
      tipo: e.target.value
    })
  }, ACOES_MELHORIA.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x)))), /*#__PURE__*/React.createElement(Field, {
    label: "O que será feito *"
  }, /*#__PURE__*/React.createElement("input", {
    value: acao.oQue,
    autoFocus: true,
    onChange: e => setAcao({
      ...acao,
      oQue: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Responsável *"
  }, /*#__PURE__*/React.createElement("select", {
    value: acao.responsavelId,
    onChange: e => setAcao({
      ...acao,
      responsavelId: e.target.value
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), equipe.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Prazo *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: acao.prazo,
    onChange: e => setAcao({
      ...acao,
      prazo: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: acao.status,
    onChange: e => setAcao({
      ...acao,
      status: e.target.value
    })
  }, STATUS_ACAO.map(s => /*#__PURE__*/React.createElement("option", {
    key: s.id,
    value: s.id
  }, s.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setAcao(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = salvarAcaoObjetivo(d, acao.objetivoId, acao, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setAcao(null);
    }
  }, "Salvar"))));
}

/* Dois olhares sobre o mesmo dado: a pessoa e a operação. */
function HistoricoCapacidade({
  db
}) {
  const [modo, setModo] = useState('operador');
  const [alvo, setAlvo] = useState('');
  const lista = modo === 'operador' ? alvo ? historicoOperador(db, alvo) : [] : alvo ? historicoOperacao(db, alvo) : [];
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Ver por"
  }, /*#__PURE__*/React.createElement("select", {
    value: modo,
    onChange: e => {
      setModo(e.target.value);
      setAlvo('');
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: "operador"
  }, "Operador"), /*#__PURE__*/React.createElement("option", {
    value: "operacao"
  }, "Operação"))), /*#__PURE__*/React.createElement(Field, {
    label: modo === 'operador' ? 'Operador' : 'Operação'
  }, /*#__PURE__*/React.createElement("select", {
    value: alvo,
    onChange: e => setAlvo(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), modo === 'operador' ? (db.colaboradores || []).filter(c => c.status !== 'Inativo').map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome)) : (db.etapas || []).map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))))), !alvo ? /*#__PURE__*/React.createElement(Empty, {
    text: "Escolha acima para ver o histórico."
  }) : lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma aferição concluída para esta escolha."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Data"), /*#__PURE__*/React.createElement("th", null, modo === 'operador' ? 'Operação' : 'Operador'), /*#__PURE__*/React.createElement("th", null, modo === 'operador' ? 'Produto' : 'Máquina'), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Resultado"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Qualidade"))), /*#__PURE__*/React.createElement("tbody", null, lista.map((h, i) => /*#__PURE__*/React.createElement("tr", {
    key: i
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, fmtDate(h.data)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, modo === 'operador' ? h.etapa : h.colaborador), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, modo === 'operador' ? h.produto : h.equipamento), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, h.resultado), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, "/h")), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, h.qualidade !== null ? h.qualidade + '%' : '—')))))), lista.length > 1 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 8
    }
  }, "A evolução mostra o efeito de treinamento e de mudança de método. Queda costuma vir de mudança no processo, não de quem opera — vale comparar com o que mudou na fábrica no período.")));
}

/* Os números que o PCP e a direção usam para decidir. */
function IndicadoresCapacidade({
  db
}) {
  const ind = indicadoresAfericao(db, {});
  const des = desempenhoContraMeta(db, {});
  const perdaTotal = ind.perdaProcesso + ind.perdaOperador + ind.perdaExterna;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cards"
  }, /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Aferições"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.afericoes), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.emAndamento, " em andamento")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Metas validadas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.metasValidadas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.operacoes, " operação(ões) aferida(s)")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Produtividade média"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.produtividadeMedia ?? '—', ind.produtividadeMedia !== null && /*#__PURE__*/React.createElement("span", {
    className: "small"
  }, "/h")), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "peças boas por hora")), /*#__PURE__*/React.createElement("div", {
    className: "card destaque"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Qualidade média"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, ind.qualidadeMedia !== null ? ind.qualidadeMedia + '%' : '—'), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ind.utilizacaoMedia !== null ? `${ind.utilizacaoMedia}% de utilização` : ''))), ind.alertas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      marginTop: 0
    }
  }, "⚠️ O que pode estar distorcendo os números"), ind.alertas.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      marginBottom: 4,
      color: x.grave ? 'var(--danger)' : undefined,
      fontWeight: x.grave ? 600 : 400
    }
  }, x.texto))), perdaTotal > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "De quem é a perda"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Somando todas as aferições. Quando a maior fatia é do processo, cobrar ritmo do operador não resolve."), [['Processo', ind.perdaProcesso, 'var(--thread)'], ['Operador', ind.perdaOperador, 'var(--warn)'], ['Externa', ind.perdaExterna, '#8a8577']].map(([nome, min, cor]) => /*#__PURE__*/React.createElement("div", {
    key: nome,
    className: "barra-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "rot"
  }, nome), /*#__PURE__*/React.createElement("span", {
    className: "trilho"
  }, /*#__PURE__*/React.createElement("span", {
    className: "preenche",
    style: {
      width: `${min / perdaTotal * 100}%`,
      background: cor
    }
  })), /*#__PURE__*/React.createElement("span", {
    className: "qtd"
  }, duracao(min))))), des.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Apontamentos contra a meta"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Só aparece o que tem meta aferida. Sem aferição, comparar é chutar."), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Data"), /*#__PURE__*/React.createElement("th", null, "Quem"), /*#__PURE__*/React.createElement("th", null, "Operação"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Meta"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Real"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Eficiência"))), /*#__PURE__*/React.createElement("tbody", null, des.slice(0, 25).map((l, i) => {
    const f = faixaEficiencia(l.eficiencia);
    return /*#__PURE__*/React.createElement("tr", {
      key: i
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, fmtDate(l.data)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, l.colaborador), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, l.etapa), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, l.meta, "/h"), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, l.real, "/h"), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, f.marca, " ", l.eficiencia, "%"));
  })))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Capacidade por operação"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "O que o PCP pode prometer sem inventar. Operação sem meta não entra — e essa ausência é a informação mais útil aqui."), (() => {
    const linhas = (db.etapas || []).map(e => ({
      etapa: e,
      cap: capacidadePlanejavel(db, e.id)
    })).filter(x => x.cap);
    if (linhas.length === 0) return /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Nenhuma operação com meta validada.");
    return /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Operação"), /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Por hora"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Por dia"))), /*#__PURE__*/React.createElement("tbody", null, linhas.map(({
      etapa,
      cap
    }) => {
      const dp = (db.departamentos || []).find(d => d.id === etapa.departamentoId);
      return /*#__PURE__*/React.createElement("tr", {
        key: etapa.id
      }, /*#__PURE__*/React.createElement("td", null, etapa.nome), /*#__PURE__*/React.createElement("td", {
        className: "small muted"
      }, dp ? dp.nome : '—'), /*#__PURE__*/React.createElement("td", {
        className: "num"
      }, cap.porHora), /*#__PURE__*/React.createElement("td", {
        className: "num"
      }, /*#__PURE__*/React.createElement("strong", null, cap.porDia)));
    })));
  })()));
}

/* ==========================================================
   PRODUÇÃO REAL — telas
========================================================== */

/* O andamento da ordem: quanto cada etapa entregou, o que está na
   fila, e quando isso termina no ritmo de agora. */
function AndamentoDaOrdem({
  ordem,
  db,
  update,
  usuario,
  podeEditar
}) {
  const [apontar, setApontar] = useState(null);
  const rvp = realVersusProjetado(ordem, db);
  const av = rvp.avanco;
  const p = rvp.projecao;
  const prev = previsaoTermino(ordem, db);
  const g = gargaloDaOrdem(ordem, db);
  const marca = f => f === 'ok' ? '🟢' : f === 'atencao' ? '🟡' : '🔴';
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cards"
  }, /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Planejado"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, av.planejado), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "peças")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Pronto"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, av.concluido), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, av.percentual, "% da ordem")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Em processo"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, av.emProcesso), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "esperando a próxima etapa")), /*#__PURE__*/React.createElement("div", {
    className: "card destaque"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Previsão"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 17,
      color: prev.atrasada ? 'var(--danger)' : undefined
    }
  }, prev.concluida ? 'concluída' : prev.data ? fmtDate(prev.data) : '—'), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, prev.atrasada ? `${prev.diasDeAtraso} dia(s) depois do combinado` : ordem.entrega ? `entrega ${fmtDate(ordem.entrega)}` : 'sem data de entrega'))), !p.original.confiavel && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "A previsão é uma estimativa."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, p.original.aferidas, " de ", p.original.etapas.length, " operação(ões) têm capacidade aferida. As demais usam o tempo digitado no roteiro, que costuma ser otimista. Aferir as operações que faltam torna esta data confiável.")), g && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600
    }
  }, "⚠️ O fluxo está parando em ", g.etapa), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, g.emEspera, " peça(s) esperando no setor ", g.setor, g.capacidade ? ` · capacidade de ${g.capacidade}/h` : '', g.travada ? ' · nada foi apontado ainda nesta etapa' : '', ". Adiantar as etapas seguintes não resolve enquanto esta não andar.")), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Recebeu"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Produziu"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Na fila"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Ritmo"), /*#__PURE__*/React.createElement("th", null, "Situação"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 90
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, av.etapas.map(e => {
    const proj = p.etapas.find(x => x.tarefa.id === e.tarefa.id);
    return /*#__PURE__*/React.createElement("tr", {
      key: e.tarefa.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, e.ordem, ". ", e.etapa), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, e.setor)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, e.disponivel), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, e.produzidas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, e.boas), e.qualidade !== null && e.qualidade < 100 && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, e.qualidade, "%")), /*#__PURE__*/React.createElement("td", {
      className: "num",
      style: e.emEspera > 0 ? {
        color: 'var(--warn)',
        fontWeight: 600
      } : undefined
    }, e.emEspera), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, e.porHora !== null ? `${e.porHora}/h` : '—', proj && proj.desvioRitmo !== null && /*#__PURE__*/React.createElement("div", {
      className: "muted",
      style: {
        color: proj.desvioRitmo < -10 ? 'var(--danger)' : undefined
      }
    }, "meta ", proj.porHora, "/h")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, e.concluida ? '🟢 pronta' : e.produzidas > 0 ? '🟡 em curso' : e.emEspera > 0 ? '🔴 parada' : '· aguardando'), false && /*#__PURE__*/React.createElement("td", null, !e.concluida && /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setApontar({
        ordemId: ordem.id,
        tarefaId: e.tarefa.id,
        colaboradorId: '',
        pecasBoas: '',
        pecasDefeito: '',
        retrabalho: '',
        minutosGastos: '',
        paradas: [],
        data: todayISO(),
        minutosPrevistos: e.tarefa.minutosPorPeca ? Number((e.tarefa.minutosPorPeca * Math.min(e.emEspera || ordem.quantidade, ordem.quantidade)).toFixed(1)) : '',
        _limite: e.ordem > 1 ? Math.max(0, e.disponivel - e.produzidas) : null,
        _etapa: e.etapa
      })
    }, "+ Apontar")));
  })))), av.totalMinutos > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Real × projetado"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Indicador"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Projetado"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Real"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Desvio"))), /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Peças boas"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, rvp.esperado ?? '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, rvp.real), /*#__PURE__*/React.createElement("td", {
    className: "num",
    style: rvp.diferenca !== null && rvp.diferenca < 0 ? {
      color: 'var(--danger)',
      fontWeight: 600
    } : undefined
  }, rvp.diferenca === null ? '—' : (rvp.diferenca >= 0 ? '+' : '') + rvp.diferenca, rvp.faixa && ' ' + marca(rvp.faixa))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Tempo"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(p.original.totalMinutos)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(av.totalMinutos)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, av.totalParada > 0 ? `${duracao(av.totalParada)} parado` : '—')), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Retrabalho"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, "—"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, av.totalRetrabalho), /*#__PURE__*/React.createElement("td", {
    className: "num"
  })), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Rejeitadas"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, "—"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, av.totalDefeito), /*#__PURE__*/React.createElement("td", {
    className: "num"
  })))), rvp.causas.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "De onde veio o desvio"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 6
    }
  }, "Peças que deixaram de sair, por causa. Nem toda diferença é ritmo de quem opera."), rvp.causas.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "barra-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "rot"
  }, c.nome), /*#__PURE__*/React.createElement("span", {
    className: "trilho"
  }, /*#__PURE__*/React.createElement("span", {
    className: "preenche",
    style: {
      width: `${Math.min(100, c.pecas / Math.max(1, Math.max(...rvp.causas.map(x => x.pecas))) * 100)}%`,
      background: c.origem === 'operador' ? 'var(--warn)' : c.origem === 'processo' ? 'var(--thread)' : '#8a8577'
    }
  })), /*#__PURE__*/React.createElement("span", {
    className: "qtd"
  }, c.pecas))), rvp.naoExplicado > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, rvp.naoExplicado, " peça(s) de diferença sem causa registrada. Apontar as paradas com motivo fecha essa conta."))), podeEditar && ordem.situacao !== 'concluida' && /*#__PURE__*/React.createElement("div", {
    className: "small muted no-print",
    style: { marginTop: 8 }
  }, "Para apontar produção, use Produção → Programação → Balanceamento da ",
    "carteira: lá a operação já vem com quem foi escalado e com a máquina."),
    ordem.situacao === 'concluida' && /*#__PURE__*/React.createElement(FechamentoOrdem, {
    ordem: ordem,
    db: db
  }), apontar && /*#__PURE__*/React.createElement(ApontarProducaoModal, {
    dados: apontar,
    db: db,
    onClose: () => setApontar(null),
    onSalvar: dados => {
      let erro = null;
      update(d => {
        const r = apontarProducao(d, dados, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setApontar(null);
    }
  }));
}

/* Apontar produção: quem, quanto, em quanto tempo e o que atrapalhou. */
function ApontarProducaoModal({
  dados,
  db,
  onClose,
  onSalvar
}) {
  const [f, setF] = useState({
    ...dados
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const ordem = (db.ordens || []).find(o => o.id === f.ordemId);
  const tarefa = ordem ? (ordem.tarefas || []).find(t => t.id === f.tarefaId) : null;
  const meta = tarefa ? metaVigente(db, tarefa.etapaId, ordem.produtoId, tarefa.equipamentoId) : null;
  const boas = num(f.pecasBoas),
    def = num(f.pecasDefeito);
  const minutos = num(f.minutosGastos);
  const ritmo = minutos > 0 ? Number((boas / (minutos / 60)).toFixed(1)) : null;
  const passou = f._limite !== null && f._limite !== undefined && boas + def > f._limite;
  const addParada = () => set('paradas', [...(f.paradas || []), {
    id: uid(),
    motivo: '',
    origem: '',
    minutos: ''
  }]);
  const setParada = (id, patch) => set('paradas', (f.paradas || []).map(p => p.id === id ? {
    ...p,
    ...patch
  } : p));
  const rmParada = id => set('paradas', (f.paradas || []).filter(p => p.id !== id));
  return /*#__PURE__*/React.createElement(Modal, {
    title: `Apontar produção — ${f._etapa || 'etapa'}`,
    onClose: onClose,
    wide: true
  }, ordem && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Ordem ", ordem.codigo, f._limite !== null && f._limite !== undefined && ` · a etapa anterior entregou ${f._limite} peça(s) ainda não processadas`, meta && ` · meta de ${meta.valorHora} peças boas por hora`), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quem produziu *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.colaboradorId,
    autoFocus: true,
    onChange: e => set('colaboradorId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), (() => {
    const ativos = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
    const setor = tarefa ? tarefa.departamentoId : '';
    const doSetor = ativos.filter(c => setor && c.departamentoId === setor);
    const resto = ativos.filter(c => !doSetor.includes(c));
    if (doSetor.length === 0) return ativos.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome));
    const dp = (db.departamentos || []).find(x => x.id === setor);
    return [/*#__PURE__*/React.createElement("optgroup", {
      key: "s",
      label: dp ? dp.nome : 'do setor'
    }, doSetor.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome))), resto.length ? /*#__PURE__*/React.createElement("optgroup", {
      key: "o",
      label: "Outros setores"
    }, resto.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.nome))) : null];
  })())), /*#__PURE__*/React.createElement(Field, {
    label: "Data"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.data,
    onChange: e => set('data', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Tempo trabalhado (min) *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: f.minutosGastos,
    onChange: e => set('minutosGastos', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Peças boas *"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: f.pecasBoas,
    onChange: e => set('pecasBoas', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Rejeitadas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: f.pecasDefeito,
    onChange: e => set('pecasDefeito', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Retrabalho"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: f.retrabalho,
    onChange: e => set('retrabalho', e.target.value)
  }))), passou && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 600,
      marginTop: -4,
      marginBottom: 8
    }
  }, "A etapa anterior só entregou ", f._limite, " peça(s). Não dá para apontar", ' ', boas + def, " agora."), ritmo !== null && ritmo > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -4,
      marginBottom: 10
    }
  }, "Ritmo deste apontamento: ", /*#__PURE__*/React.createElement("strong", null, ritmo, " peças boas por hora"), meta && /*#__PURE__*/React.createElement(React.Fragment, null, " — ", ritmo >= meta.valorHora ? `na meta de ${meta.valorHora}/h` : `${Math.round((1 - ritmo / meta.valorHora) * 100)}% abaixo da meta de ${meta.valorHora}/h`)), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small",
    style: {
      flex: 1
    }
  }, "O que parou a produção"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: addParada
  }, "+ Parada")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Sem isso, a diferença entre o previsto e o realizado acaba sobrando para quem estava na máquina."), (f.paradas || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "nenhuma parada") : (f.paradas || []).map(p => /*#__PURE__*/React.createElement("div", {
    key: p.id,
    className: "grid3",
    style: {
      alignItems: 'end'
    }
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Motivo"
  }, /*#__PURE__*/React.createElement("select", {
    value: p.motivo,
    onChange: e => setParada(p.id, {
      motivo: e.target.value,
      origem: motivoParada(e.target.value).origem
    })
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), ORIGENS_PERDA.map(o => /*#__PURE__*/React.createElement("optgroup", {
    key: o.id,
    label: o.nome
  }, MOTIVOS_PARADA.filter(m => m.origem === o.id).map(m => /*#__PURE__*/React.createElement("option", {
    key: m.id,
    value: m.id
  }, m.nome)))))), /*#__PURE__*/React.createElement(Field, {
    label: "Minutos"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: p.minutos,
    onChange: e => setParada(p.id, {
      minutos: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1
    }
  }, p.origem ? (ORIGENS_PERDA.find(o => o.id === p.origem) || {}).nome : ''), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => rmParada(p.id)
  }, "×")))), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.observacao || '',
    onChange: e => set('observacao', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !f.colaboradorId || !(minutos > 0) || passou,
    onClick: () => onSalvar(f)
  }, "Registrar")));
}

/* A conta final da ordem. */
function FechamentoOrdem({
  ordem,
  db
}) {
  const f = fechamentoDaOrdem(ordem, db);
  return /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Fechamento da ordem"), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Planejado"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, f.planejado)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Produzido"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, f.produzido)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Boas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, f.boas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Qualidade"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, f.qualidade !== null ? f.qualidade + '%' : '—'))), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta",
    style: {
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Tempo projetado"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(f.tempoProjetado))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Tempo real"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, duracao(f.tempoReal))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Desvio"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15,
      color: f.desvioTempo > 0 ? 'var(--danger)' : 'var(--ok)'
    }
  }, f.desvioTempo > 0 ? '+' : '', duracao(Math.abs(f.desvioTempo)))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Eficiência"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 15
    }
  }, f.eficiencia !== null ? f.eficiencia + '%' : '—'))), f.pessoas.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "Quem produziu"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Pessoa"), /*#__PURE__*/React.createElement("th", null, "Etapas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Ritmo"))), /*#__PURE__*/React.createElement("tbody", null, f.pessoas.map(p => /*#__PURE__*/React.createElement("tr", {
    key: p.chave
  }, /*#__PURE__*/React.createElement("td", null, p.nome), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, p.etapas.join(', ')), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.boas), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, p.porHora !== null ? p.porHora + '/h' : '—')))))), f.perdas.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "Principais perdas"), f.perdas.map(p => /*#__PURE__*/React.createElement("div", {
    key: p.id,
    className: "small muted"
  }, p.nome, ": ", duracao(p.minutos)))));
}

/* A torre de controle: o estado da fábrica agora. */
function TorreDeControle({
  db,
  update,
  usuario,
  podeEditar,
  onAbrirOrdem
}) {
  const p = painelProducao(db);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O que está em produção agora, com a previsão calculada pelo ritmo real de cada etapa."), /*#__PURE__*/React.createElement("div", { className: "faixa-dados" },
    /*#__PURE__*/React.createElement("div", { className: "linha-rotulos" },
      /*#__PURE__*/React.createElement("span", null, "Ordens abertas"),
      /*#__PURE__*/React.createElement("span", null, "Vão passar da entrega"),
      /*#__PURE__*/React.createElement("span", null, "Produção de hoje"),
      /*#__PURE__*/React.createElement("span", null, "Parada hoje")),
    /*#__PURE__*/React.createElement("div", { className: "linha-valores" },
      /*#__PURE__*/React.createElement("span", null, p.abertas,
        /*#__PURE__*/React.createElement("small", null, p.emAndamento, " com produção")),
      /*#__PURE__*/React.createElement("span", {
        className: p.atrasadas ? 'alerta' : ''
      }, p.atrasadas,
        /*#__PURE__*/React.createElement("small", null, "pelo ritmo de agora")),
      /*#__PURE__*/React.createElement("span", null, p.producaoHoje,
        /*#__PURE__*/React.createElement("small", null, p.defeitoHoje, " rejeitada(s)")),
      /*#__PURE__*/React.createElement("span", { className: "destaque" },
        duracao(p.paradaHoje),
        /*#__PURE__*/React.createElement("small", null,
          p.minutosHoje > 0 ? `de ${duracao(p.minutosHoje)} apontados` : 'sem apontamento')))), p.gargalos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      marginTop: 0
    }
  }, "⚠️ Onde o fluxo está parando"), p.gargalos.map((g, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      marginBottom: 3
    }
  }, /*#__PURE__*/React.createElement("strong", null, g.ordem.codigo), " — ", g.etapa, " (", g.setor, "):", ' ', g.emEspera, " peça(s) na fila", g.capacidade ? ` · ${g.capacidade}/h` : ''))), p.ordens.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma ordem aberta."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Ordem"), /*#__PURE__*/React.createElement("th", null, "Produto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Pronto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Em processo"), /*#__PURE__*/React.createElement("th", null, "Previsão"), /*#__PURE__*/React.createElement("th", null, "Situação"))), /*#__PURE__*/React.createElement("tbody", null, p.ordens.map(l => {
    const pr = (db.produtos || []).find(x => x.id === l.ordem.produtoId);
    return /*#__PURE__*/React.createElement("tr", {
      key: l.ordem.id,
      style: {
        cursor: onAbrirOrdem ? 'pointer' : undefined
      },
      onClick: () => onAbrirOrdem && onAbrirOrdem(l.ordem.id)
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, l.ordem.codigo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, l.ordem.quantidade, " peça(s)")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, pr ? montarNomeProduto(pr, db) || pr.codigo : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, /*#__PURE__*/React.createElement("strong", null, l.avanco.concluido), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, l.avanco.percentual, "%")), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, l.avanco.emProcesso), /*#__PURE__*/React.createElement("td", {
      className: "small",
      style: l.previsao.atrasada ? {
        color: 'var(--danger)',
        fontWeight: 600
      } : undefined
    }, l.previsao.concluida ? 'concluída' : l.previsao.data ? fmtDate(l.previsao.data) : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, l.previsao.atrasada ? '🔴 atrasada' : l.avanco.totalBoas > 0 ? '🟡 em curso' : '· não começou'));
  })))));
}

/* O relatório que sai da impressora e volta assinado. Uma folha:
   o que foi medido, em que condição, e o que se conclui — com espaço
   para as duas assinaturas. */
function RelatorioAfericao({
  afericao,
  db,
  update,
  usuario,
  podeEditar,
  onFechar
}) {
  const r = relatorioAfericao(afericao, db);
  const a = r.afericao,
    an = r.analise,
    p = r.prova;
  const [assinar, setAssinar] = useState(null);
  const linha = (rot, val) => /*#__PURE__*/React.createElement("div", {
    className: "rel-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "rot"
  }, rot), /*#__PURE__*/React.createElement("span", {
    className: "val"
  }, val || '—'));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions no-print",
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onFechar
  }, "← Voltar"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), podeEditar && !r.assinaturas.supervisor && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setAssinar({
      papel: 'supervisor',
      nome: r.supervisor || '',
      observacao: ''
    })
  }, "Registrar assinatura do supervisor"), podeEditar && !r.assinaturas.colaborador && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setAssinar({
      papel: 'colaborador',
      nome: r.colaborador || '',
      observacao: ''
    })
  }, "Registrar assinatura do colaborador"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => window.print()
  }, "Imprimir")), (r.supervisorDesatualizado || r.colaboradorDesatualizado) && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "A aferição mudou depois de assinada."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "As assinaturas registradas se referem a um resultado diferente do atual. Imprima e colha de novo antes de usar este papel.")), /*#__PURE__*/React.createElement("div", {
    className: "rel-folha"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-cabecalho"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "rel-titulo"
  }, "Aferição de capacidade produtiva"), /*#__PURE__*/React.createElement("div", {
    className: "rel-sub"
  }, "Medição do que a operação entrega em condições normais")), /*#__PURE__*/React.createElement("div", {
    className: "rel-codigo"
  }, /*#__PURE__*/React.createElement("div", null, a.codigo), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, fmtDate(a.data)))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-grade"
  }, linha('Colaborador', r.colaborador), linha('Operação', r.operacao), linha('Setor', r.setor), linha('Máquina', r.maquina), linha('Produto', r.produto), linha('Ordem', r.ordem), linha('Supervisor', r.supervisor), linha('Turno', a.turno))), a.preparacao && a.preparacao.fim && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Preparação"), /*#__PURE__*/React.createElement("div", {
    className: "rel-grade"
  }, linha('Tempo de preparação', duracao(a.preparacao.minutos)), linha('Regulagem', a.preparacao.regulagem), linha('Linha', a.preparacao.linha), linha('Agulha', a.preparacao.agulha))), !p.vazio && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Prova inicial"), /*#__PURE__*/React.createElement("div", {
    className: "rel-grade"
  }, linha('Peças produzidas', p.quantidade), linha('Tempo', duracao(p.minutos)), linha('Tempo por peça', duracao(p.tempoPorPeca)), linha('Capacidade estimada', `${p.capacidadeHora} peças/hora`)), /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "A prova serve para estimar. O número que vale é o da validação.")), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Validação — ", duracao(an.periodo), an.modo === 'lote' && an.loteAlvo > 0 && ` (lote de ${an.loteAlvo} peças)`), (an.horas || []).length > 0 && /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Bloco"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Duração"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Produzidas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Boas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Rejeitadas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Ritmo"))), /*#__PURE__*/React.createElement("tbody", null, (an.horas || []).map(h => {
    const min = num(h.minutos) || 60;
    return /*#__PURE__*/React.createElement("tr", {
      key: h.id
    }, /*#__PURE__*/React.createElement("td", null, h.hora, "º"), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, duracao(min)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.produzidas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.boas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, h.rejeitadas || 0), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, min > 0 ? (num(h.boas) / (min / 60)).toFixed(1) + '/h' : '—'));
  }), /*#__PURE__*/React.createElement("tr", {
    className: "rel-total"
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, "Total")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, duracao(an.periodo))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, an.produzidas)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, an.boas)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, an.rejeitadas)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, an.boasPorHora, "/h")))))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "O que parou a produção"), an.paradaTotal === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Nenhuma parada registrada no período.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Motivo"), /*#__PURE__*/React.createElement("th", null, "De quem é a perda"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Tempo"))), /*#__PURE__*/React.createElement("tbody", null, (an.paradas || []).map(x => {
    const m = motivoParada(x.motivo);
    const o = ORIGENS_PERDA.find(y => y.id === x.origem);
    return /*#__PURE__*/React.createElement("tr", {
      key: x.id
    }, /*#__PURE__*/React.createElement("td", null, m.nome), /*#__PURE__*/React.createElement("td", null, o ? o.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, duracao(x.minutos)));
  }))), /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Perda do processo não é desempenho de quem opera. A separação está aqui para que a leitura seja justa."))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco rel-resultado"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Resultado"), /*#__PURE__*/React.createElement("div", {
    className: "rel-numeros"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Produzidas"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, an.produzidas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Boas"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, an.boas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Qualidade"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, an.qualidade !== null ? an.qualidade + '%' : '—')), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Utilização"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, an.utilizacao !== null ? an.utilizacao + '%' : '—')), /*#__PURE__*/React.createElement("div", {
    className: "destaque"
  }, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Capacidade validada"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, an.boasPorHora, " peças boas/hora"))), an.confianca && an.confianca.fraco && /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "⚠ ", an.confianca.aviso)), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Condições durante a medição"), /*#__PURE__*/React.createElement("div", {
    className: "rel-condicoes"
  }, CONDICOES_NORMAIS.map(c => /*#__PURE__*/React.createElement("span", {
    key: c.id,
    className: "rel-cond"
  }, (a.condicoes || {})[c.id] ? '☑' : '☐', " ", c.nome))), !r.condicoes.ok && /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "⚠ Faltaram condições essenciais:", ' ', r.condicoes.faltando.map(c => c.nome.toLowerCase()).join(', '), ". O número acima não representa a operação em condição normal.")), r.meta && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Meta operacional definida"), /*#__PURE__*/React.createElement("div", {
    className: "rel-grade"
  }, linha('Meta', `${r.meta.valorHora} peças boas por hora`), linha('Por turno de 8h', r.meta.porTurno), linha('Validada por', r.meta.validadaPor), linha('Capacidade medida', `${r.meta.capacidadeValidada} peças/hora`)), /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Justificativa: ", r.meta.justificativa)), /*#__PURE__*/React.createElement("div", {
    className: "rel-aviso"
  }, "Este documento registra a capacidade medida de uma operação em uma data específica. Serve para planejamento da produção, dimensionamento e identificação de necessidade de treinamento ou melhoria de processo. Não é instrumento de avaliação disciplinar. A assinatura do colaborador significa que ele tomou ciência dos números medidos, não que concorda com eventual meta decorrente deles."), /*#__PURE__*/React.createElement("div", {
    className: "rel-assinaturas"
  }, [['supervisor', 'Supervisor responsável', r.supervisor], ['colaborador', 'Colaborador aferido', r.colaborador]].map(([papel, rot, nome]) => {
    const feita = r.assinaturas[papel];
    return /*#__PURE__*/React.createElement("div", {
      key: papel,
      className: "rel-assina"
    }, /*#__PURE__*/React.createElement("div", {
      className: "linha-assinatura"
    }), /*#__PURE__*/React.createElement("div", {
      className: "rel-nome"
    }, feita && feita.nome || nome || ''), /*#__PURE__*/React.createElement("div", {
      className: "rel-papel"
    }, rot), feita && /*#__PURE__*/React.createElement("div", {
      className: "rel-data"
    }, "ciência registrada em ", fmtDate(String(feita.em || '').slice(0, 10))), feita && feita.observacao && /*#__PURE__*/React.createElement("div", {
      className: "rel-obs"
    }, feita.observacao));
  })), /*#__PURE__*/React.createElement("div", {
    className: "rel-rodape"
  }, a.codigo, " · gerado em ", fmtDate(todayISO()), r.completo ? ' · ciência registrada pelas duas partes' : '')), assinar && /*#__PURE__*/React.createElement(Modal, {
    title: assinar.papel === 'supervisor' ? 'Assinatura do supervisor' : 'Assinatura do colaborador',
    onClose: () => setAssinar(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Registre aqui depois que a folha impressa for assinada. O sistema guarda a data e o resultado que estava no papel — se a aferição for refeita, a assinatura antiga deixa de valer para o número novo."), assinar.papel === 'colaborador' && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, "Assinar é tomar ciência dos números, não concordar com a meta. Se a pessoa discordar de algo, registre abaixo — a observação fica no documento.")), /*#__PURE__*/React.createElement(Field, {
    label: "Nome de quem assinou *"
  }, /*#__PURE__*/React.createElement("input", {
    value: assinar.nome,
    autoFocus: true,
    onChange: e => setAssinar({
      ...assinar,
      nome: e.target.value
    })
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Observação de quem assinou"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: assinar.observacao,
    placeholder: "alguma ressalva sobre a medição, se houver",
    onChange: e => setAssinar({
      ...assinar,
      observacao: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setAssinar(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !String(assinar.nome).trim(),
    onClick: () => {
      let erro = null;
      update(d => {
        const x = registrarAssinatura(d, a.id, assinar, usuario);
        erro = x.erro || null;
        return d;
      });
      if (erro) alert(erro);else setAssinar(null);
    }
  }, "Registrar"))));
}

/* O painel da engenharia: o que está pronto, o que falta e em que
   versão o produto está. */
/* A engenharia como faixa: em que pé está o produto, sempre à vista. */
function FaixaEngenharia({ produto, db, update, usuario, podeEditar, aberta }) {
  const ck = checklistEngenharia(produto, db);
  const st = statusProduto(statusDoProduto(produto));
  const versoes = versoesDoProduto(db, produto.id);
  const mud = engenhariaMudou(produto, db);
  const cor = st.produz ? 'var(--ok)'
    : (st.id === 'bloqueado' || st.id === 'obsoleto') ? 'var(--danger)' : 'var(--warn)';

  /* na aba de identidade o painel completo já aparece abaixo; aqui
     basta a régua */
  if (aberta) return null;

  return React.createElement("div", { className: "faixa-eng" },
    React.createElement("div", { className: "faixa-item" },
      React.createElement("span", { className: "k" }, "Situação"),
      React.createElement("span", { className: "v", style: { color: cor } }, st.nome)),
    React.createElement("div", { className: "faixa-item" },
      React.createElement("span", { className: "k" }, "Engenharia"),
      React.createElement("span", { className: "v" }, `${ck.percentual}%`)),
    React.createElement("div", { className: "faixa-barra" },
      React.createElement("span", {
        className: "preenche",
        style: {
          width: `${ck.percentual}%`,
          background: ck.pronto ? 'var(--ok)' : 'var(--thread)'
        }
      })),
    React.createElement("div", { className: "faixa-item" },
      React.createElement("span", { className: "k" }, "Versão"),
      React.createElement("span", { className: "v" },
        versoes.length ? versoes[0].codigo : '—')),
    !ck.pronto && React.createElement("div", { className: "faixa-falta" },
      "falta: ", ck.pendentesObrigatorios.map(i => i.nome.toLowerCase()).join(', ')),
    mud.mudou && !mud.semVersao && React.createElement("div", { className: "faixa-falta" },
      "mudou desde a ", mud.versao.codigo));
}

function EngenhariaDoProduto({
  produto,
  db,
  update,
  usuario,
  podeEditar
}) {
  const ck = checklistEngenharia(produto, db);
  const st = statusProduto(statusDoProduto(produto));
  const versoes = versoesDoProduto(db, produto.id);
  const mud = engenhariaMudou(produto, db);
  const [mudarSt, setMudarSt] = useState(null);
  const [versionar, setVersionar] = useState(null);
  const [verVersao, setVerVersao] = useState(null);
  const cor = st.produz ? 'var(--ok)' : st.id === 'bloqueado' || st.id === 'obsoleto' ? 'var(--danger)' : 'var(--warn)';
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "painel-eng",
    style: {
      borderColor: cor
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 220
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Situação do produto"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 700,
      color: cor
    }
  }, st.nome), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, st.ajuda)), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Engenharia"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 700
    }
  }, ck.percentual, "%"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, versoes.length ? `versão ${versoes[0].codigo}` : 'sem versão congelada'))), /*#__PURE__*/React.createElement("div", {
    className: "trilho",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "preenche",
    style: {
      width: `${ck.percentual}%`,
      background: ck.pronto ? 'var(--ok)' : 'var(--thread)'
    }
  }))), !st.produz && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Este produto ainda não pode abrir ordem de produção normal."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, ck.pendentesObrigatorios.length > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, "Falta na engenharia:", ' ', ck.pendentesObrigatorios.map(i => i.nome.toLowerCase()).join(', '), ".") : /*#__PURE__*/React.createElement(React.Fragment, null, "A engenharia está completa — falta apenas mudar a situação para “Liberado para produção”."), ' ', st.amostra ? 'Ordem de amostra continua permitida — é assim que se fecha a engenharia.' : 'Nem ordem de amostra é permitida nesta situação.')), mud.mudou && !mud.semVersao && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600
    }
  }, "A engenharia mudou depois da versão ", mud.versao.codigo, "."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, mud.mudancas.slice(0, 4).map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, "· ", m.campo, ": ", m.de, " → ", m.para, m.variacao !== null && m.variacao !== undefined && ` (${m.variacao > 0 ? '+' : ''}${m.variacao}%)`)), mud.mudancas.length > 4 && /*#__PURE__*/React.createElement("div", null, "· e mais ", mud.mudancas.length - 4, "…")), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    style: {
      marginTop: 8
    },
    onClick: () => setVersionar({
      motivo: ''
    })
  }, "Congelar como nova versão")), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "O que a engenharia precisa ter"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, ck.itens.map(i => /*#__PURE__*/React.createElement("tr", {
    key: i.id
  }, /*#__PURE__*/React.createElement("td", {
    style: {
      width: 34,
      fontSize: 15
    }
  }, i.ok ? i.vazio ? '·' : '✓' : i.obrigatorio ? '🔴' : '⚠'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, i.nome), i.obrigatorio && /*#__PURE__*/React.createElement("span", {
    className: "tag",
    style: {
      marginLeft: 6
    }
  }, "obrigatório"), i.falta.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, i.falta.join(', ')), i.ok && i.vazio && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "não se aplica a este produto")))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 8
    }
  }, "🔴 impede liberar · ⚠ vale conferir, mas não trava")), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Mudar a situação"), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, STATUS_PRODUTO.filter(s => s.id !== st.id).map(s => /*#__PURE__*/React.createElement("button", {
    key: s.id,
    className: "btn ghost sm",
    onClick: () => setMudarSt({
      para: s.id,
      motivo: ''
    })
  }, s.nome))), !ck.pronto && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Liberar só depois que os itens obrigatórios estiverem completos.")), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      flex: 1,
      margin: 0
    }
  }, "Versões da engenharia"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setVersionar({
      motivo: ''
    })
  }, versoes.length ? '+ Nova versão' : 'Congelar versão inicial')), versoes.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma versão congelada. Enquanto não houver, mudar o tecido ou o tempo altera o custo de ordens antigas retroativamente — a versão existe para impedir isso.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Versão"), /*#__PURE__*/React.createElement("th", null, "O que mudou"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo material"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Tempo/peça"), /*#__PURE__*/React.createElement("th", null, "Quem"))), /*#__PURE__*/React.createElement("tbody", null, versoes.map(v => {
    const usada = (db.ordens || []).filter(o => o.versaoId === v.id).length;
    return /*#__PURE__*/React.createElement("tr", {
      key: v.id,
      style: {
        cursor: 'pointer'
      },
      onClick: () => setVerVersao(v)
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, v.codigo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, fmtDataHora(v.criadaEm)), usada > 0 && /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, usada, " ordem(ns)")), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, v.motivo || 'versão inicial', (v.mudancas || []).length > 0 && /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, (v.mudancas || []).slice(0, 2).map(m => m.campo).join(', '), (v.mudancas || []).length > 2 && '…')), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, money(v.retrato.custoMaterial)), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, duracao(v.retrato.minutosPorPeca)), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, v.criadaPor));
  })))), (produto.historicoStatus || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Histórico de situação"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, (produto.historicoStatus || []).slice().reverse().map(h => /*#__PURE__*/React.createElement("tr", {
    key: h.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, statusProduto(h.de).nome, " → ", /*#__PURE__*/React.createElement("strong", null, statusProduto(h.para).nome), h.motivo && /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, h.motivo)), /*#__PURE__*/React.createElement("td", {
    className: "small muted",
    style: {
      width: 140
    }
  }, h.quem, /*#__PURE__*/React.createElement("div", null, fmtDataHora(h.quando)))))))), mudarSt && /*#__PURE__*/React.createElement(Modal, {
    title: `Mudar para ${statusProduto(mudarSt.para).nome}`,
    onClose: () => setMudarSt(null)
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, statusProduto(mudarSt.para).ajuda, ".", mudarSt.para === 'liberado' && !ck.pronto && ' A engenharia obrigatória ainda não está completa — o sistema vai recusar.'), /*#__PURE__*/React.createElement(Field, {
    label: mudarSt.para === 'bloqueado' || mudarSt.para === 'obsoleto' ? 'Motivo *' : 'Motivo'
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: mudarSt.motivo,
    autoFocus: true,
    placeholder: "o que motivou a mudança",
    onChange: e => setMudarSt({
      ...mudarSt,
      motivo: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setMudarSt(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = mudarStatusProduto(d, produto.id, mudarSt.para, mudarSt.motivo, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setMudarSt(null);
    }
  }, "Confirmar"))), versionar && /*#__PURE__*/React.createElement(Modal, {
    title: "Congelar versão da engenharia",
    onClose: () => setVersionar(null),
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "A versão guarda uma fotografia da engenharia: materiais, roteiro, tempos e custo de agora. Ordens abertas daqui em diante ficam amarradas a ela, e as antigas continuam com a versão delas."), mud.mudancas.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600,
      marginBottom: 4
    }
  }, "O que mudou desde a última versão"), mud.mudancas.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small muted"
  }, "· ", m.campo, ": ", m.de, " → ", m.para, m.variacao !== null && m.variacao !== undefined && ` (${m.variacao > 0 ? '+' : ''}${m.variacao}%)`))), /*#__PURE__*/React.createElement(Field, {
    label: versoes.length ? 'O que mudou *' : 'Observação'
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: versionar.motivo,
    autoFocus: true,
    placeholder: "ex: tecido principal alterado de meia malha 30/1 para 24/1",
    onChange: e => setVersionar({
      ...versionar,
      motivo: e.target.value
    })
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setVersionar(null)
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => {
      let erro = null;
      update(d => {
        const r = congelarVersao(d, produto.id, versionar, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setVersionar(null);
    }
  }, "Congelar"))), verVersao && /*#__PURE__*/React.createElement(Modal, {
    title: `Engenharia na versão ${verVersao.codigo}`,
    onClose: () => setVerVersao(null),
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, verVersao.motivo || 'versão inicial', " ·", ' ', fmtDataHora(verVersao.criadaEm), " ·", ' ', verVersao.criadaPor), /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Custo de material"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, money(verVersao.retrato.custoMaterial))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Consumo"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, num(verVersao.retrato.consumoTotal).toFixed(4))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Tempo por peça"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, duracao(verVersao.retrato.minutosPorPeca)))), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "Materiais"), (verVersao.retrato.tecidos || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "nenhum") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, (verVersao.retrato.tecidos || []).map(t => /*#__PURE__*/React.createElement("tr", {
    key: t.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, t.material, t.apelido && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", t.apelido)), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, t.modelagem || 'sem modelagem'))))), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "Roteiro"), (verVersao.retrato.processo || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "nenhuma operação") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, (verVersao.retrato.processo || []).map(e => /*#__PURE__*/React.createElement("tr", {
    key: e.ordem
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, e.ordem, ". ", e.etapa, /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, e.setor)), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, duracao(e.minutosPorPeca)))))), (verVersao.mudancas || []).length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 12
    }
  }, "O que mudou da versão anterior"), (verVersao.mudancas || []).map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small muted"
  }, "· ", m.campo, ": ", m.de, " → ", m.para))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setVerVersao(null)
  }, "Fechar"))));
}

/* A porta do canal: a tela que o colaborador vê sem entrar no sistema.
   Reaproveita o canal que já existe, só que sem usuário logado — o
   registro entra direto no banco, então não há caixa para recolher. */
function PortaConversaAberta({
  db,
  update,
  onVoltar
}) {
  /* o QR de cada setor traz ?setor=slug */
  const setorInicial = (() => {
    try {
      const p = new URLSearchParams(window.location.search).get('setor');
      if (!p) return '';
      const alvo = String(p).toLowerCase();
      const dp = (db.departamentos || []).find(d => String(d.nome).normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') === alvo);
      return dp ? dp.id : '';
    } catch (e) {
      return '';
    }
  })();
  const dp = (db.departamentos || []).find(x => x.id === setorInicial);
  return /*#__PURE__*/React.createElement("div", {
    className: "porta-canal-tela"
  }, /*#__PURE__*/React.createElement("div", {
    className: "porta-topo"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Entrar no sistema"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), dp && /*#__PURE__*/React.createElement("span", {
    className: "setor-marca"
  }, "📍 ", dp.nome)), /*#__PURE__*/React.createElement(CanalColaborador, {
    db: db,
    update: update,
    usuario: null,
    setorInicial: setorInicial
  }), /*#__PURE__*/React.createElement("div", {
    className: "porta-rodape"
  }, "Este canal é aberto a todos. Para tratar as manifestações, é preciso entrar no sistema."));
}

/* A tela de relatórios: a relação do período e o documento de cada
   manifestação. Só administrador chega aqui. */
function RelatoriosCanal({
  db,
  usuario
}) {
  const hoje = todayISO();
  const mesPassado = (() => {
    const d = new Date(hoje);
    d.setDate(d.getDate() - 30);
    return d.toISOString().slice(0, 10);
  })();
  const [f, setF] = useState({
    de: mesPassado,
    ate: hoje,
    categoria: '',
    departamentoId: '',
    status: '',
    soAbertas: false
  });
  const [comTeor, setComTeor] = useState(false);
  const [aberta, setAberta] = useState(null);
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  if (!podeVerRelatorioCanal(usuario)) {
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--warn)',
        background: 'var(--warn-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "Relatório restrito ao administrador do sistema."), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "A relação completa reúne todas as manifestações do período, inclusive denúncias e relatos confidenciais. Quem trata um caso vê aquele caso; o conjunto é de quem responde pela empresa."));
  }
  const r = relatorioCanalPeriodo(db, f);
  if (aberta) {
    const m = (db.manifestacoes || []).find(x => x.id === aberta);
    if (!m) return /*#__PURE__*/React.createElement(Empty, {
      text: "Manifestação não encontrada."
    });
    return /*#__PURE__*/React.createElement(RelatorioManifestacao, {
      manifestacao: m,
      db: db,
      onVoltar: () => setAberta(null)
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      marginTop: 0
    }
  }, "Período e filtros"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "De"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.de,
    onChange: e => set('de', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Até"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.ate,
    onChange: e => set('ate', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.categoria,
    onChange: e => set('categoria', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), CATEGORIAS_CANAL.map(c => /*#__PURE__*/React.createElement("option", {
    key: c.id,
    value: c.id
  }, c.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Setor"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId,
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), (db.departamentos || []).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.status,
    onChange: e => set('status', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas"), STATUS_CANAL.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Atalho"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.soAbertas ? 'abertas' : '',
    onChange: e => set('soAbertas', e.target.value === 'abertas')
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todas as situações"), /*#__PURE__*/React.createElement("option", {
    value: "abertas"
  }, "Só as em aberto")))), r.sensiveis > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: comTeor,
    onChange: e => setComTeor(e.target.checked)
  }), /*#__PURE__*/React.createElement("span", null, "Incluir o texto dos ", r.sensiveis, " relato(s) sensível(is)")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "Sem marcar, denúncias e relatos confidenciais entram na contagem mas o texto fica de fora. Papel impresso circula: marque só se este relatório não vai sair da sua mão.")), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1
    }
  }, r.total, " manifestação(ões) no período"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => window.print()
  }, "Imprimir relação"))), /*#__PURE__*/React.createElement("div", {
    className: "rel-folha"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-cabecalho"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "rel-titulo"
  }, "Conversa aberta — relação do período"), /*#__PURE__*/React.createElement("div", {
    className: "rel-sub"
  }, fmtDate(f.de), " a ", fmtDate(f.ate), f.categoria && ` · ${categoriaCanal(f.categoria).nome}`, f.departamentoId && ` · ${((db.departamentos || []).find(d => d.id === f.departamentoId) || {}).nome || ''}`)), /*#__PURE__*/React.createElement("div", {
    className: "rel-codigo"
  }, /*#__PURE__*/React.createElement("div", null, r.total), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, "manifestações"))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Resumo"), /*#__PURE__*/React.createElement("div", {
    className: "rel-numeros"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Em aberto"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.abertas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Resolvidas"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.resolvidas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Encerradas"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.encerradas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Anônimas"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.anonimas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Reconhecimentos"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.reconhecimentos)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Fora do prazo"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.atrasadas)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "Tempo médio"), /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, r.tempoMedio !== null ? `${r.tempoMedio}d` : '—'))), r.noPrazo !== null && /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, r.noPrazo, "% das manifestações encerradas foram respondidas dentro do prazo da sua prioridade.", r.acoesAbertas > 0 && ` ${r.acoesAbertas} ação(ões) do plano seguem em aberto.`)), r.total === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Nenhuma manifestação neste período.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Manifestações"), /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Protocolo"), /*#__PURE__*/React.createElement("th", null, "Data"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", null, "Relato"), /*#__PURE__*/React.createElement("th", null, "Situação"), /*#__PURE__*/React.createElement("th", null, "Prazo"))), /*#__PURE__*/React.createElement("tbody", null, r.lista.map(m => {
    const dp = (db.departamentos || []).find(d => d.id === m.departamentoId);
    const p = prazoManifestacao(m);
    const esconder = m.sensivel && !comTeor;
    return /*#__PURE__*/React.createElement("tr", {
      key: m.id,
      className: "no-print-hover",
      onClick: () => setAberta(m.id),
      style: {
        cursor: 'pointer'
      }
    }, /*#__PURE__*/React.createElement("td", null, m.protocolo, m.anonima && /*#__PURE__*/React.createElement("div", {
      className: "rel-mini"
    }, "anônima")), /*#__PURE__*/React.createElement("td", null, fmtDataHora(m.criadaEm)), /*#__PURE__*/React.createElement("td", null, categoriaCanal(m.categoria).nome, m.sensivel && /*#__PURE__*/React.createElement("div", {
      className: "rel-mini"
    }, "restrito")), /*#__PURE__*/React.createElement("td", null, dp ? dp.nome : '—'), /*#__PURE__*/React.createElement("td", null, esconder ? /*#__PURE__*/React.createElement("span", {
      className: "rel-oculto"
    }, "texto não incluído neste relatório") : /*#__PURE__*/React.createElement(React.Fragment, null, String(m.descricao || '').slice(0, 90), String(m.descricao || '').length > 90 ? '…' : '')), /*#__PURE__*/React.createElement("td", null, statusCanal(m.status).nome), /*#__PURE__*/React.createElement("td", null, p.semPrazo ? '—' : p.atrasada ? /*#__PURE__*/React.createElement("strong", null, "venceu ", fmtDate(p.limite)) : fmtDate(p.limite)));
  })))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Por tipo"), /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("tbody", null, r.porCategoria.map(c => /*#__PURE__*/React.createElement("tr", {
    key: c.id
  }, /*#__PURE__*/React.createElement("td", null, c.nome), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, c.total)))))), r.porDepartamento.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Por setor"), /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("tbody", null, r.porDepartamento.map(d => /*#__PURE__*/React.createElement("tr", {
    key: d.id
  }, /*#__PURE__*/React.createElement("td", null, d.nome), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, d.total)))))), r.porTema.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Temas identificados"), /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("tbody", null, r.porTema.map(t => /*#__PURE__*/React.createElement("tr", {
    key: t.id
  }, /*#__PURE__*/React.createElement("td", null, t.nome), /*#__PURE__*/React.createElement("td", null, t.grupo), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, t.total))))), /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Classificação preliminar por palavras do texto — orienta a leitura, não conclui o que houve."))), /*#__PURE__*/React.createElement("div", {
    className: "rel-aviso"
  }, "Documento interno. Reúne manifestações do canal Conversa aberta, algumas feitas sob anonimato. Circular este relatório fora de quem trata os casos expõe quem confiou no canal — e é o tipo de coisa que faz as pessoas pararem de usar. Guarde ou destrua depois do uso."), /*#__PURE__*/React.createElement("div", {
    className: "rel-rodape"
  }, "Gerado em ", fmtDate(hoje), " por ", usuario ? usuario.nome : '', " ·", ' ', "período de ", fmtDate(f.de), " a ", fmtDate(f.ate))));
}

/* O documento de uma manifestação: tudo que aconteceu com ela. */
function RelatorioManifestacao({
  manifestacao,
  db,
  onVoltar
}) {
  const m = manifestacao;
  const cat = categoriaCanal(m.categoria);
  const p = prazoManifestacao(m);
  const dp = (db.departamentos || []).find(d => d.id === m.departamentoId);
  const linha = (rot, val) => /*#__PURE__*/React.createElement("div", {
    className: "rel-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "rot"
  }, rot), /*#__PURE__*/React.createElement("span", {
    className: "val"
  }, val || '—'));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions no-print",
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar à relação"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => window.print()
  }, "Imprimir")), m.sensivel && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print",
    style: {
      borderColor: 'var(--danger)',
      background: 'rgba(176,58,46,.06)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 600
    }
  }, "Relato de acesso restrito."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Imprimir cria uma cópia que sai do sistema e do controle de quem vê. Se precisar, imprima na hora do uso e não deixe o papel circulando.")), /*#__PURE__*/React.createElement("div", {
    className: "rel-folha"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-cabecalho"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "rel-titulo"
  }, cat.icone, " ", cat.nome), /*#__PURE__*/React.createElement("div", {
    className: "rel-sub"
  }, "Conversa aberta — registro completo")), /*#__PURE__*/React.createElement("div", {
    className: "rel-codigo"
  }, /*#__PURE__*/React.createElement("div", null, m.protocolo), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, fmtDataHora(m.criadaEm)))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-grade"
  }, linha('Situação', statusCanal(m.status).nome), linha('Prioridade', `${prioridadeCanal(m.prioridade).marca} ` + prioridadeCanal(m.prioridade).nome), linha('Setor', dp ? dp.nome : ''), linha('Local', m.local), linha('Quando aconteceu', fmtDate(m.dataOcorrencia)), linha('Frequência', m.frequencia), linha('Impacto', m.impacto), linha('Prazo', p.semPrazo ? 'não se aplica' : `${fmtDate(p.limite)}${p.atrasada ? ' — vencido' : ''}`), linha('Identificação', m.anonima ? 'Anônima' : `${m.nome || 'sem nome'}${m.matricula ? ' · ' + m.matricula : ''}`), linha('Pediu retorno', m.querRetorno ? 'Sim' : 'Não'))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "O relato"), /*#__PURE__*/React.createElement("div", {
    style: {
      whiteSpace: 'pre-wrap',
      fontSize: 12.5,
      lineHeight: 1.6
    }
  }, m.descricao), m.envolvidos && /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Pessoas citadas: ", m.envolvidos), (m.anexos || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, (m.anexos || []).length, " arquivo(s) anexado(s):", ' ', (m.anexos || []).map(x => x.nome).join(', '), " — disponíveis no sistema.")), (m.temas || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Classificação preliminar"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12
    }
  }, (m.temas || []).map(t => {
    const x = TEMAS_CANAL.find(y => y.id === t);
    return x ? x.nome : t;
  }).join(' · ')), /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Palavras encontradas no texto orientam a leitura. Não é conclusão do sistema sobre o que aconteceu.")), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Plano de ação"), (m.acoes || []).length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "rel-nota"
  }, "Nenhuma ação registrada.") : /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "O que será feito"), /*#__PURE__*/React.createElement("th", null, "Responsável"), /*#__PURE__*/React.createElement("th", null, "Prazo"), /*#__PURE__*/React.createElement("th", null, "Situação"))), /*#__PURE__*/React.createElement("tbody", null, (m.acoes || []).map(a => {
    const resp = (db.colaboradores || []).find(c => c.id === a.responsavelId);
    return /*#__PURE__*/React.createElement("tr", {
      key: a.id
    }, /*#__PURE__*/React.createElement("td", null, a.oQue, a.evidencia && /*#__PURE__*/React.createElement("div", {
      className: "rel-mini"
    }, a.evidencia)), /*#__PURE__*/React.createElement("td", null, resp ? resp.nome : '—'), /*#__PURE__*/React.createElement("td", null, fmtDate(a.prazo)), /*#__PURE__*/React.createElement("td", null, (STATUS_ACAO.find(x => x.id === a.status) || {}).nome));
  })))), (m.interacoes || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Retorno e notas"), (m.interacoes || []).map(i => /*#__PURE__*/React.createElement("div", {
    key: i.id,
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-mini"
  }, fmtDataHora(i.quando), " · ", i.quem, i.interno ? ' · nota interna' : ' · enviado ao colaborador'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12
    }
  }, i.texto)))), /*#__PURE__*/React.createElement("div", {
    className: "rel-bloco"
  }, /*#__PURE__*/React.createElement("div", {
    className: "rel-rot"
  }, "Histórico"), /*#__PURE__*/React.createElement("table", {
    className: "rel-tabela"
  }, /*#__PURE__*/React.createElement("tbody", null, (m.historico || []).map(h => /*#__PURE__*/React.createElement("tr", {
    key: h.id
  }, /*#__PURE__*/React.createElement("td", null, h.o_que, h.nota && /*#__PURE__*/React.createElement("div", {
    className: "rel-mini"
  }, h.nota)), /*#__PURE__*/React.createElement("td", null, h.quem), /*#__PURE__*/React.createElement("td", null, fmtDataHora(h.quando))))))), /*#__PURE__*/React.createElement("div", {
    className: "rel-aviso"
  }, m.anonima ? 'Esta manifestação foi feita de forma anônima. O sistema não guarda ' + 'nome, matrícula nem vínculo com o cadastro — não há como identificar ' + 'quem escreveu, e tentar descobrir contraria o propósito do canal.' : 'Manifestação identificada. Trate a identidade de quem relatou com o ' + 'mesmo cuidado que espera para a sua.'), /*#__PURE__*/React.createElement("div", {
    className: "rel-rodape"
  }, m.protocolo, " · gerado em ", fmtDate(todayISO()))));
}

/* As vagas como o colaborador vê: o que existe e o caminho para indicar. */
function VagasParaColaborador({
  db,
  update,
  usuario
}) {
  const [indicando, setIndicando] = useState(null);
  const [pronto, setPronto] = useState(null);
  const abertas = vagasAbertas(db);
  if (pronto) {
    return /*#__PURE__*/React.createElement("div", {
      className: "canal-pronto"
    }, /*#__PURE__*/React.createElement("div", {
      className: "canal-selo"
    }, "✓"), /*#__PURE__*/React.createElement("h2", null, "Indicação registrada"), /*#__PURE__*/React.createElement("p", {
      className: "small muted"
    }, "O RH vai entrar em contato com ", pronto.nomeCandidato, ". Você recebe retorno pelo protocolo abaixo, seja qual for o resultado."), /*#__PURE__*/React.createElement("div", {
      className: "canal-protocolo"
    }, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Protocolo"), /*#__PURE__*/React.createElement("strong", null, pronto.protocolo)), /*#__PURE__*/React.createElement("button", {
      className: "btn accent",
      onClick: () => setPronto(null)
    }, "Voltar às vagas"));
  }
  if (indicando) {
    return /*#__PURE__*/React.createElement(FormIndicacao, {
      vaga: indicando,
      db: db,
      update: update,
      usuario: usuario,
      onCancelar: () => setIndicando(null),
      onPronto: r => {
        setIndicando(null);
        setPronto(r);
      }
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "canal-cabecalho"
  }, /*#__PURE__*/React.createElement("h2", null, "🤝 Vagas abertas"), /*#__PURE__*/React.createElement("p", {
    className: "small muted"
  }, "Conhece alguém que se daria bem aqui? Indique. Quem já trabalha na fábrica sabe melhor do que qualquer anúncio o que a função exige.")), abertas.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma vaga aberta no momento."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "vagas-lista"
  }, abertas.map(v => {
    const dp = (db.departamentos || []).find(x => x.id === v.departamentoId);
    return /*#__PURE__*/React.createElement("div", {
      key: v.id,
      className: "vaga-card"
    }, /*#__PURE__*/React.createElement("div", {
      className: "vaga-topo"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "vaga-titulo"
    }, v.titulo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, dp ? dp.nome : 'setor a definir', " · ", v.tipo, " · ", v.turno, num(v.vagas) > 1 && ` · ${v.vagas} vagas`)), /*#__PURE__*/React.createElement("span", {
      className: "tag"
    }, v.codigo)), v.descricao && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        marginTop: 8
      }
    }, v.descricao), v.requisitos && /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 6
      }
    }, /*#__PURE__*/React.createElement("strong", null, "O que precisa:"), " ", v.requisitos), v.experiencia && /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: 3
      }
    }, /*#__PURE__*/React.createElement("strong", null, "Experiência:"), " ", v.experiencia), /*#__PURE__*/React.createElement("button", {
      className: "btn accent sm",
      style: {
        marginTop: 10
      },
      onClick: () => setIndicando(v)
    }, "Indicar alguém"));
  })));
}

/* O formulário de indicação. O cuidado central: o contato é de outra
   pessoa, que não está aqui para concordar. */
function FormIndicacao({
  vaga,
  db,
  update,
  usuario,
  onCancelar,
  onPronto
}) {
  const [f, setF] = useState({
    vagaId: vaga.id,
    nomeCandidato: '',
    contato: '',
    experiencia: '',
    relacao: '',
    observacao: '',
    nomeIndicador: usuario ? usuario.nome : '',
    colaboradorId: usuario ? usuario.id || '' : '',
    avisou: false
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const dp = (db.departamentos || []).find(x => x.id === vaga.departamentoId);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onCancelar
  }, "← Voltar às vagas"), /*#__PURE__*/React.createElement("div", {
    className: "canal-cabecalho"
  }, /*#__PURE__*/React.createElement("h2", null, "Indicar para ", vaga.titulo), /*#__PURE__*/React.createElement("p", {
    className: "small muted"
  }, dp ? dp.nome : 'setor a definir', " · ", vaga.tipo, " · ", vaga.turno)), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--thread)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, "Antes de continuar:"), " converse com a pessoa. Você vai passar o nome e o contato dela para o RH, e ninguém gosta de receber ligação de emprego sem saber por quê. Se ela topou, siga.")), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome de quem você indica *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nomeCandidato,
    autoFocus: true,
    onChange: e => set('nomeCandidato', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Telefone ou e-mail dela *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.contato,
    placeholder: "(11) 90000-0000",
    onChange: e => set('contato', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Experiência que ela tem"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.experiencia,
    placeholder: "ex: trabalhou 3 anos com overlock",
    onChange: e => set('experiencia', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Como vocês se conhecem"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.relacao,
    placeholder: "ex: trabalhamos juntos, é minha vizinha",
    onChange: e => set('relacao', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Seu nome *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nomeIndicador,
    onChange: e => set('nomeIndicador', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Quer dizer mais alguma coisa sobre ela?"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: f.observacao,
    placeholder: "o que faz você achar que ela se daria bem aqui",
    onChange: e => set('observacao', e.target.value)
  })), /*#__PURE__*/React.createElement("label", {
    className: "check"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: f.avisou,
    onChange: e => set('avisou', e.target.checked)
  }), /*#__PURE__*/React.createElement("span", null, "Já falei com a pessoa e ela sabe que está sendo indicada")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 8
    }
  }, "A sua indicação não garante a contratação, mas garante que o RH vai falar com ela e te dar um retorno."), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onCancelar
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !f.avisou || String(f.nomeCandidato).trim().length < 3 || String(f.contato).trim().length < 8 || !String(f.nomeIndicador).trim(),
    onClick: () => {
      let erro = null,
        feito = null;
      update(d => {
        const r = registrarIndicacao(d, f, usuario);
        erro = r.erro || null;
        feito = r.indicacao || null;
        return d;
      });
      if (erro) alert(erro);else onPronto(feito);
    }
  }, "Enviar indicação")));
}

/* A tela do RH: cadastrar vagas e cuidar das indicações. */
function GestaoVagas({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [aba, setAba] = useState('vagas');
  const [editando, setEditando] = useState(null);
  const [abrindo, setAbrindo] = useState(null);
  const p = painelVagas(db);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cards"
  }, /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Vagas abertas"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, p.abertas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, p.postosAbertos, " posto(s)")), /*#__PURE__*/React.createElement("div", {
    className: "card"
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Indicações"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, p.indicacoes), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, p.contratados, " contratado(s)")), /*#__PURE__*/React.createElement("div", {
    className: "card" + (p.semRetorno ? ' alerta' : '')
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Sem contato ainda"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, p.semRetorno), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "esperando o RH")), /*#__PURE__*/React.createElement("div", {
    className: "card" + (p.paradas ? ' alerta' : '')
  }, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Paradas há mais de 7 dias"), /*#__PURE__*/React.createElement("div", {
    className: "v"
  }, p.paradas), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "quem indicou está esperando"))), p.paradas > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, p.paradas, " indicação(ões) sem movimento há mais de uma semana."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Quem indicou colocou a própria reputação no meio e está esperando notícia. Mesmo \"não seguiu\" é melhor que silêncio.")), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'vagas',
      label: `Vagas (${(db.vagas || []).length})`
    }, {
      id: 'indicacoes',
      label: `Indicações (${(db.indicacoes || []).length})`
    }]
  }), aba === 'vagas' && /*#__PURE__*/React.createElement(React.Fragment, null, podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => setEditando({
      tipo: TIPOS_VAGA[0],
      turno: TURNOS_VAGA[3],
      vagas: 1,
      status: 'aberta'
    })
  }, "+ Vaga")), (db.vagas || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma vaga cadastrada."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Vaga"), /*#__PURE__*/React.createElement("th", null, "Setor"), /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Postos"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Indicações"), /*#__PURE__*/React.createElement("th", null, "Situação"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 60
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.vagas || []).map(v => {
    const dp = (db.departamentos || []).find(x => x.id === v.departamentoId);
    const st = statusVaga(v.status);
    const qtd = indicacoesDaVaga(db, v.id).length;
    return /*#__PURE__*/React.createElement("tr", {
      key: v.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, v.titulo), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, v.codigo)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, dp ? dp.nome : '—'), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, v.tipo), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, v.vagas), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, qtd), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: st.recebe ? 'var(--ok)' : undefined
      }
    }, st.nome)), podeEditar && /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic",
      onClick: () => setEditando({
        ...v
      })
    }, "✎")));
  }))))), aba === 'indicacoes' && ((db.indicacoes || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma indicação recebida."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Protocolo"), /*#__PURE__*/React.createElement("th", null, "Indicado"), /*#__PURE__*/React.createElement("th", null, "Vaga"), /*#__PURE__*/React.createElement("th", null, "Quem indicou"), /*#__PURE__*/React.createElement("th", null, "Situação"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 90
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.indicacoes || []).slice().reverse().map(i => {
    const st = statusIndicacao(i.status);
    const ultimo = (i.historico || []).map(h => h.quando).sort().pop() || i.criadaEm;
    const dias = (() => {
      const d0 = dataValida(ultimo);
      return d0 ? Math.floor((Date.now() - d0) / 86400000) : null;
    })();
    return /*#__PURE__*/React.createElement("tr", {
      key: i.id
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, i.protocolo, /*#__PURE__*/React.createElement("div", {
      className: "muted"
    }, fmtDataHora(i.criadaEm))), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, i.nomeCandidato), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, i.contato)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, i.vagaTitulo), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, i.nomeIndicador), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, st.nome, !st.fim && dias !== null && dias > 7 && /*#__PURE__*/React.createElement("div", {
      style: {
        color: 'var(--warn)'
      }
    }, "parada há ", dias, "d")), podeEditar && /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setAbrindo(i.id)
    }, "Abrir")));
  }))))), editando && /*#__PURE__*/React.createElement(VagaModal, {
    vaga: editando,
    db: db,
    update: update,
    usuario: usuario,
    onClose: () => setEditando(null)
  }), abrindo && /*#__PURE__*/React.createElement(IndicacaoModal, {
    id: abrindo,
    db: db,
    update: update,
    usuario: usuario,
    onClose: () => setAbrindo(null)
  }));
}
function VagaModal({
  vaga,
  db,
  update,
  usuario,
  onClose
}) {
  const [f, setF] = useState({
    ...vaga
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const pendentes = vaga.id ? indicacoesDaVaga(db, vaga.id).filter(i => !statusIndicacao(i.status).fim).length : 0;
  return /*#__PURE__*/React.createElement(Modal, {
    title: vaga.id ? `Vaga ${vaga.codigo}` : 'Nova vaga',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Cargo *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.titulo || '',
    autoFocus: true,
    placeholder: "ex: Costureira de máquina reta",
    onChange: e => set('titulo', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Setor"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId || '',
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— a definir —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome))))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tipo,
    onChange: e => set('tipo', e.target.value)
  }, TIPOS_VAGA.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x)))), /*#__PURE__*/React.createElement(Field, {
    label: "Turno"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.turno,
    onChange: e => set('turno', e.target.value)
  }, TURNOS_VAGA.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }, x)))), /*#__PURE__*/React.createElement(Field, {
    label: "Quantas pessoas"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: f.vagas || 1,
    onChange: e => set('vagas', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "O que a pessoa vai fazer"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: 3,
    value: f.descricao || '',
    placeholder: "descreva com as palavras do dia a dia, não de anúncio",
    onChange: e => set('descricao', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "O que precisa"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.requisitos || '',
    placeholder: "ex: saber usar overlock e galoneira",
    onChange: e => set('requisitos', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Experiência"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.experiencia || '',
    placeholder: "ex: 1 ano na função",
    onChange: e => set('experiencia', e.target.value)
  }))), vaga.id && /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.status,
    onChange: e => set('status', e.target.value)
  }, STATUS_VAGA.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), vaga.id && f.status !== vaga.status && !statusVaga(f.status).recebe && pendentes > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, "Existem ", pendentes, " indicação(ões) sem desfecho nesta vaga. Fechar a vaga não fecha elas — dê o retorno a quem indicou antes de esquecer.")), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A faixa salarial fica de fora do anúncio de propósito: divulgar valor no chão de fábrica levanta comparação com quem já está na função. O RH informa na conversa."), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !String(f.titulo || '').trim(),
    onClick: () => {
      let erro = null;
      update(d => {
        const r = salvarVaga(d, f, usuario);
        erro = r.erro || null;
        if (!erro && vaga.id && f.status !== vaga.status) mudarStatusVaga(d, vaga.id, f.status, '', usuario);
        return d;
      });
      if (erro) alert(erro);else onClose();
    }
  }, "Salvar")));
}
function IndicacaoModal({
  id,
  db,
  update,
  usuario,
  onClose
}) {
  const i = (db.indicacoes || []).find(x => x.id === id);
  const [mover, setMover] = useState('');
  const [nota, setNota] = useState('');
  const [retorno, setRetorno] = useState('');
  if (!i) return null;
  const st = statusIndicacao(i.status);
  return /*#__PURE__*/React.createElement(Modal, {
    title: `${i.protocolo} · ${i.nomeCandidato}`,
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "painel-meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Vaga"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, i.vagaTitulo)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Contato"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, i.contato)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Indicado por"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, i.nomeIndicador)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "k"
  }, "Situação"), /*#__PURE__*/React.createElement("div", {
    className: "v",
    style: {
      fontSize: 14
    }
  }, st.nome))), i.experiencia && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("strong", null, "Experiência:"), " ", i.experiencia), i.relacao && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Conhecem-se: ", i.relacao), i.observacao && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginTop: 6
    }
  }, i.observacao), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 10,
      marginBottom: 10
    }
  }, i.nomeCandidato, " soube que estava sendo indicado(a) — quem indicou confirmou isso ao enviar."), !st.fim && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "etp-rot"
  }, "Mover para"), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Situação"
  }, /*#__PURE__*/React.createElement("select", {
    value: mover,
    onChange: e => setMover(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha —"), STATUS_INDICACAO.filter(x => x.id !== i.status).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: mover === 'nao_seguiu' ? 'Motivo *' : 'Nota'
  }, /*#__PURE__*/React.createElement("input", {
    value: nota,
    onChange: e => setNota(e.target.value),
    placeholder: mover === 'nao_seguiu' ? 'o que quem indicou vai ler' : ''
  }))), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    disabled: !mover,
    onClick: () => {
      let erro = null;
      update(d => {
        const r = moverIndicacao(d, id, mover, nota, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else {
        setMover('');
        setNota('');
      }
    }
  }, "Mover")), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 14
    }
  }, "Retorno para quem indicou"), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: retorno,
    style: {
      flex: 1
    },
    placeholder: `o que ${i.nomeIndicador} vai ler`,
    onChange: e => setRetorno(e.target.value)
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    disabled: !retorno.trim(),
    onClick: () => {
      update(d => {
        responderIndicacao(d, id, retorno, usuario);
        return d;
      });
      setRetorno('');
    }
  }, "Enviar")), (i.retornos || []).map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, fmtDataHora(r.quando), " · ", r.quem, ": ", r.texto)), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      marginTop: 14
    }
  }, "Histórico"), (i.historico || []).map(h => /*#__PURE__*/React.createElement("div", {
    key: h.id,
    className: "small muted"
  }, fmtDataHora(h.quando), " · ", h.o_que, h.nota && ` — ${h.nota}`)), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Fechar")));
}

/* ---------------- Ficha técnica ----------------
   O papel que desce para a fábrica: o que comprar, o que cortar,
   o que costurar. Tudo que o sistema já sabe, numa folha só. */
function FichaTecnica({
  produto,
  db,
  update,
  usuario,
  podeEditar,
  custo
}) {
  const c = custo;
  const nome = montarNomeProduto(produto, db);
  const grupo = (db.gruposProduto || []).find(g => g.id === produto.grupoId);
  const tipo = (db.tiposProduto || []).find(x => x.id === produto.tipoId);
  const ficha = produto.ficha || {};
  const setF = (k, v) => update(d => {
    d.produtos = d.produtos.map(p => p.id !== produto.id ? p : {
      ...p,
      ficha: {
        ...(p.ficha || {}),
        [k]: v
      }
    });
    return d;
  });
  const setP = (k, v) => update(d => {
    d.produtos = d.produtos.map(p => {
      if (p.id !== produto.id) return p;
      const novo = {
        ...p,
        [k]: v
      };
      novo.nome = montarNomeProduto(novo, d);
      return novo;
    });
    return d;
  });
  const mo = custoMaoDeObra(produto, db);
  const etiqueta = ficha.etiqueta || {};
  const setE = (k, v) => setF('etiqueta', {
    ...etiqueta,
    [k]: v
  });

  /* a composição já está no cadastro do tecido — aqui ela só é oferecida */
  const composicaoSugerida = c.cortaveis.map(t => t.material && t.material.composicao).filter(Boolean).join(' / ');
  const LIMITE_IMG = 900 * 1024;
  async function anexarImagem(e) {
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > LIMITE_IMG) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB e passa do limite ` + `de 900 KB. Reduza a foto antes de anexar — a ficha inteira fica guardada no navegador.`);
    try {
      const url = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler a imagem'));
        r.readAsDataURL(file);
      });
      setF('imagem', {
        nome: file.nome || file.name,
        tamanho: file.size,
        url
      });
    } catch (err) {
      alert(err.message);
    }
  }

  /* o desenho de cada parte, reaproveitando a geometria do risco */
  const desenhos = c.subprodutos.map(sp => {
    const risco = (db.riscos || []).find(r => r.id === sp.riscoId);
    if (!risco || !risco.geometria) return {
      sub: sp,
      peca: null,
      marcas: []
    };
    const contornos = risco.geometria.map(g => ({
      ...g,
      caixa: caixaDe(g.pontos),
      area: areaPoligono(g.pontos)
    }));
    const porMm = escalaPorId(risco.escalaId).porMm;
    /* a peça cuja assinatura bate com a do subproduto */
    const peca = contornos.filter(x => x.tipo === 'peca').find(x => {
      const l = x.caixa.largura / porMm,
        al = x.caixa.altura / porMm;
      return Math.abs(l - num(sp.larguraMm)) < 2 && Math.abs(al - num(sp.alturaMm)) < 2;
    });
    return {
      sub: sp,
      peca,
      marcas: peca ? contornos.filter(x => x.paiIndice === peca.indice) : []
    };
  });
  const totalAviamentos = c.aviamentos.reduce((a, t) => a + t.custo, 0);
  const totalTecidos = c.cortaveis.reduce((a, t) => a + t.custo, 0);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Ficha técnica"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => window.print()
  }, "🖨 Imprimir")), /*#__PURE__*/React.createElement("div", {
    className: "ficha-folha"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ficha-cab"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ficha-foto"
  }, ficha.imagem ? /*#__PURE__*/React.createElement("img", {
    src: ficha.imagem.url,
    alt: nome
  }) : /*#__PURE__*/React.createElement("div", {
    className: "ficha-foto-vazia"
  }, "sem imagem")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 200
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "ficha-selo"
  }, "Ficha técnica de produto"), /*#__PURE__*/React.createElement("div", {
    className: "ficha-nome"
  }, nome || '(produto incompleto)'), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Código ", /*#__PURE__*/React.createElement("strong", {
    style: {
      fontFamily: 'var(--mono)'
    }
  }, codigoCompletoProduto(produto, db)), ficha.referencia && ` · Referência ${ficha.referencia}`)), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    },
    className: "small muted"
  }, /*#__PURE__*/React.createElement("div", null, "Emitida em ", fmtDate(todayISO())), produto.criadoEm && /*#__PURE__*/React.createElement("div", null, "Cadastrado em ", fmtDate(produto.criadoEm)), usuario && /*#__PURE__*/React.createElement("div", null, usuario.nome))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Identificação"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    style: {
      width: '26%'
    }
  }, "Grupo"), /*#__PURE__*/React.createElement("td", null, grupo ? grupo.nome : '—'), /*#__PURE__*/React.createElement("td", {
    style: {
      width: '22%'
    }
  }, "Tipo"), /*#__PURE__*/React.createElement("td", null, tipo ? tipo.nome : '—')), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Medida"), /*#__PURE__*/React.createElement("td", null, textoMedida(produto)), /*#__PURE__*/React.createElement("td", null, "Cliente"), /*#__PURE__*/React.createElement("td", null, ficha.cliente || '—')), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Consumo de tecido"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, c.consumoTotal.toFixed(4), " m"), " por produto"), /*#__PURE__*/React.createElement("td", null, "Tempo de produção"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, duracao(mo.minutos)), " por peça")), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Custo do material"), /*#__PURE__*/React.createElement("td", null, money(c.custoTecido)), /*#__PURE__*/React.createElement("td", null, "Mão de obra"), /*#__PURE__*/React.createElement("td", null, money(mo.total), mo.temEstimativa && /*#__PURE__*/React.createElement("span", {
    className: "est"
  }, " (parcialmente estimada)")))))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Tecidos e modelagens de corte"), c.cortaveis.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhum tecido definido.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Tecido"), /*#__PURE__*/React.createElement("th", null, "Modelagem"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Largura"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Rende"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Consumo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Aproveit."), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"))), /*#__PURE__*/React.createElement("tbody", null, c.cortaveis.map(t => /*#__PURE__*/React.createElement("tr", {
    key: t.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, t.material ? t.material.nome : '—'), t.material && t.material.composicao && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, t.material.composicao, t.material.gramatura ? ` · ${t.material.gramatura} g/m²` : '')), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, t.risco ? /*#__PURE__*/React.createElement(React.Fragment, null, t.risco.codigo, /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, t.risco.arquivo)) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--warn)'
    }
  }, "não definido")), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, t.risco ? t.risco.larguraTecidoM + ' m' : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, t.calculo ? t.calculo.porRisco : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, t.consumo > 0 ? t.consumo.toFixed(4) + ' m' : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, t.risco ? t.risco.aproveitamento + '%' : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(t.custo))))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "6"
  }, /*#__PURE__*/React.createElement("strong", null, "Tecidos")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(totalTecidos))))))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Partes do produto"), c.subprodutos.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma parte — vincule uma modelagem ao tecido.") : /*#__PURE__*/React.createElement("div", {
    className: "ficha-partes"
  }, desenhos.map(({
    sub,
    peca,
    marcas
  }) => /*#__PURE__*/React.createElement("div", {
    key: sub.id,
    className: "ficha-parte"
  }, peca ? /*#__PURE__*/React.createElement(MiniPeca, {
    peca: peca,
    marcacoes: marcas,
    tamanho: 70
  }) : /*#__PURE__*/React.createElement("div", {
    className: "mini-peca",
    style: {
      width: 70,
      height: 70
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 130
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 600
    }
  }, sub.nome, sub.tamanho && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", sub.tamanho)), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, sub.codigo), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, sub.larguraMm, " × ", sub.alturaMm, " mm · ", num(sub.areaM2).toFixed(4), " m²"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, sub.porProduto, " por produto · ", num(sub.consumo).toFixed(4), " m ·", ' ', money(sub.custo))))))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Aviamentos e demais materiais"), c.aviamentos.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhum aviamento informado.") : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", null, "Unidade"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Consumo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Preço"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"))), /*#__PURE__*/React.createElement("tbody", null, c.aviamentos.map(t => /*#__PURE__*/React.createElement("tr", {
    key: t.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, t.material ? t.material.nome : '—'), t.material && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, t.material.codigo)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, (() => {
    const onde = etapasDoMaterial(t.id, produto, db);
    return onde.length ? onde.map(x => x.etapa ? x.etapa.nome : 'etapa').join(', ') : /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "não definida");
  })()), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, t.material ? t.material.unidadeEstoque : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, t.consumo > 0 ? num(t.consumo).toFixed(4) : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, t.material ? money(custoDoMaterial(t.material)) : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(t.custo))))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "4"
  }, /*#__PURE__*/React.createElement("strong", null, "Aviamentos")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(totalAviamentos))))))), (c.porMaterial || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Consumo total por material"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 6
    }
  }, "O que sai do estoque para fazer uma peça. Material usado em mais de um lugar aparece somado — é assim que se compra e se reserva."), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Material"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Usos"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Consumo por peça"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"))), /*#__PURE__*/React.createElement("tbody", null, (c.porMaterial || []).map(g => /*#__PURE__*/React.createElement("tr", {
    key: g.materialId
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, g.nome), g.material && g.material.codigo && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, g.material.codigo), g.usos.length > 1 && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, g.usos.map(u => u.apelido || (u.risco ? u.risco.codigo : u.cortavel ? 'sem modelagem' : 'quantidade informada')).join(' + '))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, g.usos.length > 1 ? /*#__PURE__*/React.createElement("strong", null, g.usos.length, "×") : g.usos.length), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, g.consumo > 0 ? `${num(g.consumo).toFixed(4)} ${g.unidade}` : /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "indefinido")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, g.custo > 0 ? money(g.custo) : '—'))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "3"
  }, /*#__PURE__*/React.createElement("strong", null, "Total de material por peça")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(c.custoTecido))))))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Custo do material por produto"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    style: {
      width: '70%'
    }
  }, "Tecidos"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(totalTecidos))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Aviamentos"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(totalAviamentos))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, "Total")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 15
    }
  }, money(c.custoTecido)))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "Mão de obra e despesas não entram aqui — este é o custo de material.")), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Grade de tamanhos"), /*#__PURE__*/React.createElement(GradeTamanhos, {
    grade: ficha.grade,
    podeEditar: podeEditar,
    onChange: g => setF('grade', g)
  })), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Etiqueta"), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Composição"
  }, /*#__PURE__*/React.createElement("input", {
    value: etiqueta.composicao || '',
    onChange: e => setE('composicao', e.target.value),
    placeholder: composicaoSugerida || '100% Poliéster'
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Origem"
  }, /*#__PURE__*/React.createElement("input", {
    value: etiqueta.origem || '',
    onChange: e => setE('origem', e.target.value),
    placeholder: "Indústria Brasileira"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Fabricante / CNPJ"
  }, /*#__PURE__*/React.createElement("input", {
    value: etiqueta.fabricante || '',
    onChange: e => setE('fabricante', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Código de barras / EAN"
  }, /*#__PURE__*/React.createElement("input", {
    value: etiqueta.ean || '',
    onChange: e => setE('ean', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Instruções de conservação"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 5,
      flexWrap: 'wrap',
      marginBottom: 7
    }
  }, CUIDADOS_PADRAO.map(x => {
    const on = (etiqueta.cuidados || []).includes(x);
    return /*#__PURE__*/React.createElement("button", {
      key: x,
      type: "button",
      className: "comp-chip" + (on ? ' on' : ''),
      onClick: () => setE('cuidados', on ? (etiqueta.cuidados || []).filter(y => y !== x) : [...(etiqueta.cuidados || []), x])
    }, x);
  })), /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: etiqueta.cuidadosExtra || '',
    onChange: e => setE('cuidadosExtra', e.target.value),
    placeholder: "outra instrução, se houver"
  }))), /*#__PURE__*/React.createElement("div", {
    className: "etiqueta-peca"
  }, /*#__PURE__*/React.createElement("div", {
    className: "etiqueta-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ficha-rot"
  }, "Composição"), /*#__PURE__*/React.createElement("strong", null, etiqueta.composicao || composicaoSugerida || /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "não informada"))), (etiqueta.cuidados || []).length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "etiqueta-linha"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ficha-rot"
  }, "Conservação"), /*#__PURE__*/React.createElement("div", null, (etiqueta.cuidados || []).join(' · '))), etiqueta.cuidadosExtra && /*#__PURE__*/React.createElement("div", {
    className: "etiqueta-linha small"
  }, etiqueta.cuidadosExtra), /*#__PURE__*/React.createElement("div", {
    className: "etiqueta-rodape small muted"
  }, [etiqueta.origem, etiqueta.fabricante, etiqueta.ean && `EAN ${etiqueta.ean}`, textoMedida(produto)].filter(Boolean).join(' · ') || '—'))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Processo produtivo — tempo e custo por departamento"), mo.semProcesso ? /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Nenhuma etapa no processo. Monte a sequência na aba", ' ', /*#__PURE__*/React.createElement("strong", null, "Processo produtivo"), ".") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    style: {
      width: 28
    }
  }, "#"), /*#__PURE__*/React.createElement("th", null, "Etapa"), /*#__PURE__*/React.createElement("th", null, "Departamento"), /*#__PURE__*/React.createElement("th", null, "Modo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Tempo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Pessoas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Min/peça"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"))), /*#__PURE__*/React.createElement("tbody", null, mo.passos.map((p, i) => /*#__PURE__*/React.createElement("tr", {
    key: p.passo.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, i + 1), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, p.etapa ? p.etapa.nome : '(etapa removida)')), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, p.departamento ? p.departamento.nome : '—', p.equipamento && /*#__PURE__*/React.createElement("div", null, p.equipamento.nome)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, modoEtapa(p.modo).nome, p.modo === 'projeto' && /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, "÷ ", p.lote, " peças")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(p.tempo), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, segundosSeAjudar(p.tempo) ? segundosSeAjudar(p.tempo) + ' · ' : '', p.modo === 'projeto' ? 'por lote' : 'por peça')), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, p.pessoas), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(p.minutosHomem), segundosSeAjudar(p.minutosHomem) && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, segundosSeAjudar(p.minutosHomem))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.custo > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, money(p.custo), p.estimado && /*#__PURE__*/React.createElement("span", {
    className: "est"
  }, "*")) : '—'))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "5"
  }, /*#__PURE__*/React.createElement("strong", null, "Total por peça"), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "lote de ", mo.lote, " peças")), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, mo.minutos.toFixed(3), " min"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, mo.minutosHomem.toFixed(4))), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(mo.total)))))), /*#__PURE__*/React.createElement("div", {
    className: "etp-rot",
    style: {
      margin: '14px 0 6px'
    }
  }, "Resumo por departamento"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Departamento"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Etapas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Min-homem"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Equipe"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo/min"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo/peça"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "%"))), /*#__PURE__*/React.createElement("tbody", null, mo.porDepartamento.map(d => /*#__PURE__*/React.createElement("tr", {
    key: d.nome,
    className: d.custoDep && d.custoDep.semSalario ? 'linha-atraso' : ''
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, d.nome)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, d.etapas.length), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, duracao(d.minutosHomem)), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, d.custoDep ? d.custoDep.pessoas : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, d.custoMinuto > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, money(d.custoMinuto), d.estimado && /*#__PURE__*/React.createElement("span", {
    className: "est"
  }, "*")) : '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, d.custo > 0 ? money(d.custo) : '—')), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, d.percentual, "%"))), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "5"
  }, /*#__PURE__*/React.createElement("strong", null, "Mão de obra por peça")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, money(mo.total))), /*#__PURE__*/React.createElement("td", null)))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "O custo do minuto vem da folha do departamento, com encargos, dividida pelos ", jornadaDe(db).produtivo, " minutos produtivos ×", ' ', (db.parametrosMaoDeObra || {}).diasUteis || DIAS_UTEIS_MES, " dias úteis do mês."), mo.temEstimativa && !mo.geral.vazia && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      marginTop: 6,
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "* Custo estimado pela média da fábrica: ", mo.estimados.join(', '), mo.estimados.length === 1 ? ' ainda não tem' : ' ainda não têm', " equipe própria cadastrada."), mo.avisos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "no-print panel",
    style: {
      marginTop: 8,
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, mo.avisos.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", x)), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "Para o custo ficar exato, cada colaborador precisa de", ' ', /*#__PURE__*/React.createElement("strong", null, "salário"), " e ", /*#__PURE__*/React.createElement("strong", null, "departamento"), " em", ' ', "Cadastros → Colaboradores. Confira em Engenharia → Jornada e mão de obra.")))), !mo.semProcesso && /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Custo total do produto"), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    style: {
      width: '70%'
    }
  }, "Material (tecidos e aviamentos)"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(c.custoTecido))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Mão de obra"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, money(mo.total))), (() => {
    /* o custo da fábrica entra na ficha: sem ele, "custo por peça" é
       uma parte do custo com nome de total */
    const ind = custoIndiretoProduto(produto, db);
    return /*#__PURE__*/React.createElement("tr", null,
      /*#__PURE__*/React.createElement("td", null, "Custo da fábrica",
        !ind.configurado && /*#__PURE__*/React.createElement("div", {
          className: "small muted"
        }, "não cadastrado — o total está incompleto")),
      /*#__PURE__*/React.createElement("td", { className: "num" },
        ind.configurado ? money(ind.custo) : '—'));
  })(), /*#__PURE__*/React.createElement("tr", {
    className: "ficha-total"
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, "Custo por peça")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 16
    }
  }, money(custoCompletoProduto(produto, db).total))))))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao"
  }, /*#__PURE__*/React.createElement("h4", null, "Instruções de produção"), podeEditar ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "grid2 no-print"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Cliente"
  }, /*#__PURE__*/React.createElement("input", {
    value: ficha.cliente || '',
    onChange: e => setF('cliente', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Referência do cliente"
  }, /*#__PURE__*/React.createElement("input", {
    value: ficha.referencia || '',
    onChange: e => setF('referencia', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "📷 ", ficha.imagem ? 'Trocar imagem' : 'Anexar imagem do produto', /*#__PURE__*/React.createElement("input", {
    type: "file",
    accept: "image/*",
    style: {
      display: 'none'
    },
    onChange: anexarImagem
  })), ficha.imagem && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setF('imagem', null)
  }, "Remover imagem"), ficha.imagem && /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, ficha.imagem.nome, " · ", Math.round(num(ficha.imagem.tamanho) / 1024), " KB")), /*#__PURE__*/React.createElement(Field, {
    label: "Acabamento e embalagem"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: ficha.acabamento || '',
    onChange: e => setF('acabamento', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: ficha.observacoes || '',
    onChange: e => setF('observacoes', e.target.value)
  })))) : null, /*#__PURE__*/React.createElement("div", {
    className: "ficha-texto"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "ficha-rot"
  }, "Acabamento e embalagem"), /*#__PURE__*/React.createElement("div", null, ficha.acabamento || /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "não informado"))), ficha.observacoes && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    className: "ficha-rot"
  }, "Observações"), /*#__PURE__*/React.createElement("div", null, ficha.observacoes)))), c.avisos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "ficha-secao no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)',
      marginBottom: 0
    }
  }, c.avisos.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", x)), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "A ficha imprime mesmo assim, mas com esses pontos em aberto."))), /*#__PURE__*/React.createElement("div", {
    className: "ficha-assina"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "linha"
  }), "Modelagem"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "linha"
  }), "Corte"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "linha"
  }), "Produção"))), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "no-print",
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Ajustar a identificação"), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Grupo"
  }, /*#__PURE__*/React.createElement("select", {
    value: produto.grupoId || '',
    onChange: e => setP('grupoId', e.target.value)
  }, (db.gruposProduto || []).map(g => /*#__PURE__*/React.createElement("option", {
    key: g.id,
    value: g.id
  }, g.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: produto.tipoId || '',
    onChange: e => setP('tipoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem gravação —"), (db.tiposProduto || []).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome))))), /*#__PURE__*/React.createElement(Field, {
    label: "Complemento do nome"
  }, /*#__PURE__*/React.createElement("input", {
    value: produto.complemento || '',
    onChange: e => setP('complemento', e.target.value)
  })))));
}

/* ---------------- Todos os subprodutos ---------------- */
/* Um subproduto avulso: nem toda parte vem de modelagem. Bolso aplicado,
   etiqueta bordada, punho comprado pronto — coisas que percorrem a
   produção mas não saem do risco. */
function salvarSubprodutoManual(d, dados, usuario) {
  const nome = String(dados.nome || '').trim();
  if (!nome) return {
    erro: 'Informe o nome da parte.'
  };
  if (!dados.produtoId) return {
    erro: 'Escolha de qual produto esta parte faz parte.'
  };
  const produto = (d.produtos || []).find(p => p.id === dados.produtoId);
  if (!produto) return {
    erro: 'Produto não encontrado.'
  };
  const antigo = dados.id ? (d.subprodutos || []).find(sp => sp.id === dados.id) : null;
  if (antigo && antigo.riscoId) return {
    erro: 'Esta parte veio de uma modelagem — edite pela modelagem, ' + 'senão a próxima importação desfaz a mudança.'
  };
  const reg = {
    id: dados.id || uid(),
    codigo: antigo && antigo.codigo || `SUB-${String((d.subprodutos || []).length + 1).padStart(4, '0')}`,
    produtoId: dados.produtoId,
    tecidoId: dados.tecidoId || '',
    riscoId: '',
    /* avulso: não pertence a modelagem */
    manual: true,
    nome,
    tamanho: String(dados.tamanho || '').trim(),
    porProduto: num(dados.porProduto) || 1,
    larguraMm: num(dados.larguraMm),
    alturaMm: num(dados.alturaMm),
    consumo: num(dados.consumo),
    observacao: String(dados.observacao || '').trim(),
    etapas: antigo && antigo.etapas || [],
    criadoEm: antigo && antigo.criadoEm || agoraISO()
  };
  d.subprodutos = antigo ? d.subprodutos.map(sp => sp.id === reg.id ? reg : sp) : [...(d.subprodutos || []), reg];
  registrarLog(d, usuario, antigo ? 'Editou subproduto' : 'Criou subproduto', `${reg.codigo} · ${reg.nome}`);
  return {
    subproduto: reg
  };
}
function removerSubprodutoManual(d, id, usuario) {
  const sp = (d.subprodutos || []).find(x => x.id === id);
  if (!sp) return {
    erro: 'Parte não encontrada.'
  };
  if (sp.riscoId) return {
    erro: 'Esta parte veio de uma modelagem e não pode ser ' + 'removida aqui. Desvincule a modelagem do tecido.'
  };
  d.subprodutos = d.subprodutos.filter(x => x.id !== id);
  /* quem apontava para ela nas etapas perde a referência */
  d.produtos = (d.produtos || []).map(p => ({
    ...p,
    processo: (p.processo || []).map(e => ({
      ...e,
      subprodutos: (e.subprodutos || []).filter(x => x !== id)
    }))
  }));
  registrarLog(d, usuario, 'Removeu subproduto', `${sp.codigo} · ${sp.nome}`);
  return {
    ok: true
  };
}

/* ---------------- Cadastro avulso de parte ---------------- */
function SubprodutoModal({
  sp,
  db,
  produtoFixo,
  onClose,
  onSalvar
}) {
  const [f, setF] = useState({
    nome: '',
    tamanho: '',
    porProduto: 1,
    larguraMm: '',
    alturaMm: '',
    consumo: '',
    tecidoId: '',
    observacao: '',
    produtoId: produtoFixo || '',
    ...sp
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const produto = (db.produtos || []).find(p => p.id === f.produtoId);
  const tecidos = produto ? produto.tecidos || [] : [];
  const matDoTecido = t => {
    const m = (db.materiais || []).find(x => x.id === t.materialId);
    return m ? m.nome : 'material removido';
  };
  const material = (() => {
    const t = tecidos.find(x => x.id === f.tecidoId);
    if (!t) return null;
    return (db.materiais || []).find(m => m.id === t.materialId) || null;
  })();
  const pronto = String(f.nome || '').trim() && f.produtoId;
  return /*#__PURE__*/React.createElement(Modal, {
    title: f.id ? `${f.codigo || ''} · ${f.nome}` : 'Nova parte',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Para partes que não saem de uma modelagem — bolso aplicado, punho pronto, etiqueta bordada. As que vêm do risco aparecem sozinhas quando você importa o PLT."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Produto *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.produtoId,
    disabled: !!produtoFixo,
    onChange: e => setF(p => ({
      ...p,
      produtoId: e.target.value,
      tecidoId: ''
    }))
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— escolha o produto —"), (db.produtos || []).map(p => /*#__PURE__*/React.createElement("option", {
    key: p.id,
    value: p.id
  }, montarNomeProduto(p, db) || p.codigo)))), /*#__PURE__*/React.createElement(Field, {
    label: "Nome da parte *"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    autoFocus: true,
    placeholder: "Bolso, punho, etiqueta…",
    onChange: e => set('nome', e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Tamanho"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.tamanho,
    placeholder: "P, M, G, único…",
    onChange: e => set('tamanho', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Quantas por produto"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: f.porProduto,
    onChange: e => set('porProduto', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Material de origem"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tecidoId,
    disabled: !produto,
    onChange: e => set('tecidoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— nenhum —"), tecidos.map(t => /*#__PURE__*/React.createElement("option", {
    key: t.id,
    value: t.id
  }, matDoTecido(t), t.apelido ? ` · ${t.apelido}` : ''))))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Largura (mm)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: f.larguraMm,
    placeholder: "0",
    onChange: e => set('larguraMm', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Altura (mm)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    value: f.alturaMm,
    placeholder: "0",
    onChange: e => set('alturaMm', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: `Consumo por peça${material ? ` (${material.unidadeEstoque})` : ''}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.0001",
    min: "0",
    value: f.consumo,
    placeholder: "0",
    onChange: e => set('consumo', e.target.value)
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Observação"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.observacao,
    placeholder: "como é feita, onde entra…",
    onChange: e => set('observacao', e.target.value)
  })), !f.id && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A parte nasce sem etapas. Depois é só marcá-la nas etapas do processo produtivo, como qualquer outra."), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: !pronto,
    onClick: () => onSalvar(f)
  }, "Salvar")));
}
function ListaSubprodutos({
  db,
  update,
  usuario,
  produtoId,
  podeEditar
}) {
  /* aberta de dentro de um produto, a lista mostra só as partes dele;
     aberta pela aba geral, deixa escolher */
  const [modal, setModal] = useState(null);
  const [fProduto, setFProduto] = useState(produtoId || '');
  const [fRisco, setFRisco] = useState('');
  const [busca, setBusca] = useState('');
  const preso = !!produtoId;
  const todos = (db.subprodutos || []).map(sp => {
    const p = (db.produtos || []).find(x => x.id === sp.produtoId);
    const r = (db.riscos || []).find(x => x.id === sp.riscoId);
    return {
      ...sp,
      produto: p,
      risco: r
    };
  });
  const alvo = preso ? produtoId : fProduto;
  const lista = todos.filter(sp => {
    if (alvo && sp.produtoId !== alvo) return false;
    if (fRisco && sp.riscoId !== fRisco) return false;
    const t = normaliza(busca);
    if (t && !normaliza(`${sp.nome} ${sp.codigo} ${sp.tamanho || ''}`).includes(t)) return false;
    return true;
  });

  /* os riscos que fazem sentido oferecer: os do produto escolhido */
  const riscosDisponiveis = (db.riscos || []).filter(r => !alvo || r.produtoId === alvo);
  const consumo = lista.reduce((a, sp) => a + num(sp.consumo), 0);
  const produtoAtual = (db.produtos || []).find(p => p.id === alvo);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, preso ? 'As partes que saíram das modelagens deste produto. Cada uma é uma peça de produção.' : 'Todas as partes que vieram das modelagens. Cada uma é uma peça de produção, ' + 'pronta para receber as etapas de costura, estampa e acabamento.'), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({})
  }, "+ Nova parte"), /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 200
    }
  }, "Partes que vêm de modelagem aparecem sozinhas na importação do PLT.")), /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, !preso && /*#__PURE__*/React.createElement(Field, {
    label: "Produto"
  }, /*#__PURE__*/React.createElement("select", {
    value: fProduto,
    onChange: e => {
      setFProduto(e.target.value);
      setFRisco('');
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos os produtos (", todos.length, " parte(s))"), (db.produtos || []).map(p => {
    const n = todos.filter(sp => sp.produtoId === p.id).length;
    return /*#__PURE__*/React.createElement("option", {
      key: p.id,
      value: p.id
    }, montarNomeProduto(p, db) || p.codigo, " — ", n, " parte(s)");
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Modelagem"
  }, /*#__PURE__*/React.createElement("select", {
    value: fRisco,
    onChange: e => setFRisco(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, riscosDisponiveis.length ? `Todas as modelagens (${riscosDisponiveis.length})` : 'Nenhuma modelagem'), riscosDisponiveis.map(r => /*#__PURE__*/React.createElement("option", {
    key: r.id,
    value: r.id
  }, r.codigo, " · ", r.arquivo)))), /*#__PURE__*/React.createElement(Field, {
    label: "Buscar parte"
  }, /*#__PURE__*/React.createElement("input", {
    value: busca,
    placeholder: "frente, manga, SUB-0003…",
    onChange: e => setBusca(e.target.value)
  }))), (fProduto || fRisco || busca) && !preso && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setFProduto('');
      setFRisco('');
      setBusca('');
    }
  }, "Limpar filtros")), lista.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: todos.length === 0 ? 'Nenhum subproduto. Eles nascem quando você vincula uma modelagem a um tecido.' : 'Nenhuma parte com esses filtros.'
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 200
    }
  }, lista.length, " parte(s)", produtoAtual && ` de ${montarNomeProduto(produtoAtual, db) || produtoAtual.codigo}`, consumo > 0 && ` · ${consumo.toFixed(4)} m de consumo somado`)), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Subproduto"), !alvo && /*#__PURE__*/React.createElement("th", null, "Produto"), /*#__PURE__*/React.createElement("th", null, "Origem"), /*#__PURE__*/React.createElement("th", null, "Etapas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por produto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Medidas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Consumo"), podeEditar && /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, lista.map(sp => /*#__PURE__*/React.createElement("tr", {
    key: sp.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, sp.nome), sp.tamanho && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", sp.tamanho), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, sp.codigo)), !alvo && /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, sp.produto ? montarNomeProduto(sp.produto, db) : /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "—")), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, sp.risco ? sp.risco.codigo : /*#__PURE__*/React.createElement("span", {
    className: "tag"
  }, "avulsa")), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, (() => {
    if (!sp.produto) return /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "—");
    const onde = etapasDoSubproduto(sp.id, sp.produto, db);
    return onde.length ? onde.map(x => x.etapa ? x.etapa.nome : 'etapa').join(', ') : /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "peça inteira");
  })()), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, sp.porProduto), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, sp.larguraMm, " × ", sp.alturaMm, " mm"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, num(sp.consumo).toFixed(4), " m"), podeEditar && /*#__PURE__*/React.createElement("td", null, sp.riscoId ? /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    title: "veio da modelagem"
  }, "—") : /*#__PURE__*/React.createElement("div", {
    className: "acoes-icone"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-accent",
    title: "Editar",
    onClick: () => setModal(sp)
  }, "✎"), /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    title: "Excluir",
    onClick: () => {
      if (!confirm(`Excluir a parte ${sp.nome}?`)) return;
      update(d => {
        const r = removerSubprodutoManual(d, sp.id, usuario);
        if (r.erro) alert(r.erro);
        return d;
      });
    }
  }, "×"))))))))), modal && /*#__PURE__*/React.createElement(SubprodutoModal, {
    sp: modal,
    db: db,
    produtoFixo: produtoId,
    onClose: () => setModal(null),
    onSalvar: dados => {
      let erro = null;
      update(d => {
        const r = salvarSubprodutoManual(d, dados, usuario);
        erro = r.erro || null;
        return d;
      });
      if (erro) alert(erro);else setModal(null);
    }
  }));
}

/* ---------------- Grupos e tipos ---------------- */
function ListasProduto({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [novoG, setNovoG] = useState({
    nome: '',
    sigla: ''
  });
  const [novoT, setNovoT] = useState({
    nome: '',
    sigla: ''
  });
  function addGrupo() {
    if (!novoG.nome.trim()) return;
    const sigla = (novoG.sigla.trim() || normaliza(novoG.nome).slice(0, 2)).toUpperCase();
    if ((db.gruposProduto || []).some(g => g.sigla === sigla)) return alert(`A sigla "${sigla}" já é usada. Ela forma o código do produto, então precisa ser única.`);
    update(d => {
      d.gruposProduto = [...(d.gruposProduto || []), {
        id: uid(),
        nome: novoG.nome.trim(),
        sigla,
        ativo: true
      }];
      return d;
    });
    setNovoG({
      nome: '',
      sigla: ''
    });
  }
  function addTipo() {
    if (!novoT.nome.trim()) return;
    update(d => {
      d.tiposProduto = [...(d.tiposProduto || []), {
        id: uid(),
        nome: novoT.nome.trim(),
        sigla: novoT.sigla.trim().toUpperCase(),
        ativo: true
      }];
      return d;
    });
    setNovoT({
      nome: '',
      sigla: ''
    });
  }
  const usoG = id => (db.produtos || []).filter(p => p.grupoId === id).length;
  const usoT = id => (db.produtos || []).filter(p => p.tipoId === id).length;
  function rm(colecao, item, uso) {
    if (uso) return alert(`${uso} produto(s) usam "${item.nome}".`);
    if (!confirm(`Remover "${item.nome}"?`)) return;
    update(d => {
      d[colecao] = d[colecao].filter(x => x.id !== item.id);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "São estas listas que montam o nome:", ' ', /*#__PURE__*/React.createElement("strong", null, "GRUPO + TIPO + MEDIDA + TECIDO"), ". A sigla do grupo forma o código."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Grupos"), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: novoG.nome,
    onChange: e => setNovoG({
      ...novoG,
      nome: e.target.value
    }),
    placeholder: "Avental, Capa…",
    style: {
      flex: 1,
      minWidth: 110
    },
    onKeyDown: e => e.key === 'Enter' && addGrupo()
  }), /*#__PURE__*/React.createElement("input", {
    value: novoG.sigla,
    onChange: e => setNovoG({
      ...novoG,
      sigla: e.target.value
    }),
    placeholder: "AV",
    maxLength: "3",
    style: {
      maxWidth: 64
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addGrupo,
    disabled: !novoG.nome.trim()
  }, "+"))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Grupo"), /*#__PURE__*/React.createElement("th", null, "Sigla"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Produtos"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 36
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.gruposProduto || []).map(g => /*#__PURE__*/React.createElement("tr", {
    key: g.id
  }, /*#__PURE__*/React.createElement("td", null, g.nome), /*#__PURE__*/React.createElement("td", {
    className: "small",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, g.sigla), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, usoG(g.id) || '—'), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => rm('gruposProduto', g, usoG(g.id))
  }, "×")))))))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Tipos"), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("input", {
    value: novoT.nome,
    onChange: e => setNovoT({
      ...novoT,
      nome: e.target.value
    }),
    placeholder: "Silk, Bordado…",
    style: {
      flex: 1,
      minWidth: 110
    },
    onKeyDown: e => e.key === 'Enter' && addTipo()
  }), /*#__PURE__*/React.createElement("input", {
    value: novoT.sigla,
    onChange: e => setNovoT({
      ...novoT,
      sigla: e.target.value
    }),
    placeholder: "SK",
    maxLength: "3",
    style: {
      maxWidth: 64
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: addTipo,
    disabled: !novoT.nome.trim()
  }, "+"))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", null, "Sigla"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Produtos"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 36
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.tiposProduto || []).map(x => /*#__PURE__*/React.createElement("tr", {
    key: x.id
  }, /*#__PURE__*/React.createElement("td", null, x.nome), /*#__PURE__*/React.createElement("td", {
    className: "small",
    style: {
      fontFamily: 'var(--mono)'
    }
  }, x.sigla || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, usoT(x.id) || '—'), /*#__PURE__*/React.createElement("td", null, podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn-ic ic-danger",
    onClick: () => rm('tiposProduto', x, usoT(x.id))
  }, "×"))))))))));
}

/* ---------------- Lista de produtos ---------------- */
function ListaProdutos({
  db,
  update,
  usuario,
  podeEditar
}) {
  const [sel, setSel] = useState(null);
  const [modal, setModal] = useState(null);
  function criar(f) {
    if (!f.grupoId) return alert('Escolha o grupo do produto.');
    let novoId = null;
    update(d => {
      const id = uid();
      const codigo = proximoCodigoProduto(d, f.grupoId);
      const reg = {
        id,
        codigo,
        grupoId: f.grupoId,
        tipoId: f.tipoId || '',
        medida: f.medida || {
          formato: 'dimensoes'
        },
        complemento: f.complemento || '',
        tecidos: [],
        ativo: true,
        criadoEm: agoraISO(),
        criadoPor: usuario ? usuario.nome : ''
      };
      reg.nome = montarNomeProduto(reg, d);
      d.produtos = [...(d.produtos || []), reg];
      registrarLog(d, usuario, 'Cadastrou produto', `${codigo} · ${reg.nome}`);
      novoId = id;
      return d;
    });
    setModal(null);
    if (novoId) setSel(novoId);
  }
  function remover(p) {
    const subs = (db.subprodutos || []).filter(sp => sp.produtoId === p.id).length;
    if (subs && !confirm(`"${p.nome}" tem ${subs} subproduto(s). Remover tudo?`)) return;
    if (!subs && !confirm(`Remover "${p.nome || p.codigo}"?`)) return;
    update(d => {
      d.subprodutos = (d.subprodutos || []).filter(sp => sp.produtoId !== p.id);
      d.produtos = d.produtos.filter(x => x.id !== p.id);
      return d;
    });
  }
  if (sel) return /*#__PURE__*/React.createElement(ProdutoDetalhe, {
    produtoId: sel,
    db: db,
    update: update,
    usuario: usuario,
    podeEditar: podeEditar,
    onVoltar: () => setSel(null)
  });
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, (db.produtos || []).length, " produto(s)"), podeEditar && /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({
      medida: {
        formato: 'dimensoes'
      }
    })
  }, "+ Produto")), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "O nome se monta sozinho: ", /*#__PURE__*/React.createElement("strong", null, "grupo + tipo + medida + tecido"), ". Ao escolher o tecido, o sistema pede a modelagem — e as partes dela viram subprodutos."), (db.produtos || []).length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum produto cadastrado."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Código"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Tecidos"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Partes"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Consumo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Custo"), /*#__PURE__*/React.createElement("th", {
    style: {
      width: 70
    }
  }))), /*#__PURE__*/React.createElement("tbody", null, (db.produtos || []).map(p => {
    const c = custearProduto(p, db);
    return /*#__PURE__*/React.createElement("tr", {
      key: p.id,
      className: c.avisos.length ? 'linha-atraso' : ''
    }, /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, /*#__PURE__*/React.createElement("strong", {
      style: {
        fontFamily: 'var(--mono)'
      }
    }, codigoCompletoProduto(p, db))), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, montarNomeProduto(p, db) || '(incompleto)')), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, c.tecidos.length), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, c.subprodutos.length), /*#__PURE__*/React.createElement("td", {
      className: "num small"
    }, c.consumoTotal > 0 ? c.consumoTotal.toFixed(4) + ' m' : '—'), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, c.custoTecido > 0 ? /*#__PURE__*/React.createElement("strong", null, money(c.custoTecido)) : '—'), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("div", {
      className: "acoes-icone"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-accent",
      title: "Abrir",
      onClick: () => setSel(p.id)
    }, "👁"), podeEditar && /*#__PURE__*/React.createElement("button", {
      className: "btn-ic ic-danger",
      onClick: () => remover(p)
    }, "×"))));
  })))), modal && /*#__PURE__*/React.createElement(NovoProdutoModal, {
    dados: modal,
    db: db,
    onClose: () => setModal(null),
    onCriar: criar
  }));
}
function NovoProdutoModal({
  dados,
  db,
  onClose,
  onCriar
}) {
  const [f, setF] = useState({
    grupoId: '',
    tipoId: '',
    complemento: '',
    medida: {
      formato: 'dimensoes'
    },
    ...dados
  });
  const set = (k, v) => setF(p => ({
    ...p,
    [k]: v
  }));
  const setM = (k, v) => setF(p => ({
    ...p,
    medida: {
      ...(p.medida || {}),
      [k]: v
    }
  }));
  const simulado = {
    id: '__novo',
    grupoId: f.grupoId,
    tipoId: f.tipoId,
    medida: f.medida,
    complemento: f.complemento,
    tecidos: []
  };
  const nome = montarNomeProduto(simulado, db);
  const codigo = f.grupoId ? proximoCodigoProduto(db, f.grupoId) : '';
  const tp = (db.tiposProduto || []).find(x => x.id === f.tipoId);
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Novo produto",
    onClose: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "O tecido entra depois, junto com a modelagem — é ela que traz as partes."), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Grupo *"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.grupoId,
    onChange: e => set('grupoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— selecione —"), (db.gruposProduto || []).filter(g => g.ativo !== false).map(g => /*#__PURE__*/React.createElement("option", {
    key: g.id,
    value: g.id
  }, g.nome)))), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.tipoId,
    onChange: e => set('tipoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem gravação —"), (db.tiposProduto || []).filter(x => x.ativo !== false).map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome))))), /*#__PURE__*/React.createElement(Field, {
    label: "Formato da medida"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.medida.formato,
    onChange: e => setM('formato', e.target.value)
  }, FORMATOS_MEDIDA.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.nome)))), f.medida.formato === 'dimensoes' && /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, ['largura', 'altura', 'profundidade'].map(c => /*#__PURE__*/React.createElement(Field, {
    key: c,
    label: c.charAt(0).toUpperCase() + c.slice(1) + ' (cm)'
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: f.medida[c] || '',
    onChange: e => setM(c, e.target.value)
  })))), f.medida.formato === 'grade' && /*#__PURE__*/React.createElement(Field, {
    label: "Tamanhos"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 5,
      flexWrap: 'wrap'
    }
  }, ['PP', 'P', 'M', 'G', 'GG', 'XG'].map(tam => {
    const on = (f.medida.tamanhos || []).includes(tam);
    return /*#__PURE__*/React.createElement("button", {
      key: tam,
      type: "button",
      className: "comp-chip" + (on ? ' on' : ''),
      onClick: () => setM('tamanhos', on ? (f.medida.tamanhos || []).filter(x => x !== tam) : [...(f.medida.tamanhos || []), tam])
    }, tam);
  }))), /*#__PURE__*/React.createElement(Field, {
    label: "Complemento do nome"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.complemento,
    onChange: e => set('complemento', e.target.value),
    placeholder: "com bolso, gola alta… (opcional)"
  })), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff',
      borderColor: 'var(--thread)'
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    style: {
      width: '26%'
    }
  }, "Código"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontFamily: 'var(--mono)',
      fontSize: 15
    }
  }, codigo, tp && tp.sigla ? '.' + tp.sigla : '', !codigo && '—'))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Nome"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 15
    }
  }, nome || '—'), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "o tecido completa o nome depois")))))), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onCriar(f),
    disabled: !f.grupoId
  }, "Criar produto")));
}

/* ---------------- Risco salvo ---------------- */
function RiscoSalvo({
  riscoId,
  db,
  update,
  usuario,
  podeEditar,
  onVoltar
}) {
  const [aba, setAba] = useState('resumo');
  const [sel, setSel] = useState(null);
  const r = (db.riscos || []).find(x => x.id === riscoId);
  if (!r) return /*#__PURE__*/React.createElement(Empty, {
    text: "Modelagem não encontrada."
  });
  const [pedido, setPedido] = useState(String(num(r.pedidoPadrao) || ''));
  const composicao = produtosDoRisco(r);

  /* reconstrói o desenho a partir da geometria guardada */
  const analise = r.geometria && r.caixa ? {
    caixa: r.caixa,
    contornos: r.geometria.map(g => ({
      ...g,
      caixa: caixaDe(g.pontos),
      area: areaPoligono(g.pontos)
    })),
    contagem: r.contagem || {
      peca: 0,
      interno: 0,
      texto: 0,
      auxiliar: 0
    },
    escala: escalaPorId(r.escalaId)
  } : null;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, r.codigo), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0
    }
  }, r.nome || r.arquivo), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, r.arquivo)), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onVoltar
  }, "← Voltar")), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Largura do tecido"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, r.larguraTecidoM, " m")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Comprimento"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, r.comprimentoM, " m")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Peças"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, r.totalPecas)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Aproveitamento"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      color: num(r.aproveitamento) >= 75 ? 'var(--ok)' : 'var(--warn)'
    }
  }, r.aproveitamento, "%")), composicao && /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Produtos por modelagem"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, composicao.produtos))), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'resumo',
      label: 'Desenho'
    }, {
      id: 'partes',
      label: `Partes (${(r.partes || []).length})`
    }, {
      id: 'enfesto',
      label: 'Enfesto'
    }]
  }), aba === 'resumo' && (analise ? /*#__PURE__*/React.createElement(DesenhoRisco, {
    analise: analise,
    selecionado: sel,
    onSelecionar: setSel,
    altura: 320
  }) : /*#__PURE__*/React.createElement(Empty, {
    text: "Geometria não guardada para esta modelagem."
  })), aba === 'partes' && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Parte"), /*#__PURE__*/React.createElement("th", null, "Tamanho"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Na modelagem"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por produto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Medidas"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Área"))), /*#__PURE__*/React.createElement("tbody", null, (r.partes || []).map(p => /*#__PURE__*/React.createElement("tr", {
    key: p.assinatura
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, p.nome)), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, p.tamanho || '—'), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.quantidade), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.porProduto), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, p.larguraMm, " × ", p.alturaMm, " mm"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.areaM2, " m²")))))), aba === 'enfesto' && /*#__PURE__*/React.createElement(PlanejarEnfesto, {
    risco: r,
    pedido: pedido,
    onPedido: setPedido,
    partes: r.partes
  }));
}

/* ---------------- Planejar o enfesto pelo pedido ----------------
   Você diz quantas peças precisa; o sistema diz quantas camadas. */
function PlanejarEnfesto({
  risco,
  pedido,
  onPedido,
  partes
}) {
  const p = planejarEnfesto(risco, pedido);
  const lista = partes || risco.partes || [];
  if (p.erro) {
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--warn)',
        background: 'var(--warn-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "⚠ ", p.erro));
  }
  const sugestoes = [50, 100, 200, 500, 1000];
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Cada camada do enfesto rende ", /*#__PURE__*/React.createElement("strong", null, p.porCamada, " ", p.unidade, "(s)"), ". Diga quantas peças o pedido tem e o sistema calcula as camadas."), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Quantas peças você precisa"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    value: pedido,
    onChange: e => onPedido(e.target.value),
    placeholder: "quantidade do pedido",
    style: {
      maxWidth: 190
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, sugestoes.map(q => /*#__PURE__*/React.createElement("button", {
    key: q,
    type: "button",
    className: "comp-chip" + (num(pedido) === q ? ' on' : ''),
    onClick: () => onPedido(String(q))
  }, q)))), p.vazio ? /*#__PURE__*/React.createElement(Empty, {
    text: "Informe a quantidade do pedido para ver o plano de corte."
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Camadas a enfestar"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, p.camadas), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, p.porCamada, " ", p.unidade, "(s) por camada")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Vai produzir"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, p.produzido), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "pedido de ", p.alvo)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Tecido"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, p.tecidoTotal, " m"), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, risco.comprimentoM, " × ", p.camadas)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Consumo"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, p.consumoDoPedido), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "m por peça do pedido")), p.sobra > 0 && /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Sobra"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      color: 'var(--warn)'
    }
  }, p.sobra), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, p.tecidoDaSobra, " m de tecido"))), p.exato ? /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "✓ ", p.camadas, " camadas fecham o pedido exato, sem sobra.")) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Com ", p.camadas, " camadas saem ", p.produzido, " — ", p.sobra, " a mais que o pedido,", ' ', "o equivalente a ", p.tecidoDaSobra, " m de tecido."), p.faltariam !== null && p.faltariam > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "Com ", p.camadas - 1, " camadas faltariam ", p.faltariam, " peça(s). A última camada é o preço de fechar o pedido.")), lista.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "O que o corte precisa entregar"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Parte"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por camada"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Total a cortar"))), /*#__PURE__*/React.createElement("tbody", null, lista.map(x => /*#__PURE__*/React.createElement("tr", {
    key: x.assinatura
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, x.nome || /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "sem nome")), x.tamanho && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", x.tamanho)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, x.quantidade), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, x.quantidade * p.camadas)))), /*#__PURE__*/React.createElement("tr", {
    style: {
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, "Peças na mesa")), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, risco.totalPecas), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", null, p.pecasCortadas)))))))));
}

/* ---------------- Análise do risco recém-lido ---------------- */
function AnaliseRisco({
  analise,
  db,
  update,
  usuario,
  onCancelar,
  onSalvar
}) {
  const [aba, setAba] = useState('resumo');
  const [sel, setSel] = useState(null);
  /* cada peça do risco é única daquele produto: nada vem preenchido
     de riscos anteriores, por mais parecida que a geometria seja */
  const [nomes, setNomes] = useState(() => {
    const inicial = {};
    (analise.grupos || []).forEach(g => {
      inicial[g.assinatura] = {
        nome: '',
        tamanho: '',
        porProduto: 1
      };
    });
    return inicial;
  });
  const [escalaId, setEscalaId] = useState(analise.escala ? analise.escala.id : 'hpgl40');
  const [pedido, setPedido] = useState('');
  const [ampliar, setAmpliar] = useState(null);

  /* reanalisa se o usuário corrigir a escala */
  const a = escalaId === (analise.escala && analise.escala.id) || !analise.texto ? analise : {
    ...analisarPLT(analise.texto, {
      escalaId,
      nome: analise.arquivo
    }),
    arquivo: analise.arquivo,
    texto: analise.texto
  };
  if (a.erro) {
    return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "page-head"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "eyebrow"
    }, "Análise da modelagem"), /*#__PURE__*/React.createElement("h2", {
      style: {
        margin: 0
      }
    }, a.arquivo)), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: onCancelar
    }, "← Voltar")), /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--bad)',
        background: 'var(--bad-bg)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontWeight: 600,
        color: 'var(--bad)'
      }
    }, "⛔ ", a.erro)), a.comandos && a.comandos.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "O que encontramos no arquivo"), /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        padding: 0
      }
    }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Comando"), /*#__PURE__*/React.createElement("th", {
      className: "num"
    }, "Ocorrências"))), /*#__PURE__*/React.createElement("tbody", null, a.comandos.map(c => /*#__PURE__*/React.createElement("tr", {
      key: c.cmd
    }, /*#__PURE__*/React.createElement("td", {
      style: {
        fontFamily: 'var(--mono)'
      }
    }, c.cmd), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, c.n))))))));
  }
  const setNome = (chave, patch) => setNomes(n => ({
    ...n,
    [chave]: {
      ...n[chave],
      ...patch
    }
  }));
  const semNome = (a.grupos || []).filter(g => !String((nomes[g.assinatura] || {}).nome || '').trim());
  const partes = (a.grupos || []).map(g => {
    const n = nomes[g.assinatura] || {};
    return {
      assinatura: g.assinatura,
      nome: n.nome || '',
      tamanho: n.tamanho || '',
      quantidade: g.quantidade,
      porProduto: num(n.porProduto) || 1,
      larguraMm: g.larguraMm,
      alturaMm: g.alturaMm,
      areaM2: g.areaM2,
      consumoM2: Number((g.areaM2 * g.quantidade).toFixed(5)),
      posicoes: g.posicoes
    };
  });
  const composicao = produtosDoRisco({
    partes
  });
  function confirmar() {
    if (semNome.length > 0) return alert(`${semNome.length} peça(s) ainda sem nome. ` + `Sem o nome elas não viram subproduto na produção.`);
    onSalvar({
      arquivo: a.arquivo,
      nome: partes.map(p => p.nome).filter(Boolean).join(' + '),
      larguraTecidoM: a.larguraTecidoM,
      comprimentoM: a.comprimentoM,
      areaRiscoM2: a.areaRiscoM2,
      areaPecasM2: a.areaPecasM2,
      aproveitamento: a.aproveitamento,
      desperdicio: a.desperdicio,
      totalPecas: a.totalPecas,
      consumoPorPeca: a.consumoPorPeca,
      escalaId: a.escala.id,
      partes,
      pedidoPadrao: num(pedido),
      contagem: a.contagem,
      trajetorias: a.trajetorias,
      comandos: a.comandos,
      /* guardamos a geometria para poder redesenhar depois */
      geometria: a.contornos.filter(c => c.tipo !== 'texto').map(c => ({
        indice: c.indice,
        tipo: c.tipo,
        fechado: c.fechado,
        pontos: c.pontos
      })),
      caixa: a.caixa
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Análise da modelagem"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0
    }
  }, a.arquivo), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, a.trajetorias, " trajetórias · ", a.pontos, " pontos · ", a.escala.nome, a.escalaInferida && ' (deduzida)')), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: onCancelar
  }, "Descartar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: confirmar
  }, "Salvar modelagem"))), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Largura do tecido"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, a.larguraTecidoM, " m")), /*#__PURE__*/React.createElement("div", {
    className: "kpi accent"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Comprimento"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, a.comprimentoM, " m")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Peças"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, a.totalPecas), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, a.grupos.length, " tipo(s)")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Aproveitamento"), /*#__PURE__*/React.createElement("div", {
    className: "val",
    style: {
      color: a.aproveitamento >= 75 ? 'var(--ok)' : 'var(--warn)'
    }
  }, a.aproveitamento, "%"), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "perda ", a.desperdicio, "%")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Consumo"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, a.consumoPorPeca), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "m por peça"))), a.aproveitamento < 70 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ Aproveitamento de ", a.aproveitamento, "%. Ou o encaixe pode melhorar, ou há peças que não foram reconhecidas — confira o desenho antes de salvar.")), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'resumo',
      label: 'Desenho'
    }, {
      id: 'partes',
      label: `Partes (${a.grupos.length})`
    }, {
      id: 'enfesto',
      label: 'Enfesto'
    }, {
      id: 'diagnostico',
      label: 'Diagnóstico'
    }]
  }), aba === 'resumo' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 8
    }
  }, "Clique numa peça do desenho para nomeá-la aqui mesmo. Peças iguais compartilham o nome — são a mesma parte repetida na modelagem."), sel ? (() => {
    const g = a.grupos.find(x => x.ocorrencias.some(o => o.indice === sel.indice));
    if (!g) return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--warn)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600
      }
    }, "Este traçado não é uma peça — é marcação interna ou texto."));
    const n = nomes[g.assinatura] || {};
    const idx = a.grupos.indexOf(g);
    const feitas = (a.grupos || []).filter(x => String((nomes[x.assinatura] || {}).nome || '').trim()).length;
    /* ir para a próxima peça sem nome poupa cliques */
    const proxima = () => {
      const resto = (a.grupos || []).filter(x => x !== g && !String((nomes[x.assinatura] || {}).nome || '').trim());
      if (resto.length === 0) return setSel(null);
      const alvo = resto[0];
      const oc = alvo.ocorrencias[0];
      const cont = a.contornos.find(c => c.indice === oc.indice);
      if (cont) setSel(cont);
    };
    return /*#__PURE__*/React.createElement("div", {
      className: "panel identificar-peca"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ident-peca"
    }, /*#__PURE__*/React.createElement(MiniPeca, {
      peca: g.desenho,
      marcacoes: g.marcacoes,
      tamanho: 104
    }), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        textAlign: 'center'
      }
    }, "peça ", idx + 1, " de ", a.grupos.length, /*#__PURE__*/React.createElement("div", null, g.larguraMm, " × ", g.alturaMm, " mm"), /*#__PURE__*/React.createElement("div", null, g.quantidade, "× na modelagem"))), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 240
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "grid3"
    }, /*#__PURE__*/React.createElement(Field, {
      label: "Nome da parte *"
    }, /*#__PURE__*/React.createElement("input", {
      autoFocus: true,
      value: n.nome || '',
      placeholder: "Frente, Costas, Manga…",
      onChange: e => setNome(g.assinatura, {
        nome: e.target.value
      }),
      onKeyDown: e => {
        if (e.key === 'Enter') proxima();
      }
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Tamanho"
    }, /*#__PURE__*/React.createElement("input", {
      value: n.tamanho || '',
      placeholder: "P, M, G, único…",
      onChange: e => setNome(g.assinatura, {
        tamanho: e.target.value
      }),
      onKeyDown: e => {
        if (e.key === 'Enter') proxima();
      }
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Quantas por produto"
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      min: "1",
      value: n.porProduto || 1,
      onChange: e => setNome(g.assinatura, {
        porProduto: e.target.value
      })
    }))), /*#__PURE__*/React.createElement("div", {
      className: "row-actions"
    }, /*#__PURE__*/React.createElement("span", {
      className: "small muted",
      style: {
        flex: 1,
        minWidth: 180
      }
    }, feitas, " de ", a.grupos.length, " peça(s) nomeada(s).", semNome.length > 0 && ' Enter vai para a próxima sem nome.'), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setAmpliar(g)
    }, "Ampliar"), semNome.length > 0 && /*#__PURE__*/React.createElement("button", {
      className: "btn accent sm",
      onClick: proxima
    }, "Próxima sem nome →"), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setSel(null)
    }, "Fechar"))));
  })() : semNome.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel no-print",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600,
      flex: 1,
      minWidth: 200
    }
  }, "⚠ ", semNome.length, " peça(s) ainda sem nome."), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: () => {
      const alvo = semNome[0];
      const oc = alvo.ocorrencias[0];
      const cont = a.contornos.find(c => c.indice === oc.indice);
      if (cont) setSel(cont);
    }
  }, "Começar a identificar"))), /*#__PURE__*/React.createElement(DesenhoRisco, {
    analise: a,
    selecionado: sel,
    onSelecionar: setSel,
    altura: 340
  }), (a.grupos || []).some(g => String((nomes[g.assinatura] || {}).nome || '').trim()) && /*#__PURE__*/React.createElement("div", {
    className: "tiras-peca"
  }, (a.grupos || []).map((g, i) => {
    const n = nomes[g.assinatura] || {};
    const nomeado = String(n.nome || '').trim();
    const ativa = sel && g.ocorrencias.some(o => o.indice === sel.indice);
    return /*#__PURE__*/React.createElement("button", {
      key: g.assinatura,
      className: "tira-peca" + (ativa ? ' on' : '') + (nomeado ? '' : ' vazia'),
      onClick: () => {
        const cont = a.contornos.find(c => c.indice === g.ocorrencias[0].indice);
        if (cont) setSel(cont);
      }
    }, /*#__PURE__*/React.createElement(MiniPeca, {
      peca: g.desenho,
      marcacoes: g.marcacoes,
      tamanho: 44
    }), /*#__PURE__*/React.createElement("span", {
      className: "small"
    }, nomeado || /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--warn)'
      }
    }, "peça ", i + 1), n.tamanho && /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, " · ", n.tamanho)));
  }))), aba === 'partes' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O sistema separou as peças pela geometria. Dê nome a cada uma — é assim que elas viram subprodutos na produção. Cada peça é única deste produto, então nada vem preenchido de outro risco."), semNome.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "⚠ ", semNome.length, " peça(s) aguardando identificação. Nada é gravado sem o seu aval.")), a.grupos.map(g => {
    const n = nomes[g.assinatura] || {};
    return /*#__PURE__*/React.createElement("div", {
      key: g.assinatura,
      className: "parte-bloco"
    }, /*#__PURE__*/React.createElement("div", {
      className: "parte-topo"
    }, /*#__PURE__*/React.createElement("div", {
      className: "parte-selo"
    }, g.quantidade, "×"), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 150
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, g.larguraMm, " × ", g.alturaMm, " mm · ", g.areaM2, " m² · proporção ", g.proporcao)), /*#__PURE__*/React.createElement(MiniPeca, {
      peca: g.desenho,
      marcacoes: g.marcacoes,
      tamanho: 72,
      onAmpliar: () => setAmpliar(g)
    })), /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginTop: -4,
        marginBottom: 8
      }
    }, (g.marcacoes || []).length > 0 ? `${g.marcacoes.length} marcação(ões) impressa(s) nesta peça — ` : 'Sem marcação impressa — ', /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "link-btn",
      onClick: () => setAmpliar(g)
    }, "ver a peça ampliada"), "."), /*#__PURE__*/React.createElement("div", {
      className: "grid3"
    }, /*#__PURE__*/React.createElement(Field, {
      label: "Nome da parte *"
    }, /*#__PURE__*/React.createElement("input", {
      value: n.nome || '',
      placeholder: "Frente, Costas, Manga…",
      onChange: e => setNome(g.assinatura, {
        nome: e.target.value
      })
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Tamanho"
    }, /*#__PURE__*/React.createElement("input", {
      value: n.tamanho || '',
      placeholder: "P, M, G, único…",
      onChange: e => setNome(g.assinatura, {
        tamanho: e.target.value
      })
    })), /*#__PURE__*/React.createElement(Field, {
      label: "Quantas por produto"
    }, /*#__PURE__*/React.createElement("input", {
      type: "number",
      min: "1",
      value: n.porProduto || 1,
      onChange: e => setNome(g.assinatura, {
        porProduto: e.target.value
      })
    }))), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Posições na modelagem: ", g.posicoes.map(p => `(${p.x}, ${p.y})`).join('  ')));
  }), composicao && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("h3", null, "Quantos produtos saem desta modelagem"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Parte"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Na modelagem"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Por produto"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Rende"))), /*#__PURE__*/React.createElement("tbody", null, partes.map(p => /*#__PURE__*/React.createElement("tr", {
    key: p.assinatura
  }, /*#__PURE__*/React.createElement("td", null, p.nome || /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "sem nome"), p.tamanho && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " · ", p.tamanho)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.quantidade), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, p.porProduto), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, Math.floor(p.quantidade / p.porProduto)))), /*#__PURE__*/React.createElement("tr", {
    style: {
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: "3"
  }, /*#__PURE__*/React.createElement("strong", null, "Produtos completos"), composicao.gargalo && /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "limitado por ", composicao.gargalo.nome || 'uma das partes')), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 16
    }
  }, composicao.produtos)))))), composicao.sobra.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      marginTop: 6
    }
  }, "Sobram sem par: ", composicao.sobra.map(s => `${s.sobrando} ${s.parte.nome || 'peça'}`).join(', '), "."))), ampliar && /*#__PURE__*/React.createElement(PecaAmpliada, {
    grupo: ampliar,
    escala: a.escala,
    onFechar: () => setAmpliar(null)
  }), aba === 'enfesto' && /*#__PURE__*/React.createElement(PlanejarEnfesto, {
    risco: {
      comprimentoM: a.comprimentoM,
      totalPecas: a.totalPecas,
      partes
    },
    pedido: pedido,
    onPedido: setPedido
  }), aba === 'diagnostico' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "O que o sistema conseguiu extrair do arquivo. Se algo estiver errado, é aqui que aparece — nada é preenchido por suposição."), /*#__PURE__*/React.createElement(Field, {
    label: "Escala do arquivo"
  }, /*#__PURE__*/React.createElement("select", {
    value: escalaId,
    onChange: e => setEscalaId(e.target.value),
    style: {
      maxWidth: 300
    }
  }, ESCALAS_PLT.map(e => /*#__PURE__*/React.createElement("option", {
    key: e.id,
    value: e.id
  }, e.nome)))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: -6,
      marginBottom: 12
    }
  }, a.escalaInferida ? `Deduzida pelo tamanho da folha: ${a.larguraTecidoM} m de largura é medida de tecido.` : 'Escolhida por você.', " Se as medidas não baterem com a régua, troque aqui."), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    style: {
      width: '55%'
    }
  }, "Arquivo"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, a.arquivo))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Trajetórias reconstruídas"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, a.trajetorias)), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Pontos lidos"), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, a.pontos)), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Extensão em unidades"), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, Math.round(a.caixa.largura), " × ", Math.round(a.caixa.altura))), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Escala aplicada"), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, a.escala.nome)), /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", null, "Em milímetros"), /*#__PURE__*/React.createElement("td", {
    className: "num small"
  }, (a.caixa.largura / a.escala.porMm).toFixed(1), " × ", (a.caixa.altura / a.escala.porMm).toFixed(1)))))), /*#__PURE__*/React.createElement("h3", null, "Contornos por tipo"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Tipo"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Quantidade"), /*#__PURE__*/React.createElement("th", null, "O que é"))), /*#__PURE__*/React.createElement("tbody", null, TIPOS_CONTORNO.map(t => /*#__PURE__*/React.createElement("tr", {
    key: t.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, t.nome)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, a.contagem[t.id]), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, t.desc)))))), /*#__PURE__*/React.createElement("h3", null, "Comandos encontrados"), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Comando"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Ocorrências"), /*#__PURE__*/React.createElement("th", null, "Função"))), /*#__PURE__*/React.createElement("tbody", null, a.comandos.map(c => /*#__PURE__*/React.createElement("tr", {
    key: c.cmd
  }, /*#__PURE__*/React.createElement("td", {
    style: {
      fontFamily: 'var(--mono)'
    }
  }, /*#__PURE__*/React.createElement("strong", null, c.cmd)), /*#__PURE__*/React.createElement("td", {
    className: "num"
  }, c.n), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, {
    IN: 'inicializa o plotter',
    PS: 'define o tamanho da folha',
    SP: 'seleciona a caneta',
    PU: 'levanta a caneta — move sem desenhar',
    PD: 'abaixa a caneta — desenha até os pontos',
    PA: 'movimento absoluto',
    PR: 'movimento relativo',
    PG: 'avança a página',
    QL: 'qualidade de traço',
    BP: 'inicia o trabalho',
    LB: 'texto',
    LT: 'tipo de linha',
    SC: 'escala',
    IP: 'ponto de referência'
  }[c.cmd] || 'não interpretado — ignorado'))))))));
}

/* ---------------- Miniatura de uma peça ----------------
   Desenha o molde e a marcação impressa nele, para você ler
   no arquivo o nome que aquela parte já carrega. */
function MiniPeca({
  peca,
  marcacoes,
  tamanho,
  onAmpliar
}) {
  if (!peca) return null;
  const cx = peca.caixa;
  const larg = cx.largura || 1,
    alt = cx.altura || 1;
  const margem = Math.max(larg, alt) * 0.04;
  const L = tamanho || 64;
  const caminho = pts => pts.map((p, i) => (i ? 'L' : 'M') + p[0] + ' ' + p[1]).join(' ');
  return /*#__PURE__*/React.createElement("div", {
    className: "mini-peca",
    style: {
      width: L,
      height: L
    },
    onClick: onAmpliar,
    title: onAmpliar ? 'Ampliar' : undefined
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: `${cx.x0 - margem} ${cx.y0 - margem} ${larg + margem * 2} ${alt + margem * 2}`,
    preserveAspectRatio: "xMidYMid meet",
    style: {
      width: '100%',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("g", {
    transform: `translate(0, ${cx.y0 * 2 + alt}) scale(1,-1)`
  }, /*#__PURE__*/React.createElement("path", {
    d: caminho(peca.pontos) + ' Z',
    fill: "rgba(47,107,79,0.12)",
    stroke: "#2f6b4f",
    strokeWidth: Math.max(larg, alt) / 90
  }), (marcacoes || []).map(m => /*#__PURE__*/React.createElement("path", {
    key: m.indice,
    d: caminho(m.pontos) + (m.fechado ? ' Z' : ''),
    fill: "none",
    stroke: "#5c5647",
    strokeWidth: Math.max(larg, alt) / 220
  })))));
}

/* A peça ampliada: o molde do arquivo em tamanho grande, com a
   marcação impressa nele. Dá para aproximar e arrastar. */
function PecaAmpliada({
  grupo,
  escala,
  onFechar
}) {
  const [zoom, setZoom] = useState(1);
  const [pan, setPan] = useState({
    x: 0,
    y: 0
  });
  const [soMarca, setSoMarca] = useState(false);
  const arraste = useRef(null);
  const peca = grupo.desenho;
  if (!peca) return null;
  const marcas = grupo.marcacoes || [];
  const porMm = escala && escala.porMm || 40;

  /* a moldura muda conforme o foco: a peça toda ou só a marcação */
  const alvo = (() => {
    if (!soMarca || marcas.length === 0) return peca.caixa;
    let x0 = Infinity,
      y0 = Infinity,
      x1 = -Infinity,
      y1 = -Infinity;
    marcas.forEach(m => {
      x0 = Math.min(x0, m.caixa.x0);
      y0 = Math.min(y0, m.caixa.y0);
      x1 = Math.max(x1, m.caixa.x1);
      y1 = Math.max(y1, m.caixa.y1);
    });
    return {
      x0,
      y0,
      x1,
      y1,
      largura: x1 - x0,
      altura: y1 - y0
    };
  })();
  const larg = alvo.largura || 1,
    alt = alvo.altura || 1;
  const margem = Math.max(larg, alt) * 0.06;
  const caminho = pts => pts.map((p, i) => (i ? 'L' : 'M') + p[0] + ' ' + p[1]).join(' ');
  const traco = Math.max(larg, alt);
  function iniciar(e) {
    const t = e.touches ? e.touches[0] : e;
    arraste.current = {
      x: t.clientX,
      y: t.clientY,
      px: pan.x,
      py: pan.y
    };
  }
  function mover(e) {
    if (!arraste.current) return;
    const t = e.touches ? e.touches[0] : e;
    setPan({
      x: arraste.current.px + (t.clientX - arraste.current.x),
      y: arraste.current.py + (t.clientY - arraste.current.y)
    });
  }
  const soltar = () => {
    arraste.current = null;
  };
  const reset = () => {
    setZoom(1);
    setPan({
      x: 0,
      y: 0
    });
  };
  return /*#__PURE__*/React.createElement(Modal, {
    title: `Peça ampliada — ${grupo.larguraMm} × ${grupo.alturaMm} mm`,
    onClose: onFechar,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "risco-barra no-print"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setZoom(z => Math.min(z * 1.5, 40))
  }, "+"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setZoom(z => Math.max(z / 1.5, 1))
  }, "−"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: reset
  }, "Ajustar"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, Math.round(zoom * 100), "%"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), marcas.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "comp-chip" + (!soMarca ? ' on' : ''),
    onClick: () => {
      setSoMarca(false);
      reset();
    }
  }, "Peça inteira"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "comp-chip" + (soMarca ? ' on' : ''),
    onClick: () => {
      setSoMarca(true);
      reset();
    }
  }, "Só a marcação"))), /*#__PURE__*/React.createElement("div", {
    className: "peca-zoom",
    onMouseDown: iniciar,
    onMouseMove: mover,
    onMouseUp: soltar,
    onMouseLeave: soltar,
    onTouchStart: iniciar,
    onTouchMove: mover,
    onTouchEnd: soltar
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: `${alvo.x0 - margem} ${alvo.y0 - margem} ${larg + margem * 2} ${alt + margem * 2}`,
    preserveAspectRatio: "xMidYMid meet",
    style: {
      width: '100%',
      height: '100%',
      cursor: 'grab',
      transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`,
      transformOrigin: 'center'
    }
  }, /*#__PURE__*/React.createElement("g", {
    transform: `translate(0, ${alvo.y0 * 2 + alt}) scale(1,-1)`
  }, /*#__PURE__*/React.createElement("path", {
    d: caminho(peca.pontos) + ' Z',
    fill: "rgba(47,107,79,0.08)",
    stroke: "#2f6b4f",
    strokeWidth: traco / (260 * zoom),
    strokeLinejoin: "round"
  }), marcas.map(m => /*#__PURE__*/React.createElement("path", {
    key: m.indice,
    d: caminho(m.pontos) + (m.fechado ? ' Z' : ''),
    fill: "none",
    stroke: "#2b2924",
    strokeWidth: traco / (420 * zoom),
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 8
    }
  }, marcas.length > 0 ? `${marcas.length} traçado(s) impressos na peça. Use + para aproximar da letra e arraste para mover.` : 'Esta peça não tem nenhuma marcação impressa.', ' ', "A peça mede ", grupo.larguraMm, " × ", grupo.alturaMm, " mm", ' ', "(", (peca.caixa.largura / porMm / 10).toFixed(1), " × ", (peca.caixa.altura / porMm / 10).toFixed(1), " cm)."));
}

/* ---------------- Desenho do risco ---------------- */
function DesenhoRisco({
  analise,
  selecionado,
  onSelecionar,
  altura
}) {
  const [zoom, setZoom] = useState(1);
  const [pan, setPan] = useState({
    x: 0,
    y: 0
  });
  const [foco, setFoco] = useState(null); // peça em close
  const [mostrar, setMostrar] = useState({
    peca: true,
    interno: true,
    texto: false,
    auxiliar: true
  });
  const arraste = useRef(null);
  const cx = analise.caixa;
  const larg = cx.largura || 1,
    alt = cx.altura || 1;
  const H = altura || 300;
  const cores = {
    peca: '#2f6b4f',
    interno: '#c08a2e',
    texto: '#8a8577',
    auxiliar: '#a8443a'
  };
  function iniciar(e) {
    const t = e.touches ? e.touches[0] : e;
    arraste.current = {
      x: t.clientX,
      y: t.clientY,
      px: pan.x,
      py: pan.y
    };
  }
  function mover(e) {
    if (!arraste.current) return;
    const t = e.touches ? e.touches[0] : e;
    setPan({
      x: arraste.current.px + (t.clientX - arraste.current.x),
      y: arraste.current.py + (t.clientY - arraste.current.y)
    });
  }
  const soltar = () => {
    arraste.current = null;
  };
  const visiveis = analise.contornos.filter(c => mostrar[c.tipo]);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "risco-barra no-print"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setZoom(z => Math.min(z * 1.4, 20))
  }, "+"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setZoom(z => Math.max(z / 1.4, 0.5))
  }, "−"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setZoom(1);
      setPan({
        x: 0,
        y: 0
      });
    }
  }, "Ajustar"), /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, Math.round(zoom * 100), "%"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), TIPOS_CONTORNO.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.id,
    type: "button",
    title: t.desc,
    className: "comp-chip" + (mostrar[t.id] ? ' on' : ''),
    onClick: () => setMostrar(m => ({
      ...m,
      [t.id]: !m[t.id]
    }))
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-block',
      width: 8,
      height: 8,
      borderRadius: 2,
      background: cores[t.id],
      marginRight: 5
    }
  }), t.nome, " (", analise.contagem[t.id], ")"))), foco && /*#__PURE__*/React.createElement("div", {
    className: "risco-foco"
  }, /*#__PURE__*/React.createElement(MiniPeca, {
    peca: foco,
    marcacoes: marcacoesDaPeca(foco, analise.contornos),
    tamanho: 110
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 140
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Peça em close"), /*#__PURE__*/React.createElement("div", {
    className: "small"
  }, (foco.caixa.largura / (analise.escala && analise.escala.porMm || 40)).toFixed(1), " ×", ' ', (foco.caixa.altura / (analise.escala && analise.escala.porMm || 40)).toFixed(1), " mm")), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setFoco(null)
  }, "Fechar")), /*#__PURE__*/React.createElement("div", {
    className: "risco-tela",
    style: {
      height: H
    },
    onMouseDown: iniciar,
    onMouseMove: mover,
    onMouseUp: soltar,
    onMouseLeave: soltar,
    onTouchStart: iniciar,
    onTouchMove: mover,
    onTouchEnd: soltar
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: `0 0 ${larg} ${alt}`,
    preserveAspectRatio: "xMidYMid meet",
    style: {
      width: '100%',
      height: '100%',
      transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`,
      transformOrigin: 'center',
      cursor: 'grab'
    }
  }, /*#__PURE__*/React.createElement("rect", {
    x: "0",
    y: "0",
    width: larg,
    height: alt,
    fill: "#fff",
    stroke: "#c9c3b6",
    strokeWidth: larg / 600
  }), /*#__PURE__*/React.createElement("g", {
    transform: `translate(${-cx.x0}, ${cx.y1}) scale(1,-1)`
  }, visiveis.map(c => {
    const ehSel = selecionado && selecionado.indice === c.indice;
    const d = c.pontos.map((p, i) => (i ? 'L' : 'M') + p[0] + ' ' + p[1]).join(' ') + (c.fechado ? ' Z' : '');
    return /*#__PURE__*/React.createElement("path", {
      key: c.indice,
      d: d,
      fill: c.tipo === 'peca' ? ehSel ? 'rgba(217,139,43,0.30)' : 'rgba(47,107,79,0.10)' : 'none',
      stroke: ehSel ? '#d98b2b' : cores[c.tipo],
      strokeWidth: (ehSel ? larg / 280 : larg / 700) / zoom,
      style: {
        cursor: c.tipo === 'peca' ? 'pointer' : 'default'
      },
      onClick: () => {
        if (c.tipo !== 'peca') return;
        setFoco(c);
        if (onSelecionar) onSelecionar(c);
      }
    });
  })))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 5
    }
  }, "Arraste para mover, use + e − para aproximar. Clique numa peça para ver as medidas."));
}

/* ---------------- Importar PLT: usado no Corte e dentro do Produto ---------------- */
const PASSOS_LEITURA = ['Lendo o arquivo', 'Interpretando os comandos', 'Reconstruindo as trajetórias', 'Identificando os contornos', 'Separando as peças', 'Agrupando as repetidas', 'Calculando áreas e consumo', 'Separando texto de contorno'];
function BotaoImportarPLT({
  onAnalisado,
  rotulo,
  classe
}) {
  const [lendo, setLendo] = useState(false);
  const [etapa, setEtapa] = useState('');
  async function importar(e) {
    const file = (e.target.files || [])[0];
    e.target.value = '';
    if (!file) return;
    if (file.size > 4 * 1024 * 1024) return alert(`"${file.name}" tem ${Math.round(file.size / 1024)} KB. ` + `O limite é 4 MB — modelagens maiores costumam vir de exportação com curvas demais.`);
    setLendo(true);
    try {
      for (let i = 0; i < 4; i++) {
        setEtapa(PASSOS_LEITURA[i]);
        await new Promise(r => setTimeout(r, 60));
      }
      const texto = await new Promise((res, rej) => {
        const r = new FileReader();
        r.onload = () => res(r.result);
        r.onerror = () => rej(new Error('não foi possível ler o arquivo'));
        r.readAsText(file, 'ISO-8859-1');
      });
      for (let i = 4; i < PASSOS_LEITURA.length; i++) {
        setEtapa(PASSOS_LEITURA[i]);
        await new Promise(r => setTimeout(r, 60));
      }
      const a = analisarPLT(texto, {
        nome: file.name
      });
      onAnalisado(a.erro ? {
        erro: a.erro,
        arquivo: file.name,
        comandos: a.comandos
      } : {
        ...a,
        arquivo: file.name,
        tamanho: file.size,
        texto
      });
    } catch (err) {
      alert(err.message);
    } finally {
      setLendo(false);
      setEtapa('');
    }
  }
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("label", {
    className: classe || 'btn accent',
    style: {
      cursor: lendo ? 'wait' : 'pointer'
    }
  }, lendo ? '⏳ Processando…' : rotulo || '+ Importar modelagem PLT', /*#__PURE__*/React.createElement("input", {
    type: "file",
    accept: ".plt,.hpgl,.hpg,.prn,text/plain",
    style: {
      display: 'none'
    },
    disabled: lendo,
    onChange: importar
  })), lendo && /*#__PURE__*/React.createElement("div", {
    className: "leitura-barra"
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600
    }
  }, etapa, "…"), /*#__PURE__*/React.createElement("div", {
    style: {
      background: '#e8e4da',
      borderRadius: 5,
      height: 7,
      marginTop: 5
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: (PASSOS_LEITURA.indexOf(etapa) + 1) / PASSOS_LEITURA.length * 100 + '%',
      height: '100%',
      borderRadius: 5,
      background: 'var(--thread)',
      transition: 'width .2s'
    }
  }))));
}

/* Grava o risco analisado e devolve o id — o mesmo caminho nos dois módulos */
function gravarRisco(d, dados, usuario, produtoId) {
  const id = uid();
  const reg = {
    ...dados,
    id,
    /* o risco nasce dentro de um produto e não é reaproveitado por outro */
    produtoId: produtoId || dados.produtoId || '',
    codigo: `RSC-${String((d.riscos || []).length + 1).padStart(4, '0')}`,
    criadoEm: agoraISO(),
    criadoPor: usuario ? usuario.nome : ''
  };
  d.riscos = [...(d.riscos || []), reg];
  registrarLog(d, usuario, 'Importou risco', dados.arquivo);
  return reg;
}

function GrupoCadastros({
  db,
  update,
  usuario,
  perm
}) {
  const [sub, setSub] = useState('clientes');
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Base"), /*#__PURE__*/React.createElement("h2", null, "Cadastros"))), /*#__PURE__*/React.createElement(SubTabs, {
    active: sub,
    onChange: setSub,
    tabs: [{
      id: 'clientes',
      label: `Clientes (${(db.clientes || []).length})`
    }, {
      id: 'fornecedores',
      label: `Fornecedores (${(db.fornecedores || []).length})`
    }, {
      id: 'colaboradores',
      label: `Colaboradores (${(db.colaboradores || []).length})`
    }, {
      id: 'perfis',
      label: 'Funções e permissões'
    }, {
      id: 'log',
      label: 'Log de acesso'
    }]
  }), sub === 'clientes' && /*#__PURE__*/React.createElement(Clientes, {
    db: db,
    update: update
  }), sub === 'fornecedores' && /*#__PURE__*/React.createElement(Fornecedores, {
    db: db,
    update: update
  }), sub === 'colaboradores' && /*#__PURE__*/React.createElement(Colaboradores, {
    db: db,
    update: update,
    usuario: usuario
  }), sub === 'perfis' && /*#__PURE__*/React.createElement(PerfisAcesso, {
    db: db,
    update: update,
    usuario: usuario
  }), sub === 'log' && /*#__PURE__*/React.createElement(LogColaboradores, {
    db: db
  }));
}
/* ==========================================================
   PERFIS DE ACESSO — por perfil base e por cargo/função
========================================================== */
function PerfisAcesso({
  db,
  update,
  usuario
}) {
  const [aba, setAba] = useState('funcoes');
  const [novaFuncao, setNovaFuncao] = useState('');
  const podeEditar = podeExecutar(usuario, 'admin');
  const podeEditarNiveis = podeEditar;
  const porCargo = db.perfisPorCargo || {};

  /* Os níveis vêm prontos no sistema, mas podem ser ajustados aqui.
     O que for alterado fica salvo em db.niveisCustom, sem tocar no padrão —
     assim é sempre possível restaurar. */
  const custom = db.niveisCustom || {};
  const nivelEfetivo = n => {
    const c = custom[n.id];
    return c ? {
      ...n,
      abas: c.abas || n.abas,
      acoes: c.acoes || n.acoes
    } : n;
  };
  const listaAcoes = n => {
    const v = nivelEfetivo(n).acoes;
    return Array.isArray(v) ? v : Object.keys(v || {}).filter(k => v[k]);
  };
  const temAcao = (n, id) => listaAcoes(n).includes(id);
  const temAbas = n => nivelEfetivo(n).abas || [];
  const alterado = n => !!custom[n.id];
  function gravarNivel(n, patch) {
    if (!podeEditarNiveis) {
      alert('Somente administradores podem alterar permissões.');
      return;
    }
    update(d => {
      const base = {
        abas: temAbas(n).slice(),
        acoes: listaAcoes(n).slice()
      };
      d.niveisCustom = {
        ...(d.niveisCustom || {}),
        [n.id]: {
          ...base,
          ...patch
        }
      };
      registrarLog(d, usuario, 'Alterou nível de acesso', n.nome);
      return d;
    });
  }
  function alternarAcao(n, acaoId) {
    const atual = listaAcoes(n);
    const nova = atual.includes(acaoId) ? atual.filter(x => x !== acaoId) : [...atual, acaoId];
    gravarNivel(n, {
      acoes: nova
    });
  }
  function alternarAba(n, abaId) {
    const atual = temAbas(n);
    const nova = atual.includes(abaId) ? atual.filter(x => x !== abaId) : [...atual, abaId];
    gravarNivel(n, {
      abas: nova
    });
  }
  function restaurarNivel(n) {
    if (!podeEditarNiveis) return;
    if (!confirm(`Restaurar "${n.nome}" para as permissões originais do sistema?`)) return;
    update(d => {
      const c = {
        ...(d.niveisCustom || {})
      };
      delete c[n.id];
      d.niveisCustom = c;
      registrarLog(d, usuario, 'Restaurou nível de acesso', n.nome);
      return d;
    });
  }
  const funcoes = Array.from(new Set([...db.colaboradores.flatMap(c => funcoesColaborador(c)), ...Object.values(porCargo).map(c => c.nome).filter(Boolean)])).sort((a, b) => a.localeCompare(b));
  function definirNivel(nome, nivel) {
    if (!podeEditar) {
      alert('Somente administradores podem alterar permissões.');
      return;
    }
    update(d => {
      const k = normaliza(nome);
      d.perfisPorCargo = {
        ...(d.perfisPorCargo || {}),
        [k]: {
          nome,
          nivel
        }
      };
      registrarLog(d, usuario, 'Definiu permissão de função', `${nome} → ${(nivelPorId(nivel) || {}).nome || 'padrão'}`);
      return d;
    });
  }
  function limpar(nome) {
    if (!podeEditar) return;
    update(d => {
      const c = {
        ...(d.perfisPorCargo || {})
      };
      delete c[normaliza(nome)];
      d.perfisPorCargo = c;
      registrarLog(d, usuario, 'Removeu permissão de função', nome);
      return d;
    });
  }
  function adicionar() {
    const v = novaFuncao.trim();
    if (!v) return;
    definirNivel(v, 'consulta'); // nova função entra só com consulta
    setNovaFuncao('');
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Funções e permissões")), !podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "Você pode consultar, mas apenas administradores alteram permissões.")), /*#__PURE__*/React.createElement(SubTabs, {
    active: aba,
    onChange: setAba,
    tabs: [{
      id: 'funcoes',
      label: 'Funções'
    }, {
      id: 'niveis',
      label: 'O que cada nível libera'
    }, {
      id: 'quem',
      label: 'Quem acessa o quê'
    }]
  }), aba === 'funcoes' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Escolha o que cada função pode acessar. Quem tem mais de uma função recebe a soma dos acessos."), podeEditar && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("input", {
    value: novaFuncao,
    onChange: e => setNovaFuncao(e.target.value),
    placeholder: "Nova função (ex: Encarregado de Costura)",
    style: {
      maxWidth: 320
    },
    onKeyDown: e => {
      if (e.key === 'Enter') adicionar();
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: adicionar,
    disabled: !novaFuncao.trim()
  }, "+ Adicionar função"))), funcoes.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhuma função cadastrada. Defina as funções dos colaboradores ou adicione uma acima."
  }) : /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Função"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Pessoas"), /*#__PURE__*/React.createElement("th", {
    style: {
      minWidth: 220
    }
  }, "Pode acessar"), /*#__PURE__*/React.createElement("th", null, "O que isso libera"), /*#__PURE__*/React.createElement("th", {
    className: "no-print"
  }))), /*#__PURE__*/React.createElement("tbody", null, funcoes.map(nome => {
    const cfg = porCargo[normaliza(nome)];
    const nv = cfg ? nivelPorId(cfg.nivel) : null;
    const quem = db.colaboradores.filter(c => funcoesColaborador(c).some(f => normaliza(f) === normaliza(nome)));
    return /*#__PURE__*/React.createElement("tr", {
      key: nome
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, quem.map(c => c.nome).join(', ') || 'ninguém ainda')), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, quem.length), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("select", {
      value: cfg ? cfg.nivel : '',
      disabled: !podeEditar,
      onChange: e => e.target.value ? definirNivel(nome, e.target.value) : limpar(nome)
    }, /*#__PURE__*/React.createElement("option", {
      value: ""
    }, "— não definido (usa o perfil) —"), NIVEIS_ACESSO.map(n => /*#__PURE__*/React.createElement("option", {
      key: n.id,
      value: n.id
    }, n.nome)))), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, nv ? nv.desc : 'Segue o perfil de acesso cadastrado no colaborador.', nv && nv.abas.length > 0 && /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 4
      }
    }, nv.abas.map(id => /*#__PURE__*/React.createElement("span", {
      key: id,
      className: "tag",
      style: {
        marginRight: 4
      }
    }, (ABAS_SISTEMA.find(t => t.id === id) || {}).label)))), /*#__PURE__*/React.createElement("td", {
      className: "no-print"
    }, nv && /*#__PURE__*/React.createElement(Badge, {
      tone: nv.cor
    }, nv.nome)));
  }))))), aba === 'niveis' && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 12
    }
  }, "Estes são os níveis disponíveis. Cada função recebe um deles.", podeEditarNiveis ? ' Clique em uma aba ou permissão para conceder ou remover.' : ' Somente administradores podem alterá-los.'), NIVEIS_ACESSO.map(n => {
    const usados = funcoes.filter(f => {
      const c = porCargo[normaliza(f)];
      return c && c.nivel === n.id;
    });
    return /*#__PURE__*/React.createElement("div", {
      key: n.id,
      className: "panel"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        flexWrap: 'wrap',
        marginBottom: 8
      }
    }, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0
      }
    }, n.nome), /*#__PURE__*/React.createElement(Badge, {
      tone: n.cor
    }, n.perfil), usados.length > 0 && /*#__PURE__*/React.createElement("span", {
      className: "small muted"
    }, "usado por: ", usados.join(', '))), /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        marginBottom: 8
      }
    }, n.desc), alterado(n) && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--warn)',
        fontWeight: 600,
        marginBottom: 6
      }
    }, "Ajustado neste sistema ·", ' ', /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "link-btn",
      onClick: () => restaurarNivel(n)
    }, "restaurar o padrão")), /*#__PURE__*/React.createElement("div", {
      className: "grid2"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginBottom: 4
      }
    }, /*#__PURE__*/React.createElement("strong", null, "Abas visíveis")), ABAS_SISTEMA.map(t2 => {
      const ligada = (temAbas(n) || []).includes(t2.id);
      return /*#__PURE__*/React.createElement("button", {
        key: t2.id,
        type: "button",
        className: "perm-toggle " + (ligada ? 'on' : 'off'),
        disabled: !podeEditarNiveis,
        title: podeEditarNiveis ? ligada ? 'clique para ocultar esta aba' : 'clique para liberar esta aba' : '',
        onClick: () => alternarAba(n, t2.id)
      }, /*#__PURE__*/React.createElement("span", {
        className: "mk"
      }, ligada ? '✓' : '✗'), " ", t2.label);
    })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "small muted",
      style: {
        marginBottom: 4
      }
    }, /*#__PURE__*/React.createElement("strong", null, "Permissões")), AREAS_PERMISSAO.map(g => {
      const ligadas = g.itens.filter(i => temAcao(n, i.id)).length;
      return /*#__PURE__*/React.createElement("div", {
        key: g.grupo,
        className: "perm-grupo"
      }, /*#__PURE__*/React.createElement("div", {
        className: "perm-grupo-topo"
      }, /*#__PURE__*/React.createElement("span", {
        className: "perm-grupo-nome"
      }, g.grupo), /*#__PURE__*/React.createElement("span", {
        className: "small muted"
      }, ligadas, "/", g.itens.length), podeEditarNiveis && /*#__PURE__*/React.createElement("button", {
        type: "button",
        className: "btn ghost sm",
        onClick: () => {
          const todas = ligadas === g.itens.length;
          g.itens.forEach(i => {
            if (temAcao(n, i.id) === todas) alternarAcao(n, i.id);
          });
        }
      }, ligadas === g.itens.length ? 'nenhuma' : 'todas')), g.itens.map(i => {
        const ligada = temAcao(n, i.id);
        return /*#__PURE__*/React.createElement("button", {
          key: i.id,
          type: "button",
          className: "perm-toggle " + (ligada ? 'on' : 'off'),
          disabled: !podeEditarNiveis,
          title: podeEditarNiveis ? ligada ? 'clique para remover' : 'clique para conceder' : '',
          onClick: () => alternarAcao(n, i.id)
        }, /*#__PURE__*/React.createElement("span", {
          className: "mk"
        }, ligada ? '✓' : '✗'), " ", i.nome);
      }));
    }))));
  })), aba === 'quem' && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 18px 0 18px'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Acesso efetivo de cada colaborador"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Resultado da soma: perfil do colaborador + nível das funções dele.")), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Colaborador"), /*#__PURE__*/React.createElement("th", null, "Funções"), /*#__PURE__*/React.createElement("th", null, "Nível pela função"), /*#__PURE__*/React.createElement("th", null, "Abas liberadas"), /*#__PURE__*/React.createElement("th", null, "Permissões"), /*#__PURE__*/React.createElement("th", null, "Acesso"))), /*#__PURE__*/React.createElement("tbody", null, db.colaboradores.map(c => {
    const perm = resolverPermissoes(c, db);
    const niveis = funcoesColaborador(c).map(f => {
      const cfg = porCargo[normaliza(f)];
      return cfg ? nivelPorId(cfg.nivel) : null;
    }).filter(Boolean);
    return /*#__PURE__*/React.createElement("tr", {
      key: c.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, c.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, c.perfil || 'Colaborador')), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, funcoesColaborador(c).join(', ') || '—'), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, niveis.length === 0 ? /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, "—") : niveis.map((n, i) => /*#__PURE__*/React.createElement("div", {
      key: i
    }, /*#__PURE__*/React.createElement(Badge, {
      tone: n.cor
    }, n.nome)))), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, perm.abas.map(id => (ABAS_SISTEMA.find(t => t.id === id) || {}).label).filter(Boolean).join(', ') || 'nenhuma'), /*#__PURE__*/React.createElement("td", {
      className: "small muted"
    }, ACOES_SISTEMA.filter(a => perm.acoes[a.id]).map(a => a.label).join(', ') || 'nenhuma'), /*#__PURE__*/React.createElement("td", null, temSenhaDefinida(c) ? c.status === 'Inativo' ? /*#__PURE__*/React.createElement(Badge, {
      tone: "idle"
    }, "inativo") : /*#__PURE__*/React.createElement(Badge, {
      tone: "ok"
    }, "liberado") : /*#__PURE__*/React.createElement(Badge, {
      tone: "bad"
    }, "sem senha")));
  })))));
}
/* ---- Log de acesso e alterações ---- */
function LogColaboradores({
  db
}) {
  const [ini, setIni] = useState('');
  const [fim, setFim] = useState('');
  const [quem, setQuem] = useState('');
  const logs = (db.logs || []).filter(l => {
    if (ini && l.data < ini) return false;
    if (fim && l.data > fim) return false;
    if (quem && l.quem !== quem) return false;
    return true;
  }).slice().reverse();
  const pessoas = Array.from(new Set((db.logs || []).map(l => l.quem))).sort();
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Filtros"), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Data início"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: ini,
    onChange: e => setIni(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Data fim"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: fim,
    onChange: e => setFim(e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Usuário"
  }, /*#__PURE__*/React.createElement("select", {
    value: quem,
    onChange: e => setQuem(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Todos"), pessoas.map(p => /*#__PURE__*/React.createElement("option", {
    key: p,
    value: p
  }, p))))), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setIni('');
      setFim('');
      setQuem('');
    }
  }, "Limpar")), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, logs.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20
    }
  }, /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum registro de log no período."
  })) : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Quando"), /*#__PURE__*/React.createElement("th", null, "Quem"), /*#__PURE__*/React.createElement("th", null, "Perfil"), /*#__PURE__*/React.createElement("th", null, "Ação"), /*#__PURE__*/React.createElement("th", null, "Detalhe"))), /*#__PURE__*/React.createElement("tbody", null, logs.map(l => /*#__PURE__*/React.createElement("tr", {
    key: l.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, fmtDataHora(l.quando)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, l.quem)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, l.perfil), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, l.acao), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, l.detalhe)))))));
}
/* ==========================================================
   OFICINAS DE TERCEIROS — envio de etapas ou da produção
   Usa o cadastro de fornecedores marcados como prestadores de serviço.
========================================================== */
const SERVICOS_TERCEIROS = ['Costura', 'Silk / Estamparia', 'Corte', 'Bordado', 'Sublimação', 'DTF', 'Lavanderia', 'Acabamento', 'Embalagem', 'Montagem', 'Outro'];
function ehFornecedorServico(f) {
  if (!f) return false;
  if (f.tipoFornecimento === 'Serviço' || f.tipoFornecimento === 'Ambos') return true;
  return Array.isArray(f.servicos) && f.servicos.length > 0;
}
function EnderecoFields({
  f,
  set
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "CEP"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.cep,
    onChange: e => set('cep', e.target.value),
    placeholder: "00000-000"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Endereço"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.endereco,
    onChange: e => set('endereco', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Número"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.numero,
    onChange: e => set('numero', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Complemento"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.complemento,
    onChange: e => set('complemento', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Bairro"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.bairro,
    onChange: e => set('bairro', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Cidade"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.cidade,
    onChange: e => set('cidade', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "UF"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.uf,
    onChange: e => set('uf', e.target.value.toUpperCase().slice(0, 2)),
    maxLength: "2"
  })));
}
const ENDERECO_VAZIO = {
  cep: '',
  endereco: '',
  numero: '',
  complemento: '',
  bairro: '',
  cidade: '',
  uf: ''
};
/* ==========================================================
   RH. COLABORADORES
========================================================== */
function Colaboradores({
  db,
  update,
  usuario
}) {
  const [modal, setModal] = useState(null);
  const [q, setQ] = useState('');
  const list = db.colaboradores.filter(c => {
    const s = q.toLowerCase();
    return !s || c.nome.toLowerCase().includes(s) || funcoesColaborador(c).some(fn => fn.toLowerCase().includes(s));
  });
  const CAMPOS_COLAB = [{
    key: 'nome',
    label: 'nome'
  }, {
    key: 'cpf',
    label: 'CPF'
  }, {
    key: 'rg',
    label: 'RG'
  }];
  function save(c) {
    if (!exigirPermissao(usuario, 'pessoas', update, 'criar ou alterar colaboradores')) return;
    const erro = checarDuplicidade(db.colaboradores, c, CAMPOS_COLAB);
    if (erro) {
      alert(erro);
      return;
    }
    // só administrador concede perfil de administrador
    if (c.perfil === 'Administrador' && !podeExecutar(usuario, 'admin')) {
      alert('Apenas um administrador pode conceder o perfil de Administrador.');
      return;
    }
    update(d => {
      if (c.id) {
        const antes = d.colaboradores.find(x => x.id === c.id) || {};
        d.colaboradores = d.colaboradores.map(x => x.id === c.id ? c : x);
        const mudancas = [];
        if (antes.perfil !== c.perfil) mudancas.push(`perfil: ${antes.perfil || '—'} → ${c.perfil}`);
        if ((antes.senha || '') !== (c.senha || '')) mudancas.push(c.senha ? 'senha definida/alterada' : 'senha removida (acesso bloqueado)');
        if (antes.status !== c.status) mudancas.push(`status: ${antes.status || '—'} → ${c.status}`);
        registrarLog(d, usuario, 'Editou colaborador', `${c.nome}${mudancas.length ? ' — ' + mudancas.join('; ') : ''}`);
      } else {
        d.colaboradores.push({
          ...c,
          id: uid()
        });
        registrarLog(d, usuario, 'Cadastrou colaborador', `${c.nome} · perfil ${c.perfil}${c.senha ? '' : ' (sem senha — acesso bloqueado)'}`);
      }
      return d;
    });
    setModal(null);
  }
  function remove(id) {
    if (!exigirPermissao(usuario, 'pessoas', update, 'excluir colaboradores')) return;
    const c = db.colaboradores.find(x => x.id === id);
    if (c && c.id === (usuario && usuario.id)) {
      alert('Você não pode excluir o próprio usuário.');
      return;
    }
    const admins = db.colaboradores.filter(x => x.perfil === 'Administrador' && x.status !== 'Inativo');
    if (c && c.perfil === 'Administrador' && admins.length <= 1) {
      alert('Este é o único administrador ativo. Cadastre outro antes de excluí-lo.');
      return;
    }
    if (!confirm('Excluir este colaborador? O histórico de produção dele será mantido.')) return;
    update(d => {
      d.colaboradores = d.colaboradores.filter(x => x.id !== id);
      registrarLog(d, usuario, 'Excluiu colaborador', c ? c.nome : '');
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Colaboradores"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({})
  }, "+ Novo colaborador")), /*#__PURE__*/React.createElement("div", {
    className: "searchbar"
  }, /*#__PURE__*/React.createElement("input", {
    placeholder: "Buscar por nome ou função…",
    value: q,
    onChange: e => setQ(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20
    }
  }, /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum colaborador cadastrado."
  })) : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Nome"), /*#__PURE__*/React.createElement("th", null, "Funções"), /*#__PURE__*/React.createElement("th", null, "Admissão"), /*#__PURE__*/React.createElement("th", {
    className: "num"
  }, "Salário"), /*#__PURE__*/React.createElement("th", null, "Perfil"), /*#__PURE__*/React.createElement("th", null, "Acesso"), /*#__PURE__*/React.createElement("th", null, "Status"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, list.map(c => {
    const temSenha = temSenhaDefinida(c);
    return /*#__PURE__*/React.createElement("tr", {
      key: c.id
    }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, c.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, c.celular || c.telefone)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, funcoesColaborador(c).join(', ') || '—'), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, fmtDate(c.dataAdmissao)), /*#__PURE__*/React.createElement("td", {
      className: "num"
    }, money(c.salario)), /*#__PURE__*/React.createElement("td", {
      className: "small"
    }, c.perfil || 'Colaborador'), /*#__PURE__*/React.createElement("td", null, temSenha ? /*#__PURE__*/React.createElement(Badge, {
      tone: "ok"
    }, "Liberado") : /*#__PURE__*/React.createElement(Badge, {
      tone: "bad"
    }, "Bloqueado")), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement(Badge, {
      tone: c.status === 'Inativo' ? 'idle' : 'ok'
    }, c.status || 'Ativo')), /*#__PURE__*/React.createElement("td", {
      className: "row-actions"
    }, /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => setModal(c)
    }, "Editar"), /*#__PURE__*/React.createElement("button", {
      className: "btn danger sm",
      onClick: () => remove(c.id)
    }, "Excluir")));
  })))), modal !== null && /*#__PURE__*/React.createElement(ColaboradorModal, {
    col: modal,
    db: db,
    onClose: () => setModal(null),
    onSave: save
  }));
}
function ColaboradorModal({
  col,
  db,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    nome: '',
    cpf: '',
    rg: '',
    dataNascimento: '',
    telefone: '',
    celular: '',
    email: '',
    cargo: '',
    funcoes: [],
    departamentoId: '',
    dataAdmissao: todayISO(),
    salario: 0,
    status: 'Ativo',
    perfil: 'Colaborador',
    ...ENDERECO_VAZIO,
    observacoes: '',
    /* máquinas que sabe operar; vazio = ainda não informado */
    equipamentos: [],
    ...col
  });
  const set = (k, v) => setF(prev => ({
    ...prev,
    [k]: v
  }));
  const [novaFuncao, setNovaFuncao] = useState('');
  const listaFuncoes = funcoesColaborador(f);
  const sugestoesFuncao = Array.from(new Set(['Vendedor', 'Costureira', 'Costureiro', 'Cortador', 'Enfestador', 'Gravador', 'Revisor', 'Embalador', 'Auxiliar', 'Supervisor', 'Mecânico', ...db.colaboradores.flatMap(c => funcoesColaborador(c))])).sort();
  function addFuncao() {
    const v = novaFuncao.trim();
    if (!v) return;
    if (listaFuncoes.some(x => normaliza(x) === normaliza(v))) {
      setNovaFuncao('');
      return;
    }
    setF(prev => ({
      ...prev,
      funcoes: [...funcoesColaborador(prev), v],
      cargo: ''
    }));
    setNovaFuncao('');
  }
  function rmFuncao(nome) {
    setF(prev => ({
      ...prev,
      funcoes: funcoesColaborador(prev).filter(x => x !== nome),
      cargo: ''
    }));
  }
  return /*#__PURE__*/React.createElement(Modal, {
    title: col.id ? 'Editar colaborador' : 'Novo colaborador',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome completo"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "CPF"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.cpf,
    onChange: e => set('cpf', e.target.value),
    placeholder: "000.000.000-00"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "RG"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.rg,
    onChange: e => set('rg', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Data de nascimento"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.dataNascimento,
    onChange: e => set('dataNascimento', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Telefone"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.telefone,
    onChange: e => set('telefone', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Celular / WhatsApp"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.celular,
    onChange: e => set('celular', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "E-mail"
  }, /*#__PURE__*/React.createElement("input", {
    type: "email",
    value: f.email,
    onChange: e => set('email', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "field"
  }, /*#__PURE__*/React.createElement("label", null, "Funções (pode ter mais de uma)"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("input", {
    value: novaFuncao,
    onChange: e => setNovaFuncao(e.target.value),
    list: "funcoes-sugeridas",
    placeholder: "Costureira, Cortador, Vendedor…",
    onKeyDown: e => {
      if (e.key === 'Enter') {
        e.preventDefault();
        addFuncao();
      }
    }
  }), /*#__PURE__*/React.createElement("datalist", {
    id: "funcoes-sugeridas"
  }, sugestoesFuncao.map(x => /*#__PURE__*/React.createElement("option", {
    key: x,
    value: x
  }))), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn accent sm",
    onClick: addFuncao,
    disabled: !novaFuncao.trim()
  }, "+")), listaFuncoes.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 5,
      marginTop: 6
    }
  }, listaFuncoes.map(fn => /*#__PURE__*/React.createElement("span", {
    key: fn,
    className: "chip-col"
  }, fn, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => rmFuncao(fn)
  }, "×")))), listaFuncoes.length === 0 && /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 4
    }
  }, "Nenhuma função definida.")), /*#__PURE__*/React.createElement(Field, {
    label: "Data de admissão"
  }, /*#__PURE__*/React.createElement("input", {
    type: "date",
    value: f.dataAdmissao,
    onChange: e => set('dataAdmissao', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Salário mensal (R$)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    value: f.salario,
    onChange: e => set('salario', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Departamento"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.departamentoId || '',
    onChange: e => set('departamentoId', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "— sem departamento —"), (db.departamentos || []).filter(d => d.ativo !== false).map(d => /*#__PURE__*/React.createElement("option", {
    key: d.id,
    value: d.id
  }, d.nome))))), /*#__PURE__*/React.createElement("h3", null, "Vale-transporte"), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("label", {
    className: "small linha-check",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: !!f.usaConducao,
    onChange: e => set('usaConducao', e.target.checked)
  }), "Usa condução para trabalhar"), f.usaConducao && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Conduções por dia"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "0",
    step: "1",
    value: f.conducoesDia || '',
    onChange: e => set('conducoesDia', e.target.value),
    placeholder: "2"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Valor de cada condução (R$)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    value: f.valorConducao || '',
    onChange: e => set('valorConducao', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Dias no mês"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    min: "1",
    max: "31",
    value: f.diasConducao || '',
    onChange: e => set('diasConducao', e.target.value),
    placeholder: "22"
  }))), (() => {
    const c = custoConducao(f, db);
    if (!(c.mensal > 0)) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "painel-meta"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Por dia"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(c.diario))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "No mês"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        color: 'var(--thread-dark)'
      }
    }, money(c.mensal))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Desconto legal (6%)"), /*#__PURE__*/React.createElement("div", {
      className: "v",
      style: {
        fontSize: 15
      }
    }, money(c.descontoLegal))), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      className: "k"
    }, "Custo da empresa"), /*#__PURE__*/React.createElement("div", {
      className: "v"
    }, money(c.custoEmpresa))));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "A lei permite descontar até 6% do salário; o que passar disso é custo da empresa. Este valor entra no custo do minuto do departamento."))), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Status"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.status,
    onChange: e => set('status', e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: "Ativo"
  }, "Ativo"), /*#__PURE__*/React.createElement("option", {
    value: "Inativo"
  }, "Inativo")))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff'
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Acesso ao sistema"), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Perfil de acesso"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.perfil,
    onChange: e => set('perfil', e.target.value)
  }, PERFIS.map(p => /*#__PURE__*/React.createElement("option", {
    key: p,
    value: p
  }, p)))), /*#__PURE__*/React.createElement(Field, {
    label: "Senha"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      flex: 1
    }
  }, temSenhaDefinida(f) ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ok)',
      fontWeight: 600
    }
  }, "definida"), f.senhaDefinidaEm && /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, " em ", f.senhaDefinidaEm)) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--bad)',
      fontWeight: 600
    }
  }, "não definida — acesso bloqueado")), temSenhaDefinida(f) && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn ghost sm",
    onClick: () => {
      if (!confirm('Redefinir a senha deste colaborador? Ele terá de criar uma nova no próximo acesso.')) return;
      setF(prev => ({
        ...prev,
        senhaHash: undefined,
        senhaSalt: undefined,
        senha: undefined,
        precisaTrocarSenha: true,
        tentativasFalhas: 0,
        bloqueadoAte: null
      }));
    }
  }, "Redefinir")))), (() => {
    const sug = perfilDoCargo(f, db);
    if (!sug || sug === f.perfil) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        background: 'var(--warn-bg)',
        color: 'var(--warn)',
        padding: '8px 10px',
        borderRadius: 6,
        marginBottom: 8,
        fontWeight: 600
      }
    }, "O cargo cadastrado corresponde ao perfil ", /*#__PURE__*/React.createElement("strong", null, sug), ".", ' ', /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "link-btn",
      onClick: () => set('perfil', sug)
    }, "aplicar"));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, f.perfil === 'Administrador' && 'Administrador: acesso total ao sistema.', f.perfil === 'Gestor' && 'Gestor: acesso restrito — não vê cadastros de colaboradores/clientes nem dados financeiros.', f.perfil === 'Colaborador' && 'Colaborador: acesso somente ao módulo de Produção.', !temSenhaDefinida(f) && ' · O colaborador define a própria senha no primeiro acesso; ela nunca é digitada aqui.')), (() => {
    /* O que a pessoa sabe operar. Sem isso o planejamento distribui
       tarefa para quem não consegue fazer — e a data não bate por um
       motivo que ninguém identifica. */
    const doSetor = (db.equipamentos || []).filter(e =>
      e.ativo !== false && e.departamentoId === f.departamentoId);
    const lista = Array.isArray(f.equipamentos) ? f.equipamentos : [];
    if (!f.departamentoId)
      return /*#__PURE__*/React.createElement(Field, { label: "Máquinas que opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Escolha o setor acima para ver as máquinas disponíveis."));
    if (doSetor.length === 0)
      return /*#__PURE__*/React.createElement(Field, { label: "Máquinas que opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Nenhuma máquina cadastrada neste setor."));
    return /*#__PURE__*/React.createElement("div", null,
      /*#__PURE__*/React.createElement("div", { className: "etp-rot",
        style: { marginBottom: 6 } },
        `Máquinas que opera (${lista.length} de ${doSetor.length})`),
      /*#__PURE__*/React.createElement("div", { className: "gente-grade" },
        doSetor.map(e => /*#__PURE__*/React.createElement("label", {
          key: e.id,
          className: "gente-check" + (lista.includes(e.id) ? " on" : "")
        },
          /*#__PURE__*/React.createElement("input", {
            type: "checkbox", checked: lista.includes(e.id),
            onChange: () => set('equipamentos', lista.includes(e.id)
              ? lista.filter(x => x !== e.id) : [...lista, e.id])
          }),
          /*#__PURE__*/React.createElement("span", null,
            /*#__PURE__*/React.createElement("strong", null, e.nome),
            /*#__PURE__*/React.createElement("span", { className: "small muted" },
              e.codigo || e.tipo || ''))))),
      /*#__PURE__*/React.createElement("div", { className: "small muted",
        style: { marginTop: 6, marginBottom: 10 } },
        lista.length === 0
          ? "Nada marcado significa \"ainda não informado\" — o planejamento " +
            "considera que a pessoa pode operar qualquer máquina do setor. " +
            "Marque o que ela realmente faz para o cálculo ficar honesto."
          : "O planejamento só dará a esta pessoa tarefas nas máquinas " +
            "marcadas. Quem opera mais de uma é mais fácil de encaixar."),
      /*#__PURE__*/React.createElement("div", { className: "row-actions",
        style: { marginBottom: 12 } },
        /*#__PURE__*/React.createElement("button", {
          type: "button", className: "btn ghost sm",
          onClick: () => set('equipamentos', doSetor.map(e => e.id))
        }, "Marcar todas"),
        lista.length > 0 && /*#__PURE__*/React.createElement("button", {
          type: "button", className: "btn ghost sm",
          onClick: () => set('equipamentos', [])
        }, "Limpar")));
  })(), /*#__PURE__*/React.createElement(EnderecoFields, {
    f: f,
    set: set
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacoes,
    onChange: e => set('observacoes', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome
  }, "Salvar")));
}
/* ==========================================================
   CL. CLIENTES
========================================================== */
function Clientes({
  db,
  update
}) {
  const [modal, setModal] = useState(null);
  const [q, setQ] = useState('');
  const list = db.clientes.filter(c => {
    const s = q.toLowerCase();
    return !s || (c.nome || '').toLowerCase().includes(s) || (c.nomeFantasia || '').toLowerCase().includes(s) || (c.documento || '').includes(s);
  });
  const CAMPOS_CLI = [{
    key: 'nome',
    label: 'nome / razão social'
  }, {
    key: 'documento',
    label: 'CPF/CNPJ'
  }, {
    key: 'ie',
    label: 'inscrição estadual'
  }];
  function save(c) {
    const erro = checarDuplicidade(db.clientes, c, CAMPOS_CLI);
    if (erro) {
      alert(erro);
      return;
    }
    update(d => {
      if (c.id) {
        d.clientes = d.clientes.map(x => x.id === c.id ? c : x);
      } else {
        d.clientes.push({
          ...c,
          id: uid()
        });
      }
      return d;
    });
    setModal(null);
  }
  function remove(id) {
    if (!confirm('Excluir este cliente?')) return;
    update(d => {
      d.clientes = d.clientes.filter(c => c.id !== id);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Clientes"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({})
  }, "+ Novo cliente")), /*#__PURE__*/React.createElement("div", {
    className: "searchbar"
  }, /*#__PURE__*/React.createElement("input", {
    placeholder: "Buscar por nome, fantasia ou CPF/CNPJ…",
    value: q,
    onChange: e => setQ(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20
    }
  }, /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum cliente cadastrado."
  })) : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Nome / Razão Social"), /*#__PURE__*/React.createElement("th", null, "Fantasia"), /*#__PURE__*/React.createElement("th", null, "CPF/CNPJ"), /*#__PURE__*/React.createElement("th", null, "Contato"), /*#__PURE__*/React.createElement("th", null, "Cidade/UF"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, list.map(c => /*#__PURE__*/React.createElement("tr", {
    key: c.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, c.nome), " ", /*#__PURE__*/React.createElement("span", {
    className: "small muted"
  }, c.tipo === 'PJ' ? 'PJ' : 'PF')), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, c.nomeFantasia), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, c.documento), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, c.celular || c.telefone), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, c.cidade, c.uf ? `/${c.uf}` : ''), /*#__PURE__*/React.createElement("td", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setModal(c)
  }, "Editar"), /*#__PURE__*/React.createElement("button", {
    className: "btn danger sm",
    onClick: () => remove(c.id)
  }, "Excluir"))))))), modal !== null && /*#__PURE__*/React.createElement(ClienteModal, {
    cli: modal,
    onClose: () => setModal(null),
    onSave: save
  }));
}
function ClienteModal({
  cli,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    tipo: 'PJ',
    nome: '',
    nomeFantasia: '',
    documento: '',
    ie: '',
    indicadorIE: 'Contribuinte',
    telefone: '',
    celular: '',
    responsavel: '',
    email: '',
    ...ENDERECO_VAZIO,
    observacoes: '',
    ...cli
  });
  const set = (k, v) => setF(prev => ({
    ...prev,
    [k]: v
  }));
  return /*#__PURE__*/React.createElement(Modal, {
    title: cli.id ? 'Editar cliente' : 'Novo cliente',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "field"
  }, /*#__PURE__*/React.createElement("label", null, "Tipo"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn sm " + (f.tipo === 'PJ' ? 'accent' : 'ghost'),
    onClick: () => set('tipo', 'PJ')
  }, "Pessoa Jurídica"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn sm " + (f.tipo === 'PF' ? 'accent' : 'ghost'),
    onClick: () => set('tipo', 'PF')
  }, "Pessoa Física"))), /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: f.tipo === 'PJ' ? 'Razão Social' : 'Nome completo'
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value)
  })), f.tipo === 'PJ' && /*#__PURE__*/React.createElement(Field, {
    label: "Nome Fantasia"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nomeFantasia,
    onChange: e => set('nomeFantasia', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: f.tipo === 'PJ' ? 'CNPJ' : 'CPF'
  }, /*#__PURE__*/React.createElement("input", {
    value: f.documento,
    onChange: e => set('documento', e.target.value)
  })), f.tipo === 'PJ' && /*#__PURE__*/React.createElement(Field, {
    label: "Inscrição Estadual"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.ie,
    onChange: e => set('ie', e.target.value)
  })), f.tipo === 'PJ' && /*#__PURE__*/React.createElement(Field, {
    label: "Indicador da IE"
  }, /*#__PURE__*/React.createElement("select", {
    value: f.indicadorIE,
    onChange: e => set('indicadorIE', e.target.value)
  }, /*#__PURE__*/React.createElement("option", null, "Contribuinte"), /*#__PURE__*/React.createElement("option", null, "Isento"), /*#__PURE__*/React.createElement("option", null, "Não contribuinte"))), /*#__PURE__*/React.createElement(Field, {
    label: "Telefone"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.telefone,
    onChange: e => set('telefone', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Celular / WhatsApp"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.celular,
    onChange: e => set('celular', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome do responsável"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.responsavel,
    onChange: e => set('responsavel', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "E-mail"
  }, /*#__PURE__*/React.createElement("input", {
    type: "email",
    value: f.email,
    onChange: e => set('email', e.target.value)
  }))), (() => {
    /* O que a pessoa sabe operar. Sem isso o planejamento distribui
       tarefa para quem não consegue fazer — e a data não bate por um
       motivo que ninguém identifica. */
    const doSetor = (db.equipamentos || []).filter(e =>
      e.ativo !== false && e.departamentoId === f.departamentoId);
    const lista = Array.isArray(f.equipamentos) ? f.equipamentos : [];
    if (!f.departamentoId)
      return /*#__PURE__*/React.createElement(Field, { label: "Máquinas que opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Escolha o setor acima para ver as máquinas disponíveis."));
    if (doSetor.length === 0)
      return /*#__PURE__*/React.createElement(Field, { label: "Máquinas que opera" },
        /*#__PURE__*/React.createElement("div", { className: "small muted" },
          "Nenhuma máquina cadastrada neste setor."));
    return /*#__PURE__*/React.createElement("div", null,
      /*#__PURE__*/React.createElement("div", { className: "etp-rot",
        style: { marginBottom: 6 } },
        `Máquinas que opera (${lista.length} de ${doSetor.length})`),
      /*#__PURE__*/React.createElement("div", { className: "gente-grade" },
        doSetor.map(e => /*#__PURE__*/React.createElement("label", {
          key: e.id,
          className: "gente-check" + (lista.includes(e.id) ? " on" : "")
        },
          /*#__PURE__*/React.createElement("input", {
            type: "checkbox", checked: lista.includes(e.id),
            onChange: () => set('equipamentos', lista.includes(e.id)
              ? lista.filter(x => x !== e.id) : [...lista, e.id])
          }),
          /*#__PURE__*/React.createElement("span", null,
            /*#__PURE__*/React.createElement("strong", null, e.nome),
            /*#__PURE__*/React.createElement("span", { className: "small muted" },
              e.codigo || e.tipo || ''))))),
      /*#__PURE__*/React.createElement("div", { className: "small muted",
        style: { marginTop: 6, marginBottom: 10 } },
        lista.length === 0
          ? "Nada marcado significa \"ainda não informado\" — o planejamento " +
            "considera que a pessoa pode operar qualquer máquina do setor. " +
            "Marque o que ela realmente faz para o cálculo ficar honesto."
          : "O planejamento só dará a esta pessoa tarefas nas máquinas " +
            "marcadas. Quem opera mais de uma é mais fácil de encaixar."),
      /*#__PURE__*/React.createElement("div", { className: "row-actions",
        style: { marginBottom: 12 } },
        /*#__PURE__*/React.createElement("button", {
          type: "button", className: "btn ghost sm",
          onClick: () => set('equipamentos', doSetor.map(e => e.id))
        }, "Marcar todas"),
        lista.length > 0 && /*#__PURE__*/React.createElement("button", {
          type: "button", className: "btn ghost sm",
          onClick: () => set('equipamentos', [])
        }, "Limpar")));
  })(), /*#__PURE__*/React.createElement(EnderecoFields, {
    f: f,
    set: set
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacoes,
    onChange: e => set('observacoes', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome
  }, "Salvar")));
}
/* ==========================================================
   FO. FORNECEDORES
========================================================== */
function Fornecedores({
  db,
  update
}) {
  const [modal, setModal] = useState(null);
  const [q, setQ] = useState('');
  const list = db.fornecedores.filter(f => {
    const s = q.toLowerCase();
    return !s || (f.nome || '').toLowerCase().includes(s) || (f.nomeFantasia || '').toLowerCase().includes(s) || (f.categoria || '').toLowerCase().includes(s);
  });
  const CAMPOS_FORN = [{
    key: 'nome',
    label: 'nome / razão social'
  }, {
    key: 'documento',
    label: 'CNPJ/CPF'
  }, {
    key: 'ie',
    label: 'inscrição estadual'
  }];
  function save(f) {
    const erro = checarDuplicidade(db.fornecedores, f, CAMPOS_FORN);
    if (erro) {
      alert(erro);
      return;
    }
    update(d => {
      if (f.id) {
        d.fornecedores = d.fornecedores.map(x => x.id === f.id ? f : x);
      } else {
        d.fornecedores.push({
          ...f,
          id: uid()
        });
      }
      return d;
    });
    setModal(null);
  }
  function remove(id) {
    if (!confirm('Excluir este fornecedor?')) return;
    update(d => {
      d.fornecedores = d.fornecedores.filter(f => f.id !== id);
      return d;
    });
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Fornecedores"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => setModal({})
  }, "+ Novo fornecedor")), /*#__PURE__*/React.createElement("div", {
    className: "searchbar"
  }, /*#__PURE__*/React.createElement("input", {
    placeholder: "Buscar por nome, fantasia ou categoria…",
    value: q,
    onChange: e => setQ(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      padding: 0
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20
    }
  }, /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum fornecedor cadastrado."
  })) : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Nome / Razão Social"), /*#__PURE__*/React.createElement("th", null, "Fantasia"), /*#__PURE__*/React.createElement("th", null, "CNPJ/CPF"), /*#__PURE__*/React.createElement("th", null, "Categoria"), /*#__PURE__*/React.createElement("th", null, "Contato"), /*#__PURE__*/React.createElement("th", null))), /*#__PURE__*/React.createElement("tbody", null, list.map(f => /*#__PURE__*/React.createElement("tr", {
    key: f.id
  }, /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("strong", null, f.nome)), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, f.nomeFantasia), /*#__PURE__*/React.createElement("td", {
    className: "small muted"
  }, f.documento), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, f.categoria), /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, f.celular || f.telefone), /*#__PURE__*/React.createElement("td", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setModal(f)
  }, "Editar"), /*#__PURE__*/React.createElement("button", {
    className: "btn danger sm",
    onClick: () => remove(f.id)
  }, "Excluir"))))))), modal !== null && /*#__PURE__*/React.createElement(FornecedorModal, {
    forn: modal,
    onClose: () => setModal(null),
    onSave: save
  }));
}
function FornecedorModal({
  forn,
  onClose,
  onSave
}) {
  const [f, setF] = useState({
    nome: '',
    nomeFantasia: '',
    documento: '',
    ie: '',
    telefone: '',
    celular: '',
    contato: '',
    email: '',
    categoria: '',
    condicaoPagamento: '',
    tipoFornecimento: 'Material',
    servicos: [],
    valorPecaPadrao: 0,
    ...ENDERECO_VAZIO,
    observacoes: '',
    ...forn
  });
  const set = (k, v) => setF(prev => ({
    ...prev,
    [k]: v
  }));
  const alternarServico = x => set('servicos', (f.servicos || []).includes(x) ? (f.servicos || []).filter(y => y !== x) : [...(f.servicos || []), x]);
  return /*#__PURE__*/React.createElement(Modal, {
    title: forn.id ? 'Editar fornecedor' : 'Novo fornecedor',
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "grid3"
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Razão Social / Nome"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nome,
    onChange: e => set('nome', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome Fantasia"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.nomeFantasia,
    onChange: e => set('nomeFantasia', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "CNPJ/CPF"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.documento,
    onChange: e => set('documento', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Inscrição Estadual"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.ie,
    onChange: e => set('ie', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Telefone"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.telefone,
    onChange: e => set('telefone', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Celular / WhatsApp"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.celular,
    onChange: e => set('celular', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Nome do contato"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.contato,
    onChange: e => set('contato', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "E-mail"
  }, /*#__PURE__*/React.createElement("input", {
    type: "email",
    value: f.email,
    onChange: e => set('email', e.target.value)
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Categoria / O que fornece"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.categoria,
    onChange: e => set('categoria', e.target.value),
    placeholder: "Tecidos, aviamentos…"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Condição de pagamento"
  }, /*#__PURE__*/React.createElement("input", {
    value: f.condicaoPagamento,
    onChange: e => set('condicaoPagamento', e.target.value),
    placeholder: "30/60 dias, à vista…"
  }))), /*#__PURE__*/React.createElement(EnderecoFields, {
    f: f,
    set: set
  }), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      background: '#fff',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("h3", null, "Tipo de fornecimento"), /*#__PURE__*/React.createElement("div", {
    className: "field"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6,
      flexWrap: 'wrap'
    }
  }, ['Material', 'Serviço', 'Ambos'].map(x => /*#__PURE__*/React.createElement("button", {
    key: x,
    type: "button",
    className: "btn sm " + (f.tipoFornecimento === x ? 'accent' : 'ghost'),
    onClick: () => set('tipoFornecimento', x)
  }, x))), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 6
    }
  }, "Fornecedores de ", /*#__PURE__*/React.createElement("strong", null, "Serviço"), " ou ", /*#__PURE__*/React.createElement("strong", null, "Ambos"), " aparecem como oficina em Produção → Oficinas, para receber etapas terceirizadas.")), (f.tipoFornecimento === 'Serviço' || f.tipoFornecimento === 'Ambos') && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "field"
  }, /*#__PURE__*/React.createElement("label", null, "Serviços que a oficina executa"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 5,
      flexWrap: 'wrap'
    }
  }, SERVICOS_TERCEIROS.map(x => /*#__PURE__*/React.createElement("button", {
    key: x,
    type: "button",
    className: "btn sm " + ((f.servicos || []).includes(x) ? 'accent' : 'ghost'),
    onClick: () => alternarServico(x)
  }, x)))), /*#__PURE__*/React.createElement(Field, {
    label: "Valor por peça de referência (R$)"
  }, /*#__PURE__*/React.createElement("input", {
    type: "number",
    step: "0.01",
    style: {
      maxWidth: 160
    },
    value: f.valorPecaPadrao,
    onChange: e => set('valorPecaPadrao', e.target.value),
    placeholder: "sugestão no envio"
  })))), /*#__PURE__*/React.createElement(Field, {
    label: "Observações"
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: "2",
    value: f.observacoes,
    onChange: e => set('observacoes', e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    onClick: () => onSave(f),
    disabled: !f.nome
  }, "Salvar")));
}

// janelas de trabalho do dia, em minutos desde 00:00
// máquinas disponíveis para uma etapa: as do departamento dela

// total já apontado numa etapa

// rótulo da OP: número do pedido + sufixo sequencial (a, b, c…) quando o pedido tem vários produtos

/* Funções do colaborador — aceita o formato antigo (campo `cargo` em texto)
   e o novo (`funcoes`, lista com uma ou mais funções). */
function funcoesColaborador(c) {
  if (!c) return [];
  if (Array.isArray(c.funcoes) && c.funcoes.length) return c.funcoes;
  if (c.cargo && String(c.cargo).trim()) return String(c.cargo).split(/[,;/]+/).map(x => x.trim()).filter(Boolean);
  return [];
}

/* ==========================================================
   PAINEL — visão dos cadastros mantidos na reestruturação
========================================================== */
function Painel({
  db,
  update,
  usuario
}) {
  const [limpando, setLimpando] = useState(null);
  const [limpezaFeita, setLimpezaFeita] = useState(null);
  const [avisoDownload, setAvisoDownload] = useState(false);
  const [copiado, setCopiado] = useState(false);
  const ativos = (db.colaboradores || []).filter(c => c.status !== 'Inativo');
  const oficinas = (db.fornecedores || []).filter(ehFornecedorServico);
  const semSenha = ativos.filter(c => !temSenhaDefinida(c));
  const ultimosLogs = (db.logs || []).slice(-8).reverse();
  function exportar() {
    try {
      const blob = new Blob([JSON.stringify(db, null, 2)], {
        type: 'application/json'
      });
      const url = URL.createObjectURL(blob);
      const a2 = document.createElement('a');
      a2.href = url;
      a2.download = `backup-${todayISO()}.json`;
      document.body.appendChild(a2);
      a2.click();
      document.body.removeChild(a2);
      URL.revokeObjectURL(url);
    } catch (e) {
      alert('Não foi possível exportar: ' + (e && e.message));
    }
  }
  /* Um atalho para a área de trabalho: o .url do Windows é um arquivo de
     texto simples que aponta para o endereço atual. Dois cliques nele e o
     sistema abre no navegador padrão, sem precisar procurar a pasta. */
  const enderecoAtual = window.location && window.location.href || '';
  function baixarAtalho() {
    try {
      const conteudo = '[InternetShortcut]\r\nURL=' + enderecoAtual + '\r\n' + 'IconIndex=0\r\n';
      const blob = new Blob([conteudo], {
        type: 'text/plain'
      });
      const url = URL.createObjectURL(blob);
      const a2 = document.createElement('a');
      a2.href = url;
      a2.download = 'Confeccao ERP.url';
      document.body.appendChild(a2);
      a2.click();
      document.body.removeChild(a2);
      URL.revokeObjectURL(url);
    } catch (e) {
      setAvisoDownload(true);
    }
  }
  function copiarEndereco() {
    try {
      const nav = typeof window !== 'undefined' && window.navigator || null;
      if (nav && nav.clipboard && nav.clipboard.writeText) {
        nav.clipboard.writeText(enderecoAtual);
        setCopiado(true);
        setTimeout(() => setCopiado(false), 2500);
      } else {
        setAvisoDownload(true);
      }
    } catch (e) {
      setAvisoDownload(true);
    }
  }
  function importar(e) {
    const file = e.target.files && e.target.files[0];
    e.target.value = '';
    if (!file) return;
    const r = new FileReader();
    r.onload = () => {
      try {
        const dados = JSON.parse(r.result);
        if (!dados || typeof dados !== 'object') throw new Error('Arquivo inválido.');
        if (!confirm('Substituir a base atual pelo arquivo? Esta ação não pode ser desfeita.')) return;
        update(() => garantirAdminPadrao({
          ...emptyDb(),
          ...dados
        }));
        alert('Base restaurada.');
      } catch (err) {
        alert('Não foi possível importar: ' + (err && err.message));
      }
    };
    r.readAsText(file);
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "page-head"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, "Visão geral"), /*#__PURE__*/React.createElement("h2", null, "Painel"))), (() => {
    const naCaixa = lerCaixaCanal().length;
    if (naCaixa === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--thread)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        fontWeight: 600
      }
    }, "💬 ", naCaixa, " registro(s) esperando na caixa do canal"), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, "Alguém usou o canal neste computador. Recolha em Conversa aberta → QR Codes para que entrem no sistema."));
  })(), (() => {
    /* manifestação crítica ou vencida não pode ficar esperando alguém
       lembrar de abrir a aba do canal */
    const minhas = manifestacoesVisiveis(db, usuario).filter(m => ABERTOS_CANAL.includes(m.status));
    const criticas = minhas.filter(m => m.prioridade === 'critica');
    const atrasadas = minhas.filter(m => prazoManifestacao(m).atrasada);
    if (criticas.length === 0 && atrasadas.length === 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      className: "panel",
      style: {
        borderColor: 'var(--danger)',
        background: 'rgba(176,58,46,.05)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--danger)',
        fontWeight: 700
      }
    }, "💬 Conversa aberta precisa de você"), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, criticas.length > 0 && `${criticas.length} manifestação(ões) crítica(s) em aberto. `, atrasadas.length > 0 && `${atrasadas.length} passaram do prazo de resposta.`));
  })(), limpezaFeita && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--ok)',
      background: 'var(--ok-bg)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 220
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, "Limpeza concluída"), limpezaFeita.map((x, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "small muted"
  }, "· ", x))), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => setLimpezaFeita(null)
  }, "Fechar"))), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Clientes"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (db.clientes || []).length)), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Fornecedores"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (db.fornecedores || []).length), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, oficinas.length, " prestador(es) de serviço")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Colaboradores ativos"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, ativos.length), /*#__PURE__*/React.createElement("div", {
    className: "sub"
  }, "de ", (db.colaboradores || []).length, " cadastrado(s)")), /*#__PURE__*/React.createElement("div", {
    className: "kpi"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lbl"
  }, "Registros de acesso"), /*#__PURE__*/React.createElement("div", {
    className: "val"
  }, (db.logs || []).length))), semSenha.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, semSenha.length, " colaborador(es) ainda sem senha definida: ", semSenha.map(c => c.nome).join(', '), ". A senha é criada por eles no primeiro acesso.")), /*#__PURE__*/React.createElement("div", {
    className: "grid2"
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Últimos acessos e alterações"), ultimosLogs.length === 0 ? /*#__PURE__*/React.createElement(Empty, {
    text: "Nenhum registro ainda."
  }) : /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("tbody", null, ultimosLogs.map(l => /*#__PURE__*/React.createElement("tr", {
    key: l.id
  }, /*#__PURE__*/React.createElement("td", {
    className: "small"
  }, /*#__PURE__*/React.createElement("strong", null, l.acao), l.detalhe && /*#__PURE__*/React.createElement("div", {
    className: "muted"
  }, l.detalhe)), /*#__PURE__*/React.createElement("td", {
    className: "small muted",
    style: {
      width: 130
    }
  }, l.quem, /*#__PURE__*/React.createElement("div", null, l.quando))))))), /*#__PURE__*/React.createElement("div", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h3", null, "Backup da base"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Os dados ficam no navegador desta máquina. Exporte antes de limpar o histórico ou de trocar de computador."), (() => {
    const st = rotuloArmazenamento();
    return /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        marginBottom: 10
      }
    }, /*#__PURE__*/React.createElement(Badge, {
      tone: st.tone
    }, st.texto), ' ', /*#__PURE__*/React.createElement("span", {
      className: "muted"
    }, Math.round(tamanhoBase(db) / 1024), " KB em uso"));
  })(), /*#__PURE__*/React.createElement("div", {
    className: "row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: exportar
  }, "↓ Exportar JSON"), /*#__PURE__*/React.createElement("label", {
    className: "btn ghost sm",
    style: {
      cursor: 'pointer'
    }
  }, "↑ Importar JSON", /*#__PURE__*/React.createElement("input", {
    type: "file",
    accept: "application/json",
    style: {
      display: 'none'
    },
    onChange: importar
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      paddingTop: 12,
      borderTop: '1px solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, "Atalho na área de trabalho"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      margin: '4px 0 8px'
    }
  }, "Para abrir o ERP com dois cliques, sem procurar a pasta nem digitar endereço. O atalho aponta sempre para este mesmo lugar, então os dados continuam onde estão."), /*#__PURE__*/React.createElement("div", {
    className: "endereco-atual small"
  }, enderecoAtual || '—'), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn accent sm",
    onClick: baixarAtalho
  }, "↓ Baixar atalho (Windows)"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: copiarEndereco
  }, copiado ? '✓ Endereço copiado' : 'Copiar endereço')), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginTop: 7
    }
  }, /*#__PURE__*/React.createElement("strong", null, "Windows:"), " baixe o atalho e arraste da pasta Downloads para a área de trabalho.", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("strong", null, "Mac:"), " arraste o ícone à esquerda da barra de endereço para a mesa.", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("strong", null, "Chrome ou Edge:"), " menu ⋮ → Salvar e compartilhar → Criar atalho, marcando “Abrir como janela” — vira um programa com ícone próprio."), avisoDownload && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--warn)',
      background: 'var(--warn-bg)',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--warn)',
      fontWeight: 600
    }
  }, "O navegador não deixou fazer isso a partir daqui."), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Copie o endereço mostrado acima na mão e cole num atalho novo, ou use o caminho do menu do navegador descrito acima."), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      marginTop: 7
    },
    onClick: () => setAvisoDownload(false)
  }, "Entendi"))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      paddingTop: 12,
      borderTop: '1px solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "small"
  }, "Limpar dados"), /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      margin: '4px 0 8px'
    }
  }, "Escolha o que apagar. Dá para zerar a produção e recomeçar mantendo materiais, cadastros e engenharia."), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      borderColor: 'var(--danger)',
      color: 'var(--danger)'
    },
    onClick: () => setLimpando([])
  }, "Escolher o que limpar")))), limpando && /*#__PURE__*/React.createElement(LimparDadosModal, {
    db: db,
    usuario: usuario,
    onClose: () => setLimpando(null),
    onLimpar: escolhidos => {
      let resumo = [];
      update(d => {
        const r = limparBlocos(d, escolhidos, usuario);
        resumo = r && r.resumo || [];
        return d;
      });
      setLimpando(null);
      /* o aviso vira uma faixa na tela: alert pode estar bloqueado */
      setLimpezaFeita(resumo.length ? resumo : ['nada havia para apagar']);
    }
  }));
}

/* ---------------- Escolher o que apagar ---------------- */
function LimparDadosModal({
  db,
  usuario,
  onClose,
  onLimpar
}) {
  const [marcados, setMarcados] = useState([]);
  /* a confirmação mora aqui dentro: o confirm() do navegador pode ser
     bloqueado depois de alguns diálogos, e aí o clique morria em silêncio */
  const [confirmando, setConfirmando] = useState(false);
  const efetivos = fecharDependencias(marcados);
  const alternar = id => {
    setConfirmando(false);
    if (marcados.includes(id)) {
      /* desmarcar solta também quem só entrou por dependência dele */
      const resto = marcados.filter(x => x !== id);
      setMarcados(resto.filter(x => fecharDependencias(resto).includes(x)));
    } else {
      setMarcados([...marcados, id]);
    }
  };
  const total = BLOCOS_LIMPEZA.filter(b => efetivos.includes(b.id)).reduce((a, b) => a + contarBloco(b, db), 0);
  return /*#__PURE__*/React.createElement(Modal, {
    title: "Limpar dados",
    onClose: onClose,
    wide: true
  }, /*#__PURE__*/React.createElement("div", {
    className: "small muted",
    style: {
      marginBottom: 10
    }
  }, "Marque os blocos a apagar. Alguns dependem de outros — marcar", /*#__PURE__*/React.createElement("strong", null, " Materiais"), " arrasta ", /*#__PURE__*/React.createElement("strong", null, "Produtos"), " junto, porque uma ficha técnica sem material aponta para o nada. Exporte um backup antes: não há como desfazer."), /*#__PURE__*/React.createElement("div", {
    className: "row-actions",
    style: {
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "small muted",
    style: {
      flex: 1,
      minWidth: 90
    }
  }, "Atalhos:"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setConfirmando(false);
      setMarcados(['producao']);
    }
  }, "Só as ordens"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setConfirmando(false);
      setMarcados(['produtos']);
    }
  }, "Ordens + produtos + modelagens"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setConfirmando(false);
      setMarcados(BLOCOS_LIMPEZA.map(b => b.id));
    }
  }, "Tudo"), marcados.length > 0 && /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: () => {
      setConfirmando(false);
      setMarcados([]);
    }
  }, "Limpar seleção")), /*#__PURE__*/React.createElement("div", {
    className: "blocos-limpeza"
  }, BLOCOS_LIMPEZA.map(b => {
    const n = contarBloco(b, db);
    const marcado = marcados.includes(b.id);
    const arrastado = !marcado && efetivos.includes(b.id);
    return /*#__PURE__*/React.createElement("button", {
      key: b.id,
      type: "button",
      disabled: n === 0 && !arrastado,
      className: "bloco-limpeza" + (marcado ? ' on' : '') + (arrastado ? ' arrastado' : ''),
      onClick: () => alternar(b.id)
    }, /*#__PURE__*/React.createElement("span", {
      className: "marca"
    }, marcado ? '✓' : arrastado ? '↳' : ''), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1
      }
    }, /*#__PURE__*/React.createElement("strong", null, b.nome), /*#__PURE__*/React.createElement("div", {
      className: "small muted"
    }, b.ajuda), arrastado && /*#__PURE__*/React.createElement("div", {
      className: "small",
      style: {
        color: 'var(--thread-dark)'
      }
    }, "vai junto por dependência")), /*#__PURE__*/React.createElement("span", {
      className: "qtd"
    }, n));
  })), efetivos.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "grid2",
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--danger)',
      background: 'rgba(176,58,46,.05)',
      margin: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 600,
      marginBottom: 5
    }
  }, "Vai sair — ", total, " registro(s)"), BLOCOS_LIMPEZA.filter(b => efetivos.includes(b.id)).map(b => /*#__PURE__*/React.createElement("div", {
    key: b.id,
    className: "small"
  }, b.nome, " ", /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "(", contarBloco(b, db), ")")))), /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      margin: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      fontWeight: 600,
      marginBottom: 5
    }
  }, "Vai ficar"), BLOCOS_LIMPEZA.filter(b => !efetivos.includes(b.id) && contarBloco(b, db) > 0).map(b => /*#__PURE__*/React.createElement("div", {
    key: b.id,
    className: "small"
  }, b.nome, " ", /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "(", contarBloco(b, db), ")"))), /*#__PURE__*/React.createElement("div", {
    className: "small muted"
  }, "Unidades, grupos, jornada e usuários"))), confirmando && /*#__PURE__*/React.createElement("div", {
    className: "panel",
    style: {
      borderColor: 'var(--danger)',
      background: 'rgba(176,58,46,.07)',
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "small",
    style: {
      color: 'var(--danger)',
      fontWeight: 700
    }
  }, "Última confirmação: ", total, " registro(s) serão apagados e não há como voltar.")), /*#__PURE__*/React.createElement("div", {
    className: "modal-actions"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: onClose
  }, "Cancelar"), !confirmando ? /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    disabled: efetivos.length === 0,
    style: efetivos.length ? {
      background: 'var(--danger)',
      borderColor: 'var(--danger)'
    } : undefined,
    onClick: () => setConfirmando(true)
  }, "Apagar o que está marcado") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setConfirmando(false)
  }, "Voltar"), /*#__PURE__*/React.createElement("button", {
    className: "btn accent",
    style: {
      background: 'var(--danger)',
      borderColor: 'var(--danger)'
    },
    onClick: () => onLimpar(efetivos)
  }, "Sim, apagar ", total, " registro(s)"))));
}

export default App;
